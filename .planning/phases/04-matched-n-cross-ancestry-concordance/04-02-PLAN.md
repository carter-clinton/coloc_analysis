---
phase: 04-matched-n-cross-ancestry-concordance
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/python/se_inflation.py
  - src/snakemake/scripts/bootstrap_driver.py
  - src/snakemake/scripts/run_matched_coloc.R
  - src/snakemake/rules/matched_n.smk
  - tests/test_matched_n_se_inflation.py
autonomous: true
requirements_addressed: [Table-2-replacement, H7-pre-registration]
must_haves:
  truths:
    - SE-inflation math (D-01a) implemented and unit-tested
    - Z_b ~ N(beta_hat/SE_matched, 1) independent draws per variant per bootstrap (D-01b)
    - SuSiE refit per bootstrap via run_susie_rss.R reused verbatim (D-01b)
    - coloc.susie re-estimation per bootstrap with AFR discovery .fit.rds held fixed (D-01c)
    - Seed determinism: seed = 1000 * trait_id + bootstrap_idx
  artifacts:
    - path: src/python/se_inflation.py
      contains: "def inflate_se"
    - path: src/python/se_inflation.py
      contains: "sqrt(n_eur / n_afr)"
    - path: src/snakemake/scripts/bootstrap_driver.py
      provides: "Per-bootstrap Z resampling + pseudo-sumstats writer"
    - path: src/snakemake/scripts/run_matched_coloc.R
      provides: "Per-bootstrap coloc.susie using AFR .fit.rds (fixed) + EUR-matched .fit.rds"
    - path: src/snakemake/rules/matched_n.smk
      contains: "rule run_matched_bootstrap:"
    - path: src/snakemake/rules/matched_n.smk
      contains: "rule run_matched_coloc:"
  key_links:
    - from: src/snakemake/scripts/bootstrap_driver.py
      to: src/snakemake/scripts/run_susie_rss.R
      via: subprocess invocation (Phase 1 script reused verbatim)
      pattern: 'run_susie_rss\.R'
    - from: src/snakemake/scripts/run_matched_coloc.R
      to: results/fine_mapping/susie/*.AFR.*.fit.rds
      via: readRDS of AFR discovery fit
      pattern: 'readRDS.*AFR.*fit\.rds'
---

# Plan 04-02 — Bootstrap engine (SE-inflation + SuSiE refit + coloc.susie per bootstrap)

<objective>
Implement the core bootstrap engine for matched-N cross-ancestry concordance testing. This plan delivers the SE-inflation math (D-01a), per-bootstrap Z resampling with deterministic seeds (D-01b), SuSiE refit via Phase 1 script reuse, and per-bootstrap coloc.susie re-estimation with AFR discovery held fixed (D-01c). These are the compute-intensive rules that produce the per-bootstrap coloc summaries consumed by downstream concordance metrics.

Purpose: Build the statistical core that powers the matched-N bootstrap — the central methodological contribution replacing the broken Table 2.
Output: src/python/se_inflation.py, bootstrap_driver.py, run_matched_coloc.R, matched_n.smk bootstrap+coloc rules, passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-matched-n-cross-ancestry-concordance/04-CONTEXT.md
@.planning/phases/04-matched-n-cross-ancestry-concordance/04-RESEARCH.md
@.planning/phases/04-matched-n-cross-ancestry-concordance/04-01-SUMMARY.md
@src/python/sumstats_utils.py
@src/snakemake/scripts/run_susie_rss.R
@src/snakemake/scripts/run_coloc_susie.R
@config/susie_policy.yaml
@tests/test_matched_n_se_inflation.py
</context>

<tasks>

<task type="auto">
  <name>T1: se_inflation.py (D-01a math + unit test)</name>
  <files>src/python/se_inflation.py, tests/test_matched_n_se_inflation.py</files>
  <read_first>
    <file>src/python/sumstats_utils.py</file>
    <file>.planning/phases/04-matched-n-cross-ancestry-concordance/04-CONTEXT.md</file>
    <file>tests/test_matched_n_se_inflation.py</file>
  </read_first>
  <action>
Create `src/python/se_inflation.py` with:
```python
"""
Matched-N SE inflation per D-01a:
    SE_EUR_matched = SE_EUR * sqrt(N_EUR / N_AFR_trait)
Independent-Z resampling per D-01b:
    Z_b ~ N(beta_hat / SE_matched, 1) per variant, independently within a region.
LD matrix R is held fixed (used only in downstream SuSiE refit, not here).

Per RESEARCH B-1 CONFIRMED: analytic consequence of Z/beta/SE scaling under fixed
per-variant effect size. Downstream SuSiE refit uses full R.
"""
import numpy as np

def inflate_se(se_eur: np.ndarray, n_eur: float, n_afr: float) -> np.ndarray:
    if n_afr <= 0 or n_eur <= 0:
        raise ValueError(f'N_EUR={n_eur}, N_AFR={n_afr}; both must be positive')
    if n_afr > n_eur:
        raise ValueError(f'Matched-N requires N_AFR ({n_afr}) <= N_EUR ({n_eur})')
    factor = np.sqrt(n_eur / n_afr)
    return se_eur * factor

def draw_z_bootstrap(beta_hat: np.ndarray, se_matched: np.ndarray,
                      seed: int, rng_backend: str = 'default_rng') -> np.ndarray:
    rng = np.random.default_rng(seed)
    mean = beta_hat / se_matched
    z = rng.normal(loc=mean, scale=1.0, size=beta_hat.shape)
    return z

def reconstruct_pseudo_sumstats(z_bootstrap: np.ndarray,
                                  se_matched: np.ndarray) -> tuple:
    beta_b = z_bootstrap * se_matched
    se_b = se_matched  # SE held at matched scale per D-01b
    return beta_b, se_b

def compute_seed(trait_id: int, bootstrap_idx: int, seed_base: int = 1000) -> int:
    return seed_base * trait_id + bootstrap_idx
```
Rewrite `tests/test_matched_n_se_inflation.py` (removing xfail):
- `test_inflate_se_identity_when_n_equal`: N_EUR=100k, N_AFR=100k -> factor=1.0
- `test_inflate_se_scales_correctly`: N_EUR=700_000, N_AFR=55_525 -> factor=sqrt(700000/55525)~3.55; assert `np.isclose(factor, np.sqrt(700000/55525))`
- `test_inflate_se_raises_on_bad_n`: N_AFR=0 or N_EUR<N_AFR -> ValueError
- `test_draw_z_determinism`: same seed -> identical draws
- `test_reconstruct_pseudo_sumstats`: `beta_b = z * se_matched`, `se_b = se_matched`
- `test_compute_seed_formula`: `compute_seed(trait_id=3, bootstrap_idx=7, seed_base=1000)` == `3007`
  </action>
  <acceptance_criteria>
    <criterion>`grep -q 'def inflate_se' src/python/se_inflation.py`</criterion>
    <criterion>`grep -q 'sqrt(n_eur / n_afr)' src/python/se_inflation.py`</criterion>
    <criterion>`grep -q 'def compute_seed' src/python/se_inflation.py`</criterion>
    <criterion>All 6 tests pass (pytest exit 0)</criterion>
    <criterion>`python -c "from src.python.se_inflation import compute_seed; assert compute_seed(3,7,1000)==3007"` exits 0</criterion>
  </acceptance_criteria>
  <verify><automated>pytest tests/test_matched_n_se_inflation.py -x -q</automated></verify>
  <done>D-01a + D-01b math implemented; seed formula deterministic; 6 unit tests green.</done>
</task>

<task type="auto">
  <name>T2: bootstrap_driver.py (per-bootstrap pseudo-sumstats + SuSiE refit invocation)</name>
  <files>src/snakemake/scripts/bootstrap_driver.py, tests/test_matched_n_bootstrap_driver.py</files>
  <read_first>
    <file>src/snakemake/scripts/run_susie_rss.R</file>
    <file>.planning/phases/01-coloc-susie-fine-mapping-spine/01-CONTEXT.md</file>
    <file>src/python/se_inflation.py</file>
    <file>config/susie_policy.yaml</file>
    <file>.planning/phases/04-matched-n-cross-ancestry-concordance/04-CONTEXT.md</file>
  </read_first>
  <action>
Create `src/snakemake/scripts/bootstrap_driver.py` (callable as `python bootstrap_driver.py --trait {t} --region {r} --bootstrap-idx {b} --eur-sumstats {path} --afr-n {N} --ld-matrix-rds {path} --output-fit-rds {path}`):
1. Load EUR sumstats (tsv.gz) for region; extract beta_hat, SE, variant_ids.
2. Load config: `n_eur = trait_sample_sizes.yaml[trait]['EUR']` (reuse Phase 5 `sumstats_utils.py`).
3. `se_matched = inflate_se(se, n_eur, n_afr)` per D-01a.
4. `seed = compute_seed(trait_id, bootstrap_idx, seed_base=config['seed_base'])` per T1.
5. `z_b = draw_z_bootstrap(beta, se_matched, seed)`.
6. Write pseudo-sumstats tsv to tmp: `{tmpdir}/bootstrap_{b}_pseudo.tsv` with columns `variant_id, chrom, pos, beta_b, se_b, N` where `beta_b=z_b*se_matched, se_b=se_matched, N=n_afr`.
7. `subprocess.run(['Rscript', 'src/snakemake/scripts/run_susie_rss.R', '--sumstats', tmp_tsv, '--ld-rds', ld_matrix_rds, '--policy', 'config/susie_policy.yaml', '--output', output_fit_rds])` — reuse Phase 1 script verbatim per CONTEXT code_context (no modifications).
8. On success: print `OK bootstrap {b}` to stdout. On SuSiE failure: write minimal failure-.rds matching Phase 1 retry-ladder policy (readRDS-compatible; reuse pattern from Phase 9 run_replication_susie.R failure path).
9. Cull tmp pseudo-sumstats after fit succeeds (retention policy per CONTEXT Claude's Discretion).
  </action>
  <acceptance_criteria>
    <criterion>`grep -q 'run_susie_rss.R' src/snakemake/scripts/bootstrap_driver.py`</criterion>
    <criterion>`grep -q 'config/susie_policy.yaml' src/snakemake/scripts/bootstrap_driver.py` (reuse D-01b)</criterion>
    <criterion>`grep -q 'inflate_se' src/snakemake/scripts/bootstrap_driver.py`</criterion>
    <criterion>`grep -q 'compute_seed' src/snakemake/scripts/bootstrap_driver.py`</criterion>
    <criterion>`python src/snakemake/scripts/bootstrap_driver.py --help` exits 0</criterion>
    <criterion>Subprocess-mock test passes (Rscript call verified)</criterion>
  </acceptance_criteria>
  <verify><automated>python src/snakemake/scripts/bootstrap_driver.py --help && pytest tests/test_matched_n_se_inflation.py tests/test_matched_n_bootstrap_driver.py -x -q</automated></verify>
  <done>Driver callable; produces bootstrap-b .fit.rds via Phase 1 script reuse; seed deterministic; susie_policy.yaml unchanged.</done>
</task>

<task type="auto">
  <name>T3: matched_n.smk bootstrap + coloc rules + run_matched_coloc.R</name>
  <files>src/snakemake/rules/matched_n.smk, src/snakemake/scripts/run_matched_coloc.R</files>
  <read_first>
    <file>src/snakemake/scripts/run_coloc_susie.R</file>
    <file>.planning/phases/09-replication-in-independent-cohorts/09-04-PLAN.md</file>
    <file>.planning/phases/04-matched-n-cross-ancestry-concordance/04-CONTEXT.md</file>
    <file>src/snakemake/scripts/bootstrap_driver.py</file>
  </read_first>
  <action>
Append to `src/snakemake/rules/matched_n.smk`:
```python
rule run_matched_bootstrap:
    input:
        eur_sumstats = lambda w: f'data/processed/region_analysis/sumstats_harmonized_fixed/{w.trait}_EUR.bgz',
        ld_rds = 'results/ld_reference/ukbb_eur/{region}.rds',
        manifest = MATCHED_N_OUT / 'manifest.tsv',
        susie_policy = 'config/susie_policy.yaml',
    output:
        fit_rds = FITS_ROOT / '{trait}/{region}/bootstrap_{b}/eur_matched.fit.rds',
    params:
        trait_id = lambda w: config['traits'].index(w.trait),  # 0..4
        bootstrap_idx = lambda w: int(w.b),
        afr_n = lambda w: read_trait_afr_n(w.trait),  # helper reading trait_sample_sizes.yaml
    resources:
        mem_mb = 8000,
        runtime = 60,
    shell:
        """
        python src/snakemake/scripts/bootstrap_driver.py \\
            --trait {wildcards.trait} --trait-id {params.trait_id} \\
            --region {wildcards.region} \\
            --bootstrap-idx {params.bootstrap_idx} \\
            --eur-sumstats {input.eur_sumstats} \\
            --afr-n {params.afr_n} \\
            --ld-matrix-rds {input.ld_rds} \\
            --output-fit-rds {output.fit_rds}
        """

rule run_matched_coloc:
    """D-01c: coloc.susie per bootstrap; AFR discovery .fit.rds held fixed."""
    input:
        afr_fit = 'results/fine_mapping/susie/{trait}.AFR.{region}.fit.rds',  # Phase 1 discovery, fixed
        eur_matched_fit = FITS_ROOT / '{trait}/{region}/bootstrap_{b}/eur_matched.fit.rds',
    output:
        coloc_rds = MATCHED_N_OUT / 'coloc/{trait}/{region}/bootstrap_{b}/coloc.rds',
        coloc_tsv = MATCHED_N_OUT / 'coloc/{trait}/{region}/bootstrap_{b}/coloc_summary.tsv',
    resources:
        mem_mb = 4000,
        runtime = 30,
    shell:
        """
        Rscript src/snakemake/scripts/run_matched_coloc.R \\
            --afr-fit {input.afr_fit} \\
            --eur-matched-fit {input.eur_matched_fit} \\
            --output-rds {output.coloc_rds} \\
            --output-tsv {output.coloc_tsv}
        """
```
Create `src/snakemake/scripts/run_matched_coloc.R` (thin wrapper calling Phase 1 `run_coloc_susie.R` logic with AFR fit as s1, EUR-matched fit as s2; reuse verbatim per CONTEXT code_context — no new coloc logic). Output TSV columns: `signal_id, pph4, pph3, pph2, pph1, pph0, cs_afr_size, cs_eur_size, lead_variant_afr, lead_variant_eur, lead_sign_agree`.
  </action>
  <acceptance_criteria>
    <criterion>`grep -q 'rule run_matched_bootstrap:' src/snakemake/rules/matched_n.smk`</criterion>
    <criterion>`grep -q 'rule run_matched_coloc:' src/snakemake/rules/matched_n.smk`</criterion>
    <criterion>`test -f src/snakemake/scripts/run_matched_coloc.R`</criterion>
    <criterion>`grep -q 'run_coloc_susie' src/snakemake/scripts/run_matched_coloc.R` (verbatim reuse)</criterion>
    <criterion>`grep -q 'lead_sign_agree' src/snakemake/scripts/run_matched_coloc.R` (D-02c hook)</criterion>
    <criterion>Snakemake dry-run for a single target exits 0</criterion>
  </acceptance_criteria>
  <verify><automated>snakemake --dry-run -s Snakefile results/matched_n/coloc/t2d/chr10_114p/bootstrap_1/coloc_summary.tsv 2>&1 | tail -20 && Rscript -e 'source("src/snakemake/scripts/run_matched_coloc.R"); cat("OK")' 2>&1 | tail -5</automated></verify>
  <done>Bootstrap + coloc rules wired; AFR discovery .fit.rds immutably held fixed on input side; Phase 1 coloc script reused verbatim; per-bootstrap coloc_summary.tsv schema locked.</done>
</task>

</tasks>

<threat_model>
Seed determinism -> compute_seed formula + test covers. Sumstats integrity -> upstream Phase 0/2 harmonization (checksummed). LSF write-path -> FITS_ROOT confined to `/rs1/researchers/c/ckclinto/`. AFR .fit.rds tampering -> input file only, never written -> readRDS is read-only by construction. `tmp` cleanup -> tmpdir per Snakemake scope. No high-severity.
</threat_model>

<verification>
Run `pytest tests/test_matched_n_se_inflation.py tests/test_matched_n_bootstrap_driver.py -x -q` for unit tests. Run `snakemake --dry-run -s Snakefile results/matched_n/coloc/t2d/chr10_114p/bootstrap_1/coloc_summary.tsv` for integration.
</verification>

<must_haves>
- [ ] SE-inflation math (D-01a) implemented and unit-tested
- [ ] Z_b ~ N(beta_hat/SE_matched, 1) independent draws per variant per bootstrap (D-01b)
- [ ] SuSiE refit per bootstrap via run_susie_rss.R reused verbatim (D-01b)
- [ ] coloc.susie re-estimation per bootstrap with AFR discovery .fit.rds held fixed (D-01c)
- [ ] Seed determinism: seed = 1000 * trait_id + bootstrap_idx
- [ ] run_matched_coloc.R reuses Phase 1 coloc logic verbatim
</must_haves>

<success_criteria>
SE-inflation formula correct and tested. Bootstrap driver callable with --help. Snakemake rules resolve for single-target dry-run. Phase 1 scripts reused without modification.
</success_criteria>

<output>
After completion, create `.planning/phases/04-matched-n-cross-ancestry-concordance/04-02-SUMMARY.md`
</output>
