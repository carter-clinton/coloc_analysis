---
phase: 04-matched-n-cross-ancestry-concordance
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/snakemake/rules/matched_n.smk
  - src/snakemake/scripts/munge_trait_pair_rg.py
  - src/python/apply_fdr.py
  - src/python/compute_detection_probability.py
  - tests/test_matched_n_fdr.py
  - tests/test_matched_n_detection.py
autonomous: true
requirements_addressed: [Table-2-replacement, H7-pre-registration]
must_haves:
  truths:
    - Up to 30 LDSC r_g tests run (10 trait-pairs x 3 ancestry-pairs per D-04a)
    - BH-FDR q<0.05 applied across ALL 30 tests jointly per D-04c
    - Same-trait cross-ancestry r_g flagged as `is_global_benchmark=TRUE` per D-04b
    - SE>0.3 flagged per research verdict A-2 option (a) minimum-deviation
    - Per-locus detection probability under empirical beta_hat/SE null (D-05a) -> trait-level expected concordance (D-05b/d)
    - Script headers explicitly mark D-05 as original-research construction (not "following Hou 2023") per RESEARCH B-2 resolution
  artifacts:
    - path: src/python/apply_fdr.py
      contains: "statsmodels.stats.multitest.multipletests"
    - path: src/python/apply_fdr.py
      contains: "method='fdr_bh'"
    - path: src/python/compute_detection_probability.py
      contains: "# ORIGINAL-RESEARCH CONSTRUCTION"
    - path: src/python/compute_detection_probability.py
      contains: "scipy.stats.ncx2"
    - path: results/matched_n/rg_matrix.tsv
      provides: "D-06d supplementary table"
    - path: results/matched_n/detection_probability.tsv
      provides: "D-05b trait-level expected concordance"
  key_links:
    - from: src/snakemake/rules/matched_n.smk
      to: LDSC ldsc.py
      via: rule ldsc_rg (extends existing munge_sumstats_ldsc infrastructure, placed in matched_n.smk to avoid cross-phase file ownership with Phase 5's pathway.smk)
      pattern: 'rule ldsc_rg:'
---

# Plan 04-04 — LDSC 30-test r_g matrix (D-04) + Hou-null detection probability (D-05)

<objective>
Build the LDSC cross-ancestry genetic correlation matrix (30 tests: 10 trait-pairs x 3 ancestry-pairs, D-04a) with BH-FDR correction (D-04c), SE>0.3 flagging (research A-2), and same-trait cross-ancestry benchmarking (D-04b). Separately, implement the per-locus detection probability under the empirical NCP null (D-05a) aggregated to trait-level expected concordance (D-05b/d) — framed as original-research construction per RESEARCH B-2 resolution. This plan runs in Wave 2 parallel with 04-02. All rules placed in matched_n.smk (not pathway.smk) to avoid cross-phase file ownership with Phase 5.

Purpose: Provide the theoretical/null-expectation side of the H7 test — what concordance would we expect given power alone? Combined with the empirical concordance from 04-03, this enables the 20pp verdict.
Output: matched_n.smk ldsc_rg rules, munge_trait_pair_rg.py, apply_fdr.py, compute_detection_probability.py, rg_matrix.tsv, detection_probability.tsv, passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-matched-n-cross-ancestry-concordance/04-CONTEXT.md
@.planning/phases/04-matched-n-cross-ancestry-concordance/04-RESEARCH.md
@.planning/phases/04-matched-n-cross-ancestry-concordance/04-01-SUMMARY.md
@src/snakemake/rules/matched_n.smk
@.planning/phases/05-pathway-partitioned-heritability/05-03-SUMMARY.md
@src/snakemake/scripts/build_magma_geneset.py
</context>

<tasks>

<task type="auto">
  <name>T1: matched_n.smk ldsc_rg rules (D-04d reuse + D-04a full 30-test expansion)</name>
  <files>src/snakemake/rules/matched_n.smk, src/snakemake/scripts/munge_trait_pair_rg.py</files>
  <read_first>
    <file>src/snakemake/rules/matched_n.smk</file>
    <file>.planning/phases/05-pathway-partitioned-heritability/05-03-SUMMARY.md</file>
    <file>.planning/phases/04-matched-n-cross-ancestry-concordance/04-CONTEXT.md</file>
  </read_first>
  <action>
Extend `src/snakemake/rules/matched_n.smk` — append a new rule block guarded by a TRAIT_PAIR_RG_COMBOS computed at Snakemake load time:
```python
# D-04a: 10 trait-pairs x 3 ancestry-pairs = up to 30 LDSC r_g tests
import itertools
RG_TRAITS = config['traits']  # 5 T1 traits
RG_TRAIT_PAIRS = list(itertools.combinations(RG_TRAITS, 2))  # 10
RG_ANCESTRY_PAIRS = config['rg_ancestry_pairs']  # [EUR_EUR, AFR_AFR, EUR_AFR]
RG_COMBOS = [(t1, t2, ap) for (t1, t2) in RG_TRAIT_PAIRS for ap in RG_ANCESTRY_PAIRS]

rule ldsc_rg:
    """D-04a: single r_g test for (trait1, trait2, ancestry_pair)."""
    input:
        munged_t1 = lambda w: f'results/ldsc/munged/{w.trait1}_{w.ancestry1}.sumstats.gz',
        munged_t2 = lambda w: f'results/ldsc/munged/{w.trait2}_{w.ancestry2}.sumstats.gz',
        ldscores1 = lambda w: f'data/reference/ldsc/ldscores_{w.ancestry1}/',
        ldscores2 = lambda w: f'data/reference/ldsc/ldscores_{w.ancestry2}/',
    output: 'results/matched_n/rg/{trait1}_{trait2}_{ancestry1}_{ancestry2}.log'
    conda: '../envs/ldsc_py3.yml'
    shell:
        """
        python $(which ldsc.py) \\
            --rg {input.munged_t1},{input.munged_t2} \\
            --ref-ld-chr {input.ldscores1}/ \\
            --w-ld-chr {input.ldscores1}/ \\
            --out results/matched_n/rg/{wildcards.trait1}_{wildcards.trait2}_{wildcards.ancestry1}_{wildcards.ancestry2}
        """

rule collect_rg_logs:
    """Parse LDSC .log files into a single TSV for FDR."""
    input: expand('results/matched_n/rg/{combo[0]}_{combo[1]}_{combo[2]}.log',
                   combo=[(t1,t2,ap.split('_')[0]+'_'+ap.split('_')[1]) for (t1,t2,ap) in RG_COMBOS])
    output: 'results/matched_n/rg_raw.tsv'
    shell: 'python src/snakemake/scripts/munge_trait_pair_rg.py --log-dir results/matched_n/rg --out {output}'
```
For EUR-AFR tests per research A-2: ref-ld-chr uses EUR ldscores, w-ld-chr uses EUR ldscores (LDSC docs default for cross-ancestry). Add a shell comment noting this limitation + that SE>0.3 flag will be applied downstream.

Create `src/snakemake/scripts/munge_trait_pair_rg.py`: parse each `.log` via regex (LDSC log format: `rg, se, z, p, h2_obs` block), emit TSV with columns `trait1, trait2, ancestry1, ancestry2, is_global_benchmark, rg, se, z, p, h2_obs_t1, h2_obs_t2`. `is_global_benchmark = (trait1==trait2 AND ancestry_pair=='EUR_AFR')` per D-04b. Combo construction: `RG_COMBOS = [(t1,t2,ap) for t1,t2 in itertools.combinations(RG_TRAITS, 2) for ap in RG_ANCESTRY_PAIRS] + [(t,t,'EUR_AFR') for t in RG_TRAITS]` — C(5,2)×3 = 30 cross-trait tests + 5 same-trait EUR-AFR benchmarks = 35 total. D-04a says "up to 30" (cross-trait only); D-04b adds 5 same-trait EUR-AFR as the distinguished global-benchmark subset. D-04c BH-FDR applied across all 35.
  </action>
  <acceptance_criteria>
    <criterion>`grep -q 'rule ldsc_rg:' src/snakemake/rules/matched_n.smk`</criterion>
    <criterion>`grep -q 'rule collect_rg_logs:' src/snakemake/rules/matched_n.smk`</criterion>
    <criterion>`grep -q "RG_COMBOS" src/snakemake/rules/matched_n.smk`</criterion>
    <criterion>`grep -q "(t,t,'EUR_AFR') for t in RG_TRAITS" src/snakemake/rules/matched_n.smk` (same-trait benchmark inclusion per D-04b)</criterion>
    <criterion>Python combos count test shows 35 tests total (30 cross-trait + 5 same-trait EUR-AFR benchmarks)</criterion>
    <criterion>`test -f src/snakemake/scripts/munge_trait_pair_rg.py`</criterion>
  </acceptance_criteria>
  <verify><automated>snakemake --list -s Snakefile | grep -c 'ldsc_rg\|collect_rg_logs' && python -c "import itertools; traits=['t2d','stroke','hypertension','asthma','bmi']; pairs=list(itertools.combinations(traits,2)); assert len(pairs)==10, len(pairs); cross=10*3; bench=5; total=cross+bench; assert total==35, total; print(f'{total} tests (30 cross-trait + 5 same-trait EUR-AFR)')"</automated></verify>
  <done>30-test r_g matrix rules wired; D-04a/b/d honored; Phase 5 ldsc_py3 env reused; downstream FDR task can consume `rg_raw.tsv`.</done>
</task>

<task type="auto">
  <name>T2: apply_fdr.py (D-04c BH across all 30 tests + SE>0.3 flag)</name>
  <files>src/python/apply_fdr.py, tests/test_matched_n_fdr.py, src/snakemake/rules/matched_n.smk</files>
  <read_first>
    <file>src/snakemake/scripts/build_magma_geneset.py</file>
    <file>.planning/phases/04-matched-n-cross-ancestry-concordance/04-CONTEXT.md</file>
    <file>.planning/phases/04-matched-n-cross-ancestry-concordance/04-RESEARCH.md</file>
  </read_first>
  <action>
Create `src/python/apply_fdr.py`:
```python
"""
D-04c: BH-FDR q<0.05 across ALL 30 r_g tests jointly (not per-ancestry-pair, not
per-trait-pair-stratified). Matches Phase 5 D-01a pathway FDR convention.

RESEARCH A-2 option (a) minimum-deviation: flag SE>0.3 as 'unreliable_se' column
per LDSC wiki guidance without removing the row.
"""
import pandas as pd
from statsmodels.stats.multitest import multipletests

def apply_bh_fdr(rg_raw_tsv: str, fdr_q: float = 0.05,
                  se_flag_threshold: float = 0.3, out_tsv: str = None) -> pd.DataFrame:
    df = pd.read_csv(rg_raw_tsv, sep='\t')
    # Drop tests where LDSC could not compute rg (p=NA)
    valid = df['p'].notna()
    rej, q, _, _ = multipletests(df.loc[valid, 'p'].values, alpha=fdr_q, method='fdr_bh')
    df.loc[valid, 'q_bh'] = q
    df.loc[valid, 'fdr_significant'] = rej
    df.loc[~valid, 'q_bh'] = pd.NA
    df.loc[~valid, 'fdr_significant'] = False
    df['unreliable_se'] = df['se'].abs() > se_flag_threshold
    if out_tsv:
        df.to_csv(out_tsv, sep='\t', index=False)
    return df
```
Rewrite `tests/test_matched_n_fdr.py`: synthetic 30-test fixture with known p-values -> assert `multipletests` result matches scipy-independent computation; assert `unreliable_se=True` when `se>0.3`. Cover edge cases: `p=NaN` rows preserved with `q_bh=NA, fdr_significant=False`.

Append rule to `matched_n.smk`:
```python
rule apply_rg_fdr:
    input: 'results/matched_n/rg_raw.tsv'
    output: 'results/matched_n/rg_matrix.tsv'
    shell: 'python src/python/apply_fdr.py --in {input} --out {output} --fdr-q 0.05 --se-flag 0.3'
```
  </action>
  <acceptance_criteria>
    <criterion>`grep -q "method='fdr_bh'" src/python/apply_fdr.py`</criterion>
    <criterion>`grep -q 'unreliable_se' src/python/apply_fdr.py`</criterion>
    <criterion>`grep -q 'D-04c' src/python/apply_fdr.py`</criterion>
    <criterion>`grep -q 'rule apply_rg_fdr:' src/snakemake/rules/matched_n.smk`</criterion>
    <criterion>All FDR tests pass</criterion>
    <criterion>Output TSV has columns: all inputs + `q_bh, fdr_significant, unreliable_se`</criterion>
  </acceptance_criteria>
  <verify><automated>pytest tests/test_matched_n_fdr.py -x -q</automated></verify>
  <done>D-04c BH-FDR applied across ALL 30 tests; D-06d supplementary table produced; SE>0.3 flag column per research A-2 minimum-deviation.</done>
</task>

<task type="auto">
  <name>T3: rg_matrix.tsv wiring + D-06d column set</name>
  <files>tests/test_matched_n_fdr.py</files>
  <read_first>
    <file>.planning/phases/04-matched-n-cross-ancestry-concordance/04-CONTEXT.md</file>
  </read_first>
  <action>
Verify `rg_matrix.tsv` (T2 output) has the final D-06d schema: `trait1, trait2, ancestry1, ancestry2, is_global_benchmark, rg, se, z, p, h2_obs_t1, h2_obs_t2, q_bh, fdr_significant, unreliable_se`. Add an acceptance integration step: Snakemake `--list` includes `apply_rg_fdr`. Add test `tests/test_matched_n_fdr.py::test_rg_matrix_schema` that reads a synthetic rg_matrix.tsv fixture and asserts the exact column set above.
  </action>
  <acceptance_criteria>
    <criterion>`test_rg_matrix_schema` passes; column set frozen</criterion>
    <criterion>`grep -q 'is_global_benchmark' tests/test_matched_n_fdr.py`</criterion>
  </acceptance_criteria>
  <verify><automated>pytest tests/test_matched_n_fdr.py::test_rg_matrix_schema -x -q && snakemake --list -s Snakefile | grep -q apply_rg_fdr</automated></verify>
  <done>D-06d rg_matrix.tsv schema frozen by test.</done>
</task>

<task type="auto">
  <name>T4: compute_detection_probability.py (D-05a/b/c/d empirical beta_hat/SE NCP)</name>
  <files>src/python/compute_detection_probability.py, tests/test_matched_n_detection.py, src/snakemake/rules/matched_n.smk</files>
  <read_first>
    <file>.planning/phases/04-matched-n-cross-ancestry-concordance/04-CONTEXT.md</file>
    <file>.planning/phases/04-matched-n-cross-ancestry-concordance/04-RESEARCH.md</file>
  </read_first>
  <action>
Create `src/python/compute_detection_probability.py`:
```python
"""
Per-locus detection probability under empirical beta_hat/SE null (D-05a).

# ORIGINAL-RESEARCH CONSTRUCTION (per RESEARCH B-2 resolution 2026-04-15).
# The originally-cited Hou 2023 Nat Genet paper (PMC10403901 -> PMC11120833) is
# the radmix paper, which does NOT describe an NCP-based detection-probability
# framework. D-05 is an original analytic construction by this study; the OSF
# pre-registration section 12.1 line 320 does NOT cite Hou by name for the NCP
# framework, so this is an internal clarification, not an OSF deviation
# (logged in .planning/osf_deviations.md as clarification).

D-05a: For each AFR Tier A locus with observed beta_hat_AFR and SE_AFR at the AFR
       lead variant, compute the chi-square non-centrality parameter at
       N_EUR_matched:
           NCP = (beta_hat_AFR / SE_AFR_at_matched_N)^2
       where SE_AFR_at_matched_N = SE_AFR (AFR discovery N already = matched N).
       Detection probability = P(chi^2_1,NCP >= T) where T = qchisq(1 - 5e-8, df=1)
       for genome-wide significance. Use scipy.stats.ncx2.sf(T, df=1, nc=NCP).
D-05b: Aggregate per-locus detection probabilities to trait-level expected
       concordance via arithmetic mean across AFR Tier A loci per trait (D-05d).
D-05c: Parametric prior from Hou et al. 2023 Table S1 is NOT used for primary.
"""
import numpy as np
import pandas as pd
from scipy import stats

GW_SIG_CHI2_THRESHOLD = stats.chi2.ppf(1 - 5e-8, df=1)  # ~29.72

def per_locus_detection_prob(beta_hat: np.ndarray, se: np.ndarray,
                               chi2_threshold: float = GW_SIG_CHI2_THRESHOLD
                               ) -> np.ndarray:
    ncp = (beta_hat / se) ** 2
    return stats.ncx2.sf(chi2_threshold, df=1, nc=ncp)

def trait_expected_concordance(tier_a_tsv: str, out_tsv: str) -> pd.DataFrame:
    df = pd.read_csv(tier_a_tsv, sep='\t')  # columns: trait, locus_id, beta_afr, se_afr
    df['detection_prob'] = per_locus_detection_prob(df['beta_afr'].values, df['se_afr'].values)
    agg = df.groupby('trait', as_index=False).agg(
        n_tier_a_loci=('locus_id', 'count'),
        expected_concordance_hou_null=('detection_prob', 'mean'),  # D-05d arithmetic mean
    )
    agg.to_csv(out_tsv, sep='\t', index=False)
    return agg
```
Rewrite `tests/test_matched_n_detection.py`:
- `test_ncp_from_empirical_beta_se`: beta_hat=0.3, SE=0.05 -> NCP=36 -> `ncx2.sf(~29.72, df=1, nc=36)` ~ 0.78; assert within 0.01 tolerance.
- `test_trait_expected_concordance_arithmetic_mean`: synthetic fixture with 3 loci per trait x 2 traits, known per-locus probs -> assert trait-level = arithmetic mean.
- `test_empty_trait_raises`: trait with 0 Tier A loci -> assert either NaN or exclusion (documented behavior).
- `test_original_research_header`: grep-based regression: `'# ORIGINAL-RESEARCH CONSTRUCTION'` present in file.
- `test_parametric_hou_not_used`: grep-based regression: `'Hou et al. 2023 Table S1 is NOT used'` present.

Append rule to `matched_n.smk`:
```python
rule compute_detection_probability:
    input: 'results/phase2/tier_assignments.tsv'
    output: 'results/matched_n/detection_probability.tsv'
    shell: 'python src/python/compute_detection_probability.py --in {input} --out {output}'
```
  </action>
  <acceptance_criteria>
    <criterion>`grep -q 'ORIGINAL-RESEARCH CONSTRUCTION' src/python/compute_detection_probability.py`</criterion>
    <criterion>`grep -q 'scipy.stats.ncx2\|stats.ncx2' src/python/compute_detection_probability.py`</criterion>
    <criterion>`grep -q 'Hou et al. 2023 Table S1 is NOT used' src/python/compute_detection_probability.py` (D-05c)</criterion>
    <criterion>`grep -q 'arithmetic mean' src/python/compute_detection_probability.py` (D-05d)</criterion>
    <criterion>All 5 detection tests pass</criterion>
    <criterion>Output TSV has columns: `trait, n_tier_a_loci, expected_concordance_hou_null`</criterion>
    <criterion>`.planning/osf_deviations.md` contains B-2-resolution entry documenting D-05 as original-research construction (not "following Hou 2023")</criterion>
  </acceptance_criteria>
  <verify><automated>pytest tests/test_matched_n_detection.py -x -q</automated></verify>
  <done>D-05 full: per-locus NCP + arithmetic-mean aggregation; OSF-safe original-research framing in header; D-05c parametric-prior-exclusion test.</done>
</task>

</tasks>

<threat_model>
LDSC munged sumstats integrity -> upstream Phase 5 (checksummed). FDR tampering -> BH result verified by independent scipy-based test. NCP framework misattribution -> header comment + regression test guard against re-introducing a broken citation. No high-severity.
</threat_model>

<verification>
Run `pytest tests/test_matched_n_fdr.py tests/test_matched_n_detection.py -x -q` for unit tests. Run `snakemake --list -s Snakefile | grep -c 'ldsc_rg\|apply_rg_fdr\|compute_detection_probability'` for integration (expect 3+).
</verification>

<must_haves>
- [ ] Up to 30 LDSC r_g tests wired (10 trait-pairs x 3 ancestry-pairs per D-04a)
- [ ] BH-FDR q<0.05 applied across ALL 30 tests jointly per D-04c
- [ ] Same-trait cross-ancestry r_g flagged as is_global_benchmark=TRUE per D-04b
- [ ] SE>0.3 flagged per research verdict A-2 option (a) minimum-deviation
- [ ] Per-locus detection probability under empirical NCP null (D-05a)
- [ ] Trait-level expected concordance via arithmetic mean (D-05b/d)
- [ ] ORIGINAL-RESEARCH CONSTRUCTION header (D-05, per RESEARCH B-2)
- [ ] D-05c parametric-prior-exclusion documented and tested
</must_haves>

<success_criteria>
30-test r_g rules resolve in Snakemake. BH-FDR and SE-flag tests pass. Detection probability tests pass with NCP math verified. All script headers cite correct D-IDs. Original-research framing regression tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-matched-n-cross-ancestry-concordance/04-04-SUMMARY.md`
</output>
