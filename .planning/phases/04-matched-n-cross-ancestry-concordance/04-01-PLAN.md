---
phase: 04-matched-n-cross-ancestry-concordance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/matched_n.yaml
  - schemas/matched_n.schema.yaml
  - src/snakemake/rules/matched_n.smk
  - tests/test_matched_n_se_inflation.py
  - tests/test_matched_n_tier_a.py
  - tests/test_matched_n_h7.py
  - tests/test_matched_n_detection.py
  - tests/test_matched_n_fdr.py
  - tests/test_matched_n_negcontrol.py
  - tests/test_matched_n_table2.py
  - tests/fixtures/matched_n/README.md
  - .planning/data_access.md
autonomous: true
requirements_addressed: [Table-2-replacement, H7-pre-registration]
must_haves:
  truths:
    - config/matched_n.yaml parses with schema validation
    - src/snakemake/rules/matched_n.smk loads without errors (snakemake --list)
    - 7 test files exist with xfail stubs (one per D-01/D-02/D-04/D-05 unit)
    - bmi.AFR ingestion path documented in data_access.md with MVP/AoU/Pan-UKBB tiered fallback
  artifacts:
    - path: config/matched_n.yaml
      contains: "bootstrap_n: 100"
    - path: config/matched_n.yaml
      contains: "seed_base: 1000"
    - path: config/matched_n.yaml
      contains: "h7_reduction_threshold_pp: 20"
    - path: config/matched_n.yaml
      contains: "concordance_threshold: 0.8"
    - path: config/matched_n.yaml
      contains: "ancestry_pairs:"
    - path: schemas/matched_n.schema.yaml
      provides: "JSON schema validation for matched_n.yaml"
    - path: src/snakemake/rules/matched_n.smk
      contains: "rule build_matched_n_manifest:"
    - path: .planning/data_access.md
      contains: "MVP phs002453"
  key_links:
    - from: Snakefile (top-level)
      to: src/snakemake/rules/matched_n.smk
      via: include directive
      pattern: 'include:.*matched_n\.smk'
---

# Plan 04-01 — Scaffold, config, Wave 0 test stubs, bmi.AFR dependency surface

<objective>
Create the foundational scaffold for Phase 4 matched-N cross-ancestry concordance: the config file with all pre-registered parameters (100 bootstraps, 20pp H7 threshold, seed formula, FDR q<0.05), JSON schema validation, the matched_n.smk Snakemake rule file with manifest generation, 7 Wave 0 pytest xfail stubs covering every downstream D-ID, and the bmi.AFR tiered data-access fallback (MVP primary, AoU alt, Pan-UKBB last-resort). This plan produces no production outputs but unblocks all subsequent plans.

Purpose: Establish the configuration contract and test scaffolding that all later plans build against. Ensures D-01/D-02/D-03/D-04/D-05 parameters are locked before implementation begins.
Output: config/matched_n.yaml, schemas/matched_n.schema.yaml, src/snakemake/rules/matched_n.smk (skeleton), 7 test stubs, data_access.md bmi.AFR section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-matched-n-cross-ancestry-concordance/04-CONTEXT.md
@.planning/phases/04-matched-n-cross-ancestry-concordance/04-RESEARCH.md
@config/pipeline.yaml
@config/susie_policy.yaml
@config/replication_cohorts.yaml
@schemas/
@src/snakemake/rules/replication.smk
@src/snakemake/rules/pathway.smk
@Snakefile
@.planning/data_access.md
@tests/phase5/conftest.py
@tests/phase9/conftest.py
</context>

<tasks>

<task type="auto">
  <name>T1: config/matched_n.yaml + schema</name>
  <files>config/matched_n.yaml, schemas/matched_n.schema.yaml</files>
  <read_first>
    <file>config/pipeline.yaml</file>
    <file>config/susie_policy.yaml</file>
    <file>config/replication_cohorts.yaml</file>
    <file>schemas/</file>
    <file>.planning/phases/04-matched-n-cross-ancestry-concordance/04-CONTEXT.md</file>
  </read_first>
  <action>
Create `config/matched_n.yaml` with **exact keys and values**:
```yaml
bootstrap_n: 100                     # D-01b
seed_base: 1000                      # seed formula: 1000 * trait_id + bootstrap_idx
h7_reduction_threshold_pp: 20        # D-02d (pre-registered)
concordance_threshold: 0.8           # D-02a Tier A
relaxed_pph4_threshold: 0.5          # D-02b Jaccard gate
rg_fdr_q: 0.05                       # D-04c BH-FDR q<0.05 across ALL 30 tests
rg_se_flag_threshold: 0.3            # RESEARCH A-2 option (a): flag SE>0.3
ancestry_pairs:
  - [EUR, AFR]                       # D-03a only (Hispanic excluded per D-03b)
traits:                              # D-03a 5 T1 traits
  - t2d
  - stroke
  - hypertension
  - asthma
  - bmi
rg_trait_pairs_n: 10                 # D-04a C(5,2)
rg_ancestry_pairs: [EUR_EUR, AFR_AFR, EUR_AFR]  # D-04a
output_root: results/matched_n
bootstrap_fits_root: /rs1/researchers/c/ckclinto/matched_n_fits  # per CONTEXT Claude's Discretion
```
Create `schemas/matched_n.schema.yaml` as JSON-schema validating all keys above with strict typing (integer, float, list-of-strings).
  </action>
  <acceptance_criteria>
    <criterion>`grep -q 'bootstrap_n: 100' config/matched_n.yaml`</criterion>
    <criterion>`grep -q 'h7_reduction_threshold_pp: 20' config/matched_n.yaml`</criterion>
    <criterion>`grep -q 'seed_base: 1000' config/matched_n.yaml`</criterion>
    <criterion>`grep -q 'rg_fdr_q: 0.05' config/matched_n.yaml`</criterion>
    <criterion>`test -f schemas/matched_n.schema.yaml`</criterion>
    <criterion>Schema validates config (python cmd exits 0)</criterion>
  </acceptance_criteria>
  <verify><automated>python -c "import yaml,jsonschema; c=yaml.safe_load(open('config/matched_n.yaml')); s=yaml.safe_load(open('schemas/matched_n.schema.yaml')); jsonschema.validate(c,s); print('OK')"</automated></verify>
  <done>Config + schema parse; 100 bootstraps + 20pp + 0.05 FDR + seed formula locked per D-01/D-02/D-04.</done>
</task>

<task type="auto">
  <name>T2: matched_n.smk skeleton + manifest rule + bmi.AFR dependency surface</name>
  <files>src/snakemake/rules/matched_n.smk, Snakefile, .planning/data_access.md</files>
  <read_first>
    <file>src/snakemake/rules/replication.smk</file>
    <file>src/snakemake/rules/pathway.smk</file>
    <file>Snakefile</file>
    <file>.planning/STATE.md</file>
    <file>.planning/data_access.md</file>
  </read_first>
  <action>
Create `src/snakemake/rules/matched_n.smk` with:
- Config load: `configfile: "config/matched_n.yaml"` (string literal per Phase 9 learning)
- Constants block: `MATCHED_N_OUT = Path(config['output_root']); FITS_ROOT = Path(config['bootstrap_fits_root'])`
- Skeleton rule `rule all_matched_n:` with `input` listing the 4 final outputs (expand over traits): `table2.tsv`, `table2_jaccard.tsv`, `rg_matrix.tsv`, `supp_violin.pdf`
- Rule `rule build_matched_n_manifest:` that:
  - `input:` Phase 2 `results/phase2/tier_assignments.tsv` + Phase 1 discovery .fit.rds glob + config bmi.AFR source flag
  - `output:` `results/matched_n/manifest.tsv` (columns: trait, ancestry_pair, region_id, afr_fit_rds, bootstrap_idx [rows 1..100])
  - `run:` python block reading tier_assignments, filtering to AFR Tier A, cross-joining config['traits'] x range(1, config['bootstrap_n']+1). Handle bmi.AFR specially: if `data/processed/region_analysis/sumstats_harmonized_fixed/bmi_AFR.bgz` missing -> emit manifest row with `bmi_afr_status=unavailable` flag; downstream rule will skip bmi bootstrap and emit `bmi`-row in table2 with "AFR-unavailable" sentinel per CONTEXT deferred-fallback-d.
- Add top-level `Snakefile`: `include: "src/snakemake/rules/matched_n.smk"` after the replication include.
- Append to `.planning/data_access.md` a new section `## bmi.AFR (Phase 4 dependency, 2026-04)` listing: **Tier 1 MVP phs002453 (N~55,525, GRCh38, dbGaP public 2024-07-22, prefer if DAR-unverified -> check first)**, **Tier 2 AoU BMI AFR (N~54,940, AoU Workbench, Carter credentialed)**, **Tier 3 Pan-UKBB AFR BMI field 21001 (N~6,000, last-resort fallback, flag in table2 per D-03c / Deferred-d)**. Each tier gets: access URL/accession, build, known N, next action with date checkbox.
  </action>
  <acceptance_criteria>
    <criterion>`grep -q 'rule build_matched_n_manifest:' src/snakemake/rules/matched_n.smk`</criterion>
    <criterion>`grep -q 'rule all_matched_n:' src/snakemake/rules/matched_n.smk`</criterion>
    <criterion>`grep -q 'include:.*matched_n\.smk' Snakefile`</criterion>
    <criterion>`grep -q 'MVP phs002453' .planning/data_access.md`</criterion>
    <criterion>`grep -q 'AoU BMI AFR' .planning/data_access.md`</criterion>
    <criterion>`snakemake --list` exits 0 and includes `build_matched_n_manifest`</criterion>
  </acceptance_criteria>
  <verify><automated>snakemake --list -s Snakefile | grep -q 'build_matched_n_manifest' && snakemake --dry-run -s Snakefile all_matched_n 2>&1 | head -40</automated></verify>
  <done>Skeleton loads; manifest rule present; bmi.AFR tiered fallback documented with MVP primary + AoU alt + Pan-UKBB last-resort; top-level Snakefile wired.</done>
</task>

<task type="auto">
  <name>T3: Wave 0 test stubs (pytest xfail, per RESEARCH Wave 0 Gaps)</name>
  <files>tests/test_matched_n_se_inflation.py, tests/test_matched_n_tier_a.py, tests/test_matched_n_h7.py, tests/test_matched_n_detection.py, tests/test_matched_n_fdr.py, tests/test_matched_n_negcontrol.py, tests/test_matched_n_table2.py, tests/fixtures/matched_n/README.md</files>
  <read_first>
    <file>.planning/phases/04-matched-n-cross-ancestry-concordance/04-RESEARCH.md</file>
    <file>.planning/phases/04-matched-n-cross-ancestry-concordance/04-VALIDATION.md</file>
    <file>tests/phase5/conftest.py</file>
    <file>tests/phase9/conftest.py</file>
  </read_first>
  <action>
Create 7 pytest files under `tests/`, all marked `@pytest.mark.phase4` with `@pytest.mark.xfail(reason='Wave 0 stub — implementation in later plan')`:
- `tests/test_matched_n_se_inflation.py::test_se_inflation_formula` — xfail stub; docstring cites D-01a formula `SE_EUR_matched = SE_EUR * sqrt(N_EUR / N_AFR)`
- `tests/test_matched_n_tier_a.py::test_bootstrap_median_tier_a` — xfail stub; docstring cites D-02a
- `tests/test_matched_n_h7.py::test_20pp_verdict` — xfail stub; docstring cites D-02d (20pp absolute reduction)
- `tests/test_matched_n_detection.py::test_ncp_from_empirical_beta_se` — xfail stub; docstring cites D-05a and marks "original-research construction per RESEARCH B-2 resolution"
- `tests/test_matched_n_fdr.py::test_bh_fdr_across_30_tests` — xfail stub; docstring cites D-04c
- `tests/test_matched_n_negcontrol.py::test_hla_pigmentation_no_tier_flip` — xfail stub (integration)
- `tests/test_matched_n_table2.py::test_table2_has_5_rows_10_cols` — xfail stub (integration)
- Create `tests/fixtures/matched_n/README.md` describing planned synthetic fixtures (2 synthetic loci x 5 bootstraps).
- Update `tests/conftest.py` (or phase4-specific if exists) to register the `phase4` marker.
  </action>
  <acceptance_criteria>
    <criterion>`ls tests/test_matched_n_*.py | wc -l` returns `7`</criterion>
    <criterion>`pytest tests/test_matched_n_*.py --collect-only -q` exits 0 (even if xfail)</criterion>
    <criterion>`grep -l 'pytest.mark.phase4' tests/test_matched_n_*.py | wc -l` returns `7`</criterion>
    <criterion>`grep -l 'D-01a' tests/test_matched_n_se_inflation.py` returns 1 match</criterion>
    <criterion>`test -f tests/fixtures/matched_n/README.md`</criterion>
  </acceptance_criteria>
  <verify><automated>pytest tests/test_matched_n_*.py -x --collect-only -q 2>&1 | tail -20</automated></verify>
  <done>All 7 xfail stubs collected by pytest; markers wired; D-IDs referenced in docstrings for traceability.</done>
</task>

</tasks>

<threat_model>
Configuration tampering -> schema validation + checksum config in CI. Seed leak -> seed formula deterministic and stored in config. LSF write-path -> bootstrap_fits_root confined to `/rs1/researchers/c/ckclinto/`. No high-severity blockers.
</threat_model>

<verification>
Run `snakemake --dry-run -s Snakefile all_matched_n` and confirm skeleton resolves. Run `pytest tests/test_matched_n_*.py --collect-only -q` and confirm all 7 stubs collected.
</verification>

<must_haves>
- [ ] config/matched_n.yaml parses with schema validation
- [ ] src/snakemake/rules/matched_n.smk loads without errors (snakemake --list)
- [ ] 7 test files exist with xfail stubs (one per D-01/D-02/D-04/D-05 unit)
- [ ] bmi.AFR ingestion path documented in data_access.md with MVP/AoU/Pan-UKBB tiered fallback
- [ ] config contains: bootstrap_n=100, seed_base=1000, h7_reduction_threshold_pp=20, concordance_threshold=0.8
- [ ] Snakefile includes matched_n.smk
- [ ] data_access.md contains MVP phs002453
</must_haves>

<success_criteria>
All config parameters locked per D-01/D-02/D-03/D-04. Schema validates. Snakemake skeleton loads. 7 Wave 0 test stubs collected. bmi.AFR fallback documented.
</success_criteria>

<output>
After completion, create `.planning/phases/04-matched-n-cross-ancestry-concordance/04-01-SUMMARY.md`
</output>
