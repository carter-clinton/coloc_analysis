---
context: phase
phase: m3-aou-afr-ld-panel-build
plan: 3
task: null
total_tasks: null
status: paused
last_updated: 2026-06-02T23:00:00.000Z
---

# Resume point — RE-FIRE Gate B (driver-collect fix landed; needs the live proof)

**One-line:** Gate A PASSED. Gate B's two blockers (driver OOM + an indefinite
driver-collect stall in `load_qc_cohort`) are both FIXED and pushed. The fix is
verified NCSU-side (tests green, Track-4 guard intact) but the LIVE proof needs a
Gate B re-fire — **Carter's trigger**, ~$1-3.

## What changed this session (2026-06-02 LATE — live Gate A/B fire + /gsd-debug)

- **Gate A = PASS.** Synthetic `range_matrix_table(50k,2k).repartition(2048).checkpoint`
  under cores=1/5g wrote a **768 MB populated** MT — the m3-W1 empty-MT
  write-finalization catastrophe **did NOT reproduce** on RW 2.0. (PASS = pure-write
  path not reproduced, NOT full validation.)
- **4 Verily env deltas** baked into AOU-0.5/AOU-1 (`dcc19ac`) → a fresh clone+pull
  needs no manual env setup: HOME=/home/dataproc (portable sys.path), WORKSPACE_BUCKET
  + WGS path setdefault, controlled WGS bucket is REQUESTER-PAYS (CUSTOM RP in the
  PYSPARK lever). See `[[project_aou_dataproc_hail_install]]`.
- **Gate B blocker #1 — driver OOM** on the default **n2-standard-2 master** →
  FIXED by an **n2-standard-16 master** (master size is create-time only).
- **Gate B blocker #2 — driver-collect stall** in `load_qc_cohort` (the real bug),
  root-caused + FIXED via `/gsd-debug` (HIGH confidence): the pre-write
  `mt.repartition(2048)` (`shuffle=True`) after `split_multi_hts` built a Spark
  RangePartitioner by **sampling keys across all partitions** → a driver-side
  `SpillingCollectIterator` (jstack: `TableValue.mapRows`) funneled ~2,077 partitions
  to the master (38 min, ~1.4s driver CPU, 0 executor threads). **Fix** (Hail-team
  "repartition after write, not before"): drop the pre-write repartition; rebalance on
  the post_split checkpoint **read-back** via `read_matrix_table(_n_partitions=…)`.
  New pure helper `_post_split_read_partitions` + `_COHORT_TARGET_PARTITIONS=2048`.
  Commits **`e23c081`** (code+tests) + **`0f92428`** (debug doc). tests/m3 **112 passed
  / 27 skipped**; 3 RED-first guards; Track-4 guard byte-identical. **This bug would
  also have crippled the full genome-wide fire.**
- **All pushed** to `origin/m3-W2-aou-deltas` (fix + debug doc + this resume refresh).
- **$0 state:** the stalled Gate B cluster was deleted after forensics were preserved
  to `gs://<WORKSPACE_BUCKET>/ld/_forensics/`. Nothing running.

## Next action — RE-FIRE GATE B (Carter holds the trigger + ~$1-3)

**Three run-sheet deltas vs the prior attempt — all matter:**

1. **Cluster:** "Hail Genomics Analysis" Dataproc, **n2-standard-16 MASTER** (NOT the
   default n2-standard-2 → OOM-kills the driver) + **4× n2-standard-16 workers**,
   non-preemptible, image Dataproc 2.2 if selectable.
2. **Pull the fix (mandatory):** `cd ~/coloc_analysis && git checkout m3-W2-aou-deltas && git pull origin m3-W2-aou-deltas`
   → confirm `git log --oneline -1` shows the resume-refresh / `e23c081` lineage
   (the clone is ephemeral; without the pull it runs the stalling code).
3. **Cheap tier:** open `AOU-1-chr22-smoke_template.ipynb`, **set
   `INTERVAL='chr22:16000000-18000000'`** (it DEFAULTS to whole `chr22` — the 2,077
   partitions in the stall ≈ whole chr22, so the default is the expensive tier).

Then: fresh kernel → Cell 1a FIRST → 1b → YARN-verify cell → run through
`load_qc_cohort`. **Watch the Spark UI:** expect **active executor threads +
progressing stages** (not a quiet driver); the post_split checkpoint should complete
in minutes; no driver jstack in `SpillingCollectIterator`.

- **PASS** (no stall; cohort MTs non-empty) → Gate C decision; I finalize the
  `/gsd-debug` session (resolve + knowledge-base entry).
- **FAIL-still-stalling** → the `/gsd-debug` session is still OPEN at human-verify;
  re-investigate (paste the new jstack / Spark UI).
- **FAIL-empty** → 1000G + read `_forensics/<phase>_capture.json`.

**Authoritative run sheet:** `.planning/quick/260601-u1b-harden-chr22-smoke-into-tiered-cheap-fir/TIERED-VALIDATION-RUNBOOK.md` §0 + §2.

## Required reading (in order)
0. `.planning/debug/m3-gateb-load-qc-cohort-driver-collect.md` — the driver-collect root cause + fix + RED evidence. **Start here.**
1. `.planning/HANDOFF.json` — full state (coordinates, cluster_sizing, completed/remaining, decisions, next_action).
2. `TIERED-VALIDATION-RUNBOOK.md` §0 + §2 — cluster provisioning + Gate B run sheet.
3. Memory `[[project_aou_dataproc_hail_install]]` (MASTER/DRIVER SIZING + 4 env deltas) and `[[project_state]]` (top section, 2026-06-02 LATE).

## Decisions / constraints (do not re-litigate)
- **Master = n2-standard-16 minimum** (default n2-standard-2 OOMs the driver; create-time only).
- `load_qc_cohort` rebalances on the checkpoint **read-back** (`_n_partitions`), never a pre-write `repartition(shuffle=True)`.
- Cluster = **Hail Genomics Analysis** (HAIL software-framework), n2-standard-16, NON-preemptible; use the env's pre-installed Hail (0.2.135), no pip-pin.
- 1000G AFR = smoke-failure safety net ONLY (`[[feedback_no_1000g_ld_pivot]]`).
- Carter holds **every** AoU launch/compute/$ trigger. Explicit git paths only (never `git add -A`/`.`).

<blockers>
None. (All AoU resources deleted -> $0; the Gate B re-fire is a Carter-trigger compute fire, not a blocker. The /gsd-debug session is OPEN at human-verify pending that re-fire.)
</blockers>
