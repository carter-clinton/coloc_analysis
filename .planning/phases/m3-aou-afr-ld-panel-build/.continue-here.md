---
context: phase
phase: m3-aou-afr-ld-panel-build
plan: 3
task: null
total_tasks: null
status: gate_c_passed_wave2_scoped
last_updated: 2026-06-04T20:00:00.000Z
---

# Resume point — GATE C PASSED + WAVE 2 SCOPED/AUDITED. → read WAVE-2-PLAN.md.

**Wave 2 is fully scoped + pre-fire-audited NCSU-side (2026-06-04). The authoritative
plan, gate sequence, run sheet, and open decisions are in
[`WAVE-2-PLAN.md`](./WAVE-2-PLAN.md) — read that first.** Headlines:
- Scope = `config/ld_regions.tsv` (161 regions × AFR+EUR = **322 cells**, NOT the 13 curated);
  ~1,117 cluster-h; "Wave 2" = the phase-plan's "W4 production" fire; **dev-10 is its gate**.
- **Pre-fire audit fixed the one fire-crashing bug** (HIGH-1: `compute_region_ld` routed 86/322
  cells with span>10 Mb into a `to_numpy()` driver OOM — incl. the first dev-10 region;
  fixed `c6c32b3`, span-veto + 3 guard tests, suite 128 passed).
- **All NCSU-side pre-fire fixes now LANDED** (`c11949e`, Carter greenlight): HIGH-3 accepted+
  documented (xlarge LD is 50-Mb-banded — downstream caveat); MED-4 (A.3 .bm populated-
  validation), MED-5 (sidecar-upload failure), MED-6 (idempotency validity) all fixed + tested;
  A.2/A.3 coverage tests added. Suite 129 passed / 33 skipped.
- **GATE 1 now needs only 3 Carter-only items:** cluster preset (256 vs 384 vCPU), cost/credit
  confirmation (~1,117 cluster-h), CDR pin. Plus export-MAF + self-ID-escalation policy.
- **HARD GATE 0 before ANY compute: AoU egress classification ruling must be in hand.**

(Original Gate C PASS detail retained below for provenance.)

---

# Gate C PASS (provenance) — all 3 cohorts. Wave 2 greenlit.

**One-line:** Gate C (whole chr22) PASSED on re-fire #2 — all 3 cohort MTs fresh + non-zero,
du-floor cells report real GB at `entries/rows/parts/`, `cohort_summary_m3_chr22.tsv` = 3
non-zero rows. Both bugs proven fixed live: the QC-ordering sample-axis collapse
(`80d0a00`/`0e3ef2a`) and the du-floor `+ _suffix` phantom-path false-positive (`1e00851`).
The cheap tiered validation did its job — **no 1000G pivot**. Cluster torn down → $0.
HEAD = `1e00851` on `origin/m3-W2-aou-deltas` (all pushed). Three debug sessions closed →
`.planning/debug/resolved/`.

## Gate C PASS evidence (cross-verified vs on-disk _SUCCESS + cohort_summary, 2026-06-04 22:46:52Z)

```
cohort                n_samples  n_variants  final MT (_SUCCESS present)              du @ entries/rows/parts/
AFR_pca_chr22          74,059     283,854    gs://.../ld/mt_afr_qc.mt                 ~22.4 GB
AFR_pca_selfid_chr22   74,059     283,854    gs://.../ld/mt_afr_pca_selfid_qc.mt      ~22.4 GB
EUR_pca_chr22         221,624     157,555    gs://.../ld/mt_eur_qc.mt                 47,589.6 MB
```
- Ordering fix held on every path (selfid kept the full 74,576-col axis; AFR ~517-sample
  call_rate trim, not a collapse). `_assert_checkpoint_nonempty` silent; Cell 6 AFR∩EUR=∅ OK.
- The "EUR wedge" mid-run was a FALSE ALARM (a stale-scrollback + empty-grep-diff misread);
  EUR's heavier final aggregate_cols gather (221K samples) completed in ~4 min.

## NEXT — Wave 2: full-genome LD reference panel rebuild (Carter-trigger; the big fire)

The chr22 smoke is the proof-of-correctness; Wave 2 runs the same `load_qc_cohort` +
`compute_region_ld` genome-wide (per-region LD per AOU-LD-PIPELINE.md). Entry point:
- Use the **production** notebook `AOU-1_template.ipynb` (NOT the chr22-smoke template) with
  the genome-wide INTERVAL / per-region region set; and `AOU-2_per_region_ld.ipynb` for LD.
- Cluster: Gate-C-class or larger (24× n2-standard-16 = 384 vCPU was fine for chr22; size up
  for genome-wide); n2-standard-16 master; non-preemptible; "Software to install = Hail".
- Before firing Wave 2, do a planning pass (/gsd-plan-phase or a Wave-2 run sheet): region
  set, per-region radius/compute budget (RESEARCH.md §radius — O(n_var²) OOM risk on
  to_numpy for large regions), export/egress plan (44 export requests), cost estimate.

### WAVE-2 WATCH-POINTS (from Gate C — carry forward, not blockers)
1. **EUR driver-side collectDArray gather** scales with samples (221K) × partitions. Fine on
   chr22; genome-wide has far more partitions. Watch driver gather time on the first
   genome-wide EUR fire; if it dominates, checkpoint sample_qc before the het aggregate or
   shrink the gather footprint. (resolved/m3-gateb-load-qc-cohort-driver-collect.md +
   resolved/m3-gatec-sample-callrate-ordering-collapse.md closeouts.)
2. **FINAL checkpoint is not interval-isolated** — `_qc_checkpoint_uri` has no interval
   suffix, so chr22 smoke finals sit at the production `mt_{ancestry}_qc.mt` paths (Wave 2
   genome-wide will overwrite them — intended, but be deliberate). A load_qc_cohort design
   question if smoke/production isolation is ever wanted.
3. `compute_region_ld` Path A.1 `to_numpy()` OOM risk on large regions (RESEARCH.md §radius).

## Decisions / constraints (do not re-litigate)
- Canonical QC order = split → **variant_qc → sample_qc** (call_rate over QC-passing variants).
- 500K nano guard keys on the RAW pre-variant-QC count (DEC-2026-06-04).
- du-floor guards measure the un-suffixed `_qc_checkpoint_uri()` path at `entries/rows/parts/`.
- A cheap-tier PASS that SKIPS a check does not validate it ([[feedback_skip_guard_masks_not_fixes]],
  [[feedback_cheap_tier_fail_not_pivot]]). 1000G stays smoke-fail safety net only.
- Push AoU-side files to origin before teardown. Carter holds every AoU $ trigger.

<blockers>
None. Gate C PASSED; m3 cohort-build pipeline is correctness-proven on chr22 + pushed (HEAD
1e00851). Wave 2 is the next compute milestone (plan first, then Carter-trigger fire).
</blockers>
