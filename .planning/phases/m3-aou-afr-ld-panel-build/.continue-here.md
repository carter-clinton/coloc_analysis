---
context: phase
phase: m3-aou-afr-ld-panel-build
plan: 3
task: null
total_tasks: null
status: cohort_banked_gate15_complete_durablefix_phase2_item1_landed_gate1cost_and_chr22smoke_pending
last_updated: 2026-06-11T15:30:00.000Z
---

> **2026-08-03 (★ LATEST ★ — SESSION CLOSE: m3-04b LANDED; `tests/m3` 444P/31S/0F; ⚠⚠ the `AFR_aou` panel is UNREACHABLE from `run_finemap` — DO NOT FIRE):**
>
> **Nothing running. $0. NO perimeter contact all session.** `origin == local == f038ce0`; remote is SSH (no PAT). No AoU fire, no cluster, no orphan jobs.
>
> **⚠⚠ DO NOT FIRE THE ~11-DAY LOOP.** `REGION_SAFE_TO_ID` (`Snakefile:47-63`) is built ONLY from `config/regions_curated.csv` (12 curated slugs), NOT from `config/region_id_mapping.tsv` (276 M2 rows) → `resolve_ld_path` asks for `AFR_aou/FTO_16q12.rds`, **misses**, and **silently falls through to `AFR_1kg`**. Landing all 276 `.rds` would change **nothing**, and the fallback chain is designed to fall through so **nothing warns you**. Verified 3 ways (`grep -c FTO_16q12 config/region_id_mapping.tsv` == 0; curated == 13 lines; mapping == 277). The `finemap.smk:118-123` comment asserts the translation exists; it never did — though the crosswalk **does** resolve on real inputs (11/12; `FTO_16q12 → m2_region_00067`). Also **123 of 276** ids (every `__subNN`) are excluded by the `m2_region_\d{5}` ingest wildcard. **FIX = m3-04c Task 1.**
>
> **★ m3-04b LANDED** (`d7dfa67` T1 + `0cae502`/`37c51df` T2 + `f038ce0` docs). T1 = `assemble_occlusion_catalog.py`, the FIRST production caller for the four zero-caller m3-07 functions. T2 = the lockstep seam (`occlusion_lockstep_cli.py` + `m3_occlusion_lockstep.smk`) wiring BOTH `run_finemap.input.sumstats` AND `input.variants`, AFR-gated so EUR/Track-A strings stay byte-identical. **`tests/m3` 420P/31S/0F → 444P/31S/0F**, re-run by the orchestrator at HEAD (422.66s). All 7 pinned files 0-line diff.
>
> **⚠ Plan wrong twice (both fixed):** `envs/m3-r-ld.yml` lacked pandas (added `pandas>=2.2` there, NOT to `python_stats.yml` which backs a dozen built rules); `pipeline.schema.yaml` is `additionalProperties:false` so the new `occlusion_lockstep:` block needed a schema entry or every Snakemake run dies at `validate()`. **⚠ One existing test edited** — `test_production_boundary_documented` REPLACED not RELAXED (all original assertions kept + 3 new; the flipped token was mutable state, not a contract; diff read and confirmed). **⚠ D-04b-01 NOT FIXED** (modules 0-diff-pinned): `bmi.AFR…tsv.bgz` stores POS as float string `5982778.0` → `int()` raises in `_canonical_key` in BOTH frozen modules → scan scores rs182965575 **6/9 not 7/9** (7/9 is correct) and the drop **keeps** the row = silent under-drop with a clean `n_dropped==0`. No blast radius on m3-04b (`stem=r"...\.AFR"` matches only asthma/stroke/t2d, all integer-POS). Fix before running either against all 9. 6 of 9 `.tbi` are stale.
>
> **⚠ OSF POSTING GATE OVERRIDDEN** by Carter for the Check-2 redefinition — logged as a DISCLOSED deviation (`3e7a01a`), and deliberately NOT under the 2026-05-05 "timing not content" defense, because 2b removes a threshold. Two obligations survive: post the amendment-update, and cite **no** redefined Check 2 as "passed" until it exists.
>
> **NEXT = execute m3-04c** (`depends_on:["04b"]` SATISFIED). T1 reachability + T2 de-stale ingest/convert + egress planner on the EXISTING `ld_egress_bundle.py` — both autonomous/$0/NC-State. T3 = terminal Carter gate. ⚠ Only the `resolve_ld_path(region_id=)` argument may change — NOT `run_finemap.params.region_id`.
>
> **Resume = `.planning/HANDOFF.json` (2026-08-03) + STATE.md 2026-08-03 block + this block.** **SUPERSEDES the 2026-07-16 block below.**

> **2026-07-16 (SUPERSEDED by the 2026-08-03 block above — SESSION CLOSE: ★ m3-07 IS CODE-COMPLETE — 07c EXECUTED on Carter's go; full `tests/m3` = 0 failed / 420 passed / 31 skipped, NO REDS AT ALL for the first time in this wave):**
>
> **Nothing running. $0. NO perimeter contact this entire session** (everything NC-State, code-only). `origin == local == e9be72e`; remote is SSH (no PAT). No AoU fire, no cluster, no orphan jobs — one stray background waiter of mine was found and killed at close.
>
> **★ m3-07c LANDED** (`c475da7` T3 + `ed3e122` T4) — authorized by Carter's **"Proceed"** in direct reply to *"your explicit go is the only thing gating it."* NEW: `src/python/occlusion_present_rate_scan.py` (`scan_present_rate`; 6 RED → **6 passed**) + `src/python/drop_occluded_from_sumstats.py` (`drop_occluded_from_sumstats(sumstats_path, manifest_path, out_path) -> dict`, **file-in/file-out**; 9 RED → **9 passed**). Built GREEN against the 07a REDs — **`tests/` 0-line diff: no test was edited to force green.** Full `tests/m3` **15F/405P/31S → 0F/420P/31S**. `finemap.smk` UNTOUCHED (**m3-04 consume-wiring stays a DISCLOSED deferral**). Frozen contracts 0-line diff ALL session; neither new module references `condition_ld_matrix`/`nan_to_num` (**m3-06 stays HELD**). **The real 9-file genome-wide scan was NOT run** — GATED INTEGRATION step, needs the perimeter.
>
> **⚠ THE ONE THING GREEN TESTS COULD NOT PROVE:** the RED does **not** pin T3's key **canonicalization**. The real producer emits `chr` as the **string `'1'`**; the RED keys on the **int `1`**. Echoing the caller's tuple back verbatim **passes all 6 tests** and would have made `enrich_occlusion_manifest` match **ZERO** liftable rows (the `63bdb59` guard would then have RAISED at integration — defense-in-depth working, but still a build-time bug). T3 canonicalizes on both paths; re-proved end-to-end: `scan_present_rate([('1', 5982778)])` → keys `[(1, 5982778)]` → feedable to `enrich_occlusion_manifest(present_rate=)`; record `{n_traits_present: 2, n_traits_scanned: 3, present_rate: 2/3}`.
>
> **FOUR PRE-EXISTING DEFECTS ALSO CLOSED** (none caused by the resume; `src/` untouched by it): **panel-column test-vs-test contradiction** `957d5a1` (UAT 14/14; the 07a RED preserved, verified PASSED-not-skipped; the call was made by the AGENT on byte-verified precedent `1a9d170`, **not** a Carter ruling — revertible in 1 line); **the scan→enrich seam key contract that BLOCKED 07c** `63bdb59` (the 07a RED was **RIGHT**, the SHIPPED 07b consumer was **WRONG**; found by a 4-agent `/assess-blast-radius` sweep **BEFORE** 07c was built — it would have silently blanked the rs182965575 "present in 7/9" evidence across all 276 regions with both suites green); **panel-TSV header guard** `fe375e7`; **P3** `ff8cc47` — the gsutil-blip bucket overwrite that would have **silently** destroyed banked provenance mid-fire (root cause: ONE helper serving TWO callers with **opposite** failure-safety requirements → see [[feedback_failsafe_default_is_caller_relative]]). Plus the **07c PLAN reconciliation** `8d4087a` — the handoff's *"07c's plan has no drift"* was **STALE AND WRONG for BOTH tasks**; T4 slipped a **name-only** check. **Rule baked: GREP THE NAME, THEN READ THE ASSERTIONS.**
>
> **⚠ TWO FLAGGED, NOT ACTIONED** (pre-existing `63bdb59` consumer behaviour — decide when the Angle-1/3 catalog lands): `present_rate` is never persisted as a manifest column (derivable as k/n), and `traits_present` **serializes as a stringified list** (`"['bmi','ldl']"`) so a catalog reader gets a `str`, not a `list`.
>
> **NEXT = the loop re-fire arc. ALL CARTER'S; ALL need the AoU perimeter (NOT reachable from NC-State):** (1) **OPERATIONAL** — rotate/delete the stale `gs://` panel TSV BEFORE firing: `gsutil stat <panel-uri>`; if present, `gsutil cat <uri> | head -1 | tr '\t' '\n' | wc -l` must be **9**, else `gsutil rm`. ⚠ **"0/276 banked" is measured in `.npz` and does NOT evidence its absence** — the `.npz` (not the TSV) gate the resume skip, and the June/July fires appended `status=error` rows unconditionally at `:808` on 7-col (Jun 28-30) and 8-col (Jul-02 `2d23d67`) code. Zero risk (only error rows = no data value), zero compute cost. (2) The **gated real-`.bim` validation** — ⚠ open **0- vs 1-based index-origin** question on `_REGION1_REAL_WINDOW_OCCLUDED_ROW_INDICES`. (3) A **region-1 re-run** must validate. (4) The **~11-day billed fire** (Carter's trigger; liveness = the GCS `.npz` listing climbing to **276** — NOT the kernel light, NOT `_SUCCESS`). AoU VM **STOPPED-not-deleted** (holds `/home/jupyter/afr_cohort`); `.npz` **0/276**.
>
> **Resume = `.planning/HANDOFF.json` (2026-07-16T01:00Z) + `.planning/STATE.md` 2026-07-16 block + this block.** **SUPERSEDES the 2026-07-15 block below.**

> **2026-07-15 (SUPERSEDED by the 2026-07-16 block above — m3-07a COMPLETE+VERIFIED and m3-07b LANDED; the occlusion span-filter + provenance manifest are REAL CODE; PAUSED before 07c; the test-vs-test contradiction it flags is now RESOLVED via `957d5a1`):**
>
> **Nothing running; $0; no perimeter contact.** `origin == local == a76ebe5`, all 5 tags on origin. **Remote is now SSH** (Carter registered `~/.ssh/id_rsa` on GitHub 2026-07-15) — pushes need **no PAT**; verify with `ssh -T git@github.com`.
>
> **m3-07b LANDED** (`7734725`/T1 + tag `m3-07b-W7-T1`; `0473b6a`/T2 + tag `m3-07b-W7-T2`; `a76ebe5` docs). NEW: `src/python/occlusion_span_filter.py`, `src/python/occlusion_manifest.py`. MODIFIED: `aou_ld_panel.py` (`build_plink_ld_command(exclude=)`) + `run_native_ld_panel.py` (`process_region` reorder — raw-window read + filter BEFORE plink, `--exclude` before `--r` — plus the `n_dropped_occluded`/`n_dropped_monomorphic` split). **Full `tests/m3` `47F/363P/31S` → `16F/394P/31S` = 32 newly GREEN**; **15 of the 16 remaining are 07c's unbuilt modules** (`ModuleNotFoundError`) = EXPECTED. Region-1 known-answer reproduced EXACTLY `{1980475, 5733487, 5922718, 7492693, 8375822}`; the tangle collapses to the single **5922718** drop attributed **UPSTREAM** to `DEL 5922716`; the excluded `.npz` is **NaN-free** and passes `content_verify_npz`. **Frozen contracts VERIFIED 0-line diff across the ENTIRE session** (`plink_ld_to_npz.py`, `ld_npz_to_rds.R`, `condition_ld_matrix.py`); `exclude=None` argv byte-identical to pre-07b; **no variant position hardcoded** (AST-verified). NaN→0 stays DEAD. GPFS hazard did NOT recur on 07b.
>
> **⚠ DO FIRST — THE ONE OPEN DECISION (a CARTER CALL, deliberately NOT taken).** Two tests contradict; no impl satisfies both; the executor correctly **refused to edit either**. (a) `test_run_native_ld_panel.py::test_panel_columns_include_n_dropped_occluded:1589` (**07a RED**) requires `n_dropped_occluded` in `drv._PANEL_COLUMNS`. (b) `test_run_native_ld_panel.py::test_panel_tsv_append_resume_safe:392` (**PRE-EXISTING**) pins `list(df.columns)` to **exactly 8** columns without it. `_append_panel_row_local` writes `columns=_PANEL_COLUMNS` → 9 columns → **(b) FAILS** (that suite = `1 failed / 51 passed`). **Diagnosis:** an OMISSION in the 07a RED — precedent **`1a9d170`** added the previous panel column RED-first **AND** updated the column-list assertion in the *same* commit. **Proposed fix (1 line, NOT applied):** add `"n_dropped_occluded"` at index 7 of that expected list. **Do NOT drop the column** — that fails the 07a RED and discards the per-region occlusion provenance `osf.io/az52u` pre-registers. Do not let an agent "fix" this by weakening the RED.
>
> **m3-07a COMPLETE + UAT-VERIFIED 11/11** (`46dd661`). Task 1 reconciled to the REAL posted amendment (`e10c893` — the placeholder file/tag NEVER existed); Wave 0 RED `296157a` + tag `m3-07a-W7-T-WAVE0`. **`/assess-blast-radius` then found the RED CONTRACT itself defective and FIXED it (`a1e8693`) BEFORE 07b was built against it** — BLOCKER: `occlusion_order` demanded `"second_order"` for snpC, **inverting** the byte-verified verdict (pair 3 IS a direct `ref_span_overlap`) and **underivable** from a genotype-free Stage A → the only route to green was **hardcoding position 5922718** = a false provenance label across all 276 regions (T-m3-07a-02 realized, arrow reversed); HIGH: the gated oracle compared **bp vs ROW INDICES** so it could never pass a correct detector; +3 MEDIUMs (strict-left `<` vs `<=`, multi-occluder `set()`-hidden duplicates, whole-key egress guard) + an untested producer→consumer seam.
>
> **PRE-EXECUTION CATCH (`f3b79fe`):** 07b's PLAN contradicted the corrected RED **3** ways (edges as `list[dict]` → unhashable → `set(edges)` TypeError; a disjoint/second-order edge the RED forbids; `occlusion_order (direct|second_order)`) — same root cause, living in the plan. **Always check a PLAN against the RED before spawning an executor.**
>
> **OSF gate CLEARED + FULLY RECORDED — no open items.** GUID **`trsx5`** (osf.io/az52u/files/trsx5). Append-only commitment **VERIFIED** (`trsx5` 1 revision; `tcujq` still 1 revision, unmodified → separate new file, NOT a re-version → **no posting deviation**). ⚠ `tcujq` (`Prereg_Phase1_amendment3.md`) is the **2026-07-04 NaN→0 amendment the update WITHDRAWS** — it was offered for the `trsx5` slot and **refused**.
>
> **NEXT:** (1) decide the panel-column conflict; (2) **07c** (present-rate scan + lockstep drop; `depends_on [07b]` now SATISFIED) **only on Carter's explicit go**; (3) the **gated real-`.bim` validation** (needs perimeter; ⚠ open **0- vs 1-based index-origin** question on `_REGION1_REAL_WINDOW_OCCLUDED_ROW_INDICES`); (4) the **loop re-fire** (~11 days, **costs money**; liveness = the GCS `.npz` listing → 276, NOT the kernel light, NOT `_SUCCESS`). AoU VM STOPPED-not-deleted; `.npz` 0/276; m3-06 HELD. Resume = `.planning/HANDOFF.json` (2026-07-15T18:00Z) + STATE.md top block. **SUPERSEDES the 2026-07-10 block below.**

> **2026-07-10 (SUPERSEDED by the 2026-07-15 block above — m3-07 PLANNED (07a/07b/07c) + OSF occlusion-exclude amendment-update POSTED+RECORDED → hard gate CLEARED; execution NOT started, Carter chose 07a-then-pause then said stop+close):**
> **Nothing running NC-State; $0.** `/gsd-plan-phase m3-07 --auto --chain` ran to completion: research `a795fac` (#1 crux resolved — panel `.bim` A2 carries the full multi-char indel REF, so a deletion span `[POS, POS+len(A2)−1]` is computable from the window `.bim` alone; no new data plumbing) → validation `7f701a8` → plan v1 `7272d03` → **checker-split into 3 plans** `0d3e37e` → gsd-plan-checker **VERIFICATION PASSED iter 2** (5/5 REQs). Plans: **m3-07a** (OSF gate + Wave 0 RED scaffolds, `wave 1`, `depends_on []`, `autonomous:false`) → **m3-07b** (T1 span-filter `--exclude`-before-`--r` + `n_dropped_occluded` + T2 provenance manifest, `wave 2`, `depends_on [07a]`) → **m3-07c** (T3 present-rate scan + T4 reusable lockstep drop; m3-04 consume-wiring deferred — `finemap.smk` m3-04-W4 STALE/pending-replan; `wave 3`, `depends_on [07b]`). **⭐ OSF HARD GATE CLEARED:** Carter posted `osf-amendment-afr-occlusion-exclude-UPDATE-2026-07-10.md` to osf.io/az52u **2026-07-10T13:32:22Z**; I recorded it in-repo (`ac4c990`: project copy + tcujq superseded-by pointer + `osf_deviations.md` entry + STATE block) and **tagged `AFR-OCCLUSION-EXCLUDE-OSF-UPDATE-POSTED-2026-07-10`**. WITHDRAWS 2026-07-04 NaN→0; pre-registers exclude-in-lockstep + provenance manifest; retains r3 PSD methods+λ. **Seth design input folded (`f1b167a`):** predicate `POS < variant_POS ≤ POS+len(REF)−1` (5/5); region-1 known-answer occluded set `{10328,44784,46714,59097,66730}`; 7-deletion inventory `60/29/7/31/31/17/29 bp`; pair-4 = SNP 5922718 occluded by UPSTREAM DEL 5922716 (not downstream 5922724); Seth's prototype = READ-ONLY executor reference, not for commit. **DECISIONS:** did NOT auto-advance to `/gsd-execute-phase` (can't resolve custom `m3-07a` naming → `phase_found:false`); execution = adapted **planner→executor pattern** (spawn `gsd-executor` on each PLAN.md, like m3-06); Carter chose **07a then pause** before 07b/07c, then stop+close (no executor fired). **⚠ OPEN (do FIRST on resume):** 07a Task 1 still names a PLACEHOLDER amendment file `osf-amendment-panel-occlusion-exclusion-2026-07-10.md` + tag `PANEL-OCCLUSION-...` in `files_modified`/`must_haves`/`<verify>` (its `<done>` is correct); the `<verify>` greps anchors `4543dcf4`/`42d70167` the real doc lacks → reconcile to the real filename+tag OR treat Task 1 as pre-CLOSED and have the executor skip its proxy verify → go to Task 2 (Wave 0 RED scaffolds; function-local imports; env `smoke_dev/bin/pytest`). Also: OSF file GUID/URL to fill; 07c dupes `REQ-PUBLIC-DATA-ONLY`. **NEXT = execute 07a (then PAUSE per Carter). No perimeter/loop re-fire (loop STOPPED, `.npz` 0/276; span-filter is the precondition). m3-06 HELD; `condition_ld_matrix.py` FROZEN; raw-panel NaN-raise intact.** Resume = `.planning/HANDOFF.json` (2026-07-10T16:10Z) + STATE.md 2026-07-10 top block. SUPERSEDES the 2026-07-08 block below.
>
> **2026-07-08 (SUPERSEDED by the 2026-07-10 block above — SESSION CLOSE: exclude-vs-flag RESOLVED = exclude-in-lockstep + provenance (Seth-endorsed; policy doc byte-verified `8f36fdf`); HOLD-THEN-PUSH-ALL-FOUR pending the verdict base64; nothing running NC-State):**
> **Nothing running server-side, $0. Local HEAD `8f36fdf` = 2 commits AHEAD of origin `bd4d50a`, HELD unpushed ON PURPOSE (Carter order = hold-then-push-all-four).** THIS SESSION (docs-only, read-only, no perimeter): ran Seth's exclude-vs-flag **hinge check** — the panel↔sumstats join is `(CHR,POS)`-only (`snp_id_bridge.R`), panel GRCh38 / sumstats GRCh37 (`ld_npz_to_rds.R` liftover); lifted the committed 3-record tangle GRCh38 chr1:5922716/5922718/5922724 → GRCh37 5982776/5982778/5982784 and scanned all `data/processed/sumstats_harmonized/*.AFR*.tsv.bgz` → the occluded SNP **`rs182965575` (GRCh37 chr1:5982778, MAF ~0.014) is PRESENT in 7/9 AFR sumstats** with real β/SE = Seth's "present" case (occluding deletion + 2nd occluded SNP ABSENT). **Committed (2 local, HELD):** quick-260707-w78 (`c4e0875`, `m3_region1_occlusion_hinge_check.md`) + quick-260707-x8e (`8f36fdf`, `m3_panel_occlusion_policy_decision.md` **byte-verified** body SHA-256 `42d70167…ab209ef`/5247 B == Seth anchor). **POLICY RESOLVED** (supersedes the verdict's open exclude-vs-flag section): exclude-in-lockstep across panel AND sumstats + a **mandatory load-bearing provenance manifest**; NaN→0 DEAD; panel-only-exclude UNSAFE (orphans sumstats-present rs182965575); scope = **upstream span-filter for all 276** + lockstep sumstats-drop at m3-04; OSF amendment-update = exclusion+provenance, never zeroing; new Seth requirement = report the genome-wide **present-rate per ancestry**. **⚠ HOLD-THEN-PUSH-ALL-FOUR:** do NOT push the 2 local commits until the 4th amendment doc — the geometry **VERDICT** `m3_region1_nan_geometry_verdict.md` (anchor SHA-256 `4543dcf4…`/5012 B; the 6/6 coordinate mechanism + full 6-pair/11-variant set) — lands as a 3rd commit; THEN one push takes all four docs to origin. **⚠ VERDICT BASE64 NOT YET ARRIVED** (the chat paste mangles its 6-row table → can't byte-match; checked message + filesystem). On arrival: `base64 -d` → `sha256sum` == `4543dcf4…` → stripped header → `tail -c 5012` re-verify → commit via `/gsd-quick` → **push all four** → verify origin==local. **STILL OPEN:** (a) verdict base64 → commit → push all four [TOP]; (b) pre-register the scoped OSF amendment-update BEFORE any fix lands; (c) the span-filter + manifest + present-rate build = a FUTURE `/gsd-plan-phase`; (d) AoU **loop-state** UNRESOLVED (in-perimeter data-layer check) — do NOT re-fire. **m3-06 STAYS HELD; `condition_ld_matrix.py` FROZEN.** Resume = `.planning/HANDOFF.json` (2026-07-08 headline) + STATE.md 2026-07-08 block. SUPERSEDES the 2026-07-07 block below.
>
> **2026-07-07 (SUPERSEDED by the 2026-07-08 block above — SESSION CLOSE: m3-06 conditioning wave LANDED then HELD; NaN mechanism RESOLVED = overlapping-deletion occlusion; verdict-commit pending base64; loop-state + 3 policy calls on Carter):**
> **(A)** m3-06-W6-ld-nan-psd-conditioning (999.1 §2-4) LANDED via autonomous plan→execute per Carter's `--auto --chain`, structured as a WAVE-in-m3 (his call, not a standalone phase): promote `20e3adc` → planner `205b03e` → plan-checker PASS → executor 6 commits HEAD `f147041` (`psd_utils.R` byte-identical r3 refactor 16/16 `identical()` → Track-A numerics UNCHANGED; `condition_ld_matrix.py`; `write_conditioned_ld_npz.py`; `tests/m3` 360/30; independently re-verified). **(B) SCIENTIFIC HOLD** (Seth peer review; I reproduced the arithmetic): the amendment's "pairwise-undefined r (0/0)" premise is REFUTED (pairwise-monomorphism ~1e-947..1e-3729 improbable) → CORRELATED missingness. Two egress-safe in-perimeter diagnostics (2×2 genotype tables + full-`.bim` REF-span geometry) RESOLVED the mechanism **6/6 = OVERLAPPING-DELETION OCCLUSION** (a deletion REF interval covers a SNP partner's POS → base absent on the deletion haplotype → uncallable = the ~100%-missing signature); 5 direct + 1 second-order (pairs 3+4 = one 3-record tangle), **0 same-position/mergeable**. ⟹ NaN→0 DEFINITIVELY dead (no "true r"); `bcftools norm -m +` fixes NONE; fix = **exclude-occluded / flag-locus WITH PROVENANCE, never zero**; SYSTEMIC (7 occluding deletions in region 1) → likely an **upstream panel-build span-filter for all 276**. m3-06 HELD landed-but-not-trusted; `condition_ld_matrix.py` FROZEN; raw-panel NaN-raise STAYS. Scientific-review MEMO committed byte-verified (`3516c18`, SHA `e82201147c`). **⚠ ONE open in-repo action:** commit the geometry VERDICT (`m3_region1_nan_geometry_verdict.md`, anchor SHA `4543dcf4`/5012 B) — BLOCKED (my chat-paste reconstruction 142 B short, table lost) → NEED a base64/fenced transfer to land it byte-verified. **⚠ On Carter (NOT auto-executed):** (1) exclude-occluded [join-safest] vs flag-locus; (2) upstream span-filter for all 276 [lean] vs per-region; (3) narrow pre-registered amendment-update [lean yes] — all vs the chr:pos:ref:alt harmonization key (I verified the join = `ld_npz_to_rds.R` + `refit_sh2b3_psd_regularized.R:106,137` + `snp_id_bridge.R`). **⚠ Loop-state UNRESOLVED** (Seth: STOPPED/PID 5170 killed; my inherited: running; loop ran on `2d23d67`, typo `2d43d67` fixed) → in-perimeter data-layer check; do NOT re-fire until the upstream policy is decided. Full state → `.planning/HANDOFF.json` (2026-07-07 headline) + STATE.md `2026-07-07 (★★ RESUME HERE ★★)` block. **NC-State idle, all pushed (`46f3c27`).**
>
> **2026-07-04 (SESSION CLOSE: 3 quick tasks landed + AFR OSF amendment RECORDED):**
> Three quick tasks this session (all committed; recording commit `a88c7c3`): **(vk9, `920ea11`)** Seth **Defect 1** — the live region-1 `window .bim has 0 rows` error was a **snplist-read RACE**, not logic; fix = `_needs_retained_subset` skip-when-no-drop + `expect_nonzero` bounded retry on the snplist read + conditional square caller (`run_native_ld_panel.py` only); TDD, `tests/m3` 336/30. **(wx2, `0f3c68b`)** expanded **ROADMAP 999.1** (NaN→0 + PSD conditioning) with Seth's resolved OSF finding + 6-step breakdown, PARKED. **(0uf, `a88c7c3`)** **the AFR NaN→0+PSD OSF amendment is POSTED + RECORDED** — Seth posted OSF file `tcujq` on az52u (auth. UTC 2026-07-04T04:14:46.635031Z; body `Date:` 2026-07-03 = disclosed 1-day deviation) + pasted the FILLED text **with SHA-256 anchors**; I VERIFIED byte-fidelity (`1a25a7ba`/12672 B + posted-body `43248aff`/8332 B) instead of fabricating, committed the byte-faithful amendment + deviations entry + coverage flag `D-AFR-NANPSD-OSF-COVERAGE: COVERED`, and **tagged `AFR-NANPSD-OSF-AMENDMENT-POSTED-2026-07-04` on `0f3c68b`**. **999.1 steps 2-6 UNBLOCKED** (conditioning under a future `/gsd-plan-phase`; 999.1 stays PARKED). **⚠ The AoU 276-region loop is STILL RUNNING on the VM (`2d43d67`)** — fixes are NOT on it, do NOT re-fire, do NOT restart the kernel; liveness = bucket `.npz` → 276 (perimeter-only). Full state → `.planning/HANDOFF.json` (2026-07-04 headline) + STATE.md `2026-07-04 (★★ RESUME HERE ★★)` block. **Nothing running NC-State-side.**
>
> **2026-07-03 (quick-260703-o0m landed; AoU loop STILL RUNNING, do NOT re-fire):**
> Seth Defects 3+4 landed NC-State-side (CODE ONLY): **(D3)** `read_square_bin` raises a NaN-specific error naming culprit variant rows BEFORE the symmetry check (corrected Seth's `.all(axis=1)`, empty on the real diagonal-1.0/sparse fingerprint); **(D4)** opt-in `--fail-fast` `RegionGateError` gate (default-off = resume-safe continue unchanged). TDD RED→GREEN, 5 commits (`28c70ff`/`b57d31e`/`ebceb43`/`12b86d6` + `5b8a51a` docs) + this handoff; full `tests/m3` **333 passed / 30 skipped**; **pushed to origin**. 17 GPFS-lost git objects recovered via `git hash-object -w`. **⚠ The AoU 276-region loop is STILL RUNNING on the VM (`2d23d67`) — the fixes are NOT on it; it MUST NOT be re-fired** (Seth Defects 1 snplist∩bim=0 + 2 true-NaN-source pending in-perimeter diagnostics). Full state → `.planning/HANDOFF.json` (2026-07-03 headline) + STATE.md `2026-07-03 (★★ RESUME HERE ★★)` block. **Nothing running NC-State-side.**
>
> *(historical, 2026-06-11 — predates the loop saga:)*

# RESUME HERE → cohort fully banked; durable-fix phase 2 ITEM 1 (read-side wiring) landed this session; GATE 1 cost + chr22 smoke are the open items

> **2026-07-02T20:20Z (SUPERSEDED — ⚠ THE LOOP IS NOT RUNNING: it was later STOPPED, banked 0/276, PID 5170 dead. This block's '🔥 LOOP FIRED + RUNNING' is HISTORY, not current state — do NOT act on it. Kept for the region-1 gate/diagnosis record; region-1 gate in flight; fix + hardening pushed `2d23d67`; VM STAYS n1-standard-32, NO respec):**
> **The 2026-06-30 "transient" diagnosis below was WRONG** — corrected 2026-07-01. Region 1's real, reproducible failure was a **symmetry-check failure**: ~11 **monomorphic (MAC=0-in-AFR)** variants make plink emit `NaN` LD → `NaN!=NaN` breaks `read_square_bin`. Proven by running `read_square_bin` on the intact 42 GB `.ld.bin` (deterministic RAISE) + a pinpoint (12 NaN across 11 rows, diag 1.0). DECISION (Carter) = **DROP MAC=0**.
> **FIX + HARDENING LANDED + PUSHED (`2d23d67`, origin==local; full `tests/m3` 321/30):** quick-260701-qcy adds `--mac 1 --nonfounders --write-snplist` to the SQUARE plink command (drops MAC=0 before `--r` → no NaN) + `_retained_window_bim` threads the plink `.snplist` so `n_var`/`.bim`/`.npz` align to the retained polymorphic set (`587c3d4`/`c56c715`). Hardening (`1a9d170`/`ed9cfd4`): H1 = a LOUD dup-SNP-id assertion (closes the one silent-misalignment path); H2 = `n_dropped_monomorphic` provenance (panel column + per-region stderr log). A 4-dimension **Fable-5 blast-radius** sweep + gsd-verifier (7/7) cleared it — downstream m3-04 `.rds`/SuSiE loaders align by **variant ID** so a retained subset is safe (and removes the NaN that would've broken Cholesky); banded/resume/egress/cost-model unaffected.
> **NOW RUNNING:** Carter fired step 4 — the 276-region loop is RUNNING server-side on VM `AoU_Jupyter_ComputeEngine_20260626b` (n1-standard-32/120 GB), nohup + `timeout 312h`, run log `~/native_ld_loop.log`; region 1 computing. Steps 1–3 (pull `2d23d67` + re-gate + `.bim` col-2 uniqueness `awk`) passed pre-fire. **Do NOT restart the kernel.**
> **GATE = region 1 (~2.5h):** PASS = bucket `.npz` 0→1 AND panel `m2_region_00001` `status==ok` AND `n_var` < 102,421 AND `n_dropped_monomorphic ≈ 11` AND a `dropped N monomorphic` log line AND no `not symmetric`/`Killed`/dmesg-OOM → **loop continues to 276** (~11 days, 2–3 day check-ins). FAIL → AoU agent kills + reports (`not symmetric` = residual NaN; `ambiguous variant id` = H1 dup-id assert → investigate the export). **Liveness = bucket `.npz` count → 276.** STOP-on-complete (stop ≠ delete) at 276 → `native-panel-recorded` → NCSU reconstructs the panel TSV → **m3-04 REPLAN**. **Authoritative resume = `.planning/HANDOFF.json` (2026-07-02T20:20Z) + `.planning/STATE.md` (2026-07-02 RESUME-HERE).** **SUPERSEDES the 2026-06-30 block below (whose "transient" call was WRONG).**
>
> **2026-06-30 (SUPERSEDED — the "NON-REPRODUCIBLE TRANSIENT" call was WRONG; real cause = monomorphic-NaN symmetry failure, see above) — FIRE #3 PROVED both OOM fixes (no OOM on 120 GB) but region 1 errored `n_var mismatch ... 0 rows`; retry GUARD landed `27af416`:**
> Fire #3 (PID 432, HEAD `1b45d43`, **n1-standard-32 / 120 GB** — the UI offered no 128 GB option and n1-standard-64/240GB busts the $500 cap; 120 GB clears the ~40 GiB matrix to ~168k vars) reached region 1: plink emitted the 102,421-var `.ld.bin` and the `.npz` **convert + `content_verify_npz` both ran with NO `dmesg` OOM → both OOM fixes (`3eac803`+`ff9e66f`) PROVEN on real data**. But region 1 errored `n_var mismatch ... window .bim has 0 rows`.
> **~6 read-only dig rounds PROVED no static defect** (src md5 `e99fd817`==committed; `_window_bim_n_var` uses `_chrom_match_key` both sides + byte-identical `a5a5f9f`→`1b45d43`, fire #2 passed it; manifest md5 matches, `chr` int64, no NaN, chr=int 1; same interpreter, pandas 2.3.2; `.bim` full+stable mtime Jun 27; the EXACT driver path **REPLAYS 102421==102421**). Float-chrom (`_chrom_match_key(1.0)`→0) proven possible but ruled out (runtime chr is int). **Conclusion: a one-off TRANSIENT short read of `bim_path.read_text()` at the instant plink finished the 42 GB write** — non-reproducible, no code bug. The loop is already resume-safe per region; "0/276" only because we `kill 432`'d it at region 1 to investigate.
> **GUARD (quick `260630-rn4`, DONE + GREEN):** `_window_bim_n_var_retry_on_zero` re-reads the `.bim` up to 3× when square-path window=0 but `bin_n_var>0`; recovery logs a loud `WARN`; persistent 0 still raises the **byte-identical** mismatch. T1 `bba9cf8` (RED) + T2 `27af416` (GREEN); full `tests/m3` **309/30**; banded/resume/`content_verify_npz` untouched.
> **RE-FIRE PENDING (guard landed, docs committed; remaining = PUSH):** PUSH origin `m3-W2-aou-deltas` + verify tip==HEAD (`[[feedback_push_ncsu_before_aou_clone_fire]]`) → AoU agent `git pull` (≥`27af416`) on the SAME **n1-standard-32** VM (**NO respec** — OOM concern resolved) → re-gate (`md5sum` ≠ `e99fd817` + grep `_window_bim_n_var_retry_on_zero`) → pre-flight region-1 ~102,421 → re-fire the SAME `loop_command` from region 1 (0 banked → clean restart) → proof `.npz` 0→1 + `df` flat + `status==ok`; a recurring transient now self-heals (WARN + npz still advances). **Authoritative resume = `.planning/HANDOFF.json` (2026-06-30T23:59Z block) + `.planning/STATE.md` (2026-06-30 RESUME-HERE).** **SUPERSEDES the 2026-06-29 block below.**
>
> **2026-06-29 (SUPERSEDED by 2026-06-30 above — the 128 GB respec is OBSOLETE; fire #3 ran on 120 GB and PROVED both OOM fixes) — RE-FIRED LOOP OOM-KILLED on region 1's `.npz` convert; banked 0/276 AGAIN (different bug); FIXED + committed `3eac803`; VM STOPPED by Carter; re-fire pending on n2-highmem-16 128 GB):**
> The 2026-06-28 re-fire (PID 6577, `a5a5f9f`) reached region 1; plink finished and emitted the **102,421-var `.ld.bin`** (the chr-prefix fix is **PROVEN GOOD**), but the Python `.ld.bin`→`.npz` convert **OOM-killed** (`anon-rss ~61 GiB` on the 64 GB VM). **Root cause:** `read_square_bin()` symmetry check `np.allclose(m, m.T)` builds several full `n_var²` float32 temporaries (~39 GiB each) on top of the 39 GiB matrix → >64 GiB → kernel OOM. `np.savez_compressed` is NOT the killer (chunked write). **Fix:** reusable `_is_symmetric_blocked(m, atol, block=1024)` bounds the transient to ~420 MB while enforcing the identical invariant (commit `3eac803`; tests/m3 `test_plink_ld_to_npz` 17 passed, full suite 302 passed/30 skipped; quick `260629-2im`). Carter STOPPED the VM (idle-billing halted; stop ≠ delete) and DECIDED to re-fire on **n2-highmem-16 (128 GB)** for headroom on any region >~115k vars.
> **★ BLAST-RADIUS UPDATE (fix #2 @ `ff9e66f`, quick `260629-402`):** a read-only blast-radius sweep of `3eac803` confirmed the predicate is provably equivalent (927 cases) but caught a SECOND OOM head — `content_verify_npz` (`run_native_ld_panel.py:228`, the in-process D-M3-10 gate run right after the convert) reloads the full `.npz` and re-runs the SAME unbounded `np.allclose(ld, ld.T)` → ~156 GiB at n_var=102,421 > 128 GB → would OOM region 1 AGAIN one function later. The `3eac803` fix ALONE was insufficient. Fixed @ `ff9e66f` (square → reuse `_is_symmetric_blocked`; banded → new `_strict_upper_is_zero_blocked`); 48 targeted + 305/30 full-suite. **Both square convert→verify OOM heads now bounded (region-1 peak ≈ 40 GiB).**
> **RE-FIRE PENDING:** push `ff9e66f` to origin (verify tip == local HEAD — fix inert on AoU until pushed) → AoU agent `git pull ff9e66f` (≥ both fixes, NOT just `3eac803`) → AoU UI respec VM to n2-highmem-16 (stop→edit machine type→start; PD + 379 GB bfile survive) → pre-flight region-1 window ≈ 102,421 → re-fire from region 1 (0 banked → clean restart) → proof point `.npz` 0→1 + `df` flat + `status==ok`. **Authoritative resume = `.planning/HANDOFF.json` (2026-06-29T07:05Z block) + `.planning/STATE.md` (2026-06-29 RESUME-HERE).** **SUPERSEDES the 2026-06-28 RE-FIRE block below.**
>
> **2026-06-28 ~05:50Z (SUPERSEDED — LOOP RE-FIRED on the FIXED code; first fire banked 0/276 from a chr-prefix verify bug, now fixed + pushed a5a5f9f; STOP-on-complete ordered = stop NOT delete):**
> **First fire banked 0/276 — fixed, re-fired clean.** The 2026-06-27 loop (PID 4237) ran ~17h and banked nothing: region 1 failed verify `n_var mismatch: .ld.bin implies 102421 but the window .bim has 0 rows`. Bank is gated on verify → no `.npz`, no `.ld.bin` reclaim → scratch hit 82% (agent killed it ~174 GiB from full, freed to 42%; **0 banked → nothing lost**).
> **Root cause (confirmed on real data):** AoU GRCh38 cohort `.bim` is `chr`-prefixed (`chr1`) but `config/ld_regions.tsv` `chr` is bare (`1`); plink1.9 normalizes the prefix (so `--chr 1` built the 102,421-var `.ld.bin`) but `_window_bim_n_var` did a literal `==` that never strips `chr` → 0 in-window rows → mismatch every region. The fake-plink test stub used the same literal compare → CI was blind.
> **Fix + PUSHED (origin==local==`a5a5f9f`):** `_chrom_match_key` strips leading `chr`/`CHR` on both sides (mirrors plink1.9); kept-line content verbatim (downstream `ld_npz_to_rds.R` strips it; GWAS side bare `chr:pos`); failing-first regression over 4 prefix combos; `tests/m3` 28+14 passed. Commits `baeb925`+`a5a5f9f` (quick `260628-244`). Also repaired a pre-existing git index dangling-blob corruption (`.planning/WAVE-1-CLOSEOUT-CHECKLIST.md`) via `git hash-object -w`.
> **RE-FIRED clean (PID 6577, 05:44:59 UTC)** on the same VM after `git pull` to `a5a5f9f`; **pre-flight region-1 chr1 window = 102,421** (== plink log). Baseline `npz=0`, `df` 42%.
> **The fix's proof point = region 1 (~06:42 UTC) + regions 1–3:** `npz` 0→1→2→3 AND `df` returns to ~42% after each bank (reclaim deletes the `.ld.bin`). `status==ok`, `n_var`≈102,421. **Regression tell:** `npz` stuck at 0 + `df` climbing, or `error: n_var mismatch`.
> **NEW — Carter ordered STOP-ON-COMPLETE (STOP, not DELETE):** the VM is auto-**stopped** at 276 — VM + reattachable PD + local bfile preserved (resume-safe). Do NOT manually UI-stop it, do NOT assume it's gone, and do NOT DELETE it (a delete destroys the local bfile; any delete is a separate Carter action). Liveness = climbing bucket `.npz` → 276; **do NOT restart the kernel**. Then agent pings `native-panel-recorded` → NCSU reconstructs panel TSV + shutdown record. **SUPERSEDES the 2026-06-27 LOOP-RUNNING block below.**
>
> **2026-06-27 ~04:25Z (SUPERSEDED by the 2026-06-28 RE-FIRE above — THIS is the fire that banked 0/276 and was killed) — LOOP RUNNING: the 276-region AFR native-plink LD loop is FIRED + clean-started; ~11-day, 2–3-day-check-in monitoring phase:**
> **The loop is LIVE** — m3-02e-T4 STEP 4 fired **04:17:52 UTC** on the loop VM **`AoU_Jupyter_ComputeEngine_20260626b`** (Hail-free Cloud Analysis VM, **n2-standard-16, 1TB disk, $0.83/hr, autostop DISABLED**), `nohup` **PID 4237**, `timeout 312h`, server-side. **Clean start:** AFR count **276**; region 1 (`m2_region_00001` chr1) computing; plink **v1.90b7.2 `--r square bin4 --keep-allele-order`**; bfile 73,122×20.7M loaded; `df` 391G/554G stable; `npz_count=0` (region 1 banks ~1h out). **NCSU $0.**
> **Liveness = climbing bucket `.npz` count** `gsutil ls gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/*.npz | wc -l` → 276 (NOT kernel/log). **Do NOT restart the kernel** — nohup'd + resume-safe (re-run same command, banked `.npz` skipped; `.npz` durable in bucket; bfile re-pullable from `gs://…/bfile/`). Backstops: `timeout 312h` (~$259 ceiling at $0.83/hr) + resume-safety + Carter 2–3 day UI check-ins / UI-stop at 276. **NO self-teardown** (pet SA list-only Dataproc → UI-only).
> **Two confirms pending from the agent before pure cadence:** (a) `.npz` 0→1; (b) post-regions-1→2 `df` + scratch-reclaim check (the `783ba91` fix at runtime).
> **This session:** Phase 1 EXPORT DONE+byte-verified (`.bed` 379,657,321,787 B → `gs://…/bfile/`; plink → `gs://…/tools/`; cluster `20260604` STOPPED) → plan PIVOTED off single-node-Dataproc (IAM-dead, pet SA list-only → `reference_aou_pet_sa_listonly_dataproc`) to the decoupled Hail-free fallback → driver scratch-fill bug FIXED `@ 783ba91` (`_reclaim_region_scratch`, 24/24 tests, **caught pre-fire**) → 500GB PD too small (base image ~192 GiB) → re-provisioned **1TB** (`reference_aou_analysis_vm_large_bfile_staging`) → gsutil `-m cp` died sparse → `gcloud storage cp` + verify by `stat`/`df`. Resume signal = **`native-panel-recorded`** → NCSU reconstructs panel TSV + shutdown record, pushes, m3-04 REPLAN. Carter's open UI click: delete stopped cluster `20260604` (~$169 standby). Primary resume = `.planning/HANDOFF.json` (04:25Z block). **SUPERSEDES the 23:35Z block below.**
>
> **2026-06-26 ~23:35Z (SUPERSEDED by the 2026-06-27 LOOP-RUNNING block above) — MID-FIRE: Phase 1 EXPORT running in-perimeter, attended; plan pivoted to the DECOUPLED HAIL-FREE FALLBACK because single-node-Dataproc is IAM-dead):**
> **Running NOW:** STEP 2 export on Hail cluster `20260604` (UI-started, **$24.07/hr**, full 25-node, **attended** by the AOU agent at a 15-min cadence). `hl.export_plink` did the distributed part-write (36,944 tasks / ~29.5 min) and is in the **single-driver merge of 353.6G** → `gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/bfile/afr_cohort.{bed,bim,fam}`. **Bucket shows only `.fam` mid-merge = EXPECTED** (the `.bed` commits to gs:// only when the resumable upload finishes); liveness = driver-Java CPU/network, NOT the empty `ls`. NCSU-side $0.
> **⚠ Single-node-Dataproc plan = DEAD (two walls):** (1) Workbench UI can't flip Standard→Single-node in-place; (2) the in-perimeter **pet SA is list-only Dataproc** (`create/get/delete/update/start/stop` all DENIED, verified via `clusters:testIamPermissions`) → no gcloud single-node create, can't `describe` to clone Hail config, **and the `trap EXIT` self-delete would PERMISSION_DENY.** Lifecycle is **UI-only**. Memory: `reference_aou_pet_sa_listonly_dataproc`.
> **NEW PLAN (Carter-authorized, ~$260, under $500 cap):** **Phase 1 EXPORT (running)** → bfile direct to bucket (Hail writes gs:// natively) → stage `plink1.9` to `gs://…/tools/` **before** stopping → byte-verify (`.bed`~353.6G/`.bim`/`.fam`==73,122) → **UI-STOP 20260604** → report + **HOLD for Phase 2 go**. **Phase 2 LOOP (pending)** = separate **Cloud Analysis VM** (n2-standard-16, 500GB **reattachable PD**, Hail-free) → pull `plink1.9`+bfile to LOCAL `/home/jupyter/` → `nohup … timeout 312h python src/python/run_native_ld_panel.py --bfile-prefix /home/jupyter/afr_cohort --out-dir gs://…/ld/afr_native_panel/ --scratch-dir /home/jupyter/native_ld_scratch --mode square --ancestry AFR --num-shards 1 --panel-tsv gs://…/ld/afr_native_panel/m3-W2-native-plink-panel.tsv` (**NO self-delete**; `timeout 312h`≈$293 ceiling). Liveness = `gsutil ls gs://…/ld/afr_native_panel/*.npz | wc -l`→276; backstops = timeout + reattachable-PD resume-safety + durable bucket `.npz` + Carter 2–3 day UI check-ins / UI-stop at 276. Resume signal = **`native-panel-recorded`** → NCSU reconstructs panel TSV + shutdown record, pushes, m3-04 REPLAN. Primary resume = `.planning/HANDOFF.json` (23:35Z block). **SUPERSEDES the 17:20Z block below (that compute shape is retired).**
>
> **2026-06-26 ~17:20Z (SUPERSEDED — the single-node-convert fire was IAM-blocked and never ran; replaced by the decoupled Hail-free fallback above; was: the m3-02e-T4 single-node LD loop BEING FIRED AUTONOMOUSLY by the in-perimeter AOU agent, server-side; Carter moving):**
> **FIRE IN PROGRESS — do NOT restart the kernel on reconnect.** Carter authorized the full single-node sequence; the AOU agent drives it on its own while disconnected (treats the byte-verified forensic mirror as the go). **On reconnect, FIRST find out how far it got:** read the agent's last chat report; check whether cluster `20604` is now single-node + Running, whether the 2 pre-spend gates passed, and the **data-layer arbiter** `gsutil ls gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/*.npz | wc -l` (climbing → 276 = healthy). Could be at the gates / exporting / loop-firing / blocked.
> **Compute shape DECIDED = single-node n2-standard-16 Dataproc, serial (`--num-shards 1`), ~11 days, ~$250–300 on-demand** (converted in place from the 25-node `20604`, was Running **$24.07/hr**; reuse-as-is ~$6.3k, killed; single-node masters can't be Spot so ~$250–300 not the dead 8-VM ~$50). **`timeout 312h` = ~$293 hard cost ceiling.**
> **Agent sequence:** stop 20604 → convert to single node n2-standard-16 / ≥500 GB / HAIL-retained (DESTRUCTIVE wipe — mirror done first) → start → GATE A ($/hr ~$0.9–1.0) + GATE B (non-interactive self-delete `--help` dry-test) → report → `git pull` to `10aaa6f` → STEP 2 export-once (`export_cohort_to_plink(AFR MT → LOCAL /home/jupyter/afr_cohort)`; verify `.bed` fits disk) → fire wrapped loop → 2–3 day check-ins.
> **⚠ `--bfile-prefix` is LOCAL (`/home/jupyter/afr_cohort`), NOT `gs://`** — plink can't read a `gs://` bfile; only `--out-dir`/`--panel-tsv` are `gs://`. **Backstop = 3 layers** (`timeout 312h` + `trap … EXIT` self-delete [conditional on GATE B] + Carter 2–3 day check-ins) — replaces the un-settable `--max-age` TTL.
> **Disk-safety gate CLEARED:** forensic mirror byte-verified (24 objs / 14,383,272 bytes, exact match) → `gs://rw-migration-aou-rw-476cdac2/forensics_archive/m3-W2-localdisk-mirror-20260626T171108Z` (in-perimeter, never egress) before the wipe; the 24 forensics were already git+remote-backed too. Resume signal from agent = **`native-panel-recorded`** → NCSU reconstructs panel TSV + shutdown record, pushes, then m3-04 REPLAN. Primary resume = `.planning/HANDOFF.json`. **SUPERSEDES the prior 2026-06-26 close block below.**
>
> **2026-06-26 (SUPERSEDED by the fire-in-progress block above — driver built+8-VM-sharded; 8-VM fast path infeasible → single-node cluster is the path; then the fire was authorized + started):**
> **Nothing running in-perimeter, $0** (cluster `20260604` STOPPED + verified). STEP 3 ACCEPTED. Resumable native-plink driver `run_native_ld_panel.py` BUILT + verified + 8-VM-sharded (quick `260625-r6m`; commits `35361e5`/`1a1a361`/`cdc2103`; full suite **287/0/30**; brief STEP 4/7 updated).
> **GCS-DURABILITY FIX — LANDED `10aaa6f`** (overnight background job, now done; $0): driver `--out-dir` accepts `gs://` via `gsutil` (compute→verify→`gsutil cp` .npz to bucket; resume-check via `gsutil stat`; Hail-free; `.bed` never uploaded), making the single-node Dataproc loop bucket-durable [[feedback_aou_use_persistent_disk]]. 22 driver tests; full suite **293/0/30**. **origin == local == `10aaa6f`** — **tomorrow STEP 1 = `git pull` on the AoU clone to reach `10aaa6f`.**
> **⚠ 8-VM/~$50/1.4-day fast path INFEASIBLE unattended** (agent can't/won't provision 8 Spot VMs; NCSU `wb` has no `create`, `gcloud` VPC-SC-walled; plink saturates 16 cores so one box can't beat ~11d). **Only executable shape = single-node n2-standard-16 Dataproc, serial, ~11 days, ~$250–300** (under the $500 cap; resume-safe so killable if Carter hand-provisions 8 VMs for speed later).
> **⚠ STEP 2 export-once PENDING** — the full-cohort plink bfile does NOT exist (only the 1-region pilot bfile). Must `export_cohort_to_plink(AFR MT → gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/bfile/afr_cohort)` (~20–40 min, Hail cluster; `.bed` in-perimeter only) before the loop.
> **NEXT (Carter, AM):** git pull → pick compute shape (single-node rec) → export-once → fire loop on the single-node cluster with a `gs://` `--out-dir` (durable), unattended + resume-safe + **self-stop/wall-capped** (billing-runaway is the recurring catastrophe class) → STOP cluster, verify 276-row panel all `status==ok`, egress aggregate `.npz`+AF only, handback `native-panel-recorded` → NCSU pushes → m3-04 REPLAN. Primary resume = `.planning/HANDOFF.json`. SUPERSEDES the prior 2026-06-25 blocks below.
>
> **2026-06-25 (SUPERSEDED by the 2026-06-26 close block above — STEP 3 ACCEPTED + cluster STOPPED; driver then built + 8-VM-sharded):**
> **Nothing running, $0.** STEP 3 production-VM re-measure **completed + ACCEPTED**. Cluster `20260604` **STOPPED** (Stopped badge, Current cost `--`, 24 workers deprovisioned; pre-stop master `NO_PLINK` + `NO_STRAY_PYTHON`; no other cluster running). **Accepted result** (`m2_region_00040__sub00` AFR, chr12, square bin4, n_var=64,060, on master n2-standard-16): wall **57.14 min**, peak RAM **17.89 GiB**, output **15.29 GiB** (contents-verified diag=1.0, sym max-abs-diff=0.0, `--keep-allele-order`, exit 0). **×276 = 262.9 VM-h** → ~$392 Spot / ~$1,101 on-demand at the brief's n2-highmem-64 labels; on a dedicated **n2-standard-16 Spot** (the VM actually measured) **~$45–50 whole panel**. Budget gate SATISFIED on every scenario. Footprint ×276 square ≈ 4.12 TB.
> **⚠ BLOCKING FINDING — do NOT fire STEP 4 as written.** The fire brief STEP 4 is **hand-bash**, not a script; the only manifest loop driver (`aou_ld_panel.py::main`) drives the **retired Hail path**; the native-plink path has **no loop driver**. ~11-day serial Spot wall → multiple preemptions near-certain. Resume guard `_existing_region_npz` (content-validated, min-bytes floor) exists but is wired only into the Hail path; a naive `[ -f .npz ]` bash guard would silently bank truncated regions ([[feedback_aou_success_marker_not_evidence_of_data]]).
> **DRIVER NOW BUILT (quick `260625-r6m`, $0):** `src/python/run_native_ld_panel.py` (NEW, Hail-free module; reuses `_existing_region_npz` MED-6 skip, plink ONLY via `build_plink_ld_command` so `--keep-allele-order` always present, content-verify float32/square/diag/sym, resume-safe panel TSV, one-bad-region-continues, retired Hail path untouched). TDD + `--validate` (plan-checker PASSED, verifier 8/8); full `pytest tests/m3` **282 passed / 0 failed / 30 skipped**. Fire brief STEP 4 re-pointed to `python src/python/run_native_ld_panel.py`. Commits `35361e5` + `1a1a361` (PUSH pending). **STEP 4 is now turnkey-resumable.**
> **8-VM SHARDING ADDED (`cdc2103`):** Carter chose 8 Spot VMs (~1.4-day wall) + ~$500 cap. Driver now has `--num-shards 8 --shard-index k` (`idx % 8 == k` over the deterministic AFR order → 8 disjoint+exhaustive partitions ≈34–35 each; partition test PASSED) + per-shard `--panel-tsv` (concurrent-append-safe); all `.npz` to ONE shared `--out-dir` (global resume + egress). Brief STEP 4 (8-VM invocation) + STEP 7 (merge-8-shard-TSVs) updated. Full suite **287 / 0 / 30**.
> **NEXT = FIRE the 8-VM STEP-4 loop** (billable, Carter, in-perimeter). Prereqs the paste-in covers: confirm/re-export the full-cohort plink `.bed/.bim/.fam` (STEP 2; may have died with cluster 20260604), a shared `--out-dir` for all 8 VMs, provision 8 n2-standard-16 Spot VMs running shard-k each. ~$500 hard cap. The 8-VM provisioning is the one new AoU ops piece (pilot/STEP-3 ran on the cluster master). After: STOP all 8, merge 8 shard TSVs → 276-row panel, egress aggregate `.npz`+AF only, handback `native-panel-recorded`. Primary resume = `.planning/HANDOFF.json`. SUPERSEDES the 2026-06-24 block below.
>
> **2026-06-24 (SUPERSEDED by the 2026-06-25 STEP-3-RUNNING block above — m3-02e wave PLANNED + checked + committed `8d3dbe4`; T1-3 since EXECUTED + pushed):**
> The pilot (below) was GREEN → this session planned the cost re-architecture as the **m3-02e** wave. The whole M3 milestone is ONE GSD phase (`m3-aou-afr-ld-panel-build`); 02b/c/d/e are WAVES inside it, so it was planned via gsd-planner → gsd-plan-checker (not discuss-phase, which would re-open all of M3). Plan = `m3-02e-W2-native-ld-export-and-public-eur-PLAN.md` (wave 4, depends_on 02b/02d/03), committed with the ROADMAP m3-02e bullet @ `8d3dbe4`. Plan-checker PASS-WITH-CONCERNS → **blocker B-1 reconciled** (m3-04's 322-cell **Hail** LD production fire recorded **SUPERSEDED-PENDING-REPLAN** in the plan `supersedes_note` + ROADMAP — m3-04 must CONSUME m3-02e's AFR `.npz` + public EUR, not rebuild via Hail) + W-1..W-4 fixed (wave#; `estimate_s` = NEW work; hard `chr:pos:REF:ALT` id test; EUR `.rds` round-trip).
> **4 tasks:** T1-T3 AUTONOMOUS NCSU code+tests (T1 `plink_ld_to_npz.py` + `export_cohort_to_plink`/`build_plink_ld_command`, `--keep-allele-order` mandatory; T2 PUBLIC Weissbrod 337k EUR via AWS S3 boto3 UNSIGNED + hg19→hg38 adapter; T3 EUR head=`EUR_ukbb_pub` + NEW `estimate_s` guard + m3-04-superseded record). **T4** = checkpoint:human-action gate=blocking, autonomous:false — the billable in-perimeter native-plink LD loop over 276 AFR windows (**Carter fires**; turnkey runbook `m3-02e-AFR-NATIVE-FIRE-BRIEF.md` authored as autonomous work; production-VM wall re-measure is a BLOCKING stop-gate).
> **NEXT = EXECUTE m3-02e T1-3 in a FRESH SESSION (Carter's choice).** ⚠ This session's gsd-executor SUBAGENT DISPATCH tripped a FALSE-POSITIVE cyber safeguard 3× (the plan's cloud/egress/perimeter wording reads as security-adjacent; work is benign LD code; executor never ran). Clean context typically clears it; if it trips again use neutral wording or execute inline. TDD RED-first, smoke_dev py3.11 for pytest, m3-r-ld R for T2 `.rds`, atomic commits per task (explicit paths, `m3-02e-TN`), honor the `.npz` triangle-flag contract, keep the retired `_write_a3_banded_correlation_bm` in-tree. Then the only billable step left is T4. Nothing running, $0, origin synced @ `8d3dbe4` (push the state commit). Resume = `.planning/HANDOFF.json` + STATE.md 2026-06-24 m3-02e-PLANNED block + the m3-02e PLAN.
>
> **2026-06-24 (SUPERSEDED by the m3-02e-PLANNED block above — native-plink PILOT COMPLETE = GREEN; artifacts pushed):**
> The AoU agent fired the pilot on the SAME cell the Hail v2 probe measured (`m2_region_00040__sub00` AFR, chr12 GRCh38 37,463,740–45,398,515, MAF≥0.005, **n_var=64,060**, count_cols=73,122). On the master n2-standard-16 (plink1.9 single-node): **banded** 25.446 min / 16.68 GiB RAM / 16.55 GiB `.ld.gz`; **square** 56.224 min / 17.89 GiB RAM / 15.29 GiB `.ld.bin` (64060² float32, diag=1.0, symmetric). **Extrapolation ×276: banded 117.05 VM-h ($174 Spot / $490 on-demand), square 258.6 VM-h ($385 / $1,084) → GREEN**, well under $3–4k and ~1–2 OOM cheaper than Hail (~17k cluster-h AFR half). Recommended mode = **square bin4** (SuSiE-ready) unless disk-tight.
> **Token-free handback** (websocket dropped at shutdown → on-disk copies lost; agent cat'd the numbers first). **NCSU reconstructed + pushed** `m3-W2-pilot-plink-native.tsv` (verbatim row, conformed to the 11-col brief schema), `m3-W2-pilot-report.md` (authored from the handback, provenance-noted), `m3-W2-pilot-cluster-shutdown.md`. Cluster 20260604 STOPPED, $0; other three untouched. CAVEATS for m3-02e: (1) $rates labelled n2-highmem-64 but pilot ran on n2-standard-16 → re-measure production-VM wall before budget commit; (2) banded ~400M pairs is estimated from size; (3) per-region export ~33s, count_cols scan ~20min is one-time.
> **NEXT = re-plan m3-02e** (Carter's explicit trigger, e.g. `/gsd-plan-phase`): export-once (QC'd AFR cohort → plink `.bed`) → native per-region AFR LD on a single Spot VM looping 276 regions (`plink1.9 --r square bin4 --keep-allele-order`) → egress-clean `.npz` → NCSU ingest AFR `.npz` + PUBLIC EUR LD (Weissbrod 337k / Pan-UKBB 420k) + hg19→hg38 liftover → coloc/SuSiE with `estimate_s`. The Hail 34k-cluster-h path is NOT taken. Nothing running, $0, origin synced.
>
> **2026-06-23 (SUPERSEDED by the 2026-06-24 PILOT-GREEN block above — COST RE-ARCHITECTURE ACCEPTED; the native-plink PILOT was the next step):**
> Carter capped at ~$1k, then asked for a cost-effective re-design (budget → **$3–4k**). 5 cited research threads → **`m3-W2-cost-effective-rearchitecture.md`**: **(1) EUR LD = PUBLIC UKBB reference** (Carter chose public; $0 + hg19→hg38 adapter on NCSU; better-matched than AoU 220k), **(2) AFR LD in-house but NATIVE** (plink1.9/LDstore2/emeraLD on a single Spot VM ~$1.49/hr, NOT Hail on a 24-node cluster ~$23.4/hr → ~1–2 OOM cheaper; MultiSuSiE NatGenet 2025 precedent), **(3) downstream on NCSU** (egress-clean). Likely < $2.5k total. The Hail 34k-cluster-h path is NOT taken.
>
> **2026-06-23 (SUPERSEDED by the re-architecture block above — AFR re-probe v2 COMPLETE + Task 5 = NOT GREEN ~34k cluster-h):**
> **DONE (cluster STOPPED, $0).** AFR re-probe v2 on HAIL **20260604** COMPLETED CLEAN: `m2_region_00040__sub00` AFR, A.3, ordering B, density-veto routed A.3 (64,176 var > 24,301), **3.0173 blk/min, 0 spill**, `.bm` ~18.5 GiB verified (`…/ld/cost_probe_m3d/bm/…`, _SUCCESS+metadata, part-000 read-back OK). Workbench clone had **NO push token** → token-free handback: AoU `cat`ed 3 artifacts + stopped (commit `07f43dc` discarded), **NCSU reconstructed + pushed** ([[feedback_push_ncsu_before_aou_clone_fire]]).
> **THIS SESSION (NCSU, all pushed @ `aa7f263`):** (1) re-probe v1 (`210e66c`) came back PARTIAL — EUR-B completed (4.85 blk/min, 180min) but AFR INTERRUPTED on an **A.2 driver-collect OOM**; (2) `/gsd-debug` root-caused + FIXED a **density-axis routing bug** (`_route_region_path` vetoed only on span; `to_numpy()` OOM is `n_var²×8` float64 → added a density veto, n_var>24,301→A.3; `31d6e6c`+`05e42a9`; tests/m3 244 passed; 119 cells flip A.2/A.1→A.3); (3) **blast-radius sweep** (4 agents → new `/assess-blast-radius` skill): fix is output-correct/no cost regression, but scales 2 pre-existing gaps into **m3-04 preconditions** — **P1** (BLOCKER) bm_to_npz densify un-wired + ingest counts only `.npz`, **P2** (HIGH) resume guard `.npz`-only so `.bm` cells recompute on disconnect; (4) **FIRE-GATE CATCH** — brief asked "ordering A vs B" but A.3 is hard-locked to **ordering B** (A retired/hang-prone), so brief patched → **v2.1** (fire ONE AFR-B cell; EUR optional).
> **TASK 5 RESULT (`m3-W2-budget-redo.md`):** ran over the **FULL 552-cell** preflight (`m3-W2-preflight-counts-FULL.tsv`, from v1 `210e66c`) + measured AFR rate + **conservative 3.01 EUR fallback** (the v1 EUR 4.85 rate counts matmul-blocks vs AFR's banded-blocks → a "measured" 0.62 factor is a block-definition artifact, physically backwards → excluded). **PROJECTED ≈ 34,207 cluster-h (×1.3 = 44,469)**, ~30× the old 1,117 est → **NOT GREEN** (YELLOW-finer-split). The committed `m3-W2-preflight-counts.tsv` (54 dev cells) is cluster-faithful but NOT the production denominator (projects only 4,684 alone). Carter: set the real cap, but NOT GREEN for any plausible cap.
> **RE-SCOPE ANALYSIS DONE (`m3-W2-rescope-levers-analysis.md`); SCOPE/BUDGET DECISION PENDING — Carter chose PAUSE.** Cap = ~$1k ≈ 1,000 cluster-h → gate RED. **$1k is 2–8× below genome-wide AFR+EUR cost; finer-split is NOT the cost lever** (per-cell tractability only; total cost RISES with split count). Real levers = band width (bounded by LD-decay) + cluster size/npm (validity UNCONFIRMED — likely distributed). Optimistic stack still ~3× over → needs a **scope cut (AFR-only / candidate regions), budget raise, or method change.** Recommended unblocker = a **~$5 sizing probe** (verified cell on master+2–4 workers; does `blocks_per_min` hold ~3 or collapse?). Options offered ($5 probe / AFR-only / raise budget / pause) — Carter PAUSED to think.
> **m3-04 still BLOCKED on correctness regardless:** HLA `region_00145__sub19` finer-split (T-M3RS2-HLA-01); **P2** (resume guard, ready to build, pure NCSU) + **P1 A/B/C** (rec = Option A in-perimeter fat-driver densify; BLOCKER, Carter's call). Full steps = `.planning/HANDOFF.json`. Nothing running, $0. DO NOT start 20260620/20260617 (no Hail); DO NOT use cores=2; DO NOT attempt ordering A.
>

> **2026-06-22 (SUPERSEDED by the 2026-06-23 block above; SESSION CLOSED end of day; m3-02c probe fired + m3-02d replanned + Tasks 1-3 done; NEXT = Task 4 re-probe in a new session):**
> NOTHING running, $0, all clusters STOPPED. **m3-02c probe** fired 2026-06-22 (partial): STEP A showed all 15 dev cells >75k var (split too coarse, outputs to 277 GiB > 50GB egress cap); minimal STEP B EUR cell had **0 spill on 64GB cores=1** (the n2-highmem premise was WRONG) but was **write-bound** (A.3 write is the bottleneck), interrupted at ~56min (no clean rate). **m3-02d** replanned that finding (`/gsd-plan-phase`: research V2 + plan + plan-checker PASS; Carter LOCKED band width **AFR 3Mb / EUR 5Mb**) and **Tasks 1-3 EXECUTED + pushed** (origin==local @ **3d53980**; tests/m3 235 passed/38 skipped/0 failed): per-ancestry buffer regen (123 AFR __sub @3M + 123 EUR @5M), ordering-B A.3 write, re-derived over_threshold (write-blocks≤150/output≤10GiB, retired the 75k proxy), new `redo_ld_cost_model.py` + `ld_egress_bundle.py`.
> **NEXT = Task 4 in-perimeter re-probe** (autonomous:false, COSTS MONEY; the gate `--auto` can't cross). Turnkey runbook = **`m3-02d-REPROBE-BRIEF.md`**: paste it to the AoU agent → START HAIL cluster **20260604** (Apps-panel, **cores=1/11g/3g** = the 0-spill config) → sync repo → STEP A preflight on the regenerated dev manifest (PULL region_00145 __sub rows from the FULL manifest for HLA — the dev selector picks 143 not 145) → STEP B fire **COMPLETING** AFR core5/buf3 + mandatory EUR core5/buf5 cells **ordering A vs B** → clean blocks_per_min into a REPLACED `m3-W2-cost-probe.tsv` (the m3-02c one is INTERRUPTED) → STEP C verified shutdown → ping "reprobe-recorded" → Task 5 `redo_ld_cost_model.py` → GREEN/RED go/no-go. 322-cell production stays in m3-04. DO NOT start 20260620/20260617 (no Hail); DO NOT use cores=2. Full steps = `.planning/HANDOFF.json`.
>
> **2026-06-20 (SESSION CLOSED; m3-02c probe one Apps-panel Start from running):**
> Carter home ~1hr. **On return: START existing HAIL cluster `20260604`** (n2-standard-16 ×24, 64GB, framework=HAIL, Workbench-registered) via the **Apps-panel Start** (NOT now — avoid ~$19-20/hr idle; NOT gcloud — keeps registration). This session diagnosed + resolved TWO false starts: the agent's cluster `20260620` (n2-standard-32 ×24, correct size) had **NO Hail** (generic "JupyterLab Spark" app, missing `software-framework=HAIL` + hailctl wheel + HAIL startup script), and the UI picker offers **no highmem** family. Starting the existing 64GB HAIL cluster sidesteps both.
> **On the started cluster:** git pull+checkout -f; override Q-RS2 to **cores=1** for 64GB (`PYSPARK_SUBMIT_ARGS` cores=1/40g exec/12g overhead/24g driver — NOT yet in the notebook, paste inline); USE_DEV_SUBSET=True; run STEP 0 → **STEP A count-only** (`write_preflight_counts` → `m3-W2-preflight-counts.tsv`, all 15 dev cells incl. region_00143 real-MHC + region_00145__sub00) → option-B STOP/review → **STEP B** 3 cells (m2_region_00006 AFR, m2_region_00040__sub00 AFR + EUR; **watch EUR spill on 64GB** = the m3-04 viability signal) → **STEP C** stop+verify → push 3 artifacts → ping NCSU "probe-recorded" → Task 4.
> STEP-A mechanism built+tested this session (commit 040002c; tests/m3 214 passed). Quota Tasks 1-2 closed. DO NOT run AOU-2 Cell 9 (compute) before STEP-B go; NEVER Cell 12 (egress). DO NOT start 20260620/20260617. Inspection VM stays up ($0.15/hr terminal). Full steps = `.planning/HANDOFF.json`. origin == local.

> **2026-06-20 (SUPERSEDED — m3-02c probe FULLY PREPPED + AoU STEP-0 inspection ALL-GREEN; PAUSED at the sized-cluster-start gate):** Pure NCSU code session — **nothing running, no cluster, $0.** Three quick tasks landed + PUSHED (origin == local **cb76115**): **260619-qjy** (A.3 AF sidecar gap CLOSED — `bm_to_npz.py` carries `allele_freq`), **260619-rqs** (post-split LD manifest regenerated via **Path B** — `--bed`/`--chain` gone so split the existing committed manifest reusing `_assemble_region_rows()`; `config/ld_regions.tsv` 322→434, 128 `__sub` @ `buffer_bp=10Mb`, dev manifest carries `m2_region_00040__sub00/01` AFR+EUR), **260619-vcp** (AOU-2 **gap-C3** WORKSPACE_BUCKET hard pin BAKED at cell idx 5 before the `_normalize_bucket` read + `cloned-mybucket` halt-assert). `tests/m3` 205/0/30. **The AoU Workbench agent ran a read-only STEP-0 inspection (cheap VM, no spend) → ALL preconditions GREEN on cb76115** and CAUGHT two real issues, both FIXED: origin was 32 commits behind (never pushed → pushed; memory `feedback_push_ncsu_before_aou_clone_fire`) + the gap-C3 bucket pin (→ vcp bake). **NEXT = run the m3-02c probe (autonomous:false, COSTS MONEY):** `/gsd-execute-phase m3` records the pre-satisfied quota (N2=5000) → pauses at Task 3 for the AoU fire → Carter authorizes the sized-cluster start → bucket-pin cell → Q-RS2 → STEP A preflight → **PAUSE** → STEP B (≥3 cells: 00006 AFR + 00040__sub00 AFR + 00040__sub00 EUR mandatory + HLA 00145) under cost controls → data-layer-verify → STEP C shutdown → 3 artifacts → Task 4 cost model + go/no-go. **PENDING (re-offer before boot):** how to authorize the ~$25-30/hr start — (A) start+stay, (B) start+stop-between-A-and-B, (C) hold (Carter's spend call). Primary resume = `.planning/HANDOFF.json`. SUPERSEDES the 2026-06-19 block below.
>
> **2026-06-19 (SUPERSEDED by the 2026-06-20 block above — m3-02b EXECUTED + adversarially reviewed + CR-01/warnings fixed + blast-radius swept; NEXT = m3-02c):** Pure NCSU code session — **nothing running, no cluster, no cost.** **Executed `m3-02b`** (Wave 0): overlapping-window xlarge split (`build_ld_region_manifest.py`, half-open cores + core±buffer windows + explicit `buffer_bp`) + dev tuple-resolve/capped-expansion + AF metadata in `aou_ld_panel.py` `.npz`; **NEW** `stitch_subregions_to_rds.R` **BANDED** sparse stitch (cross-core pairs within `buffer_bp` retained, beyond zeroed; `obj$R`+`obj$variants` for the real `load_ld_matrix`); A6 real-loader verify + sparse-parent benchmark + AOU-2 Q-RS2 cell; provisioned the `m3-r-ld` conda env for the no-skip R families (`a17a47a`/`0e3ec43`/`908de71`/`989f53b`). **Adversarial deep review** confirmed the banded-stitch geometry correct (fuzz-verified) + found **CR-01** (critical) + WR-01/02/03 (`fef84eb`). **Fixed CR-01 + all warnings** TDD RED-first (`f96fbb9`): CR-01 = honor the `.npz` `lower_triangular` flag (old `!isSymmetric` gate DOUBLED full-matrix off-diagonals r→2r). **Blast-radius sweep** (4 parallel agents, `b34dc60`) caught **BR-01** — the CR-01 fix made the flag authoritative but `bm_to_npz.py` (Path A.3, the xlarge-split deliverable path) never wrote it → A.3 off-diagonals **HALVED** (0.6→0.30); **fixed** with 2 RED-first tests (`1a06471`); a stale liftover test was also exposed + corrected (UCSC `hg38ToHg19` chain truth **53843159**, not dbSNP 53803574). **Full `pytest tests/m3` = 194 passed / 0 failed / 30 skipped.** **DEFERRED + tracked (m3-04 precondition):** `bm_to_npz.py` writes no `allele_freq` → Path A.3 regions get NA AF (todo `530ae5d` + m3-02c carry-forward note `0d265c9` + `m3-02b-W2-rescope-BLAST-RADIUS-FIX.md`). **NEXT = `m3-02c`** (quota + real-cohort cost probe + go/no-go; `autonomous:false`; Carter fires in-perimeter, costs money); open flag: `--subregion-buffer-mb 10` effectively mandatory (radius default spans the whole parent). Primary resume = `.planning/HANDOFF.json`. SUPERSEDES the 2026-06-18 dev-10-killed block below (still valid history: dev-10 on 64-vCPU/16 GB is intractable; the SPLIT done in m3-02b is the structural fix).
>
> **2026-06-18 (dev-10 LD fire KILLED: intractable on the dev cluster; Wave-2 RE-SCOPE needed — SUPERSEDED by the 2026-06-19 block above, but the capacity finding stands):** dev-10 fired on `20260617` (16× n2-standard-4, 64 vCPU / 16 GB) and was **killed with 0 regions completed** — a CAPACITY wall, not a correctness bug. `region_00006` (cheapest A.3 region, n_var=122,678, AFR 73,122 samples) crawled its 900-block Stage-4 at **~0.9–2.1 blocks/min** (vs ~66 for the `--n-samples 2000` repro) and the **64 GB master CRASHED** at ~65 GiB dense scratch; `region_00040` (xlarge ×2) ≈ ~weeks/cell. **Root cause:** the repro's 2,000 synthetic samples vs real 73k–220k → the correlation matmul inner dim **is** the sample count → ~36× (AFR)/~110× (EUR) more compute/block + 16 GB workers spilling + master can't sustain the dense scratch; the repro proved the **fix's correctness**, never its **real-cohort cost**. **A.3 fix correctness + ordering-A decision are NOT in question** — the fix is capacity-bound, and **splitting** (not re-ordering) shrinks the dense intermediate. Teardown: orphan `…/ld/AFR_aou/bm/m2_region_00006.corr_scratch.bm` (~65 GiB) DELETED, cluster `20260617` STOPPED. **NEXT = Wave-2 RE-SCOPE via `/gsd-plan-phase`** per `WAVE-2-RESCOPE-real-cohort-compute.md`: (1) sized cluster (n2-highmem-16 workers, 256–384 vCPU, highmem master); (2) **split the ~16 xlarge regions** into ≤~75–100k-var sub-regions (the load-bearing change); (3) redo the ~1,117 cluster-h cost model. Primary resume = `.planning/HANDOFF.json`. The 3 prep landmines still apply to any re-fire. SUPERSEDES the dev-10-fired block below.
>
> **2026-06-18 (GATE-2 dev-10 LD fire FIRED + RUNNING server-side — SUPERSEDED: the fire was killed as intractable, see above):** dev-10 (AOU-2, `USE_DEV_SUBSET=True`) is **LIVE** — Cell 6 per-region loop in `.planning/notebooks/AOU-2_per_region_ld.ipynb` on cluster `20260617` (16× n2-standard-4, pip-`hail==0.2.135`, `MASTER=yarn`), Spark app **`application_1781785150907_0001`**. **10 cells** = 7 AFR (`m2_region_00006,00027,00040,00067,00083,00143,00153`) + 3 EUR (`00040,00067,00083`). **`region_00006`** (the hang region; A.3 by span-veto, n_var=122,678) computing first — its `COMPLETED`/`path_a=A.3` line is the headline proof; healthy at ~2 min (64/64 vCPU, runJob 270/540). **`region_00040`** xlarge in both ancestries = the two long poles (~1.4 TiB / hours each) + the GATE-3 cost number for free. **The A.3 fix is cluster-validated** (synced to HEAD, sha256 byte-identical, helper 4, `LOADED FROM` realpath into the synced repo, `A.3 fix present ✓`); cohorts read correctly (AFR 73,122 / EUR 220,098). **3 prep landmines fixed (re-apply on any fresh cluster):** symlink `~/coloc_analysis`→synced repo · pin `WORKSPACE_BUCKET=gs://rw-migration-aou-rw-476cdac2` · `os.chdir(~/coloc_analysis)` before Cell 6. **RESUME (reconnect): do NOT restart the kernel** (server-side loop survives disconnect); liveness = Spark app advancing + `gsutil du`/`ls` of `…/ld/AFR_aou/`,`…/ld/EUR_aou/` (not the kernel light / not `_SUCCESS`); verify each region at the data layer; halt-checks Q10 + D-M3-07; all 10 land → AOU-4 memo + GATE-3 cost memo. Primary structured resume = `.planning/HANDOFF.json`. Cluster billing throughout. SUPERSEDES the ordering-experiment block below.
>
> **2026-06-18 (A.3 ordering experiment DONE; fix CLUSTER-VALIDATED; KEEP ordering A; B RETIRED; CR-01 reframed — SUPERSEDED by the dev-10 fire block above):** Quick `260618-3at`. The ordering experiment ran on a **16-worker resize of cluster `20260617`** (`scripts/a3_blockmatrix_lowering_repro.py --skip-old --n-var 130000 --n-samples 2000 --radius-bp 1000000 --budget-sec 3600`, `MASTER=yarn`, 64 concurrent tasks). **Ordering A COMPLETED 928.0 s** (valid `A/repro_A.bm`) = first cluster validation of the deployed A.3 fix; **ordering B COMPLETED 863.5 s** (hang-free — its `sparsify→checkpoint` IR-shape did NOT reproduce the OLD driver-collect hang). The 2→16 resize succeeding **diagnosed the original 24-worker prod-cluster failure as quota/size-bound, not project-level**. **DECISIVE:** `--report-scratch-size` on the real manifest shows **`banded(B) == dense(A)` for ALL 23 A.3 regions** (radius `span+500kb` cap 50 Mb ≥ span/2 → band covers ~whole region) → **B saves scratch NOWHERE**. **DECISION: KEEP ordering A** (deployed + validated); **B RETIRED** (`DRAFT-orderingB-band-before-checkpoint.md` parked — revisit only if a manifest adopts radius ≪ span). **CR-01 is ordering-INDEPENDENT** (~1.8 TiB under A *or* B for region_00145) → the real GATE-3 question is xlarge dense-materialize **compute cost vs region-splitting**, not checkpoint ordering. Debug session `m3-W2-a3-blockmatrix-write-ir-lowering-hang` = **RESOLVED**. Cluster `20260617` being **Stopped** (not deleted — preserves the verified script + `hail==0.2.135`). **NEXT:** GATE-2 dev-10 may resume on ordering A (re-fire `m2_region_00006`, data-layer-verify the `.bm`) → rest of dev-10 → AOU-4 memo; SEPARATELY, scope the GATE-3 xlarge dense-materialize cost-vs-split decision. SUPERSEDES the 2026-06-12 block below.
>
> **2026-06-12 (dev-10 PAUSED on the A.3 BlockMatrix lowering bug; cluster STOPPED — SUPERSEDED by 2026-06-18 above):** GATE-2 dev-10 was fired and **region 1 (`m2_region_00006`, AFR, 17.7 Mb, n_var=122,678) HUNG** on the first live Path-A.3 BlockMatrix write (`hl.ld_matrix(...).write()` → Hail's interpreted `BlockMatrixWrite` driven through a driver-side `ContextRDD.collect`; 736/900 blocks in ~73 min = operationally intractable). Killed via Spark-UI job-kill (soft kernel interrupt didn't take — Py4J recv block); kernel + Hail session recovered cleanly; the 99 GB orphan partial `.bm` was deleted+verified. **Cluster `…20260605` STOPPED by Carter ~2026-06-12 03:00 (verified STOPPED, $0).** **FIX LANDED + DOUBLE-REVIEWED (NCSU-side, commits `125b353` fix · `13a1055` review-remediation · `a554c26` adversarial-remediation, all pushed):** Path A.3 now materializes the correlation via `_write_a3_banded_correlation_bm` (`hl.row_correlation` → `checkpoint` → `locus_windows` → `sparsify_row_intervals(blocks_only=False)` → `write`) — byte-identical numerics to `ld_matrix` (verified against Hail `statgen.py` source). **Two reviews on record** (`.../m3-W2-a3-blockmatrix-write-ir-lowering-hang.md` + `-REVIEW.md`): the adversarial pass corrected the mechanism (ALL BlockMatrix writes run interpreted per `CanLowerEfficiently.scala` — the warning is NOT eliminated; the fix is cheap because the final write reads CONCRETE materialized blocks, not an un-materialized matmul) and surfaced **CR-01 (the current ordering A checkpoints the FULL DENSE n×n correlation → ~2 TB scratch on the largest region — a GATE-3 blocker)** and that **Pan-UKBB uses ordering B (band-then-checkpoint, ~GB scratch) at biobank scale → B is the leading production default**. tests/m3 155 passed/35 skipped. **NEXT (Carter, needs cluster restart): `wb cluster start --id=JupyterLab_Spark_cluster_for_AoU_Spark_20260605` → `git pull`+`checkout -f` (gets `a554c26`) → run `scripts/a3_blockmatrix_lowering_repro.py` (re-gated on WALL-CLOCK COMPLETION, NOT the warning; OLD must time out to be non-vacuous) to decide ordering A vs B by completion-time + scratch; if B wins, re-order the helper band-before-checkpoint (resolves CR-01) → THEN re-fire dev-10 region_00006 (valid `.bm`, data-layer verified) → resume the rest of dev-10.** GATE-2 is otherwise unblocked (GATE-1 cost CLEARED 2026-06-12; bucket double-prefix fixed `bde00ee`; read-gate wired). Debug session OPEN at human-verify. SUPERSEDES the "cluster LIVE + billing" note below (cluster is now STOPPED).

> **2026-06-11 PM (wb CLI + cluster LIVE — SUPERSEDED: cluster STOPPED 2026-06-12, see LATEST above):** Verily Workbench `wb` CLI installed + authed on the NCSU HPC node (wrappers `~/bin/wb` + `~/bin/gcloud`; full setup + capability map in memory `reference_wb_cli_hpc_setup`). **Capability boundary established empirically:** control-plane works off-perimeter (workspace/resource list+describe, `wb cluster start/stop/launch`); **data-plane is VPC-SC-WALLED** (gcloud auth login succeeds but `gcloud storage ls` → `403 org policy / vpcServiceControls` by request origin — so MT verification + job-submit CANNOT be driven from HPC; they stay in the Workbench notebook). Google creds were revoked+wiped after the test; the `wb` (auth0) login stays. **Cluster `...20260605` (HAIL 0.2.135, 24 workers, us-central1) STARTED via `wb cluster start` and is RUNNING + BILLING** (proxy `https://6ahxh6brr5emnhimhibqhhmjy4-dot-us-central1.dataproc.googleusercontent.com/gateway/default/jupyter/lab/`). dev-10 fire is code-ready (read-gate is contents-only → passes the 3 banked marker-less MTs; dev set = 10 regions; `USE_DEV_SUBSET`+`read_final_cohort_mt` wired in `AOU-2_per_region_ld.ipynb`). **GATE 1 = CLEARED** (cost/credit APPROVED by Carter 2026-06-12; CDR v8 already pinned) — GATE 2 is now fully unblocked. NEXT (Carter, browser): `git pull` + `git checkout -f` (gets the read-gate AND the bucket fix `bde00ee`; the Workbench notebook was the stale pre-`ac598ce` version) → confirm `WORKSPACE_BUCKET` → `USE_DEV_SUBSET=True` → run AOU-2 dev-10 → verify at data layer (Q10 / D-M3-07) → AOU-4 memo. Cluster `…20260605` RUNNING + billing; `wb cluster stop --id=JupyterLab_Spark_cluster_for_AoU_Spark_20260605` halts billing. Cluster is Stopped-not-Deleted on teardown ([[feedback_aou_disk_type_check]]); `wb cluster stop --id=JupyterLab_Spark_cluster_for_AoU_Spark_20260605` stops billing from the terminal.

> **2026-06-11 update (quick 260611-f5f, commits `ac598ce` + `969f6e5`):** Durable-fix PHASE 2 **item 1 (consumer wiring) is DONE** — the read-side hole is closed. New `read_final_cohort_mt(uri)` in `src/python/aou_ld_panel.py` gates-then-reads (calls `_final_is_trustworthy`, raises a loud RuntimeError naming the uri + `force_fresh=False` recovery BEFORE `import hail`), wired into AOU-2 cell 4 for both `mt_afr_qc.mt` and `mt_eur_qc.mt` (AOU-4 reads only `.rds`, so cell 4 was the only direct final-MT reader). TDD RED→GREEN, full `tests/m3` 147 passed/35 skipped (+2), verifier 5/5. **Remaining Phase-2 items 2+3 stay cluster-deferred:** chr22 smoke (verify the producer stamps `_VALIDATED` on a real `gs://` finalize) + the `_qc_checkpoint_uri` `file://` footgun (latent). Both ride the next fire, behind GATE 1 cost. So the open items below are now: **(1) GATE 1 cost → (2) GATE 2 dev-10 LD fire + (3) chr22 smoke alongside it.**

**Good morning. Overnight autonomous session summary (Carter asleep, nothing fired in the AoU perimeter).**

## What's DONE (verified)
- **All 3 cohorts banked + verified** (GATE 1.5 closed): AFR_pca 73,122×20,767,864 · EUR_pca 220,098×11,375,140 · AFR_pca_selfid 62,557×20,817,925. `cohort_summary_m3.tsv` = 3 rows, NOTES = COMPLETE. Scratch dropped (you did it); live MT du = 1.38 TB, untouched.
- **GATE 0 (egress) = already RESOLVED** (RULED PASS 2026-04-28, `aou-egress-audit-log.md`). The earlier "file it now" was a stale-doc error — corrected across all docs (commit `889f692`).
- **Default branch flipped to `m3-W2-aou-deltas`** (you ran `gh repo edit`, verified) — clone-and-rerun trap closed.
- **Durable atomic-final-write fix — PHASE 1 LANDED** (commit `f931446`, TDD, `tests/m3` 145 passed / 35 skipped). Producer `_apply_sample_qc_and_finalize` now stamps `{final}/_VALIDATED` after the non-empty assert; `_final_is_trustworthy()` (CONTENTS-ONLY gate) + helpers added. An **adversarial review team** caught and I fixed: a stale-marker false-pass (gate is now contents-only, not the OR fast-path I'd designed) + a malformed test (removed) + flagged the inert-gate + a uri footgun. The fix is **review-hardened but PHASE 2 is pending** (see below).

## What I could NOT do (and why) — all yours
Everything else you listed is **inside the AoU perimeter** (no `gsutil`/cluster/notebook access from here) and/or is a destructive trigger you hold:
- **`_forensics` delete (7.8 TiB)** — NOT done. It holds Track-1 credit-claim evidence ("vital information"), is irreversible, and is your trigger. Do it yourself only when you've confirmed you don't need the credit evidence.
- **GATE 2 / GATE 3 LD fires** — NOT done. Cost real money (~1,117 cluster-h), need GATE 1 #1 (cost/credit, which you're looking up), and are your fire triggers.
- **Cell cleanup / cluster** — Workbench-side; cluster is Stopped (good).

## YOUR open items, in priority order
1. **GATE 1 #1 — cost/credit** (the only thing gating the next fire): look up ~1,117 cluster-h → AoU credit-$ vs your balance, set a cap. (#2 CDR v8 — you confirmed ✅.)
2. **Then GATE 2** — fire the dev-10 LD subset (`AOU-2`, `USE_DEV_SUBSET=True`) + AOU-4 validation memo. The cohort substrate is built, code fixes are in, egress is cleared.
3. **Durable-fix PHASE 2** (do alongside the next fire, needs the cluster): wire `_final_is_trustworthy(final_uri)` into the **AOU-2 / AOU-4 notebook readers** (raise on False before `hl.read_matrix_table`) — until then consumers still read the final blind — then **chr22 smoke** to confirm the producer stamps `_VALIDATED` on a real `gs://` write. Details: `DURABLE-FIX-DESIGN-atomic-final-write.md` (§ Implementation status).
4. **Deferred, each its own trigger:** `_forensics` delete · debug housekeeping is done (4 sessions → resolved/, ticket MOOT) · durable-fix phase 2 before GATE 3.

## Commits this session (all on `m3-W2-aou-deltas`, pushed)
`49cb50f` bank+knowledge · `c2dfb08` cleanup · `d3ff143` main strategy · `fea82ff` default-branch flip · `889f692` GATE 0 correction · `f931446` durable-fix phase 1.

## Untouched / safe
All 3 cohort MTs, the 22 intermediates, `_forensics`, the clean tree. The `aou-ld-pipeline` skill is the operating manual for any AoU work. `git status` shows only pre-existing clutter (results_lsweep_*, targeted_rerun_*, settings.json) — none of it mine.
