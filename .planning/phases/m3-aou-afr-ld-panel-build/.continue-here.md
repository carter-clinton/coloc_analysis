---
context: phase
phase: m3-aou-afr-ld-panel-build
plan: 3
task: null
total_tasks: null
status: paused
last_updated: 2026-06-02T00:00:00.000Z
---

# Resume point — fire GATE A (Tier 0 mechanism probe) on a Hail Genomics Dataproc cluster

**One-line:** CHECK C/D done; the chr22 smoke is now a tiered cheap-first sequence
(Gate A → B → C); everything is built + pushed (`e0f4182`); both AoU clusters/envs
are deleted ($0). The only remaining work is **Carter firing Gate A** at his trigger.

## What changed this session (2026-06-02)

- **CHECK C = PASS** (confirmed live by Carter on a CDR-wired **R8** Standard env) and
  **CHECK D = done** (Hail 0.2.135). The driver self-resolves bucket + CDR + aux
  filename (`_resolve_aux_base` / `_resolve_aux_file`). See `[[project_check_c_env_derive_aux_base]]`.
- **chr22 smoke HARDENED → tiered cheap-first** (quick **260601-u1b**, pushed `e0f4182`;
  plan-check PASSED, verify **8/8**, code-review SOUND, Track-4 guard byte-identical,
  tests/m3 **109 passed**). New `AOU-0.5` probe notebook; `AOU-1` parameterized by
  `INTERVAL`; `_interval_scaled_du_floor` + standalone never-raise
  `_capture_catastrophe_forensics` (+`_coerce_mtime`).
- **Gate A cluster path RESOLVED (web-verified):** use the **"Hail Genomics Analysis"**
  Dataproc cluster (Verily `software-framework=HAIL`) — Hail **pre-installed +
  YARN-wired**, NO pip. The generic **"JupyterLab Spark cluster"** = `NONE` = no Hail
  (the 2026-06-02 blocker; that cluster was deleted). Verily offers only
  **n2-standard** machine types (no n1-highmem). See `[[project_aou_dataproc_hail_install]]`.
- **$0 state:** both the tiny CHECK-C ComputeEngine env AND the generic no-Hail
  Spark cluster are **deleted**. Nothing running.

## Next action — GATE A (Carter holds the trigger + the ~$1-3 cost)

1. **Create a "Hail Genomics Analysis" Dataproc cluster** (`software-framework=HAIL`):
   workers **4× n2-standard-16 = 64 vCPU**, secondary **0**, autoscaling **none**,
   image **Dataproc 2.2 (Spark 3.5)** if a selector appears. (Do NOT pick the generic
   "JupyterLab Spark cluster" — it has no Hail.)
2. Terminal: `git clone https://github.com/carter-clinton/coloc_analysis.git ~/coloc_analysis 2>/dev/null; cd ~/coloc_analysis && git checkout m3-W2-aou-deltas && git pull origin m3-W2-aou-deltas` → confirm `git rev-parse --short HEAD` = **`e0f4182`**.
3. Open `.planning/notebooks/AOU-0.5-mechanism-probe_template.ipynb` on the **Hail/PySpark kernel**. Fresh kernel → **Cell 1a first** → **Cell 1b** → paste the **YARN-verify cell** (master must be `yarn` not `local`, executors>1, exec mem `5g`, hail ~0.2.135) → **probe cell**.
4. **PASS** → Gate B (`AOU-1`, `INTERVAL='chr22:16000000-18000000'`, same cluster). **FAIL** → 1000G + read `gs://…/ld/_forensics/probe_capture.json`.

**Authoritative run sheet:** `.planning/quick/260601-u1b-harden-chr22-smoke-into-tiered-cheap-fir/TIERED-VALIDATION-RUNBOOK.md` **§0** (provisioning + run sequence + YARN-verify + pip fallback) and §2-§4 (Gate decision rules).

## Required reading (in order)
0. `TIERED-VALIDATION-RUNBOOK.md` §0 — cluster provisioning + Gate A run sheet. **Start here.**
1. `.planning/HANDOFF.json` — full state (coordinates, completed/remaining tasks, decisions, next_action).
2. Memory `[[project_aou_dataproc_hail_install]]` — Hail-on-Dataproc how-to + pip fallback.
3. Memory `[[project_state]]` — top section (2026-06-02).

## Decisions / constraints (do not re-litigate)
- Cluster = **Hail Genomics Analysis** (HAIL software-framework), n2-standard-16, **NON-preemptible** (spot would muddy the kill-interrupted-write hypothesis).
- Use the env's pre-installed Hail (0.2.135); do NOT pip-pin on the Hail cluster.
- 1000G AFR = smoke-failure safety net ONLY (`[[feedback_no_1000g_ld_pivot]]`).
- Carter holds **every** AoU launch/compute/$ trigger. Repo-only work; nothing auto-fires.
- Explicit git paths only on this GPFS tree (never `git add -A`/`.`).

<blockers>
None. (Both AoU resources deleted -> $0; Gate A is a Carter-trigger compute fire, not a blocker.)
</blockers>
