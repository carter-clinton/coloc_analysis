---
context: phase
phase: m3-aou-afr-ld-panel-build
plan: 3
task: null
total_tasks: null
status: paused
last_updated: 2026-06-02T23:59:00.000Z
---

# Resume point — RE-FIRE Gate B #2 (both fixes landed; needs the end-to-end live proof)

**One-line:** The Gate B **driver-collect fix is LIVE-PROVEN** (re-fire #1: post_split MT
populated in ~3 min, 0 spills, clean jstack). That re-fire then hit a separate, narrow
**colon-in-path bug** — now **fixed + pushed + NCSU-verified**. Tomorrow: recreate the
cluster and re-fire the nano tier once more to confirm the colon fix end-to-end and
complete the 3-MT cohort = **full Gate B PASS**. Cluster deleted at break → $0.

## What's done (2026-06-02)

- **Gate A = PASS** (768 MB synthetic MT; empty-MT catastrophe did NOT reproduce).
- **Driver-collect fix (`e23c081`) = LIVE-CONFIRMED WORKING** at re-fire #1 on an
  **n2-standard-16-master** cluster: state=FRESH wrote the post_split intermediate
  (**120 real row partitions**), **no stall**, **0 spills**, ~3 min not 38, clean driver
  jstack. The thing we most feared is answered: the fix works in production.
- **Colon path bug (`a96f2cf`) = FIXED + pushed + verified NCSU-side.** Re-fire #1
  errored after post_split on `java.net.URISyntaxException: Relative path in absolute URI:
  mt_afr_post_split_chr22:16000000-18000000.mt` — the nano INTERVAL's colon leaked into
  `_intermediate_checkpoint_uri`; GCS tolerated it on write but the new read-back routed
  it through Hadoop's `Path` parser. Fix = reusable `_sanitize_interval_suffix()`
  (`:`→`_`, `-`→`_`); intermediate is now `mt_afr_post_split_chr22_16000000_18000000.mt`.
  RED reproduced the exception NCSU-side; GREEN after; **tests/m3 116 passed / 27 skipped**;
  driver-collect fix + Track-4 guard byte-identical/intact; functionally proven on the
  exact failing input.
- **Pushed** to `origin/m3-W2-aou-deltas` — HEAD = **`c97bf4e`** (a96f2cf fix + c97bf4e debug doc, on top of 8445850 / 0f92428 / e23c081).
- **Cluster DELETED → $0.** Bucket persists: an orphaned colon-named
  `mt_afr_post_split_chr22:16000000-18000000.mt` (+ `.meta.json`) from re-fire #1 sits in
  `/ld/intermediate/` — **harmless** (post-fix the resume guard probes the underscore name).

## Next action — RE-FIRE GATE B #2 (Carter holds the trigger + ~$1-3)

**This proves the colon fix end-to-end + completes the nano cohort. 5 steps:**

1. **Recreate** a "Hail Genomics Analysis" Dataproc cluster — **n2-standard-16 MASTER**
   (confirmed working at re-fire #1; NOT the default n2-standard-2 → OOM) + **4× n2-standard-16**
   workers, non-preemptible, image Dataproc 2.2 if selectable.
2. **Pull (mandatory):**
   ```bash
   cd ~/coloc_analysis && git checkout m3-W2-aou-deltas && git pull origin m3-W2-aou-deltas
   git log --oneline -3   # expect c97bf4e / a96f2cf / 8445850
   ```
3. **Clean kernel:** Restart Kernel, then verify no orphan from a prior run lingers:
   ```bash
   ps -ef | grep -E "ipykernel|java" | grep -v grep   # kill -9 any leftover
   ```
4. **Set INTERVAL = nano** (resets to whole `chr22` on pull):
   ```python
   INTERVAL = "chr22:16000000-18000000"
   ```
5. **Run** Cell 1a → 1b → YARN-verify → `load_qc_cohort` through to the 3 cohort MTs.

**Full PASS gate:**
- post_split read-back **succeeds** (no `URISyntaxException`; intermediate is the
  underscore name) → run proceeds into Phase 2;
- all **3 cohort MTs** — `mt_afr_qc`, `mt_afr_pca_selfid_qc`, `mt_eur_qc` — write with
  `_SUCCESS` + populated `rows/parts/` + **non-zero `n_samples` AND `n_variants`**;
- `_assert_checkpoint_nonempty` stays **silent** (raises on any 0×0, so no error = pass);
- nano-scaled **du-floor** cells pass.
- **Expected-and-fine:** het filter may be **skipped** on a 2 Mb window (`stdev > 0` guard).
  Driver quiet during Phase 2 = executors working (jstack + 0-spills discriminator still applies).

**On PASS:** I finalize the `/gsd-debug` session (→ `resolved/`, closeout commit,
knowledge-base entry) and we move to the **Gate C** decision.
**On FAIL:** paste the error — the `/gsd-debug` session is still open at human-verify.

## Required reading (in order)
0. `.planning/debug/m3-gateb-load-qc-cohort-driver-collect.md` — both root causes (driver-collect + colon H6) + fixes + RED evidence. **Start here.**
1. `.planning/HANDOFF.json` — full state (coordinates, cluster_sizing, completed/remaining, decisions, next_action).
2. `.planning/quick/260601-u1b-harden-chr22-smoke-into-tiered-cheap-fir/TIERED-VALIDATION-RUNBOOK.md` §0 + §2 — provisioning + Gate B run sheet.
3. Memory `[[project_aou_dataproc_hail_install]]` (MASTER/DRIVER SIZING + 4 env deltas) and `[[project_state]]` (top section).

## Decisions / constraints (do not re-litigate)
- **Master = n2-standard-16** (default n2-standard-2 OOMs the driver; create-time only; confirmed working).
- `load_qc_cohort` rebalances on the checkpoint **read-back** (`_n_partitions`), never a pre-write `repartition(shuffle=True)`.
- Intermediate checkpoint interval suffix MUST be sanitized via `_sanitize_interval_suffix` (`:`/`-` → `_`).
- Cluster = **Hail Genomics Analysis** (HAIL), n2-standard-16, NON-preemptible; use the env's Hail 0.2.135, no pip-pin.
- 1000G AFR = smoke-failure safety net ONLY (`[[feedback_no_1000g_ld_pivot]]`).
- Carter holds **every** AoU launch/compute/$ trigger. Explicit git paths only (never `git add -A`/`.`).

<blockers>
None. (Cluster deleted -> $0; re-fire #2 is a Carter-trigger compute fire. The /gsd-debug session is OPEN at human-verify pending that re-fire — the driver-collect fix is already live-proven; this re-fire proves the colon fix + completes the cohort.)
</blockers>
