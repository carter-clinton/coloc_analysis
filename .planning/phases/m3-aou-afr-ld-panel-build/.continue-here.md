---
context: phase
phase: m3-aou-afr-ld-panel-build
plan: 3
task: null
total_tasks: null
status: paused
last_updated: 2026-05-29T04:07:44.428Z
---

# Resume point — RW 2.0 MIGRATION COMPLETE; next = env-side CHECK C/D → AOU-0 → chr22 smoke

**One-line:** Recovery plan is **PRESERVE → MIGRATE → STAGE**. **Preserve + Migrate are DONE and verified.** You're into STAGE: run two quick env-side checks, then AOU-0 precheck, then the chr22 smoke.

## New RW 2.0 coordinates (memorize these — the bucket/project/CDR all changed)

| Field | Value |
|---|---|
| Workspace ID | **aou-rw-476cdac2** (title still `coloc_analysis`) |
| Google project | **wb-perky-corn-6639** (was terra-vpc-sc-fe7a5641) |
| `WORKSPACE_BUCKET` | **gs://rw-migration-aou-rw-476cdac2** (content at bucket ROOT) |
| CDR | **C2024Q3R9 (cdrv8–R9)** (was R8) |
| Billing pod | user-pod-cclinton-2d12 · region us-central1 |
| CLI | `wb workspace set --id=aou-rw-476cdac2` |

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention |
|---|---|---|---|
| `git add -A` on this tree | sweeps phantom-dirty `—`/em-dash notebooks + untracked `targeted_rerun_*`/`results_lsweep_*` | blocking | explicit paths only ([[feedback_multi_terminal_staging]]) |
| `_SUCCESS` = data | empty 0×0 MTs carried valid `_SUCCESS` markers ($2,140 catastrophe) | blocking | Track 4 patches self-validate count_rows/count_cols + entries-dir size; never bypass |
| Direct AoU↔NCSU bucket transfer | NCSU has no gsutil/gcloud; AoU buckets not externally readable (403) | advisory | use Path B (Files UI → laptop → scp) |
| RW 2.0 persistent disk | NOT supported on RW 2.0 — PD rule retired on this platform | advisory | envs are ephemeral; rely on bucket + git, not PD |

<current_state>
Legacy→RW 2.0 migration COMPLETE (no errors, non-destructive). Step-4 integrity validation CLOSED: 74.74 MiB / 24,721 objects / all 5 top-level dirs present (forensics 61.69, ld 1.34, m3-W1-forensics 0.36, m3-W2-forensics 10.91, notebooks 0.45 MiB). All artifacts committed+pushed to `origin/m3-W2-aou-deltas` through **a95b86c**; HEAD==origin. A RW 2.0 validation env (n1-standard-1) may still be running ($0.05/hr) — Stop/Delete it.
</current_state>

<completed_work>
- **G1** forensic mirror on NCSU (c81ac4d); **G2** Track 4 patches verified (5/4); **G3** pre-migration inventory + env-panel (5a9b752).
- **G0** disposition: PROCEED without Abby reply — preserve-first satisfies G0's purpose; Legacy decommissioned at 6/30 regardless (012abb1, `G0-DISPOSITION.md`).
- **MIGRATE** Legacy→RW 2.0 — legacy-initiated (workspace **Data tab** → select billing pod → **Start migration** → CONTINUE past PD warning). Playbook Steps 1+3 corrected to match real UI (34a58cc).
- **STEP 4 integrity** validated (a95b86c).
- **NCSU code-impact audit:** bucket-URI change = **ZERO code changes** (env-driven); CDR R8→R9 has **no SQL refs** (cohort is pure-Hail).
</completed_work>

<remaining_work>
- **CHECK C (env-side):** verify `AUX_BASE` (`gs://fc-aou-datasets-controlled/v8/wgs/short_read/snpindel/aux/{ancestry/ancestry_preds.tsv, relatedness/relatedness_flagged_samples.tsv}`) resolves from project `wb-perky-corn-6639` and is R9-consistent. If 404 → patch `AUX_BASE` at `aou_ld_panel.py:85`. (It feeds the ancestry filter + relateds anti-join — on the cohort critical path.)
- **CHECK D (env-side):** capture Hail version on RW 2.0.
- **AOU-0 precheck** on RW 2.0 (compute-free; git-clone the repo into the env first) → `post-precheck-routing-decision.txt`.
- **chr22 smoke** (256-vCPU Dataproc, ~$30–75) → pass = Wave 2 (apply `AOU-2-AOU-4-TRACK-4-PATTERN.md`); fail = 1000G AFR safety net.
</remaining_work>

<decisions_made>
- G0 = proceed without Abby (preserve-first). 1000G AFR = smoke-failure safety net only, not genome-wide substrate ([[feedback_no_1000g_ld_pivot]]). Bucket change = zero code impact; CDR R8→R9 = no SQL, only AUX_BASE env-check.
</decisions_made>

<blockers>
- None blocking. (Abby/Zendesk #57144 still unreplied but deliberately not blocking — G0 disposition logged.)
</blockers>

## Required Reading (in order)
1. `.planning/quick/260528-l8r-.../post-migration-validation.md` — new coordinates + the consolidated CHECK C/D terminal block + audit results.
2. `.planning/quick/260528-l8r-.../MIGRATION-PLAYBOOK.md` — corrected Step 3 (legacy-initiated) + post-migration pointers.
3. `.planning/STATE.md` → `## 2026-05-29 session continuation` (incl. migration-complete entry).
4. `.planning/quick/260528-l8r-.../G0-DISPOSITION.md` — why we migrated without Abby.

## Infrastructure State
- **RW 2.0 env:** a validation env (n1-standard-1) may still be running — **Stop/Delete** (recommend Delete; no PD on RW 2.0 = full $0). chr22 smoke needs a separate 256-vCPU Dataproc cluster; AOU-0 runs on a fresh tiny VM.
- **NCSU:** no gsutil/gcloud — Path B for all AoU↔NCSU transfers.
- **Branch:** `m3-W2-aou-deltas`, HEAD==origin==a95b86c.
- **Repo clone on RW 2.0 env is ephemeral** (no PD) — `git clone https://github.com/carter-clinton/coloc_analysis.git && git checkout m3-W2-aou-deltas` each fresh env; expect phantom `—` nbstripout churn (cosmetic; `git checkout -f`; never commit it).

<context>
Migration arc fully executed this session. Carter ran env-side commands via an in-Workbench Claude agent, relaying outputs here. Everything durable is committed; the only open work is two quick env-side checks + the next staging steps. Migration is genuinely done — don't re-litigate it on resume.
</context>

<next_action>
Run env-side **CHECK C** (AUX_BASE resolves on R9/new project?) + **CHECK D** (Hail version) in a RW 2.0 terminal — consolidated block is in `post-migration-validation.md`. Then close the CDR question, run **AOU-0 precheck**, then the **chr22 smoke**. Also Stop/Delete the validation env if still running.
</next_action>
