---
context: phase
phase: m3-aou-afr-ld-panel-build
plan: 3
task: null
total_tasks: null
status: paused
last_updated: 2026-06-04T12:00:00.000Z
---

# Resume point — RE-FIRE GATE C on the fixed branch (ordering bug fixed + GREEN NCSU-side)

**One-line:** Gate C (whole chr22) FAILED with the empty-MT-shaped **sample-axis collapse**
(AFR post_split 1,859,922×74,576 → post_sample_qc 1,859,922×0). Fully diagnosed on the live
cluster: a **QC-ordering bug** — the per-sample call_rate filter (≥0.98) ran BEFORE
variant_qc, so call_rate was computed over the raw pre-variant-QC ACAF set (FT no-calls →
max 0.8490 → 0/74576 pass). **Fixed** (commit `80d0a00`): variant_qc now runs first;
probe confirmed variant-QC-first → call_rate mean 0.9975, 74,558/74,576 kept. **Next = re-fire
Gate C** to confirm the sample axis survives end-to-end. Cluster = $0 (torn down).

## What's done (2026-06-04)

- **Gate C root cause CONFIRMED on live data** (read-only probe vs the hot post_split MT):
  `[A]` RAW call_rate max=0.8490 → 0/74576 pass 0.98; `[A2]` het exonerated (74142/74576);
  `[B]` variant-QC-first (1.86M→283,854 vars) → mean 0.9975, 74558/74576 pass. Numbers saved
  NCSU-side (`088a6fb`) — cluster-local probe txt was lost at teardown.
- **Fix landed + GREEN NCSU-side** (`80d0a00`): reorder variant_qc before sample_qc; the 500K
  nano guard keys on the RAW pre-variant-QC count (DEC-2026-06-04), persisted to the renamed
  `post_variant_qc` sidecar + restored on resume. Checkpoint topology renamed post_sample_qc
  → post_variant_qc. RESEARCH.md ordering + docstrings amended. Suite **125 passed / 31
  skipped**; the staged static ordering guard now passes normally (xfail removed).
- **NOT yet pushed** — commits `088a6fb`, `80d0a00` (+ the debug-doc/continue-here docs commit)
  are local on `m3-W2-aou-deltas`. PUSH before any AoU clone.

## Next action — RE-FIRE GATE C (Carter holds the trigger; ~$35-80, ~2h)

0. **Push first** (NCSU-side): `git push origin m3-W2-aou-deltas`. The AoU clone is public/no-auth;
   it MUST include `80d0a00` or the re-fire runs the buggy order again.
1. **Recreate** cluster: *JupyterLab Spark cluster for AoU (Dataproc)* + **"Software to install
   = Hail"**. Gate C scale: **24× n2-standard-16 = 384 vCPU**, n2-standard-16 master,
   non-preemptible. See `[[project_aou_dataproc_hail_install]]`.
2. **Clone + checkout:**
   ```bash
   cd ~ && git clone https://github.com/carter-clinton/coloc_analysis.git coloc_analysis
   cd ~/coloc_analysis && git checkout m3-W2-aou-deltas && git log --oneline -1   # expect 80d0a00 (or later docs commit)
   ```
3. Restart kernel, kill orphans, **set `INTERVAL = "chr22"`** (whole chromosome — resets on pull).
   Optional hygiene: delete the orphaned `mt_afr_post_split_chr22.mt` (+ `mt_afr_post_sample_qc*`
   if present) in `gs://.../ld/intermediate/` — the fix writes the new `post_variant_qc` name so
   stale intermediates are ignored, but a clean slate avoids confusion.
4. **Run** Cell 1a → 1b → YARN-verify → `load_qc_cohort` → du-floor cells (3.5/4.5/5.5) →
   cohort_summary.

**Full Gate C PASS gate (the fix confirmation):**
- **The AFR sample axis SURVIVES sample-QC** — post_variant_qc populated, then all 3 cohort MTs
  (`mt_afr_qc`, `mt_afr_pca_selfid_qc`, `mt_eur_qc`) write with `_SUCCESS` + **non-zero n_samples
  AND n_variants** (AFR expected ~74,558 samples × ~283,854 variants per probe [B]);
- the `[load_qc_cohort] state=...` + `wrote intermediate 2: ...post_variant_qc...` log lines appear
  (NOT post_sample_qc);
- the **call_rate sample filter APPLIES** (raw chr22 count ~1.86M > 500K → guard does NOT skip;
  expect the ~18 legit low-call-sample drops, not a collapse);
- du-floor cells report real GB-scale `entries/rows/parts/`;
- `cohort_summary_m3_chr22.tsv` writes 3 non-zero rows; `_assert_checkpoint_nonempty` silent.

**On PASS:** mark `m3-gatec-sample-callrate-ordering-collapse` resolved + archive to
`resolved/` + KB entry; close the sibling entries-path + driver-collect sessions; **greenlight
Wave 2 full-genome rebuild.**
**On FAIL:** paste the error; the session is open at `awaiting_human_verify`. NOT a 1000G
trigger — diagnose further ([[feedback_cheap_tier_fail_not_pivot]]).

## Open debug sessions
- `m3-gatec-sample-callrate-ordering-collapse` — `awaiting_human_verify`; fix landed (`80d0a00`),
  close on live re-fire PASS.
- `m3-entries-path-phantom-subpath` — `awaiting_human_verify`; close when du-floor cells pass on a real MT.
- `m3-gateb-load-qc-cohort-driver-collect` — fixes proved out live; resolvable in a convention pass.

## Decisions / constraints (do not re-litigate)
- Canonical QC order is split → **variant_qc → sample_qc** (call_rate over QC-passing variants).
  The original sample_qc-first order was the Gate C collapse root cause.
- The 500K nano guard keys on the RAW (pre-variant-QC) variant count — preserves the nano-smoke
  clean-PASS tier AND the validated threshold (DEC-2026-06-04).
- A cheap-tier PASS that SKIPS a check does not validate the check ([[feedback_cheap_tier_fail_not_pivot]]).
- Real entries path = `entries/rows/parts/` (corrected 2026-06-03).
- Push AoU-side files to origin before teardown or they are LOST. Carter holds every AoU $ trigger.

<blockers>
None. Root cause CONFIRMED + fix landed + GREEN NCSU-side (HEAD 80d0a00, unpushed). Gate C
re-fire is a Carter-trigger compute fire (~$35-80) to confirm the fix live. Push before cloning.
</blockers>
