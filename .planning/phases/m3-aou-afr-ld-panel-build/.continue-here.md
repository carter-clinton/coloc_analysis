---
context: phase
phase: m3-aou-afr-ld-panel-build
plan: 3
task: null
total_tasks: null
status: genome_wide_build_running_cohort1of3_verified
last_updated: 2026-06-07T19:35:00.000Z
---

# RESUME HERE → genome-wide cohort build LIVE; cohort 1/3 (AFR-primary) VERIFIED; AFR-sens running

**One-line:** The per-chromosome fan-out fix (`ab0853a`) is **PROVEN end-to-end on a full genome** — the `count_cols`/Py4J mutual-wait that wedged twice is gone. **AFR-primary cohort is COMPLETE + VERIFIED; AFR-sens (Cell 4) is running; EUR (Cell 5) still to come.** Claude monitors + gates only — **Carter holds every fire/kill/$ trigger.** HEAD `dedffe5` on `origin/m3-W2-aou-deltas` (no code changed this session). The prior "BLOCKED → /gsd-debug" version of this file is **SUPERSEDED** (the fix it pointed to was implemented, fired, and works).

## Cohort 1/3 — AFR-primary: DONE ✅
`mt_afr_qc.mt` = **73,122 samples × 20,767,864 variants**. Verified: direct `count_cols`/`count_rows` (not a marker); `_assert_checkpoint_nonempty` silent in-run; Cell 3.5 du-floor PASS (1,603.66 GB at `entries/rows/parts/`, ~1600× the 1 GB floor); fresh `_SUCCESS` 2026-06-07T15:37:30Z (`overwrite=True` clobbered the 06-04 stub). Sample −1.3% vs Gate-C's 74,059 = genome-wide `call_rate≥0.98` correctly dropping low-quality samples chr22-alone missed (filter working). Variants 20.8M in range. The Cell 3 `[ ]:` / viewport-stuck-at-chr12 is a websocket-drop front-end artifact (N/M print lost to the front-end, not the data — recovered via a read-only `count` cell). Do NOT re-run Cell 3.

## Cohort 2/3 — AFR-sens (Cell 4): RUNNING
Started ~16:19Z. As of **19:32Z: ~5 of 22 chroms** (raw split=5/var_qc=5 minus chr22 leftover), ~3h12m in, healthy — JVM PID 5932 alive (cores=1/5g, requester-pays CUSTOM), hail.log live 19:25 (collectDArray), selfid final `_SUCCESS` still 06-04 (tail not reached), no traceback. Pace tracks AFR-primary's largest-first profile (early chroms heavy, back half accelerates).

**Intermediates are path-isolated — VERIFIED IN CODE.** Fan-out threads `sensitivity=True` (aou_ld_panel.py:1480); `_intermediate_checkpoint_uri` inserts `_pca_selfid` (:486) → Cell 4 writes `mt_afr_pca_selfid_post_split_chrN` / `..._post_variant_qc_chrN`, NOT colliding with primary's `mt_afr_post_*_chrN`. **This isolation is the contamination guard**: had `sensitivity` not threaded, sens would be a byte-identical copy of primary and would have PASSED the 73K/20.8M sanity check undetected. Verify the path-builder, not just the count.

**Arbiter:** `gsutil ls gs://rw-migration-aou-rw-476cdac2/ld/intermediate/ | grep pca_selfid` → track `mt_afr_pca_selfid_post_variant_qc_chr*` climbing to 22, **discounting the chr22 06-04 leftover** (overwrites near the end). **Completion** = `mt_afr_pca_selfid_qc.mt/_SUCCESS` flip 06-04 → 06-07 **+ Cell 4.5 PASS**.

**Cell 4.5 anchors (distinctness shows as DRIFT, not match):** samples ≠ 73,122 (self-report vs genetic ancestry = different person sets; expect a few % drift, ~70–76K); variants ≈ 20.8M but probably not byte-identical. **Byte-identical 73,122 × 20,767,864 = contamination flag → PAUSE.**

## Remaining
Cell 4 (running) → 4.5 → **Cell 5 (EUR, long pole ~221K samples; expect ~217–220K × ~10–14M variants; watch the GC-heavy union→sample-QC→write tail)** → 5.5 → Cell 6 (disjoint AFR∩EUR=∅) → Cell 7 (`cohort_summary_m3.tsv`, 3 non-zero rows) → mirror `cohort_summary_m3.tsv` to GPFS → teardown (**Stop, not Delete**). Then close debug session `m3-W2-genome-wide-countcols-py4j-wedge` → `resolved/`.

## Monitoring stop conditions (else hold cadence)
Stop + surface only on: a traceback / final-MT arbiter flatlined >20–30 min **with JVM confirmed live** / an assertion failure. Liveness arbiter = GCS object listing, NOT jstack/PID/CPU ([[feedback_aou_fanout_gcs_listing_arbiter]]). Counts not markers ([[feedback_aou_success_marker_not_evidence_of_data]]).

## Coordinates
Workspace `aou-rw-476cdac2` / project `wb-perky-corn-6639`; bucket `gs://rw-migration-aou-rw-476cdac2` (`_forensics/` = Track-1 evidence, do NOT delete); WGS `gs://vwb-aou-datasets-controlled` (requester-pays); CDR `C2024Q3R8` (v8); Software=Hail (Spark 3.5.0 / hail 0.2.135), us-central1. Cluster live + billing while Idle, started from `...20260605` (Gate-C config). 3 env guards baked into `AOU-1_template.ipynb` (29d0a1f). GATE 0 egress ruling still open (before LD egress, not this build).

<blockers>
None blocking. Cohort 1/3 verified; cohorts 2 (AFR-sens, running) + 3 (EUR) still to land. Only risk surface = the EUR GC-heavy tail (watch the write timestamp + assertion, not thread state) and the Cell 4.5 contamination check (byte-identical = pause).
</blockers>
