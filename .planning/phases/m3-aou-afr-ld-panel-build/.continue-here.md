---
context: phase
phase: m3-aou-afr-ld-panel-build
plan: 3
task: null
total_tasks: null
status: paused
last_updated: 2026-05-29T00:20:59.174Z
---

# Resume point — m3-W1 catastrophe recovery, Phase 1 (PRESERVE) COMPLETE

**One-line:** The 3-step recovery plan is **PRESERVE → MIGRATE → STAGE**. Phase 1 (PRESERVE) is done and pushed. You're cleared into Phase 2 (MIGRATE). Hard deadline for the RW 2.0 migration is **2026-06-30**.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| nbstripout phantom churn | AOU-1/2/4 `.ipynb` show as modified on every checkout (`—` vs literal `—` JSON unicode-encoding from the AoU `nbstripout` clean filter's `ensure_ascii`). Looks like real edits; isn't. | advisory | Never commit those notebook "changes". On the AoU clone, `git checkout -f m3-W2-aou-deltas` is the correct unblock — committed versions are authoritative byte-identical. |
| `git add -A` / `-A` on this tree | Would sweep phantom notebook churn + many untracked `targeted_rerun_*` / `results_lsweep_*` dirs upstream. | blocking | Explicit paths only (per [[feedback_multi_terminal_staging]]). |
| `_SUCCESS` = data | A `_SUCCESS` marker is NOT evidence a Hail MT has contents. The m3-W1 catastrophe ($2,140) was empty 0×0 MTs with valid `_SUCCESS` markers. | blocking | Track 4 patches (landed) self-validate `count_rows/count_cols > 0` + check `entries/entries/parts/` size. Do NOT fire AoU compute that bypasses them. |

<current_state>
Phase 1 PRESERVE complete. Forensic mirror of the AoU workspace-bucket catastrophe state landed on NCSU and pushed to `origin/m3-W2-aou-deltas` at commit **c81ac4d** (tarball `forensic_mirror_20260528T231308Z.tar.gz` + greppable MANIFEST under `.planning/quick/260521-w1-catastrophe-handoff/forensic-mirror/`). HEAD == origin. AoU env is DELETED ($0/hr).
</current_state>

<completed_work>
- **G2** — Track 4 patches verified present on the AoU clone (`_validate_checkpoint_populated`=5, `_assert_checkpoint_nonempty`=4 in `src/python/aou_ld_panel.py`).
- **Forensic mirror** run in a recreated AoU env (gsutil-only, ~$0.05): bundle + MANIFEST + 28 MB `hail.log` preserved.
- **Hypothesis distinguisher RESOLVED** — `_SUCCESS` mtimes: `mt_afr_qc.mt`=2026-05-19T03:11:29Z, `mt_afr_pca_selfid_qc.mt`=2026-05-20T02:34:45Z, `mt_eur_qc.mt`=no marker. **Both before the 22:30 UTC 2026-05-20 kill → Hail-finalize / checkpoint-contract-violation theory SUPPORTED; Carter's kill-as-culprit hypothesis REFUTED for MT#1+MT#2.** Strengthens the Track 1 credit-recovery narrative.
- **G1** — mirror committed (tarball + MANIFEST; loose `*.log` gitignored) + pushed (c81ac4d).
</completed_work>

<remaining_work>
- **G3** — pre-migration inventory per `MIGRATION-PLAYBOOK.md`.
- **Phase 2 MIGRATE** — RW 2.0 migration Steps 1–4 (recommended window 2026-06-02..06-15; hard deadline 2026-06-30).
- **Phase 3 STAGE** — in RW 2.0: AOU-0 precheck → `post-precheck-routing-decision.txt` → AOU-1-chr22-smoke (≥256 vCPU) → pass = Wave 2 (AOU-2/4 paste-ins); fail = 1000G AFR safety net.
</remaining_work>

<decisions_made>
- **1000G AFR = smoke-failure safety net ONLY**, not the genome-wide substrate. Don't propose it proactively; keep the staged-doc fallback language. (Carter directive 2026-05-28; see [[feedback_no_1000g_ld_pivot]].)
- Commit tarball + MANIFEST only; loose 28 MB `hail.log` left gitignored (repo `.gitignore:95 *.log`), preserved inside the tarball.
- Bundle transfer used **Path B** (AoU Files UI → laptop → scp), because gsutil/gcloud are absent on NCSU and the AoU `fc-secure-*` bucket isn't externally readable.
</decisions_made>

<blockers>
- **External (non-blocking for migration):** AoU engineering reply pending on Zendesk #57144 (Abby Doyle) for credit recovery + RW 2.0 guidance. G1+G2 are satisfied, so migration is NOT gated on Abby.
- **Human action (blocking for env-side steps):** AoU env is DELETED — must be recreated for Phase 2 Step-side actions and Phase 3 notebooks.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` → `## 2026-05-28 session update` (incl. the appended `RESOLVED` note) — live state + the resolved hypothesis table.
2. `.planning/quick/260528-l8r-stage-aou-pre-check-chr22-smoke-aou-2-4-/MIGRATION-PLAYBOOK.md` — gates G0–G3 + Steps 1–4 for the RW 2.0 migration.
3. `.planning/quick/260528-l8r-stage-aou-pre-check-chr22-smoke-aou-2-4-/260528-l8r-SUMMARY.md` — staged-notebook inventory + open items.
4. `.planning/debug/m3-W1-empty-mt-catastrophe.md` — root-cause analysis (now corroborated by the mtime finding).

## Infrastructure State
- **AoU env:** DELETED ($0/hr). Recreate for any env-side step. Disk: Reattachable PD, Standard/HDD, default size; compute n1-standard-4 for gsutil/precheck work; 256 vCPU (≥16× n1-highmem-16) only for the chr22 smoke / Wave-2 Dataproc jobs.
- **NCSU:** no gsutil/gcloud (33 conda envs checked). Use Path B for any future AoU→NCSU transfer.
- **Branch:** on `m3-W2-aou-deltas`, HEAD == origin == c81ac4d. (Local `main` sits at the same commit but is divergent vs origin/main — ignore; work lives on `m3-W2-aou-deltas`.)
- **Known bug:** `run_forensic_mirror.sh` stdout summary uses `tail -1` which grabs the `TOTAL:` footer instead of the mtime line. MANIFEST captured the real lines. Fix summary to `grep _SUCCESS` / `sed -n '2p'` in a later quick task.

<context>
Session began as `/gsd-resume-work`, became execution of Phase 1 PRESERVE. Everything durable is committed; nothing uncommitted matters (the only working-tree change is the long-standing `M .claude/settings.json` + pre-existing untracked dirs, both intentionally left alone). The competing hypothesis is now closed — no need to re-run the mtime test.
</context>

<next_action>
Pick Phase 2 entry: (a) run gate **G3 pre-migration inventory** per MIGRATION-PLAYBOOK.md, or (b) check **Zendesk #57144** first and paste Abby's reply for combined reasoning. Either way, the AoU env must be recreated for env-side steps. Migrate before **2026-06-30**.
</next_action>
