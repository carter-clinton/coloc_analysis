---
context: phase
phase: m3-aou-afr-ld-panel-build
plan: 3
task: null
total_tasks: null
status: paused_before_genome_wide_cohort_rebuild
last_updated: 2026-06-05T00:00:00.000Z
---

# RESUME HERE → fire the genome-wide cohort rebuild (`AOU-1_template.ipynb`)

**One-line:** Gate C PASSED (chr22, all 3 cohorts). Wave 2 is fully scoped + audited + all
NCSU bugs fixed + cluster decided. **PAUSED before the next AoU fire = the genome-wide cohort
rebuild.** HEAD `e5e673f` on `origin/m3-W2-aou-deltas` (all pushed). Cluster = $0. Authoritative
detail: [`WAVE-2-PLAN.md`](./WAVE-2-PLAN.md).

## Why this is the next fire (not the LD step)
The bucket cohort MTs (`mt_afr_qc` / `mt_eur_qc` / `mt_afr_pca_selfid_qc`) are **chr22-ONLY** —
Gate C's smoke overwrote the production paths. AOU-2 (LD) reads them directly, and dev-10/
production regions span all autosomes → LD would skip every region. So the cohorts must be
rebuilt **genome-wide** first. This is the first full-scale run of the now-fixed pipeline
(the m3-W1 catastrophe was the *failed* genome-wide attempt; Gate C proved the fixes on chr22).

## Pre-flight (Carter, ~30 sec)
- Confirm AoU credits cover this fire (big genome-wide read+QC; ~3–6 h, EUR the long pole).
- Confirm CDR **v8**, no migration mid-flight.
- **No egress ruling needed** for this step (writes to your bucket only; egress ruling is GATE 0
  for the later LD waves).

## Run sheet

**1. Cluster:** *JupyterLab Spark cluster for AoU (Dataproc)* + **"Software to install = Hail"**.
Workers = `n2-standard-16`, **MAX to your vCPU quota** (Gate C ran 24/384 vCPU; go higher, e.g.
32–48), **non-preemptible**. Master = `n2-standard-16` (64 GB; or `n2-standard-32` for headroom).
Do NOT add Spark config — the `executor.cores=1` + 5g/executor no-overthread lever is baked in.

**2. Terminal:**
```bash
cd ~ && git clone https://github.com/carter-clinton/coloc_analysis.git coloc_analysis 2>/dev/null
cd ~/coloc_analysis && git fetch origin && git checkout m3-W2-aou-deltas && git pull origin m3-W2-aou-deltas
git log --oneline -1     # MUST show e5e673f or later (all Wave-2 fixes)
# optional hygiene (orphaned chr22 intermediates from Gate C; harmless):
gsutil -m rm -r ${WORKSPACE_BUCKET}/ld/intermediate/mt_*_chr22*.mt 2>/dev/null
```

**3. Kernel:** Restart Kernel; `ps -ef | grep -E 'ipykernel|java' | grep -v grep` (kill -9 any orphan).

**4. Notebook:** open the **production `AOU-1_template.ipynb`** (NOT the chr22-smoke template).
**Nothing to set** — it's genome-wide by default (calls `load_qc_cohort` with no `interval_filter`).
**Restart Kernel → Run All.** (Cell 1a env → 1b `hl.init`/YARN → cohort cells AFR/selfid/EUR →
du-floor → cohort_summary.)

## PASS gate
- 3 MTs `_SUCCESS` + **genome-wide variant counts (MILLIONS, not 283K)** + non-zero samples
  (AFR ~74K, EUR ~221K).
- du-floor cells report real GB at `entries/rows/parts/`; `_assert_checkpoint_nonempty` silent.
- Log shows `wrote intermediate 2: …post_variant_qc…` (the fixed ordering).

## Watch / report back to Claude
- **Paste the AFR `post_split` timestamp** the moment it lands → tight ETA for the rest.
- This is the first full-scale run of the fixed pipeline (the m3-W1 catastrophe fire). Expect it
  to work; watch closely.
- If **EUR's final phase parks in `collectDArray`**, DON'T kill — run the jstack-CPU discriminator
  first (`jstack <PID>` ×2, 45 s apart; cpu= advancing on the worker = slow, not wedged). It's
  almost certainly slow (221K samples), not stuck.

## After the cohort rebuild PASSES → the LD waves
1. `AOU-2_per_region_ld.ipynb`, `USE_DEV_SUBSET = True` → dev-10 LD + AOU-4 Checks 1–4. (All 10
   dev regions are large/xlarge → Path A.3; first live test of the OOM-veto + MED-4 `.bm`
   validation + HIGH-3 banding.) Then `USE_DEV_SUBSET = False` → 322-cell production LD.
2. 44 per-chromosome egress requests (needs GATE 0 classification ruling in hand).

## Decisions / constraints (do not re-litigate)
- Canonical QC order = split → variant_qc → sample_qc (fixed). 500K nano guard keys on RAW count.
- compute_region_ld OOM-veto: span > PATH_A2_MAX_MB (10 Mb) → A.3, never `to_numpy` (HIGH-1 fixed).
- xlarge LD is 50-Mb-banded (HIGH-3 accepted); downstream SuSiE/RDS must treat it as banded.
- Cluster = max n2-standard-16 to quota, executor.cores=1 (no-overthread), non-preemptible.
- Cohort MTs must be genome-wide before any LD fire. Carter holds every AoU $ trigger.

<blockers>
None. All NCSU pre-fire work done + pushed (HEAD e5e673f). The next AoU fire (genome-wide cohort
rebuild) is a Carter-trigger compute step, paused only because Carter is relocating. Resume by
firing AOU-1_template.ipynb per the run sheet above.
</blockers>
