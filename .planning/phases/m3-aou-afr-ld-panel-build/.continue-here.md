---
context: phase
phase: m3-aou-afr-ld-panel-build
plan: 3
task: null
total_tasks: null
status: paused
last_updated: 2026-06-03T00:00:00.000Z
---

# Resume point — RE-FIRE Gate B #3 (call-rate degeneracy guard landed; needs the live PASS)

**One-line:** Gate B re-fire #2 (2026-06-03) reproduced an "empty-MT" signature at the
nano tier — but it was **NOT** the m3-W1 catastrophe and **NOT** a 1000G pivot trigger.
Triangulated diagnosis: a **sample-axis collapse** (118,903 rows × **0 cols**) from the
**unguarded `call_rate` sample filter** measured over un-variant-QC'd variants on a 2 Mb
window — a **scale-DEPENDENT** artifact that PASSES genome-wide. The colon fix (a96f2cf)
and driver-collect fix held. **Fix landed + pushed** (degeneracy guard + truthful
provenance + assertion-message fix + regression test; tests/m3 122 passed). Re-fire #3 is
the cheap (~$1–3) live proof.

## What's done (2026-06-03)

- **Diagnosis (HIGH, triangulated):** debugger code analysis + Carter's byte-level
  forensics (post_split part-0 245,342 B → post_sample_qc 35 B; Hail log "118903 rows and
  0 columns" → cols honestly 0 in-memory, NOT finalize corruption) + code review. Cause =
  `filter_cols(sqc.call_rate >= 0.98)` at aou_ld_panel.py:1461, unguarded, on ~119K
  un-variant-QC'd variants. H3 (aux-join) eliminated (post_split kept all cols).
- **Fix landed + PUSHED** to `origin/m3-W2-aou-deltas`, HEAD = **`121ba3a`**:
  - `MIN_VARIANTS_FOR_SAMPLE_CALLRATE = 500_000` + het-style degeneracy guard (skip +
    loud log + `sample_callrate_filtered` provenance flag below the floor).
  - `_assert_checkpoint_nonempty` message branched: N×0 = "sample axis collapsed during
    QC", 0×0 = finalize catastrophe (verbatim).
  - Faithful regression test (fixture missingness knob + Phase-2 col-retention + a
    genome-scale "still drops bad sample" lock). tests/m3 **122 passed / 29 skipped**.
  - Genome-scale path byte-identical.
- **Debug session** `.planning/debug/m3-gateb-nano-sample-axis-collapse.md` =
  `awaiting_human_verify` (the live PASS is the column-retention proof — can't run NCSU).
  Confirming forensics preserved at `.planning/debug/m3-W2-gateB-nano-FAIL-diagnostics.md`.
- **Cluster torn down → $0.** Bucket persists; orphaned nano intermediates are harmless.

## Next action — RE-FIRE GATE B #3 (Carter holds the trigger + ~$1–3)

1. **Recreate** cluster: **JupyterLab Spark cluster for AoU (Dataproc)** with **"Software
   to install = Hail"** (do NOT trust the `Spark_20260602` saved preset — its Software was
   None → no Hail). **n2-standard-16 MASTER** + **4× n2-standard-16** workers,
   non-preemptible. See `[[project_aou_dataproc_hail_install]]` (LIVE CORRECTIONS 2026-06-03).
2. **Clone (public, no auth) + pull:**
   ```bash
   cd ~ && git clone https://github.com/carter-clinton/coloc_analysis.git coloc_analysis
   cd ~/coloc_analysis && git checkout m3-W2-aou-deltas && git log --oneline -1   # expect 121ba3a
   ```
   (Fresh instance has NO repo; workspace has no git-repo resource → manual clone.)
3. **Recreate** the `/home/jupyter → /home/dataproc` symlink (per-instance; does not persist).
4. **Clean kernel:** Restart Kernel; `ps -ef | grep -E "ipykernel|java" | grep -v grep` → kill -9 orphans.
5. **Set INTERVAL = nano** (resets on pull): `INTERVAL = "chr22:16000000-18000000"`.
6. **Run** Cell 1a → 1b → YARN-verify → `load_qc_cohort` through the 3 cohort MTs.

**Full PASS gate:**
- log shows `[load_qc_cohort] SKIP call_rate sample filter — only 118903 variants (< 500000) ...`;
- `post_sample_qc` checkpoint writes with **non-zero cols** (no axis-collapse RuntimeError);
- all **3 cohort MTs** (`mt_afr_qc`, `mt_afr_pca_selfid_qc`, `mt_eur_qc`) write with
  `_SUCCESS` + populated rows/parts + **non-zero n_samples AND n_variants**;
- `_assert_checkpoint_nonempty` stays silent; du-floor cells pass.
- Expected-and-fine: het filter skipped on a 2 Mb window (`stdev > 0` guard).

**On PASS:** finalize the `/gsd-debug` session (→ resolved/, KB entry, archive commit),
refresh `[[project_state]]`, tee up the **Gate C** decision (chr22-full — where sample-QC
thresholds finally validate on ~2.4M variants).
**On FAIL:** paste the error; debug session is still open at human-verify.

## Decisions / constraints (do not re-litigate)
- **This was NOT a pivot trigger.** AoU AFR WGS stays the committed substrate; 1000G is
  smoke-fail safety net only. `[[feedback_cheap_tier_fail_not_pivot]]`, `[[feedback_no_1000g_ld_pivot]]`.
- Master = n2-standard-16 (create-time only; n2-standard-2 OOMs the driver).
- Cluster Hail = the "Software to install" toggle, NOT the saved preset.
- Push AoU-side files to origin before teardown or they are LOST (`[[project_aou_dataproc_hail_install]]` #4).
- Carter holds every AoU launch/compute/$ trigger. Explicit git paths only.

<blockers>
None. Fix landed + pushed (HEAD 121ba3a). Re-fire #3 is a Carter-trigger compute fire
(~$1–3) for the live column-retention PASS. /gsd-debug session OPEN at human-verify.
</blockers>
