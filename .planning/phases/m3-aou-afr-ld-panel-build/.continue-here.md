---
context: phase
phase: m3-aou-afr-ld-panel-build
plan: 3
task: null
total_tasks: null
status: paused
last_updated: 2026-06-03T12:00:00.000Z
---

# Resume point — GATE C decision + fire (Gate B PASSED; both NCSU fixes landed + pushed)

**One-line:** Gate B (nano tier) is a **substantive PASS** — the m3-W1 empty-MT catastrophe
did NOT reproduce; the call-rate degeneracy guard is LIVE-VERIFIED on AoU (sample axis
retained on all 3 cohorts). Two NCSU fixes from this session — the sample-axis guard and the
**entries-path phantom-subpath** Gate-C blocker (+ `/home/jupyter` portability) — are landed,
tested (tests/m3 124 passed / 30 skipped), and **pushed** (`origin/m3-W2-aou-deltas` @
`288d4bd`). **Next = Gate C** (chr22-full): the real test, where sample-QC thresholds
validate on ~2.4M variants and the two fixes get their live confirmation. Cluster = $0.

## What's done (2026-06-03)

- **Gate B #3 = PASS (substantive).** AFR `mt_afr_qc.mt` 18,648×74,065; AFR-selfid
  `mt_afr_pca_selfid_qc.mt` populated; EUR `mt_eur_qc.mt` 10,199×221,572 (~3.25 GB) — all
  `_SUCCESS`, sample axis retained end-to-end, `_assert_checkpoint_nonempty` silent. Guard
  SKIP log fired live (call_rate mean=0.7942 max=0.8503 on the 2 Mb window). Framing: "no
  cheap failure reproduced → escalating to the real test," NOT "validated."
- **Sample-axis-collapse `/gsd-debug` = RESOLVED** (`.planning/debug/resolved/`; closeout `202ec4d`).
- **Entries-path `/gsd-debug` = fix landed, `awaiting_human_verify`.** Corrected
  `entries/entries/parts` → `entries/rows/parts` at all functional sites (guard, resume
  mtime check, 4 notebooks, tests) + a hail-gated ground-truth test that pins the real Hail
  layout + bundled `/home/jupyter` portability (no more symlink bridge). Commits
  `195766b`/`39ad37e`/`2054859`/`288d4bd`. The count-based gate + sample-axis/colon/
  driver-collect fixes are untouched.
- **Everything pushed** — `origin/m3-W2-aou-deltas` @ **`288d4bd`**. Cluster torn down → **$0**.

## Next action — GATE C (Carter holds the trigger; ~$35-80, ~2h)

1. **Recreate** cluster: **JupyterLab Spark cluster for AoU (Dataproc)** + **"Software to
   install = Hail"** (NOT the `Spark_20260602` preset). For Gate C scale: **24× n2-standard-16
   = 384 vCPU**, n2-standard-16 master, non-preemptible. See `[[project_aou_dataproc_hail_install]]`.
2. **Clone (public, no auth) + pull:**
   ```bash
   cd ~ && git clone https://github.com/carter-clinton/coloc_analysis.git coloc_analysis
   cd ~/coloc_analysis && git checkout m3-W2-aou-deltas && git log --oneline -1   # expect 288d4bd
   ```
   (`/home/jupyter` symlink should NO LONGER be needed — the portability fix landed; if a
   path error still appears, the symlink bridge remains a safe fallback.)
3. Restart kernel, kill orphans, **set `INTERVAL = "chr22"`** (whole chromosome — Gate C).
4. **Run** Cell 1a → 1b → YARN-verify → `load_qc_cohort` → the du-floor cells (3.5/4.5/5.5)
   → cohort_summary.

**Full Gate C PASS gate:**
- all 3 cohort MTs write with `_SUCCESS` + non-zero n_samples AND n_variants;
- **du-floor cells now report real GB-scale `entries/rows/parts/`** (NOT 0 bytes — this is
  the live confirmation of the entries-path fix; on success → CLOSE the
  `m3-entries-path-phantom-subpath` debug session);
- the **call_rate sample filter now APPLIES** (≥2.4M variants > the 500K floor, so the guard
  does NOT skip — real sample-QC threshold validation, the thing the nano tier couldn't do);
- `cohort_summary_m3_chr22.tsv` writes (3 rows, all non-zero);
- `_assert_checkpoint_nonempty` silent.

**On PASS:** full-genome rebuild greenlit (Wave 2); close the entries-path debug session.
**On FAIL:** diagnose scale-dependence BEFORE any 1000G pivot ([[feedback_cheap_tier_fail_not_pivot]]);
1000G remains smoke-fail safety net only ([[feedback_no_1000g_ld_pivot]]).

## Open debug sessions (close on Gate C confirmation)
- `m3-entries-path-phantom-subpath` — `awaiting_human_verify`; close when du-floor cells pass on a real MT.
- `m3-gateb-load-qc-cohort-driver-collect` — fixes proved out live this round; resolvable in a convention pass.

## Decisions / constraints (do not re-litigate)
- Gate B FAILs on scale-dependent QC metrics are NOT pivot triggers ([[feedback_cheap_tier_fail_not_pivot]]).
- Real entries path = `entries/rows/parts/` (NOT `entries/entries/parts/`) ([[feedback_aou_success_marker_not_evidence_of_data]], corrected 2026-06-03).
- Cluster Hail = the "Software to install" toggle, NOT the saved preset; master n2-standard-16.
- Push AoU-side files to origin before teardown or they are LOST.
- Carter holds every AoU launch/compute/$ trigger. Explicit git paths only.

<blockers>
None. Gate B PASSED; both fixes landed + pushed (HEAD 288d4bd). Gate C is a Carter-trigger
compute fire (~$35-80). Two debug sessions await Gate-C live confirmation to close.
</blockers>
