---
context: phase
phase: m3-aou-afr-ld-panel-build
plan: 3
task: null
total_tasks: null
status: gate_1_5_complete_all_3_cohorts_banked_afr_sens_recovered
last_updated: 2026-06-11T00:00:00.000Z
---

# RESUME HERE → GATE 1.5 COMPLETE — all 3 genome-wide cohorts banked (AFR-sens recovered)

**Primary resume sources (read in this order):**
1. `.planning/STATE.md` → the **2026-06-11 ★ RESUME HERE ★** block.
2. `.planning/HANDOFF.json` → structured state (cohort table, deferred green-lights).
3. `.planning/phases/m3-aou-afr-ld-panel-build/WAVE-2-GATE-READINESS.md` → the gate sequence for what's next.

## One-line
The AFR-sens empty-final catastrophe (H1 — driver killed mid-finalize) was **recovered**: a finalize-only re-drive from the 22 intact intermediates was proven in a read-only `_scratch` dry-run, then promoted to the live URI via `gsutil -m rsync -r -d` and re-verified at the data layer. **All 3 cohorts are now banked genome-wide** and `cohort_summary_m3.tsv` is at 3 rows.

## Banked cohorts (data-layer verified)
| cohort | samples | variants | path |
|---|---|---|---|
| AFR_pca | 73,122 | 20,767,864 | `…/ld/mt_afr_qc.mt` |
| EUR_pca | 220,098 | 11,375,140 | `…/ld/mt_eur_qc.mt` |
| AFR_pca_selfid | 62,557 | 20,817,925 | `…/ld/mt_afr_pca_selfid_qc.mt` |

AFR-sens shape verified: cols 62,557 (< 63,312 union < 73,122 primary — contamination tell absent), rows 20,817,925, `self_report` present, du-floor 1,380.19 GB. Scratch dropped post-verification.

## Next — deferred green-lights (each its OWN trigger; Carter holds all; NOT auto-fired)
1. **Delete the contaminated v1** at `ld/_forensics/contaminated_afr_selfid_noop_20260608/` (~7.8 TiB reclaim) — separate green-light.
2. **Teardown the cluster** — **Stop, not Delete** ([[feedback_aou_disk_type_check]]).
3. **Close debug sessions** → `resolved/`: `m3-W2-afr-sens-empty-final-merge.md`, `m3-W2-afr-sensitivity-selfid-noop.md`, `m3-W2-genome-wide-countcols-py4j-wedge.md`.
4. **Commit** the banking (explicit paths): `cohort_summary_m3.tsv`, `cohort_summary_m3.NOTES.md`, `WAVE-2-GATE-READINESS.md`, `DURABLE-FIX-DESIGN-atomic-final-write.md`, this file, `STATE.md`, `HANDOFF.json`.

## Then — Wave 2 (LD compute), gated, see WAVE-2-GATE-READINESS.md
- 🔴 **GATE 0** (egress classification ruling) — the long pole; **file/confirm in parallel now**, it's not on the cohort path and has an AoU SLA. Blocks GATE 3 production egress.
- 🟠 **GATE 1** Carter-only: CDR pin (v8), cost/credit confirmation.
- 🟡 **GATE 2** dev-10 LD fire + AOU-4 validation memo (does NOT need GATE 0) — unblocked once GATE 1 clears.
- **Durable atomic-final-write fix** (design staged: `DURABLE-FIX-DESIGN-atomic-final-write.md`) via `/gsd-plan-phase --gaps` — slot before GATE 3 production.

## Untouched / safe
Clean cohorts + the recovered AFR-sens live MT + the 22 intermediates + `_forensics` all intact. Carter holds every fire/kill/$/delete trigger.
