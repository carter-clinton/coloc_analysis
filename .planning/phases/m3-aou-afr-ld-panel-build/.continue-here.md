---
context: phase
phase: m3-aou-afr-ld-panel-build
plan: 3
task: null
total_tasks: null
status: running_unattended_genome_wide_cohort_rebuild
last_updated: 2026-06-06T00:15:00.000Z
---

# RESUME HERE → reconnect to the RUNNING genome-wide cohort rebuild

**One-line:** The production `AOU-1_template.ipynb` genome-wide cohort build **fired and is RUNNING
server-side** on the AoU Dataproc cluster (YARN app `application_1780701402498_0001`, ~10% at
disconnect). Carter relocated/disconnected ~2026-06-06 00:15 UTC; the job continues on the cluster.
**Do NOT restart the kernel — reconnect and check state first.** HEAD `997461b` on
`origin/m3-W2-aou-deltas`. Authoritative detail: [`HANDOFF.json`](../../HANDOFF.json),
[`WAVE-2-PLAN.md`](./WAVE-2-PLAN.md).

## Why this run matters
The bucket cohort MTs were **chr22-ONLY** (Gate C smoke overwrote the production paths). AOU-2 (LD)
reads them directly and regions span all autosomes → LD couldn't run until a genome-wide rebuild.
This run is that rebuild — the **first full-scale run of the now-fixed pipeline** (m3-W1 was the
failed genome-wide attempt; Gate C proved the fixes on chr22).

## Live job coordinates (as of disconnect)
- YARN app: **`application_1780701402498_0001`** ("Hail"), RUNNING ~10% (still in
  load-WGS / cohort-def, before `split_multi` → `post_split` → checkpoint; no AFR `post_split` yet).
- Kernel PID **31804** (`ipykernel_launcher`); Hail driver JVM PID **32639** (`hail-all-spark.jar`).
- SparkUI: `http://<master ...20260605-m>:36349`. Hail log: `/tmp/hail.log`.
- Cells 1a `[2]` + 1b `[3]` clean. Cluster: 1× n2-standard-16/500GB master + 24× n2-standard-16
  workers, non-preemptible, Software=Hail (Spark 3.5.0 / hail 0.2.135), us-central1.
- Workspace `aou-rw-476cdac2` / project `wb-perky-corn-6639`, CDR `C2024Q3R8` (v8).

## RECONNECT PROTOCOL (do this first, do NOT restart the kernel)
1. Reattach the AoU env and open a **terminal**.
2. `ps -ef | grep -E 'ipykernel|java' | grep -v grep` → confirm the kernel (was 31804) + Hail JVM
   (was 32639) are still alive.
3. `yarn application -list` → is `application_1780701402498_0001` **RUNNING** or **FINISHED/FAILED**?
4. `grep -iE 'post_split|post_variant_qc|wrote intermediate|Traceback|Error' /tmp/hail.log; tail -n 60 /tmp/hail.log`
5. Branch on state:
   - **RUNNING** → keep watching. Paste the **AFR `post_split` timestamp** for a tight ETA. If EUR
     parks in `collectDArray`, `jstack <PID>` ×2 (~45 s apart) **before** any kill (almost certainly
     slow at 221K samples × more genome-wide partitions, not wedged).
   - **SUCCEEDED** → verify the PASS gate (below), then mirror `cohort_summary_m3.tsv` to GPFS.
   - **FAILED** → `gsutil cp /tmp/hail.log gs://rw-migration-aou-rw-476cdac2/ld/_forensics/hail_failrun.log`,
     capture the traceback, `/gsd-debug`.

## PASS gate
- 3 MTs `_SUCCESS` + **genome-wide variant counts (MILLIONS, not the chr22 283K)** + non-zero
  samples (AFR ~74K, EUR ~221K).
- du-floor cells report real GB at `entries/rows/parts/`; `_assert_checkpoint_nonempty` silent.
- Log shows `wrote intermediate 2: …post_variant_qc…` (fixed ordering); `cohort_summary_m3.tsv`
  3 non-zero rows; Cell 6 AFR∩EUR=∅.

## ⚠️ Two live-session gotchas (operationally resolved; durable fixes PENDING)
1. **WORKSPACE_BUCKET template pollution.** The `Spark_20260604` saved cluster config injected a
   stale placeholder `gs://cloned-mybucket-wb-perky-corn-6639` (**404, never existed**); the kernel
   had it unset (`None`). Diagnosed Row-1 (only this var wrong; project/UFID/CDR/WGS paths all
   correct; cluster SA fully owns the real bucket). **Fix = a MANUAL Cell 1a guard** pinning
   `WORKSPACE_BUCKET=gs://rw-migration-aou-rw-476cdac2` (verified: printed
   `[bucket-guard] overriding None -> gs://rw-migration-aou-rw-476cdac2`) + terminal `export`.
   **This guard is NOT in the repo** — if AOU-1 must be re-run from a fresh clone, **RE-APPLY it
   before Run All** or it writes to the 404 placeholder. Durable: bake into repo Cell 1a
   ([[feedback_aou_cluster_template_bucket_pollution]]).
2. **nbformat filter re-dirties notebooks.** A clean/smudge filter (the `MissingIDFieldWarning`
   source — adds cell IDs) re-dirties the 3 AOU notebooks on every git op, so `git checkout -- `
   can't clean the tree. Use **`git checkout -f m3-W2-aou-deltas`** (atomic discard+switch). The
   cluster clone arrived on `main @ 50f071c` dirty; `-f` landed `997461b`.

## After the rebuild PASSES → the LD waves
1. `AOU-2_per_region_ld.ipynb`, `USE_DEV_SUBSET = True` → dev-10 LD + AOU-4 Checks 1–4 (all 10 dev
   regions large/xlarge → Path A.3; first live test of OOM-veto + MED-4 `.bm` validation + HIGH-3
   banding). Then `USE_DEV_SUBSET = False` → 322-cell production LD.
2. 44 per-chromosome egress requests — needs GATE 0 (egress classification ruling) in hand.
3. Durable NCSU follow-ups: bucket-guard commit, nbformat-filter doc, ~54-deletion triage.

## Decisions / constraints (do not re-litigate)
- Canonical QC order = split → variant_qc → sample_qc (fixed). 500K nano guard keys on RAW count.
- compute_region_ld OOM-veto: span > 10 Mb → A.3, never `to_numpy` (HIGH-1).
- xlarge LD is 50-Mb-banded (HIGH-3); downstream SuSiE/RDS must treat it as banded.
- Cluster = max n2-standard-16 to quota, executor.cores=1 (no-overthread), non-preemptible.
- `_forensics/` in the LD bucket = Track 1 credit evidence — DO NOT delete.
- Carter holds every AoU $ trigger.

<blockers>
None. The genome-wide cohort rebuild is RUNNING server-side and unattended. Resume by reconnecting
per the protocol above — do NOT restart the kernel.
</blockers>
