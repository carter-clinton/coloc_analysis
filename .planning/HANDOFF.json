{
  "version": "1.0",
  "timestamp": "2026-06-29T07:05:00.000Z",
  "phase": "m3",
  "phase_name": "m3-aou-afr-ld-panel-build",
  "phase_dir": ".planning/phases/m3-aou-afr-ld-panel-build",
  "wave": "m3-02e — native LD export + public EUR. T1-3 DONE+pushed; T4 STEP 0/2/3 DONE. T4 STEP 4 (the 276-region AFR native-plink LD loop) is between fires: the 2026-06-28 re-fire reached region 1 then OOM-killed on the .npz convert step (banked 0/276 a SECOND time, different bug). The OOM is FIXED (3eac803); re-fire pending on a high-mem VM.",
  "status": "2026-06-29 — RE-FIRED LOOP OOM-KILLED ON REGION 1's .npz CONVERT (banked 0/276 again — UNRELATED to the chr-prefix bug, which is now PROVEN GOOD). The 2026-06-28 re-fire (PID 6577, a5a5f9f) reached region 1; plink --r square bin4 FINISHED and emitted the 102,421-var .ld.bin (so the chr-prefix verify fix worked — the window matched), but the very next step (Python .ld.bin->.npz convert/bank) OOM-killed: anon-rss ~61 GiB on the 64 GB VM. Root cause: read_square_bin's symmetry check np.allclose(m, m.T) materializes several full n_var^2 float32 temporaries (~39 GiB each at n_var=102,421) on top of the 39 GiB matrix -> >64 GiB peak -> kernel OOM. np.savez_compressed is NOT the killer (numpy streams chunked); the diagonal check is tiny. FIXED this session: _is_symmetric_blocked blocked check bounds the transient to ~420 MB while enforcing the identical invariant (commit 3eac803; tests/m3 test_plink_ld_to_npz 17 passed, full suite 302 passed/30 skipped; quick 260629-2im). Carter STOPPED the VM (idle-billing halted; stop != delete). DECIDED: re-fire on n2-highmem-16 (128 GB) for headroom on any region >~115k variants. ++ BLAST-RADIUS (2026-06-29) CAUGHT A SECOND OOM HEAD that the 3eac803 fix alone left live: content_verify_npz (run_native_ld_panel.py:228, the in-process D-M3-10 gate run right after the convert at :475) reloads the full .npz and re-runs the IDENTICAL unbounded np.allclose(ld, ld.T) -> ~4x matrix ~156 GiB at n_var=102,421 > the 128 GB VM -> would OOM region 1 AGAIN one function later. FIXED @ ff9e66f (quick 260629-402): square -> reuse pln._is_symmetric_blocked; banded -> new pln._strict_upper_is_zero_blocked; reason strings byte-identical; targeted 48 passed, full tests/m3 305 passed/30 skipped. BOTH square convert->verify OOM heads now bounded (region-1 peak ~40 GiB). The re-fire git-pull target is now ff9e66f, NOT 3eac803.",
  "headline": "Loop OOM-killed on region 1's .npz convert (NOT chr-prefix — that fix is proven; region-1 plink finished + emitted the 102,421-var .ld.bin). OOM fix landed in TWO commits: 3eac803 (read_square_bin) + ff9e66f (content_verify_npz, the 2nd head blast-radius caught). VM AoU_Jupyter_ComputeEngine_20260626b STOPPED by Carter (n2-standard-16, 1TB, Hail-free; reattachable PD + 379 GB local bfile preserved). RE-FIRE PENDING: push ff9e66f to origin (verify tip==HEAD) -> AoU agent git pull >= ff9e66f -> AoU UI respec the VM to n2-highmem-16 (128 GB) (stop->edit machine type->start; PD+bfile survive untouched) -> pre-flight region-1 window ~102,421 -> re-fire from region 1 (0 banked = clean restart) -> proof point .npz 0->1 + df flat + panel row status==ok. STOP-on-complete (stop != delete) still holds at 276.",
  "branch": "m3-W2-aou-deltas",
  "origin_synced": "BOTH OOM-head fixes are committed locally: 3eac803 (read_square_bin symmetry check) + ff9e66f (content_verify_npz square+banded). MUST be pushed to origin and origin tip verified == local HEAD BEFORE the AoU agent git-pulls and re-fires (feedback_push_ncsu_before_aou_clone_fire — the fix is inert on AoU until pushed). The loop VM repo is currently at a5a5f9f (the OOM'd code) and must be pulled to >= ff9e66f (NOT just 3eac803 — 3eac803 alone still OOMs at the content-verify step) before re-fire.",
  "running_now": "NOTHING running in-perimeter. The loop VM AoU_Jupyter_ComputeEngine_20260626b was STOPPED by Carter after the OOM (idle-billing halted; stop != delete — VM + reattachable PD + local bfile preserved, resume-safe). NCSU-side: nothing running, $0. Lingering standby: Hail cluster 20260604 STOPPED (~$169 PD standby; Carter may UI-DELETE it — bfile+plink durable in the bucket, 20260605 remains as a Hail fallback).",
  "refire_pending": {
    "what": "m3-02e-T4 STEP 4 (SECOND re-fire, on the OOM-fixed code 3eac803): the resumable 276-region AFR native-plink LD loop (square, --keep-allele-order), single VM, serial, writing egress-clean per-region .npz to the bucket. Same loop_command as before.",
    "why_refire": "The 2026-06-28 re-fire reached region 1 and PROVED the chr-prefix fix (plink emitted the 102,421-var .ld.bin), but OOM-killed on the .npz convert before banking anything. Banked 0/276. The OOM is now fixed (3eac803) and Carter chose a high-mem VM for headroom.",
    "fix_history": "(1) First fire 2026-06-27 (PID 4237, ~17h): banked 0/276 — chr-prefix verify bug; fixed @ baeb925/a5a5f9f (_chrom_match_key). (2) Re-fire 2026-06-28 (PID 6577): reached region 1, plink finished, but the .npz convert OOM-killed; banked 0/276; chr-prefix fix proven good. OOM fixed @ 3eac803 (_is_symmetric_blocked memory-bounded symmetry check). Both fixes are now in the code; 3eac803 includes a5a5f9f includes 783ba91 (scratch reclaim).",
    "loop_vm": "AoU_Jupyter_ComputeEngine_20260626b — AoU Jupyter Compute Engine (Standard VM, Hail-free), CURRENTLY n2-standard-16 (64 GB), 1TB disk, us-central1-a, STOPPED. UI-managed lifecycle (pet SA is list-only Dataproc). RESPEC TO n2-highmem-16 (128 GB) before re-fire: AoU UI stop->edit machine type->start; the reattachable PD + the 379 GB local bfile are on disk and survive a machine-type change untouched. Carter ordered STOP-ON-COMPLETE (stop, NOT delete) at 276.",
    "loop_command": "nohup bash -c 'export PATH=~/bin:$PATH; timeout 312h python ~/coloc_analysis/src/python/run_native_ld_panel.py --manifest ~/coloc_analysis/config/ld_regions.tsv --bfile-prefix /home/jupyter/afr_cohort --out-dir gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/ --scratch-dir /home/jupyter/native_ld_scratch --mode square --ancestry AFR --num-shards 1 --panel-tsv gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/m3-W2-native-plink-panel.tsv' > ~/native_ld_loop.log 2>&1 &",
    "preflight": "After git pull (>= ff9e66f) and the highmem respec: re-confirm region-1 chr1 window count = 102,421 (== plink log) on the real cohort .bim. The chr-prefix fix (a5a5f9f) is already proven by the 2026-06-28 fire, so this is just a sanity check.",
    "memory_math": "Dense square float32 LD matrix = n_var^2 * 4 bytes (~39 GiB at n_var=102,421). After the 3eac803 fix the convert peak ~= matrix + ~0.4 GiB block temps + overhead ~= 45 GiB (fits 64 GB for region 1). The bare matrix still scales n_var^2, so a region >~115k variants would crowd a 64 GB box -> the n2-highmem-16 (128 GB) respec gives headroom up to ~175k variants. Expect panel-row peak RAM well under 128 GB.",
    "output_paths": {
      "npz_out_dir": "gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/ (durable; per-region {region_id}.npz + .afreq)",
      "panel_tsv": "gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/m3-W2-native-plink-panel.tsv (per-region wall/RAM/output/n_var/status; the REAL production cost basis)",
      "bfile_bucket": "gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/bfile/afr_cohort.{bed,bim,fam} (.bed 379,657,321,787 B byte-verified; INDIVIDUAL-LEVEL, in-perimeter only, never egress)",
      "bfile_local_on_vm": "/home/jupyter/afr_cohort.{bed,bim,fam} (plink reads this; chr-prefixed contigs e.g. chr1; survives the machine-type respec)",
      "plink_binary": "gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/tools/plink1.9 (v1.90b7.2, byte 41,730,792)",
      "run_log": "~/native_ld_loop.log on the loop VM"
    },
    "liveness_arbiter": "Climbing bucket .npz count: `gsutil ls gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/*.npz | wc -l` (target 276). NOT the kernel light, NOT the log motion, NOT _SUCCESS. ~57 min/region baseline -> ~11 days serial.",
    "expected": "~57 min/region x 276 ~= 263 VM-h ~= ~11 days serial. n2-highmem-16 is ~1.34x the n2-standard-16 hourly -> ~$60-67 total (was ~$45-50), under the $500 cap. timeout 312h caps compute.",
    "backstops": "(1) timeout 312h caps compute; (2) RESUME-SAFE — re-run verbatim, content-valid .npz skipped (MED-6 byte-floor vs the bucket), .npz durable in the bucket; (3) Carter STOP-ON-COMPLETE order (VM auto-STOPS — not deletes — at 276; resume-safe) + 2-3 day check-ins. NO self-teardown wrapper (pet SA list-only; teardown is UI/order-driven).",
    "pending_confirms": "The OOM fix's proof at region 1 and regions 1-3: (a) npz 0->1->2->3; (b) df returns to baseline after each bank (reclaim deletes the .ld.bin); (c) panel rows status==ok with real n_var (region 1 ~= 102,421) and peak RAM well under 128 GB; (d) no OOM in `dmesg`. REGRESSION TELL = npz stuck at 0 + a `Killed` in the log / OOM in dmesg (would mean an even-larger region exceeded 128 GB — unlikely)."
  },
  "session_hazards_fixed_2026_06_29": [
    ".npz-convert OOM (the SECOND 0/276 cause): read_square_bin's symmetry check `np.allclose(m, m.T, atol=1e-4)` builds several full n_var^2 float32 temporaries (~39 GiB each at n_var=102,421) on top of the 39 GiB matrix -> >64 GiB peak -> kernel OOM-kill (anon-rss ~61 GiB). np.savez_compressed is NOT the killer (numpy streams chunked); the diagonal check is tiny. Fixed via reusable _is_symmetric_blocked(m, atol, block=1024) (compare block rows vs the matching transposed col block; transient ~block*n_var*4 ~= 420 MB) @ 3eac803; ValueError message byte-identical, invariant enforced not skipped; failing-first regression (3 tests). Same dense-collect OOM class the Hail path already guarded (_max_safe_to_numpy_n_var / _dense_footprint_bytes) — the native-plink converter never got the guard.",
    ".npz-VERIFY OOM (the THIRD 0/276 cause, caught by blast-radius BEFORE the re-fire): content_verify_npz (run_native_ld_panel.py:228, the in-process per-region D-M3-10 gate run right after the convert at :475) reloads the full .npz via np.load (~39 GiB) and re-ran the IDENTICAL unbounded np.allclose(ld, ld.T, atol=1e-4) -> measured ~4x matrix ~156 GiB at n_var=102,421 > the 128 GB re-fire VM -> would OOM region 1 AGAIN one function past the 3eac803 fix. The square fix (3eac803) alone did NOT unblock the re-fire. Fixed @ ff9e66f (quick 260629-402): square branch reuses pln._is_symmetric_blocked; banded branch (:234, same class via a full np.triu copy — off the square path but in the same shared gate) uses a new co-located pln._strict_upper_is_zero_blocked (block-wise np.triu k=i+1). Reason strings byte-identical; failing-first regression; 48 targeted + 305/30 full-suite. Both square convert->verify OOM heads now bounded (region-1 peak ~40 GiB). Blast-radius also verified the 3eac803 predicate is provably equivalent (927 cases, 0 disagreements) and .npz/callers/resume safe."
  ],
  "session_hazards_fixed_2026_06_28": [
    "chr-prefix verify bug (the FIRST 0/276 cause): _window_bim_n_var literal `str(parts[0]) == chrom` vs the chr-prefixed AoU GRCh38 cohort .bim (chr1) while ld_regions.tsv chr is bare (1) -> 0 in-window rows -> verify-fail -> no bank, no .ld.bin reclaim -> scratch fill. Fixed via _chrom_match_key (strip leading chr/CHR on BOTH sides, mirrors plink1.9) @ baeb925. PROVEN GOOD by the 2026-06-28 re-fire (region-1 plink emitted the 102,421-var .ld.bin; the window matched).",
    "pre-existing git INDEX CORRUPTION repaired @ 2026-06-28: a dangling index blob for .planning/WAVE-1-CLOSEOUT-CHECKLIST.md (object missing from the store) blocked any full tree build. Repaired via `git hash-object -w` the on-disk content.",
    "PRIOR (2026-06-27) driver scratch-fill bug fix @ 783ba91 (_reclaim_region_scratch) — included in a5a5f9f/3eac803."
  ],
  "cohorts": {
    "afr_primary": {"path": "gs://rw-migration-aou-rw-476cdac2/ld/mt_afr_qc.mt", "samples": 73122, "status": "COMPLETE_VERIFIED, untouched (path /ld/ NOT /ld/mt/). Exported to the bucket bfile (the loop's source)."},
    "eur": {"path": "gs://rw-migration-aou-rw-476cdac2/ld/mt_eur_qc.mt", "samples": 220098, "status": "COMPLETE_VERIFIED — EUR LD sourced from PUBLIC Weissbrod 337k (m3-02e T2); this AoU EUR cohort no longer needed for LD"},
    "afr_sens": {"path": "gs://rw-migration-aou-rw-476cdac2/ld/mt_afr_pca_selfid_qc.mt", "samples": 62557, "status": "COMPLETE_VERIFIED, untouched"}
  },
  "gates": {
    "m3_02e_T1_3": "DONE + pushed @ 1a7bfdd (38 tests).",
    "m3_02e_T4_step3_remeasure": "DONE + ACCEPTED (57.14 min / 17.89 GiB / 15.29 GiB contents-verified; x276 budget gate SATISFIED).",
    "m3_02e_T4_step2_export": "DONE + byte-verified (.bed 379,657,321,787 B; .bim 883,834,306; .fam 73,122 samples) at gs://.../ld/afr_native_panel/bfile/; cluster 20260604 STOPPED.",
    "m3_02e_T4_scratch_reclaim_fix": "DONE @ 783ba91 (24/24 driver tests).",
    "m3_02e_T4_chr_prefix_verify_fix": "DONE @ baeb925 + docs a5a5f9f; PROVEN GOOD by the 2026-06-28 fire (region-1 .ld.bin = 102,421 vars).",
    "m3_02e_T4_npz_convert_oom_fix": "DONE @ 3eac803 (quick 260629-2im; _is_symmetric_blocked in read_square_bin; 17 targeted + 302/30 full-suite). Fixes the FIRST of two OOM heads.",
    "m3_02e_T4_npz_verify_oom_fix": "DONE @ ff9e66f (quick 260629-402; blast-radius found content_verify_npz still ran the SAME unbounded np.allclose(ld, ld.T) ~156 GiB > 128 GB -> would re-OOM region 1). Square -> reuse pln._is_symmetric_blocked; banded -> new pln._strict_upper_is_zero_blocked; 48 targeted + 305/30 full-suite. The re-fire git-pull target. MUST be pushed before re-fire.",
    "m3_02e_T4_step4_loop": "RE-FIRE PENDING on the fully-fixed code (>= ff9e66f, includes 3eac803) + a respec to n2-highmem-16 (128 GB). VM STOPPED by Carter. All three 0/276 root causes (chr-prefix + .npz-convert OOM + .npz-verify OOM) now fixed. Liveness = bucket .npz count -> 276.",
    "m3_04": "SUPERSEDED-PENDING-REPLAN — must CONSUME m3-02e AFR .npz + public EUR .rds (NOT rebuild via Hail). Re-plan after the T4 panel lands."
  },
  "cluster": "Loop runs on the GCE/Jupyter VM 20260626b (NOT a Dataproc cluster), CURRENTLY STOPPED (n2-standard-16; respec to n2-highmem-16 before re-fire). Carter ordered STOP-ON-COMPLETE (stop, NOT delete). Dataproc clusters: 20260604 = Hail (export, now STOPPED ~$169 PD standby; Carter may UI-DELETE); 20260605 = other Hail (untouched, re-export fallback); 20260617/20260620 = NO Hail; cheap probe VM 20260608 = Stopped. Old undersized loop VM 20260626 was DELETED.",
  "resume_on_reconnect": [
    "1. The loop is NOT running — it OOM-killed on region 1 on 2026-06-28 and Carter STOPPED the VM. Both OOM heads are FIXED (3eac803 read_square_bin + ff9e66f content_verify_npz; the 2nd was caught by blast-radius and would have re-OOM'd the 128 GB re-fire). FIRST: push ff9e66f to origin and verify origin tip == local HEAD (feedback_push_ncsu_before_aou_clone_fire). The fix is inert on AoU until pushed.",
    "2. Relay the re-fire procedure to the AoU agent: (a) git pull the VM repo to >= ff9e66f; (b) AoU UI respec the VM to n2-highmem-16 (128 GB) — stop->edit machine type->start; PD + 379 GB local bfile survive untouched; (c) pre-flight region-1 chr1 window = 102,421; (d) re-fire the SAME loop_command from region 1 (0 banked = clean restart; MED-6 floor skips nothing).",
    "3. Liveness by DATA LAYER: `gsutil ls gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/*.npz | wc -l` climbing toward 276. Proof point = region 1 then 1-3: npz 0->1->2->3 AND df flat (reclaim) AND panel rows status==ok n_var~=102,421 AND peak RAM << 128 GB AND no dmesg OOM. REGRESSION = npz stuck at 0 + a `Killed`/OOM (would need a region >128 GB; unlikely).",
    "4. STOP-ON-COMPLETE IS ORDERED (STOP, not DELETE): when 276 land the VM is STOPPED automatically — VM + reattachable PD + local bfile preserved. Do NOT assume the VM is gone; do NOT DELETE it (a delete destroys the local bfile). Any later DELETE is Carter's explicit action.",
    "5. If the VM crashes mid-run: the loop is RESUME-SAFE — restart the VM, re-run the SAME loop_command (banked .npz skipped). If the VM was DELETED, re-pull the bfile from gs://.../bfile/ (gcloud storage cp, ~50 min) then re-run on 3eac803+.",
    "6. At 276 + panel TSV all status==ok: agent egresses ONLY aggregate .npz+AF, pings `native-panel-recorded`. Then NCSU reconstructs m3-W2-native-plink-panel.tsv + the shutdown record, pushes, verifies origin tip == local HEAD, and m3-04 REPLAN."
  ],
  "prep_landmines_fixed": [
    "Loop VM repo MUST be at >= ff9e66f (chr-prefix verify fix + scratch-reclaim fix + .npz-convert OOM fix) — currently at a5a5f9f; git pull before re-fire.",
    "Respec the VM to n2-highmem-16 (128 GB) before re-fire — the dense square LD matrix is n_var^2*4 bytes (~39 GiB at 102,421) and a region >~115k variants would crowd a 64 GB box even after the OOM fix.",
    "chr-format: the AoU GRCh38 cohort .bim is chr-prefixed (chr1) but ld_regions.tsv chr is bare (1); the verify is prefix-agnostic via _chrom_match_key (proven good 2026-06-28). Pre-flight `awk '$1==\"chr1\" && $4>=<from> && $4<=<to>' <cohort.bim> | wc -l` should match the plink-log n_var.",
    "--bfile-prefix is LOCAL (/home/jupyter/afr_cohort) — plink can't read gs://; only --out-dir/--panel-tsv are gs://. The local bfile survives a machine-type respec.",
    "AoU analysis-VM disk sizing: base image eats ~192 GiB of the data PD -> 1TB (memory reference_aou_analysis_vm_large_bfile_staging).",
    "Pull a huge bfile with `gcloud storage cp` (not gsutil -m cp, which died sparse) + verify by stat/df, NOT ls.",
    "Teardown is order/UI-driven (pet SA list-only) — NO self-teardown in the wrapper; Carter ordered STOP-on-complete (STOP, not delete; resume-safe). timeout 312h caps compute."
  ],
  "resume_entry_point": ".planning/HANDOFF.json (this 2026-06-29T05:55Z OOM-fixed / re-fire-pending block) + .planning/STATE.md (2026-06-29 RESUME-HERE block) + .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md + m3-02e-AFR-NATIVE-FIRE-BRIEF.md. Operating manual = aou-ld-pipeline skill. Permission boundary = memory reference_aou_pet_sa_listonly_dataproc. OOM fix detail = .planning/quick/260629-2im-fix-native-plink-ld-npz-convert-oom-via-/. chr-prefix fix detail = .planning/quick/260628-244-fix-chr-prefix-mismatch-in-native-plink-/.",
  "do_not": [
    "Do NOT re-fire on the 64 GB n2-standard-16 — respec to n2-highmem-16 (128 GB) first (Carter's decision; headroom for large regions).",
    "Do NOT re-fire before pushing ff9e66f to origin + verifying origin tip == local HEAD — the fix is inert on AoU until pushed, and the VM repo is still at the OOM'd a5a5f9f. Pulling only 3eac803 is NOT enough (content_verify_npz still OOMs); the pull target is ff9e66f.",
    "Do NOT judge loop liveness by the log/kernel — use the climbing bucket .npz count.",
    "Do NOT DELETE the VM — stop != delete; a delete would destroy the local bfile. Carter ordered STOP-ON-COMPLETE at 276.",
    "Do NOT set --bfile-prefix to gs:// — plink can't read it; the bfile must be the LOCAL /home/jupyter/afr_cohort.",
    "Do NOT add a `gcloud dataproc clusters delete/stop` self-teardown — pet SA is list-only; teardown is UI/order-only.",
    "Do NOT egress the .bed/.bim/.fam (individual-level, in-perimeter only) — only aggregate LD .npz + AF cross.",
    "Do NOT drop --keep-allele-order (hardcoded in build_plink_ld_command).",
    "Do NOT route AFR through the retired Hail A.3 BlockMatrix path.",
    "Do NOT git add -A on the GPFS tree — explicit paths only."
  ]
}
