{
  "version": "1.0",
  "timestamp": "2026-07-16T01:05:00Z",
  "phase": "m3",
  "phase_name": "m3-aou-afr-ld-panel-build",
  "phase_dir": ".planning/phases/m3-aou-afr-ld-panel-build",
  "wave": "m3-07-W7 — overlapping-deletion span-filter + provenance manifest + present-rate scan + lockstep drop. 07a COMPLETE+VERIFIED. 07b EXECUTED (span-filter + manifest LANDED, 32 newly GREEN). 07c NOT STARTED — Carter's 'PAUSE before 07c' honored; 07c needs an explicit go.",
  "headline": "m3-07b LANDED and its ONE open decision is now CLOSED. The occlusion span-filter + provenance manifest are REAL CODE (7734725/T1 + 0473b6a/T2). The test-vs-test contradiction was RESOLVED 2026-07-15 via quick-260715-sqe (957d5a1 + a88faab) — the proposed 1-line test-side fix, 07a RED preserved and verified PASSED-not-skipped. Full tests/m3 47F→16F→15F; ALL 15 remaining are 07c's unbuilt modules (expected ModuleNotFoundError), so there are ZERO unexplained reds and the driver suite (52/52) has a clean baseline. NEXT = 07c ON CARTER'S EXPLICIT GO ONLY. Nothing running; $0; no perimeter contact. origin == local == a88faab; SSH push works with no PAT.",
  "status": "2026-07-15 RESUME + quick-260715-sqe. Resumed off the 2026-07-15T18:00Z handoff and closed its single open item. The panel-column test-vs-test contradiction is RESOLVED — by the ORCHESTRATING AGENT on verified precedent evidence (1a9d170 byte-confirmed), NOT by a Carter ruling; Carter's resume args named 'decide the column conflict' as the next step. Fix = the exact 1-line test-side edit the handoff itself proposed; blast radius verified contained BEFORE the edit; 0 regressions (16F/394P → 15F/395P, exactly 1 newly green). Revertible in 1 line if Carter disagrees. PRIOR (2026-07-15 close): 07a reconciled+executed+UAT-verified, blast-radius fixed a defective RED CONTRACT before 07b was built against it, push unblocked permanently via SSH, OSF GUID captured (trsx5), 07b plan reconciled to the corrected RED, 07b executed. 07c still awaits Carter's explicit go.",
  "no_fire_running": true,
  "nc_state_cost": "$0 — nothing running server-side or NC-State-side; no perimeter contact this session",

  "resume_on_reconnect": [
    "✅ DECISION #1 — RESOLVED 2026-07-15 (quick-260715-sqe, commits 957d5a1 + a88faab; on origin). NOT a Carter ruling: Carter's /gsd-resume-work args named 'decide the column conflict' as the next step, and the ORCHESTRATING AGENT made the call on verified evidence. THE FIX APPLIED = exactly the 1-line test-side edit this handoff proposed: \"n_dropped_occluded\" added at index 7 of the expected list in test_panel_tsv_append_resume_safe. The 07a RED was NOT weakened — all three guard tests verified PASSED, not skipped. The column was NOT dropped. EVIDENCE (each checked against the live repo, not the narrative): (1) precedent 1a9d170 is byte-confirmed — it added the PREVIOUS panel column RED-first AND updated this SAME assertion in the SAME commit, message reading 'updated the panel-TSV column-list assertion for the appended column' ⟹ OMISSION, not a deliberate expectation; (2) test_panel_tsv_append_resume_safe's subject is resume-safety, not column schema (which has dedicated tests); (3) blast radius contained — :392 was the ONLY exact-column pin, all others use order-independent set(), no downstream R/Snakemake consumer reads the panel TSV. VERIFIED: driver suite 1F/51P → 52 passed/0 failed; full tests/m3 16F/394P/31S → 15F/395P/31S = exactly 1 newly GREEN, 0 regressions, all 15 remaining being expected 07c ModuleNotFoundError (9 drop_occluded_from_sumstats + 6 occlusion_present_rate_scan); frozen contracts 0-line diff; ALL of src/ untouched (test-only). ⟹ THE DRIVER SUITE NOW HAS A CLEAN BASELINE FOR 07c. If Carter disagrees, 957d5a1 is a 1-line test-only revert with nothing built on it. NOTHING TO DO HERE — do not re-litigate. HISTORICAL RECORD of the original conflict follows: TWO TESTS CONTRADICTED and no implementation satisfied both. (a) tests/m3/test_run_native_ld_panel.py::test_panel_columns_include_n_dropped_occluded:1589 (the 07a RED) requires 'n_dropped_occluded' in drv._PANEL_COLUMNS and pins _PANEL_COLUMNS[:7]. (b) tests/m3/test_run_native_ld_panel.py::test_panel_tsv_append_resume_safe:392 (PRE-EXISTING) pins list(df.columns) to EXACTLY 8 columns (the 7 leading + n_dropped_monomorphic), WITHOUT n_dropped_occluded. _append_panel_row_local writes columns=_PANEL_COLUMNS, so the TSV now has 9 columns and (b) FAILS. VERIFIED: tests/m3/test_run_native_ld_panel.py = 1 failed / 51 passed, the single failure being test_panel_tsv_append_resume_safe. DIAGNOSIS: an OMISSION in the 07a RED, not a deliberate expectation — precedent 1a9d170 added the previous panel column RED-first AND updated the column-list assertion in the SAME commit; that companion edit was never made for this column. PROPOSED FIX (1 line, NOT APPLIED): add \"n_dropped_occluded\" at index 7 of the expected list in test_panel_tsv_append_resume_safe. DO NOT instead drop the column — that would fail the 07a RED and silently discard the per-region occlusion drop count from the panel TSV, which is exactly the provenance osf.io/az52u pre-registers. The executor correctly REFUSED to edit either test and escalated; do not let a future agent 'fix' this by weakening the RED.",
    "THEN 07c (m3-07c-W7-present-rate-and-lockstep-PLAN.md; wave 3, depends_on [07b] — now SATISFIED) ONLY on Carter's explicit go. T3 = occlusion_present_rate_scan.py (genome-wide present-rate-per-ancestry over the 9 public AFR sumstats); T4 = drop_occluded_from_sumstats.py (reusable lockstep (CHR,POS) drop-only filter; the m3-04 consume-wiring is a DISCLOSED deferral — finemap.smk m3-04-W4 is STALE/SUPERSEDED-PENDING-REPLAN). 15 of the 16 current failures are T3/T4's suites (ModuleNotFoundError: drop_occluded_from_sumstats / occlusion_present_rate_scan) and go green when 07c lands. 07c's plan was CHECKED for the drift that hit 07b's plan — it has none.",
    "THEN the GATED real-.bim validation (needs AoU perimeter access — NOT available at NC-State). ⚠ Carries an OPEN reconciliation: the real-window oracle {10328,44784,46714,59097,66730} is 0-based .bim ROW INDICES (per m3_nan_conditioning_scientific_review.md 'index-adjacent (10327/10328, 46713/46714/46715)'), NOT bp — fixed in a1e8693 — but the index ORIGIN (0- vs 1-based) is ASSUMED 0-based and must be confirmed against the real region-1 .bim header before the equality is trusted. See tests/m3/test_occlusion_span_filter.py, constant _REGION1_REAL_WINDOW_OCCLUDED_ROW_INDICES.",
    "THEN the AoU loop re-fire (~11 days, COSTS MONEY, Carter's trigger). Blocked until 07c lands + a region-1 re-run validates. The AoU Cloud Analysis VM (n1-standard-32, holds /home/jupyter/afr_cohort bfile) is STOPPED-not-deleted and preserved for exactly this. Liveness arbiter = the GCS .npz object listing climbing to 276, NOT the kernel light and NOT _SUCCESS ([[feedback_aou_success_marker_not_evidence_of_data]], [[feedback_aou_fanout_gcs_listing_arbiter]])."
  ],

  "completed_this_session": {
    "e10c893": "docs(m3-07a) — reconciled Task 1 to the REAL posted OSF amendment. The plan named a PLACEHOLDER file/tag that NEVER EXISTED and its verify grepped 3 strings the real doc lacks. Fixed the PLAN, not the amendment (the amendment body is byte-identical to the OSF copy; new threat T-m3-07a-03). Task 1 PRE-CLOSED; verify retargeted → GATE_CONFIRMED_OK.",
    "296157a": "test(m3-07a) — Wave 0 RED scaffolds (4 new suites + extended driver). Tag m3-07a-W7-T-WAVE0.",
    "bd3fe0e/9f5e202": "docs(m3-07a) — SUMMARY + STATE/HANDOFF refresh.",
    "a1e8693": "test(m3-07a) — BLAST-RADIUS FIXES to the RED CONTRACT (see blast_radius_findings).",
    "46dd661": "test(m3-07a) — UAT 11/11, 0 issues (automated; headless RED-first deliverable).",
    "51dd351": "docs(osf) — tcujq's OSF-side filename (Prereg_Phase1_amendment3.md) + a guard against the GUID mix-up.",
    "558ca60/26ca36d": "docs(osf) — captured GUID trsx5 + verified the append-only commitment; closed the last OSF follow-up.",
    "f3b79fe": "docs(m3-07b) — RECONCILED 07b's plan to 07a's corrected RED (3 contradictions; see plan_vs_red_reconciliation).",
    "7734725": "feat(m3-07b) T1 — occlusion span-filter upstream of --r (exclude, never zero). Tag m3-07b-W7-T1.",
    "0473b6a": "feat(m3-07b) T2 — load-bearing occlusion provenance manifest (Stage-A + liftover). Tag m3-07b-W7-T2.",
    "a76ebe5": "docs(m3-07b) — SUMMARY; 32 newly GREEN, frozen contracts clean, the test-vs-test contradiction REPORTED not resolved."
  },

  "blast_radius_findings": "3 parallel read-only investigators swept the 07a RED. The change touched ZERO production code (all frozen contracts 0-line diff), so the real downstream consumer was 07b's IMPLEMENTATION — and the CONTRACT was defective. BLOCKER: test_occlusion_manifest.py demanded snpC (5922718) be occlusion_order=='second_order', INVERTING the byte-verified verdict (pair 3 DEL 5922716 → SNP 5922718 IS a ref_span_overlap = DIRECT; '2nd-order' attaches to the DISJOINT pair-4 edge which occludes nothing) AND underivable from a genotype-free Stage A (the detector's edge set is 5 structurally identical direct edges) → the only route to green was HARDCODING position 5922718, publishing a false provenance label across all 276 regions = T-m3-07a-02 realized, with the arrow reversed (the test would have DRIVEN the wrong impl). HIGH: the gated oracle compared _pos_of (absolute bp) against row indices — region-1 spans ~1.98M–8.38M bp so it could NEVER pass a correct detector, and would have detonated at the gated run inviting someone to weaken the one oracle validating the genome-wide panel. 3 MEDIUMs: strict-left `<` vs `<=` untested against a distinct same-position variant (Hail split_multi emits them genome-wide; region-1 has 0); multi-occluder duplicates hidden by set(); egress guard used whole-key equality so n_samples/genotype_ac sailed through. Plus an untested producer→consumer seam. ALL FIXED in a1e8693, RED preserved. VERIFIED SAFE: _MockPlink's extension is byte-identical for all pre-existing callers (15/15 replays); isolation clean; the core science (fixture geometry, A1/A2 order, pair-4 attribution, liftover anchors) is encoded faithfully.",

  "plan_vs_red_reconciliation": "CAUGHT PRE-EXECUTION (f3b79fe): 07b's plan contradicted the 07a RED in 3 ways — an executor handed both would face an UNSATISFIABLE spec whose likeliest escape is editing the tests. SAME ROOT CAUSE the blast-radius traced: the plan retained the 'edges include the disjoint second-order edge, and occlusion_order derives from it' design the RED dropped as incoherent; I'd fixed the symptom in the tests, the root lived in the plan. (1) edges were spec'd as list[dict] but the RED does set(edges) + unpacks (o,v) → dicts are UNHASHABLE → TypeError, no dict impl can pass → now hashable 2-tuples (a 2-field NamedTuple is OK; 3-field is not); geometry dropped as informationless (only one value was ever permitted). (2) a 'disjoint'/second-order edge was spec'd but the RED asserts del4 contributes NO edge and pins edge_set to exactly the 5 direct → that NaN pair is genotype-layer, invisible to a coordinate detector, resolved as a CONSEQUENCE of the single 5922718 drop. (3) occlusion_order (direct|second_order) → now OPTIONAL and 'direct'-only. 07c checked — no equivalent drift.",

  "m3_07b_deviations_accepted": [
    "n_dropped_occluded INSERTED at _PANEL_COLUMNS index 7, NOT appended as the plan said — the pre-existing test :1281 pins _PANEL_COLUMNS[-1] == 'n_dropped_monomorphic'. Both tests pass. (This is ALSO the source of the open decision above: inserting it changed the panel TSV's column count, which test_panel_tsv_append_resume_safe pins exactly.)",
    "Manifest API follows the TESTS' names (build_region_records / append_region_manifest / aggregate_manifests / add_grch37_positions); the plan's prose names differ and are provided as aliases. Tests are the contract.",
    "Rule 2 — the excludelist was added to the reclaim keep-set + uploaded in gs:// mode: the RED calls {out_prefix}.occluded.excludelist 'durable provenance', but the existing {region_id}.* scratch-reclaim glob would have DELETED it on every region in the real fire. Caught pre-fire.",
    "Nested-deletion tie-break (left undetermined by the RED, flagged as a MEDIUM in the blast-radius sweep) = nearest upstream covering deletion, deterministic."
  ],

  "verification_evidence": {
    "m3_07b_green": "tests/m3/test_occlusion_span_filter.py 17 passed/1 skipped; test_occlusion_manifest.py 11 passed. Full tests/m3 at HEAD: 16 failed / 394 passed / 31 skipped (baseline 47F/363P/31S → 32 newly GREEN, 1 newly RED = the test-vs-test conflict). 15 of the 16 remaining = 07c's unbuilt modules (expected, out of scope).",
    "frozen_contracts": "VERIFIED 0-line diff across the ENTIRE session (d172dfa..HEAD): src/python/plink_ld_to_npz.py, src/scripts/ld_npz_to_rds.R, src/python/condition_ld_matrix.py. exclude=None keeps build_plink_ld_command's argv byte-identical to pre-07b.",
    "m3_07a_uat": "11/11 automated checks, 0 issues, 0 gaps (46dd661) — headless RED-first deliverable, so verification is automated and the success criterion is RED-for-the-right-reason.",
    "osf_gate": "GATE_CONFIRMED_OK read-only on an unmodified tree; both settled-science body anchors byte-exact (tail -c 5012 == 4543dcf4…; tail -c 5247 == 42d70167…); amendment body sha256 c80d4d26… unchanged across the GUID fill.",
    "durability": "origin == local == a76ebe5, 0 ahead. All 5 tags on origin (AFR-NANPSD…, AFR-OCCLUSION-EXCLUDE…→ac4c990, m3-07a-W7-T-WAVE0→296157a, m3-07b-W7-T1→7734725, m3-07b-W7-T2→0473b6a)."
  },

  "osf_gate": {
    "status": "CLEARED + FULLY RECORDED — no open items",
    "update_guid": "trsx5 — https://osf.io/az52u/files/trsx5 (the 2026-07-10 occlusion-exclude UPDATE). Captured 2026-07-15.",
    "prior_guid": "tcujq — Prereg_Phase1_amendment3.md (the 2026-07-04 NaN→0 amendment the update WITHDRAWS). ⚠ NOT interchangeable with trsx5.",
    "append_only_verified": "trsx5 = 1 revision (2026-07-10 13:32); tcujq = still 1 revision (2026-07-04 04:14, UNMODIFIED). Separate new file, NOT a re-version → the pre-registered append-only commitment was honored → NO posting deviation to disclose.",
    "near_miss": "tcujq was offered to fill the trsx5 slot on 2026-07-15 and REFUSED — writing it would have pointed the withdrawing document at the document it withdraws. Guards added at both osf_deviations.md entries.",
    "header_vs_body_rule": "The project-side copy = posted body + a PREPENDED project-side header. The header is editable by design and precedent (tcujq's header carries a GUID OSF only assigns AT upload → necessarily written post-upload → its anchors are scoped 'content-after-header'). NEVER edit the BODY. Both headers are now complete."
  },

  "prep_landmines": [
    "GSD tooling does NOT recognize the custom m3-NN milestone-scoped naming (init/roadmap/execute-phase → phase_found:false). Execution = spawn gsd-executor DIRECTLY on the PLAN.md (adapted planner→executor pattern; 07a, 07b, m3-06, m3-02b-e all did this).",
    "⚠ BEFORE spawning an executor on a PLAN, CHECK THE PLAN AGAINST THE RED — 07b's plan contradicted 07a's RED in 3 ways (f3b79fe). A stale plan + a corrected contract = an unsatisfiable spec, and the executor's likeliest escape is editing the tests.",
    "Prepend $HOME/miniconda3/bin to PATH for gsd-tools.cjs / node CLI calls ([[feedback_node_path]]).",
    "Commit with EXPLICIT paths only — never `git add -A`/`.` on this shared GPFS tree ([[feedback_multi_terminal_staging]]).",
    "`git push` does NOT push tags — push each explicitly (`git push origin <tag>`) and verify with ls-remote. On 2026-07-15 two tags including the OSF GATE RECORD were left stranded local-only on an object store that has lost blobs 3×.",
    "origin is now SSH (git@github.com:carter-clinton/coloc_analysis.git) — pushes need NO PAT. Verify with `ssh -T git@github.com` (expect 'Hi carter-clinton!').",
    "pytest env = /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/pytest (py3.11). Full tests/m3 ≈ 6.5 min — EXCEEDS a 120s Bash timeout; background it.",
    "Liftover: the ONLY chain present is data/external/liftover/hg38ToHg19.over.chain.gz (the needed direction). conftest's chain_fixture points at an ABSENT hg19→hg38 chain → it would always skip. Don't reuse it.",
    "Keep tests/m3 _REGION1_BIM_ROWS as plain tuples of primitives — two modules share it via an importlib file-path load, safe ONLY because tuples compare by value. A dataclass/NamedTuple-with-methods would make class identity differ across the two module objects and silently break equality.",
    "tests/m3/test_sparse_parent_benchmark.py WRITES into the tracked tree (sparse_parent_benchmark.tsv) on every full-suite run — benign timing noise, contained only by the explicit-path staging rule. It shows as ' M' and is NOT yours."
  ],

  "gpfs_object_store_recovery_recipe": "RECURRED 2026-07-15 (3rd time; 13 blobs) — commit failed 'invalid object … / Error building trees'; all 13 recovered. GUARDED recipe: for each index entry (git ls-files -s), test `git cat-file -e <sha>`; for each missing blob confirm `git hash-object <path>` == the index's expected SHA, and ONLY then `git hash-object -w <path>`. If a hash does NOT match, REFUSE and escalate — never fabricate an object. NOTE `git fsck` also reports 194 broken links in OLDER history: that history is already on origin and the push set was intact, so ORIGIN IS THE GOOD COPY — push, don't attempt history surgery. See [[reference_gpfs_git_object_store_loss]].",

  "cluster": "None running. AoU Cloud Analysis VM (n1-standard-32, holds the /home/jupyter/afr_cohort bfile) is STOPPED, not deleted — preserved for the span-filtered re-fire. Leave it stopped (no fire imminent).",
  "data_state": "AoU AFR native-plink LD panel does NOT exist yet: bucket .npz = 0/276 (loop STOPPED, PID 5170 dead). The occlusion span-filter is the PRECONDITION for region 1 (and every occlusion region) to bank — the --mac 1 drop-monomorphic fix does NOT catch occlusion NaNs (occluded variants are polymorphic overall, monomorphic only on the pairwise-complete subset). 07b now LANDS that filter in code; 07c + a region-1 validation remain before any re-fire.",

  "gates": {
    "osf_pre_registration": "CLEARED + fully recorded (posted 13:32:22Z, ac4c990, tag, GUID trsx5, append-only verified). No open items.",
    "execution_gate": "Carter-gated: 07a COMPLETE+VERIFIED. 07b EXECUTED. 07c NOT authorized — needs an explicit go.",
    "panel_column_conflict": "✅ CLOSED 2026-07-15 (quick-260715-sqe: 957d5a1 fix + a88faab docs, both on origin). Resolved by the orchestrating agent on verified precedent evidence (1a9d170), NOT by a Carter ruling — Carter's resume args named 'decide the column conflict' as the next step. Applied exactly the proposed 1-line test-side fix; 07a RED preserved and verified PASSED-not-skipped; column NOT dropped. Driver suite 52/52; full tests/m3 15F/395P/31S = 1 newly GREEN, 0 regressions, all 15 remaining are expected 07c ModuleNotFoundError. The driver suite now has the clean baseline 07c wanted. Revertible in 1 line (957d5a1) if Carter disagrees.",
    "aou_loop_refire": "BLOCKED until 07c lands + a region-1 re-run validates. Costs money (~11 days); Carter's trigger."
  },

  "do_not": [
    "(SATISFIED 2026-07-15 — kept as a standing rule for the NEXT panel column.) The panel-column conflict was resolved WITHOUT weakening or deleting the 07a RED: the fix updated the PRE-EXISTING column-list assertion (precedent 1a9d170), it did not drop the column. STANDING RULE for any future _PANEL_COLUMNS addition: update BOTH the schema-pin tests AND the exact-column-list assertion in test_panel_tsv_append_resume_safe (:392 is the ONLY exact-order pin) in the SAME commit — that omission is exactly what produced this conflict. NEVER drop n_dropped_occluded: it carries the per-region occlusion provenance osf.io/az52u pre-registers.",
    "Do NOT start 07c without Carter's explicit go.",
    "Do NOT re-fire the AoU loop or touch the perimeter — 07c + a region-1 validation must land first.",
    "Do NOT revive NaN→0 / feed condition_ld_matrix.py to any fit — m3-06 STAYS HELD, condition_ld_matrix.py FROZEN, raw-panel read_square_bin NaN-raise intact.",
    "Do NOT re-post or EDIT the BODY of any OSF amendment (headers are complete; bodies are byte-locked to OSF).",
    "Do NOT hardcode any variant position in the detector or manifest (the BLOCKER's failure mode).",
    "Do NOT git add -A/. on this GPFS tree."
  ],

  "resume_entry_point": ".planning/HANDOFF.json (this block) + .planning/STATE.md top block (2026-07-15 ★ RESUME HERE ★) + .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md + the 07b SUMMARY (m3-07b-W7-span-filter-and-manifest-SUMMARY.md)"
}
