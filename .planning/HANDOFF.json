{
  "version": "1.0",
  "timestamp": "2026-08-03T19:30:00Z",
  "phase": "m3",
  "phase_name": "m3-aou-afr-ld-panel-build",
  "phase_dir": ".planning/phases/m3-aou-afr-ld-panel-build",
  "wave": "m3-04-W4 REPLANNED + m3-04b EXECUTED. The stale m3-04-W4 (322-cell Hail fire) was replanned into m3-04b (wave 1, autonomous, DONE) + m3-04c (wave 2, terminal Carter gate, NOT STARTED).",
  "headline": "★ m3-04b LANDED — the occlusion catalog assembler + the lockstep consume seam are REAL CODE and WIRED (d7dfa67 T1, 0cae502/37c51df T2, f038ce0 docs; all on origin). Full tests/m3 420P/31S/0F → 444P/31S/0F (+24, zero regressions) — I RE-RAN IT MYSELF at HEAD, 07:02, not taken on the executor's report. The four m3-07 modules that had ZERO production callers now have one. ⚠⚠ THE SESSION'S MOST IMPORTANT FINDING, from the m3-04 replan: THE AFR_aou PANEL IS UNREACHABLE FROM run_finemap — REGION_SAFE_TO_ID (Snakefile:47-63) is built ONLY from config/regions_curated.csv (12 curated slugs), NOT from config/region_id_mapping.tsv (276 M2 rows), so resolve_ld_path asks for AFR_aou/FTO_16q12.rds, MISSES, and SILENTLY falls through to AFR_1kg. Landing all 276 .rds would change NOTHING and nothing would warn you. ⇒ DO NOT FIRE until m3-04c Task 1 (panel reachability) lands. NEXT = m3-04c.",
  "status": "2026-08-03 session: (1) quick-260803-jag rectified the working tree carried since June; (2) the m3-04 REPLAN (68f2732) split the stale plan into m3-04b + m3-04c and uncovered the panel-unreachability defect; (3) Carter OVERRODE the OSF posting gate for the Check-2 redefinition — logged as a disclosed deviation (3e7a01a), NOT absorbed silently; (4) m3-04b EXECUTED both tasks. Every finding this session was PRE-EXISTING. Nothing running, $0, no perimeter contact all session.",
  "no_fire_running": true,
  "nc_state_cost": "$0 — nothing running server-side or NC-State-side; no perimeter contact this session",
  "verified_this_session_firsthand": [
    "Full tests/m3 at HEAD f038ce0 = 444 passed / 31 skipped / 0 FAILED (422.66s). I ran it; not the executor's report.",
    "All 7 pinned files 0-line diff vs bf16289: the 4 m3-07 modules (occlusion_span_filter, occlusion_manifest, occlusion_present_rate_scan, drop_occluded_from_sumstats) + the 3 frozen contracts (plink_ld_to_npz.py, ld_npz_to_rds.R, condition_ld_matrix.py).",
    "origin == local == f038ce0. Tracked tree clean except tests/m3/sparse_parent_benchmark.tsv, which MY suite run re-dirtied (timing columns only) — benign, the suite rewrites it every run.",
    "PANEL UNREACHABILITY, verified 3 ways: grep -c FTO_16q12 config/region_id_mapping.tsv == 0; regions_curated.csv == 13 lines; region_id_mapping.tsv == 277 lines.",
    "123 of 276 unique region ids (every __subNN split id) are silently excluded by region_id=r'm2_region_\\d{5}' at m3_ingest_aou_ld.smk:322 and m3_convert_npz_rds.smk:103,145.",
    "config/ld_regions.tsv = 552 data rows = 276 AFR + 276 EUR (m3-04's '322 cells' was the stale pre-m3-02d 161x2 basis).",
    "The executor EDITED ONE EXISTING TEST (test_finemap_loader_contract.py::test_production_boundary_documented). I READ THE DIFF: it REPLACED, did not RELAX — every original assertion retained, the one flipped token was mutable project state (the replan genuinely landed), and it ADDS three new requirements (m3-04b, m3-04c, occlusion_lockstep). This is NOT a weakened RED."
  ],
  "resume_on_reconnect": [
    "⚠⚠ #1 — DO NOT FIRE THE ~11-DAY LOOP YET. The AFR_aou panel is UNREACHABLE from run_finemap (see headline). resolve_ld_path requests AFR_aou/{curated_slug}.rds, misses, and SILENTLY falls through to the AFR_1kg fallback — by design, so nothing warns you. An 11-day, $385-1,084 fire would bank 276 .npz that the fine-mapping DAG ignores. The finemap.smk:118-123 CR-001 comment ASSERTS this translation exists; it never did. The planner dry-ran the crosswalk on real inputs: 11/12 curated regions map, FTO_16q12 -> m2_region_00067 exactly as the comment claims — intent right, implementation absent. SH2B3_12q24 has 18 containing candidates (all subregions of split parent m2_region_00040) so the tightest-window tie-break is load-bearing. FIX = m3-04c Task 1. The known-answer table is embedded there as the executor's oracle.",
    "▶ #2 — NEXT ACTION = EXECUTE m3-04c (.planning/phases/m3-aou-afr-ld-panel-build/m3-04c-W4-panel-reachability-egress-and-fire-PLAN.md). 3 tasks: T1 panel reachability (the crosswalk + the ONE resolve_ld_path(region_id=) argument that changes); T2 de-stale m3_ingest_aou_ld.smk + m3_convert_npz_rds.smk, egress planner on the EXISTING ld_egress_bundle.py, record the Check-2 redefinition, update ROADMAP; T3 = checkpoint:human-action gate=blocking (PRE-FIRE fixes -> region-1 gate -> ~11d fire -> egress -> hand back to DAG -> OSF posting). T1+T2 are autonomous/$0/NC-State. depends_on ['04b'] is now SATISFIED.",
    "⚠ #3 — LANDMINE FOR THE m3-04c EXECUTOR: run_finemap.params.region_id (finemap.smk:158) ALSO uses REGION_SAFE_TO_ID, feeding run_susie_rss.R --region, looked up in config/regions_curated.csv. ONLY the resolve_ld_path(region_id=) argument may change. m3-04b T2.7 and m3-04c T1.7 both carry regression tests pinning this from opposite sides. m3-04b did NOT touch params.region_id — verified.",
    "⚠ #4 — D-04b-01, A REAL DEFECT FOUND BUT DELIBERATELY NOT FIXED (both modules are 0-diff-pinned): bmi.AFR.PAGE.2019.GRCh37.tsv.bgz stores POS as the FLOAT STRING '5982778.0'. int('5982778.0') RAISES in _canonical_key in BOTH occlusion_present_rate_scan.py and drop_occluded_from_sumstats.py. Consequence: the scan scores the row ABSENT (so rs182965575 measures 6/9, not the published 7/9 — and 7/9 IS the correct number), and drop_occluded_from_sumstats sets key=None and KEEPS the row = a SILENT UNDER-DROP wearing a clean n_dropped==0. Blast radius on m3-04b: NONE — the mirror rule's stem=r'...\\.AFR' only matches asthma.AFR / stroke.AFR / t2d.AFR, all verified integer-POS. MUST be fixed before the present-rate scan or the lockstep drop is run against the full 9 files. ALSO: 6 of 9 .tbi indexes are STALE (tabix returns nothing behind a [W::] warning).",
    "▶ #5 — PRE-FIRE CHECKLIST (all Carter, all in-perimeter, NONE done): (a) PANEL REACHABILITY = m3-04c T1 (see #1) — NEW this session and the most dangerous; (b) ROTATE/DELETE THE STALE gs:// PANEL TSV: gsutil stat <panel-uri>; if present, gsutil cat <uri> | head -1 | tr '\\t' '\\n' | wc -l must be 9, else gsutil rm. ⚠ '0/276 banked' is measured in .npz and does NOT evidence its absence; (c) THE OCCLUSION MANIFEST HAS NO PATH OUT OF THE PERIMETER — run_native_ld_panel.py:822 writes {compute_dir}/occlusion_manifest.tsv to LOCAL SCRATCH (:733) and the upload set (:922-938) is ONLY .npz + .afreq + .occluded.excludelist. The DROP KEY is recoverable from the excludelists (GRCh38 varid + liftover) but the occluder attribution / REF spans / reason labels are NOT — precisely what osf.io/az52u trsx5 commits to publishing ⇒ a PRE-REGISTRATION COMPLIANCE gap, not a mechanics blocker. RECOMMENDED LOW-RISK FIX: upload a PER-REGION {region_id}.occlusion_manifest.tsv so no object is ever overwritten (aggregate_manifests already expects a list). Deliberately NOT coded — it edits the fire driver at the freeze moment, and the P3 lesson (ff8cc47) is that one upload helper serving two callers with opposite failure-safety needs silently destroyed banked provenance; (d) the gated real-.bim validation (⚠ open 0- vs 1-based index-origin question on _REGION1_REAL_WINDOW_OCCLUDED_ROW_INDICES); (e) a region-1 re-run must validate.",
    "▶ #6 — THEN the ~11-day billed fire (Carter's trigger, COSTS MONEY, ~$385-1,084). Liveness arbiter = the GCS .npz object listing climbing to 276 — NOT the kernel light, NOT _SUCCESS. The AoU Cloud Analysis VM (n1-standard-32, holds /home/jupyter/afr_cohort) is STOPPED-not-deleted and preserved for exactly this. .npz currently 0/276.",
    "✅ #7 — NOTHING IS BLOCKED ON OSF. Carter overrode the posting gate 2026-08-03; logged as a disclosed deviation in .planning/osf_deviations.md (3e7a01a). BUT TWO OBLIGATIONS SURVIVE THE OVERRIDE: (1) the Check-2 amendment-update must still be drafted + posted to osf.io/az52u (agent DRAFTS, Carter POSTS, GUID recorded in-repo — the m3-07a discipline); (2) UNTIL IT EXISTS, NO REDEFINED CHECK 2 RESULT MAY BE CITED AS 'PASSED' anywhere (manuscript, Sci Data descriptor, closeout) — report it as 'OVERRIDDEN — redefined pending amendment'. If publication comes first, fold into the pre-registration-limitations statement per the 2026-05-05 precedent."
  ],
  "completed_this_session": {
    "quick-260803-jag (ea5e54e, 3b0647d, 9fe26f6)": "Rectified the working tree carried since June. RESTORED 2 accidentally-deleted AFR disclosure docs (blob-hash-verified; ⚠ deletion MECHANISM never identified — effect repaired, cause unproven). COMMITTED the stranded 260625-r6m SUMMARY for 10aaa6f (already an ancestor of HEAD). PRUNED .claude/settings.json 37 -> 33 allow entries, dropping 4 one-off destructive grants (rm -rf data/processed/clumping/*/, the fire-log rm -f, the nohup fire launch, the PID echo) while keeping 8 durable read/inspect grants; rationale written into the commit message so it survives without the plan. REVERTED the benchmark byproduct.",
    "m3-04 REPLAN (68f2732)": "Split the stale m3-04-W4 into m3-04b (wave 1, autonomous) + m3-04c (wave 2, terminal Carter gate). Stale plan left in place as history. Uncovered the panel-unreachability defect (see headline) plus 8 other staleness axes. m3-05 now ALSO SUPERSEDED-PENDING-REPLAN (it inherits the 322-row monolith / 44 sub-manifest / EUR_aou basis) — deliberately replanned AFTER the panel lands, when counts are observable rather than assumed.",
    "OSF override (3e7a01a)": "Carter elected to bypass the OSF posting gate for the Check-2 redefinition. Logged as a DISCLOSED deviation under the 2026-05-05 operator-override precedent. ⚠ The entry deliberately does NOT reuse that precedent's central defense ('registration timing, not analysis content') — part 2b REMOVES a pass/fail threshold, so this is a CONTENT deviation and is disclosed as one. Scientific justification recorded: the 0.97 floor was written for EUR-vs-EUR where near-identity is expected; applied to AFR it would fail the panel for SUCCEEDING, since a low AoU-AFR vs 1000G-AFR r is the expected finding and the whole rationale for building the panel. 2c retains r>=0.90 and 2a is a real pass/fail gate, so the redefinition is not threshold-free.",
    "m3-04b (d7dfa67, 0cae502, 37c51df, f038ce0)": "T1 = src/python/assemble_occlusion_catalog.py — the genome-wide catalog assembler; the FIRST production caller for the four zero-caller m3-07b/07c functions (add_grch37_positions, enrich_occlusion_manifest, aggregate_manifests/build_occlusion_catalog, scan_present_rate). 10/10 genuine RED. T2 = the lockstep consume seam: src/python/occlusion_lockstep_cli.py + src/snakemake/rules/m3_occlusion_lockstep.smk, wiring BOTH run_finemap.input.sumstats AND input.variants, ancestry-gated to AFR (non-AFR path strings byte-identical, so Track-A/EUR numerics cannot move). 13/14 genuine RED."
  },
  "executor_deviations_accepted": [
    "THE PLAN WAS FACTUALLY WRONG TWICE, both blocking, both fixed: (1) envs/m3-r-ld.yml does NOT carry pandas (the plan asserted it does; ModuleNotFoundError verified in the built env) — added pandas>=2.2 THERE rather than pyliftover to python_stats.yml, because python_stats.yml backs a dozen already-built rules and re-hashing it forces a pipeline-wide rebuild; (2) src/snakemake/schemas/pipeline.schema.yaml ends in additionalProperties:false and the plan never mentions the schema, so adding occlusion_lockstep: would have broken EVERY Snakemake invocation at validate(). Schema entry added, following the ld_panel precedent already in that file.",
    "ONE EXISTING TEST EDITED — test_finemap_loader_contract.py::test_production_boundary_documented. It asserted 'SUPERSEDED-PENDING-REPLAN' in smk; m3-04b makes that token's ABSENCE an acceptance criterion, so no implementation satisfied both. REPLACED, NOT RELAXED: all original assertions retained, plus three NEW ones (m3-04b, m3-04c, occlusion_lockstep must be named). I READ THE DIFF AND CONFIRMED THIS. The token pinned mutable project state, not a contract, and the replan landed. Precedent 1a9d170.",
    "test_params_region_id_is_untouched PASSED AT BIRTH and never failed — correctly labelled a GUARD RAIL for m3-04c, not a RED, in both the commit and the SUMMARY. Honest RED accounting: T1 10/10 genuine, T2 13/14 genuine.",
    "The full-workflow --dry-run --quiet acceptance criterion is UNSATISFIABLE and always was: data/processed/ld_reference/ does not exist, so resolve_ld_path raises 'No LD panel found for FTO_16q12 AFR'. Proven PRE-EXISTING by running git show HEAD:Snakefile at 3e7a01a (identical error, identical ld_panel.py:94). Substitute evidence accepted: both new targets dry-run to exit 0 with no cycle, and both resolvers were exercised against the real config (3 AFR repointed, 5 EUR+TRANS byte-identical).",
    "bgzip: Snakemake built NO conda env this session (dry-run doesn't). The unit test uses a PATH-installed shim since htslib is not in smoke_dev — stated in the suite docstring, chosen over a skip. Proved out of band with bgzip (htslib) 1.21: output is genuine BGZF (BC\\x02\\x00), tabix -f -S 1 -s 1 -b 2 -e 2 indexes it, and a range query round-trips real rows."
  ],
  "gates": {
    "osf_pre_registration": "⚠ OVERRIDDEN 2026-08-03 by Carter for the Check-2 redefinition, logged as a DISCLOSED deviation (3e7a01a, .planning/osf_deviations.md). The prior occlusion-exclude amendment-update (GUID trsx5, posted 2026-07-10) remains CLEARED and unaffected. TWO OBLIGATIONS SURVIVE: draft+post the Check-2 amendment-update; and until it exists, NO redefined Check 2 result may be cited as 'passed' anywhere.",
    "m3_07": "✅ CODE-COMPLETE (07a+07b+07c). Unchanged this session.",
    "m3_04b": "✅ COMPLETE 2026-08-03. Both tasks landed; tests/m3 444P/31S/0F independently re-verified by the orchestrator; all 7 pinned files 0-line diff.",
    "m3_04c": "⛔ NOT STARTED. depends_on ['04b'] now SATISFIED. T1+T2 are autonomous/$0/NC-State and can run immediately. T3 is the terminal Carter gate.",
    "panel_reachability": "⛔ OPEN — THE BLOCKER. AFR_aou is unreachable from run_finemap; the fallback chain makes it SILENT. m3-04c T1. DO NOT FIRE UNTIL THIS LANDS.",
    "aou_loop_refire": "⛔ BLOCKED on panel reachability (new), the stale gs:// panel-TSV rotate, the manifest-egress gap, the real-.bim validation, and a region-1 re-run. COSTS MONEY (~11 days, ~$385-1,084). CARTER'S TRIGGER.",
    "validation_4check": "⛔ ALL FOUR validation/check_*/ dirs EMPTY — the protocol has NEVER been run, and AOU-LD-PIPELINE.md §9 calls it 'a hard gate for promoting the pipeline from dev to production'. Check 2 REDEFINED (2a code-path equivalence vs plink on public 1000G; 2b AFR-vs-1000G REPORTED NOT THRESHOLDED; 2c EUR_ukbb_pub vs 1000G EUR at r>=0.90) under the operator override.",
    "m3_06": "HELD. condition_ld_matrix.py FROZEN, never fed to a fit. NaN→0 DEAD. m3-04b imports neither condition_ld_matrix nor nan_to_num — verified."
  },
  "cluster": "None running. The AoU Cloud Analysis VM (n1-standard-32, holds the /home/jupyter/afr_cohort bfile) is STOPPED, not deleted — preserved for the fire. Leave it stopped; no fire is imminent and panel reachability must land first.",
  "data_state": "AoU AFR native-plink LD panel does NOT exist: bucket .npz = 0/276 (loop STOPPED, PID 5170 dead). ALSO: data/processed/ld_reference/ does not exist AT ALL on NC-State — so even EUR_ukbb_pub has never been built, though m3_public_eur_ld.smk exists and is included. That is a $0 NC-State prerequisite for anything assuming a EUR panel.",
  "do_not": [
    "⚠ DO NOT FIRE THE ~11-DAY LOOP until m3-04c Task 1 (panel reachability) lands — the panel would be silently ignored by the DAG.",
    "Do NOT change run_finemap.params.region_id (finemap.smk:158). ONLY the resolve_ld_path(region_id=) argument may change, and that is m3-04c T1's job.",
    "Do NOT edit the 4 m3-07 occlusion modules or the 3 frozen contracts — all 7 are 0-diff-pinned. Call them; never modify them.",
    "Do NOT revive NaN→0 or feed condition_ld_matrix.py to any fit — m3-06 STAYS HELD.",
    "Do NOT cite any redefined Check 2 result as 'passed' until the OSF amendment-update is posted — report 'OVERRIDDEN — redefined pending amendment'.",
    "Do NOT write validate_bundle_sizes.py — it already exists as src/python/ld_egress_bundle.py (ade6066), same 50 GB cap, whose docstring downgrades that cap to a working ceiling, not a hard AoU limit.",
    "Do NOT plan the lockstep filter from m3-07-RESEARCH.md §T4 — it records a SUPERSEDED signature. The shipped module is (sumstats_path, manifest_path, out_path) -> dict, file-in/file-out. GREP THE NAME, THEN READ THE ASSERTIONS.",
    "Do NOT run the present-rate scan or the lockstep drop against the full 9 AFR sumstats until D-04b-01 (float POS '5982778.0') is fixed — it silently under-drops.",
    "Do NOT re-post or EDIT the BODY of any OSF amendment (bodies are byte-locked to OSF).",
    "Do NOT git add -A / . on this GPFS tree — explicit paths only.",
    "Do NOT use worktree isolation on this GPFS tree."
  ],
  "prep_landmines": [
    "GSD tooling does NOT recognize the custom m3-NN naming: `init plan-phase m3-04` returns phase_found:false (I re-verified today). Execution = spawn gsd-planner / gsd-executor DIRECTLY on the PLAN.md (the adapted planner->executor pattern).",
    "envs/m3-r-ld.yml now carries pandas>=2.2 (added by m3-04b; it did NOT before). A fresh env build needs this.",
    "src/snakemake/schemas/pipeline.schema.yaml is additionalProperties:false — ANY new top-level pipeline.yaml block MUST get a schema entry or every Snakemake invocation dies at validate().",
    "bgzip is NOT on the login PATH and NOT in smoke_dev; it lives in envs/python_stats.yml.",
    "The m3-r-ld conda env at /rs1/researchers/c/ckclinto/conda_envs/m3-r-ld is the marker that makes the A6 stitch/loader tests RUN rather than skip. Do not delete it.",
    "tests/m3/sparse_parent_benchmark.tsv is REWRITTEN by every suite run (timing columns only). Expect it dirty after any pytest tests/m3; it is not a real change."
  ],
  "gpfs_object_store_recovery_recipe": "Did NOT fire this session (4 commits + 3 pushes clean). Prior: 3 hits in ~3 weeks. Recipe if 'invalid object / Error building trees': for each `git ls-files -s` entry test `git cat-file -e <sha>`; for misses re-hash the intact working-tree file via `git hash-object -w <path>`; then commit AND PUSH (local store unreliable).",
  "resume_entry_point": ".planning/HANDOFF.json (this block) + .planning/STATE.md 2026-08-03 ★ RESUME HERE block + .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md + the m3-04b SUMMARY (m3-04b-W4-SUMMARY.md) + deferred-items.md + the m3-04c PLAN (the next thing to execute)."
}