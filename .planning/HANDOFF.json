{
  "version": "1.0",
  "timestamp": "2026-07-02T20:20:00.000Z",
  "phase": "m3",
  "phase_name": "m3-aou-afr-ld-panel-build",
  "phase_dir": ".planning/phases/m3-aou-afr-ld-panel-build",
  "wave": "m3-02e — native LD export + public EUR. T1-3 DONE+pushed; T4 STEP 0/2/3 DONE. T4 STEP 4 (the 276-region AFR native-plink LD loop) is BETWEEN FIRES again: fire #3 (2026-06-30, PID 432, n1-standard-32/120GB, HEAD 1b45d43) PROVED both OOM fixes (region 1's convert+verify did NOT OOM on 120 GB) but region 1 errored on a NON-REPRODUCIBLE TRANSIENT short-read of the cohort .bim ('window .bim has 0 rows'). Loop stopped (kill 432) at region 1 for investigation; banked 0/276 (resume-safe). A retry guard is being added (quick 260630-rn4, executor mid-flight); re-fire pending on the guarded code — VM STAYS n1-standard-32 (NO respec).",
  "status": "2026-06-30 — FIRE #3 EXONERATED THE OOM FIXES, EXPOSED A TRANSIENT. The 2-OOM-fixed code (1b45d43) + a 120 GB VM (n1-standard-32; the AoU UI offered no 128 GB n2-highmem/n2-standard-32, and n1-standard-64/240GB busts the $500 cap, so Carter chose n1-standard-32/120GB — peak need is the ~40 GiB matrix, 120 GB clears it to ~168k vars) reached region 1: plink finished the 102,421-var .ld.bin (41,960,244,964 B), the .npz convert AND content_verify_npz BOTH ran with NO dmesg OOM (both fixes proven on real data at scale). BUT region 1 recorded status=error: 'n_var mismatch ... .ld.bin implies 102421 but the window .bim has 0 rows'. EXHAUSTIVE FORENSICS (this session, ~6 dig rounds) PROVED NO STATIC DEFECT: (a) src md5 e99fd8170feed5bd2f5f6c5a04f2d5ea == committed 1b45d43; (b) _window_bim_n_var/_chrom_match_key are chr-prefix-agnostic AND byte-identical a5a5f9f->1b45d43 (fire #2 ran the same fn and PASSED this check, reaching the convert); (c) manifest md5 62d1fb0bf6333eea037146fc8f09c5e0 == committed, chr dtype int64, no NaN, region1 chr = int 1; (d) same interpreter /opt/conda/envs/jupyter (python==python3, pandas 2.3.2); (e) cohort .bim full+stable (size 883,834,306, mtime 2026-06-27 02:26:45, 20,767,864 lines); (f) the EXACT driver path (_read_manifest -> AFR-filtered r1 -> _window_bim_n_var -> _n_var_from_ld_bin on the intact .ld.bin) REPLAYS to window_n_var=102421 == bin_n_var=102421 (MATCH — does not reproduce); (g) a float-chrom repro (_window_bim_n_var(1.0)->0) was PROVEN as a mechanism but RULED OUT as the cause (runtime chr is int, not float). CONCLUSION: a one-off TRANSIENT short read of the cohort .bim by _window_bim_n_var's bim_path.read_text() at the instant plink finished writing the 42 GB .ld.bin. Non-reproducible, no code bug. The loop is ALREADY resume-safe per region (an errored region has no .npz -> recomputed on re-fire); fire #3 shows '0/276' only because we STOPPED it at region 1 to investigate — left alone it would have banked ~275/276. Carter chose (AskUserQuestion) to add a GUARD before re-firing so a transient self-heals IN-run.",
  "headline": "🔥 2026-07-02T20:20Z — LOOP FIRED + RUNNING on the AoU VM (region-1 gate in flight). Carter fired step 4 on the drop-monomorphic + hardened code (origin==local==2d23d67); the 276-region AFR native-plink LD loop is RUNNING server-side (nohup, timeout 312h) on VM AoU_Jupyter_ComputeEngine_20260626b (n1-standard-32/120GB, us-central1-a), region 1 computing, run log ~/native_ld_loop.log. Steps 1-3 (pull to 2d23d67 + re-gate grep write-snplist/n_dropped_monomorphic + `.bim` col-2 uniqueness awk) passed before the fire. GATE = region 1 (~2.5h out): PASS = bucket .npz 0->1 AND panel row m2_region_00001 status==ok AND n_var slightly < 102,421 AND n_dropped_monomorphic ~11 AND a 'region m2_region_00001: dropped N monomorphic' log line AND no 'not symmetric'/Killed/137/dmesg-OOM -> the loop CONTINUES to 276 (no action, ~11 days serial). FAIL = region-1 status=error/not-symmetric or .npz stuck at 0 + Killed -> `kill <PID>` + report the panel row + log tail. Liveness = bucket .npz count -> 276 (NOT the kernel/log). Do NOT restart the kernel; do NOT delete the VM (STOP-on-complete at 276, stop != delete). --- FIX-HISTORY BELOW (all landed+pushed): ✅ 2026-07-02 — DROP-MONOMORPHIC FIX LANDED + GREEN (quick 260701-qcy). The SQUARE plink LD command now emits --mac 1 --nonfounders --write-snplist so plink drops MAC=0 (monomorphic-in-AFR / zero-variance) variants BEFORE --r -> no 0/0->NaN LD -> read_square_bin's symmetry check passes; the driver threads the plink .snplist (_retained_window_bim) so per-region n_var, the window .bim, and the .npz variant list align to the retained polymorphic set. T1 tests @ 587c3d4 (RED) + T2 impl @ c56c715 (GREEN); full tests/m3 green (baseline 309 + 5 new); the 27af416 transient guard PRESERVED (raw-window producer unchanged; only the cross-check operand moved to the retained count; n_var mismatch ValueError byte-identical). read_square_bin/load_bim/content_verify_npz/banded/resume UNCHANGED. HARDENED + BLAST-RADIUS CLEAN (2026-07-02): a 4-dimension Fable-5 blast-radius sweep + gsd-verifier (7/7 must_haves) cleared the fix — downstream m3-04 .rds/SuSiE-coloc loaders align by VARIANT ID so a retained subset is safe and in fact removes the NaN rows that would have broken SuSiE's Cholesky; banded/resume/egress/cost-model unaffected. Two findings closed by a hardening pass (1a9d170 RED + ed9cfd4 GREEN): H1 = a LOUD dup-SNP-id assertion in _retained_window_bim (a duplicate col-2 id raises, closing the one silent-misalignment path the sweep flagged); H2 = n_dropped_monomorphic provenance (new panel column + per-region stderr log of raw-retained, auditable since plink's .log is reclaimed). Module tests 45/45; full tests/m3 finishing on the orthogonal R-stitch tail (the fix's regression surface is the loop-driver module, proven green). RE-FIRE STILL PENDING and MUST pass the REGION-1-ONLY gate first: (1) NCSU push origin, verify tip==HEAD; (2) AoU git pull >= ed9cfd4 on the SAME n1-standard-32 (NO respec); (3) re-gate (grep write-snplist + n_dropped_monomorphic); (4) .bim col-2 uniqueness awk (confirm no dup variant ids); (5) run REGION 1 ONLY -> read_square_bin passes (no NaN, symmetric), status==ok, n_var slightly < 102,421, drop count logged; (6) ONLY THEN the full 276. ⚠ SUPERSEDES the 2026-07-01 correction below. PRIOR 2026-07-01 CORRECTION — the 2026-06-30 'transient .bim short-read' diagnosis was WRONG. Region 1's real, REPRODUCIBLE failure is a SYMMETRY-CHECK failure: ~11 monomorphic (MAC=0-in-AFR) variants make plink emit NaN LD -> NaN!=NaN breaks read_square_bin's symmetry check. PROVEN by running read_square_bin directly on the intact 42GB region-1 .ld.bin (deterministic RAISE) + a pinpoint diagnostic (12 NaN entries across 11 clustered rows, diagonals 1.0). My earlier 'transient' story anchored on a STALE fire-#1 TSV row (n_var mismatch/0 rows) and a determinism replay that tested the WRONG check (n_var count, which passes). SYSTEMIC across the 276 windows. DECISION (Carter 2026-07-01) = DROP MAC=0 variants from the LD panel. The retry-on-zero guard (27af416) is harmless but does NOT fix this. DO NOT FIRE until the drop-monomorphic fix lands + a region-1 re-run passes read_square_bin. Fix routed to /gsd-quick --research --validate (lean: --mac 1 + --write-snplist at the plink step + thread the retained variant list). VM stays n1-standard-32 (NO respec). Both OOM fixes + chr-prefix fix still proven-good.",
  "branch": "m3-W2-aou-deltas",
  "origin_synced": "origin tip == 1b45d43. Local HEAD (>= ed9cfd4) is AHEAD by: retry-guard (bba9cf8 + 27af416) + 2026-07-01 correction docs (c90a629) + drop-monomorphic fix (587c3d4 RED + c56c715 GREEN) + hardening (1a9d170 RED + ed9cfd4 GREEN) + the close-out docs commit — ALL LOCAL-ONLY, NOT yet pushed as of this block. BEFORE the re-fire: push to origin m3-W2-aou-deltas and verify origin tip == local HEAD ([[feedback_push_ncsu_before_aou_clone_fire]] — inert on AoU until pushed). The loop VM repo is at 1b45d43; it must git pull >= ed9cfd4 (drop-monomorphic + hardening) before re-fire.",
  "running_now": "AoU LOOP RUNNING server-side (fired 2026-07-02 ~20:20Z). (1) AoU-SIDE: the 276-region AFR native-plink LD loop is RUNNING on VM AoU_Jupyter_ComputeEngine_20260626b (n1-standard-32/120GB, us-central1-a) on code 2d23d67 (nohup, timeout 312h, run log ~/native_ld_loop.log); region 1 computing = the gate (~2.5h to bank). SURVIVES DISCONNECT — do NOT restart the kernel. The loop PID is in the AoU agent's step-4 fire output (not captured NCSU-side). Liveness = climbing bucket .npz count -> 276 (gsutil ls gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/*.npz | wc -l). (2) NCSU-SIDE: NOTHING running; everything pushed (origin==local==2d23d67); drop-monomorphic full suite passed 314/30, hardening full suite passed 321/30 (EXIT=0).",
  "quick_260630_rn4_state": {
    "what": "Add a transient-short-read RETRY GUARD to the native-plink LD loop's square-path window verify so a one-off .bim short read self-heals in-run instead of silently erroring a region.",
    "design": "Reusable _window_bim_n_var_retry_on_zero(bim_path, chrom, from_bp, to_bp, *, expect_nonzero, retries=_WINDOW_BIM_RETRIES=3, sleep_s=_WINDOW_BIM_RETRY_SLEEP_S=0.5): calls _window_bim_n_var once; if n_var==0 AND expect_nonzero, re-reads up to `retries` times (small sleep); a recovery (>0) emits a LOUD WARN to stderr (visible in run log) and returns the real count. In process_region SQUARE path: compute bin_n_var FIRST, then call the wrapper with expect_nonzero=(bin_n_var>0); the existing `if bin_n_var != window_n_var: raise ValueError(...)` is BYTE-IDENTICAL (a persistent 0 still raises -> status=error -> loop continues; no semantics change). Banded path, resume skip/continue, content_verify_npz, OOM-bounded checks all UNTOUCHED.",
    "status": "DONE + GREEN (executor completed during close-session). T1 (4 failing-first tests) @ bba9cf8 (RED); T2 (impl + wiring) @ 27af416 (GREEN). Full tests/m3 = 309 passed / 30 skipped (baseline 305 + 4 new). Mismatch ValueError byte-identical (git diff shows no +/- on the string). Both code files committed; SUMMARY written @ .planning/quick/260630-rn4-*/260630-rn4-SUMMARY.md. Docs committed in this close-session. REMAINING = PUSH origin (deferred to Carter/next session per close-session policy).",
    "resume_to_finish": "Guard is DONE (27af416) + green + docs-committed. ONLY remaining step = PUSH origin m3-W2-aou-deltas + verify origin tip == local HEAD (feedback_push_ncsu_before_aou_clone_fire — inert on AoU until pushed). Then relay the re-fire to the AoU agent (refire_pending.refire_steps).",
    "test_file": "tests/m3/test_run_native_ld_panel.py (new); impl in src/python/run_native_ld_panel.py"
  },
  "refire_pending": {
    "what": "m3-02e-T4 STEP 4, FOURTH fire: the resumable 276-region AFR native-plink LD loop (square, --keep-allele-order), single VM, serial, egress-clean per-region .npz to the bucket. SAME loop_command.",
    "why_refire": "Fire #3 banked 0/276 because region 1 hit a REAL, reproducible symmetry-check failure: ~11 monomorphic (MAC=0-in-AFR) variants made plink emit NaN LD. FIXED (quick 260701-qcy): --mac 1 --nonfounders --write-snplist drops MAC=0 variants BEFORE --r + the driver threads the retained snplist. Both OOM fixes + chr-prefix fix + the transient guard remain proven/intact. A REGION-1-ONLY gate must pass before the full 276.",
    "vm_decision": "VM STAYS n1-standard-32 (32 vCPU / 120 GB) — NO respec. The 2026-06-29 'respec to n2-highmem-16 (128 GB)' plan is OBSOLETE: the UI offered no 128 GB option (only n1-standard-8..96, n2-standard-8/16), and the real RAM need is the ~40 GiB dense matrix (fire #3 convert+verify cleared 120 GB with no dmesg OOM), safe to ~168k vars (region 1 = 102,421). n1-standard-64 (240 GB) was rejected — ~$800 busts the $500 cap for RAM never touched. Cost now $1.58/hr running; ~$400 for the ~262 VM-h run, under cap.",
    "loop_vm": "AoU_Jupyter_ComputeEngine_20260626b — AoU Jupyter Compute Engine (Standard VM, Hail-free), n1-standard-32 (120 GB), 1TB disk, us-central1-a, UP (loop killed). UI-managed lifecycle (pet SA list-only Dataproc). Carter STOP-ON-COMPLETE order holds (stop, NOT delete, at 276).",
    "loop_command": "nohup bash -c 'export PATH=~/bin:$PATH; timeout 312h python ~/coloc_analysis/src/python/run_native_ld_panel.py --manifest ~/coloc_analysis/config/ld_regions.tsv --bfile-prefix /home/jupyter/afr_cohort --out-dir gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/ --scratch-dir /home/jupyter/native_ld_scratch --mode square --ancestry AFR --num-shards 1 --panel-tsv gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/m3-W2-native-plink-panel.tsv' > ~/native_ld_loop.log 2>&1 &",
    "refire_steps": "1. (NCSU) PUSH origin the drop-monomorphic fix (>= c56c715), verify tip==HEAD. 2. (AoU) On the VM: cd ~/coloc_analysis; git fetch; git checkout -f m3-W2-aou-deltas; git pull --ff-only (>= c56c715). 3. RE-GATE: `md5sum src/python/aou_ld_panel.py` (changed) + `grep -n 'write-snplist' src/python/aou_ld_panel.py` (present) + `grep -n _retained_window_bim src/python/run_native_ld_panel.py` (present + wired in the SQUARE process_region path). 4. REGION-1-ONLY GATE (de-risks the ~11-day/~$400 run): run region 1 ONLY -> `read_square_bin` PASSES (no NaN, symmetric); panel row status==ok; n_var slightly < 102,421 (monomorphic dropped); `head -3 …/m2_region_00001.snplist` = bare one-ID/line; `awk '$3!=0||$4!=0' /home/jupyter/afr_cohort.fam | head` empty (all founders -> --nonfounders no-op). 5. ONLY THEN re-fire the full 276 (SAME loop_command from region 1; 0 banked -> clean restart; MED-6 byte-floor skips nothing). 6. Proof point region 1 ~2.5h out: bucket .npz 0->1; df flat (reclaim); panel row status==ok n_var slightly < 102,421; NO Killed/137, NO dmesg OOM.",
    "liveness_arbiter": "Climbing bucket .npz count: `gsutil ls gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/*.npz | wc -l` (target 276). NOT the kernel light, NOT the log motion, NOT _SUCCESS, NOT the panel TSV. ~148 min for region 1 (102k vars, heavier than the 57-min remeasure region) -> variable per region; ~11 days serial ceiling.",
    "expected": "~262 VM-h ~= ~11 days serial on n1-standard-32 @ $1.58/hr ~= ~$400 total (under the $500 cap). timeout 312h caps compute.",
    "backstops": "(1) timeout 312h; (2) RESUME-SAFE — re-run verbatim, content-valid .npz skipped (MED-6 byte-floor vs the bucket), .npz durable in the bucket; (3) the NEW retry guard self-heals a transient .bim short-read in-run (WARN-logged) instead of dropping a region; even without it, an errored region is just recomputed on the next re-fire pass; (4) Carter STOP-ON-COMPLETE order (VM auto-STOPS, not deletes, at 276) + 2-3 day check-ins. NO self-teardown wrapper (pet SA list-only).",
    "output_paths": {
      "npz_out_dir": "gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/ (durable; per-region {region_id}.npz + .afreq)",
      "panel_tsv": "gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/m3-W2-native-plink-panel.tsv",
      "bfile_local_on_vm": "/home/jupyter/afr_cohort.{bed,bim,fam} (.bed 379,657,321,787 B; .bim 883,834,306 B / 20,767,864 lines, chr-prefixed contigs; survives machine-type respec)",
      "plink_binary": "gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/tools/plink1.9",
      "run_log": "~/native_ld_loop.log on the loop VM",
      "region1_scratch_artifacts": "/home/jupyter/native_ld_scratch/m2_region_00001.ld.bin (42 GB, intact) + /home/jupyter/afr_cohort.1_10000_13506933.window.bim (102,421 rows — but this window .bim was REWRITTEN by our forensic probes, not the fire-#3 original)"
    }
  },
  "session_forensics_2026_06_30": "Fire-#3 region-1 'window .bim has 0 rows' was diagnosed over ~6 read-only dig rounds and PROVEN a non-reproducible transient (NOT chr-prefix, NOT float dtype, NOT stale code, NOT interpreter drift). Ruled out in order: (1) stale/modified src — md5 e99fd817 == committed; (2) chr-literal verify — the deployed _window_bim_n_var uses _chrom_match_key on both sides (line 284) and returns 102421 on the real .bim; (3) function regression a5a5f9f->1b45d43 — byte-identical (fire #2 passed this same check reaching the convert); (4) manifest float64 promotion — manifest md5 matches, chr dtype int64, no NaN; (5) interpreter/pandas divergence — loop python == repro python3 == /opt/conda/envs/jupyter, pandas 2.3.2; (6) determinism — the EXACT driver path (read_manifest->AFR r1->_window_bim_n_var->_n_var_from_ld_bin on the intact .ld.bin) replays 102421==102421. The float-chrom mechanism (_chrom_match_key(1.0)='1.0'!='1' -> 0 rows) was proven possible but ruled out (runtime chr is int). Only viable explanation left standing = a transient short read of bim_path.read_text() at the moment plink finished the 42 GB .ld.bin write. KEY LESSON: verify the runtime cause on real data/config before shipping ([[feedback_verify_assumption_before_shipping]]) — an early 'the verify still does a chr-literal compare' hypothesis AND a '_read_manifest reads float' hypothesis were both DISPROVEN by VM reproduction before any code was touched.",
  "session_hazards_fixed_prior": [
    ".npz-convert OOM (fixed @ 3eac803): read_square_bin's np.allclose(m, m.T) built several full n_var^2 float32 temporaries -> >64 GiB. Bounded via _is_symmetric_blocked. PROVEN on 120 GB in fire #3 (no OOM).",
    ".npz-VERIFY OOM (fixed @ ff9e66f): content_verify_npz re-ran the same unbounded np.allclose -> ~156 GiB. Square->reuse _is_symmetric_blocked; banded->_strict_upper_is_zero_blocked. PROVEN on 120 GB in fire #3 (no OOM).",
    "chr-prefix verify bug (fixed @ baeb925): _chrom_match_key strips chr on both sides. PROVEN by fires #2 AND #3 (region-1 window matched, 102,421)."
  ],
  "cohorts": {
    "afr_primary": {"path": "gs://rw-migration-aou-rw-476cdac2/ld/mt_afr_qc.mt", "samples": 73122, "status": "COMPLETE_VERIFIED, untouched (path /ld/ NOT /ld/mt/). Exported to the bucket bfile (the loop's source)."},
    "eur": {"path": "gs://rw-migration-aou-rw-476cdac2/ld/mt_eur_qc.mt", "samples": 220098, "status": "COMPLETE_VERIFIED — EUR LD sourced from PUBLIC Weissbrod 337k (m3-02e T2); this AoU EUR cohort no longer needed for LD"},
    "afr_sens": {"path": "gs://rw-migration-aou-rw-476cdac2/ld/mt_afr_pca_selfid_qc.mt", "samples": 62557, "status": "COMPLETE_VERIFIED, untouched"}
  },
  "gates": {
    "m3_02e_T1_3": "DONE + pushed @ 1a7bfdd (38 tests).",
    "m3_02e_T4_step2_export": "DONE + byte-verified (.bed 379,657,321,787 B) at gs://.../ld/afr_native_panel/bfile/.",
    "m3_02e_T4_step3_remeasure": "DONE + ACCEPTED (x276 budget gate SATISFIED).",
    "m3_02e_T4_chr_prefix_verify_fix": "DONE @ baeb925; PROVEN by fires #2 AND #3.",
    "m3_02e_T4_npz_convert_oom_fix": "DONE @ 3eac803; PROVEN on 120 GB in fire #3 (no dmesg OOM).",
    "m3_02e_T4_npz_verify_oom_fix": "DONE @ ff9e66f; PROVEN on 120 GB in fire #3 (no dmesg OOM).",
    "m3_02e_T4_transient_shortread_guard": "DONE + GREEN (quick 260630-rn4): _window_bim_n_var_retry_on_zero. T1 @ bba9cf8, T2 @ 27af416. Harmless but does NOT fix the region-1 symmetry failure; PRESERVED intact by the 260701-qcy drop-monomorphic fix (still the raw-window producer).",
    "m3_02e_T4_drop_monomorphic_mac0": "DONE + GREEN (quick 260701-qcy): SQUARE build_plink_ld_command emits --mac 1 --nonfounders --write-snplist (drop MAC=0 monomorphic BEFORE --r -> no NaN LD); new _retained_window_bim threads the plink .snplist so n_var==retained + .npz aligns. T1 @ 587c3d4 (RED), T2 @ c56c715 (GREEN); full tests/m3 green (baseline 309 + 5 new). read_square_bin/load_bim/content_verify_npz/banded/resume UNCHANGED; n_var mismatch ValueError byte-identical; 27af416 guard preserved. NOT yet pushed -> push -> region-1 gate -> re-fire.",
    "m3_02e_T4_step4_loop": "RE-FIRE PENDING on the FIXED code (c56c715, needs push) + the SAME n1-standard-32 VM (NO respec). All FOUR identified 0/276 causes now fixed & (chr-prefix + 2 OOM) proven; the monomorphic-NaN symmetry failure fixed by drop-MAC=0 (260701-qcy) — MUST be validated by a REGION-1-ONLY re-run (read_square_bin passes, status==ok, n_var slightly < 102,421) BEFORE the full 276. Liveness = bucket .npz count -> 276.",
    "m3_04": "SUPERSEDED-PENDING-REPLAN — must CONSUME m3-02e AFR .npz + public EUR .rds (NOT rebuild via Hail). Re-plan after the T4 panel lands."
  },
  "cluster": "Loop runs on the GCE/Jupyter VM 20260626b (NOT a Dataproc cluster), n1-standard-32 (120 GB), UP, loop killed. Carter STOP-ON-COMPLETE (stop, NOT delete). Dataproc clusters: 20260604 = Hail (export, STOPPED ~$169 PD standby; Carter may UI-DELETE); 20260605 = other Hail (untouched, re-export fallback); 20260617/20260620 = NO Hail; probe VM 20260608 = Stopped.",
  "resume_on_reconnect": [
    "1. THE LOOP IS RUNNING on the AoU VM (fired 2026-07-02 ~20:20Z on code 2d23d67; server-side, survives disconnect). Do NOT restart the kernel; do NOT re-fire (it is already running). FIRST via the AoU agent / data layer, check the REGION-1 GATE outcome: `gsutil ls gs://rw-migration-aou-rw-476cdac2/ld/afr_native_panel/*.npz | wc -l` (0->1+); `gsutil cat` the panel TSV row for m2_region_00001 (want status==ok, n_var slightly < 102,421, n_dropped_monomorphic ~11); grep ~/native_ld_loop.log for 'region m2_region_00001: dropped N monomorphic' AND no 'not symmetric'/Killed/137/'ambiguous variant id'; dmesg no OOM.",
    "2. IF REGION 1 PASSED (npz 0->1, status==ok, drop count logged, no error): the loop is running to 276 — nothing to do but MONITOR at a 2-3 day cadence. Liveness = climbing bucket .npz count -> 276 (~148 min region 1, ~11 days serial). STOP-on-complete (stop != delete) at 276.",
    "3. IF REGION 1 FAILED: 'not symmetric' -> a residual NaN survived --mac 1 (astronomically unlikely; run read_square_bin + the NaN pinpoint on the fresh region-1 .ld.bin). 'ambiguous variant id' -> the H1 dup-id assertion fired => the cohort .bim has duplicate col-2 ids (investigate the hl.export_plink varid). Any error -> the AoU agent kills the loop (kill <PID> from the step-4 output) + reports the panel row + log tail. Resume-safe: 0 banked -> nothing lost; a fixed re-fire restarts clean.",
    "4. Liveness by DATA LAYER ONLY (bucket .npz count), NOT the kernel light / log motion / panel-TSV mtime. A SIGKILL writes no panel row; peak_ram_gib measures the plink child not the convert -> judge OOM by grep Killed/137 + dmesg.",
    "5. STOP-ON-COMPLETE IS ORDERED (STOP, not DELETE): at 276 the VM is STOPPED (VM + PD + local bfile preserved). Do NOT delete it (a delete destroys the local /home/jupyter/afr_cohort bfile). Any DELETE is Carter's explicit action.",
    "6. At 276 + panel all status==ok: agent egresses ONLY aggregate .npz+AF, pings native-panel-recorded -> NCSU reconstructs m3-W2-native-plink-panel.tsv + shutdown record, pushes, verifies origin tip == local HEAD, and m3-04 REPLAN (CONSUME the AFR .npz + public EUR .rds; do NOT rebuild via Hail). Also pending: the all-NaN AF sidecar (native path emits no --freq) is a tracked pre-m3-04 follow-up."
  ],
  "prep_landmines_fixed": [
    "DONE: origin==local==2d23d67 (pushed); the AoU VM was git-pulled to 2d23d67 and re-gated (grep write-snplist + n_dropped_monomorphic + .bim col-2 uniqueness awk all passed) before the fire. The loop is RUNNING. On any future fresh-clone re-fire the re-gate is: grep 'write-snplist' (aou_ld_panel.py) + grep _retained_window_bim / n_dropped_monomorphic (run_native_ld_panel.py) + `.bim` col-2 uniqueness awk.",
    "Do NOT respec the VM — it STAYS n1-standard-32 (120 GB). The 2026-06-29 n2-highmem-16 plan is obsolete; 120 GB cleared the region-1 convert+verify with no OOM, and the UI offered no 128 GB option anyway.",
    "chr-format: AoU GRCh38 cohort .bim is chr-prefixed (chr1), ld_regions.tsv chr is bare (1); verify is prefix-agnostic via _chrom_match_key (proven fires #2+#3).",
    "--bfile-prefix is LOCAL (/home/jupyter/afr_cohort) — plink can't read gs://; only --out-dir/--panel-tsv are gs://. Local bfile survives a respec.",
    "The fire-#3 region-1 window .bim on scratch (afr_cohort.1_10000_13506933.window.bim, 102,421 rows) was OVERWRITTEN by our forensic probes — it is NOT the fire-#3 original; don't read it as evidence of the fire-#3 verify.",
    "Teardown is order/UI-driven (pet SA list-only) — NO self-teardown; Carter ordered STOP-on-complete (STOP, not delete). timeout 312h caps compute."
  ],
  "resume_entry_point": ".planning/HANDOFF.json (this 2026-07-02T20:20Z LOOP-FIRED-RUNNING / region-1-gate-in-flight block) + .planning/STATE.md (2026-07-02 RESUME-HERE block) + .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md + the quick 260701-qcy dir (drop-monomorphic fix + hardening + RESEARCH + VERIFICATION). Operating manual = aou-ld-pipeline skill. Permission boundary = memory reference_aou_pet_sa_listonly_dataproc. Root-cause forensics (fire #3 = monomorphic-NaN symmetry failure, NOT the earlier mis-diagnosed transient) = STATE.md 2026-07-01 CORRECTION block.",
  "do_not": [
    "⚠ 2026-07-02: the DROP-MONOMORPHIC fix has LANDED + green (c56c715) but is NOT yet pushed. DO NOT FIRE the full 276-region loop until: (1) push origin + verify tip==HEAD; (2) AoU git pull >= c56c715 on the SAME n1-standard-32 (NO respec); (3) the AoU agent runs REGION 1 ONLY and confirms read_square_bin passes (no NaN, symmetric), status==ok, n_var slightly < 102,421. Region 1's failure was a REAL, reproducible symmetry-check failure from monomorphic (MAC=0-in-AFR) variants emitting NaN LD — now fixed by --mac 1 --write-snplist + snplist threading. Skipping the region-1 gate risks a bad ~11-day / ~$400 run.",
    "Do NOT respec the VM — n1-standard-32 (120 GB) stays. The OOM concern is resolved (both fixes proven on 120 GB in fire #3).",
    "Do NOT DELETE the loop VM (destroys the local /home/jupyter/afr_cohort bfile). Carter ordered STOP-on-complete; any delete is a separate Carter action.",
    "Do NOT restart the kernel / treat the kernel light as liveness — liveness is the bucket .npz count.",
    "Do NOT re-diagnose the fire-#3 region-1 error as a transient — the 2026-06-30 transient story was WRONG (corrected @ c90a629). It is a REAL, reproducible monomorphic-NaN symmetry failure, now FIXED @ c56c715 (drop MAC=0). Do NOT revert the --mac 1 / --write-snplist / _retained_window_bim changes.",
    "Do NOT skip the REGION-1-ONLY gate before the full 276 — a bad ~11-day/~$400 run is the cost of skipping it.",
    "Do NOT modify plink_ld_to_npz.py's read_square_bin/load_bim/symmetry/diagonal/OOM-bounded checks or content_verify_npz — they CAUGHT the NaN and are correct; the fix is upstream (drop the monomorphic rows before LD)."
  ]
}
