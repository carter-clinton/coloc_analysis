{
  "version": "1.0",
  "timestamp": "2026-06-24T00:00:00.000Z",
  "phase": "m3",
  "phase_name": "m3-aou-afr-ld-panel-build",
  "phase_dir": ".planning/phases/m3-aou-afr-ld-panel-build",
  "wave": "Wave 2 (m3-02d) — write+egress-bound LD re-scope. Cost RE-ARCHITECTURE ACCEPTED (EUR=public UKBB LD, AFR=in-house native plink on a Spot VM, downstream on NCSU). Native-plink PILOT COMPLETE 2026-06-24 = GREEN (full AFR panel ~$174-1,084, ~1-2 OOM cheaper than Hail's ~17k cluster-h AFR half). Artifacts reconstructed + pushed from NCSU. NEXT = re-plan m3-02e (Carter's explicit trigger). The Hail 34k-cluster-h path is NOT taken.",
  "status": "2026-06-24. NATIVE-PLINK PILOT COMPLETE = GREEN. Same cell the Hail v2 probe measured (m2_region_00040__sub00 AFR, chr12 GRCh38 37463740-45398515, MAF>=0.005, n_var=64,060, count_cols=73,122). On the master n2-standard-16 (plink1.9 single-node): banded --r gz (ldwin 3000kb, r2 0, keep-allele-order) wall 25.446 min / peak RAM 16.68 GiB / .ld.gz 16.55 GiB; square --r square bin4 wall 56.224 min / peak RAM 17.89 GiB / .ld.bin 15.29 GiB (64060^2 float32, diag=1.0, symmetric). Extrapolation x276 regions: banded 117.05 VM-h (Spot $174 / on-demand $490), square 258.6 VM-h (Spot $385 / on-demand $1,084) -> GREEN, well under the $3-4k budget. Cluster 20260604 STOPPED ($0); resize-to-2 abandoned (UPDATING stall) so it ran on full 24 workers but plink is single-node so worker count was immaterial. Token-free handback (websocket dropped at shutdown -> on-disk copies lost; NCSU reconstructed the TSV verbatim + the report from the handback numbers + a pilot-cluster-shutdown record, and pushed). PRIOR (superseded by the cost re-architecture): AFR re-probe v2 COMPLETE + Task 5 go/no-go RUN. v2 AFR cell m2_region_00040__sub00 (A.3, ordering B) COMPLETED CLEAN: density-veto routed A.3 (64,176 var > 24,301), 3.0173 blk/min, 0 spill, .bm ~18.5 GiB verified. Cluster 20260604 STOPPED ($0). Workbench had NO push token -> token-free handback (AoU cat'ed 3 artifacts + stopped, commit 07f43dc discarded, NCSU reconstructed + pushed). TASK 5 (redo_ld_cost_model.py) over the FULL 552-cell preflight (m3-W2-preflight-counts-FULL.tsv, recovered from v1 210e66c, geometry-identical to v2 dev) + measured AFR rate + conservative 3.01 EUR fallback (the v1 EUR 4.85 rate counts matmul-blocks vs AFR banded-blocks -> a combined 0.62 factor is a block-definition artifact, physically backwards -> EXCLUDED) -> PROJECTED ~34,207 cluster-h (x1.3 = 44,469), ~30x the old 1,117 est, disposition YELLOW-finer-split. NOT GREEN for any plausible cap. m3-W2-budget-redo.md WRITTEN.",
  "headline": "PILOT GREEN settles the cost question: native plink computes one AFR region's banded LD in 25.4 min on a single n2-standard-16, vs ~24 cluster-h for the SAME cell on the 24-node Hail BlockMatrix path. Extrapolated x276 AFR regions the whole panel is ~$174-490 banded / ~$385-1,084 square (Spot/on-demand) -- well inside the $3-4k budget and ~1-2 OOM cheaper than Hail. This confirms Move 2 of the accepted re-architecture (EUR=public UKBB LD $0, AFR=in-house native on a Spot VM, downstream NCSU). Recommended output mode = square bin4 (SuSiE-ready, fixed-size, verified symmetric). PRIOR-HEADLINE (historical): m3-02d Task 4 re-probe v1 came back PARTIAL and exposed a systemic routing bug: _route_region_path demoted A.1/A.2->A.3 only on the SPAN axis, but the to_numpy() driver-OOM determinant is the DENSITY axis (n_var^2 * 8 bytes, float64). Dense-but-narrow cells (span<=10Mb, high density) OOMed the driver. FIXED with a second density veto (n_var>24,301 -> A.3), n_var threaded through both call sites; 100/106 A.2 cells flip A.2->A.3. NEXT = AFR re-probe v2: the AFR __sub00 cell now routes A.3 and will complete like EUR did, giving the clean AFR blocks_per_min the cost model needs for a real go/no-go.",
  "branch": "m3-W2-aou-deltas",
  "origin_synced": "Pre-pilot-commit: origin == local HEAD == dd495ba (clean, 0 ahead/0 behind; the older note about unpushed 31d6e6c/05e42a9 is STALE -- git shows full sync). This session adds one docs commit landing the pilot artifacts + state; push it and verify origin tip == local HEAD ([[feedback_push_ncsu_before_aou_clone_fire]]).",
  "running_now": "NOTHING running. Cluster 20260604 STOPPED ($0 idle, Apps-panel Stopped badge confirmed). NCSU side = $0. Other clusters: 20260605 HAIL spare, 20260617/20260620 do-not-start.",
  "this_session_work": {
    "probe_m3_02c": "FIRED 2026-06-22, partial. Quota Tasks 1-2 CLOSED (N2_CPUS=5000 pre-satisfied). STEP A preflight DONE (all 15 dev cells >75k var → split too coarse; outputs up to 277 GiB > 50GB egress cap). STEP B minimal EUR probe (m2_region_00040__sub00, 78,730 var): ZERO spill on a 64GB n2-standard-16 cluster at cores=1/11g/3g, master stable — the n2-highmem 'need-big-executors-or-spill' premise was WRONG; the bottleneck is the A.3 WRITE; cell INTERRUPTED at ~56min (no clean rate). 3 artifacts committed (m3-W2-preflight-counts.tsv, cost-probe.tsv [INTERRUPTED/partial], cluster-shutdown.md).",
    "replan_m3_02d": "/gsd-plan-phase: research V2 (m3-RESEARCH-W2-RESCOPE-V2.md) → plan (m3-02d-W2-rescope-write-egress-split-PLAN.md, 5 tasks) → plan-checker PASS. Carter LOCKED band width AFR 3Mb / EUR 5Mb (3-5x the LD-decay scale, under Pan-UKBB 10Mb).",
    "execute_m3_02d_tasks_1_3": "Tasks 1-3 executed by gsd-executor: 7cea579 (per-ancestry buffer + regen), ade6066 (ordering-B + re-derived over_threshold + ld_egress_bundle.py), 19fb1e3 (redo_ld_cost_model.py). tests/m3 235 passed / 38 skipped / 0 failed. Two faithful deviations: regen from the pre-split parent recovered from git 96f57d1 (committed manifest was already split → re-split would no-op); projection rows kept one-per-subregion (per-ancestry geometry lives in the manifest compute rows).",
    "staged": "m3-02d-REPROBE-BRIEF.md (turnkey Task 4 runbook). STATE + ROADMAP updated. Pushes: f1899d4 (research+plan), c695e0f (roadmap/state), 19fb1e3 (code tasks), 3d53980 (brief+state)."
  },
  "next_action": "EXECUTE m3-02e Tasks 1-3 IN A FRESH SESSION (Carter's choice 2026-06-24). The m3-02e wave is PLANNED + plan-checked + committed @ 8d3dbe4 (plan-checker PASS-WITH-CONCERNS; blocker B-1 reconciled = m3-04's 322-cell Hail LD fire recorded SUPERSEDED-PENDING-REPLAN in both the plan frontmatter `supersedes_note` and the ROADMAP m3-04 bullet; W-1..W-4 fixed). Plan = .planning/phases/m3-aou-afr-ld-panel-build/m3-02e-W2-native-ld-export-and-public-eur-PLAN.md (wave 4, depends_on 02b/02d/03). 4 tasks: T1-T3 AUTONOMOUS NCSU code+tests (T1 plink_ld_to_npz.py reader + export_cohort_to_plink/build_plink_ld_command helpers in aou_ld_panel.py, --keep-allele-order mandatory, canonical vid chr:pos:REF:ALT; T2 build_public_eur_manifest.py + extend download_ukbb_ld_tiles.py + m3_public_eur_ld.smk for the PUBLIC Weissbrod 337k EUR panel via AWS S3 boto3 UNSIGNED + hg19->hg38 adapter; T3 pipeline.yaml EUR head=EUR_ukbb_pub + NEW estimate_s guard in finemap.smk + resolver tests + record m3-04 superseded), T4 = checkpoint:human-action gate=blocking autonomous:false (the billable in-perimeter native plink LD loop over 276 AFR regions — Carter fires; its turnkey runbook m3-02e-AFR-NATIVE-FIRE-BRIEF.md is authored as autonomous T1-3-adjacent work). ===> FRESH-SESSION EXEC NOTE: this NCSU session's gsd-executor SUBAGENT DISPATCH tripped a FALSE-POSITIVE cyber safeguard 3x (the plan's cloud/egress/perimeter vocabulary reads as security-adjacent to the model filter — the work is benign LD-matrix code). A fresh session/clean context typically clears it; if the gsd-executor dispatch trips again, use neutral engineering wording or execute T1-3 inline. TDD RED-first, smoke_dev py3.11 env for pytest, m3-r-ld env R for the T2 .rds round-trip, atomic commits per task (explicit paths, token m3-02e-TN), honor the .npz lower_triangular flag contract + keep the retired _write_a3_banded_correlation_bm in-tree. After T1-3 land + pytest tests/m3 green + the runbook exists -> the ONLY remaining billable step is T4 (Carter fires per the brief; production-VM wall re-measure is a BLOCKING stop-gate). SUPERSEDED (done this session): re-plan m3-02e. The native-plink PILOT was GREEN (m3-W2-pilot-report.md / m3-W2-pilot-plink-native.tsv / m3-W2-pilot-cluster-shutdown.md, pushed). m3-02e shape = export-once (QC'd AFR cohort -> plink .bed from the Hail MT, one count_cols scan amortized) -> native per-region AFR LD on a single Spot VM looping the 276 regions (plink1.9 --r square bin4 --keep-allele-order recommended; banded with r2 floor if disk-tight) -> egress-clean aggregate LD .npz -> NCSU ingest AFR .npz + PUBLIC EUR LD (Weissbrod/PolyFun 337k or Pan-UKBB 420k) + hg19->hg38 liftover adapter -> coloc/SuSiE with estimate_s diagnostic. PILOT CAVEATS to fold into m3-02e: (1) the $4.19/$1.49 rates are labelled n2-highmem-64 but the pilot ran on n2-standard-16 -> re-measure production-VM wall before committing the budget; (2) banded pair count ~400M is estimated from output size; (3) per-region export ~33s, the ~20min count_cols scan is one-time. SUPERSEDED-CONTEXT (the cost-re-architecture decision that motivated the pilot): COST RE-ARCHITECTURE ACCEPTED (m3-W2-cost-effective-rearchitecture.md; budget stretched to $3-4k). 3 moves: EUR LD = PUBLIC UKBB reference (Carter chose public; Weissbrod 337k .npz / Pan-UKBB 420k; $0 + hg19->hg38 adapter on NCSU), AFR LD computed in-house but NATIVE (plink1.9/LDstore2/emeraLD on a single Spot VM ~$1.49/hr, NOT Hail BlockMatrix on a 24-node cluster ~$23.4/hr -> ~1-2 OOM cheaper), downstream on NCSU (egress-clean). IMMEDIATE NEXT = Carter fires the one-region native PILOT per m3-W2-PILOT-plink-native-BRIEF.md (autonomous:false, ~$5-25): START HAIL 20260604 (resize workers->2), export m2_region_00040__sub00 AFR (chr12 GRCh38 37463740-45398515, MAF>=0.005, ~64176 var -- the SAME cell the Hail v2 probe measured at ~24 cluster-h) to plink, run plink1.9 --r banded + --r square --keep-allele-order, measure wall/RAM/output, extrapolate x276 AFR regions x VM-$/hr, verdict 'AFR panel < $3-4k?', STOP cluster, cat-handback (no push token), ping 'pilot-recorded' -> NCSU reconstructs+pushes -> re-plan as m3-02e (export-once -> native per-region AFR LD on Spot VM -> egress -> NCSU ingest AFR .npz + public EUR + liftover -> coloc/SuSiE with estimate_s diagnostic). The Hail 34k-cluster-h path is NOT taken. SUPERSEDED-DETAIL (m3-02d Task 5): cap ~$1k -> gate RED. RE-SCOPE LEVER ANALYSIS DONE (m3-W2-rescope-levers-analysis.md): $1k is 2-8x BELOW genome-wide AFR+EUR cost. KEY CORRECTION: finer-split is NOT a total-cost lever (it's a per-cell tractability requirement; band-overlap replication makes total cost RISE with split count -> the model's 'YELLOW-finer-split' label misleads). Real levers = band width (~linear, bounded by LD-decay scale) + cluster-size/npm (biggest arithmetic lever ~8x, but VALIDITY UNCONFIRMED: probe evidence -- Stage 6 CPU-active -- suggests the A.3 row_correlation matmul IS distributed across workers, so shrinking the cluster ~proportionally slows each cell). Even the optimistic stack (npm=3 + buffer 1.5/2.5) ~3,052 cluster-h x1.3, ~3x over -> a SCOPE CUT (AFR-only ~-52% / candidate regions), BUDGET RAISE, or METHOD CHANGE is required, not tuning. SCOPE/BUDGET DECISION PENDING -- Carter offered {~$5 sizing probe / AFR-only / raise budget / pause} and chose PAUSE (thinking). Recommended unblocker when he returns = the ~$5 sizing probe: re-run the verified cell m2_region_00040__sub00 on a MINIMAL cluster (master + 2-4 workers) and measure whether blocks_per_min holds (~3, write-bound -> small cluster -> scope-reduced panel ~affordable) or collapses (compute-distributed -> genome-wide at $1k infeasible). Pair (free, NCSU) with regenerating the production manifest so over-threshold large regions split into tractable banded sub-cells. m3-04 stays BLOCKED on this PLUS correctness preconditions: HLA region_00145__sub19 finer-split (T-M3RS2-HLA-01), P2 (resume guard, ready to build, pure NCSU), P1 A/B/C (bm_to_npz densify; rec Option A in-perimeter fat-driver densify; BLOCKER, Carter's call). Nothing running, $0. DO NOT start 20260620/20260617 (no Hail); DO NOT use cores=2; DO NOT attempt ordering A.",
  "reprobe_brief": ".planning/phases/m3-aou-afr-ld-panel-build/m3-02d-REPROBE-BRIEF.md (turnkey, self-contained). The cluster section of the older m3-W2-AOU-FIRE-BRIEF.md is SUPERSEDED by it.",
  "prep_landmines_for_the_reprobe": [
    "START 20260604 via Apps-panel (NOT gcloud — keeps Workbench registration; NOT 20260620/20260617). Hail is a create-time config — only 20260604/05 have framework=HAIL ([[reference_aou_hail_cluster_create_time_config]]).",
    "Executor config cores=1/11g/3g/11g (14336MB < the 15564MB YARN container cap) — this is the measured 0-spill config; cores=2 won't fit. Set PYSPARK_SUBMIT_ARGS before the hail import; restart kernel if a stale config bound.",
    "git pull + checkout -f on the cluster home; symlink ~/coloc_analysis; os.chdir before the loop; USE_DEV_SUBSET=True; the WORKSPACE_BUCKET hard pin is baked (AOU-2 cell idx 5).",
    "HLA: the dev selector resolves chr6 to region_00143 NOT region_00145; region_00145 __sub rows are in the FULL manifest only → pull them for the mandatory HLA preflight (finer-split chr6 --max-subregion-span-mb 3 if a HLA sub-cell still exceeds the thresholds)."
  ],
  "cohorts": {
    "afr_primary": {"path": "gs://rw-migration-aou-rw-476cdac2/ld/mt_afr_qc.mt", "samples": 73122, "status": "COMPLETE_VERIFIED, untouched"},
    "eur": {"path": "gs://rw-migration-aou-rw-476cdac2/ld/mt_eur_qc.mt", "samples": 220098, "status": "COMPLETE_VERIFIED, untouched"},
    "afr_sens": {"path": "gs://rw-migration-aou-rw-476cdac2/ld/mt_afr_pca_selfid_qc.mt", "samples": 62557, "status": "COMPLETE_VERIFIED, untouched"}
  },
  "gates": {
    "QUOTA_tasks_1_2": "CLOSED — N2_CPUS=5000 us-central1 pre-satisfied",
    "CLUSTER_PROVISIONING": "RESOLVED — use existing HAIL cluster 20260604 (64GB, cores=1); no n2-highmem needed (0 spill measured) or creatable",
    "m3_02d_tasks_1_3": "DONE + pushed @ 3d53980 (tests/m3 235 passed)",
    "m3_02d_task_4_reprobe": "COMPLETE — AFR re-probe v2 fired on 20260604, m2_region_00040__sub00 AFR A.3 ordering-B COMPLETED CLEAN (3.0173 blk/min, 0 spill, .bm 18.5 GiB verified). Cluster STOPPED $0.",
    "m3_02d_task_5_gonogo": "DONE — NOT GREEN for the HAIL path. PROJECTED ~34,207 cluster-h (x1.3 = 44,469) over the full 552-cell preflight + measured AFR rate + conservative 3.01 EUR fallback; disposition YELLOW-finer-split. m3-W2-budget-redo.md written. SUPERSEDED by the cost re-architecture + native-plink pilot below — the Hail path is NOT taken.",
    "m3_native_plink_pilot": "DONE — GREEN (2026-06-24). Native plink1.9 on the master n2-standard-16 computed m2_region_00040__sub00 AFR banded LD in 25.446 min (vs ~24 cluster-h on Hail). Extrapolated x276 regions: banded 117.05 VM-h ($174 Spot / $490 on-demand), square 258.6 VM-h ($385 Spot / $1,084 on-demand) -> full AFR panel well under the $3-4k budget. Artifacts m3-W2-pilot-plink-native.tsv + m3-W2-pilot-report.md + m3-W2-pilot-cluster-shutdown.md reconstructed on NCSU from the token-free handback + pushed.",
    "M3_04_VIABILITY": "NOT GREEN at the current split — ~34k cluster-h projected (~30x the old 1,117 est). 0 spill at 64GB cores=1 holds, but the A.3 WRITE throughput makes the full 552-cell production intractable within any plausible cap. Binding lever = FINER SPLIT (cut blocks/cell), then re-probe + re-cost.",
    "GATE_3_322cell_production": "stays in m3-04, AFTER the m3-02d go/no-go"
  },
  "cluster": "Re-probe target = 20260604 (HAIL, n2-standard-16 ×24, 64GB, ~$19-20/hr, cores=1). wb CLI from NCSU = control-plane only (start/stop/launch-proxy; NO create) + VPC-SC-walled data plane → all in-perimeter work is agent-side. wb cluster stop is the from-NCSU billing-safety lever.",
  "resume_entry_point": ".planning/HANDOFF.json (this file) + .planning/STATE.md (2026-06-22 RESUME HERE block) + .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md (LATEST) + the m3-02d PLAN + m3-02d-REPROBE-BRIEF.md. Operating manual = aou-ld-pipeline skill.",
  "do_not": [
    "Do NOT start 20260620 (no Hail) or 20260617 (16GB, not HAIL) — only 20260604/05 are HAIL.",
    "Do NOT use cores=2 on the 64GB cluster (won't fit the 15564MB container cap) — cores=1/11g/3g.",
    "Do NOT hand-install the Hail wheel on a live cluster (JVM/Spark version-mismatch guardrail).",
    "Do NOT fire the re-probe without Carter's explicit go — it bills ~$20/hr and is in-perimeter.",
    "Do NOT git add -A on the GPFS tree — explicit paths only.",
    "Nothing is running — NO kernel to protect, NO reattach; a fresh session starts clean from the docs."
  ]
}
