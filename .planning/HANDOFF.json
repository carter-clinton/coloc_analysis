{
  "version": "1.0",
  "timestamp": "2026-09-18T19:10:00Z",
  "phase": "m3",
  "phase_name": "m3-aou-afr-ld-panel-build",
  "phase_dir": ".planning/phases/m3-aou-afr-ld-panel-build",
  "wave": "UPDATED 2026-08-12 EVENING (quick-260812-ot2 + 260812-ox1 + the five-dimension blast-radius sweep + quick-260812-thz): two clauses of the close-record below are superseded. (a) NEXT is NO LONGER “place ms-correction-v2 -> post osf-correction-v2”: obligation (2) is SKIPPED BY CARTER’S DIRECTION = DEFERRED, NOT DISCHARGED (DEC-2026-08-12-e2-obligation1-venue-and-obligation2-skip); current NEXT = venue pick (Nature-first memo) -> P-1/P-2 closing-sentence choice -> place ms-correction-v2 per the ot2 placement SPEC. (b) THE FIRE SURFACE IS NO LONGER the rcw review directly: it is the READY-TO-FIRE runbook at .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md, which wraps the corrected review; PRE-FIRE 1 LANDED (5284505) so 1b branch (i) is the live default. || 2026-08-12 CLOSE BODY, PRESERVED: SESSION CLOSE 2026-08-12. One continuous arc since resume on 2026-08-11: FIVE validated quick tasks + a five-way adversarial review. (1) 260811-oku — E-2 disclosure drafts, both framings, decision surface; (2) 260811-pmv — SR4-OPEN evidence dossier (NEVER-FROZEN, all drift traceable); (3) 260811-rcw — m3-04c Task 3 PRE-FIRE gate review, 20/20 L-checks, suites 902/31/0 + 136/1/0 re-run once; (4) 260811-tf3 — Carter DECIDED both open items: E-2 framing = B/CORRECTION (DEC-2026-08-11-e2-framing-correction, obligation 3 DISCHARGED) and SR4-OPEN = NEVER-FROZEN (DEC-2026-08-11-sr4-disposition); (5) Carter then ordered a comprehensive adversarial review (Codex CLI v0.141.0 read-only + 4 blind investigators + /assess-blast-radius) which found 5 blockers -> quick-260812-09a REMEDIATED 21/22 findings (1 reported-with-reason: STATE.md:15 is inside the frontmatter fence), DEC-2026-08-12-adversarial-review-remediation. NET STATE: v2 disclosure pair (260812-09a-SELECTED-PAIR-correction-v2.md) SUPERSEDES tf3's v1 — place/post ONLY v2; the rcw PRE-FIRE review is CORRECTED IN PLACE and is the fire surface; ZERO Carter decisions outstanding. NEXT = Carter's external actions with the v2 texts (journal-policy pre-placement check -> place ms-correction-v2 -> post osf-correction-v2 + record URL/timestamp in osf_deviations.md), then m3-04c Task 3 (the terminal fire gate — NEVER an agent).",
  "headline_2026_08_04": "⛔⛔ m3-04c IS HALTED — ITS HEADLINE must_have IS FALSE AS SPECIFIED. The m3-04b blast-radius sweep (4 independent read-only investigators + orchestrator verification, $0, no perimeter) found the 2026-08-03 panel-unreachability diagnosis is RIGHT BUT ONE LAYER TOO SHALLOW. BLOCKER-1: `{input.ld_matrix}` IS NEVER REFERENCED IN run_finemap's SHELL BLOCK. The R script gets only `--ld-dir {params.ld_dir}` + `--region {params.region_id}` and RECONSTRUCTS its own path at run_susie_rss.R:124-127 as file.path(ld_dir, ancestry, region_id.rds) — ancestry is 'AFR', NEVER 'AFR_aou', and NO rule promotes AFR_aou/*.rds into AFR/ (grep over every .smk + Snakefile = zero hits). On a miss it falls SILENTLY to an identity matrix (run_susie_rss.R:472-474). PROVEN EMPIRICALLY: with the AoU panel present on disk and correctly resolved, resolve_ld_path returns .../AFR_aou/m2_region_00067.rds (exists=True) while SuSiE opens .../AFR/FTO_16q12.rds (exists=False). SO m3-04c T1's ONE-ARGUMENT CHANGE CANNOT MAKE THE PANEL READABLE — the fire would STILL bank a panel nothing reads. This defect is PRE-EXISTING (m3-W3-T2 wired resolve_ld_path into input: only); m3-04b aggravated only the BELIEF, via its docstring. BLOCKER-4 (independent, autonomous, $0): a PARTIAL Stage-A rollup makes assemble_occlusion_catalog.py:352-359 IGNORE EVERY EXCLUDELIST and stamp the result 'stage_a_manifest' — the override fires when the rollup is merely NON-EMPTY, not COMPLETE, and run_native_ld_panel.py:821-831 already swallows the per-region manifest-append exception that creates that state. There is NO completeness check against 276 anywhere. Result = orphaned variants wearing a clean provenance stamp. ⇒ NEXT = a CARTER DECISION on the BLOCKER-1 remedy, then REPLAN m3-04c. DO NOT execute m3-04c as written; DO NOT fire.",
  "status": "2026-09-18 SESSION CLOSE. Nothing running; only app 20260626b, STOPPED; origin == local at 3c570f0. (A) THE REVIEWER'S FINDING 2 LOOP IS CLOSED: his reply and his closure are banked as received (chat pastes, NOT byte-verified), the tail disclosure reports the evidence rather than a side of 0.05, both chr15 windows are reported with neither selected, the ICC is scoped to the 719 clustered pairs, the provenance is corrected in his favour, and Finding 1 gained one scope sentence in his accepted formulation with its counts byte-frozen. He expects nothing further until a new measurement or a posting decision. Disclosure still DRAFTED - NOT POSTED; posting is Carter's alone and is NOT made. (B) STAGE C's NaN ERROR POSTURE IS STILL UNDECIDED and now blocks on ONE message in Carter's hands: courier options-draft v2 (read at 57a3105) brief-blind with the drafted cover note. He has NOT confirmed sending it. (C) COST-1 IS STILL UNMEASURED: m2_region_00071 deferred at n_var=169803 > ceiling=120000, so the runbook's 'largest square-feasible region' label for it is FALSE; Card 2 re-picks the target from a per-region n_var census.",
  "no_fire_running": "NOTHING RUNNING. Only app in the workspace: aoujupytercomputeengine20260626b, STOPPED (its reattachable PD holds the 379 GB bfile). 20260608 is GONE - confirmed 2026-09-17 at BOTH the Workbench level and via in-perimeter `gcloud compute instances list` (non-empty list, rc=0, no 20260608 row, so not a permissions artifact).",
  "nc_state_cost": "$0 for the entire 2026-08-14 -> 08-16 arc. NOTHING running server-side or NC-State-side. ZERO perimeter contact: no gsutil, no gcloud, no bq, no wb, not even read-only control-plane. No LSF jobs (bjobs: \"No unfinished job found\"). The AoU browser agent stood down AT the Step 3 GATE and the VM was NEVER STARTED. Local compute only: read-only text normalization/diff/md5 and two gate-script runs.",
  "verified_this_session_firsthand": [
    "Seth 9,907-B body: all 8 chunk md5s matched his table, body.b64 = 13,212 chars, decoded 9,907 B, md5 425d925a88ab474ec2396cbea25e665c, sha256 40831cdebcc71de21cd536fa67f0e29873877864c78f455acfe4776708f46045 (Carter ran the recipe on the NCSU node); after banking, re-derived IDENTICALLY from the git object store, not the worktree.",
    "The 149-B reconciliation: normalize both bodies and diff returns ONLY boundary whitespace; per-class counts measured with tr -cd (asterisks 121 vs 45, backticks 74 vs 0, bullets 13 vs 12); 76+74+2 = 152 and 152-3 = 149 = 9907-9758. Arithmetic closes exactly.",
    "The 72-candidate formatting-strip sweep returned ZERO md5 matches and ZERO size-only matches; nothing reached 9,695 B. OUR hypothesis, refuted by our own test.",
    "guk fire gate 10/10 ALL CHECKS PASSED - run repeatedly across the arc, including before AND after the runbook GATE-1 note (the 6b card block md5 a8d18f664fbfd0d7e281ee05f3accd4c byte-identical before and after).",
    "Nothing running: bjobs = \"No unfinished job found\"; tracked tree clean; zero perimeter contact."
  ],
  "completed_this_session": {
    "quick-260805-o7o (10c14f2, 64f420a, dc4bbd2, fb839a4, ba1fa43)": "FINDINGS H + I — discharged \"Trusting any AFR fine-map result\". H: the panel keys chr:pos:ref:alt while harmonized AFR sumstats carry chr:pos, so the SNP_ID branch matched ZERO and 100% of the join fell to a CHR:POS first-hit with REF/ALT IGNORED. Now a two-4-key matcher (exact + transpose) with duplicate panel keys removed from the match TABLE before matching, so multiallelics are disambiguated BY CONSTRUCTION. ★ The scientific call was FLIP z, NOT DROP — both sides code on ALT (plink --keep-allele-order makes .bim A1 = ALT; harmonize_sumstats.py signs BETA to ALT), so a transposed pair is legitimate data with a bookkeeping defect. Reported BETA/SE NOT flipped. Palindromes DROPPED on a panel-specific ground: ld_npz_to_rds.R lifts GRCh38->GRCh37 carrying REF/ALT VERBATIM, never complementing, so a strand-inverted block is a detectable mismatch for ordinary variants but a SILENTLY SIGN-WRONG EXACT MATCH for palindromes — the only undetectable class. I: finemap_summary.tsv carried only the CONSTANT ld_dir, so no manuscript table could tell an AoU row from a 1kG row; 14 fields APPENDED, json_error dict kept key-for-key parity. Verifier 7/7; suite 584 -> 641.",
    "quick-260805-w7u (2563451, 1815bfd, 533c154)": "FINDING E — discharged \"Any GWAS x QTL colocalization\" AS WIRING (see inert_but_correct). qtl_coloc.smk had ZERO crosswalk/resolver references and fed coloc::runsusie the legacy 1kG panel while the GWAS fit used AoU. ★ Closing E ALONE would have traded \"silently wrong LD\" for \"silently NO LD\": run_qtl_coloc.R has its own key derivation, and an incompatible overlap exited 0 through FOUR STACKED LAYERS so Snakemake marked every such job DONE. Measured, not argued: coloc::check_dataset PASSES a sparse matrix because it never calls is.matrix(); coloc:::check_ld is what stops, inside a tryCatch that writes a status JSON and quits clean. NOW rc 1 with LD_JOIN_FATAL and no JSON. ⚠ Two orchestrator-brief errors corrected by the planner and re-derived twice: build_ld_rownames is a NO-OP on the AoU path (the .rds carries dimnames so the %||% chain short-circuits), while the legacy 1kG .rds sets dimnames NULL — WHICH IS EXACTLY WHY EUR COLOC WORKS TODAY. Verifier 8/8; suite 641 -> 745.",
    "quick-260806-b77 (9c0c67b, d8cfa53, 4c7bebe, 13b82ef, 2a7c1b6)": "FINDINGS G, J, L, M closed; K deferred as K-1. G: build_ld_rds_aou_eur was RETIRED and no rule outputs EUR_aou/*.rds, yet the TRANS chain still listed TRANS_aou_eur -> EUR_aou/{region_id}.rds FIRST, and strict_aou_only guarded with source.endswith(\"_aou\") which \"TRANS_aou_eur\" fails — so strict mode could not catch it even switched ON. Now token-based. ⚠⚠ TRANS_aou_eur was deliberately KEPT: deleting the orphan leaves TRANS with NO AoU entry and makes strict_aou_only STRUCTURALLY UNABLE to ever flag it — deletion DEEPENS the silence. Config edit is COMMENT-ONLY, proven by parsed-YAML equality. J: the receipt printed \"ld_matrix None\" on the early-exit paths, indistinguishable from a regression, and fires on real inputs (HLA_6p21, PYHIN1_1q23). L: the committed crosswalk was produced by no rule and read by no test; ⚠ the remedy is deliberately NOT a Snakemake rule (the crosswalk is read at MODULE SCOPE to build the DAG — chicken-and-egg), so it is a committed-vs-rebuild drift test plus a coverage-aware WARN. M: allow-list contained instead of deny-listing unmapped. Verifier 19/19 (it reverted three sources LIVE to watch tests go RED); suite 745 -> 807.",
    "quick-260806-pd3 (bf04199, 9538af0, 672ee27)": "FINDING K — closed the last PARTIAL gate row, \"Publishing the panel provenance\". Deleted ONE line (`variant_catalog_fallback <- TRUE`) from the Path-2 brace block of the frozen run_susie_rss.R, restoring the key's legacy meaning (the Path-1 AFR empty-subset revert). STRENGTHENED the pre-existing test that mandated that exact line, from `in branch` to `not in branch`. Re-pinned the whole freeze cascade at bf04199. ⚠ The b77 decoder ring BROKE under the change and was fixed in the same window: a real Path-2 revert emits false+true, which the old decoder rendered `none` — FALSE, Path 2 DID fire — and now renders the new `path2_ld_overlap_zero_RETRY`, deliberately NOT a prefix of `..._NO_NUMERIC_CAUSE`. ⚠ The census was corrected TWICE more: the executor found results/legacy/region_analysis is a SYMLINK that `grep -r` will not follow (so the planning-time 44 was wrong), and the verifier then found the replacement 1,944 was scoped too wide (35 non-region files). Correct = 1,909 carrying the key, 1,900 false, 9 true, all 9 genuine AFR Path-1 reverts; reconciles 1,909+687=2,596. No number moves, no science changes, Track A untouched.",
    "quick-260806-sr4 (98e0ee9, 656529a, c04e672, 5f0520b)": "RESCOPED the source freeze from BYTES to CODE and CLOSED K-3. Triggered by Carter's question \"why is anything frozen?\". Built tests/m3/source_freeze.py — a comment-insensitive, string-literal-aware code pin. THREE DISCOVERIES worth more than the change itself: (1) bf16289, the SHA the handoff claimed pinned 7 files, has ZERO enforcement anywhere and 5 of those 8 files had MOVED — retracted above; (2) the repo already held NINE ad-hoc comment-strippers, two in the very file being edited, so this is a consolidation, not an invention; (3) the plan-checker's multi-line fixture exposed a REAL BUG in the masker — masking the newline inside a string dropped every subsequent line, and `len(masked) == len(text)` is STRUCTURALLY BLIND to it, so the length invariant alone would never have caught it. The plan took THREE adversarial rounds (two full check cycles plus an orchestrator constrained-fix pass landing 13 more fixes); the checker's own fixes twice introduced new defects, which is why the last pass forbade adding anything."
  },
  "executor_deviations_accepted": [
    "THE PLAN WAS FACTUALLY WRONG TWICE, both blocking, both fixed: (1) envs/m3-r-ld.yml does NOT carry pandas (the plan asserted it does; ModuleNotFoundError verified in the built env) — added pandas>=2.2 THERE rather than pyliftover to python_stats.yml, because python_stats.yml backs a dozen already-built rules and re-hashing it forces a pipeline-wide rebuild; (2) src/snakemake/schemas/pipeline.schema.yaml ends in additionalProperties:false and the plan never mentions the schema, so adding occlusion_lockstep: would have broken EVERY Snakemake invocation at validate(). Schema entry added, following the ld_panel precedent already in that file.",
    "ONE EXISTING TEST EDITED — test_finemap_loader_contract.py::test_production_boundary_documented. It asserted 'SUPERSEDED-PENDING-REPLAN' in smk; m3-04b makes that token's ABSENCE an acceptance criterion, so no implementation satisfied both. REPLACED, NOT RELAXED: all original assertions retained, plus three NEW ones (m3-04b, m3-04c, occlusion_lockstep must be named). I READ THE DIFF AND CONFIRMED THIS. The token pinned mutable project state, not a contract, and the replan landed. Precedent 1a9d170.",
    "test_params_region_id_is_untouched PASSED AT BIRTH and never failed — correctly labelled a GUARD RAIL for m3-04c, not a RED, in both the commit and the SUMMARY. Honest RED accounting: T1 10/10 genuine, T2 13/14 genuine.",
    "The full-workflow --dry-run --quiet acceptance criterion is UNSATISFIABLE and always was: data/processed/ld_reference/ does not exist, so resolve_ld_path raises 'No LD panel found for FTO_16q12 AFR'. Proven PRE-EXISTING by running git show HEAD:Snakefile at 3e7a01a (identical error, identical ld_panel.py:94). Substitute evidence accepted: both new targets dry-run to exit 0 with no cycle, and both resolvers were exercised against the real config (3 AFR repointed, 5 EUR+TRANS byte-identical).",
    "bgzip: Snakemake built NO conda env this session (dry-run doesn't). The unit test uses a PATH-installed shim since htslib is not in smoke_dev — stated in the suite docstring, chosen over a skip. Proved out of band with bgzip (htslib) 1.21: output is genuine BGZF (BC\\x02\\x00), tabix -f -S 1 -s 1 -b 2 -e 2 indexes it, and a range query round-trips real rows."
  ],
  "gates": {
    "osf_pre_registration": "⚠ OVERRIDDEN 2026-08-03 by Carter for the Check-2 redefinition, logged as a DISCLOSED deviation (3e7a01a, .planning/osf_deviations.md). The prior occlusion-exclude amendment-update (GUID trsx5, posted 2026-07-10) remains CLEARED and unaffected. TWO OBLIGATIONS SURVIVE: draft+post the Check-2 amendment-update; and until it exists, NO redefined Check 2 result may be cited as 'passed' anywhere. ⟶ 2026-08-19 (quick-260819-u8d): clause-(d) RECALIBRATION ADOPTED (DEC-2026-08-19-occlusion-recalibration-adopted; Carter 21:45 EDT, verbatim \"adopt\"). NEW OBLIGATION, now open: the recalibrated occlusion-gate amendment-update posts to az52u as a NEW supplementary file / new dated record in the chain — agent DRAFTS, Carter POSTS, NEVER a silent swap (trsx5 must still show exactly 1 revision after posting, the same append-only check that cleared the July record). The POSTED trsx5 body carries a FALSE factual sentence at :45 (\"Region 1 alone contains 7 distinct overlapping deletions\") — the NaN-pair-implicated subset written as the window inventory; the measured window inventory is 7,951 multi-base-REF rows in 102,421 records — and its correction is REQUIRED regardless of branch. The draft at .planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-XX.md is UNINSTANTIATED until PENDING PASTE #3 lands, enforced (not merely intended) by `bash .planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh paste-ready <file>`, which is RED today BY DESIGN. This is a SECOND open OSF obligation ALONGSIDE the still-open Check-2 amendment-update — neither discharges the other. ⟶ 2026-08-21 (quick 260821-jcs + close 16:25 EDT): the recalibration amendment is INSTANTIATED, Seth-cleared (no blocking objection), RE-CONFIRMED AT POSTING by the engine (PRE_EXECUTE_COMMIT d45db42), guard all GREEN, and Carter was CLEARED TO UPLOAD the extracted body (22,945 B / 422f1f28d6a3b76c7657fadec05a0237) as a NEW file on az52u at 15:00 EDT. POSTING UNCONFIRMED at close (no captures received). THIS GATE DOES NOT MOVE until the four captures arrive and the record quick task appends the deviation entry + DEC-2026-08-21-occlusion-recalibration-posted. Until then: no gate-constant change in code; no recalibrated-gate result may be cited. ⟶ 2026-08-22 02:55 UTC: POSTING_DATE moved to 2026-08-22 by the engine (upload had not happened before the UTC day rolled); current body 22,945 B / 13a49f543cabcc27ce9f1e589783c060 (Seth-verified body + Date line only). Still NOT posted; gate unchanged. ⟶ 2026-08-22 02:58:55Z: recalibration amendment POSTED as NEW file mk7ze (https://osf.io/mk7ze) on az52u; trsx5 still 1 revision; OSF-stored md5 13a49f543cabcc27ce9f1e589783c060 == repo paste block; deviation entry appended; DEC-2026-08-22-occlusion-recalibration-posted; tag AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22. THIS OBLIGATION IS CLEARED. The separate Check-2 amendment-update obligation (2026-08-03 override) REMAINS OPEN and is unaffected.",
    "m3_07": "✅ CODE-COMPLETE (07a+07b+07c). Unchanged this session.",
    "m3_04b": "✅ COMPLETE 2026-08-03. Both tasks landed; tests/m3 444P/31S/0F independently re-verified by the orchestrator. ⚠ CORRECTED 2026-08-12 (quick-260812-09a, C-HIGH): this field previously ended with the clause 'all 7 pinned files 0-line diff', which is the RETRACTED claim. It is STRICKEN here rather than left standing, because this is a LIVE status field and not a dated historical block — correcting it is not history-rewriting. See verified_this_session_firsthand[5] for the retraction and DECISIONS.md:1474 for the do-not-repeat rule; DEC-2026-08-11-sr4-disposition then answered SR4-OPEN as NEVER-FROZEN for all five drifted files. bf16289 appears in ZERO places across src/, tests/, config/ and Snakefile — nothing ever enforced it.",
    "m3_04c": "⚠ STALE 2026-08-12 — superseded by HANDOFF `suite_baselines` (902/31/0, 2026-08-07) and by the corrected 260811-rcw PRE-FIRE GATE REVIEW §2/§2.1(2). The 548P/31S/0F figure below is a 2026-08-05 SNAPSHOT and must not be quoted as current. Body preserved verbatim: ◐ EXECUTED (Tasks 1a/1b/1c/2, 14 commits) and SOUND IN DIRECTION, but BLAST-RADIUS SWEPT 2026-08-05 -> 4 BLOCKERS OPEN. See m3-04c-BLAST-RADIUS.md §gate binding. Task 3 NOT started (terminal Carter gate). Suite 548P/31S/0F, independently re-verified — and every blocker is invisible to it.",
    "panel_reachability": "⚠ STALE 2026-08-12 — superseded by the corrected 260811-rcw PRE-FIRE GATE REVIEW §2 (Layer A crosswalk PRESENT AND CORRECT, L-18; Layer B `--ld-file` read path IMPLEMENTED AND ENFORCED, L-11/L-12/L-13/L-14) and §2.1(1). Body preserved verbatim: ⛔ OPEN AND DEEPER THAN DIAGNOSED. The 2026-08-03 finding (REGION_SAFE_TO_ID has no curated->M2 crosswalk) is REAL and the crosswalk is still NECESSARY — verified: my independent reimplementation reproduced the plan's 12-row oracle EXACTLY, incl. SH2B3_12q24 -> m2_region_00040__sub00 from 18 containing candidates. But it is NOT SUFFICIENT: input.ld_matrix is never referenced in the shell block, so run_susie_rss.R reconstructs file.path(ld_dir, 'AFR', region_id.rds) and can never reach AFR_aou/, falling silently to identity. A THIRD change is required that m3-04c does not contain. CARTER'S DECISION.",
    "aou_loop_refire": "⚠ STALE 2026-08-12 — superseded by the corrected 260811-rcw PRE-FIRE GATE REVIEW §2/§5: panel reachability is no longer a blocker; what remains is Carter's PRE-FIRE 1/1b/2/3 sequence and the STEP A region-1 gate. Body preserved verbatim: ⛔ BLOCKED on panel reachability (new), the stale gs:// panel-TSV rotate, the manifest-egress gap, the real-.bim validation, and a region-1 re-run. COSTS MONEY (~11 days, ~$385-1,084). CARTER'S TRIGGER.",
    "validation_4check": "⛔ ALL FOUR validation/check_*/ dirs EMPTY — the protocol has NEVER been run, and AOU-LD-PIPELINE.md §9 calls it 'a hard gate for promoting the pipeline from dev to production'. Check 2 REDEFINED (2a code-path equivalence vs plink on public 1000G; 2b AFR-vs-1000G REPORTED NOT THRESHOLDED; 2c EUR_ukbb_pub vs 1000G EUR at r>=0.90) under the operator override.",
    "m3_06": "HELD. condition_ld_matrix.py FROZEN, never fed to a fit. NaN→0 DEAD. m3-04b imports neither condition_ld_matrix nor nan_to_num — verified.",
    "blocker1_ld_read_path": "⚠ STALE 2026-08-12 — superseded by the corrected 260811-rcw PRE-FIRE GATE REVIEW §2.1(1): 'NOT YET IMPLEMENTED' is FALSE today, corroborated by measurement (L-11/L-12 present, L-13 8P/0S, L-14 22P/0S). Body preserved verbatim: ✅ DECIDED 2026-08-05, ⛔ NOT YET IMPLEMENTED. Remedy = thread {input.ld_matrix} into run_susie_rss.R behind a new --ld-file (fallback to the existing --ld-dir rebuild retained so no caller breaks). Re-verified firsthand: run_finemap's shell: passes ONLY --ld-dir {params.ld_dir} and --region {params.region_id}; repo-wide the sole shell: consuming {input.ld_matrix} is qtl_coloc.smk:316 (a DIFFERENT rule); run_susie_rss.R:125-127 rebuilds file.path(ld_dir, ancestry, region_id.rds) with ancestry==\"AFR\", never AFR_aou, and falls SILENTLY to an identity matrix on a miss (:472-474). ACCEPTANCE TEST: prove resolved == what-the-script-opens (grep the rule's shell: for {input.ld_matrix}, then assert the R script opens that exact path). A green DAG is NOT evidence.",
    "blocker4_partial_rollup": "✅ CLOSED 2026-08-05 (fac9a93). Excludelist-only orphan regions now RAISE instead of being silently discarded under a clean stage_a_manifest stamp; explicit allow_partial_manifest opt-in. ⚠ n_regions == 276 is a FALSE INVARIANT and is deliberately NOT asserted — both the excludelist write (run_native_ld_panel.py:806-808) and the manifest append (:821-824) sit inside `if occluded_ids:`, so a zero-occlusion region legitimately writes NEITHER artifact. Coverage is REPORTED, never asserted. ⚠ config/ld_regions.tsv is 552 data rows = 276 unique region_id x 2 ancestries.",
    "kn_publication_blockers": "✅ CLOSED 2026-08-05 (3bb8783 + bf963df + fac9a93): D-04b-01 (float POS), HIGH-0 (guard could not fire), HIGH-4 (no unparseable counter), LOW-1 (stroke double-count, VISIBILITY ONLY — de-duplicating would move k AND redefine n 9->8, i.e. move a pre-registered number; the denominator fork is logged to deferred-items.md for Carter, NOT taken). Measured on the real corpus: 7 of 9, bmi now present.",
    "track_a_eur_numerics": "✅ CLOSED 2026-08-05 (aeed8c0 + 51a60ca + ab19186), END-TO-END. ld_read_path.ancestries allow-lists BOTH the crosswalk and --ld-file. Off the list the declared file is INVISIBLE, so EUR/TRANS read exactly what they read at 3f431ab — proven by identical() on the ENTIRE matrix object, plus permanent in-suite negative controls that recover the pre-change loader via `git show 5ec33bd:` and assert the DEFECTIVE outcome. Kill switch: ld_read_path.ancestries: [].",
    "blocker_a_ld_file_authoritative": "✅ CLOSED 2026-08-05 (51a60ca + ab19186). Declared panel is the SOLE candidate when authoritative; hard stop() on unreadable/use_identity/below-min-use; best_partial keeps the BEST overlap not the LAST. Tests run at REAL config/susie_policy.yaml thresholds with a guard asserting they are the production values.",
    "blocker_c_nothing_builds_the_rds": "✅ CLOSED 2026-08-05 (6643c19) as ONE OPERATOR COMMAND: `snakemake m3_convert_aou_afr_rds_all` (276 ids derived from the manifest, 575-job DAG). ⚠ DISCLOSED RESIDUAL: `snakemake all` is still NOT self-sufficient — resolve_ld_path returns the first EXISTING path, unchanged and out of scope.",
    "blocker_d_converter_oom": "◐ PARTIAL 2026-08-05 (57b381f). Dense `ld` dropped from the .rds; read block-bounded (0.351x maxRSS on real banded panels; 0.809x on a dense one — shape-dependent); mem_mb 64000; ceiling m3_convert_max_n_var=120000 fails fast. ⛔ NOT closed for MC4R / FTO / HLA: the .npz ld key is dense float32 from the FROZEN plink_ld_to_npz.py:253 (~553 GB at n≈372k). Needs a sparse .npz = a producer-side change on a frozen file.",
    "blast_radius_gate_ledger": {
      "nothing_already_merged": "CLEAR",
      "re-running any EUR fit / rebuilding EUR_ukbb_pub": "CLEAR (BLOCKER-B, quick-260805-23d)",
      "the ~11-day billed fire": "CLEAR on A/B/C; ⚠ BLOCKER-D PARTIAL — only SH2B3 __sub14 (22.8 GB) convertible",
      "trusting any AFR fine-map result": "CLEAR (H+I, quick-260805-o7o, verifier 7/7)",
      "any GWAS x QTL colocalization": "CLEAR AS WIRING (E, quick-260805-w7u, verifier 8/8) — see inert_but_correct",
      "any TRANS fit": "CLEAR (G, quick-260806-b77, verifier 19/19) — see inert_but_correct",
      "publishing the panel provenance": "⚠ PARTIAL — I and J closed; K DEFERRED as K-1 (needs TWO Carter authorizations)",
      "growing the curated region set": "CLEAR (L+M, quick-260806-b77)"
    },
    "trsx5_posted_body": "⚠ BYTE-LEVEL-CONTESTED 2026-08-13: our 2026-07-15 verification was PAGE-LEVEL (revision count) only; Seth reports the posted body 212 bytes short. Adjudication = Carter authenticated download, SIZE-FIRST: any size other than 9,758 or 9,907 bytes is a STOP BY ITSELF (truncated or otherwise anomalous; no hash comparison is required and none may overrule it). 9,758 B + md5 28ecdb3160833da80cfa25952f76415b = the repo-canonical paste block = PASS, proceed; same size with a different md5 = STOP, its own anomaly. 9,907 B + md5 425d925a88ab474ec2396cbea25e665c = the Seth-complete lineage = STOP-reconcile (we do NOT hold that body). ADVISORY ONLY, never an adjudication anchor: the truncated body's md5 is reported as c19be8b2ad7cd6a45fee1d668d8a9cf9 by Seth, read from the OSF API and unverified by us (sign-in wall). GATES THE FIRE (step 6b) AND obligation-(2) posting. Do not annotate the ledger either way until adjudicated. ⚠ CORRECTED 2026-08-14 (quick-260814-guk): this field previously carried a 31-character transcription of the short-body md5 - an md5 is 32 characters, so that comparison could never fire the STOP-truncated branch. The card is now SIZE-FIRST at every executable site. ✅ RESOLVED 2026-08-17 (quick-260817-vbu): the posted 9,695-B body is a BYTE-EXACT plain-text rendering of the COMPLETE 9,907-B lineage — not a truncation and not a third body. Seth published the 6-step transform first (strip bold/italic/backticks/bullets + blank-line re-flow + no trailing newline; 120 of 121 asterisks, 74 backticks, 13 bullets = -220 B, +8 B of inserted blank lines = net -212 = 9,907 - 9,695). WE REPLICATED IT FIRSTHAND from the git OBJECT STORE at 3684413 (source 9,907 B / 425d925a88ab474ec2396cbea25e665c), implemented from his prose spec alone, run ONCE, no fitting: output 9,695 B / c19be8b2ad7cd6a45fee1d668d8a9cf9 / sha256 1ba83e4e633cd11c7f0896969cd6a419dada7b442365a5a0a3ea7307a342aab4 — all three MATCH, every intermediate count matches his accounting. The target md5 is ALSO Carter's own firsthand STEP-6b OSF download measurement (2026-08-16), so the chain closes on a measurement we made ourselves. CONSEQUENCE: all three bodies carry the same pre-registration prose; the 212-B and 149-B deltas are pure markup; the public record is substantively correct and complete. GATE RELEASED BY CARTER ON SUBSTANCE (DEC-2026-08-17-trsx5-gate-released, 22:32 EDT). Re-post NOT taken — optional legibility only, per Seth's withdrawn 're-post required'. The 2026-08-13 BYTE-LEVEL-CONTESTED and 2026-08-14 CORRECTED readings above are RETAINED DELIBERATELY: they were the honest state of knowledge at their dates.",
    "producer_pre_fire_gates": "✅ WIRED 2026-08-13 (quick-260813-t21, d9fbc63, verifier 8/8): clause-(d) anomaly gate + --max-n-var ceiling (120000, YAML-pinned to m3_convert_max_n_var). deferred_* statuses are the gates working. ⟶ 2026-08-18 (quick-260818-uoi): the A-12 MAF-depression follow-up is REDIRECTED — the cross-cohort (panel_maf, sumstats_maf) join is NOT to be built (Seth 2026-08-18); the registered replacement is MISS-1 in .planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md (within-panel F_MISS test, post-fire). check_maf_depression stays implemented-but-unwired. ⟶ 2026-08-19 (quick-260819-u8d): the shipped clause-(d) gate constant _OCCLUSION_ANOMALY_FRACTION = 0.0005 is ROW BASIS and its calibration premise is DEAD — 21/21 pre-committed sampled regions defer (row-basis occlusion min 0.1323%, median 0.1888%, max 0.3527%), so as pre-registered the fire banks zero. It is NOT to be edited in code until the recalibrated amendment is POSTED: silent modification is excluded by the posted record's :81, by :98, and by the shipped source's own comment at run_native_ld_panel.py:129-130. The recalibrated gate will count occluded SITES (representation-invariant) with ROWS reported alongside, ceiling = 3x the measured site-basis median (instantiated once from PENDING PASTE #3). Stage A stays UNREACHABLE.",
    "stage_a": "✅ PASSED 2026-08-24 ~20:5x EDT. fire_verifier stage-a: 6/6 PASS, exit 0, report /home/jupyter/fire_gate_stageA.json. Region 1 banked (gs://…/ld/AFR_aou/m2_region_00001.npz). The gate was run with --gate-json + --excludelist and NO --expected-records (it derives 231 from the excludelist and cross-checks the sidecar). Stage B is the next gate and needs Carter's explicit go.",
    "stage_b": "⛔ HALTED 2026-08-24 ~23:10 EDT at m2_region_00057 (region 4 of 4) on the pre-registered raw-panel NaN-raise contract + --fail-fast. 3 banked ok. STEP 9-GATE NOT run — it would score an incomplete, error-bearing batch. Gate does not move until the boundary-adjacent pairwise-NaN prevalence is MEASURED and adjudicated. Record: .planning/debug/260824-STAGE-B-HALT-region57-boundary-adjacent-pairwise-NaN.md",
    "pairwise_complete_premise": "✅ CONFIRMED 2026-08-26 ~01:48Z by the three-run plink falsifier (STEP 1). VERDICT PAIRWISE-COMPLETE; mean-imputation, listwise-over-the-window and mis-selected-Z all refuted on specific cells. Evidence: .planning/debug/260826-STEP0-1-falsifier-PAIRWISE-COMPLETE-as-received.md. Limitation recorded: a falsifier only rules out the hypotheses it enumerates, and the ±200bp flank held exactly ONE Z candidate (cleared at 0.9724, no fallback) — widen the flank if reused."
  },
  "cluster": "AoU_Jupyter_ComputeEngine_20260626b STOPPED since the site-basis sweep (2026-08-20 ~20:11 EDT). Disk persists bfile, repo clone, occ_measure/ (both sweep TSVs; both banked in-repo), step7_vv.txt, region1_window.bim, the failed-notebook record. NEVER DELETE (Standard-disk semantics). Next start = the fire itself, after posting + remediation.",
  "data_state": "UNCHANGED this session — no perimeter contact. AoU AFR native-plink LD panel does NOT exist: bucket .npz = 0/276. data/processed/ld_reference/ does not exist AT ALL on NC-State, so resolve_ld_path returning \"the first EXISTING path\" currently has nothing to return — which is why several of this session's fixes are correct-but-inert wiring.",
  "do_not": [
    "AN AGENT NEVER FIRES, STARTS, STOPS OR DELETES ANY CLOUD RESOURCE — including bucket objects. Carter does all of it.",
    "DELETE Dataproc clusters when done; STOP analysis VMs, never delete them while they hold artifacts.",
    "Do NOT delete bucket data before the inventory record's §5 decisions AND a §6 dry-run that reconciles exactly.",
    "Do NOT change the Stage C NaN posture in code before the decision; do NOT extend the occlusion predicate to -1.",
    "Do NOT write that clustering EXPLAINS Finding 2 — deff 1.360 vs 1.99 is a failure to exclude.",
    "Do NOT quote the superseded Rao-Scott figures (1.652/0.0366, 1.564/0.0556, 1.969) as current.",
    "Do NOT trust gsutil/gcloud EXIT 0 on empty output; use PIPESTATUS[0] after pipes; sum large byte counts with %.0f.",
    "No OSF contact, no Seth contact, by any agent. Never edit the posted amendments in-repo.",
    "NEVER git add -A / git add . on this GPFS tree — explicit paths only. No worktree isolation."
  ],
  "prep_landmines": [
    "⚠ NEW 2026-08-04 — .planning/STATE.md's YAML FRONTMATTER DOES NOT PARSE, and has not for a long time. yaml.safe_load fails at line 17 (the `last_activity` scalar) because a 2026-06 entry embeds UNESCAPED DOUBLE QUOTES inside the double-quoted string: `_normalize_bucket(os.environ[\"WORKSPACE_BUCKET\"])`. VERIFIED PRE-EXISTING — `git show HEAD:.planning/STATE.md` fails at the SAME line, so this session's edits neither caused nor worsened it. Nothing has broken because the resume workflow reads the BODY, not the frontmatter. DELIBERATELY NOT FIXED at session close: the repair means rewriting a ~34 KB historical scalar, which is not a freeze-moment edit. Fix it in a dedicated /gsd-quick (switch last_activity to a YAML block scalar `|-`, or single-quote it) if any tool ever needs to parse it.",
    "GSD tooling does NOT recognize the custom m3-NN naming: `init plan-phase m3-04` returns phase_found:false (I re-verified today). Execution = spawn gsd-planner / gsd-executor DIRECTLY on the PLAN.md (the adapted planner->executor pattern).",
    "envs/m3-r-ld.yml now carries pandas>=2.2 (added by m3-04b; it did NOT before). A fresh env build needs this.",
    "src/snakemake/schemas/pipeline.schema.yaml is additionalProperties:false — ANY new top-level pipeline.yaml block MUST get a schema entry or every Snakemake invocation dies at validate().",
    "bgzip is NOT on the login PATH and NOT in smoke_dev; it lives in envs/python_stats.yml.",
    "The m3-r-ld conda env at /rs1/researchers/c/ckclinto/conda_envs/m3-r-ld is the marker that makes the A6 stitch/loader tests RUN rather than skip. Do not delete it.",
    "tests/m3/sparse_parent_benchmark.tsv is REWRITTEN by every suite run (timing columns only). Expect it dirty after any pytest tests/m3; it is not a real change.",
    "⚠ NEW 2026-08-24 — Cloud Analysis VM env: (a) the producer's argv names the literal `plink1.9` (aou_ld_panel.build_plink_ld_command) but the VM image ships only `plink` (/opt/workbench-tools) → Stage A stopped on Errno 2 before any banking. FIX ON EVERY FRESH VM: install the PINNED PLINK v1.90b7.2 (plink_linux_x86_64_20231211.zip) into ~/bin/plink1.9 and export PATH=\"$HOME/bin:$PATH\" in the fire shell; STEP 3 now requires `plink1.9 --version` to print that line; never shim a PLINK 2.x binary. (b) /home/jupyter/native_ld_scratch/m3-W2-native-plink-panel.tsv can be a stale June-era 7-column leftover the bucket check cannot see; the producer fail-closes on it → STEP 4b checks the local mirror's header and ROTATES (never deletes) a stale one. Record: .planning/debug/260824-STAGE-A-env-stop-plink1.9-and-stale-scratch-TSV.md"
  ],
  "gpfs_object_store_recovery_recipe": "Did NOT fire this session (4 commits + 3 pushes clean). Prior: 3 hits in ~3 weeks. Recipe if 'invalid object / Error building trees': for each `git ls-files -s` entry test `git cat-file -e <sha>`; for misses re-hash the intact working-tree file via `git hash-object -w <path>`; then commit AND PUSH (local store unreliable).",
  "resume_entry_point_2026_08_04": ".planning/phases/m3-aou-afr-ld-panel-build/m3-04b-BLAST-RADIUS.md — READ THIS FIRST. It carries the ship/no-ship verdict, the two blockers, the gate binding table (which gate each issue actually blocks), and the revised 7-step sequence. Then this HANDOFF's headline_2026_08_04 + resume_on_reconnect #0/#0b/#0c. The m3-04c PLAN is still on disk but is HALTED and must not be executed as written.",
  "headline_2026_08_05": "★ BLOCKER-1 IS DECIDED. Carter chose remedy A: thread {input.ld_matrix} into run_susie_rss.R behind a new --ld-file argument, making resolve_ld_path the SINGLE SOURCE OF TRUTH and killing the declare-vs-read split permanently. The promote/symlink rule and the per-ancestry ld_dir were both REJECTED (each leaves a second place the AFR->AFR_aou mapping can drift — the exact failure mode that produced the blocker). Recorded as DEC-2026-08-05-m3-ld-read-path in .planning/DECISIONS.md with rejected alternatives + the acceptance test. ⇒ m3-04c no longer needs a DECISION, it needs a REPLAN. ALSO LANDED: quick-260804-rtc, all four autonomous $0 fixes (3bb8783 D-04b-01 shared integral-position coercion used by all THREE key implementations; bf963df HIGH-4 unparseable counters + HIGH-0 a total-miss guard that can actually fire; fac9a93 BLOCKER-4 excludelist-only orphans now RAISE + LOW-1 visibility-only; d12a6c7 docs). tests/m3 444P/31S/0F -> 496 passed / 31 skipped / 0 failed, independently re-run by the orchestrator (three agreeing measurements). All five touched test files PURELY ADDITIVE (0 deletions) — no existing test weakened. Four frozen contracts + finemap.smk 0-line diff. REAL-DATA PROOF over 151,223,963 rows: rs182965575 present in 7 of 9 with bmi (the 100%-float-POS file that previously scored ABSENT) now counted — that presence IS the D-04b-01 proof. No pre-registered number moved. ⛔ THE FIRE IS STILL BLOCKED: BLOCKER-1 is DECIDED but NOT IMPLEMENTED.",
  "resume_entry_point_2026_08_05": ".planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md — the 2026-08-05 top block. Then .planning/DECISIONS.md DEC-2026-08-05-m3-ld-read-path (the locked BLOCKER-1 remedy + its acceptance test), then m3-04b-BLAST-RADIUS.md §4 for the remaining sequence.",
  "headline_2026_08_05b": "⚠⚠ THE REPLAN CAUGHT A SILENT WRONG-LOCUS DEFECT IN THE CROSSWALK — the single most consequential finding of the session. m3-04c selected an M2 region by smallest CONTAINING span using start_grch37/end_grch37 from config/ld_regions.tsv. But for a SPLIT parent those two columns hold the PARENT bounding box copied VERBATIM into every subregion row (build_ld_region_manifest.py:585-587,650-653) — only the *_grch38 columns vary per subregion. So all 18 subregions of m2_region_00040 tie EXACTLY and the lexicographic tie-break returns __sub00, whose real GRCh37 window is 37,857,542-45,792,298: ZERO bp overlap with SH2B3 and ~66 Mb away. The plan would have fine-mapped SH2B3_12q24 — THE TRACK A ANCHOR LOCUS — against LD from an unrelated window. It survived a \"12/12 independently reproduced\" oracle because that reproduction re-implemented the SAME mechanism: convergent reproduction of a bug, NOT verification. FIXED: candidates are now lifted GRCh38->GRCh37 via hg38ToHg19.over.chain.gz (the ONLY chain in the repo) and ranked by CORE OVERLAP first. Oracle re-derived and INDEPENDENTLY re-implemented by the orchestrator: EXACTLY 1 of 12 moved (SH2B3 __sub00 -> __sub14, core overlap 523,169 bp vs 76,831 for __sub15), 11 unchanged, BMI_Xq24 still unmapped, 0 window-liftover failures across all 276 AFR rows. ⚠ The mechanism was structurally broken for ALL 8 split parents (123 of 276 AFR regions) — only SH2B3 hits it today, so the fix is to the MECHANISM, not a special case. A SECOND self-contradicting test was also found and proved (test_ld_panel_resolver.py:364 — [^)]* cannot cross the paren CURATED_TO_M2.get() introduces), and the first proposed fix for it would have made the assertion VACUOUS (a non-greedy regex matches from the module docstring at finemap.smk:9 and can never fail); replaced with a balanced-paren call-site extractor + a PERMANENT negative-control test, both verified by simulation to genuinely FAIL when a kwarg is stripped.",
  "headline_2026_08_05c": "⛔⛔ DO NOT FIRE, AND DO NOT REBUILD EUR_ukbb_pub. m3-04c Tasks 1a/1b/1c/2 EXECUTED and are directionally right — the AoU panel IS readable when the quality gate passes (proven end-to-end) — but a 4-agent blast-radius sweep (m3-04c-BLAST-RADIUS.md) found FOUR blockers, ALL invisible to the green 548P/31S/0F suite. (A) --ld-file is a PREFERENCE, NOT A MANDATE at production thresholds: run_susie_rss.R:216 only RETURNS a candidate that passes overlap>=50 && coverage>=0.5, else it falls through to the --ld-dir 1kG panel and reports ld_loaded;overlap_ok — BLOCKER-1s exact defect class, 60 lines down. ⚠ ALL 8 tests in test_ld_read_path.py pin MIN_LD_OVERLAP<-1L/MIN_LD_COVERAGE<-0.0, i.e. the gate is DISABLED in every one, and the orchestrators own acceptance proof did the same — so nothing in the repo can observe production behaviour. (B) ★ TRACK-A EUR NUMERICS MOVE. BLOCKER-1 was SILENTLY PINNING every EUR fit to the 1kG tail; Task 1b un-pins EUR too, not just AFR. PROVEN: EUR r[1,2] 0.1->0.9, credible sets 3->10, nonzero PIPs 200->78, while ld_status AND ld_overlap_fraction stayed BYTE-IDENTICAL — the two fields anyone would check. EUR is safe TODAY only because data/processed/ld_reference/ does not exist; building EUR_ukbb_pub (a $0 roadmap prerequisite) triggers it. TRACK A IS IN SUBMISSION. (C) NOTHING BUILDS THE .rds: resolve_ld_path returns the first EXISTING path (ld_panel.py:87), m3_convert_npz_rds.smk has no aggregate target, ALL_TARGETS names no AFR_aou/*.rds — so post-fire `snakemake all` resolves AFR to 1kG SILENTLY. (D) the .npz->.rds consumer is UNBOUNDED-DENSE: ld_npz_to_rds.R:103 materialises n x n float64 under a declared mem_mb=8000 — 45.6 GB for SH2B3s subregion (the SMALLEST target) up to ~1.1 TB for FTO/HLA; same OOM class already fixed TWICE producer-side. ALSO HIGH: qtl_coloc reads the legacy 1kG panel while the GWAS fit it colocalizes used AoU (mixed LD inside one coloc.susie); the crosswalk has NO ancestry gate; retiring build_ld_rds_aou_eur orphaned the TRANS chain HEAD and strict_aou_only CANNOT catch it (TRANS_aou_eur does not endswith _aou); the sumstats<->panel join is ALLELE-BLIND. ✅ CORRECTION TO THE PRIOR FRAMING: the SH2B3 core straddle is a NON-RISK — the panel is built over the WINDOW (start_grch38 == window_start_grch38 for all 552 rows), overlap_frac is 1.000000, no variant is missing; and there is NO stitch to bypass. No revert warranted.",
  "headline_2026_08_05d_PRIOR": "★ BLAST-RADIUS REMEDIATION LANDED (quick-260805-23d, 6 tasks, commits aeed8c0/51a60ca/ab19186/6643c19/57b381f/cd177bc). BLOCKER-A, BLOCKER-B and BLOCKER-C are CLOSED; BLOCKER-D is PARTIAL by design. Suite 548 -> 584 passed / 31 skipped / 0 failed (615 collected). (A) the declared panel is now the SOLE candidate when authoritative, and assert_declared_ld_authoritative() HARD-STOPS on unreadable / use_identity / below-min-use instead of silently falling through to 1kG with a green receipt. Hard stop() chosen over a status field deliberately: a written JSON + rc 0 makes snakemake mark the job DONE, which is the exact failure class of the sweep (the resolved==opened receipt already existed and nobody read it); stop() makes the DAG the gate and --keep-going bounds it to one region. (B) ★ TRACK-A EUR IS PROTECTED, END-TO-END. A config allow-list (ld_read_path.ancestries, mirroring occlusion_lockstep.ancestries) gates BOTH the crosswalk and --ld-file; off the list the declared file is INVISIBLE (never even file.exists()-tested) so EUR/TRANS read exactly what they read at 3f431ab — proven with the ENTIRE MATRIX identical(), not just the path. (C) m3_convert_aou_afr_rds_all enumerates all 276 AFR ids DERIVED from config/ld_regions.tsv (153 whole + 123 __sub); dry-run resolves a clean 575-job DAG. (D) the dense `ld` field is GONE from the .rds and the read is block-bounded (maxRSS 0.351x on real banded panels), mem_mb 8000->64000, with an explicit ceiling m3_convert_max_n_var=120000 that fails fast in seconds. ⚠ D IS PARTIAL AND MUST NOT BE CLAIMED CLOSED for the large regions: the .npz ld key is dense float32 from the FROZEN plink_ld_to_npz.py:253, so one dense copy is unavoidable on read — 22.8 GB (SH2B3 __sub14), 67.3 GB (MC4R), ~553 GB (FTO/HLA). SH2B3 is feasible on a big-memory node; the large classes now fail fast instead of OOM-killing. A sparse .npz is a producer-side change on a frozen file.",
  "headline_PRIOR_2026_08_05": "★ m3-04b LANDED — the occlusion catalog assembler + the lockstep consume seam are REAL CODE and WIRED (d7dfa67 T1, 0cae502/37c51df T2, f038ce0 docs; all on origin). Full tests/m3 420P/31S/0F → 444P/31S/0F (+24, zero regressions) — I RE-RAN IT MYSELF at HEAD, 07:02, not taken on the executor's report. The four m3-07 modules that had ZERO production callers now have one. ⚠⚠ THE SESSION'S MOST IMPORTANT FINDING, from the m3-04 replan: THE AFR_aou PANEL IS UNREACHABLE FROM run_finemap — REGION_SAFE_TO_ID (Snakefile:47-63) is built ONLY from config/regions_curated.csv (12 curated slugs), NOT from config/region_id_mapping.tsv (276 M2 rows), so resolve_ld_path asks for AFR_aou/FTO_16q12.rds, MISSES, and SILENTLY falls through to AFR_1kg. Landing all 276 .rds would change NOTHING and nothing would warn you. ⇒ DO NOT FIRE until m3-04c Task 1 (panel reachability) lands. NEXT = m3-04c.",
  "resume_entry_point_PRIOR": ".planning/HANDOFF.json (this block) + .planning/STATE.md 2026-08-03 ★ RESUME HERE block + .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md + the m3-04b SUMMARY (m3-04b-W4-SUMMARY.md) + deferred-items.md + the m3-04c PLAN (the next thing to execute).",
  "analysis_change_to_disclose": "⚠⚠ FOR THE MANUSCRIPT AND OSF RECORD — STATE IT, do not let a reader find it by diffing versions: any AFR figure or table regenerated after 260805-o7o lands is NOT COMPARABLE to one produced before it. PIPs and credible sets MOVE (this is the correction the phase exists to make). Reported BETA and SE do NOT move, so no published direction of effect changes — verified: credible_sets carry the raw sumstats values with the allele-aware flag both on and off.",
  "inert_but_correct": [
    "FINDING E (w7u): the \"Any GWAS x QTL colocalization\" gate is safe TODAY only because ZERO AFR QTL-coloc jobs exist AT ALL — a PRE-EXISTING, unrelated condition (E-4: build_qtl_coloc_manifest.py:250 _ancestry_for_region returns \"EUR\" UNCONDITIONALLY, git-diff-identical at 7b1025d). What landed is correct, machine-verified WIRING — \"no longer wrong in principle\", NOT a production-exercised fix. Do NOT cite E as \"safe in production\".",
    "FINDING G (b77): makes TRANS's failure VISIBLE, not working. TRANS still resolves to the 1kG EUR panel today and after the fire; strict_aou_only:false keeps the strict half inert. L's WARN and M's allow-list are also inert on today's data (12/12 covered, 11/11 contained). The J receipt is the ONLY behavioural change b77 makes."
  ],
  "carter_decisions_outstanding": [
    "⭐ Stage C NaN error posture - courier options-draft v2 (57a3105) brief-blind with the drafted cover note, then DECIDE after the reviewer adjudicates. NOT CONFIRMED SENT as of 2026-09-18. This is the only thing blocking the 276-region panel fire.",
    "Posting decision on the tail disclosure (DRAFTED - NOT POSTED). The reviewer's closure is not authorization.",
    "COST-1: run Card 2 at the VM (per-region n_var census, then COST-1 on the real largest square-feasible region).",
    "Cost track: monthly cost DEPRIORITIZED by Carter 2026-09-17 ('the other apps are deleted, i dont care about monthly cost'). The storage-reclaim decisions in the inventory record §5 remain decided-but-unwritten; the AoU credit claim is MOOT (denied, final, DEC-2026-08-16-aou-credit-request-denied)."
  ],
  "authorizations_granted_do_not_relitigate": [
    "AUTH-o7o-01 (CARTER) — appended --ld-allele-aware to the EXPECTED_ADDED_TOKENS argv tripwire in tests/m3/test_ld_read_path_ancestry_gate.py. The widening was PAID FOR by a new direct assertion that the flag renders \"false\" for EUR/TRANS/EAS/HIS across all six degraded config shapes. The list got longer; the containment got STRICTER.",
    "AUTH-b77-01 (⚠ THE ORCHESTRATOR'S CALL, NOT CARTER'S — disclosed deliberately) — narrowed tests/m3/test_qtl_coloc_allele_join.py::test_params_region_id_is_not_declared_here from a whole-file finemap.smk pin against the FIXED SHA 7b1025d to `assert \"region_id\" not in diff_text`. Authorized under Carter's standing \"proceed autonomously\" after verifying FIRSTHAND that the 128-line diff contained ZERO region_id occurrences, params.region_id was byte-unchanged, and the PRIMARY guard rail (test_occlusion_lockstep_wiring.py::test_params_region_id_is_untouched) was GREEN and untouched. The narrowed form is STRICTLY STRONGER on the test's own stated subject. Negative control OBSERVED RED and permanent. If Carter disagrees it is a one-line revert — but note that reverting re-blocks finding J."
  ],
  "freeze_state": "⚠ RESCOPED 2026-08-06 by quick-260806-sr4 under AUTH-SR4-RESCOPE — THE FREEZE NOW PROTECTS CODE, NOT BYTES. run_susie_rss.R is CODE-frozen at bf04199, declared in exactly ONE place (R_CODE_REF in tests/m3/test_source_freeze_pins.py) and IMPORTED by both consumers, so a re-pinner updates one constant per frozen subject. Comments, docstrings, blank lines and trailing whitespace are now DELIBERATELY FREE; a CODE change still goes RED. Enforced by tests/m3/source_freeze.py (whole-file code-only FLOOR + symbol pins). ALSO NEWLY GATED FOR REAL: plink_ld_to_npz.py, condition_ld_matrix.py, occlusion_span_filter.py (the only three genuinely 0-diff vs bf16289). ⚠ NEVER RE-PIN a DIFFERENTIAL SUBSTRATE — every *_REF/*_REV/BASE_COMMIT/BASELINE_REV constant in tests/m3/ must now carry a `#:` bucket annotation from {CODE PIN | DIFFERENTIAL SUBSTRATE | HISTORICAL NARRATIVE}, and a permanent repo-wide gate enforces it. m3-06 stays HELD. Rationale + what the freeze deliberately does NOT cover: DEC-2026-08-06-sr4-freeze-scope.  ⚠ CORRECTED 2026-09-16 (quick-260916-vqp; the text above is RETAINED AS WRITTEN): 'Comments, docstrings, blank lines and trailing whitespace are now DELIBERATELY FREE' holds for run_susie_rss.R's CODE pin, but it was FALSE for the three files named in the very next sentence - plink_ld_to_npz.py, condition_ld_matrix.py and occlusion_span_filter.py were ALSO whole-file numstat-pinned against bf16289, so a committed docstring edit to any of them went RED. FIXED FOR condition_ld_matrix.py ONLY, since b6b1f70 (its whole-file assertion replaced by the existing code pin, proven able to fail): measured 2026-09-16 at 621701c, git diff --numstat bf16289 HEAD = 37 0 for that file and its pin is GREEN. plink_ld_to_npz.py and occlusion_span_filter.py KEEP the whole-file byte pin BY DECISION, so docstrings are NOT free for them. See DEC-2026-09-16-condition-ld-matrix-freeze-code-only.",
  "suite_baselines": {
    "tests/m3": "1229 passed / 33 skipped / 0 failed (1262 ids collected) at 621701c. Arrived at as 1187 -> 1212 (quick-260916-ocb, +25 by test id) -> 1229 (quick-260916-oyq, +17 by test id). ⛔ THE SUITE WAS NOT RE-RUN BY EITHER VERIFIER - do NOT write 'each re-run by an independent verifier'. In BOTH tasks the constraint forbade a full tests/m3 run, so a separate verifier INDEPENDENTLY RECONCILED THE RESULT BY TEST ID from the executor's own JUnit XML. 260916-ocb-VERIFICATION.md:147: 'the full-suite reconciliation was **checked from the executor's artefacts, not re-run** (the constraint forbids a full `tests/m3` run). I parsed `.../m3-{before,after}.xml` myself by node id' - before passed=1187 skipped=33, after passed=1212 skipped=33, added 25, removed 0, changed 0. 260916-oyq-VERIFICATION.md:33: 'I parsed the executor's JUnit files myself ... POST `suite_post.xml`: 1262 ids = **1229 passed / 33 skipped** ... **ADDED 17**, REMOVED 0, CHANGED 0.' quick-260916-vqp did not re-run it either (docs-only): the 1262 figure is cited straight from 260916-oyq-VERIFICATION.md:33. THE SKIPS MUST STAY AT 33. --- PRIOR ENTRY, RETAINED VERBATIM FOR ITS COMPONENT-EXACT RECONCILIATION AND THE 20 TEST NAMES: 1122 passed / 33 skipped / 0 failed (MEASURED 2026-08-28 at 1333f3f, 824.92 s; 1155 collected), and INDEPENDENTLY RE-RUN by gsd-verifier at 970.73 s with the same result. This SUPERSEDES the 1121 entry below by exactly +1: quick-260826-qq9 T4 added ONE test, test_driver_summaries_guard_independently_refuses_last_wins_with_both_upstream_layers_disabled, which reaches the driver's layer-3 `if region_id in summaries: raise` by monkeypatching the MODULE-GLOBAL _assert_unique_region_ids (neutralising layers 1 and 2 at once) and attributes the raise by the traceback's FINAL FRAME. NEGATIVE CONTROL OBSERVED THREE TIMES (orchestrator before and after the comment edits, then the verifier independently): deleting the branch makes it RED, and the POOLED denominator identity catches the duplication instead, reporting 4 summary rows against 8 emitted — the same 2x inflation that corrupted the 2026-08-26 sweep, in miniature. Scanner file 100 -> 101; non-scanner collect UNCHANGED at 1054. T4 also corrected a WRONG claim carried by T1-T3 in three places (module comment, sibling CLI test docstring, debug-record enforcer table) that no committed test could reach layer 3. --- PRIOR ENTRY, RETAINED FOR THE COMPONENT-EXACT +20 RECONCILIATION AND THE 20 TEST NAMES: 1121 passed / 33 skipped / 0 failed (MEASURED 2026-08-26 at 5078cdc, 838.26 s; 1154 collected). CORRECTED from the prior 1101 passed / 33 skipped / 0 failed (MEASURED 2026-08-25 at 1d2bd53, 813.85 s; 1134 collected) — this entry REPLACES that one, it is not appended to it. quick-260826-qq9 (the ancestry-blind manifest read repair) reconciles the move COMPONENT-EXACT: +20 items added, 0 removed, 0 renamed, all in the SAME one file tests/m3/test_pairwise_completeness_scan.py (verified by diffing collect-only against 352ac9e by NAME, not by count). 1101 + 20 = 1121 passed and 1134 + 20 = 1154 collected (and 1121 + 33 = 1154). Skips STAYED at 33 — no test added by this quick can legitimately skip; every one is pure-synthetic or a read-only parse of an in-repo file. CHECKED A THIRD, INDEPENDENT WAY: collect-only with that file --ignore'd still returns EXACTLY 1054 (proving the pre-existing set is untouched and that giving the _regions_tsv / _region_tsv fixtures an `ancestry` parameter changed ZERO item counts), and the file alone collects 100 = 80 + 20. THE 20, BY NAME — T1 the ancestry-keyed read (11 items): test_read_regions_tsv_reads_the_real_manifest_on_region_id_x_ancestry, test_read_regions_tsv_real_manifest_subset_is_n_windows_not_2n, test_read_regions_tsv_returns_the_requested_ancestrys_real_bounds, test_read_regions_tsv_selects_the_ancestrys_bounds_on_a_synthetic_manifest, test_default_ancestry_is_afr_in_the_module_and_in_the_parser, test_pending_paste_step3_carries_no_ancestry_flag_so_the_default_is_load_bearing, test_ancestry_predicate_agrees_with_the_production_filter_contract, test_region_only_in_the_unrequested_ancestry_raises_naming_the_id, test_cli_region_only_in_the_unrequested_ancestry_exits_2_and_writes_no_tsv, test_tsv_field_is_bounds_tolerant_standalone, test_read_regions_tsv_length_guard_masks_the_accessor_so_tsv_field_is_tested_alone. T2 defense in depth (9 items): test_iter_bim_windows_single_region_id_control_still_returns_six_rows (the CONTROL, green before AND after — deliberately), test_iter_bim_windows_duplicate_region_id_identical_bounds_raises, test_iter_bim_windows_duplicate_region_id_differing_bounds_raises, test_assert_unique_region_ids_names_the_offending_ids_and_their_counts, test_cli_duplicate_region_id_manifest_exits_2_and_writes_no_tsv, test_cli_stdout_table_prints_exactly_one_line_per_region_id, test_pooled_candidate_rows_is_the_summaries_basis_and_names_it, test_pooled_candidate_rows_reconciliation_raises_when_the_bases_disagree, test_two_ancestry_manifest_emits_no_inflated_counts_end_to_end.",
    "tests/phase2": "136 passed / 1 skipped / 0 failed (the 1 skip is test_negative_controls.py: bedtools not available)",
    "rule": "⚠ Skips must STAY at 33 (tests/m3) and 1 (tests/phase2). A new test landing as a SKIP is NOT evidence — check the skip count, not just the failure count. The 33 are the pre-existing 31 + the R4-COVERAGE disclosure enforcer in test_fire_verifier.py + the LAYER-2 test_region1_real_window_substrate_totals_MEASURED_NOT_DERIVED gated on data/aou/region1_window.bim. ⚠ `pytest tests` AS ONE INVOCATION DOES NOT COLLECT: tests/m2/conftest.py shadows tests/m3/conftest.py for the module name `conftest`, so 12 tests/m3 modules that import R_SUBPROCESS_TIMEOUT_S error at collection. PRE-EXISTING and unrelated to any recent change (reproduced with two untouched files: `pytest tests/m2 tests/m3/test_ld_npz_to_rds.py --collect-only`). Run the sub-suites separately, as the commands below already do.",
    "commands": "/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3 -q -rs   (~13-14 min)  and  ... tests/phase2 -q  (~2 s).  Collateral outside tests/m3 on 2026-08-22: `pytest tests --ignore=tests/m3 -q` = 20 failed / 541 passed / 18 skipped / 1 xfailed — all 20 PRE-EXISTING environment/data-state reds in tests/m2 (consumer freshness, stratum keys) and tests/phase5 / tests/phase9 (GMT + LDSC artifacts, r-gprofiler2, gcta); none of those files imports run_native_ld_panel, fire_verifier or occlusion_gate_constants."
  },
  "timestamp_reason_2026_08_12": "SESSION CLOSE 2026-08-12 (/close-session). Earlier same-day stamp was the quick-260812-09a sweep.",
  "timestamp_reason_2026_08_12_evening": "EVENING refresh after quick-260812-ot2 (E-2 obligation (1) placement prep, obligation (2) skip recorded) and quick-260812-ox1 (PRE-FIRE 1 landed, L-checks re-anchored, runbook). The /close-session record is timestamp_reason_2026_08_12; its 2026-08-12T13:10:00Z stamp was overwritten in the top-level timestamp field by this refresh and survives only in this sentence.",
  "timestamp_reason_2026_08_14": "/close-session after the 2026-08-12→14 arc: E-2 (1) discharged by directed placement, venue+P-1+no-preprint decided, blast-radius sweep + thz remediation, browser fire packages, Seth reconciliation (trsx5 contested), t21 producer gates wired.",
  "timestamp_reason_2026_08_16": "/close-session after the 2026-08-14 -> 08-16 arc: step 6b FIRED (STOP-truncated -> RECHARACTERIZED as an unexplained THIRD BODY), 1b SIGNED, hex invariant widened {32,64}, Seth 9,907-B body arrived+verified+banked, the 149-B lineage delta RECONCILED EXACTLY, our own formatting-strip hypothesis REFUTED, courier reply banked, AoU ticket 57144 recorded.",
  "timestamp_reason_2026_08_26": "quick-260826-qq9: the STEP 3 sweep landed CONTAMINATED (ancestry-blind manifest read -> 8x). Instrument repaired + tested (d8f4d54, 5078cdc), the run banked as-received with contaminated/surviving separated, and the re-run's expectation PRE-REGISTERED before it runs. tests/m3 baseline corrected to 1121/33/0.",
  "sweep_2026_09_01_COMPLETE": {
    "status": "COMPLETE, VM STOPPED, nothing running",
    "prereg_confirmed": {
      "wc_l": 353090,
      "pooled_candidate_rows": 353089,
      "distinct_undefined_pairs": 13,
      "already_occluded": 10,
      "not_already_occluded": 3,
      "offset_histogram": {
        "-14": 1,
        "-9": 1,
        "-6": 1,
        "-3": 1,
        "-1": 1,
        "0": 10
      },
      "adjustments_made": 0
    },
    "runtime": "48m (the runbook's 4h20m was calibrated on the 8x-inflated run)",
    "artifacts": {
      "pcs_pairs.tsv": {
        "bytes": 107304497,
        "md5": "287b16b1991f63423ff3933996c0334d"
      },
      "pcs_summary.json": {
        "bytes": 16412,
        "md5": "4917c46d7348de6a5c9f3da83bc610b8"
      },
      "rotated": "both .STALE.20260831T184615Z retain ORIGINAL 26-Aug mtimes and contaminated sizes"
    },
    "panelwide": {
      "pairs_member_occluded": 12,
      "pairs_surviving": 1,
      "rows_member_occluded": 14,
      "rows_surviving": 1,
      "survivor": "m2_region_00149 chr7:89454077:GCGTA:G x chr7:89454076:C:T offset -1 pair_key 9776035|9776036",
      "survivor_is_conditional_on": {
        "bim_sha256": "9cc378b701277d57b54e8c1399ff5ceaeab7ae592783fce41271e7554feeeb99",
        "bim_n_lines": 20767864,
        "region_00149_n_rows_in_window": 338354
      },
      "occluded_verdicts_are_unconditional": "occlusion is MONOTONE in the row set",
      "unforeseen_route": "2 of the 4 that die do so because the ANCHOR DELETION is itself occluded (rows 5,13)",
      "outputs": {
        "pcs_panelwide_summary.json": {
          "bytes": 14313,
          "md5": "ba577b5739cd99e70bd6c14d342bd72f"
        },
        "pcs_panelwide_verdicts.tsv": {
          "bytes": 5203,
          "md5": "44cf49b67ce348f1d7c04cbe679fc821"
        }
      }
    },
    "new_measurement_partial_confounding_tail": {
      "defined_rows_ge_0p9": 3094,
      "pct_of_defined": 0.876,
      "regions_with_max_0p994_to_0p999": "21 of 21",
      "why_it_matters": "finite r, so NO NaN-raise fires and nothing downstream sees them"
    },
    "still_open": [
      "prevalence",
      "boundary width",
      "the +1 side (00057 is not in the pre-committed sample)"
    ]
  },
  "headline_PRIOR_2026_09_01": "353089/353090 confirmed EXACTLY as pre-registered; 12 of 13 undefined pairs and 14 of 15 rows never reach the LD matrix; the ONE survivor is m2_region_00149 chr7:89454077:GCGTA:G x chr7:89454076:C:T at offset -1 — the mirror of 00057's +1, which the pre-committed sample excludes by construction.",
  "resume_entry_point_PRIOR_2026_09_01": "REFRESHED 2026-08-26 (quick-260826-qq9). Read, in order: (1) resume_on_reconnect[0] — it SUPERSEDES every earlier #0 for 'what to do next'; (2) .planning/debug/260826-PCS-ancestry-blind-manifest-read-8x-duplication-and-the-prereg-prediction.md (the contaminated-vs-surviving table, the denominator reconciliation, the 15 identity-level rows, and the PRE-REGISTERED PREDICTION); (3) .planning/quick/260826-qq9-fix-ancestry-blind-region-manifest-read-/260826-qq9-SUMMARY.md; (4) .planning/STATE.md top ★ RESUME HERE ★ block. FIRST ACTIONS = (a) confirm the AoU VM's state and stop it if it is running; (b) request STEP 2's verbatim output; (c) decide with Carter whether to re-run STEP 3 with the repaired instrument — the command is UNCHANGED. Do NOT quote any count from the 2026-08-26 sweep. REFRESHED 2026-08-25 23:40 EDT. Read: (1) resume_on_reconnect[0]; (2) sweep_in_flight (PID/ETA/liveness/NO-CHECKPOINT); (3) .planning/debug/260826-STEP0-1-falsifier-PAIRWISE-COMPLETE-as-received.md (the falsifier evidence); (4) .planning/STATE.md top ★ RESUME HERE ★ block. FIRST ACTION on return = check whether /home/jupyter/occ_measure/pcs_summary.json exists; do NOT stop the VM before it does. PRIOR: REFRESHED 2026-08-25 (quick-260825-qpf). Read: (1) resume_on_reconnect[0] (the ADVERSARIAL REVIEW REMEDIATED entry — it SUPERSEDES every earlier #0 for 'what to do next'); (2) .planning/quick/260825-qpf-remediate-the-adversarial-review-of-the-/260825-qpf-SUMMARY.md; (3) .planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md (STEP 1 is the falsifier and it is UNRUN); (4) .planning/STATE.md top ★ RESUME HERE — LATEST ★ block. FIRST ACTION = Carter starts the VM and gives the go; the first thing that runs in-perimeter is STEP 0's plink1.9 version gate, then STEP 1 the falsifier. PRIOR: REFRESHED 2026-08-24 23:35 EDT. Read: (1) resume_on_reconnect[0] (STAGE B HALTED); (2) .planning/debug/260824-STAGE-B-HALT-region57-boundary-adjacent-pairwise-NaN.md (the measured finding + what must not be assumed); (3) .planning/STATE.md top ★ RESUME HERE ★ block. FIRST ACTION = the one-command mechanism probe, then DESIGN the 21-region prevalence sweep. PRIOR: REFRESHED 2026-08-24 21:20 EDT. Read: (1) resume_on_reconnect[0] (STAGE A PASSED); (2) .planning/STATE.md top ★ RESUME HERE ★ block; (3) .planning/debug/260824-STAGE-A-PASSED-region1-as-received.md (the evidence + reconciliation); (4) deferred-items.md :: AF-1 (RE-SCOPED — not a fire blocker). FIRST ACTION = Carter decides whether to stop the idle VM, then gives (or withholds) the Stage B go. PRIOR: REFRESHED 2026-08-22 03:25 UTC — POSTED + RECORDED. Read: (1) resume_on_reconnect[0] (the 2026-08-22 03:25 UTC entry — it SUPERSEDES every earlier #0); (2) STATE.md top ★ RESUME HERE — LATEST ★ block (2026-08-22 03:25 UTC POSTED + RECORDED); (3) .planning/osf_deviations.md LAST ## entry (the mk7ze record); (4) .planning/DECISIONS.md DEC-2026-08-22-occlusion-recalibration-posted; (5) .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md ★★ LATEST ★★. NEXT = the two-condition remediation batch as a /gsd-quick --validate TDD task, THEN Stage A. ⟵ PRIOR ENTRY POINT FOLLOWS ⟶ REFRESHED 2026-08-22 02:55 UTC. Read: (1) resume_on_reconnect[0]; (2) STATE.md top ★ RESUME HERE ★ block; (3) .planning/debug/260821-POSTING-CARD-for-carter.md (REFRESHED — current anchors c7f9dee26d3070ea83d6899dfc2c90d3 / 13a49f543cabcc27ce9f1e589783c060, upload name ...-2026-08-22.md). FIRST ACTION = Carter re-runs the Mac-side extraction and uploads. PRIOR: REFRESHED 2026-08-21 16:25 EDT CLOSE. Read, in order: (1) resume_on_reconnect[0] (2026-08-21 LATE-AFTERNOON); (2) .planning/STATE.md top ★ RESUME HERE ★ block; (3) .planning/debug/260821-POSTING-CARD-for-carter.md (sections iii-vi); (4) .planning/quick/260821-jcs-bank-seth-s-final-pass-re-confirmed-at-p/260821-jcs-SUMMARY.md + VERIFICATION.md. FIRST ACTION = ask Carter whether osf-amendment-occlusion-gate-recalibration-2026-08-21.md is on osf.io/az52u. PRIOR: REFRESHED 2026-08-16 CLOSE. Read, in order: (1) resume_on_reconnect[0] (2026-08-16); (2) .planning/STATE.md the 2026-08-16 ** RESUME HERE block; (3) .planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-LINEAGE-DIFF-149.md (the mechanical reconciliation + our refuted hypothesis + its own self-corrections); (4) .planning/quick/260815-i2v-courier-reply-to-seth-149-byte-lineage-d/260815-i2v-REPLY-TO-SETH-lineage-reconciled.md (the courier awaiting Carter paste); (5) .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md (item 6b = the trsx5 gate that is HOLDING the fire; item 6 now carries the NO-CREDIT-BACKSTOP note; item 7 = the SIGNED 1b record).",
  "run2_step2_2026_09_02": {
    "artifacts_on_VM_not_in_repo": {
      "pcs_tail_verdicts.tsv": "md5 960f283734aea3b2c56c9249cf4fe94b, 1031086 B",
      "pcs_tail_summary.json": "md5 bd74c0d502d15ac4e2fb5e1bdafc72b1, 38548 B",
      "written": "2026-09-02T21:07:03Z; launched 18:26:17Z; 2h40m46s wall"
    },
    "completion_check_NOT_a_prereg": "n_tail_rows_in 3094/3094, n_tail_rows_out_of_scope 0/0, n_defined_rows_out_of_scope 0/0 — all PASS. These are an INVARIANT/COMPLETION check (3094 was carried forward from the smoke; the zeros follow by construction), NOT a pre-registration. Enforced at pcs_panelwide_reclassify.py:1106.",
    "the_governing_doc_declined_an_expectation": ".planning/debug/260901-PENDING-PASTE-POSTHOC-...md:211-216 states NO EXPECTED VALUE deliberately. The three findings are MEASURED-NOT-PREDICTED and that is a strength.",
    "blackout": "Run finished during a VM reboot. stdout and $? UNRECOVERABLE, never seen. md5s anchor FORWARD not backward — no pre-reboot hash exists.",
    "record": ".planning/debug/260902-COURIER-TO-SETH-RUN2-tail-PRE-vs-POST-filter-heterogeneity-and-definitional-disagreement.md — 621 lines, md5 f52b76ce73b5f83d03a6e09e2822563f (MEASURED 2026-09-08 after 260908-u5k added ONE additive scope-limiting annotation to the LIVE BODY; was 613 lines, md5 ce6791344916c6ebebacfadbb808c702 after 260904-dgi's corrections + re-splice; this pin and the .continue-here.md one were STALE BY TWO GENERATIONS until 260904-dgi refreshed them). ★ THE BYTE-FROZEN VERBATIM APPENDIX IS UNMOVED: 14119 B, md5 e4cb947823dd758ec2bd32667b788391, still byte-equal to 260902-vsp/CONTENT-SPEC.md '## ARTIFACTS'->EOF.",
    "clustering_measurement_2026_09_08": "SECOND READ of the SAME artifact (/home/jupyter/occ_measure/pcs_tail_verdicts.tsv), VM-side, 2026-09-08. All four banked anchors matched BEFORE anything was computed (md5 960f283734aea3b2c56c9249cf4fe94b, 1031086 B, 3110 lines, 32 columns); byte-identical after a six-day gap and a VM stop/start (mtime 2026-09-02 21:07), so the provenance above holds against THIS EXACT ARTIFACT. No re-run, no genotypes, no scan re-execution. Exit 0; all reconciliations passed. RESULT: within-window clustering measured (c_eff 1.494248, ICC 0.728930, deff 1.360272) and Finding 2 RETRACTED to NOT ESTABLISHED. Recorded in .planning/osf_deviations.md §(9) and §(10)."
  },
  "seth_round2_outcome_2026_09_03": {
    "OUR_TEST_WAS_INVALID_strike_it": "Our collapse-to-parents argument ('merging correlated units should REMOVE dispersion; it went UP to 2.62x; therefore heterogeneity is between parents') DOES NOT DISCRIMINATE. Re-simulated: collapse raises dispersion under HIS hypothesis too (65% of runs, median +3.6%). CONCLUSION SURVIVES, ARGUMENT MUST BE STRUCK FROM THE RECORD.",
    "interpretability": "Observed ~2.0 overdispersion maps to parent-rate CV ~= 0.20 (our sim; CV 0.25 gives 2.54). Seth proposed 0.25-0.30 — CORRECTED DOWNWARD to ~20% relative variation between parent regions.",
    "seth_asks_1_2_3": "Ask 1 = DISCLOSE + ANNOTATE, do not amend (two targets: methods disclosure AND data-product manifest annotation — the latter is the one that matters). Ask 2 = do NOT pre-register a dispersion figure; and (CORRECTED 260904-dgi) the covariate scan is an UNDERPOWERED NULL, NOT a negative result — at n=21 the design has 80% power only for |rho| >~ 0.61, so it excludes only STRONG correlates. The instruction to record it as a negative result is withdrawn. Ask 3 = property of the first (rank inversion from asymmetric carrier loss), NOT a third disclosure — but PIN THE DEFINITION NOW.",
    "PINNED_DEFINITION": "ANY CARRIER FLOOR MUST BE DEFINED ON min-on-the-pairwise-complete-INTERSECTION, NEVER on rarer-by-overall-MAF. They disagree on ~24% of exactly the rows a floor exists to catch.",
    "seth_conceded_NARROWED_2026_09_04": "His non-independence hypothesis is DEAD. Confirmed by simulation: all-equal parent rates + duplication -> dispersion 0.98, — but that simulation tested BETWEEN-window duplication ONLY. NARROWED by 260904-dgi: it does NOT follow that non-independence in general cannot create dispersion. WITHIN-window clustering can and does (two co-moving pairs per occluding deletion reproduces the observed 1.99 exactly with ZERO parent-rate heterogeneity), and the pairs-per-deletion distribution HAS NOT BEEN MEASURED, so Finding 2's magnitude is NOT yet attributable. The BETWEEN-window half is still HIS refutation and is still correct; the general claim was ours to overreach and is withdrawn.",
    "leave_one_out_RESTATED_AT_PARENT_LEVEL": "SUPERSEDED 2026-09-04 (260904-dgi). The window-level LOO (21 fits, overdispersion 1.99-2.48, worst-case p=0.0063) does NOT rule out an influential unit: the two chr15 windows shield each other so it cannot remove parent 00060 at all, and its minimum IS the headline. Leave-one-PARENT-out over the 19 distinct parent regions ranges 1.52-2.49 with worst-case p 0.073 — removing parent 00060 renders the heterogeneity NON-SIGNIFICANT at alpha 0.05.",
    "decision_rule_did_not_fire_2026_09_08": "The rule set before the measurement was: c ~ 2 at high ICC reproduces the observed 1.99 from a homogeneous panel; c ~ 1 leaves the dispersion open and parent-region heterogeneity live. MEASURED c_eff 1.494248 at ICC 0.728930 -> deff 1.360272: BETWEEN the branches. NEITHER FIRED. The comparison of point estimates was therefore not the decisive test at all — the Rao-Scott correction was, and NEITHER PARTY PROPOSED IT (it was constructible only once the clustering had been measured). Recorded plainly: a decision rule that fails to fire is a fact about the rule, not a licence to pick whichever branch one prefers."
  },
  "OPEN_QUESTION_BACK_TO_SETH": "mk7ze lines 108-110 (repo draft lines 275-277) state — posted = repo draft − 167, proven against the posted body's own md5 13a49f543cabcc27ce9f1e589783c060 (333 lines): 'An observable NaN requires complete-case zero variance at that pair; geometric occlusion requires only coordinate span coverage, so every NaN-implicated occlusion is a geometric occlusion but not conversely.' Read STRICTLY the survivor does NOT contradict it (it has no covering record for EITHER member, not a non-geometric one). Read for what the sentence DOES in the argument — establishing geometry as a safe superset of the NaN cases — the survivor is an undefined pair geometry does not catch, in the upstream direction the strict inequality cannot see. DISPOSITION IS SETH'S: disclosure, or correction-obligation to a posted record? THE DISCLOSURE IS NOT WRITTEN PENDING HIS ANSWER.",
  "survivor_geometry_PROMOTE_TO_HEADLINE": "Seth's strongest point: the ONE surviving undefined pair is m2_region_00149 chr7:89454077:GCGTA:G x chr7:89454076:C:T, offset -1, UPSTREAM. REF=GCGTA spans 89454077..89454081; the posted predicate d.pos < v.pos gives 89454077 < 89454076 = FALSE. Invisible BY CONSTRUCTION, class (ii), and the ONLY surviving direction — no positive offset appears anywhere across 21 regions. That supports a PROSPECTIVE positive-offset falsification check and belongs as a HEADLINE, not the 'ALSO' section where we filed it. ⚠ CORRECTED 260904-dgi: it is NOT proof that the downstream predicate is complete — n=1 supplies neither prevalence, boundary width, nor one-sidedness (pairwise_completeness_scan.py:40-45). DO NOT EXTEND THE PREDICATE TO -1: n=1, and cheapness (~0.12% of panel) is an argument AGAINST, not for. That is calibrate-to-pass. REGISTER THE PREDICTION INSTEAD, BUT SAMPLE-SCOPED (CORRECTED 260904-dgi): 'WITHIN the 21-region pre-committed sample, residual undefined-r was observed ONLY at negative offsets; a positive-offset survivor WITHIN that sample would falsify downstream predicate completeness for it.' ⚠ A positive-offset case is ALREADY KNOWN OUT-OF-SAMPLE — m2_region_00057's +1 (chr15:20394741:AT:A x chr15:20394743:T:C, one base past the span end, downstream, un-occluded, measured 2026-08-24) — and STATE.md:287 already concluded the residual class sits adjacent to the REF span on EITHER side. Production tests the rate on BOTH sides. It is post hoc relative to the 21-region scan and is NOT a pre-registration today.",
  "repo_fixes_status": [
    "1. DONE (260903-ict). The false 0.0005 ACTION ITEM was replaced by a CORRECTION, fixed AT SOURCE in 260902-vsp's CONTENT-SPEC.md and re-spliced into the courier's verbatim appendix. ⚠ REFINED 260904-dgi: that correction OVERSHOT. The two 0.0005 occurrences are DISTINCT LIVE PARAMETERS with NO RUNTIME COUPLING, but they are NOT unrelated — mk7ze records that the occlusion gate reused 'the same fractional gate as the withdrawn ceiling, re-purposed to exclusions', so the shared value has a documented COMMON ORIGIN.",
    "2. DONE (260903-ict) — as an ADDITION, not a strike: the collapse-to-parents argument was never in the courier, so the corrected statistical position was added rather than removed.",
    "3. STILL QUEUED — THE ONLY ONE OUTSTANDING, and WIDER than originally stated: condition_ld_matrix.py (:4, :153) AND write_conditioned_ld_npz.py (:4, :17, :85) cite tcujq as PRE-REGISTERING the NaN->0 policy, while osf_deviations.md:133/:166 show the 2026-07-10 trsx5 update WITHDRAWS exactly that policy. Accurate label: 'parameter of a withdrawn policy, retained in a frozen module, not called in production'. ⚠ TOUCHES src/ — NOT docs-only. Discharge requires BOTH files PLUS a named enforcer test seen RED first.  ✅ DISCHARGED 2026-09-16 by quick-260916-oyq: BOTH files now carry additive WITHDRAWN-by-trsx5 notices in their docstrings (code strings untouched, CODE-INERT), condition_ld_matrix.py's whole-file pin is rescoped to CODE-only (DEC-2026-09-16-condition-ld-matrix-freeze-code-only), and the named enforcer tests/m3/test_tcujq_withdrawal_notices.py was COMMITTED RED (3F/14P) before it went green. Commits b6b1f70 / b709ce1 / 48b8828, close-out 621701c, verified 11/11. NOTHING IS OUTSTANDING IN THIS LIST ANY MORE."
  ],
  "headline_PRIOR_2026_09_08": "THREE FINDINGS BANKED + SETH ADJUDICATION ROUND 2 DONE. (1) tail is PRE-filter dominant with a POST-filter residual in ALL 21 regions: rows 2560 PRE/534 POST of 3094 (17.26%), pairs 2047/474 of 2521 (18.80%), zero-POST regions = 0. (2) ⛔ RETRACTED 2026-09-08 (260908-tnd) — NOT ESTABLISHED. As banked it read: 'regions do NOT share a common POST rate: pair-level overdispersion 1.99x (p 6.3e-3) after dropping the chr15 double-count; UNEXPLAINED (rho -0.199/-0.201/+0.004 in a design returning +0.886 when structure is present).' Those figures assume INDEPENDENT pairs and that assumption is MEASURED FALSE (ICC 0.728930, deff 1.360272). Design-corrected, two INTERCHANGEABLE chr15 overlap treatments give p 0.0326 and p 0.0517 — STRADDLING 0.05 — and correcting while dropping both gives p 0.2171. Direction holds (phi > 1.2); magnitude UNIDENTIFIED; significance is an ARTIFACT OF ANALYTIC CHOICE. (3) rarer-vs-min definitional disagreement is a SEPARATE axis: 24.27% in tail vs 0.52% below = 46.5x enrichment, independent at rho +0.173.",
  "resume_entry_point_PRIOR_2026_09_08": ".planning/HANDOFF.json (this block) + the ★ RESUME HERE ★ block at the top of .planning/STATE.md. The banked result is .planning/debug/260902-COURIER-TO-SETH-RUN2-*.md. NOTHING IS RUNNING — do not look for a job to reattach.",
  "measurement_2026_09_08": {
    "fired_by": "Carter (VM). No agent fires. Pre-flight matched all four banked anchors BEFORE computing: md5 960f283734aea3b2c56c9249cf4fe94b, 1,031,086 B, 3,110 lines, 32 columns.",
    "artifact_survived": "byte-identical after a six-day gap and a VM stop/start (mtime Sep 2 21:07)",
    "clustering": "n_pairs 2521; n_pairs_POST 474; POST-conflicts within a pair 0; dual-anchored 573 (22.7%); n_clusters 2105; c_mean 1.1976; c_eff 1.494248; ICC 0.728930 (k=303, N=719, c0=2.371745); deff 1.360272",
    "caveats": "THREE regions ICC exactly 1.000000 = estimator BOUNDARY, not a fitted value (00027, 00040__sub10, 00161__sub13). FIVE regions NA (00017, 00033, 00053, 00064, 00088__sub01) assigned deff 1.0 = NO correction, a choice that runs AGAINST our own conclusion. Chaining diagnostic CLEAN (largest component 8 pairs, 0.32%).",
    "rao_scott_CORRECTED": "all21 1.931/p0.0074 | drop sub13 1.675/p0.0326 | drop sub12 1.579/p0.0517 | drop both 1.241/p0.2171. Pooled rate estimated FROM THE SUBSET UNDER TEST.",
    "what_was_NOT_shown": "deff 1.360 vs observed 1.99 — clustering does NOT explain Finding 2. It sits inside the observed CI [1.1,4.2], which is a FAILURE TO EXCLUDE, not a demonstration. Never write that it explains it.",
    "seth_rule_did_not_fire": "his c~2 / c~1 branches: measured c_eff 1.494 at ICC 0.729 -> BETWEEN. The Rao-Scott correction settled it and was proposed by NEITHER party."
  },
  "seth_status": "FINDING 2: CLOSED 2026-09-17. He accepted the disposition and all seven points of our reply, and WITHDREW BOTH objections (his pooled-deff 36% share; his post-hoc window selector, which he named as his own threshold objection applied to the selector). Nothing further from him until a new measurement or a posting decision. His reply and closure are banked as received (NOT byte-verified) under .planning/quick/260917-irg-*/ and .planning/quick/260917-pff-*/; Carter's reply courier is banked and marked SENT; the measurement courier's text AS SENT is banked in-repo. STAGE C: he has NOT been heard from - Carter has not confirmed sending the v2 courier. That courier is the only open Seth-facing item; do NOT send him anything else and do NOT ask him about the unattributed '0.02' figure.",
  "headline_PRIOR_2026_09_09": "FINDING 2 IS NOT ESTABLISHED. Design-corrected, two INTERCHANGEABLE chr15 overlap treatments STRADDLE p=0.05 (drop sub13 -> 1.675/p 0.0326; drop sub12 -> 1.579/p 0.0517), so the significance is an artifact of analytic choice. NEW FINDING: within-window clustering is real and strong — c_eff 1.494248, ICC 0.728930, deff 1.360272, measured from pcs_tail_verdicts.tsv. FINDING 1 UNTOUCHED: 2560/534 rows (17.26%), 2047/474 pairs (18.80%), 21/21 regions with tail rows, 0 with zero POST.",
  "cost_reclamation_2026_09_10": {
    "invoice_shape": "Aug $2,934.31 total; 98% ($2,883) in wb-perky-corn-6639; of that ~$1,968 = ~49 TB Compute Engine PD and ~$847 = ~41 TB Standard GCS bucket; ACTUAL COMPUTE $64 for the month. ~$93/day. May 2,286 / Jun 5,898 / Jul 2,857 / Aug 2,934 ~= $14,000 cumulative.",
    "root_cause": "FOUR Dataproc clusters left STOPPED since June: 20260604 (24w), 20260605 (24w), 20260617 (16w), 20260620 (24w) = 88 workers + 4 masters = 92 nodes x ~500 GB default PD ~= 46 TB. A STOPPED resource halts the vCPU/RAM meter and bills 100% of its disk indefinitely.",
    "our_own_rule_was_already_right": "STATE.md:2928 recorded Rule 1 (Delete, never Pause) for Dataproc on 2026-05-14, BECAUSE AoU Dataproc envs expose no reattachable PD. It was not followed. A reclamation click ('delete the STOPPED Hail cluster 20260604, ~$169 standby') sat in the handoff from JUNE, unactioned.",
    "action_taken": "Carter deleted all four apps via the Workbench UI (the pet SA has Dataproc create/get/delete/start/stop DENIED, so gcloud cannot do it).",
    "verified_three_ways": "(1) gcloud dataproc clusters list -> Listed 0 items; (2) dataproc operations list shows a DELETE operation for each of the four; (3) gcloud compute instances list collapsed 94 -> 2, exactly 92 removed, reconciling to the 92-node count to the unit.",
    "NOT_verified": "DISK-level reclamation is NOT confirmed by measurement — compute.disks.list is DENIED to the AoU pet SA (and returns EXIT 0 with empty output, a trap). The 92-instance collapse is the arithmetic substitute: an instance cannot exist without its boot disk. The measurement is the NEXT DAY'S SPEND.",
    "expected_effect": "PD charges prorate hourly, so the drop starts immediately. ~$2,883/mo should fall toward roughly $900-950, with the 41 TB bucket as the remaining lever. ESTIMATE from the invoice breakdown, not a measurement — confirm against daily spend.",
    "provenance_rescued": "BEFORE any reclamation the tail disclosure's single-copy artifacts were copied from the VM PD to gs://rw-migration-aou-rw-476cdac2/provenance/occ_measure/ — 4 objects, 108,390,543 B, all md5s round-trip verified: pcs_tail_verdicts.tsv 960f283734aea3b2c56c9249cf4fe94b; pcs_tail_summary.json bd74c0d502d15ac4e2fb5e1bdafc72b1; pcs_pairs.tsv 287b16b1991f63423ff3933996c0334d; pcs_summary.json 4917c46d7348de6a5c9f3da83bc610b8. Recorded hashes anchor FORWARD, not backward — losing the file would mean the numbers in a publication-bound document could never be re-verified.",
    "still_open": "(a) STOP the running analysis VM aoujupytercomputeengine20260626b (n1-standard-32, ~$1.50/hr) — Carter only; stop, NEVER delete. (b) The ~41 TB bucket is untriaged: `gsutil du` on it had produced no output after ~25 min and may run for hours (Hail MTs are millions of objects). CHEAPER PATH: read bucket total from the Cloud Console (free, from monitoring, no enumeration) and use Storage Insights for a per-prefix inventory rather than paying VM time. (c) gs://dataproc-staging-* and gs://dataproc-temp-* now serve clusters that no longer exist — likely the cleanest remaining reclaim.",
    "vm_stopped": "DONE 2026-09-10 by Carter. Stop, not delete — the reattachable PD and the 379 GB bfile are preserved and resume-safe.",
    "the_falsifiable_check": "WAS ~$93/day. IF the 92 instances' disks went with them, daily spend should fall to roughly $30/day (the ~41 TB bucket alone is ~$28/day, plus the VM's ~1 TB PD and the one remaining terminated instance). IF it stays near $90/day, the disks did NOT release and the reclamation needs an identity with compute.disks.list. Check daily spend, not the monthly invoice."
  },
  "headline_PRIOR_2026_09_10": "STOPPED IS NOT FREE. Four Dataproc clusters left STOPPED since June billed 100% of ~49 TB of PD ($1,968/mo) against $64/mo of real compute. DELETED 2026-09-10, verified three ways: clusters absent, four DELETE operations recorded, and 92 instances removed matching the node count exactly. ⚠ THE ANALYSIS VM IS STILL RUNNING and is the only billing instance left — Carter must STOP it (stop, never delete: its reattachable PD holds the 379 GB bfile). The ~41 TB bucket ($847/mo) is still untriaged.",
  "resume_entry_point_PRIOR_2026_09_10": ".planning/HANDOFF.json (this block) + the ★ RESUME HERE ★ block at the top of .planning/STATE.md. Science record = .planning/osf_deviations.md, the 2026-09-03 entry as amended through 2026-09-09. Cost record = this block. NOTHING IS COMPUTING, but the VM may still be UP.",
  "resume_on_reconnect_PRIOR_2026_09_10": [
    "0. FINDING 2 IS NOT ESTABLISHED — the outstanding measurement was FIRED on 2026-09-08 and it RETRACTED rather than confirmed. NOTHING IS OPEN before a posting DECISION: no open objection, no outstanding measurement. ⛔ THAT IS NOT AUTHORIZATION TO POST — the disclosure is STILL DRAFTED — NOT POSTED and the decision is CARTER'S ALONE. Read, in order: (1) .planning/STATE.md top '2026-09-08 (LATER)' ★ RESUME HERE ★ block; (2) .planning/osf_deviations.md §(10) — the clustering finding, the Rao-Scott table and the retraction, with its provenance in §(9); (3) .planning/quick/260908-tnd-finding-2-is-not-established-the-pairs-p/SUMMARY.md. KEY NUMBERS: c_eff 1.494248, ICC 0.728930, deff 1.360272; corrected p 0.0326 (drop sub13) vs 0.0517 (drop sub12) — INTERCHANGEABLE choices STRADDLING 0.05, which is the decisive observation, NOT the design effect. Finding 1 UNCHANGED; Finding 3 unchanged. ⚠ Clustering is NOT an explanation of Finding 2 and must never be reported as one: deff 1.360 vs observed 1.99, inside the CI [1.1, 4.2] = a FAILURE TO EXCLUDE, not a demonstration. ⚠ The three ICC = 1.000000 regions are a BOUNDARY value, not fitted estimates. NOTHING IS RUNNING — do not look for a job to reattach. AN AGENT NEVER POSTS AND NEVER FIRES.",
    "1. NOTHING IS RUNNING. VM stopped. $0 outstanding. Do not restart anything to 'check'.",
    "2. THE COUPLED POST-SWEEP TASK (three actions, must land together): (a) apply .planning/debug/260831-DEFERRED-pairwise-completeness-scan-docstring.patch; (b) rescope the live runbook's STEP 0 gate OFF whole-file md5 onto a docstring-insensitive CODE hash, PROVEN ABLE TO FAIL before it is trusted; (c) delete the §(1b) deferral note in src/python/pcs_panelwide_reclassify.py — its own self-invalidating test goes RED when (a) lands and will demand it.",
    "3. samepos_missingness_probe.py is BUILT, TESTED and NOT RUN. It replaces an unverifiable Hail split_multi_hts INFERENCE with a measurement. Running it needs the VM.",
    "4. THEN Seth: send the measurement beside his SEALED brief-blind prediction. He predicted the surviving class would be ~zero and was closer than we were.",
    "5. Read .planning/quick/260831-kw8-.../260901-kw8-PANELWIDE-RECLASSIFICATION-as-received.md for the full result with its scope condition."
  ],
  "headline_PRIOR_2026_09_16": "RESUME THE ANALYSIS AT STAGE C's NaN ERROR POSTURE. Stage A PASSED; Stage B banked 3 of 4 then --fail-fast halted on m2_region_00057 (the pre-registered raw-panel NaN-raise contract working). The halt record's steps 1 (mechanism: 871/871 deletion carriers no-called at the neighbour) and 2 (prevalence: sweep -> panel-wide reclass -> ONE survivor; tail classified) are DONE. Step 3 — how Stage C treats a region that raises on a residual boundary-adjacent NaN — is NOT decided, and the class exists on BOTH sides of the span (00057 +1, 00149 -1), so a 276-region run under --fail-fast will halt.",
  "headline": "STAGE C IS THE BLOCKER AND IT NEEDS ONE COURIER FROM CARTER. v2 of the options draft is brief-blind, courier-ready and PUSHED: .planning/debug/260916-STAGE-C-NaN-ERROR-POSTURE-options-DRAFT-v2.md at 57a3105 (md5 46537925de303ffb8a2d2b31d8160071), options A-F, 7 questions in §5, no recommendation, citations at basis 74f962d, checker GREEN 446 / selftest 70/70 / --live RED on exactly 10 known ids. A brief-blind LEAK was found while couriering and closed (quick-260917-f68): v2 no longer cites STATE.md (line 53 held the private recommendation), the working notes beside the checker are redacted, the checker docstring names no option, and hashed lk: gates print counts only. The reviewer's Finding 2 loop is CLOSED (quick-260917-irg + -pff): closure banked, both his objections withdrawn, Finding 1 is the substantive result. ⚠ A forked orchestrator branch committed twice on a superseded plan (e8532e4, f4becda), the second AFTER a stop instruction; both reverted by 29a7f68.",
  "resume_on_reconnect": [
    "0. READ FIRST: the 2026-09-18 ★ RESUME HERE ★ block at the top of .planning/STATE.md. NOTHING IS RUNNING - do not look for a job to reattach. The VM is STOPPED: start it, never create; stop it, never delete. AN AGENT NEVER FIRES, STARTS, STOPS OR DELETES ANY CLOUD RESOURCE, BUCKET OBJECTS INCLUDED.",
    "1. ⭐ THE ONLY BLOCKER: Stage C's NaN error posture is NOT DECIDED and no posture code exists. Everything agent-doable is DONE. Ask Carter one question: has the Stage C cover note gone to the reviewer? If NO, hand him the note (session scratch SETH-PASTE-2-stage-C-cover-note-DRAFT.txt; rebuild from the STATE block if scratch is gone). If YES, wait - nothing else unblocks Stage C. He adjudicates brief-blind; Carter decides after.",
    "2. When his adjudication arrives: bank it as received (label it NOT byte-verified), then Carter decides. Only after that decision may posture code be planned. Write NO posture code before it.",
    "3. COST-1 (needs Carter at the VM): m2_region_00071 DEFERRED at n_var=169803 > ceiling=120000 - nothing was measured and the runbook's 'largest square-feasible region' label for it is FALSE. Card 2 (session scratch 260917-CARD-2-COST1-largest-feasible-region.md) counts every AFR region's pre-window n_var from the .bim (driver rule: rows on the contig within the INCLUSIVE window, chr prefix stripped; 00071 = 169,803 is the known-answer control), then runs COST-1 on the real largest feasible region - into a LOCAL out-dir if that region is already banked.",
    "4. DISCLOSURE: the tail disclosure (.planning/osf_deviations.md, 2026-09-03 entry, amended through 3c570f0) is DRAFTED - NOT POSTED. The reviewer's closure is NOT authorization. Posting is Carter's alone and is not made.",
    "5. Reading peak_ram_gib: it is PLINK-ONLY since the RAM-1 launcher fix. The driver's own in-process load (~4*n_var^2 bytes: 39.08 GiB at 102,421; 53.64 GiB at 120,000) is SEPARATE and must be added for VM sizing. Never compare against the pre-fix panel rows (00001 30.6591, 00017 2.9689, sub14 26.5745, 00057 26.5745) - those were a running high-water mark.",
    "6. fire_verifier stage-b's cost_gate_denominator has been RED since 00057's error: row joined the production panel (it compares ALL rows against bankable rows) and stays RED while any non-ok row is present. Pre-existing; not caused by COST-1; not a reason to re-fire or 'repair'.",
    "7. PROCESS (bit us this session): a FORKED main-thread orchestrator branch dispatched an executor on a superseded plan and committed twice, the second time 36 s AFTER a stop instruction. ListAgents did not list it and TaskStop could not reach it; SendMessage stopped it. Before every dispatch and every commit: git log --oneline -3 AND ls -t <session>/subagents/*.meta.json | head -3. Back rogue commits out with ONE `git revert --no-commit A B` plus a commit - never reset."
  ],
  "storage_inventory_2026_09_16": {
    "record": ".planning/debug/260916-AOU-STORAGE-INVENTORY-and-reclaim-plan.md",
    "ld_total": "45,519,841,557,487 B / 1,788,824 objects, reconciled to ZERO residual in bytes AND objects",
    "finals_proven": "mt_afr_qc 73,122x20,767,864; mt_eur_qc 220,098x11,375,140; mt_afr_pca_selfid_qc 62,557x20,817,925 — each EXACT vs cohort_summary_m3.tsv, entries partition list identical to rows element-wise. No _VALIDATED marker is EXPECTED (aou_ld_panel.py _final_is_trustworthy back-compat).",
    "no_op_proven": "contaminated final cols 73,122 (working filter <= 62,557), rows 20,767,864 = mt_afr_qc rows.",
    "bim_proven": "bucket ld/afr_native_panel/bfile/afr_cohort.bim sha256 = 9cc378b7...eeb99 = disclosure anchor",
    "reclaim_candidates": "38,651,533,486,697 B / 1,345,367 objects / ~$773/mo — NOTHING DELETED",
    "keep_floor": "6,868,308,070,790 B / ~$137/mo storage; realistic floor ~$180/mo incl. 20260626b PD",
    "provenance_added": "gs://rw-migration-aou-rw-476cdac2/provenance/vm_20260608_probes/ — 7 probe files + load-env.sh + load-env, md5-verified"
  },
  "resume_entry_point": ".planning/HANDOFF.json resume_on_reconnect (above) + the 2026-09-18 ★ RESUME HERE ★ block at the top of .planning/STATE.md. ⭐ WHERE THIS FILE AND THAT BLOCK DISAGREE, STATE.md WINS - it is refreshed at every session close. Resume at item 1 (the Stage C courier, Carter's). Items 2-3 and 5-6 of the 2026-09-16 list are DONE.",
  "timestamp_reason_2026_09_16_blast_radius": "quick-260916-vqp refreshed this file's stale B1 fields on 2026-09-16 after the blast-radius review of c93e97b..621701c, which is banked BYTE-IDENTICALLY (no banked-by header added) at .planning/debug/260916-BLAST-RADIUS-c93e97b-to-621701c.md with its Codex CLI v0.141.0 sibling at .planning/debug/260916-BLAST-RADIUS-codex-review-as-received.md (= the 'codex-review.md beside this file' that §D of the banked file refers to). Fields changed: headline (PRE value preserved verbatim in headline_PRIOR_2026_09_16), resume_on_reconnect[1]/[2]/[3], carter_decisions_outstanding[0], repo_fixes_status[2], suite_baselines, freeze_state, resume_entry_point, timestamp. Every other field is byte-unchanged; every edited string still CONTAINS its pre-2026-09-16 text verbatim. DOCS ONLY: nothing fired, no OSF/Seth/cloud/network contact, $0.",
  "headline_PRIOR_2026_09_17": "RESUME THE ANALYSIS AT STAGE C's NaN ERROR POSTURE - and the OPTIONS DRAFT IS ALREADY BANKED AND NOT SENT: .planning/debug/260916-STAGE-C-NaN-ERROR-POSTURE-options-DRAFT.md (quick-260916-kht, f09535c, close-out 11f61e8; brief-blind, 88 citations verified). Halt-record steps 1 (mechanism) and 2 (prevalence) are DONE; step 3 - how Stage C treats a region that raises on a residual boundary-adjacent NaN - is NOT decided. What remains is CARTER's: judge the two residual framing cues, courier brief-blind, decide after Seth. ⚠ The 2026-09-16 blast-radius review lists 7 MEDIUM courier-readiness findings against the v1 draft (§B2 of .planning/debug/260916-BLAST-RADIUS-c93e97b-to-621701c.md), so courier a v2 or courier v1 with the basis caveat. ⚠ CORRECTED 2026-09-16: the committed Stage C command has NO --fail-fast (260812-ox1-AGENT-PROMPT.md:398), so a raising region records 'error:' and the loop CONTINUES (run_native_ld_panel.py:1287-1289 and :1421 at 621701c; :1144-1146 and :1278 at c93e97b, each re-located per citation). The PRIOR headline's 'a 276-region run under --fail-fast will halt' describes a command form that is NOT the committed one; it is preserved verbatim in headline_PRIOR_2026_09_16. RAM-1 and tcujq are DONE (f8ff9cd/9a3eb97/0231cbf; b6b1f70/b709ce1/48b8828/621701c), pushed at 621701c. Nothing is running; only app 20260626b, STOPPED.",
  "prep_landmines_2026_09_18": [
    "VM fire shell, per shell and not in the repo: export PATH=\"$HOME/bin:$PATH\" so the pinned plink1.9 v1.90b7.2 resolves (the image ships only `plink`; never shim PLINK 2.x).",
    "Run the runbook's STEP 9d checks in the SAME shell before any fire: python3 -V (measured 3.10.19 at /opt/conda/envs/jupyter/bin/python3; the RAM-1 launcher needs >= 3.9), the launcher smoke (expect plink's banner plus a (wall, peak) tuple ~0.009 GiB), and the SIGCHLD bit - test ONLY 0x10000 of SigIgn, never the whole mask (measured 0000000001380000 = SIGCHLD clear = GREEN; the extra bit is SIGXFSZ and is harmless).",
    "Stage C fire command is FORM B: `timeout 312h nohup python3 …` (survives SIGHUP to pid and group and keeps the cap, rc 124). `nohup timeout …` DIES.",
    "A region already carrying a panel-TSV row is NOT skipped by that row - the resume guard checks for a banked .npz in the bucket. But append_panel_row dedups FIRST-ROW-WINS, so if the ceiling is ever raised for 00071 its stale deferred row would survive a later ok row."
  ]
}
