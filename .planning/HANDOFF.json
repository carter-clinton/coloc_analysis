{
  "version": "1.0",
  "timestamp": "2026-08-12T13:10:00Z",
  "phase": "m3",
  "phase_name": "m3-aou-afr-ld-panel-build",
  "phase_dir": ".planning/phases/m3-aou-afr-ld-panel-build",
  "wave": "SESSION CLOSE 2026-08-12. One continuous arc since resume on 2026-08-11: FIVE validated quick tasks + a five-way adversarial review. (1) 260811-oku — E-2 disclosure drafts, both framings, decision surface; (2) 260811-pmv — SR4-OPEN evidence dossier (NEVER-FROZEN, all drift traceable); (3) 260811-rcw — m3-04c Task 3 PRE-FIRE gate review, 20/20 L-checks, suites 902/31/0 + 136/1/0 re-run once; (4) 260811-tf3 — Carter DECIDED both open items: E-2 framing = B/CORRECTION (DEC-2026-08-11-e2-framing-correction, obligation 3 DISCHARGED) and SR4-OPEN = NEVER-FROZEN (DEC-2026-08-11-sr4-disposition); (5) Carter then ordered a comprehensive adversarial review (Codex CLI v0.141.0 read-only + 4 blind investigators + /assess-blast-radius) which found 5 blockers -> quick-260812-09a REMEDIATED 21/22 findings (1 reported-with-reason: STATE.md:15 is inside the frontmatter fence), DEC-2026-08-12-adversarial-review-remediation. NET STATE: v2 disclosure pair (260812-09a-SELECTED-PAIR-correction-v2.md) SUPERSEDES tf3's v1 — place/post ONLY v2; the rcw PRE-FIRE review is CORRECTED IN PLACE and is the fire surface; ZERO Carter decisions outstanding. NEXT = Carter's external actions with the v2 texts (journal-policy pre-placement check -> place ms-correction-v2 -> post osf-correction-v2 + record URL/timestamp in osf_deviations.md), then m3-04c Task 3 (the terminal fire gate — NEVER an agent).",
  "headline_2026_08_04": "⛔⛔ m3-04c IS HALTED — ITS HEADLINE must_have IS FALSE AS SPECIFIED. The m3-04b blast-radius sweep (4 independent read-only investigators + orchestrator verification, $0, no perimeter) found the 2026-08-03 panel-unreachability diagnosis is RIGHT BUT ONE LAYER TOO SHALLOW. BLOCKER-1: `{input.ld_matrix}` IS NEVER REFERENCED IN run_finemap's SHELL BLOCK. The R script gets only `--ld-dir {params.ld_dir}` + `--region {params.region_id}` and RECONSTRUCTS its own path at run_susie_rss.R:124-127 as file.path(ld_dir, ancestry, region_id.rds) — ancestry is 'AFR', NEVER 'AFR_aou', and NO rule promotes AFR_aou/*.rds into AFR/ (grep over every .smk + Snakefile = zero hits). On a miss it falls SILENTLY to an identity matrix (run_susie_rss.R:472-474). PROVEN EMPIRICALLY: with the AoU panel present on disk and correctly resolved, resolve_ld_path returns .../AFR_aou/m2_region_00067.rds (exists=True) while SuSiE opens .../AFR/FTO_16q12.rds (exists=False). SO m3-04c T1's ONE-ARGUMENT CHANGE CANNOT MAKE THE PANEL READABLE — the fire would STILL bank a panel nothing reads. This defect is PRE-EXISTING (m3-W3-T2 wired resolve_ld_path into input: only); m3-04b aggravated only the BELIEF, via its docstring. BLOCKER-4 (independent, autonomous, $0): a PARTIAL Stage-A rollup makes assemble_occlusion_catalog.py:352-359 IGNORE EVERY EXCLUDELIST and stamp the result 'stage_a_manifest' — the override fires when the rollup is merely NON-EMPTY, not COMPLETE, and run_native_ld_panel.py:821-831 already swallows the per-region manifest-append exception that creates that state. There is NO completeness check against 276 anywhere. Result = orphaned variants wearing a clean provenance stamp. ⇒ NEXT = a CARTER DECISION on the BLOCKER-1 remedy, then REPLAN m3-04c. DO NOT execute m3-04c as written; DO NOT fire.",
  "status": "CLOSED 2026-08-12. Zero Carter decisions outstanding; both 2026-08-11 decisions RECORDED and byte-unmoved through the remediation. Remaining work is Carter's alone: E-2 obligations (1) manuscript placement and (2) OSF posting — USING THE v2 PAIR ONLY (the tf3 v1 pair misstated the mechanism/remedy/scope and is superseded; see DEC-2026-08-12) — then m3-04c Task 3 via the CORRECTED rcw PRE-FIRE review. K-2 stays declined; OSF Check-2 amendment-update obligation still open (unchanged this arc). Nothing running, $0, zero perimeter contact across the entire arc; origin == local at close.",
  "no_fire_running": true,
  "nc_state_cost": "$0 — NOTHING running server-side or NC-State-side; ZERO perimeter contact this entire session. No LSF jobs. The only compute spent was local: two full tests/m3 runs (~14 min each) plus a read-only R sweep over 618 region files.",
  "verified_this_session_firsthand": [
    "tests/m3 902 passed / 31 skipped / 0 failed AND tests/phase2 136 passed / 1 skipped / 0 failed — I ran BOTH suites myself, TWICE across the session (after pd3 and after sr4), not taken on any executor's report. Skips held at exactly 31 and 1 throughout.",
    "K-3 CLOSED: run_susie_rss.R:1018-1019 reads 1,909/1,900; grep for the stale 1,944/1,935 returns 0.",
    "The sr4 acceptance property, checked by hand: bytes DIFFER vs bf04199 while strip_to_code() output is IDENTICAL, and R_CODE_REF is still bf04199 — i.e. the comment fix provably did not move the pin.",
    "Track A containment: .planning/amendments/ and results/ untouched since 1b5b8c6; the four md5 locks (558fca45, 462ada6a, 8255c1ac, a041eecc) all present; DECISIONS.md has 0 deleted lines (append-only).",
    "The 9 extra test files touched by sr4's bucket gate are comment-only: 0 deletions and 0 non-comment additions in every one.",
    "⚠⚠ RETRACTION — the earlier claim 'All 7 pinned files 0-line diff vs bf16289' is FALSE. bf16289 appears in ZERO places across src/, tests/, config/, Snakefile (nothing ever enforced it) and 5 of the 8 files had MOVED: occlusion_manifest.py +46/-8, occlusion_present_rate_scan.py +154/-21, drop_occluded_from_sumstats.py +97/-24, ld_npz_to_rds.R +313/-62, pipeline.schema.yaml +119/-0. Only plink_ld_to_npz.py, condition_ld_matrix.py and occlusion_span_filter.py are genuinely 0-diff — those three are NOW GATED FOR REAL. Do not repeat the retracted claim.",
    "E-2 exposure measured with the SHIPPED ld_allele_join_indices() over the 207 real catalogs. Track A's five coloc regions: CXADR 0.06%, MC4R 0.07%, SH2B3 2.74% (anchor tiles 0.00%, tile 3 20.33%), APOL1 18.41%, FTO 23.80%; pooled 5.29%. Corpus per-region median 17.82%, max 38.68%, 195/206 affected.",
    "⚠ I MADE AND CORRECTED A 100x ERROR THIS SESSION: an interim report gave SH2B3 tile 3 as '0.20%' when it is 20.33% (ratio 0.2033 read as a percent) — in the REASSURING direction, inside the exact claim option A was proposed on. Corrected in deferred-items.md, DECISIONS.md and the commit message.",
    "⚠ The E-2 measurement substrate is an IDENTITY-LD STUB tree: use_identity TRUE, R NULL, status variants_exceed_threshold, and EUR/AFR/TRANS are BYTE-IDENTICAL (md5-verified on two regions). Counts hold for VARIANT BOOKKEEPING only; a real non-identity panel is NOT verified to carry the same variants frames."
  ],
  "resume_on_reconnect": [
    "▶ #0 (2026-08-12 — SUPERSEDES every earlier #0). NOTHING RUNNING. $0. ZERO perimeter contact (no gsutil, no gcloud, no bq, no wb — not even read-only control-plane). Nothing was fired. origin == local (86bba5f docs commit pushed at close; the 09a work commits below are all on origin) (20ffe03 = the v2 E-2 disclosure pair + its harness; 8c247bc = the corrected 260811-rcw PRE-FIRE gate review + its harness; plus this record-surface sweep). ⚠ ZERO CARTER DECISIONS OUTSTANDING, and this time the two that were outstanding are RECORDED: DEC-2026-08-11-e2-framing-correction (E-2 framing = B, CORRECTION) and DEC-2026-08-11-sr4-disposition (SR4-OPEN answered: NEVER ACTUALLY FROZEN for all five drifted files). Neither is reopened. E-2 OBLIGATIONS: (3) framing — ✅ DISCHARGED 2026-08-11. (1) the manuscript paragraph and (2) the OSF record entry — ⛔ still UNDISCHARGED, and both are CARTER'S EXTERNAL ACTIONS; no agent posts to OSF, edits a manuscript file, or edits the body of a posted amendment. (2) discharges only at posting AND at recording the resulting URL + timestamp in .planning/osf_deviations.md. ⚠ THE CURRENT OUTGOING TEXT IS THE v2 PAIR: .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-SELECTED-PAIR-correction-v2.md, gated by 260812-09a-check-v2-pair.sh. The 260811-tf3 v1 pair and the 260811-oku drafts are SUPERSEDED HISTORY — do not place or post them. v1 misstated which join was defective, committed to already-shipped work, and mis-scoped its re-report to an ancestry with no results. ⚠ THE FIRE SURFACE IS THE CORRECTED rcw REVIEW: .planning/quick/260811-rcw-assemble-the-m3-04c-task-3-pre-fire-gate/260811-rcw-PRE-FIRE-GATE-REVIEW.md — its liveness-poll command was DOUBLE-PREFIXED and would have FALSE-PASSED its own pre-fire check and read a healthy fire as dead; corrected at all three points of use on 2026-08-12. NEXT = Carter's two external E-2 actions, or m3-04c Task 3 — the terminal fire gate, ~11 days, $385-1,084. AN AGENT MUST NEVER FIRE IT.",
    "▶ #0-PRIOR (2026-08-07 — SUPERSEDED by the 2026-08-12 entry above; kept in place, not deleted). ⚠ EVERY STATUS CLAUSE BELOW IS STALE: the LIMITATION-vs-CORRECTION question was ANSWERED on 2026-08-11 (DEC-2026-08-11-e2-framing-correction, framing B) and obligation (3) is DISCHARGED. NOTHING RUNNING. $0. No perimeter contact. origin == local == b02707a. E-2 DECIDED as option A; ZERO Carter decisions outstanding. ⚠ BUT THREE E-2 OBLIGATIONS ARE UNDISCHARGED (manuscript limitation naming APOL1_22q12 18.41% and FTO_16q12 23.80%; OSF record; and the open LIMITATION-vs-CORRECTION question). NEXT = discharge those, or m3-04c Task 3 (the terminal fire gate — NEVER an agent).",
    "⚠ #1 — BEFORE CITING ANY RESULT: read `analysis_change_to_disclose` and `inert_but_correct` in this file. Two closures (E and G) are correct WIRING that is INERT today, and any AFR figure regenerated after o7o is not comparable to one produced before it. Both were disclosed deliberately; do not let a summary flatten them into \"closed\".",
    "▶ #2 — m3-04c Task 3 is the terminal Carter gate: PRE-FIRE checklist, the allow_degraded decision, the region-1 gate, the ~11-day / $385-1,084 billed fire, then egress. AN AGENT MUST NEVER FIRE IT. Liveness arbiter = the GCS .npz object listing climbing to 276 — NOT the kernel light, NOT _SUCCESS. .npz currently 0/276. The AoU Cloud Analysis VM (n1-standard-32, holds /home/jupyter/afr_cohort) is STOPPED-not-deleted and preserved for exactly this.",
    "⚠ #3 — BLOCKER-D IS STILL PARTIAL AND BOUNDS WHICH REGIONS CAN CONVERT. The .npz ld key is dense float32 from the FROZEN plink_ld_to_npz.py, so one dense copy is unavoidable on read: 22.8 GB (SH2B3 __sub14, the only feasible one, on a big-memory node), 67.3 GB (MC4R), ~553 GB (FTO/HLA). The large classes now FAIL FAST at m3_convert_max_n_var=120000 instead of OOM-killing. A sparse .npz is a producer-side change to a frozen file.",
    "⚠ #4 — PROCESS FINDING WORTH MORE THAN ANY SINGLE FIX: SEVEN assertions in this arc turned out STRUCTURALLY INCAPABLE of doing their stated job, each caught by a different reviewer. The seventh was one WE WROTE OURSELVES ONE TASK EARLIER (a whole-file finemap.smk pin against a fixed SHA — green exactly once, red forever after). Freshly-authored guards get LESS scrutiny than inherited ones because they feel owned, but they have been exercised less, so they deserve MORE. Two new memories: [[feedback_fixed_sha_whole_file_pin_is_a_timebomb]] and [[feedback_negative_control_defeated_by_bytecode_cache]]."
  ],
  "completed_this_session": {
    "quick-260805-o7o (10c14f2, 64f420a, dc4bbd2, fb839a4, ba1fa43)": "FINDINGS H + I — discharged \"Trusting any AFR fine-map result\". H: the panel keys chr:pos:ref:alt while harmonized AFR sumstats carry chr:pos, so the SNP_ID branch matched ZERO and 100% of the join fell to a CHR:POS first-hit with REF/ALT IGNORED. Now a two-4-key matcher (exact + transpose) with duplicate panel keys removed from the match TABLE before matching, so multiallelics are disambiguated BY CONSTRUCTION. ★ The scientific call was FLIP z, NOT DROP — both sides code on ALT (plink --keep-allele-order makes .bim A1 = ALT; harmonize_sumstats.py signs BETA to ALT), so a transposed pair is legitimate data with a bookkeeping defect. Reported BETA/SE NOT flipped. Palindromes DROPPED on a panel-specific ground: ld_npz_to_rds.R lifts GRCh38->GRCh37 carrying REF/ALT VERBATIM, never complementing, so a strand-inverted block is a detectable mismatch for ordinary variants but a SILENTLY SIGN-WRONG EXACT MATCH for palindromes — the only undetectable class. I: finemap_summary.tsv carried only the CONSTANT ld_dir, so no manuscript table could tell an AoU row from a 1kG row; 14 fields APPENDED, json_error dict kept key-for-key parity. Verifier 7/7; suite 584 -> 641.",
    "quick-260805-w7u (2563451, 1815bfd, 533c154)": "FINDING E — discharged \"Any GWAS x QTL colocalization\" AS WIRING (see inert_but_correct). qtl_coloc.smk had ZERO crosswalk/resolver references and fed coloc::runsusie the legacy 1kG panel while the GWAS fit used AoU. ★ Closing E ALONE would have traded \"silently wrong LD\" for \"silently NO LD\": run_qtl_coloc.R has its own key derivation, and an incompatible overlap exited 0 through FOUR STACKED LAYERS so Snakemake marked every such job DONE. Measured, not argued: coloc::check_dataset PASSES a sparse matrix because it never calls is.matrix(); coloc:::check_ld is what stops, inside a tryCatch that writes a status JSON and quits clean. NOW rc 1 with LD_JOIN_FATAL and no JSON. ⚠ Two orchestrator-brief errors corrected by the planner and re-derived twice: build_ld_rownames is a NO-OP on the AoU path (the .rds carries dimnames so the %||% chain short-circuits), while the legacy 1kG .rds sets dimnames NULL — WHICH IS EXACTLY WHY EUR COLOC WORKS TODAY. Verifier 8/8; suite 641 -> 745.",
    "quick-260806-b77 (9c0c67b, d8cfa53, 4c7bebe, 13b82ef, 2a7c1b6)": "FINDINGS G, J, L, M closed; K deferred as K-1. G: build_ld_rds_aou_eur was RETIRED and no rule outputs EUR_aou/*.rds, yet the TRANS chain still listed TRANS_aou_eur -> EUR_aou/{region_id}.rds FIRST, and strict_aou_only guarded with source.endswith(\"_aou\") which \"TRANS_aou_eur\" fails — so strict mode could not catch it even switched ON. Now token-based. ⚠⚠ TRANS_aou_eur was deliberately KEPT: deleting the orphan leaves TRANS with NO AoU entry and makes strict_aou_only STRUCTURALLY UNABLE to ever flag it — deletion DEEPENS the silence. Config edit is COMMENT-ONLY, proven by parsed-YAML equality. J: the receipt printed \"ld_matrix None\" on the early-exit paths, indistinguishable from a regression, and fires on real inputs (HLA_6p21, PYHIN1_1q23). L: the committed crosswalk was produced by no rule and read by no test; ⚠ the remedy is deliberately NOT a Snakemake rule (the crosswalk is read at MODULE SCOPE to build the DAG — chicken-and-egg), so it is a committed-vs-rebuild drift test plus a coverage-aware WARN. M: allow-list contained instead of deny-listing unmapped. Verifier 19/19 (it reverted three sources LIVE to watch tests go RED); suite 745 -> 807.",
    "quick-260806-pd3 (bf04199, 9538af0, 672ee27)": "FINDING K — closed the last PARTIAL gate row, \"Publishing the panel provenance\". Deleted ONE line (`variant_catalog_fallback <- TRUE`) from the Path-2 brace block of the frozen run_susie_rss.R, restoring the key's legacy meaning (the Path-1 AFR empty-subset revert). STRENGTHENED the pre-existing test that mandated that exact line, from `in branch` to `not in branch`. Re-pinned the whole freeze cascade at bf04199. ⚠ The b77 decoder ring BROKE under the change and was fixed in the same window: a real Path-2 revert emits false+true, which the old decoder rendered `none` — FALSE, Path 2 DID fire — and now renders the new `path2_ld_overlap_zero_RETRY`, deliberately NOT a prefix of `..._NO_NUMERIC_CAUSE`. ⚠ The census was corrected TWICE more: the executor found results/legacy/region_analysis is a SYMLINK that `grep -r` will not follow (so the planning-time 44 was wrong), and the verifier then found the replacement 1,944 was scoped too wide (35 non-region files). Correct = 1,909 carrying the key, 1,900 false, 9 true, all 9 genuine AFR Path-1 reverts; reconciles 1,909+687=2,596. No number moves, no science changes, Track A untouched.",
    "quick-260806-sr4 (98e0ee9, 656529a, c04e672, 5f0520b)": "RESCOPED the source freeze from BYTES to CODE and CLOSED K-3. Triggered by Carter's question \"why is anything frozen?\". Built tests/m3/source_freeze.py — a comment-insensitive, string-literal-aware code pin. THREE DISCOVERIES worth more than the change itself: (1) bf16289, the SHA the handoff claimed pinned 7 files, has ZERO enforcement anywhere and 5 of those 8 files had MOVED — retracted above; (2) the repo already held NINE ad-hoc comment-strippers, two in the very file being edited, so this is a consolidation, not an invention; (3) the plan-checker's multi-line fixture exposed a REAL BUG in the masker — masking the newline inside a string dropped every subsequent line, and `len(masked) == len(text)` is STRUCTURALLY BLIND to it, so the length invariant alone would never have caught it. The plan took THREE adversarial rounds (two full check cycles plus an orchestrator constrained-fix pass landing 13 more fixes); the checker's own fixes twice introduced new defects, which is why the last pass forbade adding anything."
  },
  "executor_deviations_accepted": [
    "THE PLAN WAS FACTUALLY WRONG TWICE, both blocking, both fixed: (1) envs/m3-r-ld.yml does NOT carry pandas (the plan asserted it does; ModuleNotFoundError verified in the built env) — added pandas>=2.2 THERE rather than pyliftover to python_stats.yml, because python_stats.yml backs a dozen already-built rules and re-hashing it forces a pipeline-wide rebuild; (2) src/snakemake/schemas/pipeline.schema.yaml ends in additionalProperties:false and the plan never mentions the schema, so adding occlusion_lockstep: would have broken EVERY Snakemake invocation at validate(). Schema entry added, following the ld_panel precedent already in that file.",
    "ONE EXISTING TEST EDITED — test_finemap_loader_contract.py::test_production_boundary_documented. It asserted 'SUPERSEDED-PENDING-REPLAN' in smk; m3-04b makes that token's ABSENCE an acceptance criterion, so no implementation satisfied both. REPLACED, NOT RELAXED: all original assertions retained, plus three NEW ones (m3-04b, m3-04c, occlusion_lockstep must be named). I READ THE DIFF AND CONFIRMED THIS. The token pinned mutable project state, not a contract, and the replan landed. Precedent 1a9d170.",
    "test_params_region_id_is_untouched PASSED AT BIRTH and never failed — correctly labelled a GUARD RAIL for m3-04c, not a RED, in both the commit and the SUMMARY. Honest RED accounting: T1 10/10 genuine, T2 13/14 genuine.",
    "The full-workflow --dry-run --quiet acceptance criterion is UNSATISFIABLE and always was: data/processed/ld_reference/ does not exist, so resolve_ld_path raises 'No LD panel found for FTO_16q12 AFR'. Proven PRE-EXISTING by running git show HEAD:Snakefile at 3e7a01a (identical error, identical ld_panel.py:94). Substitute evidence accepted: both new targets dry-run to exit 0 with no cycle, and both resolvers were exercised against the real config (3 AFR repointed, 5 EUR+TRANS byte-identical).",
    "bgzip: Snakemake built NO conda env this session (dry-run doesn't). The unit test uses a PATH-installed shim since htslib is not in smoke_dev — stated in the suite docstring, chosen over a skip. Proved out of band with bgzip (htslib) 1.21: output is genuine BGZF (BC\\x02\\x00), tabix -f -S 1 -s 1 -b 2 -e 2 indexes it, and a range query round-trips real rows."
  ],
  "gates": {
    "osf_pre_registration": "⚠ OVERRIDDEN 2026-08-03 by Carter for the Check-2 redefinition, logged as a DISCLOSED deviation (3e7a01a, .planning/osf_deviations.md). The prior occlusion-exclude amendment-update (GUID trsx5, posted 2026-07-10) remains CLEARED and unaffected. TWO OBLIGATIONS SURVIVE: draft+post the Check-2 amendment-update; and until it exists, NO redefined Check 2 result may be cited as 'passed' anywhere.",
    "m3_07": "✅ CODE-COMPLETE (07a+07b+07c). Unchanged this session.",
    "m3_04b": "✅ COMPLETE 2026-08-03. Both tasks landed; tests/m3 444P/31S/0F independently re-verified by the orchestrator. ⚠ CORRECTED 2026-08-12 (quick-260812-09a, C-HIGH): this field previously ended with the clause 'all 7 pinned files 0-line diff', which is the RETRACTED claim. It is STRICKEN here rather than left standing, because this is a LIVE status field and not a dated historical block — correcting it is not history-rewriting. See verified_this_session_firsthand[5] for the retraction and DECISIONS.md:1474 for the do-not-repeat rule; DEC-2026-08-11-sr4-disposition then answered SR4-OPEN as NEVER-FROZEN for all five drifted files. bf16289 appears in ZERO places across src/, tests/, config/ and Snakefile — nothing ever enforced it.",
    "m3_04c": "⚠ STALE 2026-08-12 — superseded by HANDOFF `suite_baselines` (902/31/0, 2026-08-07) and by the corrected 260811-rcw PRE-FIRE GATE REVIEW §2/§2.1(2). The 548P/31S/0F figure below is a 2026-08-05 SNAPSHOT and must not be quoted as current. Body preserved verbatim: ◐ EXECUTED (Tasks 1a/1b/1c/2, 14 commits) and SOUND IN DIRECTION, but BLAST-RADIUS SWEPT 2026-08-05 -> 4 BLOCKERS OPEN. See m3-04c-BLAST-RADIUS.md §gate binding. Task 3 NOT started (terminal Carter gate). Suite 548P/31S/0F, independently re-verified — and every blocker is invisible to it.",
    "panel_reachability": "⚠ STALE 2026-08-12 — superseded by the corrected 260811-rcw PRE-FIRE GATE REVIEW §2 (Layer A crosswalk PRESENT AND CORRECT, L-18; Layer B `--ld-file` read path IMPLEMENTED AND ENFORCED, L-11/L-12/L-13/L-14) and §2.1(1). Body preserved verbatim: ⛔ OPEN AND DEEPER THAN DIAGNOSED. The 2026-08-03 finding (REGION_SAFE_TO_ID has no curated->M2 crosswalk) is REAL and the crosswalk is still NECESSARY — verified: my independent reimplementation reproduced the plan's 12-row oracle EXACTLY, incl. SH2B3_12q24 -> m2_region_00040__sub00 from 18 containing candidates. But it is NOT SUFFICIENT: input.ld_matrix is never referenced in the shell block, so run_susie_rss.R reconstructs file.path(ld_dir, 'AFR', region_id.rds) and can never reach AFR_aou/, falling silently to identity. A THIRD change is required that m3-04c does not contain. CARTER'S DECISION.",
    "aou_loop_refire": "⚠ STALE 2026-08-12 — superseded by the corrected 260811-rcw PRE-FIRE GATE REVIEW §2/§5: panel reachability is no longer a blocker; what remains is Carter's PRE-FIRE 1/1b/2/3 sequence and the STEP A region-1 gate. Body preserved verbatim: ⛔ BLOCKED on panel reachability (new), the stale gs:// panel-TSV rotate, the manifest-egress gap, the real-.bim validation, and a region-1 re-run. COSTS MONEY (~11 days, ~$385-1,084). CARTER'S TRIGGER.",
    "validation_4check": "⛔ ALL FOUR validation/check_*/ dirs EMPTY — the protocol has NEVER been run, and AOU-LD-PIPELINE.md §9 calls it 'a hard gate for promoting the pipeline from dev to production'. Check 2 REDEFINED (2a code-path equivalence vs plink on public 1000G; 2b AFR-vs-1000G REPORTED NOT THRESHOLDED; 2c EUR_ukbb_pub vs 1000G EUR at r>=0.90) under the operator override.",
    "m3_06": "HELD. condition_ld_matrix.py FROZEN, never fed to a fit. NaN→0 DEAD. m3-04b imports neither condition_ld_matrix nor nan_to_num — verified.",
    "blocker1_ld_read_path": "⚠ STALE 2026-08-12 — superseded by the corrected 260811-rcw PRE-FIRE GATE REVIEW §2.1(1): 'NOT YET IMPLEMENTED' is FALSE today, corroborated by measurement (L-11/L-12 present, L-13 8P/0S, L-14 22P/0S). Body preserved verbatim: ✅ DECIDED 2026-08-05, ⛔ NOT YET IMPLEMENTED. Remedy = thread {input.ld_matrix} into run_susie_rss.R behind a new --ld-file (fallback to the existing --ld-dir rebuild retained so no caller breaks). Re-verified firsthand: run_finemap's shell: passes ONLY --ld-dir {params.ld_dir} and --region {params.region_id}; repo-wide the sole shell: consuming {input.ld_matrix} is qtl_coloc.smk:316 (a DIFFERENT rule); run_susie_rss.R:125-127 rebuilds file.path(ld_dir, ancestry, region_id.rds) with ancestry==\"AFR\", never AFR_aou, and falls SILENTLY to an identity matrix on a miss (:472-474). ACCEPTANCE TEST: prove resolved == what-the-script-opens (grep the rule's shell: for {input.ld_matrix}, then assert the R script opens that exact path). A green DAG is NOT evidence.",
    "blocker4_partial_rollup": "✅ CLOSED 2026-08-05 (fac9a93). Excludelist-only orphan regions now RAISE instead of being silently discarded under a clean stage_a_manifest stamp; explicit allow_partial_manifest opt-in. ⚠ n_regions == 276 is a FALSE INVARIANT and is deliberately NOT asserted — both the excludelist write (run_native_ld_panel.py:806-808) and the manifest append (:821-824) sit inside `if occluded_ids:`, so a zero-occlusion region legitimately writes NEITHER artifact. Coverage is REPORTED, never asserted. ⚠ config/ld_regions.tsv is 552 data rows = 276 unique region_id x 2 ancestries.",
    "kn_publication_blockers": "✅ CLOSED 2026-08-05 (3bb8783 + bf963df + fac9a93): D-04b-01 (float POS), HIGH-0 (guard could not fire), HIGH-4 (no unparseable counter), LOW-1 (stroke double-count, VISIBILITY ONLY — de-duplicating would move k AND redefine n 9->8, i.e. move a pre-registered number; the denominator fork is logged to deferred-items.md for Carter, NOT taken). Measured on the real corpus: 7 of 9, bmi now present.",
    "track_a_eur_numerics": "✅ CLOSED 2026-08-05 (aeed8c0 + 51a60ca + ab19186), END-TO-END. ld_read_path.ancestries allow-lists BOTH the crosswalk and --ld-file. Off the list the declared file is INVISIBLE, so EUR/TRANS read exactly what they read at 3f431ab — proven by identical() on the ENTIRE matrix object, plus permanent in-suite negative controls that recover the pre-change loader via `git show 5ec33bd:` and assert the DEFECTIVE outcome. Kill switch: ld_read_path.ancestries: [].",
    "blocker_a_ld_file_authoritative": "✅ CLOSED 2026-08-05 (51a60ca + ab19186). Declared panel is the SOLE candidate when authoritative; hard stop() on unreadable/use_identity/below-min-use; best_partial keeps the BEST overlap not the LAST. Tests run at REAL config/susie_policy.yaml thresholds with a guard asserting they are the production values.",
    "blocker_c_nothing_builds_the_rds": "✅ CLOSED 2026-08-05 (6643c19) as ONE OPERATOR COMMAND: `snakemake m3_convert_aou_afr_rds_all` (276 ids derived from the manifest, 575-job DAG). ⚠ DISCLOSED RESIDUAL: `snakemake all` is still NOT self-sufficient — resolve_ld_path returns the first EXISTING path, unchanged and out of scope.",
    "blocker_d_converter_oom": "◐ PARTIAL 2026-08-05 (57b381f). Dense `ld` dropped from the .rds; read block-bounded (0.351x maxRSS on real banded panels; 0.809x on a dense one — shape-dependent); mem_mb 64000; ceiling m3_convert_max_n_var=120000 fails fast. ⛔ NOT closed for MC4R / FTO / HLA: the .npz ld key is dense float32 from the FROZEN plink_ld_to_npz.py:253 (~553 GB at n≈372k). Needs a sparse .npz = a producer-side change on a frozen file.",
    "blast_radius_gate_ledger": {
      "nothing_already_merged": "CLEAR",
      "re-running any EUR fit / rebuilding EUR_ukbb_pub": "CLEAR (BLOCKER-B, quick-260805-23d)",
      "the ~11-day billed fire": "CLEAR on A/B/C; ⚠ BLOCKER-D PARTIAL — only SH2B3 __sub14 (22.8 GB) convertible",
      "trusting any AFR fine-map result": "CLEAR (H+I, quick-260805-o7o, verifier 7/7)",
      "any GWAS x QTL colocalization": "CLEAR AS WIRING (E, quick-260805-w7u, verifier 8/8) — see inert_but_correct",
      "any TRANS fit": "CLEAR (G, quick-260806-b77, verifier 19/19) — see inert_but_correct",
      "publishing the panel provenance": "⚠ PARTIAL — I and J closed; K DEFERRED as K-1 (needs TWO Carter authorizations)",
      "growing the curated region set": "CLEAR (L+M, quick-260806-b77)"
    }
  },
  "cluster": "None running. The AoU Cloud Analysis VM (n1-standard-32, holds the /home/jupyter/afr_cohort bfile) is STOPPED, not deleted — preserved for the fire. Leave it stopped; no fire is imminent and the two remaining decisions are NC-State code decisions.",
  "data_state": "UNCHANGED this session — no perimeter contact. AoU AFR native-plink LD panel does NOT exist: bucket .npz = 0/276. data/processed/ld_reference/ does not exist AT ALL on NC-State, so resolve_ld_path returning \"the first EXISTING path\" currently has nothing to return — which is why several of this session's fixes are correct-but-inert wiring.",
  "do_not": [
    "⛔ AN AGENT MUST NEVER FIRE THE ~11-DAY BILLED LOOP. It is Carter's terminal gate ($385-1,084).",
    "⛔ DO NOT cite finding E as \"safe in production\" — the gate is safe today only because ZERO AFR QTL-coloc jobs exist at all (E-4). It is machine-verified WIRING, not a production-exercised fix.",
    "⛔ DO NOT cite finding G as making TRANS work — it makes TRANS's failure VISIBLE. TRANS still resolves to the 1kG EUR panel.",
    "⛔ DO NOT DELETE TRANS_aou_eur from config/pipeline.yaml. It looks like dead config; deleting it makes strict_aou_only structurally unable to ever flag TRANS. Likewise do NOT remove EUR_aou from the EUR chain (test_ld_panel_resolver.py:184 calls eur_sources.index(\"EUR_aou\") and would ValueError), and do NOT reach for pin.TRANS — pin short-circuits AHEAD of strict mode.",
    "⛔ DO NOT claim BLOCKER-D closed for MC4R / FTO / HLA — the .npz is dense float32 from a FROZEN producer (~553 GB read).",
    "⛔ DO NOT unfreeze run_susie_rss.R's CODE without Carter — CODE-frozen at bf04199 (R_CODE_REF). Since 2026-08-06 a COMMENT-ONLY edit is FREE and needs no authorization at all (that is the point of the rescope, and K-3 was its proof). A CODE edit still needs a named unfreeze + a re-pin of R_CODE_REF alone.",
    "⛔ DO NOT publish any AFR coloc number before E-2 is decided — 25.3% of matched variants required an orientation flip.",
    "⛔ DO NOT report a PARTIAL gate row as CLEAR. \"Publishing the panel provenance\" is PARTIAL until K-1 lands.",
    "⛔ DO NOT trust a green suite as evidence about a guard you just wrote — seven assertions in this arc could not fail, and the seventh was ours from one task earlier. Every new assertion needs a negative control you have SEEN go red.",
    "Do NOT revive NaN->0 or feed condition_ld_matrix.py to any fit — m3-06 STAYS HELD.",
    "Do NOT git add -A / . on this GPFS tree — explicit paths only.",
    "Do NOT use worktree isolation on this GPFS tree.",
    "Do NOT commit tests/m3/sparse_parent_benchmark.tsv — a test rewrites it every suite run (timing jitter only); git checkout -- it.",
    "Do NOT re-post or EDIT the BODY of any OSF amendment (bodies are byte-locked to OSF).",
    "⛔ DO NOT quote E-2's pooled 5.29% figure on its own — it is dragged down by the two clean large regions and hides that APOL1_22q12 (18.41%) and FTO_16q12 (23.80%) sit near 20%. A fit is per-region; quote the per-region numbers.",
    "⛔ DO NOT cite the E-2 numbers as the REAL-LD exposure. Every panel measured is an identity-LD stub with R NULL; they are the catalog<->panel-frame transposition rate and nothing more until a real panel exists."
  ],
  "prep_landmines": [
    "⚠ NEW 2026-08-04 — .planning/STATE.md's YAML FRONTMATTER DOES NOT PARSE, and has not for a long time. yaml.safe_load fails at line 17 (the `last_activity` scalar) because a 2026-06 entry embeds UNESCAPED DOUBLE QUOTES inside the double-quoted string: `_normalize_bucket(os.environ[\"WORKSPACE_BUCKET\"])`. VERIFIED PRE-EXISTING — `git show HEAD:.planning/STATE.md` fails at the SAME line, so this session's edits neither caused nor worsened it. Nothing has broken because the resume workflow reads the BODY, not the frontmatter. DELIBERATELY NOT FIXED at session close: the repair means rewriting a ~34 KB historical scalar, which is not a freeze-moment edit. Fix it in a dedicated /gsd-quick (switch last_activity to a YAML block scalar `|-`, or single-quote it) if any tool ever needs to parse it.",
    "GSD tooling does NOT recognize the custom m3-NN naming: `init plan-phase m3-04` returns phase_found:false (I re-verified today). Execution = spawn gsd-planner / gsd-executor DIRECTLY on the PLAN.md (the adapted planner->executor pattern).",
    "envs/m3-r-ld.yml now carries pandas>=2.2 (added by m3-04b; it did NOT before). A fresh env build needs this.",
    "src/snakemake/schemas/pipeline.schema.yaml is additionalProperties:false — ANY new top-level pipeline.yaml block MUST get a schema entry or every Snakemake invocation dies at validate().",
    "bgzip is NOT on the login PATH and NOT in smoke_dev; it lives in envs/python_stats.yml.",
    "The m3-r-ld conda env at /rs1/researchers/c/ckclinto/conda_envs/m3-r-ld is the marker that makes the A6 stitch/loader tests RUN rather than skip. Do not delete it.",
    "tests/m3/sparse_parent_benchmark.tsv is REWRITTEN by every suite run (timing columns only). Expect it dirty after any pytest tests/m3; it is not a real change."
  ],
  "gpfs_object_store_recovery_recipe": "Did NOT fire this session (4 commits + 3 pushes clean). Prior: 3 hits in ~3 weeks. Recipe if 'invalid object / Error building trees': for each `git ls-files -s` entry test `git cat-file -e <sha>`; for misses re-hash the intact working-tree file via `git hash-object -w <path>`; then commit AND PUSH (local store unreliable).",
  "resume_entry_point_2026_08_04": ".planning/phases/m3-aou-afr-ld-panel-build/m3-04b-BLAST-RADIUS.md — READ THIS FIRST. It carries the ship/no-ship verdict, the two blockers, the gate binding table (which gate each issue actually blocks), and the revised 7-step sequence. Then this HANDOFF's headline_2026_08_04 + resume_on_reconnect #0/#0b/#0c. The m3-04c PLAN is still on disk but is HALTED and must not be executed as written.",
  "headline_2026_08_05": "★ BLOCKER-1 IS DECIDED. Carter chose remedy A: thread {input.ld_matrix} into run_susie_rss.R behind a new --ld-file argument, making resolve_ld_path the SINGLE SOURCE OF TRUTH and killing the declare-vs-read split permanently. The promote/symlink rule and the per-ancestry ld_dir were both REJECTED (each leaves a second place the AFR->AFR_aou mapping can drift — the exact failure mode that produced the blocker). Recorded as DEC-2026-08-05-m3-ld-read-path in .planning/DECISIONS.md with rejected alternatives + the acceptance test. ⇒ m3-04c no longer needs a DECISION, it needs a REPLAN. ALSO LANDED: quick-260804-rtc, all four autonomous $0 fixes (3bb8783 D-04b-01 shared integral-position coercion used by all THREE key implementations; bf963df HIGH-4 unparseable counters + HIGH-0 a total-miss guard that can actually fire; fac9a93 BLOCKER-4 excludelist-only orphans now RAISE + LOW-1 visibility-only; d12a6c7 docs). tests/m3 444P/31S/0F -> 496 passed / 31 skipped / 0 failed, independently re-run by the orchestrator (three agreeing measurements). All five touched test files PURELY ADDITIVE (0 deletions) — no existing test weakened. Four frozen contracts + finemap.smk 0-line diff. REAL-DATA PROOF over 151,223,963 rows: rs182965575 present in 7 of 9 with bmi (the 100%-float-POS file that previously scored ABSENT) now counted — that presence IS the D-04b-01 proof. No pre-registered number moved. ⛔ THE FIRE IS STILL BLOCKED: BLOCKER-1 is DECIDED but NOT IMPLEMENTED.",
  "resume_entry_point_2026_08_05": ".planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md — the 2026-08-05 top block. Then .planning/DECISIONS.md DEC-2026-08-05-m3-ld-read-path (the locked BLOCKER-1 remedy + its acceptance test), then m3-04b-BLAST-RADIUS.md §4 for the remaining sequence.",
  "headline_2026_08_05b": "⚠⚠ THE REPLAN CAUGHT A SILENT WRONG-LOCUS DEFECT IN THE CROSSWALK — the single most consequential finding of the session. m3-04c selected an M2 region by smallest CONTAINING span using start_grch37/end_grch37 from config/ld_regions.tsv. But for a SPLIT parent those two columns hold the PARENT bounding box copied VERBATIM into every subregion row (build_ld_region_manifest.py:585-587,650-653) — only the *_grch38 columns vary per subregion. So all 18 subregions of m2_region_00040 tie EXACTLY and the lexicographic tie-break returns __sub00, whose real GRCh37 window is 37,857,542-45,792,298: ZERO bp overlap with SH2B3 and ~66 Mb away. The plan would have fine-mapped SH2B3_12q24 — THE TRACK A ANCHOR LOCUS — against LD from an unrelated window. It survived a \"12/12 independently reproduced\" oracle because that reproduction re-implemented the SAME mechanism: convergent reproduction of a bug, NOT verification. FIXED: candidates are now lifted GRCh38->GRCh37 via hg38ToHg19.over.chain.gz (the ONLY chain in the repo) and ranked by CORE OVERLAP first. Oracle re-derived and INDEPENDENTLY re-implemented by the orchestrator: EXACTLY 1 of 12 moved (SH2B3 __sub00 -> __sub14, core overlap 523,169 bp vs 76,831 for __sub15), 11 unchanged, BMI_Xq24 still unmapped, 0 window-liftover failures across all 276 AFR rows. ⚠ The mechanism was structurally broken for ALL 8 split parents (123 of 276 AFR regions) — only SH2B3 hits it today, so the fix is to the MECHANISM, not a special case. A SECOND self-contradicting test was also found and proved (test_ld_panel_resolver.py:364 — [^)]* cannot cross the paren CURATED_TO_M2.get() introduces), and the first proposed fix for it would have made the assertion VACUOUS (a non-greedy regex matches from the module docstring at finemap.smk:9 and can never fail); replaced with a balanced-paren call-site extractor + a PERMANENT negative-control test, both verified by simulation to genuinely FAIL when a kwarg is stripped.",
  "headline_2026_08_05c": "⛔⛔ DO NOT FIRE, AND DO NOT REBUILD EUR_ukbb_pub. m3-04c Tasks 1a/1b/1c/2 EXECUTED and are directionally right — the AoU panel IS readable when the quality gate passes (proven end-to-end) — but a 4-agent blast-radius sweep (m3-04c-BLAST-RADIUS.md) found FOUR blockers, ALL invisible to the green 548P/31S/0F suite. (A) --ld-file is a PREFERENCE, NOT A MANDATE at production thresholds: run_susie_rss.R:216 only RETURNS a candidate that passes overlap>=50 && coverage>=0.5, else it falls through to the --ld-dir 1kG panel and reports ld_loaded;overlap_ok — BLOCKER-1s exact defect class, 60 lines down. ⚠ ALL 8 tests in test_ld_read_path.py pin MIN_LD_OVERLAP<-1L/MIN_LD_COVERAGE<-0.0, i.e. the gate is DISABLED in every one, and the orchestrators own acceptance proof did the same — so nothing in the repo can observe production behaviour. (B) ★ TRACK-A EUR NUMERICS MOVE. BLOCKER-1 was SILENTLY PINNING every EUR fit to the 1kG tail; Task 1b un-pins EUR too, not just AFR. PROVEN: EUR r[1,2] 0.1->0.9, credible sets 3->10, nonzero PIPs 200->78, while ld_status AND ld_overlap_fraction stayed BYTE-IDENTICAL — the two fields anyone would check. EUR is safe TODAY only because data/processed/ld_reference/ does not exist; building EUR_ukbb_pub (a $0 roadmap prerequisite) triggers it. TRACK A IS IN SUBMISSION. (C) NOTHING BUILDS THE .rds: resolve_ld_path returns the first EXISTING path (ld_panel.py:87), m3_convert_npz_rds.smk has no aggregate target, ALL_TARGETS names no AFR_aou/*.rds — so post-fire `snakemake all` resolves AFR to 1kG SILENTLY. (D) the .npz->.rds consumer is UNBOUNDED-DENSE: ld_npz_to_rds.R:103 materialises n x n float64 under a declared mem_mb=8000 — 45.6 GB for SH2B3s subregion (the SMALLEST target) up to ~1.1 TB for FTO/HLA; same OOM class already fixed TWICE producer-side. ALSO HIGH: qtl_coloc reads the legacy 1kG panel while the GWAS fit it colocalizes used AoU (mixed LD inside one coloc.susie); the crosswalk has NO ancestry gate; retiring build_ld_rds_aou_eur orphaned the TRANS chain HEAD and strict_aou_only CANNOT catch it (TRANS_aou_eur does not endswith _aou); the sumstats<->panel join is ALLELE-BLIND. ✅ CORRECTION TO THE PRIOR FRAMING: the SH2B3 core straddle is a NON-RISK — the panel is built over the WINDOW (start_grch38 == window_start_grch38 for all 552 rows), overlap_frac is 1.000000, no variant is missing; and there is NO stitch to bypass. No revert warranted.",
  "headline_2026_08_05d_PRIOR": "★ BLAST-RADIUS REMEDIATION LANDED (quick-260805-23d, 6 tasks, commits aeed8c0/51a60ca/ab19186/6643c19/57b381f/cd177bc). BLOCKER-A, BLOCKER-B and BLOCKER-C are CLOSED; BLOCKER-D is PARTIAL by design. Suite 548 -> 584 passed / 31 skipped / 0 failed (615 collected). (A) the declared panel is now the SOLE candidate when authoritative, and assert_declared_ld_authoritative() HARD-STOPS on unreadable / use_identity / below-min-use instead of silently falling through to 1kG with a green receipt. Hard stop() chosen over a status field deliberately: a written JSON + rc 0 makes snakemake mark the job DONE, which is the exact failure class of the sweep (the resolved==opened receipt already existed and nobody read it); stop() makes the DAG the gate and --keep-going bounds it to one region. (B) ★ TRACK-A EUR IS PROTECTED, END-TO-END. A config allow-list (ld_read_path.ancestries, mirroring occlusion_lockstep.ancestries) gates BOTH the crosswalk and --ld-file; off the list the declared file is INVISIBLE (never even file.exists()-tested) so EUR/TRANS read exactly what they read at 3f431ab — proven with the ENTIRE MATRIX identical(), not just the path. (C) m3_convert_aou_afr_rds_all enumerates all 276 AFR ids DERIVED from config/ld_regions.tsv (153 whole + 123 __sub); dry-run resolves a clean 575-job DAG. (D) the dense `ld` field is GONE from the .rds and the read is block-bounded (maxRSS 0.351x on real banded panels), mem_mb 8000->64000, with an explicit ceiling m3_convert_max_n_var=120000 that fails fast in seconds. ⚠ D IS PARTIAL AND MUST NOT BE CLAIMED CLOSED for the large regions: the .npz ld key is dense float32 from the FROZEN plink_ld_to_npz.py:253, so one dense copy is unavoidable on read — 22.8 GB (SH2B3 __sub14), 67.3 GB (MC4R), ~553 GB (FTO/HLA). SH2B3 is feasible on a big-memory node; the large classes now fail fast instead of OOM-killing. A sparse .npz is a producer-side change on a frozen file.",
  "headline_PRIOR_2026_08_05": "★ m3-04b LANDED — the occlusion catalog assembler + the lockstep consume seam are REAL CODE and WIRED (d7dfa67 T1, 0cae502/37c51df T2, f038ce0 docs; all on origin). Full tests/m3 420P/31S/0F → 444P/31S/0F (+24, zero regressions) — I RE-RAN IT MYSELF at HEAD, 07:02, not taken on the executor's report. The four m3-07 modules that had ZERO production callers now have one. ⚠⚠ THE SESSION'S MOST IMPORTANT FINDING, from the m3-04 replan: THE AFR_aou PANEL IS UNREACHABLE FROM run_finemap — REGION_SAFE_TO_ID (Snakefile:47-63) is built ONLY from config/regions_curated.csv (12 curated slugs), NOT from config/region_id_mapping.tsv (276 M2 rows), so resolve_ld_path asks for AFR_aou/FTO_16q12.rds, MISSES, and SILENTLY falls through to AFR_1kg. Landing all 276 .rds would change NOTHING and nothing would warn you. ⇒ DO NOT FIRE until m3-04c Task 1 (panel reachability) lands. NEXT = m3-04c.",
  "resume_entry_point_PRIOR": ".planning/HANDOFF.json (this block) + .planning/STATE.md 2026-08-03 ★ RESUME HERE block + .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md + the m3-04b SUMMARY (m3-04b-W4-SUMMARY.md) + deferred-items.md + the m3-04c PLAN (the next thing to execute).",
  "headline": "⚠ SUPERSEDED 2026-08-12 (quick-260812-09a, C-HIGH + C-MEDIUM). Two clauses in the 2026-08-07 body below are FALSE TODAY and are corrected here rather than left standing in an undated LIVE field. (a) 'THREE UNDISCHARGED OBLIGATIONS and one open question' -> obligation (3) is DISCHARGED (DEC-2026-08-11-e2-framing-correction, framing B) and the 'one open question' (SR4-OPEN) is ANSWERED (DEC-2026-08-11-sr4-disposition, never-frozen); only obligations (1) and (2) remain, both Carter's external actions. (b) 'corpus per-region MEDIAN 17.82%' is a UNIT ERROR: 17.82% is the TILE-ROW median (195 of 206 tile-rows affected, max 38.68%); the LOCUS median is 0.4234% (49 of 51 loci affected, max 38.6824%). Never quote a corpus figure without its unit. The CURRENT outgoing E-2 text is the v2 pair at .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-SELECTED-PAIR-correction-v2.md. || 2026-08-07 BODY, PRESERVED: ★ E-2 IS DECIDED (option A) AND ZERO CARTER DECISIONS REMAIN — but THREE UNDISCHARGED OBLIGATIONS and one open question do. The E-2 exposure was measured on the REAL corpus for the first time (the previously-quoted 46/182 = 25.3% was a SYNTHETIC FIXTURE, and the receipts the record told Carter to read CANNOT EXIST yet: the counter path is AFR-gated, AFR has zero QTL-coloc jobs per E-4, and the panel is 0/276). Measured with the SHIPPED ld_allele_join_indices() over the 207 real catalogs: corpus per-region MEDIAN 17.82%, max 38.68%, 195/206 regions affected. Over the five regions Track A's coloc numbers actually depend on: CXADR 0.06%, MC4R 0.07%, SH2B3 2.74% (anchor tiles 0.00%, tile 3 20.33%), APOL1 18.41%, FTO 23.80%, pooled 5.29%. ⚠ TWO OF FIVE ARE MATERIALLY EXPOSED. ⚠ CAVEAT BOUNDING ALL OF IT: every panel measured is an IDENTITY-LD STUB (use_identity TRUE, R NULL, EUR/AFR/TRANS byte-identical) — the counts are meaningful for VARIANT BOOKKEEPING but are NOT verified against a real non-identity panel. Nothing running, $0, tree clean, origin == local == b02707a. tests/m3 902/31/0, tests/phase2 136/1/0.",
  "analysis_change_to_disclose": "⚠⚠ FOR THE MANUSCRIPT AND OSF RECORD — STATE IT, do not let a reader find it by diffing versions: any AFR figure or table regenerated after 260805-o7o lands is NOT COMPARABLE to one produced before it. PIPs and credible sets MOVE (this is the correction the phase exists to make). Reported BETA and SE do NOT move, so no published direction of effect changes — verified: credible_sets carry the raw sumstats values with the allele-aware flag both on and off.",
  "inert_but_correct": [
    "FINDING E (w7u): the \"Any GWAS x QTL colocalization\" gate is safe TODAY only because ZERO AFR QTL-coloc jobs exist AT ALL — a PRE-EXISTING, unrelated condition (E-4: build_qtl_coloc_manifest.py:250 _ancestry_for_region returns \"EUR\" UNCONDITIONALLY, git-diff-identical at 7b1025d). What landed is correct, machine-verified WIRING — \"no longer wrong in principle\", NOT a production-exercised fix. Do NOT cite E as \"safe in production\".",
    "FINDING G (b77): makes TRANS's failure VISIBLE, not working. TRANS still resolves to the 1kG EUR panel today and after the fire; strict_aou_only:false keeps the strict half inert. L's WARN and M's allow-list are also inert on today's data (12/12 covered, 11/11 contained). The J receipt is the ONLY behavioural change b77 makes."
  ],
  "carter_decisions_outstanding": [
    "✅ E-2 — DECIDED 2026-08-07 as option A (DEC-2026-08-07-e2-orientation-disposition). Code NOT changed; the exposure is DISCLOSED. ⚠ THE EXPOSURE IS MATERIAL IN 2 OF THE 5 TRACK-A COLOC REGIONS: APOL1_22q12 18.41% and FTO_16q12 23.80%; SH2B3_12q24 tile 3 is 20.33% while its md5-pinned ANCHOR tiles 1-2 are 0.00%; CXADR_F2RL1_6p21 and MC4R_18q21 are clean at ~0.06%. DO NOT quote the 5.29% pooled figure alone. ⚠ A 100x ERROR WAS MADE AND CORRECTED IN-SESSION: an interim report gave SH2B3 tile 3 as '0.20%' (a ratio of 0.2033 misread as a percent), in the reassuring direction, inside the very claim A was proposed on. THREE OBLIGATIONS SURVIVE, NONE DISCHARGED: (1) a manuscript limitation paragraph naming APOL1_22q12 and FTO_16q12 explicitly; (2) an OSF record entry; (3) ⚠ AN OPEN QUESTION ABOVE EXECUTOR AUTHORITY — is ~18-24% in two of five coloc regions a LIMITATION or a CORRECTION? B becomes right bundled with E-4 after the panel exists; E-2 and E-4 are COUPLED. ▶ UPDATE 2026-08-11 — OBLIGATION (3) IS DISCHARGED: framing B (CORRECTION) is chosen (DEC-2026-08-11-e2-framing-correction), selecting the matched pair ms-correction + osf-correction, paste-ready at .planning/quick/260811-tf3-record-carter-decisions-e-2-framing-b-co/260811-tf3-SELECTED-PAIR-correction.md whose two bodies are proven byte-identical to their 260811-oku sources by cmp. (1) AND (2) REMAIN OPEN AND ARE CARTER'S EXTERNAL ACTIONS: (1) discharges when ms-correction is placed in the Track A manuscript, after a PRE-PLACEMENT JOURNAL-POLICY CHECK — if the target journal reads \"correction\" on an in-submission manuscript as a formal post-publication correction notice, keep B's CONTENT with A's PLACEMENT (the Limitations section), which changes placement only and reopens no framing; (2) discharges when osf-correction is posted as a NEW supplementary file on osf.io/az52u (never a new version of trsx5 or tcujq — OSF bodies are append-only) AND its URL + timestamp are recorded in .planning/osf_deviations.md. ⚠ framing B is NOT disposition option B — the code is still NOT changed and the option A disposition stands unmoved; only the framing axis moved. ⚠ POSTING MAKES A CURRENTLY-INTERNAL COMMITMENT PUBLIC: the osf-correction body commits to an allele-aware join, a real-panel re-measurement, and affected African-ancestry results regenerated and re-reported with a before/after comparison, posted as a further update whether or not conclusions change — register it as a tracked E-4 obligation at that point. ⛔ No agent posts to OSF, edits a manuscript, or edits a posted amendment body. ▶ UPDATE 2026-08-12 (quick-260812-09a) — ⚠ THE PASTE-READY PAIR NAMED ABOVE IS SUPERSEDED. DO NOT PLACE OR POST THE 260811-tf3 v1 PAIR. A five-way adversarial review found it misstated WHICH JOIN was defective (it called the catalog-to-panel join allele-blind; ld_allele_join.R:205-270 is a 4-key allele-aware matcher whose own 'flipped' counter PRODUCED the disclosed percentages, and the real disclosed property is the orientation MEASURED AND NOT APPLIED at run_qtl_coloc.R:478-479), committed to work that had ALREADY SHIPPED, mis-scoped its re-report to an ancestry with ZERO coloc jobs, dropped the manuscript paragraph's bounding sentences, and equivocated TILE units for LOCUS units. The CURRENT outgoing text is the v2 pair at .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-SELECTED-PAIR-correction-v2.md, gated by 260812-09a-check-v2-pair.sh. The framing decision itself (B, CORRECTION) is UNCHANGED and is not reopened; only the text is replaced.",
    "✅ K-1 — DECIDED 2026-08-06 (option B) and CLOSED by quick-260806-pd3 (bf04199, 9538af0, 672ee27). Kept here so the record shows it was decided, not dropped. Two FOLLOW-ONS were registered rather than silently absorbed: K-2 (the ld_allele_join.R extraction, DECLINED — it would put a first-of-its-kind runtime source() dependency on the ~11-day/$385-1,084 fire path, and freeze economy is not sufficient justification to accept fire-path risk) and K-3 (the wrong 1,944 census figure still shipping inside the re-frozen R comment at :1018-1019; correct is 1,909 — cosmetic, needs a trivial future unfreeze).",
    "✅ SR4-OPEN — DECIDED 2026-08-11: NEVER ACTUALLY FROZEN (DEC-2026-08-11-sr4-disposition). The handoff's \"frozen/pinned at bf16289\" language for the five files (occlusion_manifest.py +46/-8, occlusion_present_rate_scan.py +154/-21, drop_occluded_from_sumstats.py +97/-24, ld_npz_to_rds.R +313/-62, pipeline.schema.yaml +119/-0) was WRONG and is corrected, not defended. NO DRIFT REVIEW IS REQUIRED. EVIDENCE: .planning/quick/260811-pmv-sr4-open-evidence-dossier-were-the-five-/260811-pmv-DOSSIER.md (f78bbc1, 399c50f, 2b13dce) — bf16289 is a docs(handoff) session-close commit that changed one file and touched none of the eight (a bookmark, not an act of freezing); zero DECISIONS.md declarations for any of the five and zero bf16289 matches under tests/ src/ config/ Snakefile at every one of the 8 drift commits, measured at the drift commits themselves; all 8 drift commits traceable to a reviewed task SUMMARY; the label was COLLECTIVE and a status report, not a prohibition. ⚠ BOTH CONTRARY FACTS STAND, STATED NOT SMOOTHED: for F1-F4 that collective label (2bda675, 2026-08-03) DID predate the drift — genuine contrary evidence, bounded by three things (a status report not a freeze instruction; a handoff narrative not the decision register; enforced by measured zero at each drift commit); and F5 (pipeline.schema.yaml) was NEVER in the \"7 pinned files\" roster at all and its only label postdates all of its drift, so there is nothing to review. CONSEQUENCES: no drift review; the 3 genuinely-0-diff files (plink_ld_to_npz.py, condition_ld_matrix.py, occlusion_span_filter.py) stay gated by tests/m3/source_freeze.py at PY_CODE_REF; NO NEW PIN is created — none of the five is added to PY_FROZEN_RELS and test_the_handoff_frozen_claim_is_recorded_as_partly_false stays green asserting they are OUT; any future pin is its own task costing a recorded decision, a #: bucket annotation and a negative control OBSERVED red (and a YAML stripper source_freeze.py does not have, for F5). ⚠ LIMITS: this is a statement about process and intent, NOT a code review of the drift; the narrative first-appearance dates are LOWER BOUNDS (unreadable git objects skew old), but recovering an earlier label would not move the disposition. ⚠ RESIDUAL LIVE SITES, a registered follow-up OUT of 260811-tf3's write scope: .planning/STATE.md:15 and .planning/ROADMAP.md:1077 still assert ld_npz_to_rds.R is frozen/unchanged when it is +313/-62. ⚠ The dated historical '>' blocks in STATE.md and .continue-here.md are NOT correction sites — rewriting them would destroy the record this disposition rests on. ▶ UPDATE 2026-08-12 (quick-260812-09a) — THE RESIDUAL LIVE SITES ARE DISCHARGED, ONE OF THEM BY DISCLOSURE RATHER THAN BY EDIT. .planning/ROADMAP.md:1077 is CORRECTED in place (and a SECOND site the file-by-file scope had missed, ROADMAP.md:213, was found by the claim-level sweep and corrected too). .planning/STATE.md:15 is NOT edited and CANNOT be: measured at HEAD, STATE.md's frontmatter runs lines 1-24 and :15 sits INSIDE that fence, so editing it would break the frontmatter byte-identity gate this project requires and whose YAML is pre-existing-unparseable. It is recorded instead as a body-side note in STATE.md's Session Continuity and in the DEC-2026-08-12 canonical stale-site table. The full claim-level hit list lives at .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-stale-claim-sweep.tsv."
  ],
  "authorizations_granted_do_not_relitigate": [
    "AUTH-o7o-01 (CARTER) — appended --ld-allele-aware to the EXPECTED_ADDED_TOKENS argv tripwire in tests/m3/test_ld_read_path_ancestry_gate.py. The widening was PAID FOR by a new direct assertion that the flag renders \"false\" for EUR/TRANS/EAS/HIS across all six degraded config shapes. The list got longer; the containment got STRICTER.",
    "AUTH-b77-01 (⚠ THE ORCHESTRATOR'S CALL, NOT CARTER'S — disclosed deliberately) — narrowed tests/m3/test_qtl_coloc_allele_join.py::test_params_region_id_is_not_declared_here from a whole-file finemap.smk pin against the FIXED SHA 7b1025d to `assert \"region_id\" not in diff_text`. Authorized under Carter's standing \"proceed autonomously\" after verifying FIRSTHAND that the 128-line diff contained ZERO region_id occurrences, params.region_id was byte-unchanged, and the PRIMARY guard rail (test_occlusion_lockstep_wiring.py::test_params_region_id_is_untouched) was GREEN and untouched. The narrowed form is STRICTLY STRONGER on the test's own stated subject. Negative control OBSERVED RED and permanent. If Carter disagrees it is a one-line revert — but note that reverting re-blocks finding J."
  ],
  "freeze_state": "⚠ RESCOPED 2026-08-06 by quick-260806-sr4 under AUTH-SR4-RESCOPE — THE FREEZE NOW PROTECTS CODE, NOT BYTES. run_susie_rss.R is CODE-frozen at bf04199, declared in exactly ONE place (R_CODE_REF in tests/m3/test_source_freeze_pins.py) and IMPORTED by both consumers, so a re-pinner updates one constant per frozen subject. Comments, docstrings, blank lines and trailing whitespace are now DELIBERATELY FREE; a CODE change still goes RED. Enforced by tests/m3/source_freeze.py (whole-file code-only FLOOR + symbol pins). ALSO NEWLY GATED FOR REAL: plink_ld_to_npz.py, condition_ld_matrix.py, occlusion_span_filter.py (the only three genuinely 0-diff vs bf16289). ⚠ NEVER RE-PIN a DIFFERENTIAL SUBSTRATE — every *_REF/*_REV/BASE_COMMIT/BASELINE_REV constant in tests/m3/ must now carry a `#:` bucket annotation from {CODE PIN | DIFFERENTIAL SUBSTRATE | HISTORICAL NARRATIVE}, and a permanent repo-wide gate enforces it. m3-06 stays HELD. Rationale + what the freeze deliberately does NOT cover: DEC-2026-08-06-sr4-freeze-scope.",
  "suite_baselines": {
    "tests/m3": "902 passed / 31 skipped / 0 failed — raised from 822 by quick-260806-sr4 (+80)",
    "tests/phase2": "136 passed / 1 skipped / 0 failed (the 1 skip is test_negative_controls.py: bedtools not available)",
    "rule": "⚠ Skips must STAY at 31 and 1. A new test landing as a SKIP is NOT evidence — check the skip count, not just the failure count.",
    "commands": "/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3 -q   (~14 min)  and  ... tests/phase2 -q  (~2 s)"
  },
  "resume_entry_point": "⚠ REFRESHED 2026-08-12. Read, in this order: (1) resume_on_reconnect[0] in this file (dated 2026-08-12); (2) .planning/DECISIONS.md DEC-2026-08-11-e2-framing-correction and DEC-2026-08-11-sr4-disposition — the two decisions that were outstanding on 2026-08-07 and are now RECORDED and STANDING — plus DEC-2026-08-12-adversarial-review-remediation, which carries the ONE CANONICAL stale-site table; (3) .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/ — the v2 E-2 outgoing pair (the current text), the corrected fire surface's changelog, and the claim-level sweep; (4) .planning/quick/260811-rcw-assemble-the-m3-04c-task-3-pre-fire-gate/260811-rcw-PRE-FIRE-GATE-REVIEW.md — the fire surface, corrected 2026-08-12; read its '## Corrections (2026-08-12)' section before acting on any command in it. THEN the 2026-08-07 material, which is history and not current: .planning/HANDOFF.json (this block) + .planning/STATE.md 2026-08-07 ★★ RESUME HERE ★★ block + .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md 2026-08-07 SESSION CLOSE block + .planning/DECISIONS.md DEC-2026-08-07-e2-orientation-disposition and DEC-2026-08-06-sr4-freeze-scope + the phase deferred-items.md (E-2 decided-as-A with its evidence update, E-3, E-4, K-2, K-3, and the CLOSED K-1 record).",
  "timestamp_reason_2026_08_12": "SESSION CLOSE 2026-08-12 (/close-session). Earlier same-day stamp was the quick-260812-09a sweep."
}
