---
phase: m3-aou-afr-ld-panel-build
plan: 260530-q04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260530-q04-author-aou-0-check-c-runbook-for-next-rw/260530-q04-AOU-0-CHECK-C-RUNBOOK.md
  - .planning/HANDOFF.json
  - .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md
autonomous: true
requirements: [REQ-M3-AUX-CHECK]

must_haves:
  truths:
    - "Carter can launch a Standard Analysis env and immediately tell whether the CDR bound cleanly (no 'cdrv8 - R9 not found' error)"
    - "Carter can run one capture block that reveals the real WGS/AUX prefix R9 binds"
    - "Carter can follow a decision tree that says 'no code change' vs 'one-line env-derive patch' based on the capture"
    - "HANDOFF.json reflects CHECK-D=done, CHECK-C=deferred-to-AOU-0 (env-not-wired), keep-pin decision, env-derive fix candidate"
    - ".continue-here.md reflects the same reframe and points at the new runbook"
  artifacts:
    - path: ".planning/quick/260530-q04-author-aou-0-check-c-runbook-for-next-rw/260530-q04-AOU-0-CHECK-C-RUNBOOK.md"
      provides: "Operational runbook for Carter: env preconditions, capture block, AUX decision tree, keep-pin decision, forward sequence"
      min_lines: 80
    - path: ".planning/HANDOFF.json"
      provides: "Refreshed structured handoff with this session's outcomes"
    - path: ".planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md"
      provides: "Refreshed checkpoint with this session's outcomes"
  key_links:
    - from: ".continue-here.md <next_action>"
      to: "260530-q04-AOU-0-CHECK-C-RUNBOOK.md"
      via: "explicit path reference"
    - from: "HANDOFF.json next_action"
      to: "260530-q04-AOU-0-CHECK-C-RUNBOOK.md"
      via: "explicit path reference"
---

<objective>
Author a single operational runbook that Carter executes on the AoU Researcher
Workbench to close the CDR/AUX question (CHECK C) on the next CDR-wired Standard
Analysis env, then refresh the two GSD state artifacts (HANDOFF.json,
.continue-here.md) with this session's resolved outcomes.

Purpose: This session resolved CHECK D (Hail 0.2.135 captured → catastrophe
mechanism still live → Track 4 patches stay load-bearing) and reframed CHECK C
from a presumed FAIL into INCONCLUSIVE/env-not-wired (the featherweight terminal
env was never CDR-bound, so its 403s prove nothing). The Resources-tab read
confirmed the workspace IS correctly version-bound to R9 — the R8-pin hypothesis
is ruled out and the pin is deliberately kept. The runbook hands Carter the
decisive env-var capture block so the NEXT CDR-wired env can settle "no code
change" vs "one-line env-derive AUX_BASE patch" in one shot.

Output: 1 new runbook + 2 refreshed state artifacts. DOCS ONLY — no source edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/HANDOFF.json
@.planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md
@.planning/quick/260528-l8r-stage-aou-pre-check-chr22-smoke-aou-2-4-/post-migration-validation.md

<interfaces>
<!-- The AUX path seam in source — the runbook references these but does NOT edit them. -->

From src/python/aou_ld_panel.py (lines 84-88) — the CHECK-C hardcoded literal:
```python
CDR_VERSION = "v8"
AUX_BASE = f"gs://fc-aou-datasets-controlled/{CDR_VERSION}/wgs/short_read/snpindel/aux"
RELATED_SAMPLES_PATH = f"{AUX_BASE}/relatedness/relatedness_flagged_samples.tsv"
ANCESTRY_PREDS_PATH = f"{AUX_BASE}/ancestry/ancestry_preds.tsv"
```

From src/python/aou_ld_panel.py (lines 646-648, 674-675) — the existing override seam
that a future env-derive patch would use (NO new hardcoded literal needed):
```python
# docstring:
#   ancestry_table_path: Override ANCESTRY_PREDS_PATH (mostly used by tests...)
#   relateds_table_path: Override RELATED_SAMPLES_PATH (tests only).
# usage:
anc_path = ancestry_table_path or ANCESTRY_PREDS_PATH
rel_path = relateds_table_path or RELATED_SAMPLES_PATH
```

The WGS MT path is already env-driven via $WGS_ACAF_THRESHOLD_MULTI_HAIL_PATH
(read through _require_env). The AUX TSVs are siblings of the WGS ACAF MT — so
deriving AUX_BASE from that env var is the clean fix IF the capture shows the
prefix is no longer `/v8/...`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the AOU-0 + CHECK-C runbook</name>
  <files>.planning/quick/260530-q04-author-aou-0-check-c-runbook-for-next-rw/260530-q04-AOU-0-CHECK-C-RUNBOOK.md</files>
  <action>
Create a NEW markdown runbook written FOR Carter to execute on the AoU Researcher
Workbench (Carter holds every launch/compute trigger — this is instructions, not
something we run). Structure it as these labeled sections:

**0. Why this runbook exists (one paragraph).** This session resolved CHECK D
and reframed CHECK C. The two prior envs failed to bind the CDR because they were
featherweight / terminal-only Compute-Engine envs that never ran the CDR-binding
startup. A 403 from a non-bound env tells us NOTHING about whether the legacy AUX
path is correct for R9. So CHECK C is deferred to a real CDR-wired env.

**1. Session outcomes baked in (status table).** A markdown table:
  - CHECK D = PASS — Hail 0.2.135 (full 0.2.135-034ef3e08116) on RW 2.0, captured
    from terminal Python. SAME Hail 0.2.x family as Legacy → the
    checkpoint-contract-violation mechanism behind the m3-W1 empty-MT catastrophe
    is STILL LIVE on RW 2.0. Consequence: Track 4 defensive patches
    (count_rows/count_cols asserts + entries-dir gsutil checks) remain load-bearing;
    chr22 smoke stays gated on them.
  - CHECK C = INCONCLUSIVE (not FAIL) — both `gsutil ls` on the legacy v8 AUX
    paths returned `AccessDeniedException: 403` (storage.objects.list denied for
    pet SA pet-...@wb-perky-corn-6639.iam.gserviceaccount.com). Listing the bucket
    ROOT `gs://fc-aou-datasets-controlled/` gave the SAME 403 (NOT 404 → not a
    wrong path; an authorization-scope failure). `env | grep` for CDR/AUX/dataset
    vars returned NOTHING. Terminal startup printed `Failed to get CDR
    configuration: CDR version not found: cdrv8 - R9 (env: prod, access tier:
    controlled)`. DIAGNOSIS: env-not-wired, not workspace-misbound.
  - Resources-tab read (FREE, $0, read-only) — workspace correctly version-bound
    to C2024Q3R9 = "cdrv8 - R9 (version-bound)", a Referenced BigQuery dataset,
    Source "All of Us Controlled Tier", Project ID wb-silky-artichoke-2408, Dataset
    ID C2024Q3R9. That CDR BigQuery project is SEPARATE from the compute project
    wb-perky-corn-6639 (normal AoU split, irrelevant to us — cohort is pure-Hail,
    zero BigQuery). R8-pin hypothesis RULED OUT. The soft "a different version is
    available" banner's only actions are View Release Notes + Close; adopting a
    different version needs a deliberate Data-collections-catalog action (nothing
    one-click / accidentally trippable). The Resources tab exposes the BigQuery side
    ONLY — it does NOT surface the fc-aou-datasets-controlled WGS/AUX Cloud Storage
    path where ancestry_preds.tsv + relatedness_flagged_samples.tsv live, so it does
    NOT hand us AUX_BASE directly.

**2. Decision (locked): KEEP the version-bound R9 pin.** A pinned CDR is the
reproducibility-correct posture (data-layer equivalent of the Snakemake pin; R9
won't shift mid-analysis). Do NOT adopt the catalog default. Reopen ONLY if the
pinned R9 snapshot fails to expose WGS/AUX paths at AOU-0 (no evidence of that).
Aligns with feedback_rigor_over_speed.

**3. Env launch preconditions (MUST read before spending).**
  - Use a **Standard Analysis (Jupyter) env**, NOT a featherweight / terminal-only
    Compute-Engine env. The Standard env runs the CDR-binding startup; the
    featherweight one does not (that is the whole reason the prior two probes 403'd).
  - **FIRST check on startup:** confirm NO `Failed to get CDR configuration /
    cdrv8 - R9 not found` error appears in the startup log. If it DOES → STOP, do
    not spend further compute, report back before proceeding.
  - RW 2.0 has NO persistent disks → the repo clone is ephemeral. Each fresh env:
    `git clone https://github.com/carter-clinton/coloc_analysis.git && cd
    coloc_analysis && git checkout m3-W2-aou-deltas`. Expect cosmetic nbstripout
    `—` (em-dash) churn — `git checkout -f`, NEVER commit it.

**4. THE DECISIVE CAPTURE BLOCK (run in the CDR-wired Standard env terminal).**
Fence this in a ```bash block, verbatim-ish:
```bash
echo "WORKSPACE_CDR = $WORKSPACE_CDR"
echo "WGS_ACAF MT   = $WGS_ACAF_THRESHOLD_MULTI_HAIL_PATH"
env | grep -iE 'CDR|WGS|AUX|fc-aou|DATASET' | sort
```
Then `gsutil ls -l` the two AUX siblings under whatever bucket prefix the WGS path
reveals (substitute the real prefix the capture prints):
```bash
# substitute <WGS_PREFIX> with the bucket+CDR prefix the capture printed
gsutil ls -l "<WGS_PREFIX>/wgs/short_read/snpindel/aux/ancestry/ancestry_preds.tsv"
gsutil ls -l "<WGS_PREFIX>/wgs/short_read/snpindel/aux/relatedness/relatedness_flagged_samples.tsv"
```

**5. Decision tree (no-change vs env-derive patch).** As an explicit
if/then list:
  - IF the WGS prefix is `gs://fc-aou-datasets-controlled/v8/...` AND the two
    `gsutil ls -l` now SUCCEED → CHECK C PASSES with NO code edit. The hardcoded
    `AUX_BASE` at aou_ld_panel.py:85 is correct for R9. Mark CHECK C done.
  - IF the WGS prefix is NOT `gs://fc-aou-datasets-controlled/v8/...` (e.g. a
    different bucket or `/v9/` or an R9-specific path) → that printed prefix IS the
    answer. The code gets a **one-line env-derive patch**: derive AUX_BASE from
    $WGS_ACAF_THRESHOLD_MULTI_HAIL_PATH (the AUX TSVs are siblings of the WGS ACAF
    MT) rather than the hardcoded literal — cleaner than a new literal. The existing
    `ancestry_table_path` / `relateds_table_path` override seam (lines 646-648,
    674-675) already supports threading an override. DO NOT apply the patch here —
    capture the prefix, route to a follow-up /gsd-quick that edits source.
  - IF startup showed the CDR-not-found error → env was not wired; do not trust any
    result; relaunch a Standard Analysis env or report.

State clearly: **DO NOT apply any source patch from this runbook.** This runbook
only captures the data that decides which path; the edit (if any) is a separate
code-touching quick task.

**6. Forward sequence (Carter holds every trigger).** Numbered:
  1. Close the CDR/AUX question via the capture block above (no-change or route to
     env-derive patch quick task).
  2. Run the **AOU-0 precheck notebook** (compute-free; git clone the repo into the
     env FIRST) → produces post-precheck-routing-decision.txt.
  3. **chr22 smoke** — 256-vCPU Dataproc, ~$30-75, cost-signed-off; Carter holds
     the trigger. Pass → Wave 2 AOU-2/AOU-4 paste-ins per
     AOU-2-AOU-4-TRACK-4-PATTERN.md. Fail → 1000G AFR safety net per
     feedback_no_1000g_ld_pivot (fallback only; do not propose proactively).

**7. New RW 2.0 coordinates (quick-reference table).** workspace aou-rw-476cdac2,
project wb-perky-corn-6639, WORKSPACE_BUCKET=gs://rw-migration-aou-rw-476cdac2,
CDR C2024Q3R9, billing pod user-pod-cclinton-2d12, region us-central1,
`wb workspace set --id=aou-rw-476cdac2`.

Fence ALL code/shell blocks in ```...``` per feedback_code_paste_fences.
  </action>
  <verify>
    <automated>test -f .planning/quick/260530-q04-author-aou-0-check-c-runbook-for-next-rw/260530-q04-AOU-0-CHECK-C-RUNBOOK.md && grep -q "WGS_ACAF_THRESHOLD_MULTI_HAIL_PATH" .planning/quick/260530-q04-author-aou-0-check-c-runbook-for-next-rw/260530-q04-AOU-0-CHECK-C-RUNBOOK.md && grep -q "0.2.135" .planning/quick/260530-q04-author-aou-0-check-c-runbook-for-next-rw/260530-q04-AOU-0-CHECK-C-RUNBOOK.md && grep -qi "Standard Analysis" .planning/quick/260530-q04-author-aou-0-check-c-runbook-for-next-rw/260530-q04-AOU-0-CHECK-C-RUNBOOK.md</automated>
  </verify>
  <done>Runbook file exists with all 7 sections; contains the capture block, the no-change vs env-derive decision tree, the keep-pin decision, the Standard-Analysis-env precondition + CDR-bind sanity check, and the forward sequence with Carter-holds-trigger gates. All shell blocks fenced.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh HANDOFF.json and .continue-here.md with session outcomes</name>
  <files>.planning/HANDOFF.json, .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md</files>
  <action>
Edit BOTH state artifacts to reflect this session's resolved outcomes. Keep them
internally consistent.

**.planning/HANDOFF.json:**
  - Update `timestamp` to the current session time.
  - In `remaining_tasks`:
    - CHECK-D → move to `completed_tasks` (or set status "done") with note:
      "Hail 0.2.135 (0.2.135-034ef3e08116) captured on RW 2.0 terminal Python —
      SAME 0.2.x family as Legacy → checkpoint-contract-violation catastrophe
      mechanism STILL LIVE → Track 4 patches remain load-bearing; chr22 smoke
      gated on them."
    - CHECK-C → keep in `remaining_tasks` but REFRAME name/status to:
      "DEFERRED to AOU-0 (env-not-wired diagnosis). Featherweight terminal env was
      never CDR-bound (startup: 'cdrv8 - R9 not found'); both legacy-v8 AUX `gsutil
      ls` AND bucket-ROOT ls returned identical 403 (authz-scope failure, not 404 →
      not a wrong path). 403 from a non-bound env proves nothing. Resolve via the
      decisive env-var capture in a CDR-wired Standard Analysis env (see runbook)."
  - In `decisions`, ADD:
    - {decision: "KEEP the version-bound R9 CDR pin", rationale: "Resources-tab
      confirmed workspace correctly bound to C2024Q3R9 (cdrv8-R9, version-bound);
      R8-pin hypothesis RULED OUT. A pinned CDR is the reproducibility-correct
      posture (data-layer Snakemake-pin equivalent). Do NOT adopt catalog default;
      reopen only if R9 snapshot fails to expose WGS/AUX at AOU-0.",
      ref: "feedback_rigor_over_speed"}
    - {decision: "CHECK-C fix candidate = env-derive AUX_BASE from
      $WGS_ACAF_THRESHOLD_MULTI_HAIL_PATH (AUX TSVs are siblings of the WGS ACAF
      MT); existing ancestry_table_path/relateds_table_path override seam supports
      it. PENDING env-var capture — applied only if capture shows prefix != /v8/.",
      ref: "src/python/aou_ld_panel.py:85,646-648,674-675"}
  - Add a brief note in `context_notes` that the CDR BigQuery dataset project
    (wb-silky-artichoke-2408) is separate from the compute project
    (wb-perky-corn-6639) — normal AoU split, irrelevant since cohort is pure-Hail.
  - Update `next_action` to point at the AOU-0 runbook:
    "Launch a CDR-wired **Standard Analysis (Jupyter)** env (NOT featherweight),
    confirm NO 'cdrv8 - R9 not found' on startup, run the decisive env-var capture
    block, then close CHECK C (no-change vs env-derive patch) per
    .planning/quick/260530-q04-author-aou-0-check-c-runbook-for-next-rw/260530-q04-AOU-0-CHECK-C-RUNBOOK.md.
    Then AOU-0 precheck → chr22 smoke (Carter holds trigger)."
  - Preserve JSON validity (valid JSON after edit).

**.planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md:**
  - Bump `last_updated` in frontmatter to current session time.
  - `<current_state>`: append that CHECK D is resolved (Hail 0.2.135 → mechanism
    live → Track 4 stays load-bearing) and CHECK C is reframed to env-not-wired /
    deferred-to-AOU-0; Resources-tab confirmed R9 version-bound (R8-pin ruled out)
    + keep-pin decision.
  - `<remaining_work>`: update the CHECK C bullet to the deferred/env-not-wired
    framing + point at the new runbook for the decisive capture; mark CHECK D done.
  - `<decisions_made>`: add keep-the-R9-pin + env-derive-AUX_BASE fix candidate.
  - `<next_action>`: replace with the Standard-Analysis-env launch + CDR-bind sanity
    check + capture block, then close CHECK C, then AOU-0 → chr22 smoke, with an
    explicit path reference to 260530-q04-AOU-0-CHECK-C-RUNBOOK.md.
  - Optionally add the runbook to the "Required Reading" list as item 0/1.

Do NOT edit STATE.md (the orchestrator handles STATE.md in workflow Step 7).
Stage with explicit paths only when committing — NEVER `git add -A` / `git add .`
(feedback_multi_terminal_staging).
  </action>
  <verify>
    <automated>python3 -c "import json; json.load(open('.planning/HANDOFF.json'))" && grep -q "0.2.135" .planning/HANDOFF.json && grep -qi "version-bound R9" .planning/HANDOFF.json && grep -q "260530-q04-AOU-0-CHECK-C-RUNBOOK.md" .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md && grep -qi "env-not-wired\|env not wired\|deferred" .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md</automated>
  </verify>
  <done>HANDOFF.json is valid JSON with CHECK-D=done, CHECK-C reframed as deferred/env-not-wired, keep-pin + env-derive decisions added, next_action pointing at the runbook. .continue-here.md mirrors the reframe and references the runbook path. STATE.md untouched.</done>
</task>

</tasks>

<verification>
- Runbook exists and contains: 7 labeled sections, the decisive capture block, the
  no-change vs env-derive decision tree, the keep-pin decision, the Standard-Analysis
  env precondition + CDR-bind sanity check, the forward sequence with Carter-holds-trigger.
- HANDOFF.json is valid JSON and reflects CHECK-D=done + CHECK-C reframe + new decisions
  + runbook-pointing next_action.
- .continue-here.md mirrors the reframe and references the runbook.
- STATE.md NOT edited by the executor.
- No source code files changed (docs-only).
</verification>

<success_criteria>
- A different Claude (or Carter) can launch the next AoU env from the runbook alone,
  capture the decisive env vars, and route CHECK C to "no change" or "env-derive patch"
  without re-deriving any of this session's reasoning.
- Resuming from HANDOFF.json or .continue-here.md surfaces the correct next action
  (CDR-wired Standard env + capture block) and the keep-pin decision without ambiguity.
</success_criteria>

<output>
After completion, create
`.planning/quick/260530-q04-author-aou-0-check-c-runbook-for-next-rw/260530-q04-SUMMARY.md`
summarizing: runbook authored (path), state artifacts refreshed, CHECK-D resolved /
CHECK-C reframed / keep-pin locked, and the next action (CDR-wired Standard Analysis env).

Note for orchestrator: append a one-line session-continuation paragraph to STATE.md in
Step 7 — "2026-05-30 q04: CHECK D=PASS (Hail 0.2.135, mechanism live, Track 4 stays);
CHECK C reframed INCONCLUSIVE/env-not-wired → deferred to AOU-0; Resources-tab confirmed
R9 version-bound (R8-pin ruled out); KEEP-PIN locked; AOU-0+CHECK-C runbook authored."
</output>
