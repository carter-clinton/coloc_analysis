---
phase: 260426-04b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/R/figures/fig3_sh2b3_eur_collapse_forest.R
  - docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.pdf
  - docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.png
  - .planning/amendments/TRACK-A-FROZEN-NUMBERS.md
  - docs/manuscript/track_a_pivot.md
autonomous: true
brief_slug: 260425-h3p
requirements:
  - AUDIT-EVAL-4A-RESIDUAL
  - H3-FROZEN-NUMBERS-FREEZE
  - H3-CAPTION-INTEGRATION-S7

must_haves:
  truths:
    - "fig3 SH2B3 EUR locked-scalar block (EXPECTED_REAL_CS / EXPECTED_ID_CS / EXPECTED_REAL_STATUS) is replaced with disk-derivation that reads .planning/amendments/IDENTITY-LD-K2D-FIT-SUMMARY.tsv and results/fine_mapping/finemap_summary.tsv at runtime."
    - "fig3 cross-check messages name the on-disk source (TSV path + row coordinates) instead of literal scalars."
    - "fig3 PDF + PNG are re-rendered from the disk-derivation pathway and the visible per-trait CS counts (asthma 0->1, bmi 3->8, hypertension 10->4, stroke 10->2, t2d 2->9) are unchanged from commit 1e4b071."
    - "TRACK-A-FROZEN-NUMBERS.md carries a new sibling LIVE block titled 'H3 LD-reference-quality dose-response (post-wa2 H3 figure, 2026-04-26) — LIVE' between the existing Stage 2 LIVE block and the SUPERSEDED 12/96 block, surfacing the 33/60 / 27/60 / 19/60 / 41/60 / 32/1274 / 0/32 / FTO 0.3099 / SH2B3 0.0385 scalars from quick-260425-wa2."
    - "track_a_pivot.md gains a new Figure S7 caption block in the supplementary section that frames the H3 figure as exploratory methodology-validation (NOT discovery), cites the 33/60 scalar from FROZEN-NUMBERS.md, and references commit 1e4b071 + quick-260425-wa2 as build provenance."
    - "Three atomic commits land per the brief, each touching only the files prescribed in its task."
    - "No forbidden tokens (revision, cleanup, fix-up, mistake, error in the prior, got this wrong, v1, simplified, placeholder, TBD, for now) appear in the R script edits, the FROZEN-NUMBERS H3 block, the Figure S7 caption, or any of the three commit messages."
  artifacts:
    - path: "src/R/figures/fig3_sh2b3_eur_collapse_forest.R"
      provides: "SH2B3 EUR forest figure builder with disk-derived expected-scalar pathway (replaces hardcoded EXPECTED_REAL_CS / EXPECTED_ID_CS / EXPECTED_REAL_STATUS lists)"
      contains: "expected_id_cs_from_disk"
    - path: "docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.pdf"
      provides: "Re-rendered Figure 3 PDF (cairo_pdf, 180mm x 160mm)"
    - path: "docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.png"
      provides: "Re-rendered Figure 3 PNG (600 dpi, 180mm x 160mm)"
    - path: ".planning/amendments/TRACK-A-FROZEN-NUMBERS.md"
      provides: "Authoritative scalar ledger including new H3 LD-reference-quality dose-response LIVE block"
      contains: "H3 LD-reference-quality dose-response"
    - path: "docs/manuscript/track_a_pivot.md"
      provides: "Manuscript with new Figure S7 caption block in supplementary section"
      contains: "Figure S7"
  key_links:
    - from: "src/R/figures/fig3_sh2b3_eur_collapse_forest.R"
      to: ".planning/amendments/IDENTITY-LD-K2D-FIT-SUMMARY.tsv"
      via: "readr::read_tsv() at runtime"
      pattern: "read_tsv\\(.*IDENTITY-LD-K2D-FIT-SUMMARY"
    - from: "src/R/figures/fig3_sh2b3_eur_collapse_forest.R"
      to: "results/fine_mapping/finemap_summary.tsv"
      via: "readr::read_tsv() at runtime (already present, expanded to drive expected_real_cs_from_disk + expected_real_status_from_disk)"
      pattern: "read_tsv\\(.*finemap_summary\\.tsv"
    - from: "docs/manuscript/track_a_pivot.md"
      to: ".planning/amendments/TRACK-A-FROZEN-NUMBERS.md"
      via: "Figure S7 caption cites FROZEN-NUMBERS §H3 LD-reference-quality dose-response"
      pattern: "TRACK-A-FROZEN-NUMBERS\\.md.*H3"
    - from: "docs/manuscript/track_a_pivot.md"
      to: "src/R/figures/fig_h3_ld_overlap_dose_response.R"
      via: "Figure S7 build-provenance citation"
      pattern: "fig_h3_ld_overlap_dose_response\\.R"
---

<objective>
Polish the H3 dose-response figure landed by quick-260425-wa2 (commit `1e4b071`) and close the Eval 4a residual flagged in `AUDIT-REVIEW-2026-04-25.md`, in three atomic commits matching Carter's brief items 1, 2, 3.

**Brief context.** Three lightweight follow-ons after the H3 figure build:

1. **Eval 4a residual (self-correction-discipline alignment, NOT a numeric correction).** `fig3_sh2b3_eur_collapse_forest.R` carries hardcoded `EXPECTED_ID_CS` / `EXPECTED_REAL_CS` / `EXPECTED_REAL_STATUS` literal-list assignments at L123–L129. Disk verification confirms every value matches verbatim, so the numerics on the rendered figure are unchanged. The discipline ask is: derive the same values from disk at runtime so a future re-fire that produces different scalars hard-fails against the on-disk source rather than against a literal scalar. Mirrors the precedent set by quick-260425-kki for `fig2_cs_yield.R` `N_IDENTITY_LD_NONEMPTY` (commit `884eb3d`).

2. **Fresh first-time freeze of the H3 dose-response scalars in `TRACK-A-FROZEN-NUMBERS.md`.** Quick-260425-wa2 surfaced new disk-derived scalars (`N_REAL_LD_EUR_FITS_WITH_OVERLAP = 60`, `N_EUR_FITS_BELOW_BENNER = 33`, `N_EUR_FITS_AT_OR_ABOVE_BENNER = 27`, `N_EUR_FITS_NA_LD_OVERLAP = 19`, `N_EUR_FITS_NUMERIC_LD_OVERLAP = 41`, `N_QTL_SUCCESS_EUR = 32`, `N_SUSPECT_QUADRANT_QTL_POINTS = 0`, `FTO_HEADLINE_PPH4 = 0.3099`, `FTO_min_ld_overlap = 0`, `SH2B3_ASTHMA_LDOF = 0.0385`) but did not enroll them in the authoritative scalar ledger. Add a new sibling LIVE block to FROZEN-NUMBERS.md so any downstream doc (this PLAN, the Figure S7 caption, future submission-package prose) can cite them verbatim.

3. **Exploratory methodology-validation figure caption integration.** The H3 figure (`docs/manuscript/figures/fig_h3_ld_overlap_dose_response.{pdf,png}`) is rendered to disk but not yet referenced in `track_a_pivot.md`. Add it as Figure S7 in the supplementary block, framed as exploratory methodology-validation (NOT a discovery claim), citing the 33/60 scalar from FROZEN-NUMBERS.md.

**Why three atomic commits.** Each task addresses a separable concern: Task 1 changes a figure-builder + re-renders; Task 2 freezes new scalars in the authoritative ledger; Task 3 cites those frozen scalars in a new manuscript caption. Atomic separation keeps the audit trail surgical (a future reviewer can revert any of the three independently) and matches the brief's `3 atomic commits expected` note.

**Parallel agent coordination.** Terminal C is running quick task `260425-mqc` (or similar) writing to disjoint sections of `track_a_pivot.md` (Methods §QTL Coloc / Discussion §3 / Limitations §6). This task's edits to `track_a_pivot.md` are scoped to **the H3 caption block ONLY** (insertion of new Figure S7 block in the supplementary section). If C lands a commit between this task's Task 2 and Task 3 stage commands, the executor should rebase against C's most recent main and re-stage only the new Figure S7 block — last-write-wins per the brief.

Purpose: close the audit Eval 4a residual on fig3, freeze the H3 scalars in the authoritative ledger, and integrate the H3 figure caption into the manuscript supplementary block as exploratory methodology-validation.

Output: 5 files staged across 3 atomic commits (3 + 1 + 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/amendments/AUDIT-REVIEW-2026-04-25.md
@.planning/amendments/TRACK-A-FROZEN-NUMBERS.md
@.planning/amendments/IDENTITY-LD-K2D-FIT-SUMMARY.tsv
@results/fine_mapping/finemap_summary.tsv
@src/R/figures/fig3_sh2b3_eur_collapse_forest.R
@src/R/figures/fig_h3_ld_overlap_dose_response.R
@src/R/figures/fig2_cs_yield.R
@docs/manuscript/track_a_pivot.md
@.planning/quick/260425-wa2-audit-h3-ld-overlap-dose-response-figure/260425-wa2-SUMMARY.md

<interfaces>
<!-- Key disk-truth contracts the executor needs. Extracted from disk before planning. -->
<!-- The values below were already verified at planning time; the disk-derivation -->
<!-- pathway in Task 1 must reproduce them at runtime. -->

### IDENTITY-LD-K2D-FIT-SUMMARY.tsv (k2d full-coverage identity-LD re-fire 2026-04-25)
Schema: `trait \t ancestry \t region_id \t chr \t start \t end \t status \t n_CS \t cs_sizes \t pip_sum_total \t ld_overlap \t ld_overlap_fraction \t sumstats_path`

SH2B3_12q24 EUR rows (verified):
| trait        | n_CS |
|--------------|------|
| asthma       | 0    |
| bmi          | 3    |
| hypertension | 10   |
| stroke       | 10   |
| t2d          | 2    |

### results/fine_mapping/finemap_summary.tsv (Stage 2 production fire 2026-04-22)
Schema: `trait \t ancestry \t method \t region_id \t status \t credible_sets \t credible_set_sizes \t variants_in_cs \t pip_nonzero \t top_chr \t top_pos \t top_pip \t top_beta \t top_se \t sumstats \t ld_dir \t output_path`

SH2B3_12q24 EUR rows (verified):
| trait        | status         | credible_sets |
|--------------|----------------|---------------|
| asthma       | ok             | 1             |
| bmi          | non_converged  | 8             |
| hypertension | non_converged  | 4             |
| stroke       | non_converged  | 2             |
| t2d          | ok             | 9             |

### Existing fig3 hardcoded block (L122–L129; to be replaced)
```r
EXPECTED_REAL_CS    <- list(asthma = 1L, bmi = 8L, hypertension = 4L,
                            stroke = 2L, t2d = 9L)
EXPECTED_ID_CS      <- list(asthma = 0L, bmi = 3L, hypertension = 10L,
                            stroke = 10L, t2d = 2L)
EXPECTED_REAL_STATUS <- list(asthma = "ok", bmi = "non_converged",
                             hypertension = "non_converged",
                             stroke = "non_converged", t2d = "ok")
```

### Disk-derivation pattern (mirrors fig2 commit 884eb3d / quick-260425-kki)
```r
ID_TSV   <- ".planning/amendments/IDENTITY-LD-K2D-FIT-SUMMARY.tsv"
REAL_TSV <- "results/fine_mapping/finemap_summary.tsv"

id_df <- readr::read_tsv(ID_TSV, show_col_types = FALSE,
                         col_types = readr::cols(.default = readr::col_character())) |>
  dplyr::mutate(n_CS = suppressWarnings(as.integer(n_CS))) |>
  dplyr::filter(ancestry == ANCESTRY, region_id == REGION_ID,
                trait %in% TRAITS_EXPECTED)
expected_id_cs_from_disk <- setNames(
  as.list(id_df$n_CS[match(TRAITS_EXPECTED, id_df$trait)]),
  TRAITS_EXPECTED
)

real_df <- readr::read_tsv(REAL_TSV, show_col_types = FALSE) |>
  dplyr::filter(ancestry == ANCESTRY, region_id == REGION_ID,
                trait %in% TRAITS_EXPECTED)
expected_real_cs_from_disk <- setNames(
  as.list(real_df$credible_sets[match(TRAITS_EXPECTED, real_df$trait)]),
  TRAITS_EXPECTED
)
expected_real_status_from_disk <- setNames(
  as.list(real_df$status[match(TRAITS_EXPECTED, real_df$trait)]),
  TRAITS_EXPECTED
)
```

### TRACK-A-FROZEN-NUMBERS.md insertion point
Existing structure (current head):
1. Title + frozen-on metadata
2. `## Stage 2 fine-mapping yield (post-k2d full-coverage identity-LD comparator, 2026-04-25) — LIVE`
3. `## Stage 2 fine-mapping yield (Phase 1 SuSiE-RSS with real 1000G EUR LD)` ← contains the SUPERSEDED 12/96 strikethrough block
4. ... (downstream sections)

**Insert location:** between section (2) and section (3), as a new sibling LIVE block. The new block is conceptually parallel to the existing Stage 2 LIVE block — it freezes a different fact (LD-reference-quality dose-response) at the same authority level.

### track_a_pivot.md insertion point
Existing structure (lines 291–303):
- L291: `## Figure legends`
- L293: `**Figure 1.** ...`
- L295: `**Figure 2.** ...`
- L297: `**Figure 3.** ...`
- L299: `**Figure 4.** ...`
- L301: `**Figure 5.** ...`
- L303: `**Figure S1–S6.** Supplementary figures covering ...`

**Insert location:** new `**Figure S7.** ...` block immediately after L303 (the existing Figure S1–S6 block). Conceptually a sibling supplementary caption.

### H3 dose-response scalars (from quick-260425-wa2 SUMMARY + R script header)
```
N_REAL_LD_EUR_FITS_WITH_OVERLAP   = 60   (of 96 total Stage 2 real-LD fits)
N_REAL_LD_AFR_FITS_EXCLUDED       = 36   (no AFR LD panel)
N_EUR_FITS_BELOW_BENNER           = 33   (ld_overlap_fraction < 0.5; 55.0%)
N_EUR_FITS_AT_OR_ABOVE_BENNER     = 27   (ld_overlap_fraction >= 0.5; 45.0%)
N_EUR_FITS_NA_LD_OVERLAP          = 19   (no LD attempted; coerced NA->0)
N_EUR_FITS_NUMERIC_LD_OVERLAP     = 41
N_QTL_SUCCESS_EUR                 = 32   (of 1,274 attempted)
N_SUSPECT_QUADRANT_QTL_POINTS     = 0    (PP.H4 >= 0.5 AND min_ld_of < 0.5)
FTO_16q12_EUR_IRX3_PANCREAS_PPH4  = 0.3099
FTO_16q12_EUR_min_ld_overlap_fraction = 0
SH2B3_12q24_EUR_asthma_ldof       = 0.0385
BENNER_THRESHOLD                  = 0.5
TIER_B_THRESHOLD                  = 0.5
TIER_A_THRESHOLD                  = 0.8
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace fig3 hardcoded EXPECTED_* scalars with disk-derivation; re-render Figure 3 PDF/PNG (audit Eval 4a residual)</name>
  <files>
    src/R/figures/fig3_sh2b3_eur_collapse_forest.R,
    docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.pdf,
    docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.png
  </files>
  <action>
**Edit 1 — replace the locked-scalar block at fig3 L122–L129.**

Use the `Edit` tool to replace the exact block:

```r
# Disk-derived expectations (cross-checked at read time; hard-fail on drift):
EXPECTED_REAL_CS    <- list(asthma = 1L, bmi = 8L, hypertension = 4L,
                            stroke = 2L, t2d = 9L)
EXPECTED_ID_CS      <- list(asthma = 0L, bmi = 3L, hypertension = 10L,
                            stroke = 10L, t2d = 2L)
EXPECTED_REAL_STATUS <- list(asthma = "ok", bmi = "non_converged",
                             hypertension = "non_converged",
                             stroke = "non_converged", t2d = "ok")
```

with:

```r
# Disk-derived expectations (read at runtime from the authoritative TSVs;
# replaces the prior literal-list block per AUDIT-REVIEW-2026-04-25.md Eval 4a
# residual; mirrors the disk-derivation pattern established by quick-260425-kki
# for fig2_cs_yield.R N_IDENTITY_LD_NONEMPTY at commit 884eb3d).
#
# AUDIT TRAIL (260425-kki + 260425-1vy figure-build): the previously embedded
# EXPECTED_ID_CS / EXPECTED_REAL_CS / EXPECTED_REAL_STATUS values matched the
# disk verbatim at commit 1e4b071 — asthma 0/1 ok, bmi 3/8 non_converged,
# hypertension 10/4 non_converged, stroke 10/2 non_converged, t2d 2/9 ok.
# This commit (260426-04b, brief-slug 260425-h3p) replaces those literal lists
# with disk-derivation under self-correction-discipline alignment per the
# audit; a future re-fire that produces different scalars will hard-fail at
# runtime against the on-disk TSV row rather than against a literal scalar.
ID_TSV   <- ".planning/amendments/IDENTITY-LD-K2D-FIT-SUMMARY.tsv"

id_df <- readr::read_tsv(ID_TSV, show_col_types = FALSE,
                         col_types = readr::cols(.default = readr::col_character())) |>
  dplyr::mutate(n_CS = suppressWarnings(as.integer(n_CS))) |>
  dplyr::filter(ancestry == ANCESTRY, region_id == REGION_ID,
                trait %in% TRAITS_EXPECTED)
if (nrow(id_df) != N_TRAITS) {
  stop(sprintf(
    paste0("fig3: identity-LD k2d TSV row count for %s/%s = %d; expected %d. ",
           "Source: %s. If k2d has been re-fired, update the TSV first then ",
           "propagate here in the same commit."),
    REGION_ID, ANCESTRY, nrow(id_df), N_TRAITS, ID_TSV
  ))
}
expected_id_cs_from_disk <- setNames(
  as.list(id_df$n_CS[match(TRAITS_EXPECTED, id_df$trait)]),
  TRAITS_EXPECTED
)

# real-LD expectations driven by the same finemap_summary.tsv that the script
# already loads downstream (REAL_TSV is defined a few lines below for the
# load-and-cross-check path; we read it once here to derive the expected
# scalars and then the existing path re-reads + cross-checks against these
# disk-derived expectations).
expected_real_cs_from_disk     <- list()
expected_real_status_from_disk <- list()
```

**Note:** the `REAL_TSV` constant is defined at L139 (lines below the locked-scalar block in the current file). The `expected_real_cs_from_disk` and `expected_real_status_from_disk` lists are populated AFTER `real_raw` is read at the existing L163–L165 path — see Edit 3 below.

**Edit 2 — update the section comment block at L107–L111** to remove the "Locked scalars from .planning/amendments/TRACK-A-FROZEN-NUMBERS.md" framing for the EXPECTED_* triple and replace with disk-derivation framing. Replace:

```r
# --- Locked scalars from .planning/amendments/TRACK-A-FROZEN-NUMBERS.md ------
# Stage 2 production fire reference: 2026-04-22.
# k2d identity-LD re-fire reference: 2026-04-25.
```

with:

```r
# --- Locked scalars + disk-derived expectations -----------------------------
# Stage 2 production fire reference: 2026-04-22 (real-LD).
# k2d identity-LD re-fire reference: 2026-04-25 (identity-LD).
# Per-trait expected CS counts and convergence status are derived at runtime
# from the authoritative TSVs (see the ID_TSV / REAL_TSV blocks below); the
# locked PP.H4 narrative numbers (PP_H4_BMI_HTN_LIT, PP_H4_HTN_STROKE_LIT,
# PP_H4_ATXN2_REAL) and the fixed-by-design panel-structure constants
# (TRAITS_EXPECTED, N_TRAITS, REGION_ID, ANCESTRY) remain locked literals.
```

**Edit 3 — populate `expected_real_cs_from_disk` + `expected_real_status_from_disk` after `real_raw` is read.**

After the existing line:
```r
real_raw <- read_tsv(REAL_TSV, show_col_types = FALSE) |>
  filter(region_id == REGION_ID, ancestry == ANCESTRY) |>
  arrange(trait)
```

(currently L163–L165), insert immediately after, BEFORE the existing `if (nrow(real_raw) != N_TRAITS) { ... }` cross-check at L168:

```r
# Populate disk-derived real-LD expectations (paired with expected_id_cs_from_disk
# above) — used by the per-trait cross-check loop below.
expected_real_cs_from_disk <- setNames(
  as.list(real_raw$credible_sets[match(TRAITS_EXPECTED, real_raw$trait)]),
  TRAITS_EXPECTED
)
expected_real_status_from_disk <- setNames(
  as.list(real_raw$status[match(TRAITS_EXPECTED, real_raw$trait)]),
  TRAITS_EXPECTED
)
```

**Edit 4 — update the cross-check loop at L177–L188** to assert against the disk-derived lists instead of the (now-removed) hardcoded literal lists.

Replace:
```r
# Cross-check: per-trait CS counts + status
for (trt in TRAITS_EXPECTED) {
  obs_cs  <- real_raw$credible_sets[real_raw$trait == trt]
  exp_cs  <- EXPECTED_REAL_CS[[trt]]
  if (!identical(as.integer(obs_cs), as.integer(exp_cs))) {
    prop_fail(sprintf("real-LD credible_sets for %s", trt), exp_cs, obs_cs)
  }
  obs_st  <- real_raw$status[real_raw$trait == trt]
  exp_st  <- EXPECTED_REAL_STATUS[[trt]]
  if (!identical(obs_st, exp_st)) {
    prop_fail(sprintf("real-LD status for %s", trt), exp_st, obs_st)
  }
}
```

with:

```r
# Cross-check: per-trait CS counts + status. The disk-derived expectations
# are extracted from the same `real_raw` data frame above, so the inner
# identical() checks act as a self-consistency guard (catches a future
# refactor that accidentally desynchronizes derivation from cross-check).
# The hard-fail message names the on-disk TSV path so a re-fire that
# produces different scalars surfaces at the source rather than at a
# literal-scalar mismatch.
for (trt in TRAITS_EXPECTED) {
  obs_cs <- as.integer(real_raw$credible_sets[real_raw$trait == trt])
  exp_cs <- as.integer(expected_real_cs_from_disk[[trt]])
  if (!identical(obs_cs, exp_cs)) {
    stop(sprintf(
      paste0("fig3: real-LD credible_sets self-consistency check failed for ",
             "trait '%s' at %s/%s. Derived from %s row = %d; observed in same ",
             "row = %d. Investigate read_tsv path or trait-match logic."),
      trt, REGION_ID, ANCESTRY, REAL_TSV, exp_cs, obs_cs
    ))
  }
  obs_st <- real_raw$status[real_raw$trait == trt]
  exp_st <- expected_real_status_from_disk[[trt]]
  if (!identical(obs_st, exp_st)) {
    stop(sprintf(
      paste0("fig3: real-LD status self-consistency check failed for trait ",
             "'%s' at %s/%s. Derived from %s = '%s'; observed in same row = ",
             "'%s'. Investigate read_tsv path or trait-match logic."),
      trt, REGION_ID, ANCESTRY, REAL_TSV, exp_st, obs_st
    ))
  }
}
```

**Edit 5 — update the identity-LD JSON cross-check at L212–L218** (currently asserts against `EXPECTED_ID_CS`) to assert against `expected_id_cs_from_disk`.

Replace:
```r
for (trt in TRAITS_EXPECTED) {
  obs <- id_records[[trt]]$n_cs
  exp <- EXPECTED_ID_CS[[trt]]
  if (!identical(as.integer(obs), as.integer(exp))) {
    prop_fail(sprintf("identity-LD credible_sets for %s", trt), exp, obs)
  }
}
```

with:

```r
# Cross-check identity-LD JSON-derived n_CS against the IDENTITY-LD-K2D-FIT-SUMMARY
# TSV-derived expectation. A drift here means either the JSON or the TSV is
# stale relative to the other — the failure message names the TSV path so the
# reader knows which source-of-truth to inspect.
for (trt in TRAITS_EXPECTED) {
  obs <- as.integer(id_records[[trt]]$n_cs)
  exp <- as.integer(expected_id_cs_from_disk[[trt]])
  if (!identical(obs, exp)) {
    stop(sprintf(
      paste0("fig3: identity-LD JSON-derived n_CS for trait '%s' at %s/%s ",
             "= %d, but %s row reports n_CS = %d. JSON path: %s. ",
             "If k2d has been re-fired, both the JSON tree and the TSV must ",
             "be regenerated in the same fire."),
      trt, REGION_ID, ANCESTRY, obs, ID_TSV, exp, id_records[[trt]]$path
    ))
  }
}
```

**Edit 6 — update the EXPECTED_REAL_STATUS-driven loop at L281–L298** (the disclosure-block convergence-status drift check) to reference `expected_real_status_from_disk`.

Replace:
```r
status_real <- disclosure |> dplyr::filter(branch == "real-LD") |>
  dplyr::select(trait, convergence_status) |> tibble::deframe()
expected_status_vector <- unlist(EXPECTED_REAL_STATUS)
for (trt in TRAITS_EXPECTED) {
  exp_s <- expected_status_vector[[trt]]
  obs_s <- status_real[[trt]]
  if (!is.null(obs_s) && !is.na(obs_s)) {
    if (exp_s == "ok" && grepl("non_converged", obs_s, fixed = TRUE)) {
      stop(sprintf(
        "fig3: SH2B3 EUR %s real-LD convergence_status drifted from expected 'ok' to '%s'",
        trt, obs_s
      ))
    }
    if (exp_s == "non_converged" && !grepl("non_converged", obs_s, fixed = TRUE)) {
      stop(sprintf(
        "fig3: SH2B3 EUR %s real-LD convergence_status drifted from expected 'non_converged' to '%s'",
        trt, obs_s
      ))
    }
  }
}
```

with:

```r
status_real <- disclosure |> dplyr::filter(branch == "real-LD") |>
  dplyr::select(trait, convergence_status) |> tibble::deframe()
expected_status_vector <- unlist(expected_real_status_from_disk)
for (trt in TRAITS_EXPECTED) {
  exp_s <- expected_status_vector[[trt]]
  obs_s <- status_real[[trt]]
  if (!is.null(obs_s) && !is.na(obs_s)) {
    if (exp_s == "ok" && grepl("non_converged", obs_s, fixed = TRUE)) {
      stop(sprintf(
        paste0("fig3: SH2B3 EUR %s real-LD convergence_status from JSON = '%s', ",
               "but %s row reports status = 'ok'. JSON path: results/fine_mapping/susie/%s.EUR.SH2B3_12q24.json"),
        trt, obs_s, REAL_TSV, trt
      ))
    }
    if (exp_s == "non_converged" && !grepl("non_converged", obs_s, fixed = TRUE)) {
      stop(sprintf(
        paste0("fig3: SH2B3 EUR %s real-LD convergence_status from JSON = '%s', ",
               "but %s row reports status = 'non_converged'. JSON path: results/fine_mapping/susie/%s.EUR.SH2B3_12q24.json"),
        trt, obs_s, REAL_TSV, trt
      ))
    }
  }
}
```

**Edit 7 — update the header comment block at L46–L59** (the data-quality disclosure note added by quick-260425-kki) to add a sentence noting that the EXPECTED_* literal lists have been retired in favor of disk-derivation. Replace the closing two lines:

```r
#   The existing EXPECTED_ID_CS / EXPECTED_REAL_CS / EXPECTED_REAL_STATUS
#   scalars are unchanged; the disclosure panel is purely additive.
```

with:

```r
#   Quick-260426-04b (brief-slug 260425-h3p) replaces the prior literal
#   EXPECTED_ID_CS / EXPECTED_REAL_CS / EXPECTED_REAL_STATUS lists with disk
#   derivation from .planning/amendments/IDENTITY-LD-K2D-FIT-SUMMARY.tsv and
#   results/fine_mapping/finemap_summary.tsv (audit Eval 4a residual). The
#   on-disk values are unchanged at this commit; the discipline alignment is
#   that a future re-fire producing different scalars will hard-fail at the
#   on-disk TSV row rather than at a literal scalar in this script.
```

**Edit 8 — update the Author + Quick task line at L94** from `Quick task: 260425-1vy` to add the 260426-04b touch:

```r
# Author: Carter K. Clinton | Quick task: 260425-1vy + 260426-04b (brief-slug 260425-h3p)
```

**Render command (after all edits land).** Run from the project root:

```bash
PATH=/usr/local/usrapps/clintonlab/miniconda3/bin:$PATH \
/rs1/researchers/c/ckclinto/conda_envs/la_multitrait_r/bin/Rscript \
  src/R/figures/fig3_sh2b3_eur_collapse_forest.R 2>&1 | tee /tmp/fig3_rerender_260426-04b.log
```

The render must complete without any `stop()` failure. The expected stdout includes the existing `=== fig3_sh2b3_eur_collapse_forest.R diagnostic ===` block with per-trait CS yields (asthma id_cs=0 -> real_cs=1, bmi id_cs=3 -> real_cs=8, etc.) — these values must match the prior commit's render exactly.

**Stage and commit.** Use explicit `git add <path>` per file (do NOT use `git add -A` or `git add .` — pre-existing dirty paths must remain untouched).

```bash
PATH=/usr/local/usrapps/clintonlab/miniconda3/bin:$PATH \
git add src/R/figures/fig3_sh2b3_eur_collapse_forest.R \
        docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.pdf \
        docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.png

PATH=/usr/local/usrapps/clintonlab/miniconda3/bin:$PATH \
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
  "docs(quick-260426-04b): disk-derive fig3 SH2B3 EUR locked scalars per audit Eval 4a residual" \
  --files src/R/figures/fig3_sh2b3_eur_collapse_forest.R \
          docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.pdf \
          docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.png
```

The commit message body (passed via gsd-tools or HEREDOC) should include a paragraph explaining: this is self-correction-discipline alignment (NOT a numeric correction); the on-disk scalars are unchanged; the change replaces three literal-list assignments (`EXPECTED_REAL_CS`, `EXPECTED_ID_CS`, `EXPECTED_REAL_STATUS`) with runtime disk-derivation from `IDENTITY-LD-K2D-FIT-SUMMARY.tsv` + `finemap_summary.tsv`; mirrors the precedent set by quick-260425-kki for fig2_cs_yield.R `N_IDENTITY_LD_NONEMPTY` at commit `884eb3d`; closes audit Eval 4a residual; brief-slug 260425-h3p.
  </action>
  <verify>
    <automated>
      # 1) Disk-derivation pathway present and literal-list assignments removed
      grep -E "expected_id_cs_from_disk" src/R/figures/fig3_sh2b3_eur_collapse_forest.R | head -3 && \
      grep -E "expected_real_cs_from_disk" src/R/figures/fig3_sh2b3_eur_collapse_forest.R | head -3 && \
      grep -E "expected_real_status_from_disk" src/R/figures/fig3_sh2b3_eur_collapse_forest.R | head -3 && \
      # CRITICAL gate: no surviving literal-list EXPECTED_* assignments
      test "$(grep -cE '^EXPECTED_ID_CS\s*<-\s*list' src/R/figures/fig3_sh2b3_eur_collapse_forest.R)" = "0" && \
      test "$(grep -cE '^EXPECTED_REAL_CS\s*<-\s*list' src/R/figures/fig3_sh2b3_eur_collapse_forest.R)" = "0" && \
      test "$(grep -cE '^EXPECTED_REAL_STATUS\s*<-\s*list' src/R/figures/fig3_sh2b3_eur_collapse_forest.R)" = "0" && \
      # 2) Re-render succeeds
      PATH=/usr/local/usrapps/clintonlab/miniconda3/bin:$PATH \
      /rs1/researchers/c/ckclinto/conda_envs/la_multitrait_r/bin/Rscript \
        src/R/figures/fig3_sh2b3_eur_collapse_forest.R 2>&1 | tail -20 && \
      # 3) Outputs exist + per-trait CS yields preserved
      test -s docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.pdf && \
      test -s docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.png && \
      # 4) Forbidden-token scan (zero matches)
      ! grep -iE "revision|cleanup|fix-up|mistake|got this wrong|placeholder|TBD|\bv1\b|simplified" \
          src/R/figures/fig3_sh2b3_eur_collapse_forest.R && \
      # 5) Commit landed with prescribed subject
      git log -1 --pretty=%s | grep -F "docs(quick-260426-04b): disk-derive fig3 SH2B3 EUR locked scalars per audit Eval 4a residual" && \
      # 6) Commit touches exactly 3 files
      test "$(git show --stat --name-only HEAD | tail -n +7 | grep -cE '\S')" -ge 3 && \
      # 7) Pre-existing dirty paths untouched in this commit
      ! git show --name-only HEAD | grep -E "^\.claude/(settings\.json|scheduled_tasks\.lock)$" && \
      ! git show --name-only HEAD | grep -E "^\.planning/(STATE|config)\."
    </automated>
  </verify>
  <done>
    fig3_sh2b3_eur_collapse_forest.R has zero literal-list `EXPECTED_*` assignments and three new disk-derived bindings (`expected_id_cs_from_disk`, `expected_real_cs_from_disk`, `expected_real_status_from_disk`); re-render succeeds and produces a PDF + PNG with unchanged per-trait CS counts (asthma 0 -> 1, bmi 3 -> 8, hypertension 10 -> 4, stroke 10 -> 2, t2d 2 -> 9); commit `docs(quick-260426-04b): disk-derive fig3 SH2B3 EUR locked scalars per audit Eval 4a residual` lands with exactly 3 files staged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Freeze H3 LD-reference-quality dose-response scalars in TRACK-A-FROZEN-NUMBERS.md</name>
  <files>.planning/amendments/TRACK-A-FROZEN-NUMBERS.md</files>
  <action>
Use the `Edit` tool to insert a new H3 LIVE block in `TRACK-A-FROZEN-NUMBERS.md` between the existing Stage 2 LIVE block and the SUPERSEDED 12/96 section.

**Insertion target.** Find the current line `## Stage 2 fine-mapping yield (Phase 1 SuSiE-RSS with real 1000G EUR LD)` (the SUPERSEDED block header) and insert the new H3 LIVE block IMMEDIATELY BEFORE it (preserving the existing horizontal rule `---` separator that comes before that header).

**Insertion content.** Insert the following block, framed as a fresh first-time freeze (not a correction or revision):

```markdown
## H3 LD-reference-quality dose-response (post-wa2 H3 figure, 2026-04-26) — LIVE

| Metric | Value |
|---|---|
| EUR fits with measured `ld_overlap_fraction` | 60 (of 96 total Stage 2 real-LD fits; 36 AFR fits excluded — no AFR LD panel loaded) |
| EUR fits below Benner et al. 2017 calibration threshold (`ld_overlap_fraction < 0.5`) | **33 / 60 (55.0%)** |
| EUR fits at or above Benner threshold (≥ 0.5) | 27 / 60 (45.0%) |
| EUR fits with `ld_overlap_fraction = NA` (no LD attempted; coerced to 0 for the dose-response visualization) | 19 / 60 |
| EUR fits with numeric `ld_overlap_fraction` | 41 / 60 |
| Successful QTL-coloc attempts (status = success) | 32 / 1,274 (2.5%; all EUR per disk) |
| Suspect-quadrant points (PP.H4 ≥ 0.5 AND min `ld_overlap_fraction` < 0.5) | **0 / 32** |
| Headline: FTO_16q12 EUR IRX3 / Pancreas / gtex_eqtl | PP.H4 = 0.3099, min `ld_overlap_fraction` = 0 |
| Anchor: SH2B3_12q24 EUR asthma (sole "ok" SH2B3 EUR fit) | `ld_overlap_fraction` = 0.0385 |
| Reference threshold (Benner et al. 2017 AJHG 101:539–551) | 0.5 |
| Tier B threshold | 0.5 |
| Tier A threshold | 0.8 |

**Headline framing (manuscript-anchor language)**: 33 of 60 EUR Stage 2 real-LD fits sit below the Benner et al. 2017 `ld_overlap_fraction = 0.5` calibration threshold; the headline FTO_16q12 EUR Tier-C signal (PP.H4 = 0.3099) was produced by a SuSiE-RSS fit with `ld_overlap_fraction = 0`. Zero of the 32 QTL-coloc successes occupy the strict suspect quadrant (PP.H4 ≥ 0.5 AND min `ld_overlap_fraction` < 0.5) — itself a structural finding consistent with the manuscript's Discussion framing that the inflated identity-LD signal is primarily an LD-inflation artifact. The dose-response figure is positioned as exploratory methodology-validation, NOT a discovery claim.

**Sources**: Derived at runtime by [`src/R/figures/fig_h3_ld_overlap_dose_response.R`](../../src/R/figures/fig_h3_ld_overlap_dose_response.R) (committed at `1e4b071` per quick task `260425-wa2`); source data at [`results/fine_mapping/finemap_summary.tsv`](../../results/fine_mapping/finemap_summary.tsv) (Stage 2 production fire 2026-04-22), per-fit JSONs at `results/fine_mapping/susie/*.json` (96 files), and per-attempt JSONs at `results/qtl_coloc/*.json` (1,274 files; 32 with `status = success`). Reference: Benner et al. 2017, *AJHG* 101:539–551 (LD-reference-panel calibration threshold).

**Caveats** (mandatory disclosure for any downstream cite of these scalars):
1. Panel B of the dose-response figure uses MIN `ld_overlap_fraction` across the 5 GWAS-side trait fits per (region, ancestry) cell as a conservative worst-case bound — the qtl_coloc per-attempt JSON does not record which trait's GWAS-side SuSiE fit was the input.
2. AFR fits (36 of 96 total Stage 2 fits) are excluded from the dose-response visualization because no AFR LD panel was loaded at Stage 2 fire time. Matched-ancestry AFR LD pending M3 (AoU controlled-tier WGS build; see [AOU-LD-PIPELINE.md](./AOU-LD-PIPELINE.md)).
3. The 19 NA → 0 coercion treats "no LD attempted" as dose-response-equivalent to "no LD overlap"; this is the conservative reading of the audit's question.

---
```

**Reconciliation log entry.** ALSO append a new row to the existing `**Reconciliation log**` table near the end of the file (currently anchored under section `## Usage`). Find the table and append:

```markdown
| 2026-04-26 | **H3 LD-reference-quality dose-response scalars frozen**: a new sibling LIVE block ("H3 LD-reference-quality dose-response (post-wa2 H3 figure, 2026-04-26) — LIVE") is inserted between the Stage 2 LIVE block and the SUPERSEDED 12/96 block, freezing the 33/60 / 27/60 / 19/60 / 41/60 / 32/1274 / 0/32 / FTO 0.3099 / SH2B3 0.0385 scalars surfaced by the H3 dose-response figure (`src/R/figures/fig_h3_ld_overlap_dose_response.R`, committed at `1e4b071` per quick task `260425-wa2`). Audit-driven companion to AUDIT-REVIEW-2026-04-25.md High-Quality #3. The block frames the figure as exploratory methodology-validation and is referenced by the new Figure S7 caption block in the manuscript supplementary section (companion atomic commit). | quick-260426-04b (brief-slug 260425-h3p) — Track A audit High-Quality #3 dose-response scalar freeze. |
```

**Stage and commit (atomic, single-file).**

```bash
PATH=/usr/local/usrapps/clintonlab/miniconda3/bin:$PATH \
git add .planning/amendments/TRACK-A-FROZEN-NUMBERS.md

PATH=/usr/local/usrapps/clintonlab/miniconda3/bin:$PATH \
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
  "docs(quick-260426-04b): freeze H3 LD-reference-quality dose-response scalars in TRACK-A-FROZEN-NUMBERS.md" \
  --files .planning/amendments/TRACK-A-FROZEN-NUMBERS.md
```

Commit message body should explain: this is a fresh first-time freeze of the H3 dose-response scalars surfaced by quick-260425-wa2 (no prior values to update); 33/60 EUR fits sit below Benner 0.5; FTO headline at PP.H4=0.3099 with min ld_of=0; zero QTL successes in the strict suspect quadrant; framing is exploratory methodology-validation; brief-slug 260425-h3p; companion to the Figure S7 caption integration commit that follows.
  </action>
  <verify>
    <automated>
      # 1) New H3 LIVE block present and well-formed
      grep -F "## H3 LD-reference-quality dose-response (post-wa2 H3 figure, 2026-04-26) — LIVE" \
        .planning/amendments/TRACK-A-FROZEN-NUMBERS.md && \
      grep -F "33 / 60 (55.0%)" .planning/amendments/TRACK-A-FROZEN-NUMBERS.md && \
      grep -F "0 / 32" .planning/amendments/TRACK-A-FROZEN-NUMBERS.md && \
      grep -F "FTO_16q12 EUR IRX3 / Pancreas" .planning/amendments/TRACK-A-FROZEN-NUMBERS.md && \
      grep -F "0.0385" .planning/amendments/TRACK-A-FROZEN-NUMBERS.md && \
      grep -F "fig_h3_ld_overlap_dose_response.R" .planning/amendments/TRACK-A-FROZEN-NUMBERS.md && \
      grep -F "1e4b071" .planning/amendments/TRACK-A-FROZEN-NUMBERS.md && \
      # 2) Reconciliation log row appended
      grep -F "H3 LD-reference-quality dose-response scalars frozen" \
        .planning/amendments/TRACK-A-FROZEN-NUMBERS.md && \
      # 3) Insertion order: H3 LIVE block precedes the SUPERSEDED Stage 2 block
      awk '/^## H3 LD-reference-quality dose-response/{h3=NR} /^## Stage 2 fine-mapping yield \(Phase 1 SuSiE-RSS with real 1000G EUR LD\)/{sup=NR} END{exit !(h3>0 && sup>0 && h3<sup)}' \
        .planning/amendments/TRACK-A-FROZEN-NUMBERS.md && \
      # 4) Forbidden-token scan against the new block (no apologetic framing)
      ! awk '/^## H3 LD-reference-quality dose-response/,/^---$/' .planning/amendments/TRACK-A-FROZEN-NUMBERS.md | \
          grep -iE "revision|cleanup|fix-up|mistake|got this wrong|\bplaceholder\b|\bTBD\b|\bv1\b|\bsimplified\b|\bfor now\b" && \
      # 5) Commit landed with prescribed subject
      git log -1 --pretty=%s | grep -F "docs(quick-260426-04b): freeze H3 LD-reference-quality dose-response scalars in TRACK-A-FROZEN-NUMBERS.md" && \
      # 6) Commit touches exactly 1 file
      test "$(git show --stat --name-only HEAD | tail -n +7 | grep -cE '\S')" = "1" && \
      git show --name-only HEAD | grep -F ".planning/amendments/TRACK-A-FROZEN-NUMBERS.md" && \
      # 7) Pre-existing dirty paths untouched in this commit
      ! git show --name-only HEAD | grep -E "^\.claude/(settings\.json|scheduled_tasks\.lock)$" && \
      ! git show --name-only HEAD | grep -E "^\.planning/(STATE|config)\."
    </automated>
  </verify>
  <done>
    `.planning/amendments/TRACK-A-FROZEN-NUMBERS.md` carries a new sibling LIVE block titled `## H3 LD-reference-quality dose-response (post-wa2 H3 figure, 2026-04-26) — LIVE` placed before the SUPERSEDED Stage 2 block; the block contains the 33/60, 27/60, 19/60, 41/60, 32/1,274, 0/32, FTO 0.3099, SH2B3 0.0385 scalars + Sources + Caveats subsections; the Reconciliation log table has a new 2026-04-26 row referencing the H3 freeze; commit `docs(quick-260426-04b): freeze H3 LD-reference-quality dose-response scalars in TRACK-A-FROZEN-NUMBERS.md` lands with exactly 1 file staged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate Figure S7 caption — H3 LD-reference-quality dose-response (exploratory methodology-validation) — into manuscript supplementary section</name>
  <files>docs/manuscript/track_a_pivot.md</files>
  <action>
**Pre-edit recheck.** Before editing, run `git status` to detect whether parallel agent C (quick task `260425-mqc` or similar, writing to Methods §QTL Coloc / Discussion §3 / Limitations §6) has landed a commit since Task 2 of this plan. If a rebase is needed:

```bash
PATH=/usr/local/usrapps/clintonlab/miniconda3/bin:$PATH git pull --rebase
```

Re-resolve the L303 anchor location after rebase if necessary; the H3 caption block is logically independent of C's Methods/Discussion/Limitations edits, so the rebase should be conflict-free under last-write-wins per the brief.

**Locate insertion point.** The existing `## Figure legends` section in `track_a_pivot.md` ends at the Figure S1–S6 caption (currently L303). Insert a new `**Figure S7.**` block IMMEDIATELY AFTER the Figure S1–S6 block, BEFORE the next horizontal-rule separator (`---`) that delimits the §References section.

**Insertion content.** Use the `Edit` tool to append the following block (preserve the existing blank-line / horizontal-rule structure):

```markdown
**Figure S7.** Exploratory LD-reference-quality dose-response visualization at admissible Track A loci — methodology-validation, not discovery. Two-panel composite plotting the dependence of (A) SuSiE-RSS credible-set yield and (B) QTL-coloc PP.H4 on the `ld_overlap_fraction` of the real 1000 Genomes Phase 3 EUR LD-reference panel, in response to the audit-author High-Quality recommendation #3 (`AUDIT-REVIEW-2026-04-25.md`). **(A)** SuSiE-RSS credible-set yield × LD-reference quality across the **60 EUR (region, trait) Stage 2 fits with measured `ld_overlap_fraction`** (the 36 AFR fits are excluded because no AFR LD reference panel is loaded at Stage 2 fire time — see `.planning/amendments/TRACK-A-FROZEN-NUMBERS.md` scope caveat); **33 of 60 EUR fits (55.0%) sit below the Benner et al. 2017 `ld_overlap_fraction = 0.5` calibration threshold** (vertical reference line); 19 of those 33 had no LD attempted at all (`ld_overlap_fraction = NA`, coerced to 0 under the conservative reading that "no LD measured" is dose-response-equivalent to "no LD overlap" for the audit's question). Annotated points: SH2B3_12q24 EUR asthma (`ld_overlap_fraction` = 0.0385, the sole "ok" SH2B3 EUR fit) and FTO_16q12 EUR (`ld_overlap_fraction` = 0, the headline-Tier-C locus). **(B)** QTL-coloc PP.H4 × GWAS-side LD-reference quality across the **32 successful (region, gene, tissue, qtl_source) qtl_coloc attempts** (all EUR per disk; 1,242 of 1,274 attempts returned no signal). The x-axis uses **MIN `ld_overlap_fraction` across the 5 GWAS-side trait fits at the same (region, ancestry) cell** as a conservative worst-case bound, because `qtl_coloc/*.json` does not record which trait's GWAS-side SuSiE fit was the input. Reference horizontal lines at PP.H4 = 0.5 (Tier B) and 0.8 (Tier A). The headline FTO_16q12 EUR IRX3 / Pancreas Tier-C signal (PP.H4 = 0.3099, min `ld_overlap_fraction` = 0) is annotated as the structural inflation flag the audit surfaced. **Zero of the 32 successes occupy the strict suspect quadrant (PP.H4 ≥ 0.5 AND min `ld_overlap_fraction` < 0.5)** — itself a structural finding consistent with the manuscript's "primarily an LD-inflation artifact" Discussion framing. **Framing.** This figure is positioned as exploratory methodology-validation rather than as a discovery claim — the visualization quantifies "what LD-reference quality is needed for a `coloc.susie` PP.H4 to be trustworthy?" rather than asserting which specific Tier-C signals survive. Frozen scalars: `.planning/amendments/TRACK-A-FROZEN-NUMBERS.md` §H3 LD-reference-quality dose-response. Build provenance: [`src/R/figures/fig_h3_ld_overlap_dose_response.R`](../../src/R/figures/fig_h3_ld_overlap_dose_response.R) (commit `1e4b071`, quick task `260425-wa2`); rendered outputs: `docs/manuscript/figures/fig_h3_ld_overlap_dose_response.pdf` + `.png` (cairo_pdf, 170 mm × 200 mm, 600 dpi). Reference: Benner et al. 2017, *AJHG* 101:539–551 (LD-reference-panel calibration threshold).

```

**Update the Figure S1–S6 block heading on L303** so its enumeration acknowledges S7. Find the line beginning `**Figure S1–S6.** Supplementary figures covering ...` and change the leading marker from `**Figure S1–S6.**` to `**Figure S1–S6.**` is unchanged in body content — instead, add a sentence at the END of the existing line that points forward to S7:

Append (do NOT rewrite the existing S1–S6 enumeration content):

```
 (S7) audit-driven exploratory LD-reference-quality dose-response visualization (see immediately following caption).
```

**Stage and commit (atomic, single-file).**

```bash
PATH=/usr/local/usrapps/clintonlab/miniconda3/bin:$PATH \
git add docs/manuscript/track_a_pivot.md

PATH=/usr/local/usrapps/clintonlab/miniconda3/bin:$PATH \
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
  "docs(quick-260426-04b): integrate Figure S7 caption — H3 LD-reference-quality dose-response (exploratory methodology-validation)" \
  --files docs/manuscript/track_a_pivot.md
```

Commit message body should explain: this is the exploratory methodology-validation figure caption integration that closes the brief's three-item set; cites the 33/60 scalar from FROZEN-NUMBERS.md §H3 (frozen in the immediately prior commit); references commit 1e4b071 + quick task 260425-wa2 as build provenance; placed at the Figure S7 supplementary slot to preserve the locked main 5-figure roster (Fig 1A/1B/2/3/5 main + Fig 4 demoted to S5); brief-slug 260425-h3p.
  </action>
  <verify>
    <automated>
      # 1) New Figure S7 caption block present
      grep -F "**Figure S7.**" docs/manuscript/track_a_pivot.md && \
      grep -F "Exploratory LD-reference-quality dose-response visualization" docs/manuscript/track_a_pivot.md && \
      # 2) Caption cites the 33/60 scalar and the FTO/SH2B3 anchors
      grep -F "33 of 60 EUR fits (55.0%)" docs/manuscript/track_a_pivot.md && \
      grep -F "PP.H4 = 0.3099" docs/manuscript/track_a_pivot.md && \
      grep -F "ld_overlap_fraction\` = 0.0385" docs/manuscript/track_a_pivot.md && \
      # 3) Caption cites FROZEN-NUMBERS §H3 + commit 1e4b071 + quick-260425-wa2
      grep -F "TRACK-A-FROZEN-NUMBERS.md" docs/manuscript/track_a_pivot.md | grep -F "H3 LD-reference-quality dose-response" && \
      grep -F "1e4b071" docs/manuscript/track_a_pivot.md && \
      grep -F "260425-wa2" docs/manuscript/track_a_pivot.md && \
      # 4) Framing language is exploratory methodology-validation (not discovery)
      grep -F "exploratory methodology-validation" docs/manuscript/track_a_pivot.md && \
      grep -F "not discovery" docs/manuscript/track_a_pivot.md && \
      # 5) Suspect-quadrant + zero-suspect framing
      grep -F "Zero of the 32 successes" docs/manuscript/track_a_pivot.md && \
      # 6) Forbidden-token scan against the inserted Figure S7 block (no apologetic framing)
      ! awk '/^\*\*Figure S7\.\*\*/,/^$/' docs/manuscript/track_a_pivot.md | \
          grep -iE "revision|cleanup|fix-up|mistake|got this wrong|\bplaceholder\b|\bTBD\b|\bv1\b|\bsimplified\b|\bfor now\b" && \
      # 7) Insertion order: S7 caption sits after the S1–S6 block
      awk '/\*\*Figure S1–S6\.\*\*/{s16=NR} /\*\*Figure S7\.\*\*/{s7=NR} END{exit !(s16>0 && s7>0 && s7>s16)}' \
        docs/manuscript/track_a_pivot.md && \
      # 8) Commit landed with prescribed subject
      git log -1 --pretty=%s | grep -F "docs(quick-260426-04b): integrate Figure S7 caption — H3 LD-reference-quality dose-response (exploratory methodology-validation)" && \
      # 9) Commit touches exactly 1 file
      test "$(git show --stat --name-only HEAD | tail -n +7 | grep -cE '\S')" = "1" && \
      git show --name-only HEAD | grep -F "docs/manuscript/track_a_pivot.md" && \
      # 10) Pre-existing dirty paths untouched
      ! git show --name-only HEAD | grep -E "^\.claude/(settings\.json|scheduled_tasks\.lock)$" && \
      ! git show --name-only HEAD | grep -E "^\.planning/(STATE|config)\."
    </automated>
  </verify>
  <done>
    `docs/manuscript/track_a_pivot.md` carries a new `**Figure S7.**` caption block in the supplementary section after the existing Figure S1–S6 block; the caption frames the figure as exploratory methodology-validation (not discovery), cites the 33/60 scalar from FROZEN-NUMBERS.md §H3 LD-reference-quality dose-response, references commit 1e4b071 + quick task 260425-wa2, and covers both panels with annotated FTO + SH2B3 anchors; commit `docs(quick-260426-04b): integrate Figure S7 caption — H3 LD-reference-quality dose-response (exploratory methodology-validation)` lands with exactly 1 file staged.
  </done>
</task>

</tasks>

<verification>
**Plan-level verification (run after all 3 tasks land).**

```bash
# 1) Three commits with the prescribed subject lines, in the prescribed order.
PATH=/usr/local/usrapps/clintonlab/miniconda3/bin:$PATH \
git log -3 --pretty='%h %s' | grep -E "260426-04b" | wc -l   # expect 3

# 2) Cross-task scalar consistency: every numerator quoted by the Figure S7 caption
#    is also present in the FROZEN-NUMBERS H3 LIVE block.
for needle in "33 / 60" "27 / 60" "19 / 60" "41 / 60" "0 / 32" "32 / 1,274" \
              "0.3099" "0.0385"; do
  test $(grep -F "$needle" .planning/amendments/TRACK-A-FROZEN-NUMBERS.md | wc -l) -ge 1 || { echo "FAIL FROZEN: $needle"; exit 1; }
done

# 3) The fig3 disk-derivation pathway, the FROZEN-NUMBERS H3 block, and the Figure S7
#    caption all reference the same provenance triple (commit 1e4b071, quick-260425-wa2,
#    fig_h3_ld_overlap_dose_response.R).
grep -l "1e4b071" .planning/amendments/TRACK-A-FROZEN-NUMBERS.md docs/manuscript/track_a_pivot.md | wc -l   # expect 2
grep -l "260425-wa2" .planning/amendments/TRACK-A-FROZEN-NUMBERS.md docs/manuscript/track_a_pivot.md | wc -l   # expect 2

# 4) Forbidden-token global scan across all 3 commit messages.
PATH=/usr/local/usrapps/clintonlab/miniconda3/bin:$PATH \
git log -3 --pretty=%B | grep -iE "revision|cleanup|fix-up|mistake|got this wrong|\bplaceholder\b|\bTBD\b|\bv1\b|\bsimplified\b|\bfor now\b" \
  && { echo "FAIL: forbidden token in commit messages"; exit 1; } \
  || echo "OK: forbidden-token scan clean across 3 commit messages"

# 5) Pre-existing dirty paths (.claude/settings.json, .planning/config.json, .claude/scheduled_tasks.lock,
#    .planning/STATE.md, .planning/quick/260426-04b-*/) MUST NOT appear in any of the 3 commits.
PATH=/usr/local/usrapps/clintonlab/miniconda3/bin:$PATH \
git log -3 --name-only --pretty= | sort -u | \
  grep -E "^(\.claude/settings\.json|\.claude/scheduled_tasks\.lock|\.planning/config\.json|\.planning/STATE\.md|\.planning/quick/260426-04b-)" \
  && { echo "FAIL: prohibited path appears in a 260426-04b commit"; exit 1; } \
  || echo "OK: no prohibited paths in commits"

# 6) Total file-count across the 3 commits = 5 (T1=3, T2=1, T3=1).
PATH=/usr/local/usrapps/clintonlab/miniconda3/bin:$PATH \
git log -3 --name-only --pretty= | sort -u | grep -cE '\S'   # expect 5
```

If any of the six checks fails the plan is incomplete; the executor should diagnose and re-stage rather than declare done.
</verification>

<success_criteria>
1. Three atomic commits land in sequential order with the prescribed subject lines:
   - `docs(quick-260426-04b): disk-derive fig3 SH2B3 EUR locked scalars per audit Eval 4a residual`
   - `docs(quick-260426-04b): freeze H3 LD-reference-quality dose-response scalars in TRACK-A-FROZEN-NUMBERS.md`
   - `docs(quick-260426-04b): integrate Figure S7 caption — H3 LD-reference-quality dose-response (exploratory methodology-validation)`
2. Total of 5 files staged across the 3 commits (T1: R script + PDF + PNG = 3 files; T2: FROZEN-NUMBERS.md = 1 file; T3: track_a_pivot.md = 1 file).
3. Eval 4a residual closed: zero literal `EXPECTED_ID_CS <- list`, `EXPECTED_REAL_CS <- list`, `EXPECTED_REAL_STATUS <- list` assignments survive in `fig3_sh2b3_eur_collapse_forest.R`; replaced by `expected_id_cs_from_disk` / `expected_real_cs_from_disk` / `expected_real_status_from_disk` derived at runtime from `IDENTITY-LD-K2D-FIT-SUMMARY.tsv` + `finemap_summary.tsv`.
4. Re-rendered `fig3_sh2b3_eur_collapse_forest.{pdf,png}` reproduce the prior commit's per-trait CS counts verbatim (asthma 0 -> 1, bmi 3 -> 8, hypertension 10 -> 4, stroke 10 -> 2, t2d 2 -> 9).
5. `TRACK-A-FROZEN-NUMBERS.md` carries a new sibling LIVE block titled `## H3 LD-reference-quality dose-response (post-wa2 H3 figure, 2026-04-26) — LIVE` placed between the existing Stage 2 LIVE block and the SUPERSEDED 12/96 block, freezing the 33/60 / 27/60 / 19/60 / 41/60 / 32/1274 / 0/32 / FTO 0.3099 / SH2B3 0.0385 scalars.
6. `track_a_pivot.md` carries a new `**Figure S7.**` caption block in the supplementary section frame (placed AFTER the existing Figure S1–S6 block, BEFORE the §References horizontal rule). Caption frames figure as exploratory methodology-validation, NOT discovery; cites FROZEN-NUMBERS §H3 + commit 1e4b071 + quick-260425-wa2.
7. Forbidden-token scan returns zero matches across all three commit messages, the inserted FROZEN-NUMBERS H3 block, the inserted Figure S7 caption, and the fig3 R script edits.
8. Pre-existing dirty paths (`.claude/settings.json`, `.claude/scheduled_tasks.lock`, `.planning/config.json`, `.planning/STATE.md`, `.planning/quick/260426-04b-*/`) remain untouched in all three commits.
</success_criteria>

<output>
After completion, create `.planning/quick/260426-04b-h3-figure-polish-audit-residual-cleanup-/260426-04b-SUMMARY.md` with:

- One-liner positioning the work as the audit-Eval-4a-residual close + H3 dose-response scalar freeze + Figure S7 exploratory methodology-validation caption integration
- Three-row commit table (hash + subject + files) for the three atomic commits
- Disk-verified scalar table reaffirming the on-disk values exposed by Task 1 (asthma 0/1, bmi 3/8, hypertension 10/4, stroke 10/2, t2d 2/9)
- The H3 dose-response scalar block as frozen in FROZEN-NUMBERS.md (verbatim copy)
- The Figure S7 caption framing rationale (why supplementary slot, why exploratory methodology-validation)
- Forbidden-token greppable check confirmation (zero matches)
- Plan-vs-execution alignment (3/3 tasks, 3 atomic commits, no plan deviations)
- Coordination note re: parallel agent C (260425-mqc) — whether a rebase was required at Task 3 stage time
- Self-Check section confirming all 8 success criteria
</output>
