---
phase: 260716-0lj
plan: 01
quick_id: 260716-0lj
type: execute
mode: quick
wave: 1
depends_on: []
autonomous: true
tags: [docs, m3, occlusion, present-rate, plan-reconciliation, red-is-the-contract]
files_modified:
  - .planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md
requirements:
  - REQ-PUBLIC-DATA-ONLY
base_commit: 13a2e6c

must_haves:
  truths:
    - "The m3-07c PLAN's T3 spec names the RED's ACTUAL export (scan_present_rate) and the RED's ACTUAL return keys (n_traits_present / n_traits_scanned / present_rate / traits_present) at EVERY site — zero occurrences of present_rate_for_variants / present_count / scanned_count / files_present remain in the plan."
    - "The PLAN states the scan's key TYPE explicitly: a (chr:int, pos:int) GRCh37 TUPLE — the RED does res[target] where target = (1, 5_982_778)."
    - "The PLAN records the 63bdb59 seam contract it predates: enrich_occlusion_manifest(present_rate=...) consumes EXACTLY {(chr, pos_grch37): {...}}, joins POST-lift on (chr, pos_grch37) (NOT variant_id), and RAISES ValueError if liftable keys exist but none match — so scan_present_rate's return is DIRECTLY feedable to present_rate=."
    - "The PLAN carries a plan_vs_red_reconciliation note (mirroring f3b79fe) recording what drifted and that it was checked against the RED POST-63bdb59, so the next reader does not re-derive it — and the note is HONESTLY SCOPED (it does not claim T4 is reconciled)."
    - "NOTHING in src/ or tests/ changed: `git diff --stat 13a2e6c..HEAD -- src/ tests/` is EMPTY. The 15 RED failures stay RED (6 present_rate + 9 lockstep). m3-07c is NOT started."
  artifacts:
    - path: ".planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md"
      provides: "The m3-07c plan, reconciled to the 07a RED contract + the 63bdb59 seam"
      contains: "scan_present_rate"
  key_links:
    - from: ".planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md"
      to: "tests/m3/test_occlusion_present_rate_scan.py"
      via: "the plan's T3 spec now matches the RED's export + return keys (tests are the contract)"
      pattern: "scan_present_rate"
    - from: ".planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md"
      to: "src/python/occlusion_manifest.py"
      via: "the documented present_rate= seam: {(chr, pos_grch37): {...}} joined post-lift"
      pattern: "enrich_occlusion_manifest"
---

<objective>
**DOCS-ONLY.** Reconcile the m3-07c PLAN to the 07a RED that governs it, BEFORE 07c executes.

The handoff claims "07c's plan was CHECKED for the drift that hit 07b's plan — it has none."
**That claim is STALE and WRONG.** The plan's T3 spec names an export and a return shape that
appear **nowhere** in the RED (grep-verified: 0 occurrences of all four names). An executor
handed this plan would face an unsatisfiable spec whose likeliest escape is **editing the tests** —
the exact failure mode [[feedback_check_plan_against_red_before_executing]] exists to prevent.

**PRECEDENCE (settled by m3-07b — do NOT re-litigate): TESTS ARE THE CONTRACT.** 07b's SUMMARY:
"Manifest API follows the TESTS' names …; the plan's prose names differ and are provided as
aliases. Tests are the contract." Precedent `f3b79fe` reconciled 07b's plan to 07a's corrected RED
pre-execution and caught 3 contradictions. **This is the same move: FIX THE PLAN, NOT THE RED.**

Purpose: make the 07c plan EXECUTABLE-AS-WRITTEN, and record the reconciliation so the next
reader does not re-derive it.
Output: one reconciled PLAN.md. **No code. m3-07c is NOT started** (it needs Carter's explicit GO).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md

# THE CONTRACT — read, DO NOT EDIT:
@tests/m3/test_occlusion_present_rate_scan.py

<interfaces>
<!-- Verified against the working tree @ m3-W2-aou-deltas, HEAD 13a2e6c, this session. -->

THE VERIFIED T3 DRIFT (all four grep-confirmed at 0 occurrences in the RED):

| concern        | PLAN says (WRONG)          | RED requires (AUTHORITATIVE) |
|----------------|----------------------------|------------------------------|
| function name  | `present_rate_for_variants`| `scan_present_rate`          |
| numerator key  | `present_count`            | `n_traits_present`           |
| denominator key| `scanned_count`            | `n_traits_scanned`           |
| trait-list key | `files_present`            | `traits_present`             |

RED evidence (tests/m3/test_occlusion_present_rate_scan.py):
```python
target = (1, 5_982_778)                              # :72  — (chr:int, pos:int) GRCh37 TUPLE
res = prs.scan_present_rate([target], [f1, f2, f3])  # :79
rec = res[target]                                    # :81  — keyed BY THE TUPLE
assert rec["n_traits_present"] == 2                  # :82
assert rec["n_traits_scanned"] == 3                  # :83
assert rec["present_rate"] == pytest.approx(2 / 3)   # :84
assert sorted(res[v]["traits_present"]) == ["bmi", "sbp"]   # :132
```
More at :96-101, :111-113, :155-157, :168-172.

THE PLAN ALSO CONTRADICTS ITSELF: :96 says "populates the manifest's traits_present /
n_traits_present columns" (the RED's COLUMN names — CORRECT) while :117 specs
`-> per-variant {present_count, scanned_count, present_rate, files_present}` (WRONG).

THE SIX `present_rate_for_variants` SITES (grep-confirmed — fix ALL, not just the first):
  :20  must_haves/truths      :27  artifact `exports:`     :96  prose (interfaces)
  :117 Task-1 `<behavior>`    :129 Task-1 `<action>`       :142 Task-1 `<done>`
The wrong RETURN KEYS live at :117-118 only.

THE 63bdb59 SEAM THE PLAN PREDATES (src/python/occlusion_manifest.py, landed this session):
`enrich_occlusion_manifest(manifest_path, chain_path, *, out_path=None, present_rate=None)` (:320-323)
  * `present_rate` is keyed EXACTLY `{(chr, pos_grch37): {...}}` — GRCh37, POST-LIFTOVER (:330-331);
    the docstring itself cites `test_occlusion_present_rate_scan.py:72` as the key's source of truth.
  * joins on `(chr, pos_grch37)` via `_present_rate_key` (:296-317, :372-374) — **NOT** on `variant_id`
    (the scan reads GRCh37 sumstats by (CHR,POS) and can never compute a GRCh38 variant_id).
  * RAISES `ValueError` when non-empty present_rate + >=1 liftable row + ZERO key matches (:385-388).
  * `STAGE_B_TRAIT_COLUMNS = ["traits_present", "n_traits_present", "n_traits_scanned"]` (:103) —
    the RED's key names ARE the manifest's column names.
  * 4 seam tests added by 63bdb59 in tests/m3/test_occlusion_manifest.py:
    test_present_rate_joins_on_the_chr_pos_grch37_tuple_key,
    test_present_rate_leaves_na_for_a_variant_that_did_not_lift,
    test_present_rate_matching_no_liftable_row_raises_not_silent_na,
    test_present_rate_against_a_wholly_unliftable_manifest_does_not_raise.
=> `scan_present_rate`'s return is **DIRECTLY FEEDABLE** to `present_rate=`. The plan's :95-96
   ALREADY correctly assumed the manifest carries `(chr, pos_grch37)`; 63bdb59 aligned the shipped
   consumer to that assumption, so **the plan's premise is now TRUE in code**.

RED BASELINE (must be UNCHANGED by this docs-only task):
  tests/m3/test_occlusion_present_rate_scan.py = **6** tests, all RED
  tests/m3/test_occlusion_lockstep_drop.py     = **9** tests, all RED
  (= 15 total. NOTE: the split is 6+9, NOT 9+6 — collect-only verified this session.)
  Full tests/m3 baseline @ 13a2e6c: **15 failed / 405 passed / 31 skipped**.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the m3-07c PLAN's T3 spec to the 07a RED + record the 63bdb59 seam (DOCS-ONLY)</name>
  <read_first>
    .planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md (the file to fix),
    tests/m3/test_occlusion_present_rate_scan.py lines 60-175 (THE CONTRACT — read only, NEVER edit),
    src/python/occlusion_manifest.py lines 296-390 (the 63bdb59 present_rate= seam — read only).
  </read_first>
  <files>.planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md</files>
  <action>
    Edit ONLY the file above. **Touch NOTHING in src/ or tests/.**

    (1) RENAME the spec'd export at ALL SIX sites: `present_rate_for_variants` -> `scan_present_rate`.
        Known sites: :20 (must_haves/truths), :27 (artifact `exports:`), :96 (prose), :117
        (Task-1 `<behavior>`), :129 (Task-1 `<action>`), :142 (Task-1 `<done>`).
        **GREP THE WHOLE FILE — do not fix only the first hit.** (f3b79fe's lesson.)

    (2) FIX THE RETURN-SHAPE SPEC at :117-118 to the RED's ACTUAL keys — replace
        `{present_count, scanned_count, present_rate, files_present}` with a per-variant dict:
        `{"n_traits_present": int, "n_traits_scanned": int, "present_rate": float, "traits_present": list[str]}`.
        This also resolves the plan's SELF-CONTRADICTION (:96 already used the RED's names).

    (3) MAKE THE KEY TYPE EXPLICIT AND LOAD-BEARING in the Task-1 `<behavior>`: the return is keyed by a
        **(chr:int, pos:int) GRCh37 TUPLE** — the RED does `res[target]` where `target = (1, 5_982_778)`
        (test_occlusion_present_rate_scan.py:72,81). Not a string, not a variant_id.

    (4) ADD THE 63bdb59 SEAM CONTRACT to the `<interfaces>` block (the plan PREDATES it and does not know it):
        `enrich_occlusion_manifest(present_rate=...)` in `src/python/occlusion_manifest.py` now consumes
        EXACTLY `{(chr, pos_grch37): {...}}`, joins on `(chr, pos_grch37)` **POST-lift** (NOT `variant_id`),
        and **RAISES ValueError** if liftable keys exist but not one matches. Therefore `scan_present_rate`'s
        return is **directly feedable** to `present_rate=` — no adapter. Cite `occlusion_manifest.py` and the
        4 seam tests 63bdb59 added to `tests/m3/test_occlusion_manifest.py` (named in `<interfaces>` above).
        **State explicitly** that the plan's :95-96 ALREADY correctly assumed the manifest carries
        `(chr, pos_grch37)`, and that 63bdb59 aligned the shipped consumer to that assumption — so the
        plan's premise is now TRUE in code, not aspirational.

    (5) DO NOT CHANGE THE SCIENCE OR SCOPE. Preserve verbatim in intent:
        T3 = present-rate scan over the 9 public GRCh37 AFR harmonized sumstats (header
        `CHR POS REF ALT BETA SE P EAF N SNP_ID TRAIT ANCESTRY BUILD`, CHR/POS auto-detected BY NAME);
        T4 = the reusable lockstep (CHR,POS) drop-only filter; the m3-04 consume-wiring stays a DISCLOSED
        deferral (`finemap.smk` m3-04-W4 STALE/SUPERSEDED-PENDING-REPLAN). REQ-PUBLIC-DATA-ONLY, no perimeter.
        Do not renumber tasks, do not touch the threat_model, do not alter T4's science.

    (6) ADD a `plan_vs_red_reconciliation` note (mirror `f3b79fe`'s shape) near the end of the plan recording:
        WHAT drifted (the 4-row table above), THAT the RED is authoritative and UNEDITED (0-line diff),
        THAT tests-are-the-contract is settled precedent (07b SUMMARY), and THAT the plan was checked
        against the RED **POST-63bdb59** — so the next reader does not re-derive it.
        **SCOPE THE NOTE HONESTLY — this is the whole point of the note.** The stale "it has none" claim is
        what burned this session; do not replace it with a fresh over-broad claim. The note MUST say the
        reconciliation covered **T3 only**, and MUST carry this OPEN flag verbatim in substance:
          "T4 OPEN / NOT RECONCILED — REPORTED, awaiting Carter's call. T4's function NAME matches the RED,
           but its SIGNATURE and RETURN SHAPE do NOT: this plan specs
           `drop_occluded_from_sumstats(sumstats_df, manifest, build='GRCh37') -> (filtered_df, drop_log)`
           (:21, :156) while the RED pins
           `drop_occluded_from_sumstats(sumstats_path, manifest_path, out_path) -> dict`
           (test_occlusion_lockstep_drop.py:10 docstring; call sites :104,:121,:136,:196-197,:314; the dict
           is asserted on `n_in`/`n_dropped`/`n_out` at :241-247). TESTS ARE THE CONTRACT -> the RED's
           file-in/file-out signature GOVERNS. This was out of scope for the 260716-0lj pass (T4 fix not
           authorized) and MUST be resolved before 07c's T4 executes."

    Commit DOCS-ONLY with EXPLICIT PATHS (never `git add -A` / `.` on this GPFS tree).
    **DO NOT STAGE** the pre-existing dirty files: `.claude/settings.json`,
    `.planning/quick/260625-r6m-*/260625-r6m-SUMMARY.md`, `tests/m3/sparse_parent_benchmark.tsv`.
    Suggested message: `docs(m3-07c): reconcile the plan's T3 spec to the 07a RED + record the 63bdb59 seam`.
  </action>
  <acceptance_criteria>
    - The four drifted names are GONE from the plan:
      `grep -c "present_rate_for_variants\|present_count\|scanned_count\|files_present" <plan>` == 0.
    - `grep -c "scan_present_rate" <plan>` >= 6 (all six renamed sites).
    - All four RED keys present: `grep -c "n_traits_present" <plan>` >=1, `n_traits_scanned` >=1,
      `traits_present` >=1, `present_rate` >=1.
    - The tuple key is stated: `grep -c "5_982_778\|5982778" <plan>` >= 1.
    - The seam is recorded: `grep -c "enrich_occlusion_manifest" <plan>` >= 1 AND `grep -c "63bdb59" <plan>` >= 1.
    - The reconciliation note exists AND is honestly scoped:
      `grep -c "plan_vs_red_reconciliation" <plan>` >= 1 AND `grep -c "T4 OPEN" <plan>` >= 1.
    - **CODE GATE (hard):** `git diff --stat 13a2e6c..HEAD -- src/ tests/` is **EMPTY**.
    - **RED GATE (hard):** the 15 failures STAY RED, split **6** (present_rate) + **9** (lockstep).
      A drop below 15 means 07c was BUILT -> **STOP, do not commit, report.**
    - **REGRESSION (hard):** full `tests/m3` UNCHANGED at **15 failed / 405 passed / 31 skipped**
      (baseline HEAD 13a2e6c). Docs-only cannot move this; any delta = STOP.
  </acceptance_criteria>
  <verify>
    <automated>
    set -u
    PLAN=.planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md
    PYTEST=/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/pytest

    # 1. drifted names GONE (grep -c returns 0 -> `|| true` keeps set -e semantics sane)
    test "$(grep -c 'present_rate_for_variants\|present_count\|scanned_count\|files_present' "$PLAN" || true)" -eq 0

    # 2. RED's names PRESENT
    test "$(grep -c 'scan_present_rate' "$PLAN")" -ge 6
    grep -q 'n_traits_present' "$PLAN" && grep -q 'n_traits_scanned' "$PLAN" && grep -q 'traits_present' "$PLAN"
    grep -q '5_982_778\|5982778' "$PLAN"                     # tuple key stated
    grep -q 'enrich_occlusion_manifest' "$PLAN" && grep -q '63bdb59' "$PLAN"   # seam recorded
    grep -q 'plan_vs_red_reconciliation' "$PLAN" && grep -q 'T4 OPEN' "$PLAN"  # note + honest scope

    # 3. HARD CODE GATE — src/ and tests/ untouched (MUST be empty)
    test -z "$(git diff --stat 13a2e6c..HEAD -- src/ tests/)"

    # 4. RED stays RED, 6 + 9 (NOT 9 + 6)
    $PYTEST tests/m3/test_occlusion_present_rate_scan.py -q 2>&1 | tail -1 | grep -q '6 failed'
    $PYTEST tests/m3/test_occlusion_lockstep_drop.py     -q 2>&1 | tail -1 | grep -q '9 failed'

    # 5. FULL REGRESSION — ~6.5 min, RUN IN BACKGROUND (run_in_background:true), then assert:
    #    $PYTEST tests/m3 -q  ->  15 failed / 405 passed / 31 skipped  (UNCHANGED)
    echo "VERIFY OK (foreground gates); full tests/m3 regression must be confirmed from the backgrounded run"
    </automated>
  </verify>
  <done>
    The m3-07c PLAN names `scan_present_rate` at all six sites with the RED's actual return keys
    (`n_traits_present` / `n_traits_scanned` / `present_rate` / `traits_present`) keyed by the
    `(chr:int, pos:int)` GRCh37 tuple; the 63bdb59 `present_rate=` seam is recorded as directly feedable;
    the science/scope is unchanged; an honestly-scoped `plan_vs_red_reconciliation` note records the T3 fix
    AND flags T4 as OPEN/REPORTED; `git diff --stat 13a2e6c..HEAD -- src/ tests/` is EMPTY; the 15 RED
    failures stand (6+9); full tests/m3 unchanged at 15/405/31; **m3-07c is NOT started**.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan prose -> executor | The plan is the PROMPT. A drifted spec is an unsatisfiable instruction whose likeliest escape is the executor EDITING THE TESTS — i.e. the plan can corrupt the contract by proxy. This task closes that. |
| docs edit -> code/tests tree | A "docs-only" task that touches `src/` or `tests/` would silently start 07c and/or edit the RED. Gated by `git diff --stat 13a2e6c..HEAD -- src/ tests/` == EMPTY. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0lj-01 | Tampering | tests/m3/test_occlusion_present_rate_scan.py (the RED contract) | mitigate | Task is docs-only; hard gate `git diff --stat 13a2e6c..HEAD -- src/ tests/` EMPTY + RED-stays-RED (6+9). Tests-are-the-contract precedent (07b) is restated IN the plan so a later executor cannot "resolve" a mismatch by editing the RED. |
| T-0lj-02 | Elevation of privilege | m3-07c scope | mitigate | 07c needs Carter's EXPLICIT GO (not given). No `src/python/occlusion_present_rate_scan.py` or `drop_occluded_from_sumstats.py` created; RED failure count dropping below 15 = STOP signal. |
| T-0lj-03 | Repudiation / stale claim | the reconciliation record | mitigate | The stale "checked, has none" claim caused this rework. The new note is scope-bound to T3 and carries an explicit `T4 OPEN` flag rather than a fresh over-broad "clean" assertion. |
| T-0lj-04 | Tampering | GPFS shared tree staging | mitigate | Explicit-path commit only; the 3 known pre-existing dirty files are named as DO-NOT-STAGE ([[feedback_multi_terminal_staging]]). |
</threat_model>

<verification>
- Drifted names gone (count 0); `scan_present_rate` at >=6 sites; all four RED keys present.
- Tuple key `(1, 5_982_778)` stated; 63bdb59 seam + `enrich_occlusion_manifest` recorded.
- `plan_vs_red_reconciliation` note present, scoped to T3, carrying the `T4 OPEN` flag.
- **HARD:** `git diff --stat 13a2e6c..HEAD -- src/ tests/` EMPTY (nothing in src/ or tests/ moved).
- **HARD:** RED stays RED at 6 (present_rate) + 9 (lockstep) = 15.
- **HARD:** full `tests/m3` UNCHANGED at 15 failed / 405 passed / 31 skipped. ~6.5 min -> **BACKGROUND IT**
  (`run_in_background: true`) and confirm before closing; do not block the foreground on it.
- NO perimeter access, NO spend, NO loop contact. m3-07c NOT started.
</verification>

<success_criteria>
- The m3-07c PLAN is EXECUTABLE-AS-WRITTEN for T3: every name it specs exists in the RED, and every
  name the RED requires is what the plan specs. The self-contradiction at :96 vs :117 is resolved.
- The 63bdb59 seam is documented, so 07c's executor feeds `scan_present_rate` -> `present_rate=` with
  no adapter and no rediscovery of the ValueError raise-boundary.
- The reconciliation is recorded honestly and scope-bound (T3 done; T4 flagged OPEN) — the next reader
  neither re-derives the T3 drift nor inherits a fresh stale "it's clean" claim.
- Code and tests are byte-untouched; the RED stands at 15; **07c remains un-started pending Carter's GO**.
</success_criteria>

<output>
After completion, create `.planning/quick/260716-0lj-reconcile-the-m3-07c-plan-to-the-07a-red/260716-0lj-SUMMARY.md`.
Record: the 4-row T3 drift table as FIXED; the six renamed sites; the 63bdb59 seam paragraph added;
the `git diff --stat 13a2e6c..HEAD -- src/ tests/` EMPTY confirmation; the RED-still-15 (6+9) confirmation;
the full tests/m3 15/405/31 unchanged confirmation; and the **T4 OPEN signature drift as a REPORTED
open decision for Carter** (name matches, signature/return does not).
</output>
