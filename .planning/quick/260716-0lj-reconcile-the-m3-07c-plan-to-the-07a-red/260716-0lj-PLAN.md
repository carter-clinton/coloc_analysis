---
phase: 260716-0lj
plan: 01
quick_id: 260716-0lj
type: execute
mode: quick
wave: 1
depends_on: []
autonomous: true
tags: [docs, m3, occlusion, present-rate, lockstep, plan-reconciliation, red-is-the-contract]
files_modified:
  - .planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md
requirements:
  - REQ-PUBLIC-DATA-ONLY
base_commit: 13a2e6c

must_haves:
  truths:
    - "T3: the m3-07c PLAN names the RED's ACTUAL export (scan_present_rate) and return keys (n_traits_present / n_traits_scanned / present_rate / traits_present) at EVERY site — zero occurrences of present_rate_for_variants / present_count / scanned_count / files_present remain in the SPEC BODY (the reconciliation note may quote them; that is its purpose)."
    - "T3: the PLAN states the scan's key TYPE explicitly — a (chr:int, pos:int) GRCh37 TUPLE (the RED does res[target] where target = (1, 5_982_778))."
    - "T4: the PLAN specs the RED's ACTUAL signature drop_occluded_from_sumstats(sumstats_path, manifest_path, out_path) -> dict — FILE-IN/FILE-OUT with a counts dict (n_in / n_dropped / n_out), NOT DataFrame-in/tuple-out; zero occurrences of sumstats_df / filtered_df / drop_log / build= remain."
    - "T4: the PLAN records that each drop is logged to STDERR (the RED asserts via capsys.readouterr().err), NOT returned as a drop_log; and that idempotence is re-running on the OUTPUT file (n_dropped == 0 + out2 bytes == out1 bytes)."
    - "The PLAN records the 63bdb59 seam it predates: enrich_occlusion_manifest(present_rate=...) consumes EXACTLY {(chr, pos_grch37): {...}}, joins POST-lift on (chr, pos_grch37) (NOT variant_id), and RAISES ValueError if liftable keys exist but none match — so scan_present_rate's return is DIRECTLY feedable."
    - "A plan_vs_red_reconciliation note covers BOTH T3 and T4, records that the RED is the contract, that this was checked POST-63bdb59, and that the handoff's '07c's plan has no drift' claim was STALE AND WRONG for BOTH tasks."
    - "NOTHING in src/ or tests/ changed: `git diff --stat 13a2e6c..HEAD -- src/ tests/` is EMPTY. The 15 RED failures stay RED (6 present_rate + 9 lockstep). m3-07c is NOT started."
  artifacts:
    - path: ".planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md"
      provides: "The m3-07c plan, reconciled to the 07a RED contract (T3 + T4) + the 63bdb59 seam"
      contains: "scan_present_rate"
  key_links:
    - from: ".planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md"
      to: "tests/m3/test_occlusion_present_rate_scan.py"
      via: "the plan's T3 spec now matches the RED's export + return keys (tests are the contract)"
      pattern: "scan_present_rate"
    - from: ".planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md"
      to: "tests/m3/test_occlusion_lockstep_drop.py"
      via: "the plan's T4 spec now matches the RED's file-in/file-out signature + counts dict"
      pattern: "sumstats_path, manifest_path, out_path"
    - from: ".planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md"
      to: "src/python/occlusion_manifest.py"
      via: "the documented present_rate= seam: {(chr, pos_grch37): {...}} joined post-lift"
      pattern: "enrich_occlusion_manifest"
---

<objective>
**DOCS-ONLY.** Reconcile the m3-07c PLAN to the 07a RED that governs it, BEFORE 07c executes.

The handoff claims "07c's plan was CHECKED for the drift that hit 07b's plan — it has none."
**That claim is STALE and WRONG — BOTH tasks drifted.** T3 specs an export and return shape that
appear NOWHERE in the RED. T4's function NAME matches (which is why a name-only check passed it),
but its SIGNATURE, RETURN, and LOGGING MECHANISM all contradict the RED. An executor handed this
plan faces an unsatisfiable spec whose likeliest escape is **editing the tests** — the exact failure
[[feedback_check_plan_against_red_before_executing]] exists to prevent.

**PRECEDENCE (settled by m3-07b — do NOT re-litigate): TESTS ARE THE CONTRACT.** 07b's SUMMARY:
"Manifest API follows the TESTS' names …; the plan's prose names differ and are provided as
aliases. Tests are the contract." Precedent `f3b79fe` reconciled 07b's plan to 07a's corrected RED
pre-execution and caught 3 contradictions. **This is the same move: FIX THE PLAN, NOT THE RED.**

Purpose: make the 07c plan EXECUTABLE-AS-WRITTEN for BOTH tasks, and record the reconciliation so
the next reader does not re-derive it and does not trust the stale claim again.
Output: one reconciled PLAN.md. **No code. m3-07c is NOT started** (it needs Carter's explicit GO).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md

# THE CONTRACT — read, DO NOT EDIT:
@tests/m3/test_occlusion_present_rate_scan.py
@tests/m3/test_occlusion_lockstep_drop.py

<interfaces>
<!-- Verified against the working tree @ m3-W2-aou-deltas, HEAD 13a2e6c, this session.
     Every "0 occurrences" below is grep-confirmed, not inferred. -->

=== THE VERIFIED T3 DRIFT (tests/m3/test_occlusion_present_rate_scan.py) ===

| concern         | PLAN says (WRONG)           | RED requires (AUTHORITATIVE) |
|-----------------|-----------------------------|------------------------------|
| function name   | `present_rate_for_variants` | `scan_present_rate`          |
| numerator key   | `present_count`             | `n_traits_present`           |
| denominator key | `scanned_count`             | `n_traits_scanned`           |
| trait-list key  | `files_present`             | `traits_present`             |

RED evidence:
```python
target = (1, 5_982_778)                              # :72  — (chr:int, pos:int) GRCh37 TUPLE
res = prs.scan_present_rate([target], [f1, f2, f3])  # :79
rec = res[target]                                    # :81  — keyed BY THE TUPLE
assert rec["n_traits_present"] == 2                  # :82
assert rec["n_traits_scanned"] == 3                  # :83
assert rec["present_rate"] == pytest.approx(2 / 3)   # :84
assert sorted(res[v]["traits_present"]) == ["bmi", "sbp"]   # :132
```
More at :96-101, :111-113, :155-157, :168-172.

PLAN SELF-CONTRADICTION: :96 says "populates the manifest's traits_present / n_traits_present
columns" (the RED's names — CORRECT) while :117 specs
`-> per-variant {present_count, scanned_count, present_rate, files_present}` (WRONG).

SIX `present_rate_for_variants` SITES — fix ALL:
  :20 must_haves/truths   :27 artifact `exports:`   :96 prose
  :117 Task-1 `<behavior>`  :129 Task-1 `<action>`  :142 Task-1 `<done>`
Wrong RETURN KEYS live at :117-118 only.

=== THE VERIFIED T4 DRIFT (tests/m3/test_occlusion_lockstep_drop.py) ===
The NAME matches — that is why a name-only check passed it. Nothing else does.

| concern   | PLAN says (WRONG)                          | RED requires (AUTHORITATIVE)                    |
|-----------|--------------------------------------------|-------------------------------------------------|
| params    | `(sumstats_df, manifest, build='GRCh37')`  | `(sumstats_path, manifest_path, out_path)`      |
| shape     | DataFrame-in / tuple-out                   | **FILE-IN / FILE-OUT** (writes `out_path`)      |
| return    | `(filtered_df, drop_log)`                  | a **dict**: `n_in` / `n_dropped` / `n_out`      |
| logging   | `drop_log` records each drop               | logged to **STDERR** (`capsys.readouterr().err`)|
| `build=`  | `build='GRCh37'` kwarg                     | **no such kwarg exists**                        |

Grep-confirmed 0 occurrences in the RED: `sumstats_df`, `filtered_df`, `drop_log`, `build=`.

FOUR T4 drift sites (NOT just :21/:156 — grep the whole file):
  :21 must_haves/truths   :156 Task-2 `<behavior>` (signature+return)
  :158 `drop_log records each dropped (chr,pos,variant_id,reason)`
  :160 `IDEMPOTENT: a second application is a no-op (drop_log empty)`

RED evidence (contract docstring :10; call sites :104, :121, :136, :196-197, :314):
```python
res = dof.drop_occluded_from_sumstats(ss, mf, out)   # :104 — 3 positional PATHS
assert res["n_in"] == 3                              # :243
assert res["n_dropped"] == 1                         # :244
assert res["n_out"] == 2                             # :245
assert res["n_in"] - res["n_dropped"] == res["n_out"]     # :246 — invariant
assert len(_body_lines(out)) == res["n_out"]              # :247 — n_out == rows WRITTEN
```
Other load-bearing RED facts (encode these; do NOT paraphrase them away):
  * MANIFEST IS A FILE — TSV, columns `region_id, variant_id, chr, pos_grch37` (`_write_manifest` :69-80);
    only `(chr, pos_grch37)` is load-bearing; id/region ride along as provenance.
  * LOGGING IS STDERR — `test_logs_each_drop(tmp_path, capsys)` :208; `err = capsys.readouterr().err` :223;
    asserts each dropped COORDINATE is named in stderr (`str(_SNP_C_B37) in err`, `"8000000" in err` :226-227).
  * IDEMPOTENCE IS RE-RUNNING ON THE OUTPUT — `res2 = dof.drop_occluded_from_sumstats(out1, mf, out2)` :197;
    `res2["n_dropped"] == 0` :200; `out2.read_bytes() == out1.read_bytes()` :201. The OUTPUT must therefore
    be re-readable AS INPUT (same header/format) — a stronger contract than "drop_log empty".
  * `n_dropped == 0` IS A VALID NO-OP, not an error (:127-139: present-rate k/n < 1 is the normal case).
  * CHR-AWARE — the same POS on a different chromosome SURVIVES (:112-124); a POS-only key would
    silently delete unrelated variants genome-wide.
  * BYTE-IDENTICAL SURVIVORS — header verbatim + surviving rows verbatim and in order (:160-162);
    SNP_ID passed through untouched, no re-key (:165-168).
  * THE SEAM TEST (:272-319) runs the REAL 07b producer end-to-end
    (`build_region_records` -> `add_grch37_positions` -> `to_csv` -> `drop_occluded_from_sumstats`)
    and asserts every lifted record carries the `chr` + `pos_grch37` drop key (:301-304). It FAILS today
    (chain file present, pyliftover installed — MEASURED, it does not skip).

=== THE 63bdb59 SEAM THE PLAN PREDATES (src/python/occlusion_manifest.py) ===
`enrich_occlusion_manifest(manifest_path, chain_path, *, out_path=None, present_rate=None)` (:320-323)
  * `present_rate` keyed EXACTLY `{(chr, pos_grch37): {...}}` — GRCh37, POST-LIFTOVER (:330-331);
    the docstring itself cites `test_occlusion_present_rate_scan.py:72` as the key's source of truth.
  * joins on `(chr, pos_grch37)` via `_present_rate_key` (:296-317, :372-374) — **NOT** `variant_id`.
  * RAISES `ValueError` when non-empty present_rate + >=1 liftable row + ZERO key matches (:385-388).
  * `STAGE_B_TRAIT_COLUMNS = ["traits_present", "n_traits_present", "n_traits_scanned"]` (:103) —
    the RED's key names ARE the manifest's column names.
  * 4 seam tests added by 63bdb59 in tests/m3/test_occlusion_manifest.py:
    test_present_rate_joins_on_the_chr_pos_grch37_tuple_key,
    test_present_rate_leaves_na_for_a_variant_that_did_not_lift,
    test_present_rate_matching_no_liftable_row_raises_not_silent_na,
    test_present_rate_against_a_wholly_unliftable_manifest_does_not_raise.
=> `scan_present_rate`'s return is **DIRECTLY FEEDABLE** to `present_rate=`. The plan's :95-96 ALREADY
   correctly assumed the manifest carries `(chr, pos_grch37)`; 63bdb59 aligned the shipped consumer to
   that assumption, so **the plan's premise is now TRUE in code**, not aspirational.

=== RED BASELINE (MEASURED this session — must be UNCHANGED by this docs-only task) ===
  tests/m3/test_occlusion_present_rate_scan.py = **6 tests, 6 failed** (missing module
    `occlusion_present_rate_scan`, T3)
  tests/m3/test_occlusion_lockstep_drop.py     = **9 tests, 9 failed** (missing module
    `drop_occluded_from_sumstats`, T4)
  Total 15. **State it BY FILE and BY MODULE — a bare "9 + 6" is ambiguous and inverts the two.**
  Full tests/m3 baseline @ 13a2e6c: **15 failed / 405 passed / 31 skipped**.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the m3-07c PLAN's T3 + T4 specs to the 07a RED + record the 63bdb59 seam (DOCS-ONLY)</name>
  <read_first>
    .planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md (the file to fix),
    tests/m3/test_occlusion_present_rate_scan.py lines 60-175 (T3 CONTRACT — read only, NEVER edit),
    tests/m3/test_occlusion_lockstep_drop.py lines 1-30 + 90-250 (T4 CONTRACT — read only, NEVER edit;
    read the ACTUAL assertions at :241-247 and :196-201, do not paraphrase them from this plan),
    src/python/occlusion_manifest.py lines 296-390 (the 63bdb59 present_rate= seam — read only).
  </read_first>
  <files>.planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md</files>
  <action>
    Edit ONLY the file above. **Touch NOTHING in src/ or tests/.**
    Precedence for EVERY item below: **TESTS ARE THE CONTRACT — fix the PLAN, not the RED.**

    --- T3 ---
    (1) RENAME the spec'd export at ALL SIX sites: `present_rate_for_variants` -> `scan_present_rate`
        (:20, :27, :96, :117, :129, :142). **GREP THE WHOLE FILE — do not fix only the named sites**
        (the T3 rename had 6 sites where 4 were predicted; T4 had 4 where 2 were predicted).

    (2) FIX THE T3 RETURN-SHAPE SPEC at :117-118 to the RED's ACTUAL keys — replace
        `{present_count, scanned_count, present_rate, files_present}` with a per-variant dict:
        `{"n_traits_present": int, "n_traits_scanned": int, "present_rate": float, "traits_present": list[str]}`.
        This also resolves the plan's SELF-CONTRADICTION (:96 already used the RED's names).

    (3) MAKE THE T3 KEY TYPE EXPLICIT AND LOAD-BEARING in Task-1 `<behavior>`: the return is keyed by a
        **(chr:int, pos:int) GRCh37 TUPLE** — `res[target]`, `target = (1, 5_982_778)`
        (test_occlusion_present_rate_scan.py:72,81). Not a string, not a variant_id.

    --- T4 (same precedence rule; the NAME is right, nothing else is) ---
    (4) FIX THE T4 SIGNATURE at :21 and :156 (and any other site grep finds) to the RED's ACTUAL:
        `drop_occluded_from_sumstats(sumstats_path, manifest_path, out_path) -> dict`
        — three positional args, ALL FILE PATHS; the function WRITES `out_path`.
        **CALL OUT EXPLICITLY: this is FILE-IN/FILE-OUT, NOT DataFrame-in/tuple-out. It changes what the
        module IS — an executor must NOT "adapt" the RED to a DataFrame API.** Delete the `build='GRCh37'`
        kwarg entirely: it does NOT exist in the RED (0 occurrences).

    (5) FIX THE T4 RETURN at :156 to the RED's ACTUAL dict and keys (per :243-247):
        `{"n_in": int, "n_dropped": int, "n_out": int}`, with the invariant `n_in - n_dropped == n_out`
        (:246) and `n_out` == the number of body rows actually WRITTEN to `out_path` (:247).
        Replace `(filtered_df, drop_log)` — neither name exists in the RED.

    (6) FIX THE T4 LOGGING SPEC at :158: the RED asserts each drop is logged to **STDERR**
        (`test_logs_each_drop(tmp_path, capsys)` :208; `capsys.readouterr().err` :223), naming each dropped
        COORDINATE (:226-227). It is NOT a returned `drop_log` structure. Keep "every drop is LOGGED
        (provenance, not a debug nicety)" — only the MECHANISM changes.

    (7) FIX THE T4 IDEMPOTENCE SPEC at :160: idempotence is **re-running the filter on its own OUTPUT**
        (`drop_occluded_from_sumstats(out1, mf, out2)` :197) yielding `n_dropped == 0` (:200) and
        `out2.read_bytes() == out1.read_bytes()` (:201) — so the OUTPUT must be re-readable AS INPUT.
        Replace "a second application is a no-op (drop_log empty)" — `drop_log` does not exist.

    (8) While in T4, record (do not invent — these are RED facts): the MANIFEST IS A FILE (TSV, columns
        `region_id, variant_id, chr, pos_grch37`; only `(chr, pos_grch37)` load-bearing); `n_dropped == 0`
        is a VALID no-op not an error (:127-139); the key is CHR-AWARE (:112-124); and the seam test
        (:272-319) runs the REAL 07b producer end-to-end and pins that lifted records carry `chr` +
        `pos_grch37`. Do NOT alter T4's SCIENCE (drop-only, no re-key, no orphan, rs182965575 7/9).

    --- SEAM + SCOPE + NOTE ---
    (9) ADD THE 63bdb59 SEAM CONTRACT to the `<interfaces>` block (the plan PREDATES it):
        `enrich_occlusion_manifest(present_rate=...)` consumes EXACTLY `{(chr, pos_grch37): {...}}`, joins
        on `(chr, pos_grch37)` **POST-lift** (NOT `variant_id`), and **RAISES ValueError** if liftable keys
        exist but not one matches. Therefore `scan_present_rate`'s return is **directly feedable** to
        `present_rate=` — no adapter. Cite `occlusion_manifest.py` + the 4 seam tests 63bdb59 added to
        `tests/m3/test_occlusion_manifest.py` (named in `<interfaces>` above). **State explicitly** that
        the plan's :95-96 ALREADY correctly assumed `(chr, pos_grch37)`, and that 63bdb59 aligned the
        shipped consumer to it — the plan's premise is now TRUE in code.

    (10) DO NOT CHANGE THE SCIENCE OR SCOPE. Preserve in intent: T3 = present-rate scan over the 9 public
        GRCh37 AFR harmonized sumstats (header `CHR POS REF ALT BETA SE P EAF N SNP_ID TRAIT ANCESTRY BUILD`,
        CHR/POS auto-detected BY NAME); T4 = the reusable lockstep (CHR,POS) drop-only filter; the m3-04
        consume-wiring stays a DISCLOSED deferral (`finemap.smk` m3-04-W4 STALE/SUPERSEDED-PENDING-REPLAN).
        REQ-PUBLIC-DATA-ONLY, no perimeter. Do not renumber tasks; do not touch the threat_model.

    (11) ADD a `plan_vs_red_reconciliation` note (mirror `f3b79fe`'s shape), **covering BOTH T3 and T4**.

        **PLACEMENT IS LOAD-BEARING — the gates depend on it.** The note MUST be ONE block at the very
        END of the plan (after `<output>`), introduced by a line containing the literal token
        `plan_vs_red_reconciliation`, and that token MUST appear **EXACTLY ONCE** in the whole file.
        WHY: the note's JOB is to quote the OLD drifted identifiers (`present_rate_for_variants`,
        `drop_log`, `sumstats_df`, ...) — but the SPEC BODY must contain ZERO of them. The verify cuts
        everything from that token to EOF and asserts the drifted names are absent from what REMAINS.
        So the old names are ALLOWED (and expected) INSIDE the note, and BANNED everywhere above it.
        If you place the note mid-file, or repeat the token, the cut is wrong and the gate is meaningless.

        It MUST record:
          * WHAT drifted in T3 (the 4-row table) and WHAT drifted in T4 (the 5-row table: params, shape,
            return, logging, build=) — including that T4's NAME matched, which is why a name-only check
            passed it;
          * that the RED is the CONTRACT and is UNEDITED (0-line diff), per settled 07b precedent;
          * that the plan was checked against the RED **POST-63bdb59**;
          * that the handoff's "07c's plan was CHECKED … it has none" claim was **STALE AND WRONG — BOTH
            tasks drifted** — so the next reader does not trust it again.
        Do NOT add a "T4 OPEN" flag: T4 is reconciled in this pass, not deferred.

    Commit DOCS-ONLY with EXPLICIT PATHS (never `git add -A` / `.` on this GPFS tree).
    **DO NOT STAGE** the pre-existing dirty files: `.claude/settings.json`,
    `.planning/quick/260625-r6m-*/260625-r6m-SUMMARY.md`, `tests/m3/sparse_parent_benchmark.tsv`.
    Suggested message: `docs(m3-07c): reconcile the plan's T3 + T4 specs to the 07a RED + record the 63bdb59 seam`.
  </action>
  <acceptance_criteria>
    - The `plan_vs_red_reconciliation` token appears EXACTLY ONCE (it is the note's cut marker).
    - T3 drifted names GONE **from the SPEC BODY** (= the file with the reconciliation note cut off):
      `present_rate_for_variants` / `present_count` / `scanned_count` / `files_present` == 0 occurrences.
      (They MAY appear inside the note — quoting them there is the note's purpose.)
    - T4 drifted names GONE **from the SPEC BODY** (same cut): `sumstats_df` / `filtered_df` /
      `drop_log` / `build=` == 0 occurrences. (Again: allowed inside the note only.)
    - `grep -c "scan_present_rate" <plan>` >= 6 (all six T3 sites renamed).
    - T3 keys present: `n_traits_present`, `n_traits_scanned`, `traits_present`, `present_rate` each >= 1.
    - T3 tuple key stated: `grep -c "5_982_778\|5982778" <plan>` >= 1.
    - T4 signature present: `grep -c "sumstats_path, manifest_path, out_path" <plan>` >= 1.
    - T4 counts dict present: `n_in`, `n_dropped`, `n_out` each >= 1.
    - T4 file-in/file-out called out: `grep -ci "file-in" <plan>` >= 1.
    - T4 stderr logging recorded: `grep -ci "stderr" <plan>` >= 1.
    - Seam recorded: `grep -c "enrich_occlusion_manifest" <plan>` >= 1 AND `grep -c "63bdb59" <plan>` >= 1.
    - Note exists + covers both: `grep -c "plan_vs_red_reconciliation" <plan>` >= 1
      AND `grep -ci "STALE AND WRONG" <plan>` >= 1 AND `grep -c "T4 OPEN" <plan>` == 0 (no stale flag).
    - **CODE GATE (hard):** `git diff --stat 13a2e6c..HEAD -- src/ tests/` is **EMPTY**.
    - **RED GATE (hard):** 15 failures stand — `test_occlusion_present_rate_scan.py` = **6 failed**,
      `test_occlusion_lockstep_drop.py` = **9 failed**. Below 15 => 07c was BUILT => **STOP, report.**
    - **REGRESSION (hard):** full `tests/m3` UNCHANGED at **15 failed / 405 passed / 31 skipped**
      (baseline HEAD 13a2e6c). Docs-only cannot move this; any delta = STOP.
  </acceptance_criteria>
  <verify>
    <automated>
    set -u
    PLAN=.planning/phases/m3-aou-afr-ld-panel-build/m3-07c-W7-present-rate-and-lockstep-PLAN.md
    PYTEST=/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/pytest

    # 1. The reconciliation note legitimately QUOTES the old drifted names, so the ban is scoped to the
    #    SPEC BODY: cut from the note's marker to EOF, then assert the drifted names are gone from the rest.
    #    The marker must occur exactly once, or the cut point is ambiguous.
    test "$(grep -c 'plan_vs_red_reconciliation' "$PLAN" || true)" -eq 1
    SPEC="$(sed '/plan_vs_red_reconciliation/,$d' "$PLAN")"

    test "$(printf '%s' "$SPEC" | grep -c 'present_rate_for_variants\|present_count\|scanned_count\|files_present' || true)" -eq 0
    test "$(printf '%s' "$SPEC" | grep -c 'sumstats_df\|filtered_df\|drop_log\|build=' || true)" -eq 0
    test "$(grep -c 'T4 OPEN' "$PLAN" || true)" -eq 0          # stale flag must NOT survive, anywhere

    # 2. T3 contract present
    test "$(grep -c 'scan_present_rate' "$PLAN")" -ge 6
    grep -q 'n_traits_present' "$PLAN" && grep -q 'n_traits_scanned' "$PLAN" && grep -q 'traits_present' "$PLAN"
    grep -q '5_982_778\|5982778' "$PLAN"

    # 3. T4 contract present
    grep -q 'sumstats_path, manifest_path, out_path' "$PLAN"
    grep -q 'n_in' "$PLAN" && grep -q 'n_dropped' "$PLAN" && grep -q 'n_out' "$PLAN"
    grep -qi 'file-in' "$PLAN"      # file-in/file-out called out explicitly
    grep -qi 'stderr' "$PLAN"       # logging mechanism recorded

    # 4. seam + reconciliation note
    grep -q 'enrich_occlusion_manifest' "$PLAN" && grep -q '63bdb59' "$PLAN"
    grep -qi 'STALE AND WRONG' "$PLAN"    # note marker count already asserted == 1 above

    # 5. HARD CODE GATE — src/ and tests/ untouched (MUST be empty)
    test -z "$(git diff --stat 13a2e6c..HEAD -- src/ tests/)"

    # 6. RED stays RED — BY FILE (6 present_rate, 9 lockstep). MEASURED, not assumed.
    $PYTEST tests/m3/test_occlusion_present_rate_scan.py -q 2>&1 | tail -1 | grep -q '6 failed'
    $PYTEST tests/m3/test_occlusion_lockstep_drop.py     -q 2>&1 | tail -1 | grep -q '9 failed'

    # 7. FULL REGRESSION — ~6.5 min, RUN IN BACKGROUND (run_in_background:true), then assert:
    #    $PYTEST tests/m3 -q  ->  15 failed / 405 passed / 31 skipped  (UNCHANGED)
    echo "VERIFY OK (foreground gates); full tests/m3 regression must be confirmed from the backgrounded run"
    </automated>
  </verify>
  <done>
    T3: the plan names `scan_present_rate` at all six sites with the RED's actual return keys
    (`n_traits_present` / `n_traits_scanned` / `present_rate` / `traits_present`) keyed by the
    `(chr:int, pos:int)` GRCh37 tuple. T4: the plan specs
    `drop_occluded_from_sumstats(sumstats_path, manifest_path, out_path) -> dict` — file-in/file-out
    (explicitly called out), counts dict `n_in`/`n_dropped`/`n_out` with the `n_in - n_dropped == n_out`
    invariant, stderr logging, output-as-input idempotence, and no `build=` kwarg. The 63bdb59
    `present_rate=` seam is recorded as directly feedable. Science/scope unchanged. A
    `plan_vs_red_reconciliation` note covers BOTH tasks and records the handoff claim as STALE AND WRONG.
    `git diff --stat 13a2e6c..HEAD -- src/ tests/` is EMPTY; RED stands at 6+9=15; full tests/m3 unchanged
    at 15/405/31; **m3-07c is NOT started**.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan prose -> executor | The plan is the PROMPT. A drifted spec is an unsatisfiable instruction whose likeliest escape is the executor EDITING THE TESTS — i.e. the plan can corrupt the contract by proxy. This task closes that for BOTH tasks. |
| docs edit -> code/tests tree | A "docs-only" task that touches `src/` or `tests/` would silently start 07c and/or edit the RED. Gated by `git diff --stat 13a2e6c..HEAD -- src/ tests/` == EMPTY. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0lj-01 | Tampering | tests/m3/test_occlusion_{present_rate_scan,lockstep_drop}.py (the RED contract) | mitigate | Task is docs-only; hard gate `git diff --stat 13a2e6c..HEAD -- src/ tests/` EMPTY + RED-stays-RED (6+9). Tests-are-the-contract precedent (07b) is restated IN the plan, and T4's file-in/file-out shape is called out explicitly, so an executor cannot "resolve" the mismatch by adapting the RED to a DataFrame API. |
| T-0lj-02 | Elevation of privilege | m3-07c scope | mitigate | 07c needs Carter's EXPLICIT GO (not given). No `occlusion_present_rate_scan.py` / `drop_occluded_from_sumstats.py` created; RED count dropping below 15 = STOP signal. |
| T-0lj-03 | Repudiation / stale claim | the reconciliation record | mitigate | The stale "checked, has none" claim caused this rework — and a NAME-ONLY check is exactly how T4 slipped through. The note records both drifts and names the stale claim as wrong, rather than asserting a fresh unqualified "clean". |
| T-0lj-04 | Tampering | GPFS shared tree staging | mitigate | Explicit-path commit only; the 3 known pre-existing dirty files are named as DO-NOT-STAGE ([[feedback_multi_terminal_staging]]). |
</threat_model>

<verification>
- T3 drifted names gone (count 0); `scan_present_rate` >= 6 sites; all four RED keys; tuple key stated.
- T4 drifted names gone (`sumstats_df` / `filtered_df` / `drop_log` / `build=` == 0); RED signature
  `sumstats_path, manifest_path, out_path` present; counts dict `n_in`/`n_dropped`/`n_out` present;
  file-in/file-out called out; stderr logging recorded.
- 63bdb59 seam + `enrich_occlusion_manifest` recorded; `plan_vs_red_reconciliation` note covers BOTH
  tasks, says STALE AND WRONG, and carries NO `T4 OPEN` flag.
- **HARD:** `git diff --stat 13a2e6c..HEAD -- src/ tests/` EMPTY.
- **HARD:** RED stays RED — 6 (present_rate, missing module `occlusion_present_rate_scan`) +
  9 (lockstep, missing module `drop_occluded_from_sumstats`) = 15.
- **HARD:** full `tests/m3` UNCHANGED at 15 failed / 405 passed / 31 skipped. ~6.5 min -> **BACKGROUND IT**
  (`run_in_background: true`) and confirm before closing.
- NO perimeter access, NO spend, NO loop contact. m3-07c NOT started.
</verification>

<success_criteria>
- The m3-07c PLAN is EXECUTABLE-AS-WRITTEN for BOTH T3 and T4: every name it specs exists in the RED,
  and every name/shape the RED requires is what the plan specs. The :96-vs-:117 self-contradiction is gone.
- T4's file-in/file-out identity is unmistakable, so no executor "adapts" the RED to a DataFrame API.
- The 63bdb59 seam is documented, so 07c's executor feeds `scan_present_rate` -> `present_rate=` with no
  adapter and no rediscovery of the ValueError raise-boundary.
- The reconciliation is recorded for BOTH tasks and the stale handoff claim is explicitly retired — the
  next reader neither re-derives the drift nor trusts "07c has no drift" again.
- Code and tests are byte-untouched; the RED stands at 15 (6+9); **07c remains un-started pending Carter's GO**.
</success_criteria>

<output>
After completion, create `.planning/quick/260716-0lj-reconcile-the-m3-07c-plan-to-the-07a-red/260716-0lj-SUMMARY.md`.
Record: the T3 drift table (4 rows) and the T4 drift table (5 rows) as FIXED, with the site counts
(T3 = 6 sites, T4 = 4 sites — both wider than predicted); the 63bdb59 seam paragraph added; the
`git diff --stat 13a2e6c..HEAD -- src/ tests/` EMPTY confirmation; the RED-still-15 confirmation stated
BY FILE and BY MODULE (6 present_rate / `occlusion_present_rate_scan`; 9 lockstep /
`drop_occluded_from_sumstats`); the full tests/m3 15/405/31 unchanged confirmation; and the standing note
that the handoff's "07c's plan has no drift" claim was STALE AND WRONG for BOTH tasks — a NAME-ONLY check
is how T4 slipped through.
</output>
