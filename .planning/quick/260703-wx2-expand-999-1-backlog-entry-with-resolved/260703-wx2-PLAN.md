---
quick_id: 260703-wx2
slug: expand-999-1-backlog-entry-with-resolved
description: "Expand 999.1 backlog entry with resolved OSF finding + 6-step work breakdown (docs-only, ROADMAP.md)"
date: 2026-07-04
mode: quick
must_haves:
  truths:
    - "The 999.1 backlog entry gains Seth's resolved OSF-coverage finding + 6-step work breakdown, APPENDED after the existing stub (stub text intact)."
    - "Status stays 'parked' — this is design capture, NOT promotion (no milestone scheduled, no phase fired)."
    - "The appended block is a #### sub-heading UNDER the existing ### 999.1 entry — ROADMAP.md still parses (no heading-level break)."
  artifacts:
    - ".planning/ROADMAP.md — 999.1 design-detail subsection appended"
  key_links:
    - ".planning/ROADMAP.md:1000 (999.1 stub final Refs line — the append anchor)"
    - "src/R/regularization/refit_sh2b3_psd_regularized.R:71,80 (psd_regularize_ridge/_eigclip — reuse target)"
    - ".planning/amendments/osf-amendment-r3-2026-05-04.md (EUR-only — the resolved OSF finding)"
---

# Quick Task 260703-wx2: Expand 999.1 backlog with resolved OSF finding

Docs-only. APPEND Seth's `#### 999.1 design detail (Seth, 2026-07-04)` subsection after
the 999.1 stub's final `**Refs:**` line (ROADMAP.md:1000). Keep stub intact. Status
stays **parked** — design capture, not promotion.

Provenance verified at HEAD 7750448 (read-only, this session):
- FIND anchor unique at ROADMAP.md:1000.
- `osf-amendment-r3-2026-05-04.md` exists + is **EUR-only** (1000G Phase 3 EUR panel,
  `ld_reference/EUR/SH2B3_12q24.rds`, 5 EUR traits, defers AFR to "Track B … All of Us
  EUR") → does NOT cover the AFR native panel or a NaN→0 step → a **new OSF amendment**
  is the true blocker on promotion.
- `refit_sh2b3_psd_regularized.R` has `psd_regularize_ridge` (L71) + `psd_regularize_eigclip`
  (L80), applied at fit time on `R_sub` (L170/172) → reuse via a shared `psd_utils.R`.

## Task 1 — append the 999.1 design detail

- **files:** `.planning/ROADMAP.md`
- **action:** Replace the stub's final two-line `**Refs:**` block with itself + `---` +
  the `#### 999.1 design detail` subsection (Seth verbatim: reuse-not-reinvent, NaN
  topology, pipeline placement + ordering, OSF gate RESOLVED, fine-mapping caveats,
  6-step work breakdown, do-NOTs, design refs).
- **verify:** `grep -c "^### 999.1" ROADMAP.md` == 1 (still one entry, no heading break);
  `grep -c "999.1 design detail" ROADMAP.md` == 1; "parked" unchanged; no `## Milestone`
  / phase scheduling added.
- **done:** stub intact + design detail appended; ROADMAP.md parses; STATE.md touched in
  the same atomic commit; explicit paths only.
