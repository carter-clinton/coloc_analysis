---
phase: quick-260821-w68
plan: 01
subsystem: pre-registration
tags: [osf, amendment, occlusion, posting, record, deviation-entry, dec, tag, handoff, m3-07]

requires:
  - "Carter's four OSF captures (2026-08-21 22:51–23:07 EDT) — the sole OSF-side source of truth"
  - "HEAD c61d179 (engine Class-P date move), origin == local, tracked tree clean"
  - ".planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md, paste block 22,945 B / 13a49f543cabcc27ce9f1e589783c060"
provides:
  - "THE RECORD: filled deviation entry for mk7ze in .planning/osf_deviations.md (pure append, lines 421-530)"
  - "DEC-2026-08-22-occlusion-recalibration-posted — the gate-clear decision + the TWO-condition authorisation"
  - "Plain git tag AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22 on the single-path record commit, local + origin"
  - "m3-07 clause-(d) OSF gate CLEARED in HANDOFF.json (Check-2 obligation left OPEN)"
  - "Three refreshed resume surfaces (HANDOFF resume[0], STATE.md top block, .continue-here.md)"
affects:
  - "The remediation batch (next, separate task): the code-constant change is AUTHORISED and NOT YET MADE"

tech-stack:
  added: []
  patterns:
    - "script-extraction of a prepared template (never retyped) — M1/r3/tcujq/trsx5 pattern"
    - "pure-append proven three ways (prefix md5 + numstat deletions + line-count delta)"
    - "frozen-public-body invariant proven by a same-command 2-cell negative control"

key-files:
  created:
    - .planning/quick/260821-w68-record-the-osf-posting-of-the-occlusion-/260821-w68-SUMMARY.md
  modified:
    - .planning/osf_deviations.md
    - .planning/DECISIONS.md
    - .planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
    - .planning/HANDOFF.json
    - .planning/STATE.md
    - .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md

decisions:
  - "DEC-2026-08-22-occlusion-recalibration-posted: the m3-07 clause-(d) OSF gate is CLEARED; the code-constant change is AUTHORISED and ONLY as the TWO-condition gate (site fraction > 0.5056% OR occluded-site row/site inflation > 3.42x); ceiling-alone NOT authorised; accounting stays ROW-keyed; both numbers emitted; Stage A stays HELD."
  - "The three things that could have misled a future reader are disclosed in the record itself, not smoothed over: the 4c4 **Date:**-line divergence from the reviewed body, the API-reported (not re-downloaded) integrity hash, and the template's failed 'Date created' prediction."

metrics:
  duration: "~25 min"
  completed: 2026-08-22
  tasks: 3
  commits: 3
  files_modified: 6
---

# Quick 260821-w68: Record the OSF posting of the occlusion-gate recalibration amendment — Summary

The recalibration amendment is on the OSF record as `mk7ze` (`2026-08-22T02:58:55Z`) and the repo
now says so, in three atomic docs-only commits: the prepared deviation entry was **script-extracted**
from the amendment's fenced `## PREPARED` block, filled, and **pure-appended**;
`DEC-2026-08-22-occlusion-recalibration-posted` was banked with the TWO-condition authorisation
stated in both constants; the amendment's own post-paste checklist was marked items 1-5 DONE / item 6
AUTHORISED (all strictly below the `--- PASTE ENDS HERE ---` marker); the HANDOFF gate moved to
CLEARED while leaving the separate Check-2 obligation OPEN; the record commit was tagged; and branch
plus tag were pushed. The public paste block never moved — proven, and proven to be a real check.

---

## 1. Commits, tag, and origin

| # | Task | Commit (40-hex) | Short | Paths |
|---|------|-----------------|-------|-------|
| 1 | Fill + append the deviation entry — THE RECORD COMMIT | `a2f4fa9289da9a0d47cf66f87d8bbfeed47c4364` | `a2f4fa9` | `.planning/osf_deviations.md` (ONE path) |
| 2 | DEC + amendment post-paste status | `3b9d824e96bd73b76be1de256129925ddcd60210` | `3b9d824` | `.planning/DECISIONS.md`, `.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md` |
| 3 | HANDOFF gate CLEARED + three resume surfaces | `84b583d9be4b14f1b435dfdbb7998929c2235412` | `84b583d` | `.planning/HANDOFF.json`, `.planning/STATE.md`, `.planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md` |

Every commit staged with explicit file paths only. 21 untracked `results/` backup dirs remained
untracked throughout; `git status --porcelain | grep -v '^??' | wc -l` == `0` at close.

**The tag** — plain (non-annotated), like its July sibling:

```
$ git cat-file -t AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22
commit
$ git rev-parse AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22
a2f4fa9289da9a0d47cf66f87d8bbfeed47c4364
$ git show --name-only --format= AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22
.planning/osf_deviations.md
```

**Push + origin confirmation** (`git push` does NOT push tags — the second push was required):

```
$ git push
To github.com:carter-clinton/coloc_analysis.git
   c61d179..84b583d  m3-W2-aou-deltas -> m3-W2-aou-deltas

$ git push origin AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22
To github.com:carter-clinton/coloc_analysis.git
 * [new tag]         AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22 -> AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22

$ git status -sb | head -1
## m3-W2-aou-deltas...origin/m3-W2-aou-deltas          <- no 'ahead'

$ git ls-remote --tags origin | grep AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22
a2f4fa9289da9a0d47cf66f87d8bbfeed47c4364	refs/tags/AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22
```

`ls-remote` count == **1**, and it points at the record commit.

---

## 2. The appended deviation entry — key lines

Appended chunk occupies **lines 421-530** of `.planning/osf_deviations.md` (line 421 is the
separating blank line; the entry heading is line 422; `### Recorded at posting (2026-08-22)` is
line 488).

**Heading (line 422):**

```
## 2026-08-22 — AFR native-panel occlusion anomaly-gate RECALIBRATION + factual correction to trsx5 (m3-07 OSF gate, second record)
```

**Posted bullet (lines 424-428):**

```
- **Posted:** OSF file `mk7ze` on parent record az52u —
  `https://osf.io/mk7ze` — filename
  `osf-amendment-occlusion-gate-recalibration-2026-08-22.md` (append-only;
  M1/r3/tcujq/trsx5 pattern). Posted as a NEW supplementary file, NOT as a new version of
  trsx5.
```

**Timestamp bullet (lines 433-442) — the refuted template sentence REPLACED, and the failed
prediction itself recorded:**

```
- **OSF timestamp (authoritative, UTC):** `2026-08-22T02:58:55Z` — the Recent Activity log
  entry "Carter Clinton added file
  osf-amendment-occlusion-gate-recalibration-2026-08-22.md to OSF Storage" (the widget
  renders that instant as "Aug 21, 2026 10:58 PM" ET). ⚠ **The prepared template predicted
  that this file page's "Date created" would show the PARENT RECORD's creation date
  (2026-04-10, osf.io/pvb5j) — as `trsx5`'s page does. That expectation was NOT borne out
  here.** The observed "Date created" read `2026-08-22T02:58:53Z`: this object's own
  creation, 2 s before the activity-log entry. The Recent Activity entry remains the
  authoritative stamp of record; the 2 s gap is upload-then-index, not a discrepancy, and
  it is written down here so a future reader is not surprised by it.
```

**Pre-execute gate commit (line 444):**

```
- **Pre-execute gate commit:** `07df11e44f2d56536ef4ef0753c8d2f8fdb55ae8`. At OSF post time NO change to
```

**The five posting-time bullets under `### Recorded at posting (2026-08-22)` (line 488):**

| Line | Bullet | Substance carried |
|------|--------|-------------------|
| 490 | **Posting-date disclosure.** | provisional 2026-08-21 → 2026-08-22 via the engine's Class-P pass (`c61d179`), never by hand; reviewed body 22,945 B / `422f1f28d6a3b76c7657fadec05a0237` vs posted 22,945 B / `13a49f543cabcc27ce9f1e589783c060`; the COMPLETE difference is the `**Date:**` line, `diff` reports exactly `4c4`; no scientific statement, number or commitment differs |
| 497 | **Integrity.** | repo paste block == OSF's own stored md5 for `mk7ze` v1 (22,945 B). ⚠ Method caveat written in substance: the scripted byte-for-byte re-download was **REFUSED** by OSF's file server (private project; cross-origin authenticated fetch rejected), so the hash was **read from the file's API record**, not recomputed from a downloaded byte stream; a local md5 of a manually re-downloaded copy was **NOT** performed — OPTIONAL, NON-BLOCKING follow-up; "not a repo echo, and not a re-download" |
| 507 | **Append-only.** | `trsx5` still exactly **1 revision** ("1. Jul 10, 2026 09:32 AM" ET; API: one version created `2026-07-10T13:32:21Z`); `mk7ze` is its own single-version file on the OSF Storage **ROOT** (Az52u / Files / Mk7ze), a **SIBLING** not a child; stored filename exact-lowercase; two distinct GUIDs → no silent swap |
| 515 | **Precedence — SATISFIED, no deviation.** | `c61d179` at `2026-08-22T02:48:03Z`, ten minutes before the `2026-08-22T02:58:55Z` OSF stamp; `run_native_ld_panel.py:133` still `_OCCLUSION_ANOMALY_FRACTION = 0.0005` (last moved `d9fbc63`, 2026-08-13); `git diff --stat 2689cae HEAD -- src/ tests/ config/` EMPTY; no recalibrated-gate output anywhere |
| 521 | **Record.** | `DEC-2026-08-22-occlusion-recalibration-posted`; tag `AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22` (July sibling named); the seven `.planning/debug/` supporting records enumerated; the posting card cited |

**Marker completeness:**

```
$ LAST=$(awk '/^## /{n=NR} END{print n}' .planning/osf_deviations.md)   # 422
$ tail -n +422 .planning/osf_deviations.md | grep -c 'TO BE FILLED AT POSTING'
0
mk7ze                                    -> 4
https://osf.io/mk7ze                     -> 1
2026-08-22T02:58:55Z                     -> 2
13a49f543cabcc27ce9f1e589783c060         -> 2
07df11e44f2d56536ef4ef0753c8d2f8fdb55ae8 -> 1
2026-07-10T13:32:21Z                     -> 1
```

---

## 3. Append proof — three independent ways

```
pre-task  : 420 lines / 31,301 B / md5 dd3806312977513a8727463ec3a032df
chunk     : 110 lines  ($SCR/w68-append.md, never staged)
post-task : 530 lines                       <- 420 + 110 exactly

$ head -420 .planning/osf_deviations.md | md5sum
dd3806312977513a8727463ec3a032df  -         <- byte-identical prefix

$ git diff --numstat -- .planning/osf_deviations.md     (pre-commit)
110	0	.planning/osf_deviations.md              <- deletions column = 0

$ git diff --numstat HEAD~1 HEAD -- .planning/osf_deviations.md | cut -f2   (at a2f4fa9)
0

$ git show --name-only --format= a2f4fa9 | tr -d '[:space:]'
.planning/osf_deviations.md                 <- record commit touches ONE path
```

---

## 4. Template fidelity — extracted, not retyped

Extraction anchored on the `## PREPARED …` heading **prefix** (survives Task 2's heading edit),
then the first fenced block after it:

```
heading line (1-based): 526
open fence: 535 close fence: 594
template lines: 58                     <- as the plan predicted
$ grep -c 'TO BE FILLED AT POSTING' $SCR/w68-template.md
6
```

Verbatim-carriage check over every template line that carried **no** marker:

```
template lines carried verbatim: 50/52
MISSING: '  Activity entry). The file page\'s "Date created" is the PARENT RECORD\'s creation date'
MISSING: "  (2026-04-10, osf.io/pvb5j), NOT this file's upload date."
```

**Both misses are the two deliberate ones** — exactly the two lines of the refuted "Date created"
claim, replaced by the observed facts plus an explicit statement that the template's expectation was
not borne out. Nothing else is missing, so the extraction was faithful.

---

## 5. Frozen-block assertion at EACH commit

The one command, `awk '/^--- PASTE INTO OSF FROM HERE ---$/{f=1;next} /^--- PASTE ENDS HERE ---$/{f=0} f' "$A"`:

| Point | Bytes | md5 |
|-------|-------|-----|
| Task 1 pre-flight (at `c61d179`) | `22945` | `13a49f543cabcc27ce9f1e589783c060` |
| Task 1 post-append, pre-commit | — | `13a49f543cabcc27ce9f1e589783c060` |
| Task 1 verify (at `a2f4fa9`) | — | `13a49f543cabcc27ce9f1e589783c060` (`PASTE BLOCK FROZEN: OK`) |
| Task 2 pre-flight | `22945` | `13a49f543cabcc27ce9f1e589783c060` |
| Task 2 post-edit, pre-commit | `22945` | `13a49f543cabcc27ce9f1e589783c060` |
| Task 2 verify (at `3b9d824`) | `22945` | `13a49f543cabcc27ce9f1e589783c060` (`PASTE BLOCK SIZE: OK` + `PASTE BLOCK FROZEN: OK`) |
| Task 3 pre-flight | — | `13a49f543cabcc27ce9f1e589783c060` |
| Task 3 pre-commit | — | `13a49f543cabcc27ce9f1e589783c060` |
| Task 3 verify (at `84b583d`) | — | `13a49f543cabcc27ce9f1e589783c060` (`PASTE BLOCK FROZEN: OK`) |
| Plan-wide verification (final) | `22945` | `13a49f543cabcc27ce9f1e589783c060` |

The three per-commit lines the output spec asks for are the Task 1 / Task 2 / Task 3 verify rows:
all `22945 / 13a49f54…`.

**The three edits are provably outside the markers.** `--- PASTE ENDS HERE ---` is at line **501**;
the amendment's entire diff for this task is two hunks:

```
$ git diff -U0 -- <amendment> | grep -E '^@@'
@@ -506,0 +507,2 @@       <- D3, the checklist STATUS line (+ blank)
@@ -526 +528,3 @@         <- D1 heading flip + D2 provenance line (+ blank)
$ git diff --numstat -- <amendment>
5	1	.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
```

Both hunks are strictly below 501. The single deletion is the in-place heading rewrite.

---

## 6. Negative control — the invariant SEEN RED in this task

Same command, same fixture shape, in the scratchpad, never in the tree
(`$SCR/w68-nc/`, `git status --porcelain | grep -c w68-nc` == `0`):

```
$ PBF() { awk '/^--- PASTE INTO OSF FROM HERE ---$/{f=1;next} /^--- PASTE ENDS HERE ---$/{f=0} f' "$1" | md5sum; }

perturbed line 169                              <- one byte flipped strictly inside the block
CELL 1 unperturbed: 13a49f543cabcc27ce9f1e589783c060  -
CELL 2 perturbed:   c6ded5b3cadfa69017eecd167772a630  -
cmp -l $SCR/w68-nc/unperturbed.md $SCR/w68-nc/perturbed.md | wc -l
1
in-tree amendment (untouched): 13a49f543cabcc27ce9f1e589783c060  -
```

A **one-byte** difference (`cmp -l` == 1) moves the md5 off `13a49f54…` on the *same* command that
returns `13a49f54…` on the unperturbed copy. The green is therefore evidence, not decoration.
No control fixture was ever staged.

---

## 7. Amendment: new whole-file anchor + guard

```
BEFORE : 43,042 B / c7f9dee26d3070ea83d6899dfc2c90d3 / 594 lines
AFTER  : 43,931 B / 9174c274ed50825c1f28380aab96e9e7 / 598 lines
```

```
$ bash .planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh all <amendment>
… (draft / paste-ready / arith / quote — every check PASS)
GUARD all: GREEN
GUARD EXIT=0

$ grep -c '{{' <amendment>                    -> 0
$ grep -cE '^  [A-Z0-9_]+ = ' <amendment>     -> 21     (never 22)
$ grep -c 'NOT-YET-APPENDED' <amendment>      -> 0
$ grep -c 'APPENDED 2026-08-22' <amendment>   -> 1
$ grep -c 'items 1-5 DONE' <amendment>        -> 1
$ grep -c '0.5056' <amendment>                -> 9
```

Both inserted lines were asserted, before writing, to not match `^  [A-Z0-9_]+ = ` and to contain
neither `(site basis)` nor `(row basis)` nor `{{`. The guard is a consumer dependency and was not
edited. The instantiation engine was **never invoked**.

Note on the guard's own output: it reports `paste block 23001 B`, which is its
**marker-inclusive** measurement (22,945 + the two marker lines = 23,001). That is the guard's
convention and it is unchanged by this task; the plan's hard invariant is the marker-**exclusive**
`awk` extraction, which is `22945 / 13a49f54…` throughout.

---

## 8. No code moved

```
$ sed -n '133p' src/python/run_native_ld_panel.py
_OCCLUSION_ANOMALY_FRACTION = 0.0005

$ git diff --stat c61d179 HEAD -- src/ tests/ config/ | wc -l
0                       <- asserted at all three commits and again at close

$ git diff --stat 2689cae HEAD -- src/ tests/ config/ | wc -l
0

$ git diff --stat c61d179 HEAD -- .planning/amendments/osf-amendment-afr-occlusion-exclude-UPDATE-2026-07-10.md | wc -l
0                       <- the POSTED July amendment is byte-unchanged
```

---

## 9. Resume surfaces

**`.planning/HANDOFF.json`** — valid JSON, minimal diff (`5` additions / `4` deletions; hunks at
lines 3, 9, 20, 55, 149 = exactly `timestamp`, `status`, the new `resume_on_reconnect[0]`,
`gates.osf_pre_registration`, `resume_entry_point`). Round-trip serialization style confirmed
byte-exact before editing (`json.dumps(d, indent=2, ensure_ascii=False) + '\n'` reproduced the
original file), so nothing else in 110 KB of one-line strings moved.

```
$ python3 -m json.tool .planning/HANDOFF.json > /dev/null; echo $?
0
timestamp                       : 2026-08-22T03:25:00Z
resume_on_reconnect             : 19  (was 18; the existing 18 survive, shifted, none deleted)
gates.osf_pre_registration      : appended, ends "… THIS OBLIGATION IS CLEARED. The separate
                                  Check-2 amendment-update obligation (2026-08-03 override)
                                  REMAINS OPEN and is unaffected."
resume_on_reconnect[0]          : "> #0 (2026-08-22 ~03:25 UTC — POSTED AND RECORDED, SUPERSEDES
                                  every earlier #0). …" — carries the four captures, the anchors
                                  (incl. the 4c4 date-line note and the new whole-file anchor),
                                  the record commit a2f4fa9…/tag/DEC, precedence, the
                                  nothing-running/$0/zero-OSF-contact state, and the five-step
                                  remediation NEXT
HANDOFF: OK
```

The pre-existing Check-2 override history in the gate string survives verbatim — the new sentence
was **appended**, asserted with `new.startswith(old)`.

**`.planning/STATE.md`** — new `## 2026-08-22 03:25 UTC … ✅ POSTED + RECORDED …
(★ RESUME HERE — LATEST ★)` block inserted above the 02:55 block, which was demoted in place to
`(SUPERSEDED by the 2026-08-22 POSTED block above)`. New `Last activity:` line added; the previous
one demoted to `Last activity (prior):`. `19` additions / `2` deletions (the two in-place
demotions). Exactly **one** `★ RESUME HERE — LATEST ★` remains. **No `### Quick Tasks Completed`
row was added** — that section is still at line 1740 and untouched; the orchestrator owns it.

**`.planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md`** — new
`> **2026-08-22 03:25 UTC — POSTED + RECORDED (★★ LATEST ★★ …)**` block prepended after the
frontmatter; the 02:55 block's marker demoted. Exactly **one** `★★ LATEST ★★` remains.
Frontmatter refreshed:

```
status: amendment_POSTED_mk7ze__2026-08-22T02-58-55Z__gate_CLEARED__two_condition_code_change_AUTHORISED_not_yet_made__fire_HELD
last_updated: 2026-08-22T03:25:00.000Z
```

---

## 10. Safety statement

- **Zero OSF contact by any agent** — no fetch, no API call, no download, no page visit, at any
  point in this task. Carter posted; his four captures in the plan's `<interfaces>` were the sole
  OSF-side source and were used exactly as given, unreformatted.
- **Nothing was fired.** No AoU / Workbench / VM / Dataproc / cluster action of any kind. `$0`.
- **No edit under `src/`, `tests/`, `config/`, `Snakefile`, or the ox1 runbooks** — asserted empty
  at every commit against both `c61d179` and `2689cae`.
- **The code-constant change is AUTHORISED and NOT YET MADE.**
  `src/python/run_native_ld_panel.py:133` still reads `_OCCLUSION_ANOMALY_FRACTION = 0.0005`. The
  remediation batch is the next, **separate** task, and it is authorised **only** as the TWO-condition
  gate (site fraction `> 0.5056%` **OR** occluded-site row/site inflation `> 3.42x`); the ceiling
  alone remains **NOT** authorised, accounting stays ROW-keyed, both numbers are emitted, and
  **Stage A stays HELD**.
- The `260820-s2x-instantiate.py` engine was **never run**, and the posted July amendment
  `osf-amendment-afr-occlusion-exclude-UPDATE-2026-07-10.md` is byte-unchanged.

---

## Deviations from Plan

**None.** The plan executed exactly as written: three tasks, three commits, the plan's commit
messages verbatim, all pre-flight anchors matched (HEAD `c61d179`; amendment 43,042 B /
`c7f9dee2…` / 594 lines; paste block 22,945 B / `13a49f54…`; deviations 420 lines /
`dd380631…`; DECISIONS 2,302 lines; `resume_on_reconnect` 18 entries; ledger 21; `{{` 0; guard
exit 0; line 133 == `0.0005`; code diff empty), and every predicted count came in as predicted
(58 template lines, 6 markers, exactly 2 deliberate fidelity misses).

### Honest count / measurement notes

Three places where a measured number is worth stating rather than glossing:

1. **`.planning/HANDOFF.json` measured 110,426 B pre-task, not the plan's stated "111,358 B
   serialized."** Neither the pre-task on-disk byte count (110,426) nor an `ensure_ascii=True`
   re-serialization of it (111,903) equals 111,358, so the plan's figure appears to be a plan-time
   measurement of something slightly different. It is an informational anchor only — no verify
   block asserts it — and every structural fact the plan *did* assert about the file held exactly
   (valid JSON; `timestamp` `2026-08-22T02:55:00Z`; `resume_on_reconnect` a list of 18 strings;
   `gates.osf_pre_registration` one long string). Reported rather than forced.
2. **`grep -c '0.5056'` on the amendment returns 9, not 1.** The plan's verify block prints this
   count without asserting a value; 8 of the 9 are pre-existing occurrences inside the paste block
   and the pre-existing checklist item 6. Only the D3 STATUS line is new. The count is the
   file-wide total, as the command asks for, and it is not evidence of a duplicated insert.
3. **`grep -c 'mk7ze'` counts are per-line, not per-occurrence** — 4 lines in the deviation entry,
   2 lines in `.continue-here.md`. The plan's floors are `>= 1`; both clear.

### One scope observation, recorded not acted on

Inside the amendment's fenced-template provenance paragraph (below the PASTE markers), the
pre-existing sentence *"Until then `.planning/osf_deviations.md` is byte-unchanged, and this entry
does not exist anywhere but here"* is now historically stale. The plan authorised **exactly three**
edits and explicitly directed that "the fenced template below is kept as drafted, for provenance,"
so it was left alone — and the D2 line inserted immediately above it now states that the entry
**is** appended, with the tag and GUID, so no reader is misled. Flagging it rather than silently
widening the plan's edit scope on a file whose body is public.

---

## Self-Check: PASSED

Files claimed, verified present on disk:

```
FOUND: .planning/osf_deviations.md                                              (530 lines)
FOUND: .planning/DECISIONS.md                                                   (2,411 lines)
FOUND: .planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md (598 lines)
FOUND: .planning/HANDOFF.json                                                   (valid JSON)
FOUND: .planning/STATE.md
FOUND: .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md
```

Commits and tag, verified in the object store and on origin:

```
FOUND: a2f4fa9289da9a0d47cf66f87d8bbfeed47c4364
FOUND: 3b9d824e96bd73b76be1de256129925ddcd60210
FOUND: 84b583d9be4b14f1b435dfdbb7998929c2235412
FOUND: refs/tags/AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22 -> a2f4fa9… (local + origin, count 1)
```

Plan-wide `<verification>` block: sections 1-8 all green (record complete + 0 unfilled markers +
prefix md5 `dd380631…`; plain tag on a single-path commit, 1 on origin; paste block
`22945` / `13a49f54…` + guard exit 0 + `{{` 0 + ledger 21; DEC present; line 133 `0.0005` and both
code diffs 0 lines; HANDOFF valid with gate cleared and `mk7ze` in `resume[0]`; `git status -sb`
not ahead; July amendment 0-line diff).
