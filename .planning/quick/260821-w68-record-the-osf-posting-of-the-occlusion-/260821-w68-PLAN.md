---
phase: quick-260821-w68
plan: 01
type: execute
wave: 1
depends_on: []
subsystem: pre-registration
tags: [osf, amendment, occlusion, posting, record, deviation-entry, dec, tag, handoff, m3-07]

files_modified:
  - .planning/osf_deviations.md
  - .planning/DECISIONS.md
  - .planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
  - .planning/HANDOFF.json
  - .planning/STATE.md
  - .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md
  - .planning/quick/260821-w68-record-the-osf-posting-of-the-occlusion-/260821-w68-PLAN.md
  - .planning/quick/260821-w68-record-the-osf-posting-of-the-occlusion-/260821-w68-SUMMARY.md

autonomous: true

requirements:
  - DEC-2026-08-19-occlusion-recalibration-adopted
  - OSF-AMEND-OCCLUSION-RECORD-POSTING
  - OSF-AMEND-OCCLUSION-DEC-POSTED
  - OSF-AMEND-OCCLUSION-TAG-RECORD-COMMIT
  - OSF-GATE-M3-07-CLAUSE-D-CLEAR

user_setup: []

must_haves:
  truths:
    - "The record exists and is complete: the LAST `## ` entry of `.planning/osf_deviations.md` carries `mk7ze`, `https://osf.io/mk7ze`, `2026-08-22T02:58:55Z`, `13a49f543cabcc27ce9f1e589783c060`, `07df11e44f2d56536ef4ef0753c8d2f8fdb55ae8` and `2026-07-10T13:32:21Z`, and contains ZERO `<TO BE FILLED AT POSTING>` markers."
    - "The record is a pure APPEND, not a rewrite: the first 420 lines of `.planning/osf_deviations.md` are byte-identical to their pre-task state (md5 `dd3806312977513a8727463ec3a032df`), `git diff --numstat` reports 0 deletions on that path, and the file's line-count delta equals exactly the line count of the chunk that was appended."
    - "The entry is the template, filled — not a retyped paraphrase: the fenced PREPARED block was extracted from the amendment BY SCRIPT, and every line of the extracted template that carried no `<TO BE FILLED AT POSTING>` marker appears verbatim in the appended entry."
    - "The three things the reader could otherwise be misled about are disclosed in the entry itself: the posting date moved 2026-08-21 → 2026-08-22 by the engine's Class-P pass and the reviewed body (`422f1f28…`) differs from the posted body (`13a49f54…`) by exactly the `**Date:**` line (`4c4`); the integrity hash is OSF's own API-reported md5 and NOT a local re-download (the scripted re-download was refused; the local check is an optional non-blocking follow-up); and the template's prediction that 'Date created' shows the parent record's 2026-04-10 date was NOT borne out (observed `2026-08-22T02:58:53Z`, the object's own creation, 2 s before the authoritative Recent-Activity stamp)."
    - "The record commit is TAGGED and the tag means what it says: `AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22` exists locally AND on origin, is a plain (non-annotated) tag like its July sibling, and points at a commit whose diff touches `.planning/osf_deviations.md` and nothing else."
    - "The public body is frozen and PROVEN frozen: at every commit this task makes, the amendment's paste block (the region between the two PASTE markers, exclusive of both) is still 22,945 B / `13a49f543cabcc27ce9f1e589783c060`; the guard `all` exits 0; `grep -c '{{'` is 0; and the SLOT_LEDGER still carries exactly 21 ledger-shaped lines."
    - "The frozen-ness is EVIDENCE and not decoration, because it was seen to fail: a one-byte flip inside the paste block of a scratchpad copy moves the awk-extracted md5 off `13a49f54…`, on the same command that returns `13a49f54…` on the unperturbed copy."
    - "The decision is banked where a future session reads it: `.planning/DECISIONS.md` contains `DEC-2026-08-22-occlusion-recalibration-posted`, it records the alternatives that were rejected (not posting; uploading as a new version of trsx5; hand-editing the date), and it states the consequence precisely — the code-constant change is AUTHORISED and ONLY as the TWO-condition gate (site fraction > 0.5056% OR occluded-site row/site inflation > 3.42x), with ceiling-alone NOT authorised, accounting staying in ROWS, both numbers emitted."
    - "Precedence is satisfied and re-measured, not assumed: `src/python/run_native_ld_panel.py:133` still reads `_OCCLUSION_ANOMALY_FRACTION = 0.0005`, `git diff --stat c61d179 HEAD -- src/ tests/ config/` is EMPTY at every commit, and the record commit `c61d179` (2026-08-22T02:48:03Z) precedes the OSF stamp 02:58:55Z."
    - "Resume surfaces agree with the record: `.planning/HANDOFF.json` is valid JSON, `gates.osf_pre_registration` ends with the posting sentence containing `THIS OBLIGATION IS CLEARED` while explicitly leaving the separate Check-2 obligation OPEN, and `resume_on_reconnect[0]` names `mk7ze`, the anchors, the tag, and the remediation batch as NEXT."
    - "Nothing was posted, nothing was fired, nothing cost money: zero OSF contact by any agent, zero AoU/VM/cluster action, no edit under `src/`, `tests/`, `config/`, and the posted July amendment `osf-amendment-afr-occlusion-exclude-UPDATE-2026-07-10.md` is byte-unchanged."
    - "The branch is fully published: after Task 3, `git status -sb` shows no `ahead`, and `git ls-remote --tags origin` matches the new tag exactly once."
  artifacts:
    - path: ".planning/osf_deviations.md"
      provides: "THE RECORD — the filled deviation entry for the posted recalibration amendment, appended as the file's last `## ` section"
      contains: "mk7ze"
      min_lines: 470
    - path: ".planning/DECISIONS.md"
      provides: "DEC-2026-08-22-occlusion-recalibration-posted — the gate-clear decision and the TWO-condition authorisation of the code change"
      contains: "DEC-2026-08-22-occlusion-recalibration-posted"
    - path: ".planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md"
      provides: "Post-paste status: the PREPARED heading marked APPENDED, the checklist marked items 1-5 DONE / item 6 AUTHORISED — all OUTSIDE the paste markers, paste block byte-frozen"
      contains: "APPENDED 2026-08-22"
    - path: ".planning/HANDOFF.json"
      provides: "The cleared OSF gate sentence and a new resume_on_reconnect[0] that supersedes every earlier #0"
      contains: "THIS OBLIGATION IS CLEARED"
    - path: ".planning/STATE.md"
      provides: "New 2026-08-22 03:25 UTC POSTED + RECORDED top block; the 02:55 date-move block demoted in place"
      contains: "POSTED + RECORDED"
    - path: ".planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md"
      provides: "Phase-local resume block carrying the four captures and the remediation NEXT"
      contains: "mk7ze"
  key_links:
    - from: ".planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md (fenced PREPARED block, lines 535-594)"
      to: ".planning/osf_deviations.md (appended entry)"
      via: "script extraction between the ``` fences under the `## PREPARED` heading, then marker substitution — never retyped"
      pattern: "M1/r3/tcujq/trsx5 pattern"
    - from: "the record commit (touches only .planning/osf_deviations.md)"
      to: "tag AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22"
      via: "plain git tag on that commit, pushed explicitly (`git push` does NOT push tags)"
      pattern: "AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22"
    - from: ".planning/DECISIONS.md DEC-2026-08-22-occlusion-recalibration-posted"
      to: ".planning/HANDOFF.json gates.osf_pre_registration"
      via: "the gate sentence names the DEC id and the tag, and declares the recalibration obligation CLEARED while the Check-2 obligation stays OPEN"
      pattern: "DEC-2026-08-22-occlusion-recalibration-posted"
    - from: ".planning/amendments/… Post-Paste checklist item 6"
      to: ".planning/DECISIONS.md consequences section"
      via: "item 6 AUTHORISED — and only as the TWO-condition gate; the DEC restates the same two conditions with the same two constants"
      pattern: "0\\.5056"
---

<objective>
Carter posted the occlusion-gate recalibration amendment to OSF and captured the four record
items. This task MAKES THE RECORD: it fills and appends the prepared deviation entry, banks the
posting decision, marks the amendment's own post-paste checklist done, moves the HANDOFF gate to
CLEARED, tags the record commit, refreshes the three resume surfaces, and pushes branch + tag.

Purpose: a pre-registration is only as good as the in-repo record of what was posted, when, and
whether the posted bytes are the reviewed bytes. Three things in this posting could quietly
mislead a future reader and each is disclosed rather than smoothed over — the posting date moved
after review (one line, `4c4`), the integrity hash is OSF's own API-reported md5 rather than a
local re-download (the scripted re-download was refused by OSF's file server), and the prepared
template's prediction about the file page's "Date created" was not borne out.

Output: one filled deviation entry, one DEC, one git tag, two status lines in the amendment
outside the frozen paste block, and three refreshed resume surfaces — in exactly three atomic,
explicit-path commits, followed by a push of the branch and the tag.

DOCS-ONLY. NO code change. The code-constant change is a LATER, SEPARATE task. An agent never
posts and never fires.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/HANDOFF.json
@.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
@.planning/osf_deviations.md
@.planning/DECISIONS.md
@.planning/debug/260821-POSTING-CARD-for-carter.md
@.planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md

<interfaces>
<!-- Every path, anchor, line number, command and captured value this task needs. Measured
     read-only at plan time, 2026-08-21 ~23:10 EDT, at HEAD c61d179, origin == local.
     Do NOT go hunting. Re-MEASURE these before acting; do not re-DERIVE them. -->

REPO ROOT (all relative paths are from here; run everything from here):
  /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis

SCRATCHPAD (negative-control fixtures and the staged append chunk — NEVER staged in git):
  /gpfs_common/share01/clintonlab/ckclinto/tmp/claude-410819/-gpfs-common-share01-clintonlab-ckclinto-coloc-analysis/19c6a323-a3ca-4c18-bffc-bfcb052f7fe3/scratchpad
  Referred to below as <SCR>. Task 2's control lives at <SCR>/w68-nc/.

CARTER'S FOUR CAPTURES (2026-08-21 23:07 EDT — use EXACTLY these strings; do not reformat,
re-case, or "tidy" any of them):
  1. GUID            mk7ze
     URL             https://osf.io/mk7ze
     Parent record   az52u
     Placement       the project's OSF Storage ROOT, as its own file
                     (breadcrumb: Az52u / Files / Mk7ze) — NOT under trsx5, NOT a child of it
     Stored filename osf-amendment-occlusion-gate-recalibration-2026-08-22.md
                     (exact lowercase; the OSF page HEADING title-cases it visually — the
                     file list, the API and the activity log all show the lowercase name)
  2. Authoritative UTC stamp   2026-08-22T02:58:55Z
     Source          the Recent Activity log entry "Carter Clinton added file
                     osf-amendment-occlusion-gate-recalibration-2026-08-22.md to OSF Storage"
     Widget render   "Aug 21, 2026 10:58 PM" ET (same instant)
     ⚠ OBSERVED, contradicting the template: the file page's "Date created" read
                     2026-08-22T02:58:53Z — the OBJECT's creation, 2 s before the log entry —
                     NOT the parent record's 2026-04-10 date that the prepared template
                     predicted. The Recent Activity entry is the authoritative stamp.
  3. OSF-stored md5  13a49f543cabcc27ce9f1e589783c060   (22,945 bytes, version 1)
     ⚠ METHOD CAVEAT, to be recorded faithfully, not paraphrased away: a scripted
       byte-for-byte re-download from inside the page was REFUSED by OSF's file server (the
       project is private; the cross-origin authenticated fetch was rejected). The hash above
       is the one OSF Storage itself computed and stores for the object, read from the file's
       API record after upload. A local md5 of a manually re-downloaded copy was NOT
       performed — an OPTIONAL belt-and-suspenders follow-up, NON-BLOCKING. It equals the
       repo paste-block md5 exactly.
  4. trsx5 append-only  still exactly 1 revision. Revisions panel shows a single entry
       "1. Jul 10, 2026 09:32 AM" (ET); the API confirms one version created
       2026-07-10T13:32:21Z. Nothing was added as a new version.

THE AMENDMENT (the ONE file under .planning/amendments/ this task touches):
  .planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
  PRE-TASK anchors, measured at plan time — assert BEFORE and AFTER every edit:
    whole file   43,042 B / c7f9dee26d3070ea83d6899dfc2c90d3 / 594 lines
    paste block  22,945 B / 13a49f543cabcc27ce9f1e589783c060   <- PUBLIC. FROZEN. NEVER MOVES.
    SLOT_LEDGER  exactly 21 lines matching ^  [A-Z0-9_]+ = 
    '{{'         0 occurrences
    '<TO BE FILLED AT POSTING>'  7 lines (531 prose, 536, 538, 539, 540, 547, 551)
  Line map at plan time (locate by CONTENT, never by number — Task 2 shifts everything below 505):
    167  --- PASTE INTO OSF FROM HERE ---   <- marker, EXCLUSIVE of the block
    501  --- PASTE ENDS HERE ---            <- marker, EXCLUSIVE of the block
    503  ## Post-Paste Reference (do NOT paste this block)
    505  **Verification checklist after OSF posting:**      <- Task 2b inserts ONE line under this
    507-521  checklist items 1-6
    523  **Rollback:** …
    526  ## PREPARED `.planning/osf_deviations.md` ENTRY — ⚠ NOT-YET-APPENDED   <- Task 2b edits
    528-533  the "TEXT ONLY / appends only when Carter has posted" paragraph
    535  ```                                <- opening fence of the PREPARED template
    536-593  the template body (58 lines)   <- Task 1 extracts THIS, by script
    594  ```                                <- closing fence

PASTE-BLOCK EXTRACTION (the exact awk; marker lines excluded by construction — use this and
nothing else, at every assertion point in all three tasks):
  awk '/^--- PASTE INTO OSF FROM HERE ---$/{f=1;next} /^--- PASTE ENDS HERE ---$/{f=0} f' <amendment>

PREPARED-TEMPLATE EXTRACTION (Task 1 — anchor on the HEADING PREFIX, which survives Task 2's
heading edit, then take the first fenced block after it):
  python3 - <<'PY'
  import re, sys
  src = open('.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md').read().splitlines()
  i = next(k for k,l in enumerate(src) if l.startswith('## PREPARED `.planning/osf_deviations.md` ENTRY'))
  o = next(k for k,l in enumerate(src[i:], i) if l.strip() == '```')
  c = next(k for k,l in enumerate(src[o+1:], o+1) if l.strip() == '```')
  open('<SCR>/w68-template.md','w').write('\n'.join(src[o+1:c]) + '\n')
  print('template lines:', c-o-1)   # expect 58
  PY

THE GUARD (a CONSUMER dependency — do NOT edit it; if it goes red on anything this plan does not
declare, STOP AND REPORT):
  bash .planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh \
       all .planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
  Exit 0 = every check PASSED (verified exit 0 at plan time).
  It requires EXACTLY 21 SLOT_LEDGER lines matching `^  [A-Z0-9_]+ = ` — never add such a line.
  It fails on `{{`. It locates the PASTE markers by grep, not by line number, so prose edits
  OUTSIDE the markers do not perturb it. Its `(site basis)` / `(row basis)` literal counts are
  computed over the PASTE BLOCK only — but as belt-and-braces, the lines Task 2b inserts must
  NOT contain the literal strings `(site basis)` or `(row basis)`.

⛔ THE INSTANTIATION ENGINE IS NOT INVOKED BY THIS TASK.
  .planning/quick/260820-s2x-instantiate-the-occlusion-recalibration-/260820-s2x-instantiate.py
  Class-P force substitution would rewrite the `**Date:**` line INSIDE the now-public paste
  block. Do not run it. No slot value changes in this task.

TARGET FILES — pre-task anchors (re-measure before editing):
  .planning/osf_deviations.md   420 lines / 31,301 B / dd3806312977513a8727463ec3a032df
    ends with a newline; last `## ` section starts at line 133 (2026-07-10); lines 254 and 346
    are `### ` sub-sections of it. The new entry is a NEW `## ` section appended at EOF.
  .planning/DECISIONS.md        2,302 lines; last `## 2026…` heading at 2145
    (DEC-2026-08-19-occlusion-recalibration-adopted); ends with a newline.
  .planning/HANDOFF.json        valid JSON, 111,358 B serialized; timestamp 2026-08-22T02:55:00Z;
    resume_on_reconnect is a list of 18 strings; gates.osf_pre_registration is one long string.
  .planning/STATE.md            line 28 = current `(★ RESUME HERE — LATEST ★)` block heading
    (2026-08-22 02:55 UTC date-move); line 36 = the prior block, already demoted;
    line 1443 = `Last activity:` ; 1444+ = `Last activity (prior):` ;
    line 1723 = `### Quick Tasks Completed`  <- the ORCHESTRATOR adds that row, NOT this task.
  .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md
    frontmatter lines 1-9 (`status:` line 7, `last_updated:` line 8); line 11 = current
    `★★ LATEST ★★` block (2026-08-22 02:55 UTC date move).

REPO FACTS re-measured at plan time (re-measure, do not trust):
  HEAD                                    c61d17901fb4cc779588d53d0edc4d2e8e23f154 (c61d179)
  HEAD commit time                        2026-08-21T22:48:03-04:00 = 2026-08-22T02:48:03Z
  branch                                  m3-W2-aou-deltas ; origin == local ; tracked tree clean
  git diff --stat c61d179 HEAD -- src/ tests/ config/     EMPTY (0 lines)
  git diff --stat 2689cae HEAD -- src/ tests/ config/     EMPTY (0 lines)
  src/python/run_native_ld_panel.py:133   _OCCLUSION_ANOMALY_FRACTION = 0.0005
  existing tags matching AFR-OCCLUSION*   AFR-OCCLUSION-EXCLUDE-OSF-UPDATE-POSTED-2026-07-10
                                          -> plain (non-annotated) tag on commit ac4c990
  ~15 untracked results/ backup dirs are present and MUST stay untracked.

THE SEVEN SUPPORTING RECORDS (all present under .planning/debug/ — verified at plan time):
  260819-SETH-VERDICT-adjudication-confirmed-as-received.md
  260819-SETH-C1C2C3-convergence-as-received.md
  260819-occ-measure-sweep-results-as-received.md
  260819-supplement-results-as-received.md
  260820-site-basis-sweep-results-as-received.md
  260820-SETH-ATTACK-instantiated-amendment-as-received.md
  260821-SETH-FINAL-PASS-no-blocking-objection-as-received.md
  Posting card: .planning/debug/260821-POSTING-CARD-for-carter.md
</interfaces>
</context>

<constraints>
DOCS-ONLY. This task records a posting that already happened. It creates no code and fires nothing.

FORBIDDEN — absolute:
- Any OSF contact of any kind, by any agent, by any means: no fetch, no API call, no download,
  no "just verifying the page". Carter posted. The captures in `<interfaces>` are the ONLY
  source of OSF-side truth. AN AGENT NEVER POSTS.
- Any AoU / Workbench / VM / Dataproc / cluster action. AN AGENT NEVER FIRES. Nothing here costs
  money.
- Any edit under `src/`, `tests/`, `config/`, `Snakefile`, or the ox1 runbooks.
- Editing the POSTED July amendment `.planning/amendments/osf-amendment-afr-occlusion-exclude-UPDATE-2026-07-10.md`.
- Editing ANYTHING between the two PASTE markers in the recalibration amendment. That body is
  PUBLIC as of 2026-08-22T02:58:55Z. It must never change again, for any reason, including a
  typo. If you find an error inside it, STOP AND REPORT — the remedy is a further OSF record,
  never a repo edit.
- Running `260820-s2x-instantiate.py`. Class-P would rewrite the `**Date:**` line inside the
  public block. Not "carefully" — not at all.
- Editing `260819-u8d-placeholder-guard.sh` or `260820-s2x-instantiate.py`. This task is a
  consumer of both. If either goes red on something this plan does not declare, STOP AND REPORT;
  do not repair a tool to make a task pass.
- Adding any line matching `^  [A-Z0-9_]+ = ` to the amendment. The guard counts exactly 21 and
  a 22nd is a hard red.
- `git add .` / `git add -A` / `git add <directory>`. GPFS shared tree, ~15 untracked
  results/backup dirs. EXPLICIT FILE PATHS ONLY, every time. No worktrees.
- Adding the `### Quick Tasks Completed` row to STATE.md. The ORCHESTRATOR owns that row.

REQUIRED — the frozen-body assertion:
Every task asserts, BEFORE its first edit and AFTER its commit:

```
A=.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
awk '/^--- PASTE INTO OSF FROM HERE ---$/{f=1;next} /^--- PASTE ENDS HERE ---$/{f=0} f' "$A" | md5sum
```

MUST print `13a49f543cabcc27ce9f1e589783c060`. Any other value at any point = STOP AND REPORT,
do not commit, do not "restore and continue". That md5 is now a public fact about an OSF object.

⚠ THE TRAP THAT WILL BITE. The prepared template contains the sentence *"The file page's 'Date
created' is the PARENT RECORD's creation date (2026-04-10, osf.io/pvb5j), NOT this file's upload
date."* Carter's observation CONTRADICTS it. Filling the marker and leaving that sentence
standing would ship a false statement in the one artifact whose job is establishing what was
true. The sentence must be REPLACED with the observed facts, and the entry must SAY the
template's expectation was not borne out. Do not silently delete it either — a template
prediction that failed is itself worth recording.

⚠ THE SECOND TRAP. "It matches" is doing a lot of work in this entry. The OSF-stored md5 equals
the repo paste-block md5 — but it was READ FROM OSF'S API, not recomputed from a downloaded
byte stream, because OSF refused the scripted re-download. Write the caveat in substance. Do not
compress it to "verified"; do not upgrade an API-reported hash into a re-download.

Evidence discipline:
- A green is evidence ONLY if it has been seen red IN THIS TASK. Task 2 executes the paste-block
  negative control as a 2-cell matrix on the SAME command and the SAME fixture path
  (unperturbed → `13a49f54…`; one byte flipped inside the block → anything else), with both
  transcripts in the SUMMARY. A transcript copied from an earlier task does not satisfy this.
- Control fixtures live under `<SCR>/w68-nc/` and are NEVER staged, NEVER created in the repo tree.

Commits: exactly three, exactly the messages this plan gives, atomic, explicit paths, in order.
GPFS object-store contingency: if a commit fails with "invalid object" / "Error building trees",
run the guarded `git hash-object -w` recovery loop over the staged paths and retry ONCE. Twice
failing = STOP and report.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Fill and append the prepared deviation entry — THE RECORD COMMIT — then tag it</name>

  <files>
.planning/osf_deviations.md                       (pure append at EOF; nothing above line 420 moves)
&lt;SCR&gt;/w68-template.md, &lt;SCR&gt;/w68-append.md        (staging — NEVER committed)
  </files>

  <action>
The prepared entry was drafted weeks ago and has been sitting in the amendment marked
NOT-YET-APPENDED. It gets into `.planning/osf_deviations.md` by EXTRACTION, not by retyping — a
retype is a silent-rewrite vector in the one file whose purpose is establishing what was on the
record.

**STEP A — pre-flight. Assert the world is the world this plan measured.**

```
cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis
SCR=/gpfs_common/share01/clintonlab/ckclinto/tmp/claude-410819/-gpfs-common-share01-clintonlab-ckclinto-coloc-analysis/19c6a323-a3ca-4c18-bffc-bfcb052f7fe3/scratchpad
A=.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
D=.planning/osf_deviations.md
PB() { awk '/^--- PASTE INTO OSF FROM HERE ---$/{f=1;next} /^--- PASTE ENDS HERE ---$/{f=0} f' "$1"; }

git rev-parse HEAD                       # expect c61d17901fb4cc779588d53d0edc4d2e8e23f154
git status --porcelain -- .planning/ src/ tests/ config/ | head
wc -c "$A"; wc -l "$A"; md5sum "$A"      # 43042 / 594 / c7f9dee26d3070ea83d6899dfc2c90d3
PB "$A" | wc -c; PB "$A" | md5sum        # 22945 / 13a49f543cabcc27ce9f1e589783c060
wc -l "$D"; md5sum "$D"                  # 420 / dd3806312977513a8727463ec3a032df
sed -n '133p' src/python/run_native_ld_panel.py
git diff --stat c61d179 HEAD -- src/ tests/ config/ | wc -l   # MUST print 0
```

Any mismatch → STOP AND REPORT. Do not fix forward.

**STEP B — extract the template BY SCRIPT.** Use the extractor given verbatim in
`<interfaces>` (anchored on the `## PREPARED …` heading PREFIX, then the first fenced block after
it). It must report `template lines: 58` and write `$SCR/w68-template.md`. Confirm:

```
wc -l $SCR/w68-template.md                                   # 58
grep -c 'TO BE FILLED AT POSTING' $SCR/w68-template.md       # 6
head -1 $SCR/w68-template.md                                 # the ## <TO BE FILLED…> heading
```

**STEP C — fill the six markers, and replace the one sentence Carter's observation refutes.**
Do this with a python script that operates on `$SCR/w68-template.md` and writes
`$SCR/w68-filled.md`, so every substitution is auditable. Exact targets:

1. Heading line → `## 2026-08-22 — AFR native-panel occlusion anomaly-gate RECALIBRATION + factual correction to trsx5 (m3-07 OSF gate, second record)`
2. `**Posted:** OSF file `<TBF>`` → `` `mk7ze` ``
3. The bare `` `<TBF>` `` on the next line (the URL slot) → `` `https://osf.io/mk7ze` ``
4. `osf-amendment-occlusion-gate-recalibration-<TBF>.md` → `…-2026-08-22.md`
5. `**OSF timestamp (authoritative, UTC):** `<TBF>`` → `` `2026-08-22T02:58:55Z` ``
6. `**Pre-execute gate commit:** `<TBF>`` → `` `07df11e44f2d56536ef4ef0753c8d2f8fdb55ae8` ``

Then REPLACE the timestamp bullet's trailing sentence — the one asserting "Date created" is the
parent record's date — so the whole bullet reads:

```
- **OSF timestamp (authoritative, UTC):** `2026-08-22T02:58:55Z` — the Recent Activity log
  entry "Carter Clinton added file
  osf-amendment-occlusion-gate-recalibration-2026-08-22.md to OSF Storage" (the widget
  renders that instant as "Aug 21, 2026 10:58 PM" ET). ⚠ **The prepared template predicted
  that this file page's "Date created" would show the PARENT RECORD's creation date
  (2026-04-10, osf.io/pvb5j) — as `trsx5`'s page does. That expectation was NOT borne out
  here.** The observed "Date created" read `2026-08-22T02:58:53Z`: this object's own
  creation, 2 s before the activity-log entry. The Recent Activity entry remains the
  authoritative stamp of record; the 2 s gap is upload-then-index, not a discrepancy, and
  it is written down here so a future reader is not surprised by it.
```

**STEP D — append the posting-time findings.** After the filled template, add the sub-heading
`### Recorded at posting (2026-08-22)` and exactly these five bullets (substance is mandatory;
wording may be tightened but nothing may be dropped):

- **Posting-date disclosure.** The amendment carried a PROVISIONAL posting date of 2026-08-21.
  Carter had not uploaded when the UTC day rolled, so `POSTING_DATE` moved 2026-08-21 →
  2026-08-22 through the instantiation engine's Class-P pass (commit `c61d179`), never by hand.
  The body Seth's final pass cleared was 22,945 B / `422f1f28d6a3b76c7657fadec05a0237`; the body
  actually posted is 22,945 B / `13a49f543cabcc27ce9f1e589783c060`; the COMPLETE difference is
  the `**Date:**` line — `diff` reports exactly `4c4`, one line changed, nothing else. No
  scientific statement, number or commitment differs between the reviewed and the posted body.
- **Integrity.** The repo's paste block (between the two PASTE markers, exclusive) is 22,945 B /
  `13a49f543cabcc27ce9f1e589783c060`, equal to the md5 OSF Storage itself computed and stores for
  `mk7ze` version 1 (22,945 bytes) — so what OSF serves IS the repo body. ⚠ Method caveat: a
  scripted byte-for-byte re-download from inside the OSF page was REFUSED by OSF's file server
  (private project; cross-origin authenticated fetch rejected), so this hash was read from the
  file's API record after upload rather than recomputed from a downloaded byte stream. A local
  md5 of a manually re-downloaded copy was NOT performed; that belt-and-suspenders check is an
  OPTIONAL, NON-BLOCKING follow-up. The hash is OSF's own computation over the stored object, not
  a repo echo.
- **Append-only.** `trsx5` still shows exactly **1 revision** — the Revisions panel lists a single
  entry "1. Jul 10, 2026 09:32 AM" (ET) and the API confirms one version created
  `2026-07-10T13:32:21Z`. Nothing was added to it as a new version. `mk7ze` is its own
  single-version file on the project's OSF Storage ROOT (breadcrumb Az52u / Files / Mk7ze), a
  SIBLING of `trsx5`, not a child of it. Two distinct GUIDs, each one version → no silent swap.
- **Precedence (Post-Paste checklist item 1) — SATISFIED, no deviation.** HEAD `c61d179` was
  committed `2026-08-22T02:48:03Z`, ten minutes BEFORE the OSF stamp `2026-08-22T02:58:55Z`.
  `src/python/run_native_ld_panel.py:133` still reads `_OCCLUSION_ANOMALY_FRACTION = 0.0005`,
  last changed in `d9fbc63` (2026-08-13, before this recalibration existed);
  `git diff --stat 2689cae HEAD -- src/ tests/ config/` is EMPTY; no recalibrated-gate output
  exists anywhere. The pre-registration precedes the analysis it governs.
- **Record.** Decision: `DEC-2026-08-22-occlusion-recalibration-posted` in
  `.planning/DECISIONS.md`. Git tag on the record commit:
  `AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22` (July sibling:
  `AFR-OCCLUSION-EXCLUDE-OSF-UPDATE-POSTED-2026-07-10`). The seven supporting records the
  amendment's pre-paste checklist counts live in `.planning/debug/`: the four `260819-…`
  (SETH-VERDICT, SETH-C1C2C3, occ-measure-sweep, supplement), the two `260820-…`
  (site-basis-sweep, SETH-ATTACK) and `260821-SETH-FINAL-PASS-…`. Carter's posting procedure:
  `.planning/debug/260821-POSTING-CARD-for-carter.md`.

Write filled template + sub-heading + bullets, with ONE leading blank line, to `$SCR/w68-append.md`.

**STEP E — prove the chunk before it touches the tree.**

```
grep -c 'TO BE FILLED AT POSTING' $SCR/w68-append.md          # MUST be 0
for s in mk7ze https://osf.io/mk7ze 2026-08-22T02:58:55Z \
         13a49f543cabcc27ce9f1e589783c060 \
         07df11e44f2d56536ef4ef0753c8d2f8fdb55ae8 2026-07-10T13:32:21Z \
         422f1f28d6a3b76c7657fadec05a0237 4c4 0.0005 ; do
  printf '%-45s %s\n' "$s" "$(grep -c -F -- "$s" $SCR/w68-append.md)"; done   # all >= 1
N=$(wc -l < $SCR/w68-append.md); echo "APPEND LINES=$N"
```

Also prove the extraction is faithful: every template line that carried no marker must appear
verbatim in the filled entry.

```
python3 - <<'PY'
t=[l for l in open('<SCR>/w68-template.md').read().splitlines() if 'TO BE FILLED AT POSTING' not in l]
a=set(open('<SCR>/w68-append.md').read().splitlines())
miss=[l for l in t if l not in a]
print('template lines carried verbatim: %d/%d' % (len(t)-len(miss), len(t)))
print('MISSING:', miss[:6])
PY
```

Expect the two sentences of the replaced "Date created" claim to be the ONLY misses (they are
deliberate). Anything else missing = a botched extraction → STOP AND REPORT.

**STEP F — append, assert pure-append, commit, tag.**

```
cat $SCR/w68-append.md >> "$D"
wc -l "$D"                                   # MUST be 420 + N
head -420 "$D" | md5sum                      # MUST be dd3806312977513a8727463ec3a032df
git diff --numstat -- "$D"                   # deletions column MUST be 0
grep -c 'TO BE FILLED AT POSTING' "$D"       # MUST be 0
PB "$A" | md5sum                             # MUST still be 13a49f543cabcc27ce9f1e589783c060

git add .planning/osf_deviations.md
git commit -m "docs(osf): record POSTED occlusion-gate recalibration amendment — mk7ze on az52u, 2026-08-22T02:58:55Z, OSF-stored md5 13a49f54 == repo paste block; trsx5 still 1 revision; precedence satisfied"
git tag AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22
git rev-parse HEAD
git rev-parse AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22
git cat-file -t AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22   # MUST print: commit
```

Record the record-commit 40-hex; Tasks 2 and 3 cite it. Do NOT push yet — Task 3 pushes both.
  </action>

  <verify>
    <automated>
cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis
D=.planning/osf_deviations.md
A=.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
LAST=$(awk '/^## /{n=NR} END{print n}' "$D")
echo "--- last ## entry starts at line $LAST ---"
for s in mk7ze https://osf.io/mk7ze 2026-08-22T02:58:55Z 13a49f543cabcc27ce9f1e589783c060 \
         07df11e44f2d56536ef4ef0753c8d2f8fdb55ae8 2026-07-10T13:32:21Z ; do
  c=$(tail -n +$LAST "$D" | grep -c -F -- "$s"); echo "$s -> $c"; test "$c" -ge 1 || exit 1; done
test "$(tail -n +$LAST "$D" | grep -c 'TO BE FILLED AT POSTING')" -eq 0 && echo "NO UNFILLED MARKERS: OK"
test "$(head -420 "$D" | md5sum | cut -d' ' -f1)" = dd3806312977513a8727463ec3a032df && echo "PREFIX BYTE-IDENTICAL: OK"
test "$(git diff --numstat HEAD~1 HEAD -- "$D" | cut -f2)" = "0" && echo "PURE APPEND (0 deletions): OK"
test "$(git show --name-only --format= HEAD | tr -d '[:space:]')" = ".planning/osf_deviations.md" && echo "RECORD COMMIT TOUCHES ONE PATH: OK"
test "$(git cat-file -t AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22)" = commit && echo "PLAIN TAG: OK"
test "$(git rev-parse AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22)" = "$(git rev-parse HEAD)" && echo "TAG ON RECORD COMMIT: OK"
test "$(awk '/^--- PASTE INTO OSF FROM HERE ---$/{f=1;next} /^--- PASTE ENDS HERE ---$/{f=0} f' "$A" | md5sum | cut -d' ' -f1)" = 13a49f543cabcc27ce9f1e589783c060 && echo "PASTE BLOCK FROZEN: OK"
test "$(git diff --stat c61d179 HEAD -- src/ tests/ config/ | wc -l)" -eq 0 && echo "NO CODE TOUCHED: OK"
    </automated>
  </verify>

  <done>
`.planning/osf_deviations.md` ends with a new `## 2026-08-22 —` entry that is the extracted
template with all six markers filled, the refuted "Date created" sentence replaced by the
observed facts, and a `### Recorded at posting (2026-08-22)` sub-section carrying the five
required bullets. Zero `<TO BE FILLED AT POSTING>` remain. The first 420 lines are byte-identical
(`dd3806312977513a8727463ec3a032df`), the diff has zero deletions, and the commit touches that one
path. Plain tag `AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22` points at it. The
amendment's paste block still hashes `13a49f543cabcc27ce9f1e589783c060`. Nothing under
`src/ tests/ config/` moved. Nothing pushed yet.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bank DEC-2026-08-22-occlusion-recalibration-posted and mark the amendment's post-paste status — with the frozen-block invariant seen RED</name>

  <files>
.planning/DECISIONS.md                                                          (append one `## 2026-08-22 —` entry)
.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md   (2 edits + 1 insert, ALL outside the PASTE markers)
&lt;SCR&gt;/w68-nc/                                                                   (negative-control fixtures — NEVER staged)
  </files>

  <action>
**STEP A — pre-flight, identical assertions to Task 1 STEP A, plus:**

```
git log -1 --oneline                          # the record commit from Task 1
grep -c 'TO BE FILLED AT POSTING' .planning/osf_deviations.md   # 0
grep -cE '^  [A-Z0-9_]+ = ' "$A"              # 21
grep -c '{{' "$A"                             # 0
bash .planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh all "$A" >/dev/null; echo "GUARD EXIT=$?"   # 0
```

**STEP B — the negative control, FIRST, so the invariant is known to be a real check before it is
leaned on.** Build a 2-cell matrix on the SAME command, in the scratchpad, never in the tree:

```
mkdir -p $SCR/w68-nc
cp "$A" $SCR/w68-nc/unperturbed.md
cp "$A" $SCR/w68-nc/perturbed.md
python3 - <<'PY'
p='<SCR>/w68-nc/perturbed.md'
s=open(p).read().splitlines(True)
lo=next(i for i,l in enumerate(s) if l.rstrip('\n')=='--- PASTE INTO OSF FROM HERE ---')
hi=next(i for i,l in enumerate(s) if l.rstrip('\n')=='--- PASTE ENDS HERE ---')
# flip exactly ONE byte on a line strictly inside the block
for i in range(lo+1,hi):
    if len(s[i])>20:
        s[i]=s[i][:10]+('X' if s[i][10]!='X' else 'Y')+s[i][11:]
        print('perturbed line',i+1); break
open(p,'w').writelines(s)
PY
PBF() { awk '/^--- PASTE INTO OSF FROM HERE ---$/{f=1;next} /^--- PASTE ENDS HERE ---$/{f=0} f' "$1" | md5sum; }
echo "CELL 1 unperturbed: $(PBF $SCR/w68-nc/unperturbed.md)"   # MUST be 13a49f543cabcc27ce9f1e589783c060
echo "CELL 2 perturbed:   $(PBF $SCR/w68-nc/perturbed.md)"     # MUST be anything else
cmp -l $SCR/w68-nc/unperturbed.md $SCR/w68-nc/perturbed.md | wc -l   # MUST be 1
```

Both cells must be transcribed into the SUMMARY. If cell 2 equals `13a49f54…`, the invariant is
not measuring what we think it measures → STOP AND REPORT. Never stage `$SCR/w68-nc/`.

**STEP C — append the DEC to `.planning/DECISIONS.md`,** mirroring the shape of
`DEC-2026-08-19-occlusion-recalibration-adopted` (heading → `**Decision (…):**` → what was
adopted → alternatives → why → consequences → cross-refs). Heading, verbatim:

```
## 2026-08-22 — DEC-2026-08-22-occlusion-recalibration-posted: the recalibration amendment is POSTED (mk7ze, 2026-08-22T02:58:55Z) — the m3-07 OSF gate for clause (d) is CLEARED; the code-constant change is AUTHORISED, and ONLY as the TWO-condition gate
```

Then `**Decision (CARTER, 2026-08-21 22:51–23:07 EDT):**` — Carter uploaded the extracted body as
a NEW file on `osf.io/az52u` and captured the four record items. PARAPHRASE his actions; quote
ONLY the four captured values (`mk7ze`, `https://osf.io/mk7ze` on az52u, `2026-08-22T02:58:55Z`,
`13a49f543cabcc27ce9f1e589783c060` / 22,945 B / version 1, trsx5 still 1 revision at
`2026-07-10T13:32:21Z`). Carry the two caveats from the deviation entry in one sentence each: the
`4c4` `**Date:**`-line difference from the Seth-cleared `422f1f28…` body, and the API-reported
(not locally re-downloaded) integrity hash.

**Alternatives considered and rejected** — all three, each with its failure mode:
- *Not posting, and changing the constant anyway.* Rejected: the false 0.0005 premise would
  remain the LIVE pre-registration while the code silently ran a different gate — exactly the
  post-hoc-calibration failure the pre-registration exists to prevent.
- *Uploading as a new VERSION of `trsx5`.* Rejected: a silent swap. It would alter the record
  being corrected and destroy the append-only property that cleared the July posting. `trsx5`
  must — and does — still show exactly 1 revision.
- *Hand-editing the posting date when the UTC day rolled.* Rejected: `POSTING_DATE` appears three
  times in lockstep and the engine's anchors are computed over them; a hand edit breaks the
  lockstep and de-anchors the document. The engine's Class-P pass moved all three (`c61d179`).

**Why:** the posted body is the reviewed body up to one date line; the correction is on the public
record BEFORE the corrected gate executes rather than after; precedence is measured, not assumed.

**Consequences (this is the operative part):** the remediation batch is AUTHORISED —
- the producer gate becomes: DEFER if occluded-**site** fraction **> 0.5056%** **OR** occluded-site
  **row/site inflation > 3.42x**. BOTH conditions. **The ceiling alone is NOT authorised** — that
  is the multiplicity-blind single-metric gate this revision replaced.
- exclusion **accounting stays ROW-keyed**; **both** numbers (sites and rows, plus the inflation
  ratio) are emitted; either condition routes to the same `BRANCH_AFR_OCC_DEFERRED` token — no
  fourth branch.
- test oracle, `fire_verifier.py` `expected_records`, the runbook EXPECTs and the suite
  re-baseline follow, in that order, as a `/gsd-quick --validate` TDD task.
- **Stage A stays HELD** until remediation lands and STEP 7 passes in-perimeter. **An agent never
  fires.**

**Cross-refs:** the deviation entry, the tag, the seven `.planning/debug/` records, the posting
card, and `DEC-2026-08-19-occlusion-recalibration-adopted`.

Append with one blank line before; do not touch anything above the current line 2302.

**STEP D — the amendment's post-paste status. THREE surgical edits, ALL below the
`--- PASTE ENDS HERE ---` marker. Nothing inside the markers. Do not touch the fenced template.**

D1. The heading (currently `## PREPARED \`.planning/osf_deviations.md\` ENTRY — ⚠ NOT-YET-APPENDED`):
    replace the trailing `— ⚠ NOT-YET-APPENDED` with `— ✅ APPENDED 2026-08-22`.

D2. Insert exactly ONE line immediately after that heading:

```
Appended to `.planning/osf_deviations.md` at the record commit (tag `AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22`) with GUID `mk7ze` / `2026-08-22T02:58:55Z`; the fenced template below is kept as drafted, for provenance.
```

D3. Insert exactly ONE status line immediately under `**Verification checklist after OSF posting:**`:

```
**STATUS 2026-08-22 — items 1-5 DONE:** (1) precedence SATISFIED — `c61d179` at 02:48:03Z precedes the 02:58:55Z OSF stamp, `_OCCLUSION_ANOMALY_FRACTION` still 0.0005, no recalibrated-gate output; (2) GUID `mk7ze` + authoritative UTC captured; (3) `trsx5` still exactly 1 revision; (4) the prepared entry is APPENDED to `.planning/osf_deviations.md`; (5) the record commit is TAGGED `AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22`. **Item 6 is therefore now AUTHORISED — and ONLY as the TWO-condition gate (site-basis fraction > 0.5056% OR occluded-site row/site inflation > 3.42x); the ceiling condition alone remains NOT authorised.**
```

Neither inserted line may match `^  [A-Z0-9_]+ = ` (they don't — no two-space indent), and neither
may contain the literal `(site basis)` or `(row basis)`.

**STEP E — post-edit assertions, then commit.**

```
PB "$A" | wc -c; PB "$A" | md5sum       # 22945 / 13a49f543cabcc27ce9f1e589783c060  <- HARD GATE
grep -cE '^  [A-Z0-9_]+ = ' "$A"        # 21
grep -c '{{' "$A"                       # 0
grep -c 'NOT-YET-APPENDED' "$A"         # 0
grep -c 'APPENDED 2026-08-22' "$A"      # 1
bash .planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh all "$A"; echo "GUARD EXIT=$?"   # 0
wc -c "$A"; wc -l "$A"; md5sum "$A"     # NEW whole-file anchor -> record it in the SUMMARY
grep -c 'DEC-2026-08-22-occlusion-recalibration-posted' .planning/DECISIONS.md   # >= 1
git status --porcelain -- .planning/ | head

git add .planning/DECISIONS.md .planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
git commit -m "docs(dec+amendment): DEC-2026-08-22-occlusion-recalibration-posted — m3-07 clause-(d) OSF gate CLEARED; checklist items 1-5 DONE, item 6 AUTHORISED as the TWO-condition gate only; paste block untouched (13a49f54)"
```
  </action>

  <verify>
    <automated>
cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis
A=.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
test "$(awk '/^--- PASTE INTO OSF FROM HERE ---$/{f=1;next} /^--- PASTE ENDS HERE ---$/{f=0} f' "$A" | md5sum | cut -d' ' -f1)" = 13a49f543cabcc27ce9f1e589783c060 && echo "PASTE BLOCK FROZEN: OK"
test "$(awk '/^--- PASTE INTO OSF FROM HERE ---$/{f=1;next} /^--- PASTE ENDS HERE ---$/{f=0} f' "$A" | wc -c)" -eq 22945 && echo "PASTE BLOCK SIZE: OK"
bash .planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh all "$A" >/dev/null; echo "GUARD EXIT=$?"
test "$(grep -cE '^  [A-Z0-9_]+ = ' "$A")" -eq 21 && echo "LEDGER 21: OK"
test "$(grep -c '{{' "$A")" -eq 0 && echo "NO SENTINELS: OK"
test "$(grep -c 'NOT-YET-APPENDED' "$A")" -eq 0 && echo "HEADING FLIPPED: OK"
grep -c 'APPENDED 2026-08-22' "$A"
grep -c 'items 1-5 DONE' "$A"
grep -c '0.5056' "$A"
test "$(grep -c 'DEC-2026-08-22-occlusion-recalibration-posted' .planning/DECISIONS.md)" -ge 1 && echo "DEC PRESENT: OK"
git show --name-only --format= HEAD
test "$(git diff --stat c61d179 HEAD -- src/ tests/ config/ | wc -l)" -eq 0 && echo "NO CODE TOUCHED: OK"
git diff --numstat HEAD~1 HEAD -- .planning/osf_deviations.md | wc -l   # MUST print 0 (Task 1's file untouched here)
    </automated>
  </verify>

  <done>
`.planning/DECISIONS.md` carries `DEC-2026-08-22-occlusion-recalibration-posted` with Carter's
action, the three rejected alternatives, and the TWO-condition authorisation stated with both
constants. The amendment's PREPARED heading reads `— ✅ APPENDED 2026-08-22`, carries the one-line
provenance sentence, and the checklist carries the one-line items-1-5-DONE / item-6-AUTHORISED
status — all below the `--- PASTE ENDS HERE ---` marker. Paste block still 22,945 B /
`13a49f543cabcc27ce9f1e589783c060`; guard `all` exit 0; `{{` count 0; ledger still 21 lines; new
whole-file anchor recorded. The negative control was executed in this task and both cells are
transcribed: unperturbed → `13a49f54…`, one-byte-flipped → a different md5, `cmp -l` = 1 byte.
No control fixture is staged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Move the HANDOFF gate to CLEARED, refresh the three resume surfaces, and push branch + tag</name>

  <files>
.planning/HANDOFF.json                                            (python edit; must stay valid JSON)
.planning/STATE.md                                                (new top block + Last-activity line; NO Quick Tasks row)
.planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md      (new ★★ LATEST ★★ block + frontmatter)
  </files>

  <action>
**STEP A — pre-flight.** Re-assert the frozen paste block, guard exit 0, `git log --oneline -3`
(Task 1's record commit and Task 2's commit present), and `python3 -m json.tool .planning/HANDOFF.json > /dev/null; echo $?` = 0.

**STEP B — HANDOFF.json, edited by python (never by hand — it is 111 KB of one-line strings).**

B1. APPEND to `gates.osf_pre_registration` (do not rewrite what is there — the Check-2 override
history must survive verbatim), exactly:

```
 ⟶ 2026-08-22 02:58:55Z: recalibration amendment POSTED as NEW file mk7ze (https://osf.io/mk7ze) on az52u; trsx5 still 1 revision; OSF-stored md5 13a49f543cabcc27ce9f1e589783c060 == repo paste block; deviation entry appended; DEC-2026-08-22-occlusion-recalibration-posted; tag AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22. THIS OBLIGATION IS CLEARED. The separate Check-2 amendment-update obligation (2026-08-03 override) REMAINS OPEN and is unaffected.
```

B2. PREPEND a new string at `resume_on_reconnect[0]` (the existing 18 shift down; do not delete
any), in the established `> #0 (…)` style, beginning:

```
> #0 (2026-08-22 ~03:25 UTC — POSTED AND RECORDED, SUPERSEDES every earlier #0).
```

and carrying, in this order:
- **The four captures.** `mk7ze` / `https://osf.io/mk7ze` on `az52u`, its own single-version file
  on OSF Storage root (NOT under trsx5), stored name
  `osf-amendment-occlusion-gate-recalibration-2026-08-22.md`; authoritative UTC
  `2026-08-22T02:58:55Z` from Recent Activity (file page "Date created" read
  `2026-08-22T02:58:53Z` — the object, 2 s earlier; the template's parent-record-date expectation
  was NOT borne out); OSF-stored md5 `13a49f543cabcc27ce9f1e589783c060` / 22,945 B / v1, read from
  the API because the scripted re-download was refused (local re-download md5 = optional,
  non-blocking follow-up); `trsx5` still exactly 1 revision (`2026-07-10T13:32:21Z`).
- **Anchors.** Paste block 22,945 B / `13a49f543cabcc27ce9f1e589783c060` — **PUBLIC AND FROZEN,
  never changes again**; the reviewed body `422f1f28…` differs by exactly the `**Date:**` line
  (`4c4`); the amendment's NEW whole-file anchor from Task 2.
- **Record.** The Task-1 record commit 40-hex + tag
  `AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22`; `DEC-2026-08-22-occlusion-recalibration-posted`.
- **Precedence SATISFIED.** `c61d179` at 02:48:03Z < 02:58:55Z; constant still 0.0005 (last moved
  `d9fbc63`, 2026-08-13); `src/ tests/ config/` diff since `2689cae` empty; no recalibrated output.
- **NOTHING RUNNING / VM 20260626b STOPPED / $0 / zero OSF contact by any agent.**
- **NEXT = the remediation batch, as a `/gsd-quick --validate` TDD task, in this order:**
  (i) producer gate in `src/python/run_native_ld_panel.py` — `_OCCLUSION_ANOMALY_FRACTION`
  (line 133) + its comparison (~line 853) → the TWO-condition rule (occluded SITES grouped by
  `chr:pos` / total sites **> 0.5056%** **OR** `occ_rows/occ_sites` **> 3.42x** → DEFER; row
  accounting KEPT; site count, row count and inflation all emitted), constants in ONE pinned
  place, RED tests first;
  (ii) test oracle in `tests/m3/test_occlusion_span_filter.py` — the settled-5 as a SUBSET at
  exact indices, and the measured 231/7,951 pins labelled MEASURED-NOT-DERIVED;
  (iii) `src/python/fire_verifier.py` `expected_records` → detector-derived, plus the new gate
  semantics;
  (iv) runbook EXPECT retirement outside the pinned 6b ranges, with `260817-vbu-verify.sh`
  byte-identical;
  (v) suite re-baseline from 992/32/0, reconciled component-exact;
  THEN Stage A from STEP 7. **AN AGENT NEVER FIRES.**

B3. Set `timestamp` to `2026-08-22T03:25:00Z`. PREFIX `status` and `resume_entry_point` with the
posted-and-recorded headline (keep their existing text after the new prefix — these are the
fields a cold session reads first). Re-serialize with the file's existing indent style; the diff
must show the intended keys and the shifted `resume_on_reconnect` list, nothing else.

```
python3 -m json.tool .planning/HANDOFF.json > /dev/null; echo "JSON EXIT=$?"
python3 -c "import json;d=json.load(open('.planning/HANDOFF.json'));print(len(d['resume_on_reconnect']))"   # 19
```

**STEP C — STATE.md.**
- INSERT a new top block immediately ABOVE the current `## 2026-08-22 02:55 UTC …` block
  (line 28 at plan time), headed:
  `## 2026-08-22 03:25 UTC (21 Aug 23:25 EDT) — ✅ POSTED + RECORDED — **THE RECALIBRATION AMENDMENT IS ON THE OSF RECORD (mk7ze, 2026-08-22T02:58:55Z); THE m3-07 CLAUSE-(d) GATE IS CLEARED; THE TWO-CONDITION CODE CHANGE IS AUTHORISED AND NOT YET MADE. NOTHING RUNS; VM STOPPED.** (★ RESUME HERE — LATEST ★)`
  Body: what happened; the four captures; the two caveats (`4c4` date line; API-reported hash);
  precedence satisfied; the record commit + tag + DEC; the paste block is public and frozen; the
  remediation batch as NEXT; an agent never posts and never fires.
- DEMOTE the 02:55 block's marker `(★ RESUME HERE — LATEST ★)` →
  `(SUPERSEDED by the 2026-08-22 POSTED block above)`. Leave its body in place.
- At line 1443, PREFIX a new `Last activity:` line for this task and demote the current one to
  `Last activity (prior):`.
- ⛔ Do NOT add a `### Quick Tasks Completed` row — the orchestrator owns it.

**STEP D — `.planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md`.**
- PREPEND, immediately after the frontmatter `---`, a block headed
  `> **2026-08-22 03:25 UTC — POSTED + RECORDED (★★ LATEST ★★ — mk7ze on az52u; m3-07 clause-(d) gate CLEARED; two-condition code change AUTHORISED, not yet made):**`
  carrying the four captures, the anchors, the record commit + tag + DEC, precedence, and the
  RESUME = remediation batch → Stage A, ending with the standing prohibitions.
- DEMOTE the existing `★★ LATEST ★★` marker on the 02:55 block to
  `(SUPERSEDED by the 2026-08-22 POSTED + RECORDED block above)`.
- Frontmatter: `last_updated: 2026-08-22T03:25:00.000Z`; `status:` →
  `amendment_POSTED_mk7ze__2026-08-22T02-58-55Z__gate_CLEARED__two_condition_code_change_AUTHORISED_not_yet_made__fire_HELD`.

**STEP E — commit, then push BOTH the branch and the tag.** `git push` does NOT push tags — the
July record's tag needed the same explicit second push.

```
git add .planning/HANDOFF.json .planning/STATE.md .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md
git commit -m "docs(handoff): OSF gate CLEARED — recalibration amendment POSTED (mk7ze, 2026-08-22T02:58:55Z) and RECORDED; STATE + continue-here refreshed; NEXT = two-condition remediation batch, then Stage A"
git push
git push origin AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22
git status -sb | head -1
git ls-remote --tags origin | grep -c AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22
```

`git status -sb` must show NO `ahead`; the `ls-remote` count must be exactly 1.
  </action>

  <verify>
    <automated>
cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis
A=.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
python3 -m json.tool .planning/HANDOFF.json > /dev/null && echo "JSON VALID: OK"
python3 - <<'PY'
import json,sys
d=json.load(open('.planning/HANDOFF.json'))
g=d['gates']['osf_pre_registration']
assert 'THIS OBLIGATION IS CLEARED' in g, 'gate not cleared'
assert 'mk7ze' in g and 'AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22' in g
assert 'REMAINS OPEN' in g, 'Check-2 obligation must stay open'
r=d['resume_on_reconnect']; assert len(r)==19, len(r)
assert 'mk7ze' in r[0] and '2026-08-22' in r[0] and '13a49f543cabcc27ce9f1e589783c060' in r[0]
assert d['timestamp'].startswith('2026-08-22T03:25')
print('HANDOFF: OK')
PY
grep -c 'POSTED + RECORDED' .planning/STATE.md
test "$(grep -c '★ RESUME HERE — LATEST ★' .planning/STATE.md)" -eq 1 && echo "ONE RESUME MARKER: OK"
test "$(grep -c '★★ LATEST ★★' .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md)" -eq 1 && echo "ONE CONTINUE MARKER: OK"
grep -c 'mk7ze' .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md
test "$(awk '/^--- PASTE INTO OSF FROM HERE ---$/{f=1;next} /^--- PASTE ENDS HERE ---$/{f=0} f' "$A" | md5sum | cut -d' ' -f1)" = 13a49f543cabcc27ce9f1e589783c060 && echo "PASTE BLOCK FROZEN: OK"
sed -n '133p' src/python/run_native_ld_panel.py | grep -q '_OCCLUSION_ANOMALY_FRACTION = 0.0005' && echo "CONSTANT UNCHANGED: OK"
test "$(git diff --stat c61d179 HEAD -- src/ tests/ config/ | wc -l)" -eq 0 && echo "NO CODE TOUCHED: OK"
git status -sb | head -1 | grep -qv 'ahead' && echo "NOT AHEAD: OK"
test "$(git ls-remote --tags origin | grep -c AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22)" -eq 1 && echo "TAG ON ORIGIN: OK"
    </automated>
  </verify>

  <done>
`.planning/HANDOFF.json` is valid JSON with `timestamp` 2026-08-22T03:25:00Z, a
`gates.osf_pre_registration` that now ends with the posting sentence containing
`THIS OBLIGATION IS CLEARED` while explicitly leaving the Check-2 obligation OPEN, and a
19-element `resume_on_reconnect` whose `[0]` carries the four captures, the anchors, the record
commit + tag + DEC, precedence, the nothing-running state and the remediation-batch NEXT.
STATE.md has exactly one `★ RESUME HERE — LATEST ★` (the new POSTED + RECORDED block) and a new
`Last activity:` line, with no Quick Tasks row added. `.continue-here.md` has exactly one
`★★ LATEST ★★` block and refreshed frontmatter. Branch and tag are both on origin; `git status -sb`
shows no `ahead`; the paste block and the code constant are untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OSF (public/external) → repo record | The posted `mk7ze` body is now public and immutable-by-policy; the repo's paste block is the only in-repo object claiming to equal it |
| Human-reported captures → in-repo record | The four captures arrive as prose from Carter's browser session; no agent may verify them at the source (OSF contact is prohibited) |
| Repo record → future sessions / reviewers | `osf_deviations.md`, `DECISIONS.md` and `HANDOFF.json` are what a cold session and an external reviewer read as ground truth |
| Docs commit → code path | The task must not enable or imply the code-constant change, which is a separate, later, gated task |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-w68-01 | Tampering | amendment paste block (now public) | mitigate | awk-extracted md5 asserted == `13a49f543cabcc27ce9f1e589783c060` before AND after every task; all three edits are strictly below `--- PASTE ENDS HERE ---`; the instantiation engine is prohibited outright |
| T-w68-02 | Tampering | `.planning/osf_deviations.md` (silent rewrite of prior record) | mitigate | pure-append proven three ways: `head -420 \| md5sum` == `dd3806312977513a8727463ec3a032df`, `git diff --numstat` deletions == 0, line-count delta == appended chunk lines |
| T-w68-03 | Repudiation | posted-vs-reviewed body divergence | mitigate | the `4c4` `**Date:**`-line difference between `422f1f28…` and `13a49f54…` is disclosed in the entry, the DEC and `resume_on_reconnect[0]` — not merely in a commit message |
| T-w68-04 | Information disclosure (false assurance) | integrity hash provenance | mitigate | the entry records that the hash is OSF's API-reported md5, that the scripted re-download was REFUSED, and that a local re-download md5 was NOT performed (optional, non-blocking) |
| T-w68-05 | Spoofing | template prediction vs observed "Date created" | mitigate | the refuted sentence is REPLACED, the observed `2026-08-22T02:58:53Z` recorded, and the failed prediction itself written down rather than deleted |
| T-w68-06 | Elevation of privilege | docs task silently authorising the code change | mitigate | zero edits under `src/ tests/ config/`, asserted by `git diff --stat c61d179 HEAD` == empty in all three tasks; the DEC and checklist state the TWO-condition rule and that ceiling-alone is NOT authorised |
| T-w68-07 | Denial of service | GPFS multi-terminal staging collision | mitigate | explicit-path `git add` only, three atomic commits, `git status --porcelain` inspected before each; ~15 untracked dirs must remain untracked |
| T-w68-08 | Repudiation | tag exists locally but never reaches origin | mitigate | `git push` does not push tags; an explicit `git push origin <tag>` plus a `git ls-remote --tags` count == 1 assertion |
| T-w68-09 | Tampering | negative-control fixture leaking into the tree | mitigate | all fixtures under `<SCR>/w68-nc/`, never created in-repo, never staged; `git status --porcelain` checked before each commit |
| T-w68-10 | Information disclosure | any agent contacting OSF | accept→prohibit | absolute prohibition in `<constraints>`; the captures in `<interfaces>` are the sole OSF-side source; an agent never posts |
</threat_model>

<verification>
Run from the repo root after Task 3:

```
A=.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
D=.planning/osf_deviations.md
LAST=$(awk '/^## /{n=NR} END{print n}' "$D")

# 1 — the record is complete and filled
for s in mk7ze https://osf.io/mk7ze 2026-08-22T02:58:55Z 13a49f543cabcc27ce9f1e589783c060 \
         07df11e44f2d56536ef4ef0753c8d2f8fdb55ae8 2026-07-10T13:32:21Z ; do
  echo "$s -> $(tail -n +$LAST "$D" | grep -c -F -- "$s")"; done
tail -n +$LAST "$D" | grep -c 'TO BE FILLED AT POSTING'          # 0
head -420 "$D" | md5sum                                          # dd3806312977513a8727463ec3a032df

# 2 — tag, locally and on origin, on a single-path commit
git cat-file -t AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22          # commit
git show --name-only --format= AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22
git ls-remote --tags origin | grep -c AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22   # 1

# 3 — the public body is frozen
awk '/^--- PASTE INTO OSF FROM HERE ---$/{f=1;next} /^--- PASTE ENDS HERE ---$/{f=0} f' "$A" | wc -c    # 22945
awk '/^--- PASTE INTO OSF FROM HERE ---$/{f=1;next} /^--- PASTE ENDS HERE ---$/{f=0} f' "$A" | md5sum   # 13a49f54…
bash .planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh all "$A"; echo "GUARD=$?"   # 0
grep -c '{{' "$A"; grep -cE '^  [A-Z0-9_]+ = ' "$A"              # 0 ; 21

# 4 — the decision is banked
grep -c 'DEC-2026-08-22-occlusion-recalibration-posted' .planning/DECISIONS.md  # >= 1

# 5 — no code moved
sed -n '133p' src/python/run_native_ld_panel.py                  # _OCCLUSION_ANOMALY_FRACTION = 0.0005
git diff --stat c61d179 HEAD -- src/ tests/ config/ | wc -l      # 0

# 6 — resume surfaces
python3 -m json.tool .planning/HANDOFF.json > /dev/null; echo "JSON=$?"
python3 -c "import json;d=json.load(open('.planning/HANDOFF.json'));g=d['gates']['osf_pre_registration'];print('CLEARED' in g and 'THIS OBLIGATION IS CLEARED' in g, 'mk7ze' in d['resume_on_reconnect'][0])"

# 7 — published
git status -sb | head -1                                         # no 'ahead'

# 8 — the posted July record is untouched
git diff --stat c61d179 HEAD -- .planning/amendments/osf-amendment-afr-occlusion-exclude-UPDATE-2026-07-10.md | wc -l   # 0
```
</verification>

<success_criteria>
- Exactly three commits, in order, with the given messages, each staged by explicit file paths.
- `.planning/osf_deviations.md` ends with a filled `## 2026-08-22 —` entry: six markers filled, the
  refuted "Date created" sentence replaced by the observed facts, and a
  `### Recorded at posting (2026-08-22)` sub-section with all five bullets (posting-date
  disclosure incl. `4c4`; integrity incl. the refused-re-download caveat; append-only incl. trsx5
  = 1 revision and mk7ze on Storage root; precedence SATISFIED; record incl. DEC, tag, seven
  debug records, posting card). Zero unfilled markers. Pure append, proven three ways.
- Plain tag `AFR-OCCLUSION-GATE-RECALIBRATION-OSF-POSTED-2026-08-22` on the single-path record
  commit, present locally and exactly once on origin.
- The amendment's paste block is 22,945 B / `13a49f543cabcc27ce9f1e589783c060` at every commit;
  guard `all` exit 0; `{{` count 0; ledger 21 lines; the new whole-file anchor recorded in the
  SUMMARY.
- The paste-block invariant was SEEN RED in this task: a one-byte flip inside a scratchpad copy
  moves the awk md5, with both cells transcribed.
- `DEC-2026-08-22-occlusion-recalibration-posted` in `.planning/DECISIONS.md`, with the three
  rejected alternatives and the TWO-condition authorisation (`> 0.5056%` OR `> 3.42x`, ROWS
  accounting, both numbers emitted, ceiling-alone NOT authorised, Stage A HELD).
- `.planning/HANDOFF.json` valid, `timestamp` 2026-08-22T03:25:00Z, gate ends with
  `THIS OBLIGATION IS CLEARED` while the Check-2 obligation stays OPEN, `resume_on_reconnect`
  has 19 entries and `[0]` names mk7ze, the anchors, the tag and the remediation NEXT.
- STATE.md has one `★ RESUME HERE — LATEST ★` (the new POSTED + RECORDED block), a new
  `Last activity:` line, and NO Quick Tasks row. `.continue-here.md` has one `★★ LATEST ★★` block
  and refreshed frontmatter.
- Branch and tag pushed; `git status -sb` shows no `ahead`.
- Zero OSF contact, zero AoU/VM action, `$0`, no edit under `src/ tests/ config/`, and the posted
  July amendment byte-unchanged.
</success_criteria>

<output>
After completion, create
`.planning/quick/260821-w68-record-the-osf-posting-of-the-occlusion-/260821-w68-SUMMARY.md`.

It MUST carry, verbatim where the plan says verbatim:
1. The three commit hashes + the tag hash, and the confirmation the tag is on origin.
2. The full appended deviation entry's key lines (heading, Posted bullet, timestamp bullet, the
   five posting-time bullets) — so the record is reviewable without opening the file.
3. The append proof: pre/post line counts, the appended-chunk line count, the
   `head -420 | md5sum`, and the `git diff --numstat` deletions column.
4. The template-fidelity count (`N/N template lines carried verbatim`) and the deliberate misses.
5. The frozen-block assertion transcript at EACH commit (three `22945 / 13a49f54…` lines).
6. The negative-control 2-cell matrix: the command, cell 1 (`13a49f54…`), cell 2 (different md5),
   and `cmp -l | wc -l` == 1.
7. The amendment's NEW whole-file anchor (bytes / md5 / lines) and the guard exit 0.
8. The `git diff --stat c61d179 HEAD -- src/ tests/ config/` empty result and the line-133 grep.
9. An explicit statement that no agent contacted OSF, nothing was fired, and the code-constant
   change remains NOT MADE and is the next, separate task.
</output>
