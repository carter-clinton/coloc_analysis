---
phase: 260816-w8h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/DECISIONS.md
  - .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md
  - .claude/skills/aou-ld-pipeline/SKILL.md
autonomous: true
requirements: [QUICK-260816-W8H]

must_haves:
  truths:
    - "The AoU determination on ticket 57144 is on the record as the VENDOR'S POSITION, with our reading recorded separately and clearly labelled as ours."
    - "The one part we accept (their verbatim mt.checkpoint() sentence) is marked as independent corroboration of our 2026-05-21 forensics; the rest is marked UNACCEPTED."
    - "Their repro-example offer is recorded as DECLINED-BY-LAPSE with the reason, so the decline is not silently forgotten."
    - "An operator reading GATE 1 in the runbook learns there is no credit backstop and that Stage B's measured extrapolation carries the Stage-C go/no-go."
    - "The aou-ld-pipeline skill's _SUCCESS invariant can cite vendor confirmation, not only our forensics."
    - "The fire surface is byte-unregressed: the guk fire section is still 10/10 and the 6b card block is byte-identical."
  artifacts:
    - path: ".planning/DECISIONS.md"
      provides: "DEC-2026-08-16-aou-credit-request-denied entry"
      contains: "DEC-2026-08-16-aou-credit-request-denied"
    - path: ".planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md"
      provides: "NO CREDIT BACKSTOP note inside item 6"
      contains: "NO CREDIT BACKSTOP"
    - path: ".claude/skills/aou-ld-pipeline/SKILL.md"
      provides: "vendor-confirmed annotation on invariant 1"
      contains: "ticket 57144"
  key_links:
    - from: "260812-ox1-READY-TO-FIRE.md item 6"
      to: ".planning/DECISIONS.md"
      via: "DEC id citation"
      pattern: "DEC-2026-08-16-aou-credit-request-denied"
    - from: ".claude/skills/aou-ld-pipeline/SKILL.md invariant 1"
      to: ".planning/DECISIONS.md"
      via: "DEC id citation"
      pattern: "DEC-2026-08-16-aou-credit-request-denied"
---

<objective>
Record the All of Us support determination on our compute-credit request (ticket
57144, Kyera Actkins, 2026-07-24 16:35 CDT, relayed by Carter 2026-08-16) and
propagate its two operational consequences.

The determination is **no credits or refunds** for compute charges from user-run
analyses — final — which covers the 2026-05-21 empty-MT catastrophe. Buried in
the denial is a sentence that **independently corroborates the root cause we
reached forensically** and hardened into two standing project rules.

Purpose: get the vendor's position on the record WITHOUT letting it become the
accepted account of the catastrophe; upgrade our `_SUCCESS` rule from "our
forensics" to "our forensics, confirmed by the platform team"; and tell the
operator standing at GATE 1 that there is no money backstop behind that gate.

Output: three small doc edits, one atomic commit each.

⛔ **Docs-only. $0. No network, no OSF, no perimeter contact, nothing fired.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md
@.claude/skills/aou-ld-pipeline/SKILL.md

⚠ **`.planning/STATE.md` is 836 KB — do NOT read it and do NOT touch it.** The
orchestrator owns it; `/close-session` runs immediately after this task.

⚠ `.planning/DECISIONS.md` is 1,949 lines. Read the **tail only**
(`sed -n '1830,1949p'`) to match entry conventions. Task 1 is a **pure append**.
</context>

<source_material>
**AoU support reply — ticket 57144, Kyera Actkins (AoU), 2026-07-24 16:35 CDT.**
Transcribe quoted text exactly. Do not paraphrase inside quotation marks.

**Their determination and reasoning:**
1. **No credits or refunds** for compute charges associated with user-run
   analyses. Final. (Applies to the 2026-05-21 empty-MT catastrophe.)
2. ★ **Verbatim — the load-bearing sentence:** *"Hail's mt.checkpoint() will
   still write a dataset and produce _SUCCESS markers even if the underlying
   MatrixTable being checkpointed is empty."* They recommend verifying the MT
   contained data prior to checkpoint (e.g. `mt.count()` before the write).
3. They flag the job configuration/scale: a large job on relatively few workers
   plus a "highly customized Spark configuration (spark.executor.cores=1,
   spark.executor.memory=5g)" can affect execution and "introduce unintended
   side effects that are difficult to diagnose or reproduce."
4. They note the paused/resumed environment may mean the job did not complete as
   expected, or that logs were lost; without a complete log or reproducible run
   they cannot isolate a root cause.
5. Forward recommendation: *"validate workflows on smaller subsets of data and
   with default configurations before scaling up to long-running, high-cost
   jobs."*
6. They offer to review a smaller reproducible example (notebook/script), and
   point to the Hail community (discuss.hail.is / hail.zulipchat.com).

**OUR READING — record AS OURS, clearly separated from their position:**
1. ★ Their verbatim `checkpoint()` sentence **independently corroborates the
   root cause we reached forensically on 2026-05-21** and hardened into two
   standing rules (`_SUCCESS` is not evidence of data; Hail writes `_SUCCESS`
   on driver-side task accounting, not contents validation). This upgrades those
   rules from "our forensics" to "our forensics, confirmed by the platform team."
2. ⚠ **Their two explanations are in tension.** If `checkpoint()` stamps
   `_SUCCESS` over empty contents **by design** — their words — then the
   pause/resume + lost-logs story is **superfluous** to explaining what we saw.
3. ⚠ The custom-Spark-config critique is **soft**: `hl.init(spark_conf=...)` is
   **silently overridden on YARN**, which is precisely why the
   `PYSPARK_SUBMIT_ARGS` route existed. That was a documented workaround for real
   platform behavior, not gratuitous tuning. (Now moot — the Hail producer is dead.)
4. **Do NOT record their framing as the accepted account of the catastrophe.**
   Record it as the vendor's position, set beside ours, with the corroborated
   part called out as corroborated and the rest as unaccepted.
5. Their forward recommendation is **already implemented**: the staged ramp
   (Stage A region-1 → Stage B 4-region de-risk incl. the deliberate worst case
   `m2_region_00071` → measured cost gate → Stage C full 276). Independent
   corroboration of a design Carter asked for.
6. Their repro-example offer is **DECLINED-BY-LAPSE**: it targets the **killed
   Hail producer**, superseded by the native-plink path. Building a repro for a
   code path we will never run again spends real time off the critical path.
   Record the decline and the reason so it is not silently forgotten.
</source_material>

<baseline>
**Captured 2026-08-16 during planning, on a clean tree. These are the numbers
"unregressed" is compared against — but the executor MUST re-capture them in
Task 2 rather than trust these.**

- `bash .planning/quick/260814-guk-seth-update-2-reply-remediation-fix-inva/260814-guk-verify.sh fire`
  → **`RESULT: ALL CHECKS PASSED (section: fire)`**, F1–F10 all PASS (10/10).
- 6b card block (script-identical extraction), md5 = **`a8d18f664fbfd0d7e281ee05f3accd4c`**, **60 lines**.
- All three target files clean in `git status --porcelain`.

★ **The Task 2 edit below was DRY-RUN PROVEN during planning** — applied to the
real file, verified, then reverted with `git checkout --` (tree confirmed clean,
card md5 back to baseline, fire back to 10/10). The proven result was:
`git diff --numstat` = **`5	0`**, a single hunk **`@@ -110,0 +111,5 @@`**, 6b card
md5 **unchanged** at `a8d18f664fbfd0d7e281ee05f3accd4c`, guk fire **10/10**.
So the note text is known-safe; **any deviation from those four numbers means the
executor's edit differs from the proven one** — re-read STEP B before proceeding.
</baseline>

<tasks>

<task type="auto">
  <name>Task 1: Append DEC-2026-08-16-aou-credit-request-denied to DECISIONS.md</name>
  <files>.planning/DECISIONS.md</files>
  <action>
Read the tail first to match house style: `sed -n '1830,1949p' .planning/DECISIONS.md`.
Entries are `## YYYY-MM-DD — DEC-<slug>: <one-line headline>` followed by bolded
lead-in paragraphs and a closing `**Cross-refs:**` block, wrapped at ~80 cols.
The file currently ends newline-terminated at line 1949.

**PURE APPEND.** Do not edit, reflow, reorder, or re-wrap a single existing line.
Append the entry below to the end of the file (one blank line separating it from
the current last line).

The two headed sections — **THEIR POSITION** and **OUR READING** — must stay
visibly separated; a later reader must never be able to mistake the vendor's
framing for our accepted account.

Append exactly:

```markdown
## 2026-08-16 — DEC-2026-08-16-aou-credit-request-denied: AoU DENIES the compute-credit request (final) — and in denying it, CONFIRMS our `_SUCCESS` root cause

**Source:** All of Us support ticket **57144**, Kyera Actkins (AoU), **2026-07-24
16:35 CDT**; relayed by Carter 2026-08-16. Covers the **2026-05-21 empty-MT
catastrophe** (~$2,100 of compute against a `_SUCCESS`-stamped, 0-byte MT).

**Outcome: DENIED, final.** No credits or refunds are issued for compute charges
associated with user-run analyses. There is no further appeal path and none will
be pursued. **The money is spent.**

### THEIR POSITION (the vendor's account — recorded, not adopted)

1. ★ **Verbatim, and this is the load-bearing sentence:** *"Hail's mt.checkpoint()
   will still write a dataset and produce _SUCCESS markers even if the underlying
   MatrixTable being checkpointed is empty."* They recommend verifying the MT
   contained data prior to checkpoint (e.g. `mt.count()` before the write).
2. **Job configuration / scale.** A large job on relatively few workers plus a
   "highly customized Spark configuration (spark.executor.cores=1,
   spark.executor.memory=5g)" can affect execution and "introduce unintended side
   effects that are difficult to diagnose or reproduce."
3. **Paused/resumed environment.** The job may not have completed as expected, or
   logs may have been lost; without a complete log or a reproducible run they
   cannot isolate a root cause.
4. **Forward recommendation:** *"validate workflows on smaller subsets of data and
   with default configurations before scaling up to long-running, high-cost jobs."*
5. **Offer:** they will review a smaller reproducible example (notebook/script),
   and point to the Hail community (discuss.hail.is / hail.zulipchat.com).

### OUR READING (ours, not theirs — what we accept and what we do not)

1. ★ **ACCEPTED, and it is the valuable part.** Their (1) **independently
   corroborates the root cause we reached forensically on 2026-05-21**, before we
   ever filed this ticket. Two standing project rules already encode it —
   `[[feedback_aou_success_marker_not_evidence_of_data]]` (`_SUCCESS` is NOT
   evidence of data) and `[[feedback_hail_checkpoint_contract_violation]]` (Hail
   writes `_SUCCESS` on driver-side task accounting, not contents validation).
   Those rules are hereby upgraded from **"our forensics"** to **"our forensics,
   confirmed by the platform team."** Recorded in the `aou-ld-pipeline` skill so
   the operational invariant can cite vendor confirmation.
2. ⚠ **UNACCEPTED — their two explanations are in tension.** If `checkpoint()`
   stamps `_SUCCESS` over empty contents **by design** — their own words — then
   the pause/resume + lost-logs story in their (3) is **superfluous** to explaining
   what we observed. Their (1) already fully accounts for it.
3. ⚠ **UNACCEPTED — the custom-Spark-config critique is soft.**
   `hl.init(spark_conf=...)` is **silently overridden on YARN**
   (`[[feedback_aou_dataproc_pyspark_submit_args]]`), which is precisely why the
   `PYSPARK_SUBMIT_ARGS` route existed. That was a documented workaround for real
   platform behavior, not gratuitous tuning. Now moot — the Hail producer is dead,
   superseded by the native-plink path.
4. **Their framing is NOT the accepted account of the catastrophe.** It is recorded
   here as the vendor's position, set beside ours, with the corroborated part called
   out as corroborated and the remainder as unaccepted.
5. **Their forward recommendation (4) is ALREADY IMPLEMENTED.** The staged ramp —
   Stage A region-1 → Stage B 4-region de-risk (including the deliberate worst case
   `m2_region_00071`) → measured cost gate → Stage C full 276 — is exactly
   "validate on smaller subsets before scaling to long-running, high-cost jobs."
   Independent corroboration of a design Carter asked for.

**Their repro-example offer: DECLINED-BY-LAPSE.** The offer targets the **killed
Hail producer**, superseded by the native-plink path. Building a reproducible
example for a code path we will never run again spends real time off the critical
path for no recoverable benefit (the refund is final either way). Recorded here so
the decline is a decision and not a silent omission. If the Hail producer is ever
revived, this offer is the first thing to re-open.

**Operational consequence — carried into the fire runbook.** With refunds denied,
there is **no credit backstop** behind GATE 1. Stage B's **measured** cost
extrapolation is what must carry the Stage-C go/no-go — an overrun is
unrecoverable, not reimbursable. Noted at item 6 of
`260812-ox1-READY-TO-FIRE.md`.

**Cross-refs:** `[[feedback_aou_success_marker_not_evidence_of_data]]`;
`[[feedback_hail_checkpoint_contract_violation]]`;
`[[feedback_aou_dataproc_pyspark_submit_args]]`;
`.claude/skills/aou-ld-pipeline/SKILL.md` (invariant 1, vendor-confirmed line);
`260812-ox1-READY-TO-FIRE.md` §6 (GATE 1).
```
  </action>
  <verify>
    <automated>
# 1 — PURE APPEND: zero deleted lines against HEAD (this is the hard gate)
git diff --numstat -- .planning/DECISIONS.md
#    expected: "<N>	0	.planning/DECISIONS.md"  — the deletions column MUST be 0

# 2 — the entry exists exactly once
grep -c 'DEC-2026-08-16-aou-credit-request-denied' .planning/DECISIONS.md   # >= 1
grep -c '^## 2026-08-16 — DEC-2026-08-16-aou-credit-request-denied' .planning/DECISIONS.md  # == 1

# 3 — the verbatim sentence is present and unparaphrased
grep -cF 'will still write a dataset and produce _SUCCESS markers even if the' .planning/DECISIONS.md  # == 1

# 4 — both headed sections present, and the decline is recorded
grep -c '^### THEIR POSITION' .planning/DECISIONS.md   # == 1
grep -c '^### OUR READING'    .planning/DECISIONS.md   # == 1
grep -c 'DECLINED-BY-LAPSE'   .planning/DECISIONS.md   # == 1
    </automated>
  </verify>
  <done>
`git diff --numstat` reports **0 deletions** on DECISIONS.md; the entry heading,
both `### THEIR POSITION` / `### OUR READING` sections, the verbatim
`checkpoint()` sentence and `DECLINED-BY-LAPSE` are each present exactly once.
Committed atomically with explicit-path staging.
  </done>
</task>

<task type="auto">
  <name>Task 2: ⚠ THE RISKY ONE — one-line NO CREDIT BACKSTOP note in runbook item 6</name>
  <files>.planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md</files>
  <action>
⛔ **This file is the FIRE SURFACE.** It is guarded by ten checks (F1–F10) in the
guk verifier. **Touch item 6 and NOTHING else.**

**STEP A — capture the baseline BEFORE any edit** (so "unregressed" is a
comparison, not a hope):

```bash
RF=.planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md
GUK=.planning/quick/260814-guk-seth-update-2-reply-remediation-fix-inva/260814-guk-verify.sh

# A1 — the 6b card block md5, script-identical extraction (awk mirrors the
#      verifier's own block() helper: start-inclusive, end-exclusive)
awk -v s='^## 6b' -v e='^## 7[.]' \
  'BEGIN{p=0} (p && $0 ~ e){exit} ($0 ~ s){p=1} p{print}' "$RF" | md5sum   # record BEFORE
awk -v s='^## 6b' -v e='^## 7[.]' \
  'BEGIN{p=0} (p && $0 ~ e){exit} ($0 ~ s){p=1} p{print}' "$RF" | wc -l    # record BEFORE

# A2 — the guk fire section baseline
bash "$GUK" fire
```

Expected BEFORE (from planning, on a clean tree — re-capture, do not assume):
6b card md5 = `a8d18f664fbfd0d7e281ee05f3accd4c`, 60 lines; guk fire =
`RESULT: ALL CHECKS PASSED (section: fire)` with F1–F10 all PASS.
**If the BEFORE baseline is not already 10/10, STOP and report — do not edit.**

**STEP B — the edit.** Item 6 currently occupies lines 105–109:

```
105  ## 6. GATE 1 — cost/credit eyeball
106  (blank)
107  Read the **live balance in the Workbench billing panel** before the $385–1,084 commit.
108  (The CDR-pin half needs NO re-verification: `_resolve_aux_base` makes v8→v9 a path
109  no-op.)
110  (blank)
111  ## 6b. THE trsx5 BYTE CHECK — GATES THE FIRE (added 2026-08-14, Seth escalation)
```

Insert **after line 109 (`no-op.)`)**: one blank line, then the note below.
Line 110's existing blank line stays, so `## 6b` still has a blank line above it.
The note is a **single logical note**, soft-wrapped to the file's ~86-col
convention. Insert exactly:

```markdown
⚠ **NO CREDIT BACKSTOP behind this gate** (`DEC-2026-08-16-aou-credit-request-denied`):
AoU support has ruled that compute charges from user-run analyses are **not
refundable** — final. An overrun past this gate is unrecoverable, not reimbursable,
so **Stage B's measured extrapolation is what must carry the Stage-C go/no-go.**
```

★ **This exact insertion was dry-run proven during planning** (see `<baseline>`):
it yields `5	0` / a single `@@ -110,0 +111,5 @@` hunk / an unchanged card md5 /
fire 10/10. Reproduce it byte-for-byte rather than re-wording it.

⛔ **Content constraints — these are derived from the live F-checks, not style
preferences. Violating any one turns a green fire section red:**
- The note must sit **strictly ABOVE `## 6b`** — this keeps the 6b card block
  byte-identical, which is what protects F1 and F4.
- Must **NOT** contain `nothing is lost` or `nothing scientific is lost` (F10).
- Must **NOT** contain `c19e8b2` or the 31-char literal (F5).
- Must **NOT** contain any hex run of 20+ chars (F1).
- Must **NOT** begin any line with `## ` — no new heading (F7 numbering).
- Must **NOT** begin any line with `- **C —` / `- **D —` (F9 block delimiters)
  or `**Deferral vocabulary` (F8 block delimiter).

**STEP C — verify, then decide.** Run the `<verify>` block. Then:

> **If the fire section regresses for any reason, REVERT the runbook edit
> (`git checkout --`) and report** — the note is explicitly NOT worth a
> fire-surface regression.

That is unconditional: it applies to a single failed F-check, a changed 6b card
md5, an unexpected `--numstat` shape, or anything else that is not exactly
10/10 + identical card md5 + a small addition confined to item 6. Do not "fix
forward" on the fire surface. Revert, report, stop. Tasks 1 and 3 stand
independently and are unaffected by reverting this one.
  </action>
  <verify>
    <automated>
RF=.planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md
GUK=.planning/quick/260814-guk-seth-update-2-reply-remediation-fix-inva/260814-guk-verify.sh

# (i) 6b CARD BLOCK BYTE-IDENTICAL — AFTER md5 must equal the STEP-A BEFORE md5
awk -v s='^## 6b' -v e='^## 7[.]' \
  'BEGIN{p=0} (p && $0 ~ e){exit} ($0 ~ s){p=1} p{print}' "$RF" | md5sum
#     MUST equal the BEFORE value (a8d18f664fbfd0d7e281ee05f3accd4c on a clean tree)

# (ii) SMALL ADDITION CONFINED TO ITEM 6
git diff --numstat -- "$RF"        # expect "5	0	<path>" — deletions column MUST be 0
git diff -U0 -- "$RF"              # MUST be a SINGLE hunk: @@ -110,0 +111,5 @@ (item 6 only)
grep -n '^## ' "$RF" | head -14    # heading list + line numbers; 6 -> 6b -> 7 -> 8 -> 9 -> 10 -> 11, no renumbering
grep -c 'NO CREDIT BACKSTOP' "$RF" # == 1

# (iii) THE FIRE SECTION — REQUIRE 10/10
bash "$GUK" fire
#     MUST print: RESULT: ALL CHECKS PASSED (section: fire)   with F1-F10 all PASS

# NEGATIVE-CONTROL SPOT CHECKS (cheap, and the F-checks this edit could plausibly break)
grep -ci 'nothing is lost' "$RF"   # == 0  (F10)
grep -cF 'c19e8b2' "$RF"           # == 0  (F5)
    </automated>
  </verify>
  <done>
The 6b card block md5 is **identical** BEFORE and AFTER; `git diff --numstat`
shows a small addition with **0 deletions**, every hunk confined to item 6; the
guk fire section prints **`ALL CHECKS PASSED`** with **F1–F10 all PASS (10/10)**;
`NO CREDIT BACKSTOP` appears exactly once. Committed atomically with
explicit-path staging.

**OR** — the edit was reverted with `git checkout --`, the runbook is unmodified,
the regression is reported verbatim, and no commit was made for this task.
Either outcome is a valid completion; a red fire section that was committed is not.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record vendor confirmation on the aou-ld-pipeline skill's _SUCCESS invariant</name>
  <files>.claude/skills/aou-ld-pipeline/SKILL.md</files>
  <action>
The skill's **invariant 1** (line 19, under `## The invariants (these are the
mistakes — do not repeat them)`) currently rests on our forensics alone:

> 1. **`_SUCCESS` is NOT evidence of data.** Hail writes `_SUCCESS` on
>    driver-side task accounting, not contents. … The 2026-05-21 empty-MT
>    catastrophe ($2,100) and the 2026-06-10 empty-final catastrophe both passed
>    a `_SUCCESS` check over 0 bytes.

**ADDITIVE ONLY.** Do not modify line 19 itself, do not touch the YAML
frontmatter (including the `description` field on line 3), and do not renumber
invariants 2–5. Insert the sub-bullet below **immediately after line 19**, as an
indented continuation of item 1:

```markdown
   - ★ **Platform-team CONFIRMED 2026-07-24** (AoU support ticket 57144, K. Actkins),
     verbatim: *"Hail's mt.checkpoint() will still write a dataset and produce
     _SUCCESS markers even if the underlying MatrixTable being checkpointed is
     empty."* Their own recommendation matches ours — verify the MT held data
     **before** the write (`mt.count()`). This invariant is now vendor-confirmed,
     not only our 2026-05-21 forensics. (`DEC-2026-08-16-aou-credit-request-denied`)
```

Why this matters operationally: an operator who wants to skip the `du` +
`count_cols`/`count_rows` verification can no longer argue it is our
over-caution. It is documented platform behavior, stated by the platform team.
  </action>
  <verify>
    <automated>
SK=.claude/skills/aou-ld-pipeline/SKILL.md

# (i) ADDITIVE ONLY — 0 deletions
git diff --numstat -- "$SK"        # expect "6	0	<path>" — deletions column MUST be 0

# (ii) the annotation landed, exactly once, under invariant 1
grep -c 'ticket 57144' "$SK"                                # == 1
grep -cF 'Platform-team CONFIRMED 2026-07-24' "$SK"         # == 1
grep -cF 'DEC-2026-08-16-aou-credit-request-denied' "$SK"   # == 1
grep -n '^1\. \*\*`_SUCCESS`' "$SK"                         # still line 19
grep -n '^   - ★ \*\*Platform-team CONFIRMED' "$SK"         # line 20 (immediately after)

# (iii) invariants 2-5 NOT renumbered and NOT touched
grep -n '^[1-5]\. ' "$SK"          # exactly 5 items, numbered 1..5 in order
git diff -U0 -- "$SK"              # single hunk, @@ -19,0 +20,N @@

# (iv) frontmatter untouched
git diff -U0 -- "$SK" | grep -c '^-'   # only the '--- a/...' line; no '-' content lines

# (v) closeout net — nothing leaked onto the fire surface
bash .planning/quick/260814-guk-seth-update-2-reply-remediation-fix-inva/260814-guk-verify.sh fire
#     MUST still print: RESULT: ALL CHECKS PASSED (section: fire)
    </automated>
  </verify>
  <done>
`git diff --numstat` shows **0 deletions** on SKILL.md; the vendor-confirmed
sub-bullet sits immediately after invariant 1 with the verbatim sentence and the
DEC id; invariants 1–5 are still numbered 1–5 in order; the YAML frontmatter is
byte-unchanged; the guk fire section is still **10/10**. Committed atomically
with explicit-path staging.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| externally-authored text → repo record | Vendor prose (ticket 57144) enters the permanent record; the risk is that their framing is later read as our accepted account. |
| doc edit → frozen fire surface | `260812-ox1-READY-TO-FIRE.md` gates a $385–1,084 irreversible compute fire and is guarded by F1–F10. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-w8h-01 | Tampering | `260812-ox1-READY-TO-FIRE.md` fire surface | mitigate | 6b card block md5 asserted identical BEFORE/AFTER; `git diff -U0` hunk confined to item 6; guk `fire` required 10/10; unconditional `git checkout --` revert on any regression. |
| T-w8h-02 | Repudiation | `.planning/DECISIONS.md` | mitigate | Pure append enforced by `git diff --numstat` deletions == 0; existing entries provably unrewritten. |
| T-w8h-03 | Spoofing (attribution) | vendor text in DECISIONS.md / SKILL.md | mitigate | `### THEIR POSITION` and `### OUR READING` are separate headed sections; quoted text transcribed verbatim; unaccepted items explicitly labelled UNACCEPTED. |
| T-w8h-04 | Information disclosure | all three files | accept | Docs-only, no secrets, no PII beyond a support agent's already-public name on a ticket we filed. |
| T-w8h-05 | Denial of service | git object store (GPFS) | transfer | Known GPFS "invalid object / Error building trees" failure mode — STOP and report verbatim; recovery is the documented `[[reference_gpfs_git_object_store_loss]]` recipe, out of scope here. |
| T-w8h-06 | Elevation of privilege | fire trigger | accept | Nothing in this plan fires, authorizes, or unblocks anything; the note only tightens the gate's framing. |
</threat_model>

<verification>
**Cross-task, run once at the end:**

```bash
# every file this plan touched, and nothing else
git status --porcelain

# the fire surface is unregressed (the load-bearing check)
bash .planning/quick/260814-guk-seth-update-2-reply-remediation-fix-inva/260814-guk-verify.sh fire

# STATE.md and ROADMAP.md were NOT touched (orchestrator owns them)
git status --porcelain .planning/STATE.md .planning/ROADMAP.md   # MUST be empty

# three atomic commits, one per task
git log --oneline -3
```

**Guardrails, restated:**
- Docs-only. **$0.** No network, no OSF, no perimeter contact, nothing fired.
- **Explicit-path `git add` ONLY** — never `git add .` or `git add -A` on this
  GPFS shared tree (`[[feedback_multi_terminal_staging]]`).
- **Do NOT touch `.planning/STATE.md`** (836 KB, orchestrator-owned) **or
  `.planning/ROADMAP.md`**.
- **Do NOT push** — the orchestrator pushes.
- GPFS `invalid object` / `Error building trees` on commit → **STOP and report
  the error verbatim.** Do not retry blindly.
</verification>

<success_criteria>
- [ ] `DEC-2026-08-16-aou-credit-request-denied` appended to DECISIONS.md with
      **0 deleted lines**, carrying their determination, the verbatim
      `checkpoint()` sentence, and our reading in a separately headed section.
- [ ] The corroborated part is marked corroborated; items 2 and 3 of our reading
      are marked **UNACCEPTED**; their framing is not the accepted account.
- [ ] The repro-example offer is recorded **DECLINED-BY-LAPSE** with its reason.
- [ ] Runbook item 6 carries the one-line **NO CREDIT BACKSTOP** note citing the
      DEC id and naming Stage B's measured extrapolation as the Stage-C carrier —
      **OR** the edit was cleanly reverted and reported.
- [ ] 6b card block md5 **identical** BEFORE/AFTER; guk `fire` **10/10**.
- [ ] SKILL.md invariant 1 cites **platform-team CONFIRMED 2026-07-24 (ticket
      57144)** with the verbatim sentence; additive only, invariants 1–5 intact.
- [ ] Three atomic commits, explicit-path staging, nothing pushed.
- [ ] STATE.md and ROADMAP.md untouched.
</success_criteria>

<output>
After completion, create
`.planning/quick/260816-w8h-record-aou-support-credit-denial-vendor-/260816-w8h-SUMMARY.md`.

Report in the summary: the BEFORE and AFTER 6b card md5 values, the guk `fire`
result, and — if Task 2 was reverted — the exact regression observed, verbatim.
</output>
</content>
</invoke>
