# Deferred — `260908-u5k`

Out of scope for this task (annotate-forward only). Recorded, not acted on.

## D1 — the parenthetical `(2.36 / 1.97 / 1.99 / 1.52)` mixes two roundings of the same table

The annotation transcribes CONTENT-SPEC.md **verbatim**. Its four overlap figures are
attested in `.planning/osf_deviations.md` §(10b), but not in one consistent rounding:

| treatment | osf §(10b) table | courier body | annotation |
| --- | --- | --- | --- |
| all 21 windows | 2.362 | 2.36x (`:143`) | 2.36 |
| drop `sub13` (headline) | 1.969 | **1.99x** (`:136`, `:481`, `:487`) | 1.97 *and* 1.99? |
| drop `sub12` (mirror) | 1.972 | *not quoted* | |
| drop BOTH chr15 | 1.535 | 1.52 (`:139`) | 1.52 |

So `1.97` reads as the osf rounding of the **headline** while `1.99` is the courier's own
headline figure for the **same** treatment. **The load-bearing property is unaffected** —
all four values, under every rounding in the record, are `> 1.3`, which is the only thing
the parenthetical is asked to support.

⚠ **The real underlying question is PRE-EXISTING and is NOT created by this task:** the
courier quotes the drop-`sub13` overdispersion as **1.99** where `osf_deviations.md`
§(10b) computes **1.969**. Whether that is a rounding/derivation difference or a genuine
disagreement between two records was **NOT** investigated here — investigating it would
have meant re-deriving a number, which this DOCS-ONLY annotate-forward task must not do.

**Disposition:** DEFERRED. It touches neither the annotation's claim nor the appendix.
