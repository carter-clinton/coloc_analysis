---
quick_id: 260424-k2e
phase: quick-260424-k2e
plan: 01
type: execute
wave: 1
depends_on:
  - 260424-k2c  # PATHWAY-RECOMPUTE-PENDING marker placed by k2c Discussion R1
requirements:
  - ROUTE-A-PATHWAY-RECOMPUTE
  - ROUTE-A-L190-EXTRACT-RESOLVE
  - ROUTE-A-L222-CONDITIONAL-TIGHTEN
files_modified:
  - docs/manuscript/track_a_pivot.md
files_created:
  - results/pathway/gprofiler/tier_ab_genes.txt (empty; empirical proof of zero surviving genes)
tags:
  - track-a
  - manuscript
  - pathway-enrichment
  - pathway-recompute
  - original-research
title: "Route A — Pathway re-compute resolution (real-LD Tier A+B = 0 empirically confirmed; L190 [EXTRACT:] resolved + L222 conditional tightened + PATHWAY-RECOMPUTE-PENDING marker removed)"
must_haves:
  truths:
    - "The real-LD Tier A + Tier B gene set under `results/qtl_coloc/tier_assignments.tsv` contains zero rows (confirmed via pandas.read_csv + filter on tier.isin(['Tier A','Tier B']) — matches TRACK-A-FROZEN-NUMBERS.md stated `Tier A = 0, Tier B = 0`)."
    - "The empirical proof file `results/pathway/gprofiler/tier_ab_genes.txt` exists, is 0 bytes, and would be the input to the `gprofiler_enrichment` rule — confirming that no enrichment can be computed at the manuscript's confidence threshold."
    - "The 3 named Tier C resolving genes (APOL1 on cultured fibroblasts PP.H4=0.013; IRX3 on pancreas PP.H4=0.310; ATXN2 on adrenal gland PP.H4=0.052) are all below the 0.5 threshold used to enter pathway-enrichment input; n=3 is insufficient for a well-powered enrichment test under any standard framework."
    - "The manuscript L190 pathway-enrichment `[EXTRACT: fold enrichments from results/pathway/ outputs using the real-LD–filtered gene list]` placeholder is replaced with concrete prose stating the zero-gene finding + the 3 named Tier C genes + the explicit withdrawal of the ~40/~13/~13/~10-fold + 63% claims."
    - "The manuscript L222 Reframing paragraph is tightened from conditional (`if the real-LD re-compute substantially weakens these enrichments`) to definite (`yields zero Tier A + Tier B genes at the manuscript's confidence threshold, making the enrichment test non-computable`). The §4.17 P2 verbatim quote `primarily an LD-inflation artifact` remains intact."
    - "The `<!--PATHWAY-RECOMPUTE-PENDING-->` HTML comment placed by k2c (commit 6c679de) at L222 is removed; grep count goes 1 → 0 file-wide."
    - "Net `[EXTRACT:]` delta across the file = -1 (L190 pathway placeholder resolved; other Methods/Results/Tables placeholders unchanged at their 16 residual count)."
  artifacts:
    - path: "docs/manuscript/track_a_pivot.md"
      provides: "Pathway Enrichment Analysis L190 resolved + Reframing L222 definitive + PATHWAY-RECOMPUTE-PENDING marker removed"
      contains: "zero genes"
    - path: "results/pathway/gprofiler/tier_ab_genes.txt"
      provides: "Empirical proof file — extract_tier_ab_genes output is 0 bytes under real-LD tier_assignments.tsv"
      min_bytes: 0
      max_bytes: 0
    - path: ".planning/quick/260424-k2e-route-a-pathway-recompute-resolves-l222-pen/260424-k2e-SUMMARY.md"
      provides: "Fire-and-resolve summary with tier enumeration + before/after manuscript diffs + 7-gate verification"
      contains: "Tier A + Tier B"
  key_links:
    - from: "results/qtl_coloc/tier_assignments.tsv (233 rows)"
      to: "results/pathway/gprofiler/tier_ab_genes.txt (0 lines)"
      via: "extract_tier_ab_genes rule (src/snakemake/rules/pathway.smk:1901) applied inline via pandas (no Snakemake fire needed; the output is empirically zero)"
      pattern: "Tier A|Tier B"
    - from: "docs/manuscript/track_a_pivot.md L190 (Pathway Enrichment Analysis)"
      to: "docs/manuscript/track_a_pivot.md L222 (Reframing of Cardiometabolic Pleiotropy Claims)"
      via: "L222 cites L190 (`Pathway enrichment on the real-LD–surviving gene set, reported in Results §Pathway Enrichment Analysis, yields zero Tier A + Tier B genes...`)"
      pattern: "§Pathway Enrichment Analysis"
    - from: ".planning/amendments/TRACK-A-PIVOT.md §4.17 P2"
      to: "docs/manuscript/track_a_pivot.md L222"
      via: "The §4.17 P2 verbatim quote `primarily an LD-inflation artifact` is preserved as the definitive conclusion now that the conditional has been resolved"
      pattern: "primarily an LD-inflation artifact"
---

<objective>
Route A — resolve the `PATHWAY-RECOMPUTE-PENDING` handoff placed at `docs/manuscript/track_a_pivot.md` L222 by k2c (commit 6c679de). Empirical finding from `results/qtl_coloc/tier_assignments.tsv`: the real-LD Tier A + Tier B gene set contains **zero genes** (0 Tier A + 0 Tier B; 9 Tier C with only 3 named resolving genes, all at PP.H4 < 0.5). The identity-LD–sourced claims (~40-fold appetite-regulation, ~13-fold insulin-signaling, ~13-fold glucose-metabolism, ~10-fold fatty-acid-metabolism, 63% metabolic-pathway dominance) therefore cannot be reproduced at the manuscript's confidence threshold — not because the pathway enrichment rule was not run, but because the input gene list at Tier A+B filter is empirically empty.

This is not a Snakemake fire (there is no enrichment to compute on zero genes). It is an analytical resolution: write the empirical-zero result into L190 (Pathway Enrichment Analysis), tighten the L222 Reframing conditional to definite, remove the `<!--PATHWAY-RECOMPUTE-PENDING-->` marker, and produce the empirical proof file (empty `results/pathway/gprofiler/tier_ab_genes.txt`) so downstream readers can verify the zero-signal claim from disk.

Output: 3 surgical manuscript edits (L190 [EXTRACT:] → concrete prose; L222 conditional → definite; marker removed) + 1 empirical proof file (0-byte tier_ab_genes.txt). Framing: original-research identity-vs-real-LD audit; the zero-gene result is the definitive evidence for the "primarily an LD-inflation artifact" conclusion. NEVER "revision" / "cleanup" / "fix".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/amendments/TRACK-A-PIVOT.md
@.planning/amendments/TRACK-A-FROZEN-NUMBERS.md
@docs/manuscript/track_a_pivot.md
@.planning/quick/260424-k2c-route-a-step-2-2-e-discussion-rewrite-fo/260424-k2c-SUMMARY.md
@src/snakemake/rules/pathway.smk
@results/qtl_coloc/tier_assignments.tsv

<interfaces>
<!-- Empirical facts from direct inspection (this session, 2026-04-24 17:15 EDT) -->

### tier_assignments.tsv contents (233 rows total)

```
tier                 count
negative_control     224
Tier C                 9
Tier A                 0
Tier B                 0
```

Tier A = 0 and Tier B = 0 per TRACK-A-FROZEN-NUMBERS.md (already frozen pre-pivot) and re-verified this session.

### Tier C breakdown (9 rows; only 3 with named resolving genes)

```
region                 ancestry  best_gwas_pph4  best_qtl_pph4  resolving_gene      tissue
APOL1_22q12            AFR       NaN             0.000          (none)              —
APOL1_22q12            EUR       NaN             0.013          ENSG00000100342     Cells_Cultured_fibroblasts
CXADR_F2RL1_6p21       EUR       NaN             0.000          (none)              —
FTO_16q12              AFR       NaN             0.000          (none)              —
FTO_16q12              EUR       NaN             0.310          ENSG00000177508     Pancreas  (IRX3)
MC4R_18q21             AFR       NaN             0.000          (none)              —
MC4R_18q21             EUR       NaN             0.000          (none)              —
SH2B3_12q24            AFR       NaN             0.000          (none)              —
SH2B3_12q24            EUR       NaN             0.052          ENSG00000204842     Adrenal_Gland  (ATXN2)
```

All three named genes are below the 0.5 confidence threshold used for pathway-enrichment input.
n = 3 is insufficient for well-powered enrichment (gProfiler/MAGMA gene-set tests typically require n ≥ 10).

### extract_tier_ab_genes rule behavior under real-LD

`src/snakemake/rules/pathway.smk` line 1901–1938 — the rule's Python body:

```python
df = pd.read_csv(input.tiers, sep="\t")
tier_ab = df[df["tier"].isin(["Tier A", "Tier B"])]
if "gene_symbol" in tier_ab.columns:
    genes = sorted(tier_ab["gene_symbol"].dropna().unique())
elif "gene" in tier_ab.columns:
    genes = sorted(tier_ab["gene"].dropna().unique())
else:
    raise ValueError(...)  # tier_assignments.tsv has resolving_gene, not gene_symbol/gene

with open(output.gene_list, "w") as f:
    for gene in genes:
        f.write(f"{gene}\n")
```

On the current tier_assignments.tsv, the rule's implicit column-lookup would ValueError (neither `gene_symbol` nor `gene` exists — the column is `resolving_gene`). We worked around this inline by reading `resolving_gene` directly and confirmed the filtered Tier A+B subset is empty regardless: `len(tier_ab) == 0`. The empty output file at `results/pathway/gprofiler/tier_ab_genes.txt` is the empirical proof.

This also flags a separate rule-hardening opportunity (handoff note): the `extract_tier_ab_genes` rule should accept `resolving_gene` as the column name. Not in scope for this quick.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce empty `results/pathway/gprofiler/tier_ab_genes.txt` (empirical proof)</name>
  <files>results/pathway/gprofiler/tier_ab_genes.txt</files>
  <action>
Run inline python to produce the 0-byte empirical proof file that downstream readers can verify against disk:

```bash
/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python3 <<'EOF'
import pandas as pd
df = pd.read_csv('results/qtl_coloc/tier_assignments.tsv', sep='\t')
tier_ab = df[df["tier"].isin(["Tier A", "Tier B"])]
# tier_assignments.tsv column is `resolving_gene` (not gene_symbol/gene), handled directly
genes = sorted(tier_ab["resolving_gene"].dropna().unique()) if "resolving_gene" in df.columns else []
with open('results/pathway/gprofiler/tier_ab_genes.txt', 'w') as f:
    for gene in genes:
        f.write(f"{gene}\n")
EOF
```

Expected: file exists, 0 lines, 0 bytes.
  </action>
  <verify>
    <automated>
OUT=results/pathway/gprofiler/tier_ab_genes.txt
test -f "$OUT" || { echo "FAIL T1.1: $OUT missing"; exit 1; }
BYTES=$(wc -c < "$OUT")
test "$BYTES" = "0" || { echo "FAIL T1.2: expected 0 bytes, got $BYTES"; exit 1; }
LINES=$(wc -l < "$OUT")
test "$LINES" = "0" || { echo "FAIL T1.3: expected 0 lines, got $LINES"; exit 1; }
echo "T1-VERIFY: OK (empirical proof file exists, 0 bytes, 0 lines)"
    </automated>
  </verify>
  <done>0-byte tier_ab_genes.txt at `results/pathway/gprofiler/tier_ab_genes.txt`.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve L190 Pathway Enrichment Analysis [EXTRACT:] with concrete prose</name>
  <files>docs/manuscript/track_a_pivot.md</files>
  <action>
Replace the `[EXTRACT: fold enrichments from ...]` placeholder at L190 with concrete prose reporting the zero-gene finding + 3 named Tier C genes + explicit withdrawal of identity-LD claims. Preserve surrounding prose and section heading `### Pathway Enrichment Analysis`.

Replacement paragraph (~200 words):

> Under real-LD, the Tier A + Tier B gene set filtered from `results/qtl_coloc/tier_assignments.tsv` (Tier A: GWAS PP.H4 ≥ 0.8 AND QTL PP.H4 ≥ 0.8; Tier B: GWAS PP.H4 ≥ 0.8 AND QTL PP.H4 ≥ 0.5) contains **zero genes** (0 Tier A + 0 Tier B rows, confirmed at `results/pathway/gprofiler/tier_ab_genes.txt`, 0 bytes). The 9 Tier C rows contain only 3 named resolving genes (APOL1 on cultured fibroblasts, PP.H4 = 0.013; IRX3 on pancreas, PP.H4 = 0.310; ATXN2 on adrenal gland, PP.H4 = 0.052) — all below the 0.5 confidence threshold used for pathway-enrichment input, and too few (n = 3) to support a well-powered enrichment test under any standard framework. The original identity-LD–sourced claims — ~40-fold appetite-regulation enrichment, ~13-fold insulin-signaling enrichment, ~13-fold glucose-metabolism enrichment, ~10-fold fatty-acid-metabolism enrichment, 63% metabolic-pathway dominance — therefore cannot be reproduced under real-LD at the manuscript's confidence threshold. These claims are withdrawn; the side-by-side comparison in Figure S5 / Table S7 reports "real-LD Tier A+B = 0 genes; pathway enrichment non-computable at threshold" against the identity-LD fold-enrichment columns.

Also update the follow-on paragraph (currently "We do NOT retain the "63% of pleiotropic genes converge on metabolic pathways" headline unless it survives the re-compute. If the enrichment pattern changes substantially, the biological-interpretation section below is reframed accordingly.") to reflect the definitive outcome:

> The "63% of pleiotropic genes converge on metabolic pathways" headline does not survive the re-compute and is retracted. The biological-interpretation section below is reframed accordingly.
  </action>
  <verify>
    <automated>
FILE=docs/manuscript/track_a_pivot.md
PATH_EXTRACT=$(awk 'NR>=188 && NR<=192' "$FILE" | grep -c '\[EXTRACT:.*pathway')
test "$PATH_EXTRACT" = "0" || { echo "FAIL T2.1: Pathway [EXTRACT:] still present (count=$PATH_EXTRACT)"; exit 1; }
ZERO=$(awk 'NR==190' "$FILE" | grep -c 'zero genes')
test "$ZERO" -ge "1" || { echo "FAIL T2.2: 'zero genes' not present in L190"; exit 1; }
WITHDRAWN=$(awk 'NR==190 || NR==192' "$FILE" | grep -c 'withdrawn\|retracted')
test "$WITHDRAWN" -ge "1" || { echo "FAIL T2.3: withdrawal/retraction language missing"; exit 1; }
echo "T2-VERIFY: OK"
    </automated>
  </verify>
  <done>L190 resolved with concrete zero-gene finding; follow-on paragraph tightened to retraction language.</done>
</task>

<task type="auto">
  <name>Task 3: Tighten L222 Reframing conditional + remove PATHWAY-RECOMPUTE-PENDING marker</name>
  <files>docs/manuscript/track_a_pivot.md</files>
  <action>
In the L222 Reframing paragraph, replace:

> "Pathway enrichment on the real-LD–surviving gene set is re-computed in Results §Pathway Enrichment Analysis; if the real-LD re-compute substantially weakens these enrichments, <!--PATHWAY-RECOMPUTE-PENDING--> the previously-reported pathway-level architecture of cardiometabolic pleiotropy is, at these 50 curated loci, primarily an LD-inflation artifact rather than a biological signal."

with definitive prose:

> "Pathway enrichment on the real-LD–surviving gene set, reported in Results §Pathway Enrichment Analysis, yields zero Tier A + Tier B genes at the manuscript's confidence threshold (PP.H4 ≥ 0.5), making the enrichment test non-computable at threshold. The previously-reported pathway-level architecture of cardiometabolic pleiotropy is, at these 50 curated loci, primarily an LD-inflation artifact rather than a biological signal."

Preservations:
- The §4.17 P2 verbatim quote "primarily an LD-inflation artifact" — unchanged
- SH2B3 12q24 EUR collapse as micro-scale analog sentence — unchanged
- Track B forward-pointer (All-of-Us AFR LD panel) — unchanged
- All frozen-numerics — unchanged
  </action>
  <verify>
    <automated>
FILE=docs/manuscript/track_a_pivot.md
HANDOFF=$(grep -c 'PATHWAY-RECOMPUTE-PENDING' "$FILE")
test "$HANDOFF" = "0" || { echo "FAIL T3.1: PATHWAY-RECOMPUTE-PENDING still present (count=$HANDOFF)"; exit 1; }
IF_CLAUSE=$(awk 'NR==222' "$FILE" | grep -c 'if the real-LD re-compute')
test "$IF_CLAUSE" = "0" || { echo "FAIL T3.2: conditional 'if the real-LD re-compute' still present"; exit 1; }
P2=$(awk 'NR==222' "$FILE" | grep -c 'primarily an LD-inflation artifact')
test "$P2" = "1" || { echo "FAIL T3.3: §4.17 P2 quote preservation failed"; exit 1; }
NONCOMPUT=$(awk 'NR==222' "$FILE" | grep -c 'non-computable\|zero Tier A')
test "$NONCOMPUT" -ge "1" || { echo "FAIL T3.4: definite language missing in L222"; exit 1; }
echo "T3-VERIFY: OK"
    </automated>
  </verify>
  <done>L222 definitive (no conditional "if"), PATHWAY-RECOMPUTE-PENDING removed, §4.17 P2 quote preserved.</done>
</task>

</tasks>

<verification>
File-level final gates:
1. `grep -c 'PATHWAY-RECOMPUTE-PENDING' docs/manuscript/track_a_pivot.md` = 0.
2. Net `[EXTRACT:]` file delta = -1 (pre=17, post=16).
3. §4.17 P2 direct quote `primarily an LD-inflation artifact` appears exactly 1× in file.
4. Stage 2 real-LD artifacts UNTOUCHED (md5 preserved).
5. k2d identity-LD fire (PID 830748) unaffected — does not depend on the manuscript or pathway files.
</verification>

<success_criteria>
- L190 Pathway Enrichment Analysis section reports the empirical zero-gene finding with concrete language.
- L222 Reframing paragraph is definitive (no conditional "if"), retains §4.17 P2 verbatim quote, no PATHWAY-RECOMPUTE-PENDING marker.
- `results/pathway/gprofiler/tier_ab_genes.txt` exists at 0 bytes as empirical proof.
- Framing discipline preserved: original research, never "revision" / "cleanup" / "fix".
- k2c handoff fully resolved: no downstream pass needs to touch L222 again for this reason.
</success_criteria>

<output>
After completion, the executor creates:
- `results/pathway/gprofiler/tier_ab_genes.txt` (0 bytes; empirical proof)

And modifies:
- `docs/manuscript/track_a_pivot.md` (L190 + L222 edits, ~3 line-level replacements)

And writes:
- `.planning/quick/260424-k2e-route-a-pathway-recompute-resolves-l222-pen/260424-k2e-PLAN.md`
- `.planning/quick/260424-k2e-route-a-pathway-recompute-resolves-l222-pen/260424-k2e-SUMMARY.md`

Commit (single): `docs(quick-260424-k2e): Route A pathway re-compute resolution — real-LD Tier A+B=0 empirical + L190/L222 manuscript edits + STATE.md row`. Note: tier_ab_genes.txt is under `results/pathway/gprofiler/` which is gitignored — the file exists on disk as evidence but is NOT committed (downstream readers verify on-disk).
</output>
