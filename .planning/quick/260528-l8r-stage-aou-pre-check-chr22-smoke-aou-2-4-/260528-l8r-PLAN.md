---
quick_id: 260528-l8r
description: Stage pre-migration moves (Abby Zendesk reply + forensic mirror script + migration playbook) + post-migration AoU notebook staging (precheck + chr22-smoke + AOU-2/4 pattern doc) — fresh files only; preserve everything currently committed
mode: quick
date: 2026-05-28
status: ready_for_execution
must_haves:
  truths:
    - "Scope expanded mid-task: discovered RW 2.0 migration hard deadline 2026-06-30 (33 days from today). Pre-migration moves added to preserve Track 1 credit-recovery forensic evidence + nudge Abby's engineering response."
    - "New file .planning/quick/260528-l8r-.../ABBY-MIGRATION-CLARIFICATION-DRAFT.md exists with paste-ready Zendesk reply asking Abby whether migration affects forensic-evidence access + announces Carter's 1-2 week migration window"
    - "New file .planning/quick/260528-l8r-.../run_forensic_mirror.sh exists — paste-ready bash that runs inside AoU env (paused-cluster compatible; gsutil + base64-encoded hail.log fetch only) and produces an immutable JSON+TXT mirror of the catastrophe evidence (du -s, ls -l + mtime, metadata.json.gz parse, hail.log preserve) that Carter cps to NCSU GPFS"
    - "New directory .planning/quick/260521-w1-catastrophe-handoff/forensic-mirror/ exists with README.md explaining the receiver layout — populated when Carter runs run_forensic_mirror.sh from AoU env post-recreation"
    - "New file .planning/quick/260528-l8r-.../MIGRATION-PLAYBOOK.md exists — pre-migration gates (Zendesk reply sent + forensic mirror committed + workspace prep done) + Step 1-4 contextualized to coloc_analysis workspace + post-migration validation checklist"
    - "New file .planning/notebooks/AOU-0-precheck_template.ipynb exists and validates AoU clone HEAD includes Track 4 patches + RW 2.0 platform context + parses _SUCCESS mtimes + reports hypothesis verdict (Carter kill-as-culprit vs debug-doc Hail-finalize) BEFORE any compute fires"
    - "New file .planning/notebooks/AOU-1-chr22-smoke_template.ipynb exists and fires the 3 load_qc_cohort calls with interval_filter='chr22' + chr22-scaled MIN_BYTES floor (~50 MB instead of 1 GB) in Cells 3.5/4.5/5.5 + smoke-specific cluster sizing guidance"
    - "New file .planning/notebooks/AOU-2-AOU-4-TRACK-4-PATTERN.md exists with paste-ready cell snippets that apply the Track 4 contents-validation pattern to AOU-2 (MT read validation + per-region .npz output validation) and AOU-4 (per-region .npz input validation) — to be applied when Wave 2 fires, NOT now"
    - "All 3 existing notebooks (AOU-1_template.ipynb HEAD, AOU-2_per_region_ld.ipynb HEAD, AOU-4_validation.ipynb HEAD) remain byte-identical to origin/m3-W2-aou-deltas tip — preserved for AoU tech support / Abby Doyle review and Wave 2 fire context"
    - "All new artifacts validate via nbformat.validate() and pytest module collection succeeds with 38 PASSED unchanged"
  artifacts:
    - .planning/quick/260528-l8r-stage-aou-pre-check-chr22-smoke-aou-2-4-/260528-l8r-PLAN.md
    - .planning/quick/260528-l8r-stage-aou-pre-check-chr22-smoke-aou-2-4-/260528-l8r-SUMMARY.md
    - .planning/quick/260528-l8r-stage-aou-pre-check-chr22-smoke-aou-2-4-/ABBY-MIGRATION-CLARIFICATION-DRAFT.md
    - .planning/quick/260528-l8r-stage-aou-pre-check-chr22-smoke-aou-2-4-/run_forensic_mirror.sh
    - .planning/quick/260528-l8r-stage-aou-pre-check-chr22-smoke-aou-2-4-/MIGRATION-PLAYBOOK.md
    - .planning/quick/260521-w1-catastrophe-handoff/forensic-mirror/README.md
    - .planning/notebooks/AOU-0-precheck_template.ipynb
    - .planning/notebooks/AOU-1-chr22-smoke_template.ipynb
    - .planning/notebooks/AOU-2-AOU-4-TRACK-4-PATTERN.md
  key_links:
    - .planning/quick/260528-jvd-land-m3-w1-track-4-defensive-code-patche/260528-jvd-SUMMARY.md (Track 4 patches that this staging builds on)
    - .planning/quick/260521-w1-catastrophe-handoff/HANDOFF.md (catastrophe handoff + 4-track plan)
    - .planning/debug/m3-W1-empty-mt-catastrophe.md (root cause analysis source)
    - .planning/phases/m3-aou-afr-ld-panel-build/m3-CONTEXT.md (D-M3-10 protocol locked here)
    - ~/.claude/projects/-gpfs-common-share01-clintonlab-ckclinto-coloc-analysis/memory/feedback_w1_catastrophe_hypothesis_distinguisher.md (mtime test rule)
    - https://support.researchallofus.org/hc/en-us/articles/48266066855188 (AoU RW 2.0 migration support article; 2026-06-30 hard deadline)
---

# PLAN — AoU pre-check + chr22-smoke + AOU-2/4 Track-4-pattern staging

## Goal

Stage 3 new artifacts that prep the next AoU session for fast action WITHOUT firing AoU compute today. Preserve every existing committed notebook for Abby Doyle / tech support review (origin/m3-W2-aou-deltas tip = e38e8f2 stays the truth-of-record for the catastrophe execution context + Track 4 defensive patches).

When Abby's reply lands:
- pre-check notebook runs gsutil-only on a paused env (~$0.14/hr) to resolve Carter's kill-as-culprit hypothesis via _SUCCESS mtime probe + confirm Track 4 patches reached the AoU clone via git pull
- chr22-smoke notebook fires (~$30-75, ~3h cluster time) to validate the new assertions under live Hail before any full-cohort rebuild
- AOU-2 / AOU-4 inherit the Track 4 pattern when Wave 2 dev fire is greenlit (per D-M3-10)

## Tasks (4 atomic commits + 1 closeout docs commit)

### Task 1 — AOU-0-precheck_template.ipynb (NEW)

**files:** `.planning/notebooks/AOU-0-precheck_template.ipynb`

**action:** Create a new compute-free notebook (gsutil + bash subprocess only; NO Hail/Spark) that runs as the mandatory pre-flight before any AOU-1 / AOU-1-chr22-smoke / AOU-2 fire. Cells:

- Cell 0 (markdown): purpose, expected runtime (~2 min), cost (~$0.05 — gsutil only), when-to-run (every env recreation, every git pull, every Abby-reply-routing decision).
- Cell 1 (markdown): Track 4 protocol summary + cross-references to D-M3-10 / debug doc / hypothesis-distinguisher memory.
- Cell 2 (code): clone HEAD verification — `git rev-parse HEAD` inside `/home/jupyter/coloc_analysis` + assert source includes `_validate_checkpoint_populated`, `_assert_checkpoint_nonempty`, `MIN_ENTRIES_FILE_BYTES`. If any missing, print remediation: `cd /home/jupyter/coloc_analysis && git pull origin m3-W2-aou-deltas`.
- Cell 3 (code): env-var assertion (`WORKSPACE_BUCKET`, `GOOGLE_PROJECT`, `WGS_ACAF_THRESHOLD_MULTI_HAIL_PATH`).
- Cell 4 (code): catastrophe-MT inventory — gsutil ls on the 3 cohort MT directories; print presence/absence + total bytes per `gsutil du -s`.
- Cell 5 (code): hypothesis distinguisher — `gsutil ls -l` on existing `_SUCCESS` files; parse mtime; print verdict block (Carter's kill-as-culprit IF mtime ≥ 2026-05-20 22:30:00 UTC; debug-doc Hail-finalize IF mtime is in the 2026-05-19 / 2026-05-20 stage windows before kill). Be explicit about both windows.
- Cell 6 (code): entries-dir contents check — `gsutil du -s` against `mt_*/entries/entries/parts/` to confirm the catastrophe pattern is still present (or has cleared if the bucket was purged).
- Cell 7 (markdown): action-routing matrix per pre-check findings (Abby's reply / Carter's hypothesis-test result / clone state).

**verify:**
- `python3 -c "import nbformat; nbformat.validate(nbformat.read('.planning/notebooks/AOU-0-precheck_template.ipynb', as_version=4))"` → no error.
- Cell count = 8 (0-7).
- `grep -c "feedback_w1_catastrophe_hypothesis_distinguisher" .planning/notebooks/AOU-0-precheck_template.ipynb` ≥ 1.
- `grep -c "_validate_checkpoint_populated\|_assert_checkpoint_nonempty" .planning/notebooks/AOU-0-precheck_template.ipynb` ≥ 2.

**done:** atomic commit `feat(m3-W1): AOU-0 pre-check notebook — clone+env+mtime+entries probe before any AoU fire (staging T1/3)`.

### Task 2 — AOU-1-chr22-smoke_template.ipynb (NEW)

**files:** `.planning/notebooks/AOU-1-chr22-smoke_template.ipynb`

**action:** Create a smoke-test variant of AOU-1_template.ipynb. Same structural skeleton (Cells 0 markdown intro, Cell 1a PYSPARK_SUBMIT_ARGS, Cell 1b hl.init + verify, Cells 3/3.5/4/4.5/5/5.5/6/7) but:

- Cell 0 markdown rewritten to call out: chr22 smoke purpose; expected $30-75 cost; expected ~3h cluster time on 16× n1-highmem-16 (same as production — Hail's partition explosion is per-genome-region not per-output, so smoke still needs 256 vCPU); MUST run after AOU-0 pre-check passes.
- Cells 1a + 1b unchanged from AOU-1_template.ipynb (same cores=1/mem=5g + hl.init + verify pattern; Track 4 patches already in `_qc_checkpoint_uri` import path).
- Cells 3/4/5: pass `interval_filter="chr22"` to `load_qc_cohort()`. Print line tweaked to flag chr22-suffix MT URIs (`mt_afr_qc_chr22.mt`, etc. per `_intermediate_checkpoint_uri` interval-suffix convention).
- Cells 3.5/4.5/5.5: SAME structure as AOU-1 but `_MIN_BYTES = 50_000_000` (50 MB floor) — chr22 ≈ 1/22nd of genome so expect ~500 MB - 2 GB entries-dir for AFR primary; 50 MB cleanly catches footer-stub state without false-positiving on legitimately small chr22 cohorts. Add markdown note explaining the threshold derivation.
- Cell 6 disjoint check unchanged (still meaningful on chr22 subset).
- Cell 7 cohort summary writes `cohort_summary_m3_chr22.tsv` instead of `cohort_summary_m3.tsv` to keep the smoke output distinct from any future production-fire artifact.

**verify:**
- nbformat.validate clean.
- Cell count ≥ 9 (same as AOU-1 post-Track 4 = 12 cells; smoke variant matches).
- `grep -c "interval_filter=\"chr22\"" .planning/notebooks/AOU-1-chr22-smoke_template.ipynb` ≥ 3 (one per cohort cell).
- `grep -c "_MIN_BYTES = 50_000_000\|50_000_000.*chr22" .planning/notebooks/AOU-1-chr22-smoke_template.ipynb` ≥ 3 (one per assertion cell).
- `grep -c "cohort_summary_m3_chr22.tsv" .planning/notebooks/AOU-1-chr22-smoke_template.ipynb` = 1.

**done:** atomic commit `feat(m3-W1): AOU-1-chr22-smoke notebook — chr22 staging for live-Hail validation of Track 4 assertions (staging T2/3)`.

### Task 3 — AOU-2-AOU-4-TRACK-4-PATTERN.md (NEW)

**files:** `.planning/notebooks/AOU-2-AOU-4-TRACK-4-PATTERN.md`

**action:** Write a paste-ready cell-snippet document for applying D-M3-10 contents-validation to AOU-2 + AOU-4 at the time Wave 2 fires. The snippets are NOT applied to AOU-2 / AOU-4 today — those notebooks stay byte-identical for tech support review and to preserve the Wave 2 dev fire surface area. Structure:

- **§1 Why this doc exists** — D-M3-10 mandates contents-validation across Waves 1+2+4+5; AOU-2 (Wave 2 per-region LD compute) reads the same cohort MTs the W1 catastrophe produced + emits per-region .npz files; AOU-4 (validation harness) consumes those .npz files. Either step is silently corruptible without inheritance of the Track 4 pattern.
- **§2 AOU-2 Cell 4 paste-in: cohort-MT contents validation** — insert AFTER `hl.read_matrix_table(...)` for `mt_afr` + `mt_eur`; assert `count_cols()>0 + count_rows()>0` + run `_validate_checkpoint_populated` against the bucket URI via the imported helper. ~10 lines.
- **§3 AOU-2 Cell 6 paste-in: per-region .npz output validation** — insert AFTER each `compute_region_ld()` call inside the region loop; assert the returned status is `"ok"` OR `"skipped_idempotent"`, NOT a silent empty-status; if `"ok"`, assert the emitted `.npz` exists + load it + assert `ld.shape[0] > 0` + `np.all(np.isfinite(ld))`. ~15 lines.
- **§4 AOU-4 Check 1+2+3+4 paste-ins: input .npz validation** — at the top of each check, load the input `.npz` and assert non-empty + finite values + correct dtype before any R-side compute fires. ~5 lines per check.
- **§5 D-M3-07 sensitivity check paste-in** — same pattern applied to AOU-4 Cell 11.
- **§6 Application rules** — apply paste-ins at Wave 2 fire time, not before; preserve the existing notebooks until then for the audit trail; new cells should be commented with `# Track 4 D-M3-10 paste-in (260528-l8r)` for git-blame traceability.
- **§7 Cross-references** — Track 4 quick task 260528-jvd commits + D-M3-10 + debug doc.

**verify:**
- File exists.
- `grep -c "AOU-2 Cell 4\|AOU-2 Cell 6\|AOU-4 Check 1\|AOU-4 Check 2\|AOU-4 Check 3\|AOU-4 Check 4\|D-M3-07 sensitivity" .planning/notebooks/AOU-2-AOU-4-TRACK-4-PATTERN.md` ≥ 6.
- `grep -c "_validate_checkpoint_populated\|count_cols\|count_rows\|np.isfinite\|np.all" .planning/notebooks/AOU-2-AOU-4-TRACK-4-PATTERN.md` ≥ 5.
- `grep -c "D-M3-10\|Track 4\|260528-jvd" .planning/notebooks/AOU-2-AOU-4-TRACK-4-PATTERN.md` ≥ 6.

**done:** atomic commit `docs(m3-W1): AOU-2/AOU-4 Track-4-pattern paste-in doc (staging T3/3)`.

### Task 4 (closeout) — SUMMARY + STATE + PLAN docs commit

Same pattern as 260528-jvd close-out: SUMMARY.md captures must_haves vs reality + test sweep evidence; STATE.md row appended to Quick Tasks Completed; STATE.md last_updated + last_activity bumped; single docs commit per `/gsd-quick` workflow Step 8.

## Out of scope

- Modifying AOU-1_template.ipynb / AOU-2_per_region_ld.ipynb / AOU-4_validation.ipynb (Carter directive: preserve everything we currently have for tech support review).
- Firing AoU compute (chr22 smoke, env recreation, gsutil mtime probe execution) — deferred until Abby's reply.
- Updating Track 4 source code patches (already landed in 260528-jvd).
- chr22 sub-1GB threshold tuning beyond the 50 MB floor — if chr22 fire reports a legitimate cohort size under 50 MB, that's a separate signal worth a quick task to investigate.
- AoU clone `git pull` (Carter user-action when env is next recreated).
- Wave 2 fire planning (gated on Track 1 credit recovery + Abby's reply + LD substrate decision).
