---
phase: quick-260429-s10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/python/build_cojo_inputs.py
  - tests/m2/test_build_cojo_inputs_hm3.py
  - bin/fire_m2_post_m3_08_mtcojo.sh
  - data/processed/mtcojo/m2_post_m3_08_bjobs.tsv
  - .planning/m2_post_m3_rerun_queue.tsv
  - .planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/HARVEST-RUNBOOK.md
  - .planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/bjobs.tsv
autonomous: true
requirements:
  - M2-POST-M3-08
---

<objective>
Hypothesis-driven original research: deliver an HM3-restricted COJO input
materializer + LSF long-queue burst-fire that re-derives the M2 mtCOJO
production sensitivity table over the 13 eligible (target_trait, stratum)
tuples locked in `data/processed/mtcojo/{EUR,AFR,TRANS}/mtcojo_eligible_targets.tsv`.

The current `mtcojo_sensitivity.tsv` artifacts carry `sensitivity_flag=FAIL`
for all rows because GCTA mtCOJO's internal LDSC bivariate-intercept regression
step terminates with `Error: no SNP in common between the summary data and
the LD score files` when fed full-genome COJO inputs (witnessed in
`data/processed/mtcojo/EUR/bmi.EUR.GIANT-UKBB.2018.mtcojo.fire.log`). The
reviewer-defensible remediation specified by `m2_post_m3_rerun_queue.tsv`
row M2-POST-M3-08 is to pre-restrict the per-trait COJO inputs to the
HapMap3 SNP list (`tools/mtag/ld_ref_panel/eur_w_ld_chr/w_hm3.snplist`,
~1.22 M SNPs) so GCTA's LDSC step receives a file whose SNP namespace is
guaranteed to overlap the eur_w_ld_chr ld-score files.

This is a LAUNCH ("fire") task: the executor extends the COJO materializer,
writes a stratum-aware LSF driver, runs ONE pre-flight smoke test to confirm
the HM3 intersection unblocks GCTA's LDSC step, then bsubs 13 production
jobs to LSF long-queue and returns a manifest + harvest runbook. The
executor does NOT block on GCTA wall time — Carter harvests results when
LSF jobs land (~6.5 hr aggregate at ~30 min/target).

Purpose: close obligation M2-POST-M3-08, replacing the all-FAIL placeholder
sensitivity table with a real PASS/WARN/FAIL classification per Q8 schema.

Output:
  - Extended `src/python/build_cojo_inputs.py` with new `--hm3-snplist` flag
    (default OFF — legacy genome-wide behavior preserved when flag absent;
    pytest covering both branches)
  - `bin/fire_m2_post_m3_08_mtcojo.sh` LSF burst-fire driver (mirrors
    `m2_mtcojo.smk` argv contract per the established Wave 2/3 bypass
    pattern, Rule 3 deviation; documented inline)
  - 13 LSF jobs in flight on `long` queue + `data/processed/mtcojo/m2_post_m3_08_bjobs.tsv`
    job manifest (committed)
  - `HARVEST-RUNBOOK.md` for the post-LSF harvest step (NOT executed in
    this quick task — Carter harvests when LSF lands)
  - Obligation queue updated: M2-POST-M3-08 status `in_flight` with
    submit timestamp + LSF job IDs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/m2_post_m3_rerun_queue.tsv
@bin/fire_m2_04_mtcojo.sh
@src/python/build_cojo_inputs.py
@src/python/select_mtcojo_eligible_targets.py
@src/python/build_mtcojo_sensitivity_table.py
@src/snakemake/rules/m2_mtcojo.smk
@config/bsub_wrapper.sh
@data/processed/mtcojo/EUR/mtcojo_eligible_targets.tsv
@data/processed/mtcojo/AFR/mtcojo_eligible_targets.tsv
@data/processed/mtcojo/TRANS/mtcojo_eligible_targets.tsv
@data/processed/mtcojo/EUR/mtcojo_sensitivity.tsv
@data/processed/mtcojo/EUR/bmi.EUR.GIANT-UKBB.2018.mtcojo.fire.log

<interfaces>
<!-- Authoritative argv contracts the LSF driver MUST mirror (m2_mtcojo.smk + existing fire script). -->
<!-- These are the canonical entry points for the Wave 2/3 bypass pattern (Rule 3 deviation). -->

From src/python/build_cojo_inputs.py (CURRENT — pre-extension):
```python
def build_inputs(
    target: str,
    stratum: str,
    sidecar: Path,
    harmonized_dir: Path,
    out_dir: Path,
) -> Path:
    """Materialize per-trait COJO files + mtcojo input list.
       Returns path to the mtcojo input list file (target_trait first)."""

# Required CLI args (current):
#   --target <trait_key>
#   --stratum <EUR|AFR|TRANS>
#   --sidecar <path/to/residcov.trait_order.json>
#   --harmonized-dir <path>
#   --out-dir <path>
# COJO file written: <out_dir>/{trait_key}.cojo  (whitespace-TSV: SNP A1 A2 freq b se p N)
# List file written: <out_dir>/{target}.mtcojo.list  (target FIRST, then covariates)
```

From src/python/build_cojo_inputs.py — TARGET extension contract:
```python
def materialize_cojo(
    harmonized_path: Path,
    out_path: Path,
    hm3_snps: set[str] | None = None,   # NEW — when provided, intersect SNP column
) -> int:
    """Read harmonized .tsv.bgz, emit COJO-format file (SNP A1 A2 freq b se p N).
       When hm3_snps is provided, drop rows whose SNP is not in the set
       BEFORE the duplicate-drop and write. Returns row count after intersection."""

def build_inputs(
    target: str,
    stratum: str,
    sidecar: Path,
    harmonized_dir: Path,
    out_dir: Path,
    hm3_snplist: Path | None = None,    # NEW — when set, load + pass to materialize_cojo
) -> Path:

# New CLI flag (additive — default OFF):
#   --hm3-snplist <path>   # optional; when present, intersect each per-trait COJO file
                           # with the HM3 snplist before emitting. Default OFF
                           # preserves legacy genome-wide behavior.
```

From w_hm3.snplist format (tools/mtag/ld_ref_panel/eur_w_ld_chr/w_hm3.snplist):
```
SNP	A1	A2
rs3094315	G	A
rs3131972	A	G
...
```
1,217,312 rows incl. header. Intersection key is column `SNP`.

From src/snakemake/rules/m2_mtcojo.smk — argv the LSF driver MUST mirror:
```
gcta-1.94.1 \
    --mtcojo-file <out_dir>/cojo_inputs/<target>.mtcojo.list \
    --mbfile <mbfile_listing_22_per_chr_bed_prefixes> \
    --w-ld-chr   data/external/ldscore/eur_w_ld_chr/ \
    --ref-ld-chr data/external/ldscore/eur_w_ld_chr/ \
    --out        <out_dir>/<target>.mtcojo
```

Stratum→LD-ref dispatch (D-M2-Q3, locked):
  EUR   → data/reference/ldsc/1000G_EUR_Phase3_plink/1000G.EUR.QC.{1..22}
  AFR   → data/reference/ldsc/1000G_AFR_Phase3_plink/1000G.AFR.QC.{1..22}
  TRANS → 1000G_EUR_Phase3_plink (primary; AFR sensitivity = M2-POST-M3-04, NOT this task)
LDSC weights for ALL strata in this fire: data/external/ldscore/eur_w_ld_chr/
  (cross-ancestry approximation per D-M2-Q2; AFR ld-score supersede = M2-POST-M3-05)

From config/bsub_wrapper.sh:
  -q long  → -W 14400 (10-day wall, queue max)
  LSF_UNIT_FOR_LIMITS=GB so -R "rusage[mem=8]" = 8 GB
</interfaces>

<research_notes>
## Failure mode (witnessed)

GCTA-1.94.1 mtCOJO log (bmi.EUR.GIANT-UKBB.2018.mtcojo.fire.log) terminates with:

> Error: no SNP in common between the summary data and the LD score files. Please check.

This fires AFTER GCTA reads 75,783 GW-sig SNPs into PLINK genotype memory. The
LDSC bivariate-intercept regression (gcov_int between target and each
covariate) requires SNPs simultaneously in the target sumstats, covariate
sumstats, AND the eur_w_ld_chr LDSC ld-score files (~1.22 M HM3 SNPs).

## Empirical SNP intersection counts (orchestrator-measured)

  HM3 snplist size:           1,217,312
  BMI cojo size:              2,327,236  → HM3 ∩ BMI cojo  = 1,019,880
  HDL cojo size:             ~11,300,000 → HM3 ∩ HDL cojo  = 1,199,355
  BMI gwsig (p<5e-8) ∩ HM3:    17,850

Geometric overlap exists. GCTA's LDSC step is NOT pre-intersecting to HM3 on
its own; we materialize HM3-only COJO files upstream so its internal SNP
matcher receives a file whose namespace lies inside the LDSC ld-score namespace.

## Authoritative obligation row (m2_post_m3_rerun_queue.tsv row M2-POST-M3-08)

  description: "mtCOJO production sensitivity LSF re-fire for the 13 eligible
                (target_trait, stratum) tuples with HM3-intersected COJO
                inputs (per-target ~10-30 min wall on full HM3+EUR Phase3 LD
                reference; 13 targets × ~30 min ≈ 6.5 hr LSF long-queue)"
  supersede_artifact: "data/processed/mtcojo/{EUR,AFR,TRANS}/mtcojo_sensitivity.tsv
                       with sensitivity_flag ∈ {PASS, WARN, FAIL} based on
                       actual mtcojo_p vs mtag_p_original ratio +
                       max_overlapping_intercept witness"
  dependency_blockers: "HM3-intersected COJO input materializer (extension of
                        src/python/build_cojo_inputs.py) + LSF long-queue
                        allocation"
  priority: high  owner: carter

## Eligible target inventory (13 total)

  EUR (5):   bmi.EUR.GIANT-UKBB.2018, hdl.EUR.GLGC.2021, ldl.EUR.GLGC.2021,
             tc.EUR.GLGC.2021, tg.EUR.GLGC.2021
  AFR (4):   hdl.AFR.GLGC.2021, ldl.AFR.GLGC.2021, tc.AFR.GLGC.2021,
             tg.AFR.GLGC.2021
  TRANS (4): hdl.TRANS.GLGC.2021, ldl.TRANS.GLGC.2021, tc.TRANS.GLGC.2021,
             tg.TRANS.GLGC.2021

## Tooling pins (do NOT improvise)

  GCTA:    /rs1/researchers/c/ckclinto/conda_envs/gcta/bin/gcta-1.94.1
  Python:  /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python  (Snakemake-pinned 3.11)
  LSF:     bsub -q long  ⇒ -W 14400 via config/bsub_wrapper.sh
           -n 4   (matches m2_mtcojo.smk threads: 4)
           -R "rusage[mem=8]"   (8 GB; matches m2_mtcojo.smk mem_mb=8000, LSF_UNIT_FOR_LIMITS=GB)
           -J m2p3_08_<stratum>_<short_target>
           -o logs/lsf/m2p3_08_<stratum>_<short_target>.out

## Standing rules from auto-memory

- Snakemake 7.32.4 requires Python 3.11 — never invoke from miniconda3 base
- LSF wall set by queue (long=14400 min); LSF_UNIT_FOR_LIMITS=GB
- Multi-terminal git staging — explicit paths only on GPFS, never `git add .` / `-A`
- Always pick rigor over time-saving — favor reviewer-defensible options
- Original-research framing — never frame as "fix" / "revision" / "cleanup"

## Out of scope for this quick task (called out, NOT fixed here)

The current EUR sensitivity placeholder shows only 4 rows (hdl/ldl/tc/tg) —
bmi.EUR is missing despite being in `mtcojo_eligible_targets.tsv`. This is a
likely defect in `build_mtcojo_sensitivity_table.py`'s row-emission logic
(only emits rows when MTAG-novel SNPs intersect the cojo file; bmi.EUR's
empty cojo from the failed first-pass produced no rows AT ALL, not even a
FAIL placeholder). This issue is documented in HARVEST-RUNBOOK.md and gets
diagnosed during the post-LSF harvest step — once the 13 jobs land we will
know whether the HM3-restricted re-fire produces a non-empty bmi.EUR cojo,
which moots the question. If bmi.EUR is STILL absent post-harvest, the
sensitivity-aggregator gets a follow-up patch in a separate quick task.
</research_notes>

<wave_2_3_bypass_pattern>
This task creates a new bash fire script that mirrors `m2_mtcojo.smk` argv
WITHOUT invoking snakemake. This is the established Wave 2/3 bypass pattern,
documented as "Rule 3 deviation" in M2 closeout (precedents:
`bin/fire_m2_04_mtcojo.sh` Wave 4-D4, Wave 2-D6, Wave 5).

Rule for this task:
  1. The Snakemake rule `m2_mtcojo.smk` REMAINS canonical — do NOT modify it.
  2. The new fire script duplicates (with the HM3 extension) the per-target
     argv emitted by the rule's `shell:` block.
  3. If a future re-fire needs to be re-discoverable from the snakemake DAG,
     the rule's contract is the source of truth; this bash driver is a
     bypass-only LSF launcher.
</wave_2_3_bypass_pattern>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend build_cojo_inputs.py with optional HM3-intersection flag (TDD)</name>
  <files>
    src/python/build_cojo_inputs.py
    tests/m2/test_build_cojo_inputs_hm3.py
  </files>
  <behavior>
    pytest contract for the extended materializer (write tests FIRST, run RED, then implement, run GREEN):

    Test 1 — `test_materialize_cojo_no_flag_preserves_legacy_behavior`:
      Given a fixture harmonized .tsv.bgz with 50 SNPs (10 of which are NOT in a
      tiny HM3 fixture set), when `materialize_cojo(..., hm3_snps=None)` is called,
      the output COJO file MUST contain all 50 SNPs (after dedup/dropna). Asserts
      legacy behavior is unchanged when flag is absent.

    Test 2 — `test_materialize_cojo_with_hm3_intersects`:
      Given the same fixture and a 40-SNP HM3 set (covering 40 of the 50 fixture
      SNPs), when `materialize_cojo(..., hm3_snps={...})` is called, the output
      COJO file MUST contain exactly the 40 SNPs in the intersection. Asserts
      non-HM3 SNPs are dropped before write.

    Test 3 — `test_build_inputs_passes_hm3_through`:
      Patch `materialize_cojo` (mock); call `build_inputs(..., hm3_snplist=Path)`.
      Assert that for each per-trait call, `hm3_snps` is passed as a `set[str]`
      whose contents match the SNP column of the snplist file.

    Test 4 — `test_build_inputs_no_hm3_passes_none`:
      Patch `materialize_cojo`; call `build_inputs(..., hm3_snplist=None)`.
      Assert `hm3_snps=None` is forwarded on every call (legacy default).

    Test 5 — `test_cli_accepts_hm3_snplist_flag`:
      Invoke the script via subprocess with `--hm3-snplist <fixture>` plus the
      existing required flags pointing at minimal fixtures. Assert exit 0 AND
      that the materializer wrote intersected COJO files. (Smoke-level CLI test.)

    Fixtures:
      - tests/m2/fixtures/ likely already has helpers; create
        tests/m2/fixtures/hm3_mini.snplist  (header `SNP\tA1\tA2` + 5 rs IDs)
        OR generate inline via tmp_path.
      - For the harmonized-input fixture, follow the pattern in
        tests/m2/test_mtcojo_eligible_targets.py / test_build_mtag_residcov_slice.py
        (read a tmp `.tsv.bgz` via `gzip.open` write — gzip-compressed TSV is
        what `pd.read_csv(..., compression='gzip')` expects per existing code).

    All 5 tests MUST be in tests/m2/test_build_cojo_inputs_hm3.py — a NEW file
    (do not append to an existing test module).
  </behavior>
  <action>
    RED phase — write the test file FIRST:

    Create `tests/m2/test_build_cojo_inputs_hm3.py` with the 5 tests above. Use
    pytest + pandas + tmp_path. Import the production module via:
        sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "src" / "python"))
        import build_cojo_inputs
    (mirror tests/m2/conftest.py if it already wires sys.path; do NOT duplicate.)

    Run: `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m2/test_build_cojo_inputs_hm3.py -x -v`
    Expect RED (`hm3_snps` arg / `--hm3-snplist` flag don't exist yet).

    Commit the failing tests:
      git add tests/m2/test_build_cojo_inputs_hm3.py
      git commit -m "test(quick-260429-s10): add failing pytest for HM3-intersected COJO materializer (M2-POST-M3-08)

      Per m2_post_m3_rerun_queue.tsv row M2-POST-M3-08 dependency_blockers:
      'HM3-intersected COJO input materializer (extension of
      src/python/build_cojo_inputs.py)'.

      Witness: bmi.EUR.GIANT-UKBB.2018.mtcojo.fire.log GCTA error
      'no SNP in common between the summary data and the LD score files'
      after reading 75,783 GW-sig SNPs into PLINK memory. Pre-restricting
      COJO inputs to HM3 namespace places GCTA's internal LDSC step
      inside the eur_w_ld_chr ld-score namespace.

      Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"

    GREEN phase — extend src/python/build_cojo_inputs.py:

    1. Add `hm3_snps: set[str] | None = None` kwarg to `materialize_cojo`. After
       the `df.dropna(subset=["SNP", "A1", "A2", "p"])` line (current line 69)
       and BEFORE the `df.dropna(subset=["b","se","freq","N"])` step, insert:

           if hm3_snps is not None:
               df = df[df["SNP"].isin(hm3_snps)]

       Place the HM3 filter BEFORE duplicate-drop so duplicates outside HM3
       don't shadow valid in-HM3 rows.

    2. Add `hm3_snplist: Path | None = None` kwarg to `build_inputs`. At the top
       of the function (after `trait_order = side["trait_order"]`), load:

           hm3_snps: set[str] | None = None
           if hm3_snplist is not None:
               hm3_df = pd.read_csv(hm3_snplist, sep=r"\s+", engine="python")
               hm3_snps = set(hm3_df["SNP"].astype(str))
               print(f"  hm3: loaded {len(hm3_snps)} SNPs from {hm3_snplist}")

       Pass `hm3_snps=hm3_snps` into the `materialize_cojo(...)` call inside
       the `for trait in trait_order:` loop.

       IMPORTANT — cache invalidation: the existing skip-if-exists guard
       (`if not cojo_path.exists() or cojo_path.stat().st_size == 0`) will NOT
       re-materialize files that were written WITHOUT HM3 intersection. The LSF
       driver in Task 2 sidesteps this by writing per-target COJO files into a
       UNIQUE per-target subdir (so no stale legacy cache exists). DO NOT add
       cache-busting logic to build_cojo_inputs.py — keep the materializer pure
       and let the driver own output path uniqueness.

    3. Add `--hm3-snplist` CLI flag in `_main`:

           ap.add_argument("--hm3-snplist", type=Path, default=None,
                           help="Optional HM3 SNP-list TSV (cols: SNP A1 A2 + header). "
                                "When provided, per-trait COJO files are intersected to "
                                "HM3 SNPs before write. Required for mtCOJO LSF re-fire "
                                "(M2-POST-M3-08): GCTA's internal LDSC step requires "
                                "input SNPs lie within the eur_w_ld_chr namespace.")

       Pass `hm3_snplist=args.hm3_snplist` into `build_inputs(...)`.

    Run pytest again, expect GREEN:
        /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m2/test_build_cojo_inputs_hm3.py -x -v

    Commit the implementation:
      git add src/python/build_cojo_inputs.py
      git commit -m "feat(quick-260429-s10): add --hm3-snplist flag to build_cojo_inputs (M2-POST-M3-08)

      Default OFF preserves legacy genome-wide behavior. When supplied,
      per-trait COJO files are intersected to the HapMap3 SNP namespace
      before write so GCTA mtCOJO's internal LDSC bivariate-intercept
      step receives an input whose SNP namespace lies inside the
      eur_w_ld_chr ld-score namespace.

      Closes the dependency_blocker on M2-POST-M3-08:
      'HM3-intersected COJO input materializer'.

      Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"

    DO NOT touch select_mtcojo_eligible_targets.py (eligibility is settled).
    DO NOT touch m2_mtcojo.smk (canonical Snakemake rule must remain
    re-discoverable; bash driver is bypass-only per Wave 2/3 pattern).
    DO NOT touch build_mtcojo_sensitivity_table.py (the bmi.EUR-missing-row
    issue is OUT OF SCOPE for this quick task — see research_notes).
  </action>
  <verify>
    <automated>/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m2/test_build_cojo_inputs_hm3.py -x -v</automated>
  </verify>
  <done>
    All 5 pytest cases PASS. `build_cojo_inputs.py --help` shows `--hm3-snplist`
    flag. Two atomic commits land (RED test commit + GREEN feature commit).
    Legacy invocation (no `--hm3-snplist`) still produces identical COJO files
    to pre-extension (verified by Test 1 + Test 4).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write bin/fire_m2_post_m3_08_mtcojo.sh LSF burst-fire driver</name>
  <files>
    bin/fire_m2_post_m3_08_mtcojo.sh
  </files>
  <action>
    Create `bin/fire_m2_post_m3_08_mtcojo.sh` — a bash driver that submits 13
    independent LSF jobs (one per eligible (stratum, target) tuple) to queue
    `long` and returns immediately with a job manifest. Each LSF jobscript runs
    HM3-intersected COJO materialization → mbfile build → GCTA mtCOJO →
    completion sentinel.

    File header (mandatory comment block):

        #!/usr/bin/env bash
        # bin/fire_m2_post_m3_08_mtcojo.sh — M2-POST-M3-08 LSF burst-fire driver
        #
        # Closes obligation row M2-POST-M3-08 in
        # .planning/m2_post_m3_rerun_queue.tsv:
        #   "mtCOJO production sensitivity LSF re-fire for the 13 eligible
        #    (target_trait, stratum) tuples with HM3-intersected COJO inputs"
        #
        # Mirrors src/snakemake/rules/m2_mtcojo.smk argv exactly (Wave 2/3
        # bypass pattern; documented as Rule 3 deviation). The Snakemake rule
        # remains canonical for re-discoverability — this driver is a bypass.
        #
        # Failure-mode addressed (witness:
        # data/processed/mtcojo/EUR/bmi.EUR.GIANT-UKBB.2018.mtcojo.fire.log):
        #   GCTA mtCOJO LDSC step terminates "no SNP in common between the
        #   summary data and the LD score files". HM3 intersection of COJO
        #   inputs upstream places GCTA's SNP namespace inside the
        #   eur_w_ld_chr ld-score namespace.
        #
        # LSF resources (per m2_mtcojo.smk: mem_mb=8000, threads=4):
        #   -q long                  → -W 14400 via config/bsub_wrapper.sh
        #   -n 4                     → 4 cores
        #   -R "rusage[mem=8]"       → 8 GB (LSF_UNIT_FOR_LIMITS=GB)
        #
        # Wall-time expectation: ~30 min per target × 13 targets, fully
        # parallel on long queue → manifest delivered immediately.
        set -euo pipefail

    Variables block:

        REPO=/gpfs_common/share01/clintonlab/ckclinto/coloc_analysis
        GCTA=/rs1/researchers/c/ckclinto/conda_envs/gcta/bin/gcta-1.94.1
        PYTHON=/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python
        HM3_SNPLIST=$REPO/tools/mtag/ld_ref_panel/eur_w_ld_chr/w_hm3.snplist
        LDSC_DIR=$REPO/data/external/ldscore/eur_w_ld_chr
        # Per-task output root: data/processed/mtcojo/<stratum>/m2p3_08/<target>/
        # (unique per-target subdir to avoid clobbering legacy cojo_inputs/ from
        # the failed first-pass fire — see build_cojo_inputs.py cache note.)

        cd "$REPO"
        mkdir -p logs/lsf data/processed/mtcojo

        QUICK_DIR=".planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen"
        mkdir -p "$QUICK_DIR"
        BJOBS_TSV="$QUICK_DIR/bjobs.tsv"
        BJOBS_COMMITTED="data/processed/mtcojo/m2_post_m3_08_bjobs.tsv"

        # Manifest header
        printf "submit_ts\tjob_id\tstratum\ttarget_trait\tld_ref\tjobscript\n" > "$BJOBS_TSV"

    Iteration: read each of the 3 eligible_targets.tsv files. For each row
    (skipping the header), build a stratum-specific LD-ref prefix per the
    D-M2-Q3 dispatch (EUR/TRANS → 1000G_EUR_Phase3_plink; AFR → 1000G_AFR_Phase3_plink).
    Generate a per-job script under `logs/lsf/m2p3_08_<stratum>_<short>.sh`
    where `<short>` = the part of the trait_key before the first dot (e.g.
    bmi, hdl, ldl, tc, tg) — use this short tag in `-J` and `-o` only; the
    FULL trait_key remains the source of truth in the manifest and inside
    the jobscript.

    Per-target jobscript template (written to logs/lsf/m2p3_08_<stratum>_<short>.sh):

        #!/usr/bin/env bash
        set -euo pipefail
        cd "$REPO"

        STRATUM="<stratum>"
        TARGET="<full_trait_key>"
        LD_REF="<stratum_ld_ref_prefix>"   # e.g. data/reference/ldsc/1000G_EUR_Phase3_plink/1000G.EUR.QC

        OUTDIR="data/processed/mtcojo/$STRATUM/m2p3_08/$TARGET"
        mkdir -p "$OUTDIR/cojo_inputs"

        # Step 1: HM3-intersected COJO materialization
        PYTHONPATH="$REPO/src/python:${PYTHONPATH:-}" "$PYTHON" \
            "$REPO/src/python/build_cojo_inputs.py" \
            --target "$TARGET" \
            --stratum "$STRATUM" \
            --sidecar "data/processed/mtag/$STRATUM/residcov.trait_order.json" \
            --harmonized-dir "data/processed/sumstats_harmonized" \
            --hm3-snplist "$HM3_SNPLIST" \
            --out-dir "$OUTDIR/cojo_inputs"

        # Step 2: build mbfile from per-chr 1000G {EUR|AFR} bfiles
        MBFILE="$OUTDIR/.mbfile"
        : > "$MBFILE"
        for c in $(seq 1 22); do
            BF="${LD_REF}.${c}"
            if [ -f "${BF}.bed" ]; then
                echo "${BF}" >> "$MBFILE"
            fi
        done

        # Step 3: GCTA mtCOJO (mirrors m2_mtcojo.smk argv exactly)
        OUT_PREFIX="$OUTDIR/${TARGET}.mtcojo"
        "$GCTA" \
            --mtcojo-file "$OUTDIR/cojo_inputs/${TARGET}.mtcojo.list" \
            --mbfile "$MBFILE" \
            --w-ld-chr "$LDSC_DIR/" \
            --ref-ld-chr "$LDSC_DIR/" \
            --out "$OUT_PREFIX" \
            > "$OUT_PREFIX.fire.log" 2>&1

        # Step 4: canonicalize output path (GCTA may write .mtcojo.cma.cojo)
        if [ ! -f "$OUT_PREFIX.cojo" ]; then
            for ext in .mtcojo.cma.cojo .cma.cojo .cma _cojo; do
                cand="${OUT_PREFIX}${ext}"
                if [ -f "$cand" ]; then cp "$cand" "$OUT_PREFIX.cojo"; break; fi
            done
        fi
        test -s "$OUT_PREFIX.cojo"   # MUST be non-empty on success

        # Step 5: completion sentinel (used by the harvest runbook)
        touch "$OUTDIR/${TARGET}.bjob.done"

    Submission loop in the driver:

        for STRATUM in EUR AFR TRANS; do
            ELIG="$REPO/data/processed/mtcojo/$STRATUM/mtcojo_eligible_targets.tsv"
            if [ ! -s "$ELIG" ]; then
                echo "[fire_m2_post_m3_08] WARN: no eligible_targets for $STRATUM — skipping"
                continue
            fi

            if [ "$STRATUM" = "AFR" ]; then
                LD_REF="data/reference/ldsc/1000G_AFR_Phase3_plink/1000G.AFR.QC"
            else
                LD_REF="data/reference/ldsc/1000G_EUR_Phase3_plink/1000G.EUR.QC"
            fi

            tail -n +2 "$ELIG" | while IFS=$'\t' read -r TARGET _rest; do
                [ -z "$TARGET" ] && continue
                SHORT="${TARGET%%.*}"   # e.g. bmi.EUR.GIANT-UKBB.2018 → bmi
                JOBSCRIPT="logs/lsf/m2p3_08_${STRATUM}_${SHORT}.sh"

                # render jobscript with substitution (use a heredoc with
                # explicit variable expansion of STRATUM, TARGET, LD_REF only)
                cat > "$JOBSCRIPT" <<EOF
        #!/usr/bin/env bash
        set -euo pipefail
        cd "$REPO"
        STRATUM="$STRATUM"
        TARGET="$TARGET"
        LD_REF="$LD_REF"
        # ... (full per-target body from the template above, with $REPO,
        # $PYTHON, $GCTA, $HM3_SNPLIST, $LDSC_DIR pre-resolved at render time)
        EOF
                chmod +x "$JOBSCRIPT"

                # Submit
                BSUB_OUT=$(bsub \
                    -q long \
                    -n 4 \
                    -R "rusage[mem=8]" \
                    -J "m2p3_08_${STRATUM}_${SHORT}" \
                    -o "logs/lsf/m2p3_08_${STRATUM}_${SHORT}.out" \
                    -e "logs/lsf/m2p3_08_${STRATUM}_${SHORT}.err" \
                    < "$JOBSCRIPT")
                JOB_ID=$(echo "$BSUB_OUT" | grep -oP '(?<=Job <)[0-9]+(?=>)' || echo "UNKNOWN")
                SUBMIT_TS=$(date -Iseconds)
                printf "%s\t%s\t%s\t%s\t%s\t%s\n" \
                    "$SUBMIT_TS" "$JOB_ID" "$STRATUM" "$TARGET" "$LD_REF" "$JOBSCRIPT" \
                    >> "$BJOBS_TSV"
                echo "[fire_m2_post_m3_08] submitted $STRATUM/$TARGET as job $JOB_ID"
            done
        done

        cp "$BJOBS_TSV" "$BJOBS_COMMITTED"
        echo "[fire_m2_post_m3_08] DONE. Manifest: $BJOBS_TSV (also $BJOBS_COMMITTED)"

    Make the file executable: `chmod +x bin/fire_m2_post_m3_08_mtcojo.sh`.

    DO NOT execute the driver in this task — Task 3 (smoke gate) runs first.
    Only WRITE the script + commit it.

    Commit:
      git add bin/fire_m2_post_m3_08_mtcojo.sh
      git commit -m "feat(quick-260429-s10): add LSF burst-fire driver for M2-POST-M3-08

      bin/fire_m2_post_m3_08_mtcojo.sh — submits 13 LSF long-queue jobs
      (one per eligible target × stratum) for the M2 mtCOJO production
      sensitivity re-fire with HM3-intersected COJO inputs.

      Mirrors src/snakemake/rules/m2_mtcojo.smk argv exactly (Wave 2/3
      bypass pattern, Rule 3 deviation; Snakemake rule remains canonical
      for re-discoverability). Each per-target jobscript materializes
      HM3-intersected COJO files into a unique per-target output subdir
      to avoid stale legacy cache, then invokes GCTA mtCOJO with the
      stratum-specific LD reference (D-M2-Q3 dispatch).

      Driver writes a job manifest to .planning/quick/.../bjobs.tsv and
      copies it to data/processed/mtcojo/m2_post_m3_08_bjobs.tsv. The
      driver returns immediately after submitting all 13 jobs; results
      are harvested when LSF jobs complete (per HARVEST-RUNBOOK.md).

      Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
  </action>
  <verify>
    <automated>bash -n bin/fire_m2_post_m3_08_mtcojo.sh && grep -E "(--hm3-snplist|--mtcojo-file|--w-ld-chr|--ref-ld-chr|-q long|rusage\[mem=8\]|-n 4)" bin/fire_m2_post_m3_08_mtcojo.sh | wc -l | awk '$1 >= 6 {print "ARGV-CONTRACT-OK"} $1 < 6 {print "ARGV-CONTRACT-MISSING"; exit 1}'</automated>
  </verify>
  <done>
    Driver script exists, is executable, passes `bash -n` syntax check, mirrors
    all required GCTA argv (`--mtcojo-file`, `--w-ld-chr`, `--ref-ld-chr`) plus
    the new `--hm3-snplist` flag, and uses the LSF resource pins
    (`-q long`, `-n 4`, `rusage[mem=8]`). NOT executed yet.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pre-flight smoke gate — interactive single-target HM3 mtCOJO run (NO bsub)</name>
  <files>
    (no committed files — produces /tmp staging artifacts only; smoke log printed to console)
  </files>
  <action>
    RIGOR GATE — per Carter's standing rule "always pick rigor over time-saving".
    Before bsubbing 13 production jobs, confirm interactively that HM3 intersection
    actually unblocks GCTA's LDSC step. The witness target is bmi.EUR (the trait
    that produced the failure log; reproducing its success here closes the
    diagnostic loop).

    Steps:

    1. Set up a /tmp staging dir (NOT in repo tree, to avoid contaminating the
       LSF output paths the driver expects):

         SMOKE_DIR=$(mktemp -d -t m2p3_08_smoke_XXXXXX)
         echo "[smoke] staging dir: $SMOKE_DIR"

    2. Materialize HM3-intersected COJO inputs for bmi.EUR:

         REPO=/gpfs_common/share01/clintonlab/ckclinto/coloc_analysis
         PYTHON=/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python
         HM3=$REPO/tools/mtag/ld_ref_panel/eur_w_ld_chr/w_hm3.snplist

         PYTHONPATH="$REPO/src/python" "$PYTHON" \
             "$REPO/src/python/build_cojo_inputs.py" \
             --target bmi.EUR.GIANT-UKBB.2018 \
             --stratum EUR \
             --sidecar "$REPO/data/processed/mtag/EUR/residcov.trait_order.json" \
             --harmonized-dir "$REPO/data/processed/sumstats_harmonized" \
             --hm3-snplist "$HM3" \
             --out-dir "$SMOKE_DIR/cojo_inputs"

       Sanity check: each trait's .cojo file should have ≤ ~1.22 M rows
       (HM3 ceiling), not the legacy 2-11 M:

         for f in "$SMOKE_DIR"/cojo_inputs/*.cojo; do
             echo "[smoke] $(basename $f): $(wc -l < $f) rows"
         done
       Expect each trait_key.cojo to be 600k-1.2M rows.

    3. Build mbfile (1000G_EUR_Phase3_plink, 22 chr):

         MBFILE="$SMOKE_DIR/.mbfile"
         : > "$MBFILE"
         for c in $(seq 1 22); do
             BF="$REPO/data/reference/ldsc/1000G_EUR_Phase3_plink/1000G.EUR.QC.${c}"
             [ -f "${BF}.bed" ] && echo "${BF}" >> "$MBFILE"
         done

    4. Run GCTA mtCOJO INTERACTIVELY (no bsub) — capture full log to console:

         GCTA=/rs1/researchers/c/ckclinto/conda_envs/gcta/bin/gcta-1.94.1
         "$GCTA" \
             --mtcojo-file "$SMOKE_DIR/cojo_inputs/bmi.EUR.GIANT-UKBB.2018.mtcojo.list" \
             --mbfile "$MBFILE" \
             --w-ld-chr "$REPO/data/external/ldscore/eur_w_ld_chr/" \
             --ref-ld-chr "$REPO/data/external/ldscore/eur_w_ld_chr/" \
             --out "$SMOKE_DIR/bmi.EUR.GIANT-UKBB.2018.mtcojo" \
             2>&1 | tee "$SMOKE_DIR/smoke.log"

       Expected wall: 10-30 min on login01.

    5. Decision gate — read the smoke log and check for the witness phrase that
       distinguishes a passing run from the prior failure:

         FAIL_PHRASE="no SNP in common between the summary data and the LD score files"
         LDSC_PHRASE="LD score regression"

         if grep -q "$FAIL_PHRASE" "$SMOKE_DIR/smoke.log"; then
             echo "[smoke] FAILURE — HM3 intersection did NOT unblock GCTA. ABORT before bsub."
             echo "[smoke] log: $SMOKE_DIR/smoke.log"
             exit 1
         fi

         if ! grep -qi "$LDSC_PHRASE" "$SMOKE_DIR/smoke.log"; then
             echo "[smoke] WARNING — GCTA log does NOT mention LD score regression."
             echo "[smoke] log may have terminated early; inspect manually before bsub."
             exit 1
         fi

         # Also require the .cojo file to be non-empty (the production
         # success criterion):
         OUT_COJO="$SMOKE_DIR/bmi.EUR.GIANT-UKBB.2018.mtcojo.mtcojo.cma.cojo"
         [ -s "$OUT_COJO" ] || OUT_COJO="$SMOKE_DIR/bmi.EUR.GIANT-UKBB.2018.mtcojo.cojo"
         if [ ! -s "$OUT_COJO" ]; then
             echo "[smoke] FAILURE — no non-empty .cojo output produced. ABORT."
             exit 1
         fi

         echo "[smoke] PASS — HM3 intersection unblocks GCTA LDSC step."
         echo "[smoke] artifacts in: $SMOKE_DIR"

    6. If smoke FAILS: STOP. Do NOT proceed to Task 4. Report the smoke log
       contents to the user and request guidance — possibilities:
         (a) HM3 intersection is correct but GCTA needs an even more restrictive
             SNP set (e.g., HapMap3 ∩ MAF>0.05 in 1000G EUR). Document and
             escalate as a follow-up obligation.
         (b) The harmonized sumstats namespace doesn't fully overlap HM3 SNP
             IDs (e.g., chr:pos vs rsID encoding mismatch). Inspect a few
             rows of the materialized .cojo file vs the snplist.

    7. If smoke PASSES: tear down /tmp dir (optional — can leave for forensic
       inspection) and proceed to Task 4.

    NO commit on this task. The smoke run produces only /tmp artifacts.
  </action>
  <verify>
    <automated>echo "Smoke gate verification is the explicit grep checks in step 5 of the action; this task's <automated> is the smoke run itself completing without the witness FAIL_PHRASE."</automated>
  </verify>
  <done>
    Smoke run for bmi.EUR.GIANT-UKBB.2018 completes; smoke.log contains "LD
    score regression" wording AND does NOT contain the witness "no SNP in
    common between the summary data and the LD score files" phrase; a
    non-empty .cojo file is produced. Console reports "[smoke] PASS".
  </done>
</task>

<task type="auto">
  <name>Task 4: Bsub the 13 LSF production jobs + commit job manifest</name>
  <files>
    data/processed/mtcojo/m2_post_m3_08_bjobs.tsv
    .planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/bjobs.tsv
    (logs/lsf/*.sh + *.out + *.err are gitignored — NOT committed)
  </files>
  <action>
    Precondition: Task 3 (smoke gate) reported "[smoke] PASS". If not, ABORT.

    1. Confirm prerequisites are present:

         test -x bin/fire_m2_post_m3_08_mtcojo.sh
         test -s data/processed/mtcojo/EUR/mtcojo_eligible_targets.tsv     # 5 + header rows
         test -s data/processed/mtcojo/AFR/mtcojo_eligible_targets.tsv     # 4 + header rows
         test -s data/processed/mtcojo/TRANS/mtcojo_eligible_targets.tsv   # 4 + header rows
         test -s tools/mtag/ld_ref_panel/eur_w_ld_chr/w_hm3.snplist        # ~1.22M rows

    2. Confirm LSF login is alive:

         bjobs -u $USER 2>&1 | head -3
         # If this errors with auth failure, that's a checkpoint moment —
         # report and stop. Do NOT auto-retry forever.

    3. Fire:

         cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis
         bash bin/fire_m2_post_m3_08_mtcojo.sh

       Expected console output:
         [fire_m2_post_m3_08] submitted EUR/bmi.EUR.GIANT-UKBB.2018 as job <id>
         [fire_m2_post_m3_08] submitted EUR/hdl.EUR.GLGC.2021 as job <id>
         ...  (13 lines total)
         [fire_m2_post_m3_08] DONE. Manifest: .../bjobs.tsv

    4. Verify the manifest has 13 data rows (+ 1 header):

         test "$(wc -l < .planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/bjobs.tsv)" -eq 14

       If row count != 14, investigate. Likely cause: an `eligible_targets.tsv`
       was malformed or a bsub returned non-zero. Inspect the manifest's
       job_id column for any "UNKNOWN" entries.

    5. Verify all 13 jobs are visible to bjobs:

         JOBS=$(awk 'NR>1 {print $2}' .planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/bjobs.tsv | tr '\n' ' ')
         bjobs -w $JOBS | head -20
         # Most should be in PEND or RUN; none should be EXIT/DONE this fast.

    6. Commit ONLY the manifest copy that lives in `data/processed/`:

         git add data/processed/mtcojo/m2_post_m3_08_bjobs.tsv
         git commit -m "feat(quick-260429-s10): submit 13 LSF jobs for M2-POST-M3-08 mtCOJO production fire

         Manifest: data/processed/mtcojo/m2_post_m3_08_bjobs.tsv

         13 LSF long-queue jobs submitted via bin/fire_m2_post_m3_08_mtcojo.sh
         (Wave 2/3 bypass driver). One job per eligible (stratum, target_trait)
         tuple from data/processed/mtcojo/{EUR,AFR,TRANS}/mtcojo_eligible_targets.tsv:

           EUR (5):   bmi, hdl, ldl, tc, tg
           AFR (4):   hdl, ldl, tc, tg
           TRANS (4): hdl, ldl, tc, tg

         Pre-flight smoke gate (interactive bmi.EUR mtCOJO with HM3-intersected
         COJO inputs) confirmed HM3 intersection unblocks GCTA's LDSC
         bivariate-intercept step before submission.

         LSF wall expectation: ~30 min/target × 13 in parallel ≈ 30 min real
         time; budgeted under long-queue 14400-min cap with significant margin.

         Harvest procedure: see HARVEST-RUNBOOK.md.

         Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"

       NOTE: The driver writes per-target jobscripts under `logs/lsf/*.sh` and
       LSF stdout/stderr to `logs/lsf/*.out` / `logs/lsf/*.err`. These should
       NOT be committed (they're typically .gitignored). Per Carter's standing
       rule "Multi-terminal git staging — explicit paths only on GPFS, never
       `git add .` / `-A`", the commit above adds ONLY the manifest TSV.

       The .planning/.../bjobs.tsv copy is committed in Task 5 alongside the
       runbook + queue update (single .planning commit).

    DO NOT wait for jobs to complete. The driver returns immediately.
  </action>
  <verify>
    <automated>test -s data/processed/mtcojo/m2_post_m3_08_bjobs.tsv && test "$(wc -l < data/processed/mtcojo/m2_post_m3_08_bjobs.tsv)" -eq 14 && awk 'NR>1 && $2 == "UNKNOWN" {bad++} END {exit (bad ? 1 : 0)}' data/processed/mtcojo/m2_post_m3_08_bjobs.tsv</automated>
  </verify>
  <done>
    `data/processed/mtcojo/m2_post_m3_08_bjobs.tsv` exists with exactly 14 lines
    (1 header + 13 data rows), no UNKNOWN job IDs, and is committed. All 13
    LSF jobs are visible to `bjobs -w` (PEND or RUN; none EXIT). Manifest
    duplicate at `.planning/quick/.../bjobs.tsv` exists (committed in Task 5).
  </done>
</task>

<task type="auto">
  <name>Task 5: Update obligation queue + write HARVEST-RUNBOOK.md</name>
  <files>
    .planning/m2_post_m3_rerun_queue.tsv
    .planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/HARVEST-RUNBOOK.md
    .planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/bjobs.tsv
  </files>
  <action>
    Step A — Update obligation queue.

    The current header row of `.planning/m2_post_m3_rerun_queue.tsv` is:

      obligation_id source_decision description current_artifact supersede_artifact dependency_blockers priority owner queued_date

    Add three new columns at the end: `status`, `submit_ts`, `lsf_job_ids`.
    Backfill all 8 existing rows with `not_started	-	-`. Set the M2-POST-M3-08
    row to `in_flight	<ISO_8601_timestamp>	<comma_separated_job_ids>`.

    Recommended approach (preserves existing rows verbatim):

      python3 - <<'PY'
      import csv, os, datetime
      from pathlib import Path

      repo = Path("/gpfs_common/share01/clintonlab/ckclinto/coloc_analysis")
      qfile = repo / ".planning/m2_post_m3_rerun_queue.tsv"
      bjobs = repo / "data/processed/mtcojo/m2_post_m3_08_bjobs.tsv"

      # Load bjobs manifest to get job IDs + earliest submit_ts
      with bjobs.open() as f:
          rows = list(csv.DictReader(f, delimiter="\t"))
      job_ids = ",".join(r["job_id"] for r in rows)
      submit_ts = min(r["submit_ts"] for r in rows)

      # Load + rewrite queue
      with qfile.open() as f:
          rdr = csv.reader(f, delimiter="\t")
          header = next(rdr)
          data = list(rdr)

      new_cols = ["status", "submit_ts", "lsf_job_ids"]
      if not all(c in header for c in new_cols):
          header = header + new_cols
          for row in data:
              if row[0] == "M2-POST-M3-08":
                  row += ["in_flight", submit_ts, job_ids]
              else:
                  row += ["not_started", "-", "-"]
      else:
          # Idempotent re-run: just update the M2-POST-M3-08 row
          idx = {c: i for i, c in enumerate(header)}
          for row in data:
              if row[0] == "M2-POST-M3-08":
                  row[idx["status"]] = "in_flight"
                  row[idx["submit_ts"]] = submit_ts
                  row[idx["lsf_job_ids"]] = job_ids

      with qfile.open("w", newline="") as f:
          w = csv.writer(f, delimiter="\t", lineterminator="\n")
          w.writerow(header)
          w.writerows(data)
      print(f"queue updated: M2-POST-M3-08 status=in_flight submit_ts={submit_ts}")
      PY

    Verify exit 0 + that `M2-POST-M3-08	...	in_flight` appears in the file.

    Step B — Write HARVEST-RUNBOOK.md at:

      .planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/HARVEST-RUNBOOK.md

    Required sections (bullet-list / numbered-list format; this is a runbook,
    not a discussion document):

      # M2-POST-M3-08 Harvest Runbook

      ## Why this exists
      One-paragraph: 13 LSF jobs in flight; harvest = post-LSF aggregation.

      ## 1. Monitoring (during flight)
      - `bjobs -u $USER -J 'm2p3_08_*' -w` — list all M2-POST-M3-08 jobs
      - `bjobs $(awk 'NR>1 {print $2}' data/processed/mtcojo/m2_post_m3_08_bjobs.tsv | tr '\n' ' ')`
      - LSF stdout: `tail -f logs/lsf/m2p3_08_<stratum>_<short>.out`
      - GCTA log:   `tail -f data/processed/mtcojo/<stratum>/m2p3_08/<target>/<target>.mtcojo.fire.log`

      ## 2. Detecting completion
      Each per-target jobscript writes a `.bjob.done` sentinel as its LAST step.
      Count complete jobs:
        find data/processed/mtcojo/{EUR,AFR,TRANS}/m2p3_08 -name '*.bjob.done' | wc -l
      Target: 13. When count == 13, harvest.

      Also confirm via LSF:
        bjobs -d $(awk 'NR>1 {print $2}' data/processed/mtcojo/m2_post_m3_08_bjobs.tsv | tr '\n' ' ') | grep -c DONE
      Target: 13.

      Per-target failure check:
        for f in data/processed/mtcojo/{EUR,AFR,TRANS}/m2p3_08/*/*.mtcojo.fire.log; do
            if grep -q "no SNP in common" "$f"; then echo "STILL FAILING: $f"; fi
        done

      ## 3. Migrating outputs into the canonical sensitivity layout
      The driver writes COJO outputs under
        data/processed/mtcojo/<stratum>/m2p3_08/<target>/<target>.mtcojo.cojo
      but `build_mtcojo_sensitivity_table.py` reads from
        data/processed/mtcojo/<stratum>/<target>.mtcojo.cojo

      Either symlink or copy each target's .cojo into the canonical location
      BEFORE running the aggregator:
        for STRATUM in EUR AFR TRANS; do
            for d in data/processed/mtcojo/$STRATUM/m2p3_08/*/; do
                target=$(basename "$d")
                cp "${d}${target}.mtcojo.cojo" "data/processed/mtcojo/$STRATUM/${target}.mtcojo.cojo"
            done
        done

      ## 4. Rebuild the sensitivity tables
      For each stratum:
        /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python \
            src/python/build_mtcojo_sensitivity_table.py \
            --stratum <STRATUM> \
            --eligible data/processed/mtcojo/<STRATUM>/mtcojo_eligible_targets.tsv \
            --mtcojo-dir data/processed/mtcojo/<STRATUM> \
            --mtag-filtered data/processed/mtag/<STRATUM>/<STRATUM>_mtag_maxfdr_filtered.txt \
            --out data/processed/mtcojo/<STRATUM>/mtcojo_sensitivity.tsv

      Expected: sensitivity_flag column has PASS / WARN / FAIL classifications,
      not all-FAIL.

      ## 5. Verify bmi.EUR is included (current pre-fire artifact has only 4 EUR rows; target = 5)
      Open data/processed/mtcojo/EUR/mtcojo_sensitivity.tsv and confirm a row
      with trait=bmi.EUR.GIANT-UKBB.2018 exists.

      If bmi.EUR is STILL missing (i.e., the cojo file from the LSF re-fire is
      empty OR build_mtcojo_sensitivity_table.py drops it):
        a. Inspect data/processed/mtcojo/EUR/bmi.EUR.GIANT-UKBB.2018.mtcojo.cojo
           — non-empty?
        b. If non-empty, the aggregator at line ~63 of
           build_mtcojo_sensitivity_table.py emits NO row when no MTAG-novel
           SNP intersects the cojo output. This is by design but produces a
           silent omission. Open a follow-up quick task to add a "no-intersect
           FAIL row" emission so every eligible target appears in the table.
        c. If empty, GCTA still failed for bmi.EUR specifically — inspect
           bmi.EUR.GIANT-UKBB.2018.mtcojo.fire.log for residual issues.

      ## 6. Status flip in the obligation queue
      Once mtcojo_sensitivity.tsv has non-FAIL rows, edit
      .planning/m2_post_m3_rerun_queue.tsv row M2-POST-M3-08:
        status: in_flight  →  completed
        Add new column `completed_ts` if desired.

      ## 7. Out-of-scope items called out for follow-up
      - bmi.EUR-missing-from-sensitivity-table: see §5(b). Likely a separate
        quick task on build_mtcojo_sensitivity_table.py.
      - AFR LD-score supersede (M2-POST-M3-05) and AoU AFR LD panel
        (M2-POST-M3-01/03) are NOT addressed by this re-fire — those remain
        open obligations.
      - TRANS AFR-LD sensitivity (M2-POST-M3-04) is a separate obligation.
      - This re-fire uses the cross-ancestry EUR ld-score approximation
        (D-M2-Q2) for ALL strata including AFR. AFR rows of the re-fired
        sensitivity table will need to be re-derived again under M2-POST-M3-05
        once AoU AFR ld-scores land.

    Step C — Copy the bjobs.tsv into the .planning quick dir if not already there.

      cp data/processed/mtcojo/m2_post_m3_08_bjobs.tsv \
         .planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/bjobs.tsv
      # The driver writes both copies, but if the executor re-ran only
      # parts of Task 4 ensure both exist.

    Commit (single docs commit; explicit paths per Carter's GPFS rule):

      git add .planning/m2_post_m3_rerun_queue.tsv \
              .planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/HARVEST-RUNBOOK.md \
              .planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/bjobs.tsv
      git commit -m "docs(quick-260429-s10): mark M2-POST-M3-08 in_flight + write harvest runbook

      .planning/m2_post_m3_rerun_queue.tsv:
        - Added status, submit_ts, lsf_job_ids columns to schema.
        - M2-POST-M3-08 → status=in_flight with LSF job manifest.
        - Backfilled remaining 7 rows as not_started.

      .planning/quick/260429-s10-.../HARVEST-RUNBOOK.md:
        Documents (a) bjobs monitoring, (b) .bjob.done sentinel detection,
        (c) cojo output migration into canonical sensitivity layout,
        (d) build_mtcojo_sensitivity_table.py invocation, (e) bmi.EUR
        verification + follow-up criteria, (f) queue status flip
        in_flight → completed, (g) explicitly out-of-scope items.

      .planning/quick/260429-s10-.../bjobs.tsv: LSF job manifest copy.

      Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
  </action>
  <verify>
    <automated>grep -E "^M2-POST-M3-08\b.*\bin_flight\b" .planning/m2_post_m3_rerun_queue.tsv && test -s .planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/HARVEST-RUNBOOK.md && test -s .planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/bjobs.tsv && grep -c "^## [0-9]" .planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/HARVEST-RUNBOOK.md | awk '$1 >= 6 {print "RUNBOOK-OK"} $1 < 6 {exit 1}'</automated>
  </verify>
  <done>
    Queue file shows M2-POST-M3-08 status=in_flight with non-empty submit_ts +
    lsf_job_ids columns. HARVEST-RUNBOOK.md exists with all 7 numbered
    sections. bjobs.tsv exists in both `.planning/quick/...` and
    `data/processed/mtcojo/`. Single docs commit lands.
  </done>
</task>

</tasks>

<verification>
Phase-level verification (after all 5 tasks complete):

1. Tests green:
     /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m2/test_build_cojo_inputs_hm3.py -x -v

2. Code shipped:
     git log --oneline -10 | grep -E "(quick-260429-s10|M2-POST-M3-08)"
   Should show 4 commits: RED test, GREEN feature, fire driver, manifest, docs.

3. LSF jobs in flight:
     bjobs -u $USER -J 'm2p3_08_*' -w
   Expect 13 jobs (mix of PEND / RUN; none EXIT).

4. Manifest correctness:
     wc -l data/processed/mtcojo/m2_post_m3_08_bjobs.tsv   # → 14 (1 header + 13 data)

5. Smoke witness (Task 3):
     console output recorded "[smoke] PASS"

6. Obligation queue updated:
     grep "^M2-POST-M3-08" .planning/m2_post_m3_rerun_queue.tsv | grep -q in_flight

7. Runbook present:
     test -s .planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/HARVEST-RUNBOOK.md

8. Canonical Snakemake rule UNTOUCHED:
     git diff HEAD~5 -- src/snakemake/rules/m2_mtcojo.smk | wc -l   # → 0

9. Eligible-targets selector UNTOUCHED:
     git diff HEAD~5 -- src/python/select_mtcojo_eligible_targets.py | wc -l  # → 0
</verification>

<success_criteria>
This quick task is COMPLETE (modulo Carter's harvest step) when:

- [x] `src/python/build_cojo_inputs.py` exposes `--hm3-snplist` flag with default OFF preserving legacy behavior
- [x] 5 pytest cases in `tests/m2/test_build_cojo_inputs_hm3.py` all pass (RED→GREEN cycle visible in git log)
- [x] `bin/fire_m2_post_m3_08_mtcojo.sh` exists, executable, mirrors `m2_mtcojo.smk` argv (Wave 2/3 bypass pattern documented in header), uses correct LSF resources (-q long, -n 4, mem=8GB), dispatches LD-ref per stratum (D-M2-Q3)
- [x] Pre-flight smoke gate (Task 3) confirmed HM3 intersection unblocks GCTA's LDSC step on bmi.EUR
- [x] 13 LSF jobs visible to `bjobs` immediately after Task 4
- [x] `data/processed/mtcojo/m2_post_m3_08_bjobs.tsv` has exactly 14 lines (header + 13 data rows), all rows have valid LSF job_ids
- [x] `.planning/m2_post_m3_rerun_queue.tsv` schema extended with `status`, `submit_ts`, `lsf_job_ids` columns; M2-POST-M3-08 row marked `in_flight`
- [x] `HARVEST-RUNBOOK.md` documents post-LSF harvest in 7 numbered sections including bmi.EUR-missing-row diagnosis path
- [x] Canonical artifacts UNTOUCHED: `m2_mtcojo.smk`, `select_mtcojo_eligible_targets.py`, `build_mtcojo_sensitivity_table.py`
- [x] All commits use explicit paths only (no `git add .` / `-A` per Carter's GPFS rule)
- [x] Framing is hypothesis-driven original research throughout (no "fix" / "revision" / "cleanup" wording in commit messages or runbook)

OUT OF SCOPE for this quick task (correctly NOT closed here):
- LSF job completion + sensitivity-table rebuild (= Carter's harvest step, runbook owns)
- bmi.EUR-missing-row defect in `build_mtcojo_sensitivity_table.py` (diagnosed during harvest; separate quick task if still present)
- AoU AFR LD panel + ld-score supersede (M2-POST-M3-01/03/05)
- TRANS AFR-LD sensitivity (M2-POST-M3-04)
- MTAG --fdr LSF re-fire (M2-POST-M3-07)
- GWAS Catalog v_lock_M5 refresh (M2-POST-M3-06, deferred)
</success_criteria>

<output>
After all tasks complete, create:
  .planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/260429-s10-SUMMARY.md

documenting (a) the 4 commits landed, (b) the smoke-gate witness, (c) the 13
LSF job IDs + submit timestamps, (d) the harvest path Carter follows when
LSF lands, (e) the explicit "out of scope" / follow-up obligations not
touched by this fire.
</output>
