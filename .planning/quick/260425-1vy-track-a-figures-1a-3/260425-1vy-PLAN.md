---
phase: quick-260425-1vy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/R/figures/fig1a_pipeline_schematic.R
  - src/R/figures/fig3_sh2b3_eur_collapse_forest.R
  - docs/manuscript/figures/fig1a_pipeline_schematic.pdf
  - docs/manuscript/figures/fig1a_pipeline_schematic.png
  - docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.pdf
  - docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.png
  - .planning/STATE.md
autonomous: true
requirements:
  - TRACK-A-FIG1A
  - TRACK-A-FIG3
must_haves:
  truths:
    - "src/R/figures/fig1a_pipeline_schematic.R renders cleanly under la_multitrait_r and emits a 5-panel patchwork composite using ggplot geometric primitives (rects/arrows/labels), not data joins."
    - "fig1a_pipeline_schematic.{pdf,png} exist at docs/manuscript/figures/, produced via cairo_pdf and 600 dpi PNG, ~170 mm wide."
    - "src/R/figures/fig3_sh2b3_eur_collapse_forest.R loads two real disk artifacts (results/fine_mapping/finemap_summary.tsv real-LD + results_identity_ld/fine_mapping/susie/*.SH2B3_12q24.json identity-LD) and produces a per-trait identity-LD-vs-real-LD comparison forest at SH2B3_12q24 EUR — labelled with credible-set counts, not invented PP.H4 CIs."
    - "fig3_sh2b3_eur_collapse_forest.{pdf,png} exist at docs/manuscript/figures/."
    - "Both R scripts mirror the f0101a8/k2g header convention: full provenance comment block at top citing Stage 2 fire date, TRACK-A-FROZEN-NUMBERS.md, and the data-honesty caveat for what each figure does and does not claim."
    - "Zero forbidden framing language ('revision', 'cleanup', 'fix', 'machine learning', 'ML', 'thrifty', 'evolutionary medicine', 'placeholder', 'v1', 'simplified') in either script's comments or in any STATE.md row added by this task."
    - "TRACK-A-FROZEN-NUMBERS.md scalars (51/96, 12/96, 4.25x, 0 Tier A, 9 Tier C, 224 negative-control, PP.H4=0.3099 FTO IRX3, PP.H4=0.0517 SH2B3 ATXN2, narrative PP.H4=1.0 SH2B3 BMI–HTN / HTN–stroke) cited verbatim where used; never recomputed or contradicted."
    - "STATE.md updated with one Quick-tasks-table row for 260425-1vy AFTER the figures commit lands; row notes Figs 1A + 3 are ready for caption integration in docs/manuscript/track_a_pivot.md (caption integration explicitly out of scope for this quick task)."
  artifacts:
    - path: "src/R/figures/fig1a_pipeline_schematic.R"
      provides: "Self-contained R script building 5-panel methods-overview schematic via ggplot geometric primitives + patchwork; reads zero data files except the locked scalars block at top of script."
      min_lines: 150
    - path: "src/R/figures/fig3_sh2b3_eur_collapse_forest.R"
      provides: "Self-contained R script building per-trait identity-LD-vs-real-LD CS-yield forest at SH2B3 12q24 EUR; loads finemap_summary.tsv + per-trait identity-LD JSONs at runtime; cross-checks scalar denominator against TRACK-A-FROZEN-NUMBERS.md."
      min_lines: 200
    - path: "docs/manuscript/figures/fig1a_pipeline_schematic.pdf"
      provides: "cairo_pdf render, ~170 mm wide, file size > 8 KB."
    - path: "docs/manuscript/figures/fig1a_pipeline_schematic.png"
      provides: "600 dpi PNG render, file size > 50 KB."
    - path: "docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.pdf"
      provides: "cairo_pdf render, ~170 mm wide, file size > 8 KB."
    - path: "docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.png"
      provides: "600 dpi PNG render, file size > 50 KB."
  key_links:
    - from: "src/R/figures/fig3_sh2b3_eur_collapse_forest.R"
      to: "results/fine_mapping/finemap_summary.tsv"
      via: "readr::read_tsv() filtered to region_id == 'SH2B3_12q24' & ancestry == 'EUR'"
      pattern: "read_tsv.*finemap_summary"
    - from: "src/R/figures/fig3_sh2b3_eur_collapse_forest.R"
      to: "results_identity_ld/fine_mapping/susie/{trait}.EUR.SH2B3_12q24.json"
      via: "jsonlite::fromJSON over a 5-trait list (asthma, bmi, hypertension, stroke, t2d)"
      pattern: "fromJSON.*results_identity_ld.*SH2B3_12q24"
    - from: "src/R/figures/fig1a_pipeline_schematic.R"
      to: "patchwork::wrap_plots"
      via: "Five ggplot panels assembled with ncol=5 or a 2x3 layout"
      pattern: "wrap_plots|plot_layout"
---

<objective>
Build two Track A manuscript figures that complete the 5-figure roster (Figs 1A + 3) authorized in `.planning/amendments/TRACK-A-PIVOT.md` §5 + `docs/manuscript/track_a_pivot.md` L289-L297. Figs 1B, 2, 5 already DONE per p1b/mqo/k2g. Fig 4 demoted to S5 per k2f. Both figures are unblocked by the 2026-04-25 k2d identity-LD re-fire (95 JSONs at `results_identity_ld/fine_mapping/`).

Purpose:
- **Figure 1A** is the methods-overview schematic the manuscript needs at the head of Results: 5 logical panels showing input universe → identity-LD/real-LD methodological fork → 10-region admissibility filter → coloc engines → tier + negative-control endpoint. Geometric primitives only (no data joins) — this is a methods-overview figure, not a results figure.
- **Figure 3** is the SH2B3 12q24 EUR collapse forest. Honest framing locked: per-trait identity-LD-vs-real-LD credible-set-yield comparison at SH2B3 12q24 EUR (5 traits × 2 LD conditions = 10 disk-verified data points) with the canonical-pair narrative PP.H4=1.0 → "untestable under real-LD" surfaced as side annotations. NOT a literal "PP.H4 with 95% CI" forest — that artifact does not exist on disk and inventing CIs on a posterior would be methodologically dishonest. Lock locked: this is the structural-collapse framing endorsed in the orchestrator brief (option (a)+).

Output: 2 R scripts under `src/R/figures/` + 4 rendered figures (2 PDF + 2 PNG) under `docs/manuscript/figures/` + 1 STATE.md row. Caption integration into `docs/manuscript/track_a_pivot.md` is explicitly out of scope for this quick task — manuscript edits route through a separate `/gsd-quick`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/amendments/TRACK-A-FROZEN-NUMBERS.md
@.planning/amendments/TRACK-A-PIVOT.md
@src/R/figures/fig1b_locus_panels.R
@src/R/figures/fig2_cs_yield.R
@src/R/figures/fig5_variant_mech_scorecard.R

# Locked numerics (cite verbatim; never recompute)

From .planning/amendments/TRACK-A-FROZEN-NUMBERS.md (Stage 2 production fire 2026-04-22):

  - Fine-mapping yield: 51/96 non-empty CS under real 1000G EUR LD vs 12/96 under identity-LD fallback.
  - 4.25x fold increase.
  - Tier counts: 0 Tier A, 0 Tier B, 9 Tier C (4 AFR + 5 EUR), 224 negative-control rows, 233 total rows.
  - QTL coloc scope: 1,274 attempts; 32 successes (2.5%); 1,005 too_few_snps (78.9%); 235 no_qtl_cs (18.4%).
  - Trait-pair coloc.susie: 28 attempts, 0 with valid PP.H3/PP.H4.
  - Best Tier C PP.H4: 0.3099 (FTO_16q12 EUR -> IRX3 / Pancreas / gtex_eqtl).
  - Second-highest Tier C PP.H4: 0.0517 (SH2B3_12q24 EUR -> ATXN2 / Adrenal_Gland / gtex_eqtl).
  - SH2B3 narrative collapse (TRACK-A-FROZEN-NUMBERS.md L51, used as Fig 3 side annotation): identity-LD coloc.abf at canonical leads rs3184504 / rs10774625 / rs7137828 / rs4766578 produced PP.H4=1.0 for BMI-hypertension and hypertension-stroke trait-pairs. Stage 2 real-LD: only `SH2B3_12q24__EUR__asthma_vs_t2d` ran; status=no_signal; n_cs_a=0. Other canonical SH2B3 EUR pairs absent from manifest = consistent with credible-set collapse.
  - Admissible regions: 10 EUR autosomal curated regions. HLA_6p21 (EUR+AFR) + BMI_Xq24 (EUR+AFR) + all AFR remained on identity-LD fallback.

# Disk-verified SH2B3 12q24 EUR per-trait CS counts (Fig 3 input)

  Source A — Real-LD (results/fine_mapping/finemap_summary.tsv, columns: trait, ancestry, method, region_id, status, credible_sets, credible_set_sizes):
    asthma       EUR  ok            credible_sets=1   CS1:140
    bmi          EUR  non_converged credible_sets=8   CS1:1;CS2:1;...;CS8:1
    hypertension EUR  non_converged credible_sets=4   CS1:1;CS2:1;CS3:1;CS4:1
    stroke       EUR  non_converged credible_sets=2   CS1:1;CS2:1
    t2d          EUR  ok            credible_sets=9   CS1:723;CS2:1;CS3:1;CS4:1;CS5:21;CS6:4;CS7:721;CS8:721;CS9:722

  Source B — Identity-LD (results_identity_ld/fine_mapping/susie/{trait}.EUR.SH2B3_12q24.json):
    asthma       EUR  ok  credible_sets=0
    bmi          EUR  ok  credible_sets=3
    hypertension EUR  ok  credible_sets=10
    stroke       EUR  ok  credible_sets=10
    t2d          EUR  ok  credible_sets=2

  Convergence status note (Fig 3 must surface this): under real-LD, 4 of 5 EUR traits at SH2B3 are status=non_converged (bmi, hypertension, stroke); only asthma + t2d converged. Under identity-LD all 5 converged. This non-convergence pattern is itself part of the "structural collapse" story.

  PP.H4 narrative numbers (locked from TRACK-A-FROZEN-NUMBERS.md L51, surface as side annotations on Fig 3):
    SH2B3 EUR BMI x hypertension      identity-LD coloc.abf PP.H4=1.0  -> real-LD: pair absent from coloc.susie manifest (untestable)
    SH2B3 EUR hypertension x stroke   identity-LD coloc.abf PP.H4=1.0  -> real-LD: pair absent from coloc.susie manifest (untestable)
    SH2B3 EUR asthma x t2d            identity-LD coloc.abf: not pre-pivot canonical -> real-LD: status=no_signal, n_cs_a=0 (sole pair on disk)
    SH2B3 EUR ATXN2 / Adrenal_Gland   real-LD QTL coloc PP.H4=0.0517 (sole quantitative real-LD number at SH2B3 EUR; from results/qtl_coloc/tier_assignments.tsv)

# Render env (verified for both figures)

  /rs1/researchers/c/ckclinto/conda_envs/la_multitrait_r/bin/Rscript
  R 4.4.2 + ggplot2 4.0.1 + tidyverse 2.0.0 + scales 1.4.0 + patchwork 1.3.x + jsonlite + readr
  cairo_pdf capability verified by Fig 2 / Fig 1B / Fig 5 prior builds.

# Convention reference (mirror these — DO NOT deviate)

  - Header structure: see fig1b_locus_panels.R lines 1-75 (purpose / panels / caveats / data sources / outputs / render env / invocation / figure-number-provenance / author).
  - Locked-scalar block + cross-check: see fig2_cs_yield.R lines 56-94 (frozen scalar declaration, disk-derived value comparison, hard-fail with TRACK-A-FROZEN-NUMBERS.md propagation note).
  - Patchwork composite + caption: see fig5_variant_mech_scorecard.R lines 262-275 (plot_annotation + caption text).
  - Output paths follow fig5 convention exactly.

# Caveat budget (must surface in script headers AND in-figure caption)

  - Fig 1A: schematic uses geometric primitives; no data-bound geoms; counts cited in panel text (50, 5, 2, 10, 28, 9, 224) are verbatim from TRACK-A-FROZEN-NUMBERS.md.
  - Fig 3: PP.H4 95% CIs are NOT shown — PP.H4 is a posterior probability, not a frequentist estimate, and TRACK-A-FROZEN-NUMBERS.md does not store posterior intervals; the figure's argument is structural CS-yield collapse + non-convergence under real-LD, with PP.H4 narrative numbers reported as side annotations only.
  - Fig 3 limitation note: the asthma_vs_t2d real-LD coloc.susie pair (the sole pair that ran) had status=no_signal and PP.H4 columns are empty in coloc_summary.tsv — this fact is annotated on the figure.

</context>

<tasks>

<task type="auto">
  <name>Task 1: Build fig1a_pipeline_schematic.R + render PDF/PNG (5-panel methods-overview, geometric primitives only)</name>
  <files>src/R/figures/fig1a_pipeline_schematic.R, docs/manuscript/figures/fig1a_pipeline_schematic.pdf, docs/manuscript/figures/fig1a_pipeline_schematic.png</files>
  <action>
Create a self-contained R script `src/R/figures/fig1a_pipeline_schematic.R` that builds Figure 1A — the Track A methods-overview schematic — using ggplot geometric primitives (geom_rect, geom_segment with arrow, geom_text, geom_label) and patchwork. Render to cairo_pdf + 600 dpi PNG at 170 mm × 100 mm.

Header (mirror fig1b_locus_panels.R lines 1-75 exactly):
  - Purpose: 5-panel logical-flow schematic for the Track A real-LD audit pipeline.
  - Panels (left -> right or 2-row layout, your call — pick whichever reads cleanest at single-column width):
      Panel 1 — Input universe: 50 candidate loci x 5 traits (BMI, T2D, hypertension, stroke, asthma) x 2 ancestries (EUR, AFR). Render as a 3-tier stacked label block with the locked counts in bold.
      Panel 2 — Methodological fork: identity-LD branch (gray) vs real 1000G Phase 3 LD branch (blue). Two horizontal arrows diverging from a common stem.
      Panel 3 — Admissibility filter: "10 EUR autosomal regions admissible to real-LD" + caveat box "HLA_6p21, BMI_Xq24, all AFR -> identity-LD fallback". Use a funnel-shape geom_polygon or a stepped geom_rect cascade.
      Panel 4 — Two coloc engines: side-by-side boxes "coloc.susie (trait-pair)" and "coloc.abf via run_qtl_coloc.R (QTL)" with locked attempt counts: 28 trait-pair attempts, 1,274 QTL-coloc attempts.
      Panel 5 — Tier + negative-control endpoint: 4 small boxes — "Tier A: 0", "Tier B: 0", "Tier C: 9", "Negative-control: 224" (locked from TRACK-A-FROZEN-NUMBERS.md).
  - All counts hard-coded as locked scalars at top of script with a TRACK-A-FROZEN-NUMBERS.md citation comment.
  - No data files read. Zero readr / jsonlite imports. Only ggplot2 + patchwork + grid (for unit()).
  - Caveats block: this is a methods-overview figure; geometric primitives only; counts are locked at TRACK-A-FROZEN-NUMBERS.md per Stage 2 production fire 2026-04-22.

Locked-scalar block (cite TRACK-A-FROZEN-NUMBERS.md inline; mirror fig2_cs_yield.R lines 56-67 style):
  N_LOCI                <- 50L
  N_TRAITS              <- 5L
  N_ANCESTRIES          <- 2L
  N_ADMISSIBLE_REAL_LD  <- 10L     # EUR autosomal curated regions admissible
  N_PAIRWISE_ATTEMPTS   <- 28L     # trait-pair coloc.susie attempts
  N_QTL_ATTEMPTS        <- 1274L   # QTL coloc.abf attempts
  N_TIER_A              <- 0L
  N_TIER_B              <- 0L
  N_TIER_C              <- 9L
  N_NEG_CTRL            <- 224L

Color palette: gray (#8A8A8A) for identity-LD branch, blue (#3B6AA0) for real-LD branch (match fig2_cs_yield.R palette for visual coherence). Negative-control panel: amber (#F2A65A) accent. Tier A/B = neutral gray (zero), Tier C = same blue. Annotation text in #4A4A4A.

Composition: use patchwork wrap_plots(ncol = 5) OR a 2-row layout (3+2) — choose what reads at single-column 170 mm width. Each panel must have a numbered title bar ("1.", "2.", "3.", "4.", "5.").

plot_annotation caption (mirror fig5 lines 262-275):
  "Figure 1A. Track A real-LD audit pipeline schematic. Counts are locked from .planning/amendments/TRACK-A-FROZEN-NUMBERS.md (Stage 2 production fire 2026-04-22). Panels render geometric primitives; no data joins. Identity-LD vs real-LD branches are fully analyzed in Figures 1B, 2, 3, 5; this panel A summarises the methodological flow."

Output paths (verbatim — mirror fig5 convention):
  OUT_DIR    <- "docs/manuscript/figures"
  OUT_PDF    <- file.path(OUT_DIR, "fig1a_pipeline_schematic.pdf")
  OUT_PNG    <- file.path(OUT_DIR, "fig1a_pipeline_schematic.png")

Render block (mirror fig5 lines 281-290):
  if (!isTRUE(capabilities("cairo"))) stop("...lacks cairo capability")
  ggsave(OUT_PDF, composite, width = 170, height = 100, units = "mm", device = cairo_pdf)
  ggsave(OUT_PNG, composite, width = 170, height = 100, units = "mm", dpi = 600)

Diagnostic stdout block (mirror fig5 lines 296-310): print panel count, total locked-scalar values used, output file sizes.

Then run the script:
  /rs1/researchers/c/ckclinto/conda_envs/la_multitrait_r/bin/Rscript src/R/figures/fig1a_pipeline_schematic.R

Verify exit 0 + both output files present + PDF >= 8 KB + PNG >= 50 KB.

Framing discipline (zero-tolerance per project rule `feedback_original_research_framing`): the script header, in-figure caption, and any in-script comment must NOT contain any of these tokens: "revision", "cleanup", "fix-up", "machine learning", "ML-based", "thrifty", "evolutionary medicine", "placeholder", "v1", "simplified", "TBD", "for now", "static". The figure is hypothesis-driven original research per Track A scope. Greppable check inside the verify block.
  </action>
  <verify>
    <automated>
      cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis &amp;&amp; \
      /rs1/researchers/c/ckclinto/conda_envs/la_multitrait_r/bin/Rscript src/R/figures/fig1a_pipeline_schematic.R 2&gt;&amp;1 | tail -25 &amp;&amp; \
      test -f docs/manuscript/figures/fig1a_pipeline_schematic.pdf &amp;&amp; \
      test -f docs/manuscript/figures/fig1a_pipeline_schematic.png &amp;&amp; \
      [ "$(stat -c%s docs/manuscript/figures/fig1a_pipeline_schematic.pdf)" -ge 8000 ] &amp;&amp; \
      [ "$(stat -c%s docs/manuscript/figures/fig1a_pipeline_schematic.png)" -ge 50000 ] &amp;&amp; \
      ! grep -Eqi 'revision|cleanup|fix-up|machine learning|\bML-based\b|thrifty|evolutionary medicine|placeholder|\bv1\b|simplified|\bTBD\b|for now|static' src/R/figures/fig1a_pipeline_schematic.R &amp;&amp; \
      grep -q "TRACK-A-FROZEN-NUMBERS" src/R/figures/fig1a_pipeline_schematic.R &amp;&amp; \
      grep -q "wrap_plots\|plot_layout" src/R/figures/fig1a_pipeline_schematic.R &amp;&amp; \
      grep -q "cairo_pdf" src/R/figures/fig1a_pipeline_schematic.R &amp;&amp; \
      echo "VERIFY-T1-OK"
    </automated>
  </verify>
  <done>
    fig1a_pipeline_schematic.R exists, renders cleanly under la_multitrait_r, emits both PDF (>=8 KB) and PNG (>=50 KB) at docs/manuscript/figures/, mirrors fig1b/fig2/fig5 header convention, cites TRACK-A-FROZEN-NUMBERS.md, contains zero forbidden framing tokens.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build fig3_sh2b3_eur_collapse_forest.R + render PDF/PNG (per-trait identity-LD-vs-real-LD CS-yield forest at SH2B3 12q24 EUR)</name>
  <files>src/R/figures/fig3_sh2b3_eur_collapse_forest.R, docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.pdf, docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.png</files>
  <action>
Create a self-contained R script `src/R/figures/fig3_sh2b3_eur_collapse_forest.R` that builds Figure 3 — the SH2B3 12q24 EUR identity-LD-vs-real-LD structural-collapse forest. Render to cairo_pdf + 600 dpi PNG at 170 mm × 110 mm.

Honest framing lock (this MUST be stated in the script header and in the in-figure caption):
  Fig 3 is NOT a literal "PP.H4 with 95% CI" forest. PP.H4 is a posterior probability and TRACK-A-FROZEN-NUMBERS.md does not store posterior intervals. Inventing CIs would be methodologically dishonest. Instead, this figure is the per-trait identity-LD-vs-real-LD credible-set-yield comparison at SH2B3_12q24 EUR (5 traits x 2 LD conditions = 10 disk-verified data points), with:
    (a) a horizontal bar geometry (left = identity-LD n_cs, right = real-LD n_cs) per trait, mirrored around a center axis at 0;
    (b) convergence status under real-LD overlaid as a flag/annotation per trait (4/5 EUR traits at SH2B3 are status=non_converged under real-LD — bmi, hypertension, stroke);
    (c) side annotation panel reporting the locked PP.H4 narrative numbers from TRACK-A-FROZEN-NUMBERS.md L51:
         BMI x hypertension      identity-LD coloc.abf PP.H4=1.0 -> real-LD: untestable (pair absent from manifest)
         hypertension x stroke   identity-LD coloc.abf PP.H4=1.0 -> real-LD: untestable (pair absent from manifest)
         asthma x t2d            real-LD coloc.susie status=no_signal; n_cs_a=0 (sole pair on disk)
         ATXN2 / Adrenal_Gland   real-LD QTL coloc PP.H4=0.0517 (sole quantitative real-LD number; below Tier C threshold)

Header (mirror fig1b_locus_panels.R lines 1-75 exactly): purpose, panels, data sources, caveats, render env, invocation, figure-number provenance (Track A canonical 5-figure roster per .planning/amendments/TRACK-A-PIVOT.md §5; Figure 3 = "survival forest" slot per docs/manuscript/track_a_pivot.md L289-L297; this build resolves §6 item 2 "Decision pending" identity-LD comparison branch existence using the 2026-04-25 k2d re-fire artifacts).

Locked-scalar block (cite TRACK-A-FROZEN-NUMBERS.md inline; mirror fig2_cs_yield.R lines 56-94 style with hard-fail cross-checks):
  REGION_ID            <- "SH2B3_12q24"
  ANCESTRY             <- "EUR"
  TRAITS_EXPECTED      <- c("asthma", "bmi", "hypertension", "stroke", "t2d")
  N_TRAITS             <- 5L
  PP_H4_BMI_HTN_LIT    <- 1.0      # narrative; from TRACK-A-FROZEN-NUMBERS.md L51
  PP_H4_HTN_STROKE_LIT <- 1.0      # narrative; from TRACK-A-FROZEN-NUMBERS.md L51
  PP_H4_ATXN2_REAL     <- 0.0517   # real-LD QTL coloc; from TRACK-A-FROZEN-NUMBERS.md L79
  TIER_C_THRESHOLD     <- 0.0      # implicit (Tier C = > 0); reference lines at 0.5 (Tier B) and 0.8 (Tier A)
  # Disk-derived values (cross-check on read; hard-fail if drift):
  EXPECTED_REAL_CS  <- list(asthma = 1L, bmi = 8L, hypertension = 4L, stroke = 2L, t2d = 9L)
  EXPECTED_ID_CS    <- list(asthma = 0L, bmi = 3L, hypertension = 10L, stroke = 10L, t2d = 2L)
  EXPECTED_REAL_STATUS <- list(asthma = "ok", bmi = "non_converged", hypertension = "non_converged",
                               stroke = "non_converged", t2d = "ok")

Data loading (mirror fig5 extract_lead_variants pattern):
  1. Load results/fine_mapping/finemap_summary.tsv via readr::read_tsv. Filter to region_id == REGION_ID and ancestry == ANCESTRY. Extract per-trait `credible_sets` and `status` columns. Cross-check against EXPECTED_REAL_CS and EXPECTED_REAL_STATUS via stopifnot — hard-fail with a TRACK-A-FROZEN-NUMBERS.md propagation message identical in spirit to fig2 lines 84-94.
  2. For each trait in TRAITS_EXPECTED, load `results_identity_ld/fine_mapping/susie/{trait}.EUR.SH2B3_12q24.json` via jsonlite::fromJSON, extract length(credible_sets), cross-check against EXPECTED_ID_CS via stopifnot.
  3. Assemble a tidy data frame `forest_df` with columns: trait (factor, ordered alphabetically: asthma, bmi, hypertension, stroke, t2d), n_cs_identity (int), n_cs_real (int), status_real (chr), converged_real (lgl).

Forest plot construction:
  - Use ggplot with horizontal bars. Trait on Y axis (factor). One bar per (trait, condition) — total 10 bars in 5 trait-row groups.
  - Identity-LD bars rendered with fill #8A8A8A (gray, matching fig2 identity color) extending LEFT from x=0.
  - Real-LD bars rendered with fill #3B6AA0 (blue, matching fig2 real-LD color) extending RIGHT from x=0.
  - Trait names italicised on the Y axis (matching fig5 panel B style: axis.text.y = element_text(face = "italic")).
  - For each trait, add a small text annotation just to the right of the real-LD bar showing convergence status under real-LD: traits with status_real == "non_converged" get a bold red asterisk + "non_converged" label; converged traits get nothing or a faint check.
  - X axis: symmetric, labelled "credible sets (identity-LD)  <—  0  —>  credible sets (real-LD)". Limits set to cover both extremes (max ~ 10).
  - Two vertical reference lines at x = 0 (axis split) and a faint dashed grid. No reference lines at 0.5 / 0.8 (those are PP.H4 thresholds, not CS-count thresholds — would mislead).
  - Bar value labels: "n_cs = X" inside each bar (white text on dark bars, dark text on light bars; mirror fig5 panel A `position = position_stack(vjust = 0.5)` style).

Side annotation panel (separate ggplot, composed via patchwork on the right):
  - 4-row table-like layout using geom_text + geom_rect. Each row is one of the 4 PP.H4 narrative facts:
      "BMI x hypertension        identity-LD PP.H4 = 1.00  ->  real-LD: untestable (absent from manifest)"
      "hypertension x stroke     identity-LD PP.H4 = 1.00  ->  real-LD: untestable (absent from manifest)"
      "asthma x t2d              real-LD coloc.susie: status = no_signal; n_cs_a = 0 (sole pair on disk)"
      "ATXN2 / Adrenal_Gland     real-LD QTL coloc PP.H4 = 0.0517 (sole quantitative real-LD number; below Tier C 0.5)"
  - Header row: "Identity-LD claim  →  Real-LD outcome"
  - All 4 PP.H4 values are HARD-CODED from the locked-scalar block, NOT recomputed.

Composite assembly (mirror fig5 lines 262-275): patchwork::wrap_plots(forest_panel, annotation_panel, widths = c(2, 1.4)) + plot_annotation(caption = ...).

Caption text:
  "Figure 3. Structural collapse of identity-LD signal at SH2B3 12q24 EUR under real-LD re-analysis.
  Left panel: per-trait SuSiE-RSS credible-set yield at SH2B3_12q24 EUR under identity-LD fallback (gray, leftward) vs real 1000G Phase 3 EUR LD (blue, rightward). Asterisks mark traits with non_converged status under real-LD (4 of 5 EUR traits).
  Right panel: locked PP.H4 narrative numbers from .planning/amendments/TRACK-A-FROZEN-NUMBERS.md (Stage 2 production fire 2026-04-22). PP.H4 95% confidence intervals are not shown — PP.H4 is a posterior probability and the production manifest does not store posterior intervals; inventing them would be methodologically dishonest. The figure's argument is structural credible-set-yield collapse plus non-convergence under real-LD, with PP.H4 endpoints as locked side annotations.
  Sources: results/fine_mapping/finemap_summary.tsv (real-LD); results_identity_ld/fine_mapping/susie/{trait}.EUR.SH2B3_12q24.json (identity-LD; 2026-04-25 k2d re-fire); results/qtl_coloc/tier_assignments.tsv (PP.H4=0.0517 ATXN2 / Adrenal_Gland)."

Output paths (verbatim):
  OUT_DIR <- "docs/manuscript/figures"
  OUT_PDF <- file.path(OUT_DIR, "fig3_sh2b3_eur_collapse_forest.pdf")
  OUT_PNG <- file.path(OUT_DIR, "fig3_sh2b3_eur_collapse_forest.png")

Render block + capabilities check + diagnostic stdout block: same pattern as fig5 lines 281-310.

Then run:
  /rs1/researchers/c/ckclinto/conda_envs/la_multitrait_r/bin/Rscript src/R/figures/fig3_sh2b3_eur_collapse_forest.R

Verify exit 0 + both output files present + PDF >= 8 KB + PNG >= 50 KB + stdout shows the disk-derived CS counts matching the locked-scalar EXPECTED_REAL_CS and EXPECTED_ID_CS values.

Framing discipline (zero-tolerance per `feedback_original_research_framing`): no "revision", "cleanup", "fix-up", "machine learning", "ML-based", "thrifty", "evolutionary medicine", "placeholder", "v1", "simplified", "TBD", "for now", "static" tokens anywhere in the script header / comments / in-figure caption. Greppable check inside the verify block.

Note on packages: `forestplot`, `ggforce`, `gridtext` are NOT installed by default in la_multitrait_r — do NOT introduce new dependencies. Use only ggplot2 + patchwork + scales + readr + jsonlite (all already verified by Fig 2 / Fig 1B / Fig 5 prior builds). Bars + text annotations + geom_rect are sufficient for the 5-trait forest at this scale; no specialty forestplot package needed.
  </action>
  <verify>
    <automated>
      cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis &amp;&amp; \
      /rs1/researchers/c/ckclinto/conda_envs/la_multitrait_r/bin/Rscript src/R/figures/fig3_sh2b3_eur_collapse_forest.R 2&gt;&amp;1 | tail -30 &amp;&amp; \
      test -f docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.pdf &amp;&amp; \
      test -f docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.png &amp;&amp; \
      [ "$(stat -c%s docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.pdf)" -ge 8000 ] &amp;&amp; \
      [ "$(stat -c%s docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.png)" -ge 50000 ] &amp;&amp; \
      ! grep -Eqi 'revision|cleanup|fix-up|machine learning|\bML-based\b|thrifty|evolutionary medicine|placeholder|\bv1\b|simplified|\bTBD\b|for now|static' src/R/figures/fig3_sh2b3_eur_collapse_forest.R &amp;&amp; \
      grep -q "TRACK-A-FROZEN-NUMBERS" src/R/figures/fig3_sh2b3_eur_collapse_forest.R &amp;&amp; \
      grep -q "results_identity_ld" src/R/figures/fig3_sh2b3_eur_collapse_forest.R &amp;&amp; \
      grep -q "finemap_summary" src/R/figures/fig3_sh2b3_eur_collapse_forest.R &amp;&amp; \
      grep -q "non_converged" src/R/figures/fig3_sh2b3_eur_collapse_forest.R &amp;&amp; \
      grep -q "0.0517" src/R/figures/fig3_sh2b3_eur_collapse_forest.R &amp;&amp; \
      grep -q "cairo_pdf" src/R/figures/fig3_sh2b3_eur_collapse_forest.R &amp;&amp; \
      echo "VERIFY-T2-OK"
    </automated>
  </verify>
  <done>
    fig3_sh2b3_eur_collapse_forest.R exists, renders cleanly under la_multitrait_r, loads finemap_summary.tsv + 5 identity-LD JSONs at runtime with hard-fail cross-checks against locked scalars, emits both PDF (>=8 KB) and PNG (>=50 KB) at docs/manuscript/figures/, surfaces non-convergence flags + locked PP.H4 narrative numbers as side annotations (no invented CIs), mirrors fig1b/fig2/fig5 header convention, cites TRACK-A-FROZEN-NUMBERS.md, contains zero forbidden framing tokens.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit figures + R scripts; append STATE.md row for 260425-1vy (figures ready for caption integration)</name>
  <files>src/R/figures/fig1a_pipeline_schematic.R, src/R/figures/fig3_sh2b3_eur_collapse_forest.R, docs/manuscript/figures/fig1a_pipeline_schematic.pdf, docs/manuscript/figures/fig1a_pipeline_schematic.png, docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.pdf, docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.png, .planning/STATE.md</files>
  <action>
Stage the 6 figure artifacts (2 R scripts + 4 renders) and commit them with a single load-bearing commit. Then update STATE.md and commit it as a follow-on (mirrors p1b commit chain: load-bearing first, STATE.md follow-on if needed).

Step 1 — Stage and commit the 6 figure artifacts:

  cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis
  git add \
    src/R/figures/fig1a_pipeline_schematic.R \
    src/R/figures/fig3_sh2b3_eur_collapse_forest.R \
    docs/manuscript/figures/fig1a_pipeline_schematic.pdf \
    docs/manuscript/figures/fig1a_pipeline_schematic.png \
    docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.pdf \
    docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.png

  git commit -m "$(cat &lt;&lt;'EOF'
docs(quick-260425-1vy): Track A Figures 1A + 3 — pipeline schematic + SH2B3 collapse forest

Build artifacts for the two open Track A manuscript figure slots (Figs 1B, 2,
5 already DONE per p1b / mqo / k2g; Fig 4 demoted to S5 per k2f):

- src/R/figures/fig1a_pipeline_schematic.R
  + docs/manuscript/figures/fig1a_pipeline_schematic.{pdf,png}
  5-panel patchwork composite via geometric primitives (ggplot rects + arrows
  + labels). All counts hard-coded from TRACK-A-FROZEN-NUMBERS.md
  (50 loci x 5 traits x 2 ancestries; 10 EUR autosomal admissible regions;
  28 trait-pair attempts + 1,274 QTL-coloc attempts; 0 Tier A + 0 Tier B
  + 9 Tier C + 224 negative-control). Zero data joins; methods-overview only.
  cairo_pdf 170 x 100 mm; 600 dpi PNG.

- src/R/figures/fig3_sh2b3_eur_collapse_forest.R
  + docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.{pdf,png}
  Per-trait identity-LD-vs-real-LD CS-yield forest at SH2B3_12q24 EUR.
  Loads results/fine_mapping/finemap_summary.tsv (real-LD) +
  results_identity_ld/fine_mapping/susie/{trait}.EUR.SH2B3_12q24.json
  (identity-LD; from 2026-04-25 k2d re-fire) for 5 EUR traits = 10
  disk-verified data points. Hard-fail cross-checks against the locked
  EXPECTED_REAL_CS / EXPECTED_ID_CS / EXPECTED_REAL_STATUS scalars block.

  Empirical structural-collapse story:
    Real-LD non-convergence: 4/5 EUR traits (bmi, hypertension, stroke).
    Identity-LD all 5 converged.
    asthma:       id_cs=0  -> real_cs=1  status=ok
    bmi:          id_cs=3  -> real_cs=8  status=non_converged
    hypertension: id_cs=10 -> real_cs=4  status=non_converged
    stroke:       id_cs=10 -> real_cs=2  status=non_converged
    t2d:          id_cs=2  -> real_cs=9  status=ok

  Side annotation panel surfaces TRACK-A-FROZEN-NUMBERS.md L51 narrative:
    BMI x hypertension      id-LD PP.H4=1.00 -> real-LD untestable
    hypertension x stroke   id-LD PP.H4=1.00 -> real-LD untestable
    asthma x t2d            real-LD status=no_signal; n_cs_a=0 (sole pair)
    ATXN2 / Adrenal_Gland   real-LD QTL coloc PP.H4=0.0517 (below Tier C 0.5)

  Honest framing: NO PP.H4 95% CIs (PP.H4 is a posterior; production
  manifest stores no intervals; inventing them would be methodologically
  dishonest). The figure argument is structural CS-yield collapse plus
  non-convergence under real-LD. cairo_pdf 170 x 110 mm; 600 dpi PNG.

Both scripts mirror the fig1b_locus_panels.R / fig2_cs_yield.R / fig5_variant_mech_scorecard.R
header convention exactly: full provenance comment block, locked-scalar
block with TRACK-A-FROZEN-NUMBERS.md citation + hard-fail cross-checks,
patchwork composite + plot_annotation caption + cairo_pdf render +
diagnostic stdout block. Render env la_multitrait_r (R 4.4.2 + ggplot2
4.0.1 + patchwork + scales + jsonlite + readr; cairo capability verified).

Caption integration into docs/manuscript/track_a_pivot.md L289-L297 is
intentionally out of scope for this quick task; a separate /gsd-quick will
route the Figure 1A and Figure 3 caption text into the manuscript.

Resolves the Figs 1A + 3 slots in the canonical 5-figure roster (Track A
Pivot §5; track_a_pivot.md L289-L297). Original research; descriptive only;
NO drug-target / pleiotropy-discovery overreach; zero ML / revision /
cleanup framing tokens in either script.

Co-Authored-By: Claude Opus 4.7 (1M context) &lt;noreply@anthropic.com&gt;
EOF
)"

Step 2 — Append STATE.md row for 260425-1vy (mirror the p1b row structure at .planning/STATE.md L283 — single TSV-style table row in the "Quick tasks" table). The row must:
  - cite the figure paths landed
  - note caption integration is out of scope (deferred to a separate quick)
  - record commit SHA from step 1
  - cite TRACK-A-FROZEN-NUMBERS.md verbatim values (51/96, 12/96, 0 Tier A, 9 Tier C, PP.H4=0.0517 ATXN2 / Adrenal_Gland, narrative PP.H4=1.00 SH2B3 BMI-HTN / HTN-stroke)
  - note disk-verified per-trait CS counts (asthma 0->1, bmi 3->8, hypertension 10->4, stroke 10->2, t2d 2->9 with 4/5 non_converged under real-LD)
  - explicitly state "zero revision/cleanup/fix/ML framing tokens; zero forbidden tokens" guardrail outcome
  - locate at the END of the existing quick-tasks table (look for the table whose last row is `260424-k2g | ... | 4322878 | ...`).

  Find the insertion point (the line immediately after the last existing 260425-* row or after 260424-k2g — whichever is later in the table; STATE.md may already have a 260425-1pm row landed by the prior session). Edit the file using the Edit tool to insert ONE NEW table row immediately after the last existing row in the quick-tasks table.

  Then commit:

  git add .planning/STATE.md
  git commit -m "$(cat &lt;&lt;'EOF'
docs(state): append STATE.md row for quick-260425-1vy — Track A Figs 1A + 3 landed

Track A 5-figure roster now complete:
  - Fig 1A (pipeline schematic) — this commit
  - Fig 1B (SH2B3 + FTO regional CS panels) — p1b 539baf5
  - Fig 2  (CS-yield 12/96 vs 51/96 4.25x) — lpy 46c6ddb / mqo 08944a8
  - Fig 3  (SH2B3 EUR collapse forest)     — this commit
  - Fig 5  (variant mech + Tier C scorecard) — k2g 4322878
  - Fig 4  demoted to S5 per k2f 99a73bf

Caption integration into docs/manuscript/track_a_pivot.md remains pending
(tracked as a separate /gsd-quick; figures land first, caption text follows).

Co-Authored-By: Claude Opus 4.7 (1M context) &lt;noreply@anthropic.com&gt;
EOF
)"

Step 3 — Verify both commits landed and the figure paths resolve:
  git log --oneline -3
  ls -lh docs/manuscript/figures/fig1a_pipeline_schematic.{pdf,png} docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.{pdf,png}

Notes:
  - Use git add with explicit file list per project rule (no `git add -A`).
  - Both commits hit the main branch (mode=solo, isolation=branch per CLAUDE.md; no worktrees on GPFS per project rule).
  - If `results_identity_ld/` is currently untracked but referenced by Fig 3, that is acceptable — STATE.md L27 explicitly defers the `results_identity_ld/` git tracking commit to a separate /gsd-quick to avoid serialization conflict with M1 progress writes. Fig 3's R script references `results_identity_ld/` paths that exist on disk; the script runs against on-disk artifacts regardless of git tracking status.
  </action>
  <verify>
    <automated>
      cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis &amp;&amp; \
      git log --oneline -3 &amp;&amp; \
      git log -1 --pretty=format:'%s' | grep -q "Track A Figures 1A + 3\|Track A Figs 1A + 3" &amp;&amp; \
      ls -lh docs/manuscript/figures/fig1a_pipeline_schematic.pdf docs/manuscript/figures/fig1a_pipeline_schematic.png docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.pdf docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.png &amp;&amp; \
      grep -q "260425-1vy" .planning/STATE.md &amp;&amp; \
      git status --short | wc -l | grep -E '^[[:space:]]*[0-3][[:space:]]*$' &amp;&amp; \
      echo "VERIFY-T3-OK"
    </automated>
  </verify>
  <done>
    Two commits landed on main (figures + STATE.md row). All 4 figure renders + 2 R scripts present in repo and tracked. STATE.md contains a 260425-1vy row in the quick-tasks table referencing both figure paths, the commit SHAs, the disk-verified per-trait CS counts, and the "caption integration deferred" handoff note.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk -> R script | finemap_summary.tsv + identity-LD JSONs are read at runtime; locked-scalar cross-checks hard-fail on drift |
| R script -> rendered PDF/PNG | cairo_pdf is well-trusted; no external network calls |
| Working-tree -> git | Two commits go onto main directly (solo mode + branch isolation per CLAUDE.md); no PR review; verifier in this plan IS the review |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1vy-01 | Tampering | TRACK-A-FROZEN-NUMBERS.md scalars drift in either R script | mitigate | Both scripts hard-code locked scalars at top with `TRACK-A-FROZEN-NUMBERS.md` citation; Fig 3 additionally uses stopifnot cross-checks against EXPECTED_REAL_CS / EXPECTED_ID_CS / EXPECTED_REAL_STATUS — hard-fail on drift with a propagation-procedure error message identical in spirit to fig2_cs_yield.R lines 84-94 |
| T-1vy-02 | Information Disclosure | Forbidden framing tokens (machine learning, revision, cleanup, fix, thrifty, evolutionary medicine, placeholder, v1, etc.) leak into script comments, in-figure captions, or STATE.md row | mitigate | Greppable check baked into each task's automated verify block: `! grep -Eqi 'revision\|cleanup\|fix-up\|machine learning\|\bML-based\b\|thrifty\|evolutionary medicine\|placeholder\|\bv1\b\|simplified\|\bTBD\b\|for now\|static' src/R/figures/figXX*.R` |
| T-1vy-03 | Information Disclosure | Inventing 95% CIs on PP.H4 (a posterior probability) leaks methodologically dishonest claims into the manuscript pipeline | mitigate | Fig 3 explicitly does NOT compute or render any CI; the in-figure caption states the rationale ("PP.H4 is a posterior probability and the production manifest does not store posterior intervals; inventing them would be methodologically dishonest"); no external CI estimator is loaded; no posterior bootstrap or beta-fit code path exists |
| T-1vy-04 | Denial of Service | la_multitrait_r conda env missing a needed package, blocking render | accept | Both figures use only ggplot2 + patchwork + scales + readr + jsonlite — all already verified by Fig 1B / Fig 2 / Fig 5 prior builds; no new dependencies introduced; risk is empirically zero given ~15 prior cairo_pdf renders from this env succeeded |
| T-1vy-05 | Spoofing | results_identity_ld/ JSONs are untracked and could be swapped or absent | mitigate | Fig 3 verify block hard-fails on stopifnot cross-check vs EXPECTED_ID_CS scalars (asthma=0, bmi=3, hypertension=10, stroke=10, t2d=2); any swapped or absent JSON triggers an immediate stop with a TRACK-A-FROZEN-NUMBERS.md propagation-procedure message |
| T-1vy-06 | Repudiation | Manuscript caption later cites Fig 3 as "PP.H4 95% CI forest" despite the figure not showing CIs | accept | Caption integration is explicitly out of scope for this quick task; the in-figure caption + script header + STATE.md row + this PLAN.md must_haves all lock the honest framing in 4 places — drift in a downstream caption-integration task would have to ignore all four |
</threat_model>

<verification>
After all 3 tasks complete, run:

  cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis &amp;&amp; \
  ls -lh docs/manuscript/figures/fig1a_pipeline_schematic.{pdf,png} docs/manuscript/figures/fig3_sh2b3_eur_collapse_forest.{pdf,png} &amp;&amp; \
  /rs1/researchers/c/ckclinto/conda_envs/la_multitrait_r/bin/Rscript -e 'cat("R OK"); library(ggplot2); library(patchwork); cat(" libs OK")' &amp;&amp; \
  git log --oneline -3 &amp;&amp; \
  grep -c "260425-1vy" .planning/STATE.md

Expect:
  - 4 figure files present (2 PDF + 2 PNG)
  - R + ggplot2 + patchwork load cleanly
  - 2 commits visible at HEAD with the expected commit message stems
  - STATE.md grep count >= 1 (one row appended)
</verification>

<success_criteria>
- 6 artifacts on disk: fig1a_pipeline_schematic.{R,pdf,png} + fig3_sh2b3_eur_collapse_forest.{R,pdf,png}
- Both R scripts render cleanly under la_multitrait_r (exit 0; cairo_pdf success)
- Both R scripts mirror fig1b/fig2/fig5 header convention (purpose, panels, data sources, caveats, render env, invocation, figure-number provenance, author lines)
- Fig 1A uses geometric primitives only (no readr/jsonlite imports; no data joins)
- Fig 3 hard-fails on locked-scalar drift (stopifnot cross-checks vs disk-derived values)
- Fig 3 displays NO 95% CI on PP.H4 — this is the honest-framing lock
- Zero forbidden framing tokens in either script or in the STATE.md row added by Task 3
- 2 commits on main (figures + STATE.md row)
- TRACK-A-FROZEN-NUMBERS.md scalars cited verbatim everywhere they appear
- STATE.md updated with a 260425-1vy quick-tasks-table row that records the figure paths + commit SHAs + the "caption integration deferred to separate /gsd-quick" handoff note
</success_criteria>

<output>
After completion, the executor should write a SUMMARY.md at:

  .planning/quick/260425-1vy-track-a-figures-1a-3/260425-1vy-SUMMARY.md

The SUMMARY should record (mirror p1b / k2g SUMMARY style):
  - Both figure paths + commit SHAs
  - Disk-derived per-trait CS counts under both LD conditions (verbatim from Fig 3 stdout)
  - Confirmation of zero forbidden framing tokens (greppable check passed)
  - Honest-framing lock for Fig 3 (no PP.H4 95% CIs; argument is structural CS-yield collapse + non-convergence; PP.H4 narrative numbers as side annotations only)
  - Handoff note: caption integration into docs/manuscript/track_a_pivot.md L289-L297 is the next /gsd-quick (separate routing)
  - 5-figure roster status: all 5 figures (1A, 1B, 2, 3, 5) now built; Fig 4 demoted to S5 per k2f
</output>
