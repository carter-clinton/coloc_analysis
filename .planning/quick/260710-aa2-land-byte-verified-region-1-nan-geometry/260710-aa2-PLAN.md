# Quick Task 260710-aa2 — Land byte-verified region-1 NaN geometry verdict (4th amendment doc)

**Created:** 2026-07-10
**Branch:** m3-W2-aou-deltas (no worktree — GPFS)

## Objective

Land the region-1 NaN **geometry VERDICT** (`m3_region1_nan_geometry_verdict.md`) — the 4th and final
amendment doc — byte-faithfully from Carter's base64 transfer, clearing the standing
**HOLD-THEN-PUSH-ALL-FOUR** order so a single push can take all four amendment docs to origin.

The verdict transfer had blocked twice because a chat paste mangles its 6-row table
(cf. [[reference_byte_faithful_artifact_transfer]]). This task lands the base64-transferred,
SHA-256-anchored copy.

## Byte-fidelity gate (already satisfied before commit)

- Anchor: body SHA-256 `4543dcf4a61c3cf79061c8c55b71b316c38c4a938541cf0040c94212c8cdc06a` / 5012 B.
- `base64 -d` of Carter's blob → 5012 bytes → `sha256sum` == anchor ✅.
- Final committed file = a 2-line self-locating header + blank line + the verbatim 5012-byte body,
  so `tail -c 5012 <file> | sha256sum` == anchor ✅ (re-verified in the repo location post-`cp`).
- No re-typing / reconstruction — the file was `cp`'d verbatim from the verified scratchpad copy.

## Task

1. **T1 — land the verdict + clear the hold.**
   - `files`: `.planning/amendments/m3_region1_nan_geometry_verdict.md` (new), `.planning/STATE.md`
   - `action`: `cp` the byte-verified file into place (done); refresh the STATE.md 2026-07-08
     RESUME-HERE block — verdict LANDED + byte-verified, HOLD-THEN-PUSH-ALL-FOUR CLEARED, all four
     docs (WHY `3516c18` / JOIN-IMPACT `c4e0875` / POLICY `8f36fdf` / VERDICT this commit) push together.
   - `verify`: `tail -c 5012 .planning/amendments/m3_region1_nan_geometry_verdict.md | sha256sum`
     == `4543dcf4…`.
   - `done`: file present + byte-verified; STATE reflects the cleared hold; atomic commit made.

## Out of scope (orchestrator handles after commit)

- `git push origin m3-W2-aou-deltas` (all four docs) + `origin==local` verification — run **after** this commit.
- The upstream overlapping-deletion span-filter build for all 276 + provenance manifest +
  present-rate scan = a **future `/gsd-plan-phase`**. m3-06 stays HELD; `condition_ld_matrix.py` FROZEN.
