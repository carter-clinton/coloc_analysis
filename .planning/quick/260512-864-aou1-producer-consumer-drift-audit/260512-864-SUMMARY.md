# 260512-864 — AOU-1 producer/consumer drift audit — SUMMARY

**Verdict:** **NO additional producer/consumer drifts of the 260512-jd9 class detected.** Pipeline is clean. Cells 4-8 are safe to proceed after Cell 3 closes.

**Framing lock:** audit-driven re-analysis ([[feedback_original_research_framing]]). Zero forbidden tokens in narrative sections.

**Date / Audit method:** 2026-05-12; single Explore-class subagent end-to-end read of in-scope files + manual cross-reference against the 260512-jd9 producer/consumer signature.

## Files audited

| File | Lines | Audit scope |
|---|---|---|
| `.planning/notebooks/AOU-1_template.ipynb` | 9 cells (post 260512-ldj 1a+1b split) | Cells 3-8 (3 = primary AFR; 4 = sensitivity AFR; 5 = primary EUR; 6 = disjoint check; 7 = cohort_summary; 8 = closing markdown) |
| `src/python/aou_ld_panel.py` | ~520 | URI helpers, `load_qc_cohort()`, `compute_region_ld()`, `_save_npz()`, `main()` output-bucket construction |
| `.planning/notebooks/AOU-2_per_region_ld.ipynb` | Cells 2-3 | checkpoint reads + OUT_BUCKET construction |
| `.planning/notebooks/AOU-4_validation.ipynb` | Cells 1, 3, 9, 11 | region table, .rds reads, sensitivity-cohort cross-check |
| `.planning/phases/m3-aou-afr-ld-panel-build/m3-region-class-projection.tsv` | full | region-class metadata producer/consumer interface |

## Drift inventory

**Empty.** No producer/consumer drifts of the 260512-jd9 class were detected in the audited files.

## Clean-pattern inventory (producer/consumer pairs verified consistent)

The following 6 producer/consumer pairs were verified consistent — they represent the audit trail of what was specifically checked, with line-level evidence:

1. **Checkpoint URI helper at producer + consumer alignment**
   - Producer: `_qc_checkpoint_uri(bucket, ancestry, sensitivity)` at `src/python/aou_ld_panel.py:147-160` correctly gates the `_pca_selfid_qc` suffix on BOTH `ancestry` AND `sensitivity` parameters (commit `36e8062` patch verified live in this audit).
   - Three distinct checkpoint paths produced:
     - `gs://${WORKSPACE_BUCKET}/ld/mt_afr_qc.mt` (primary AFR, `sensitivity=False`)
     - `gs://${WORKSPACE_BUCKET}/ld/mt_afr_pca_selfid_qc.mt` (sensitivity AFR, `sensitivity=True`)
     - `gs://${WORKSPACE_BUCKET}/ld/mt_eur_qc.mt` (EUR, `sensitivity=False`)
   - Consumer (AOU-2 Cell 2): reads all three checkpoints at AOU-2 markdown lines 14-16 + code lines 51-52 (`hl.read_matrix_table(f"gs://${WORKSPACE_BUCKET}/ld/mt_afr_qc.mt")` etc.)
   - Consumer (AOU-4 Cell 11): D-M3-07 sensitivity check reads `AFR_aou_pca_selfid` subdirectory at AOU-4 line 271, consistent with the sensitivity cohort label.

2. **Output-bucket construction omits sensitivity (intentional)**
   - Producer: `main()` at `src/python/aou_ld_panel.py:513` constructs `out_bucket = f"gs://{ws}/ld/{anc_upper}_aou"` from `ancestry` only.
   - Consumer (AOU-2 Cell 3 lines 64-65 + markdown lines 21-22): constructs matching `OUT_BUCKET_AFR = f"gs://{os.environ['WORKSPACE_BUCKET']}/ld/AFR_aou"` and `OUT_BUCKET_EUR`. No sensitivity branching.
   - **Verified intentional:** sensitivity cohorts share the same per-region `.npz` output prefix; validation-time sensitivity comparison reads from distinct input checkpoints (point 1 above), not from output-suffix differentiation.

3. **BlockMatrix sidecar paths**
   - Producer: `src/python/aou_ld_panel.py:402-417` writes `{out_bucket}/bm/{region_id}.bm` + sidecar TSVs.
   - Consumer (AOU-2 line 70): `.npz` files land at `{OUT_BUCKET_AFR,OUT_BUCKET_EUR}/{region_id}.npz`.
   - No sensitivity discriminator in output paths — confirmed consistent with the design in clean-pattern 2.

4. **Region-class Path-A branching**
   - Producer: `compute_region_ld()` at `src/python/aou_ld_panel.py:363-374` correctly gates branching on both `region_class` and `span_mb`:
     - Path A.1 (small / ≤5 Mb): direct `.npz` via `_save_npz()` (line 366).
     - Path A.2 (medium / 5-10 Mb): sparsified `.npz` (line 372).
     - Path A.3 (large/xlarge / >10 Mb): BlockMatrix write + sidecar TSVs (lines 402-417).
   - Consumer (AOU-4 Cells 3 + 9): consumes `.rds` files from `data/processed/ld_reference/{AFR,EUR}_aou/` and `AFR_aou_pca_selfid/` via Wave 3 NCSU-side conversion (AOU-2 lines 221-227). Validation logic does not differentiate by path-class internally — relies on the upstream `.npz` / `.bm` to `.rds` pipeline. No path-class discrepancies detected.

5. **Variant-list cohort identifiers**
   - Producer: `compute_region_ld()` at `src/python/aou_ld_panel.py:330-360` collects `variant_ids` and `rsids` in a single `aggregate_rows()` call (CR-002 defensive pairing per source comment at lines 322-329).
   - Consumer (Wave 3 `bm_to_npz.py` per source comment at line 378-381): expects `variant_ids` + `rsids` side-by-side in `.npz` metadata.
   - Both tied to the same MT row pass; no missing discriminator parameters.

6. **AOU-1 Cell 3 / Cell 4 / Cell 5 cohort calls → distinct checkpoint URIs**
   - Cell 3: `load_qc_cohort(..., ancestry="afr", sensitivity=False)` → `mt_afr_qc.mt`.
   - Cell 4: `load_qc_cohort(..., ancestry="afr", sensitivity=True)` → `mt_afr_pca_selfid_qc.mt`.
   - Cell 5: `load_qc_cohort(..., ancestry="eur", sensitivity=False)` → `mt_eur_qc.mt`.
   - Three distinct URIs — silent-overwrite class of 260512-jd9 is ruled out.

## Decision

**Proceed with Wave 2 dev fire.** No source-code edits, no test edits, no notebook edits required.

Audit-tool recommendation block (Explore agent report verbatim, quoted for traceability):

> Proceed normally — no action needed. The current pipeline is clean. The 260512-jd9 [patch] (distinct sensitivity/primary checkpoint URIs) is in place and verified. All downstream readers (AOU-2, AOU-4) correctly reference the three distinct checkpoint paths. Output bucket construction correctly omits sensitivity from the per-region export path (intentional — sensitivity cohorts reuse the same `.npz` / `.bm` output directories; validation-time LD correlation is computed from distinct input checkpoints, not output suffixes). Region-class Path-A branching and variant-list cohort identifiers are free of missing-parameter drift. Wave 2 dev fire (45-90 min Cell 3 + Cells 4-8) is safe to proceed.

## Cross-references

- **Predecessor (origin of audit class):** `36e8062` (260512-jd9 — `_qc_checkpoint_uri()` helper + 4 TDD regression tests pinning distinct primary vs sensitivity URIs).
- **Predecessor (init-layer sync):** `5389a88` + `e7063f5` (260512-ldj — NCSU AOU-1 Cell 1 split into Cell 1a + 1b PYSPARK_SUBMIT_ARGS init pattern).
- **Predecessor (Hail OOM remediation):** `8cc6f64` (m3-W2 RegionPool OOM remediation via `spark.executor.cores=1` + `naive_coalesce(2048)`, DEC-2026-05-04-01).
- **This commit:** `(m3-W1-aou1-drift-audit-260512-864)` — atomic audit-trail commit (PLAN + SUMMARY + STATE.md).

## Honest framing self-check

Audit-driven re-analysis. The audit class (silent-overwrite producer/consumer drift) is the same one that 260512-jd9 surfaced empirically. This SUMMARY records a no-drift verdict across 5 producer/consumer interfaces. No source-code, test, or notebook edits were proposed because no drifts were found — not because the audit was skipped or shallow. Cells 4-8 are not blocker-cleared by this audit (they were already runnable post 260512-jd9 + 260512-ldj); this audit is reproducibility / reviewer audit trail.
