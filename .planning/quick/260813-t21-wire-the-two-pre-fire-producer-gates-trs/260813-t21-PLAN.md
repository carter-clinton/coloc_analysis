---
phase: quick-260813-t21
plan: 1
slug: wire-the-two-pre-fire-producer-gates-trs
type: execute
wave: 1
status: planned
authored: 2026-08-13
depends_on: []
autonomous: true
requirements: [TRSX5-CLAUSE-D, DEBUG-M3-PRODUCER-UNBOUNDED-DENSE-READ]
decision_anchor: 260813-t21-CONTEXT.md (LOCKED — Carter "wire it" after Seth's pre-fire review)
files_modified:
  - src/python/run_native_ld_panel.py                # the ONLY production-code file touched
  - tests/m3/test_run_native_ld_panel.py             # 7 new tests + 9 existing tests gain a pin-open line
  - .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-AGENT-PROMPT.md   # LOCKED exception to the dated-quick-dir ban (CONTEXT "After the code lands")
  - .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-BROWSER-PASTE.md  # same LOCKED exception
  - .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md  # same LOCKED exception (item 10 note)
  - .planning/HANDOFF.json                           # suite_baselines bump 907 -> 914
  - .planning/STATE.md                               # atomic refresh ([[feedback_state_md_keep_current]])
  - .planning/quick/260813-t21-wire-the-two-pre-fire-producer-gates-trs/260813-t21-SUMMARY.md

must_haves:
  truths:
    - "A square-mode region whose occluded count EXCEEDS 0.0005 x pre_window_n_var is DEFERRED (status starts 'deferred_occlusion_anomaly'), with NOTHING excluded, no excludelist, no manifest, no plink call, no uploads — and the loop continues to the next region (clause (d): defer-not-exclude)"
    - "A square-mode region whose pre_window_n_var exceeds --max-n-var is DEFERRED (status 'deferred_infeasible_square: n_var=N > ceiling=M') BEFORE occlusion detection, with no plink and no artifacts — and the loop continues"
    - "The --max-n-var argparse default equals m3_convert_max_n_var in config/pipeline.yaml, pinned by a test that READS the YAML (never two hardcoded 120000s)"
    - "--fail-fast semantics UNCHANGED: it halts (RegionGateError) on any status != 'ok', which now includes deferrals — documented in the help text, never special-cased"
    - "A sub-ceiling occluded region (count <= 0.0005 x n_var) keeps the FULL existing exclude-in-lockstep behavior: excludelist + --exclude argv + manifests + verified .npz, status ok"
    - "Both deferral paths keep n_dropped_occluded and n_var None on the result dict (detail lives in the status string, following the 'error: ...' precedent); the panel row IS appended under the unchanged 9-column header"
    - "tests/m3 = 914 passed / 31 skipped / 0 failed; tests/phase2 = 136 / 1 / 0; skips stay EXACTLY 31 and 1"
    - "The Stage-C hold blocks in the two browser packages are replaced with dated gates-landed notes, and READY-TO-FIRE item 10 carries the deferral-vocabulary + clause-(d) post-fire disclosure paragraph"
  artifacts:
    - path: "src/python/run_native_ld_panel.py"
      provides: "_OCCLUSION_ANOMALY_FRACTION = 0.0005 module constant (clause-(d) citation, NOT CLI-tunable); _DEFAULT_MAX_N_VAR = 120000; --max-n-var flag; the two gates in process_region's square path"
      contains: "_OCCLUSION_ANOMALY_FRACTION"
    - path: "tests/m3/test_run_native_ld_panel.py"
      provides: "section 19: 7 new gate tests (shared -k substrings anomaly_gate / feasibility_ceiling / max_n_var_default / fail_fast_halts_on_deferral)"
      contains: "deferred_occlusion_anomaly"
    - path: ".planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-BROWSER-PASTE.md"
      provides: "Stage-C hold replaced by dated LIFTED note with commit SHA + deferral vocabulary"
    - path: ".planning/quick/260813-t21-wire-the-two-pre-fire-producer-gates-trs/260813-t21-SUMMARY.md"
      provides: "RED/GREEN/mutation evidence log + suite counts + commit SHAs"
  key_links:
    - from: "main() argparse --max-n-var"
      to: "process_region(max_n_var=...)"
      via: "run_native_ld_panel(max_n_var=...) keyword threading"
      pattern: "max_n_var"
    - from: "process_region square path"
      to: "_OCCLUSION_ANOMALY_FRACTION"
      via: "MODULE-GLOBAL name lookup at gate-evaluation time (monkeypatchable — see Task 1 note M)"
      pattern: "_OCCLUSION_ANOMALY_FRACTION \\* pre_window_n_var"
    - from: "test_max_n_var_default_pins_consumer_ceiling"
      to: "config/pipeline.yaml m3_convert_max_n_var"
      via: "yaml.safe_load in the test (PyYAML 6.0.3 confirmed in smoke_dev)"
      pattern: "m3_convert_max_n_var"
---

<objective>
Wire the two MISSING pre-fire producer gates into `run_native_ld_panel.py`'s
`process_region()` square path, TDD (RED first), then lift the Stage-C hold in the
260812-ox1 browser packages.

Purpose: the ~11-day / $385-1,084 Stage-C fire is HELD on exactly these two gates.
(1) trsx5 clause (d) — an occluded count above 0.0005 x n_var marks a substrate
anomaly that must be DEFERRED and disclosed, never auto-excluded (today the driver
excludes unconditionally, which the pre-registration prohibits). (2) The n_var
feasibility ceiling — the standing diagnosis `.planning/debug/m3-producer-unbounded-
dense-read.md` proved the producer materializes dense n_var^2 float32 with NO bound
(52/276 AFR cells exceed the consumer's 120,000 ceiling; the 2026-06-28 fire
OOM-killed at region 1). This task carries the consumer's number back to the
producer, as the diagnosis prescribes.

This task implements pre-registered policy; it makes NO new scientific decision.
If any step appears to require one, STOP and report.

Output: two gates + `--max-n-var` CLI threading, 7 new tests (baseline 907 -> 914),
Stage-C hold lifted in 3 runbook docs, HANDOFF/STATE/SUMMARY refreshed.
</objective>

<context>
@.planning/quick/260813-t21-wire-the-two-pre-fire-producer-gates-trs/260813-t21-CONTEXT.md
@src/python/run_native_ld_panel.py
@tests/m3/test_run_native_ld_panel.py
@tests/m3/test_gate.py
@.planning/debug/m3-producer-unbounded-dense-read.md
@.planning/amendments/osf-amendment-afr-occlusion-exclude-UPDATE-2026-07-10.md   (READ-ONLY — clause (d) at :61)
@config/pipeline.yaml   (:393 m3_convert_max_n_var: 120000 — READ, never edit)

<interfaces>
<!-- Extracted from the codebase; the executor should not need to re-derive these. -->

From src/python/run_native_ld_panel.py (as of HEAD):
- `process_region(row, *, bfile_prefix, out_dir, mode="square", panel_tsv=None, scratch_dir=None) -> dict`
  Square path (:792-846): `pre_window_n_var, pre_window_bim = _window_bim_n_var(bim_path, chrom, from_bp, to_bp)`
  -> `raw_rows` parse -> `occluded_ids, occlusion_edges = detect_occluded_variants(raw_rows)`
  -> `if occluded_ids:` writes `{out_prefix}.occluded.excludelist` + shared/per-region
  occlusion manifests -> `build_plink_ld_command(...)` at :847.
  The `skipped_idempotent` early-return SHAPE to mirror (:748-757): build result dict
  -> `append_panel_row(panel_tsv, result, scratch_dir=compute_dir)` -> `return result`.
  Tail `append_panel_row` at :971 runs only if no early return (append dedups by region_id).
- `run_native_ld_panel(manifest_path, bfile_prefix, out_dir, *, mode="square", panel_tsv=None,
  ancestry="AFR", num_shards=1, shard_index=0, scratch_dir=None, fail_fast=False) -> list[dict]`
  fail-fast seam (:1099): `if fail_fast and str(res.get("status")) != "ok": raise RegionGateError(...)`
  — deferrals halt AUTOMATICALLY under fail_fast; no code change needed there.
- `main(argv=None)` argparse at :1104-1141; `--fail-fast` help at :1134-1137.
- `_PANEL_COLUMNS` (:101-120) = 9 columns: region_id, chr, n_var, wall_min, peak_ram_gib,
  output_gib, status, n_dropped_occluded, n_dropped_monomorphic. DO NOT CHANGE.
- Status vocabulary precedent: `ok` / `verify_failed` / `error: ...` / `skipped_idempotent`.

From src/python/occlusion_span_filter.py (FROZEN — import only):
- `detect_occluded_variants(rows: Sequence[Sequence]) -> tuple[list[str], list[OcclusionEdge]]`
  rows = 6-field .bim rows [chr, snp_id, cm, bp, A1=ALT, A2=REF]. Occlusion rule:
  V occluded iff exists window D with len(REF_D) > 1 and POS_D < POS_V <= POS_D + len(REF_D) - 1.

From tests/m3/test_run_native_ld_panel.py (harness — reuse, do not rebuild):
- `_setup_cohort(tmp_path, n=20, chrom=12, bp0=53_000_000)` -> (bfile_prefix, bim, (chrom, from_bp, to_bp))
- `_default_bim_rows(n, chrom, bp0)` — plain SNPs spaced 100 bp, A1="A", A2="T"
- `_write_bim` / `_write_af` / `_write_manifest` (manifest cols: region_id, chr, ancestry,
  window_start_grch38, window_end_grch38)
- `_MockPlink(bim_path, *, corrupt_regions=None, seed=0, mono_snps=None, nan_snps=None)` —
  records `.calls` (argv list) + `.exclude_calls`; writes synthetic `.ld.bin` sized from the window
- `_MockGsutil()` — records `.calls` ([verb, src, dst]); `.contents[uri]` banks uploaded bytes;
  cp destinations = `[c[2] for c in mock_gs.calls if c[0] == "cp"]`
- `_setup_region1_cohort(tmp_path)` -> (bfile, bim, (chrom=1, 1_980_000, 8_400_000), occluded_ids, vid_at)
  — 11 rows (7 deletions + 4 SNPs), EXACTLY 5 occluded. NOTE: 5/11 = 45% (see Task 1 note F).
- `drv._DEFAULT_PANEL_NAME`, `drv._gs_join`, `drv.content_verify_npz` monkeypatch precedent (:2008-2011).
- Style reference: the 5 `per_region_occlusion_manifest` tests (:1894-2091) — shared `-k`
  name substring, docstrings stating WHY + the negative-control story.

From tests/m3/test_gate.py: `RegionGateError` carries `.region_id` / `.status`; existing
tests already prove fail_fast halts on ANY non-'ok' status via a stubbed process_region.

Suite commands (HANDOFF.json, verified):
- `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3 -q`  (~14 min; baseline 907/31/0)
- `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/phase2 -q`  (~2 s; 136/1/0)
- After tests/m3: `git checkout -- tests/m3/sparse_parent_benchmark.tsv`  (known jitter)
- PyYAML 6.0.3 present in smoke_dev (verified 2026-08-13).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — RED tests, then wire the two gates + --max-n-var threading</name>
  <files>tests/m3/test_run_native_ld_panel.py, src/python/run_native_ld_panel.py</files>
  <behavior>
7 new tests in a new section 19 of tests/m3/test_run_native_ld_panel.py (names carry
`anomaly_gate` / `feasibility_ceiling` / `max_n_var_default` / `fail_fast_halts_on_deferral`
so one `-k` selects the suite):

1. `test_occlusion_anomaly_gate_defers_never_excludes` (CONTEXT (a); gs mode — the
   fire-faithful surface). Build ONE cohort .bim = the 11 region-1-topology rows
   (from `_region1_fixture_module()._REGION1_BIM_ROWS`, chr1 1.98-8.4 Mb) + 20 plain
   rows `_default_bim_rows(20, chrom=1, bp0=30_000_000)`; `_write_af` for 31 rows.
   Manifest: row 1 = anomaly window (m2_region_00001, 1_980_000-8_400_000; 11 in-window,
   5 occluded, 5 > 0.0005*11 = 0.0055 with the REAL constant -> fires); row 2 = clean
   window (m2_region_00002, 30_000_000-30_001_900; 20 rows -> ok).
   `_MockPlink(bim, nan_snps=occluded_ids)`, `_MockGsutil()`,
   gs_out = "gs://test-bucket/ld/AFR_aou", scratch_dir=tmp. Assert:
   - res[0]["status"].startswith("deferred_occlusion_anomaly") and "5 occluded of 11" in it
   - res[0]["n_dropped_occluded"] is None and res[0]["n_var"] is None
   - NOTHING excluded: no *.occluded.excludelist and no *occlusion_manifest.tsv anywhere
     under scratch (clause (d): defer, NOT auto-exclude)
   - plink NEVER ran for region 1: len(mock.calls) == 1 and that argv's --out endswith
     m2_region_00002
   - NO cp of any region-1 artifact: no cp dst contains "m2_region_00001"; permitted cp
     dsts are ONLY the panel-TSV URI + region-2 artifacts (mirror the :2024-2032 pattern)
   - loop CONTINUED: res[1]["status"] == "ok" and the region-2 .npz uri was uploaded
   - panel TSV (mock_gs.contents[panel_uri]): header line == "\t".join(drv._PANEL_COLUMNS),
     len(drv._PANEL_COLUMNS) == 9, both region rows present, region-1 row carries the
     deferred status in the status column
2. `test_occlusion_anomaly_gate_open_at_subceiling_rate` (CONTEXT (b), REAL constant).
   Cohort: 2,000 plain SNPs (`_default_bim_rows(2000, chrom=12)`) + 1 deletion inserted
   at bp0+50 with A2=REF of length 60 (e.g. "T"*60) — occludes ONLY the SNP at bp0+100
   (rule: (POS_D, POS_D+59] contains exactly that bp). n_var = 2001, occluded = 1,
   ceiling = 1.0005, 1 > 1.0005 is False -> gate stays OPEN. Local out_dir. Assert the
   FULL existing artifact set lands: status "ok"; excludelist file exists with exactly
   the 1 occluded id; "--exclude" in the plink argv; n_dropped_occluded == 1; .npz
   exists and drv.content_verify_npz passes. (Mock dense matrix = 2001^2 float32
   = 16 MB — cheap.)
3. `test_occlusion_anomaly_gate_boundary_strict_greater`. Same construction with 1,999
   plain SNPs + 1 deletion -> n_var = 2000, occluded = 1, ceiling = 0.0005*2000 = 1.0
   EXACTLY. Clause (d) says "exceeds" -> strict >; 1 > 1.0 is False -> status "ok",
   excludelist written. (count == ceiling does NOT defer.)
4. `test_feasibility_ceiling_defers_infeasible_region` (CONTEXT (c); local mode).
   `_setup_cohort` (20 rows); manifest row 1 = full window (20 in-window), row 2 = a
   sub-window covering rows 0-4 only (from_bp..from_bp+400, 5 in-window). Call
   `run_native_ld_panel(..., max_n_var=10)`. Assert:
   - res[0]["status"] == "deferred_infeasible_square: n_var=20 > ceiling=10" (LOCKED format)
   - res[0]["n_var"] is None; only 1 plink call, for region 2; no region-1 .npz or
     excludelist under out_dir
   - loop continued: res[1]["status"] == "ok"
   - panel TSV under out_dir: 9-col header, deferred row present
5. `test_feasibility_ceiling_runs_before_occlusion_detect` (LOCKED ordering: ceiling
   FIRST — it needs only the count; no occlusion detect on an infeasible region).
   Region-1-topology cohort, ONE-region manifest; monkeypatch `drv.detect_occluded_variants`
   with a spy that increments a counter then delegates to the real function. Run once
   with max_n_var=5 (11 > 5 -> defer): spy count == 0. Run again (fresh out_dir) with
   max_n_var=120000 and the anomaly gate pinned open (note F / raising=False caveat):
   spy count == 1 — the BUILT-IN control proving the spy can detect a call.
6. `test_max_n_var_default_pins_consumer_ceiling` (CONTEXT (d) — THE CARRIED-BACK-NUMBER
   PIN; the debug file's thesis made enforceable). Monkeypatch `drv.run_native_ld_panel`
   with a stub capturing kwargs and returning []; call
   `drv.main(["--bfile-prefix", "x", "--out-dir", str(tmp_path)])`; then
   `cfg = yaml.safe_load((PROJECT_ROOT / "config" / "pipeline.yaml").read_text())`.
   Assert `isinstance(cfg["m3_convert_max_n_var"], int)` (loud failure if the key moves)
   and `captured["max_n_var"] == cfg["m3_convert_max_n_var"]`. The YAML read IS the
   test — never assert two literal 120000s.
7. `test_fail_fast_halts_on_deferral` (CONTEXT (e) — documents the LOCKED semantics:
   deferrals halt under --fail-fast; Stage C therefore runs WITHOUT it). Real harness,
   2-region manifest (infeasible first, feasible second), `fail_fast=True, max_n_var=10`:
   `pytest.raises(RegionGateError)`; `ei.value.status.startswith("deferred_infeasible_square")`;
   zero plink calls (region 2 never reached).
  </behavior>
  <action>
**Step 1 — RED (commit 1).** Write the 7 tests above. Touch NOTHING else. Run the named
RED command (verify block, command 1). EXPECTED RED/GREEN split — record verbatim in the
SUMMARY evidence block:
- RED (5): tests 1, 4, 5, 6, 7. Test 1 fails on the status assertion (driver currently
  excludes and returns "ok"); 4 and 7 fail with TypeError (unexpected kwarg max_n_var);
  5 fails on the spy count (detect IS called today); 6 fails with KeyError (max_n_var
  absent from captured kwargs).
- GREEN-before (2): tests 2 and 3 pin NO-CHANGE behavior, so RED-first is STRUCTURALLY
  IMPOSSIBLE for them (pre-implementation the driver returns "ok" on every sub-ceiling
  region — exactly what they assert). Their failability is MUTATION-PROVEN in Step 3.
NOTE: any pre-implementation monkeypatch of `drv._OCCLUSION_ANOMALY_FRACTION` would
raise AttributeError — so test 5's second run must use
`monkeypatch.setattr(drv, "_OCCLUSION_ANOMALY_FRACTION", 1.0, raising=False)` (the ONLY
place raising=False is permitted; it self-heals at GREEN).
Commit 1 (explicit path — NEVER `git add .` / `-A` on this GPFS tree):
  `git add tests/m3/test_run_native_ld_panel.py`
  msg: `test(260813-t21): RED — clause-(d) anomaly gate + n_var feasibility ceiling (5 red observed; 2 no-change pins deferred to mutation proof)`

**Step 2 — GREEN (commit 2).** Implement in src/python/run_native_ld_panel.py:
(A) Module constants near _PANEL_COLUMNS:
    `_OCCLUSION_ANOMALY_FRACTION = 0.0005` — comment citing trsx5 clause (d)
    (.planning/amendments/osf-amendment-afr-occlusion-exclude-UPDATE-2026-07-10.md:61,
    PASTE block "(d) Anomaly gate (per region)"): a count EXCEEDING fraction x n_var is
    a substrate anomaly -> DEFER for re-diagnosis + disclose as a deviation, NEVER
    auto-exclude. Pre-registered — deliberately NOT CLI-tunable (a knob would invite
    silent deviation).
    `_DEFAULT_MAX_N_VAR = 120000` — comment: MUST equal m3_convert_max_n_var
    (config/pipeline.yaml:393, the consumer ceiling from the 2026-08-05 blast-radius D
    remediation); equality pinned by test_max_n_var_default_pins_consumer_ceiling;
    diagnosis: .planning/debug/m3-producer-unbounded-dense-read.md ("nobody carried the
    number back to the producer").
(B) `process_region(..., max_n_var: int = _DEFAULT_MAX_N_VAR)` — keyword-only param.
(C) GATE 1 — feasibility ceiling, inside `if mode == "square":` IMMEDIATELY after the
    `pre_window_n_var, pre_window_bim = _window_bim_n_var(...)` line (:793-795), BEFORE
    the raw_rows parse (the ceiling needs only the count):
    ```python
    if pre_window_n_var > max_n_var:
        result["status"] = (f"deferred_infeasible_square: n_var={pre_window_n_var} "
                            f"> ceiling={max_n_var}")
        print(f"region {region_id}: DEFERRED (infeasible square) — {result['status']}",
              file=sys.stderr, flush=True)
        append_panel_row(panel_tsv, result, scratch_dir=compute_dir)
        return result
    ```
    (mirrors the skipped_idempotent early-return shape :748-757; n_var / n_dropped_*
    stay None — the observed count lives in the status string, the "error: ..."
    detail-in-status precedent. The early return skips the tail append at :971 —
    correct: the row is already appended, and append_panel_row dedups regardless.)
(D) GATE 2 — anomaly gate, after `detect_occluded_variants(raw_rows)` (:801) and BEFORE
    `if occluded_ids:` (the excludelist write / manifests / plink must NEVER run on an
    anomaly region — clause (d): "NOT auto-excluded"):
    ```python
    if len(occluded_ids) > _OCCLUSION_ANOMALY_FRACTION * pre_window_n_var:
        result["status"] = (
            f"deferred_occlusion_anomaly: {len(occluded_ids)} occluded of "
            f"{pre_window_n_var} (ceiling "
            f"{int(_OCCLUSION_ANOMALY_FRACTION * pre_window_n_var)})")
        print(f"region {region_id}: DEFERRED (occlusion anomaly, trsx5 clause (d)) — "
              f"{result['status']}", file=sys.stderr, flush=True)
        append_panel_row(panel_tsv, result, scratch_dir=compute_dir)
        return result
    ```
    Comment: cite clause (d) + the region-1 headroom math (real region 1: 5/102,421 =
    4.9e-5 vs ceiling 51 = int(0.0005 x 102,421) — 10x headroom; the gate must not
    fire there).
(E) Thread the number: `run_native_ld_panel(..., max_n_var: int = _DEFAULT_MAX_N_VAR)`
    passes `max_n_var=max_n_var` to process_region; `main()` adds
    `p.add_argument("--max-n-var", dest="max_n_var", type=int,
    default=_DEFAULT_MAX_N_VAR, help=...)` and passes `max_n_var=args.max_n_var`.
    Help text: feasibility ceiling for square mode (dense n_var^2 float32 must fit ONE
    process's RAM); default MUST equal the consumer's m3_convert_max_n_var
    (config/pipeline.yaml); over-ceiling regions get status deferred_infeasible_square
    and the loop continues.
(F) FIXTURE-DENSITY ADAPTATION (the one non-obvious edit — planned, not discovered
    mid-task): the region-1 TEST fixture is 11 rows / 5 occluded = 45%, a
    topology-dense synthetic; the REAL region 1 is 5/102,421 = 4.9e-5. With the
    pre-registered fraction the gate FIRES on the fixture, so these 9 existing tests
    (which correctly assert sub-ceiling exclude-in-lockstep behavior) must PIN THE GATE
    OPEN with one line each (or one tiny shared pytest fixture):
    `monkeypatch.setattr(drv, "_OCCLUSION_ANOMALY_FRACTION", 0.5)` + comment
    "fixture is topology-dense (5/11); pin the clause-(d) gate OPEN — the gate's own
    tests live in section 19":
      test_driver_writes_occluded_excludelist_with_exactly_the_occluded_ids,
      test_exclude_reaches_the_plink_argv,
      test_keep_allele_order_still_present_alongside_exclude,
      test_occlusion_filtered_npz_has_no_nan_and_verifies,
      test_n_dropped_occluded_is_separated_from_n_dropped_monomorphic,
      test_gs_per_region_occlusion_manifest_uploaded_for_occluded_region,
      test_gs_per_region_occlusion_manifest_only_for_occluded_regions,
      test_gs_per_region_occlusion_manifest_never_uploaded_on_verify_failed,
      test_per_region_occlusion_manifest_leaves_shared_local_manifest_byte_unchanged.
    The pin patches the CONSTANT, never the clause; the asserted DRIVER behavior stays
    byte-unchanged (CONTEXT (b)).
(M) IMPLEMENTATION CONSTRAINT the pins depend on: the gate must read
    `_OCCLUSION_ANOMALY_FRACTION` as a MODULE-GLOBAL lookup at evaluation time (a plain
    reference inside process_region, exactly as written in (D)) — NEVER bound as a
    default argument or captured at import time, or every monkeypatch above is a no-op.
Guards to respect (existing source-scan tests): no module-scope hail or yaml import in
the driver (the YAML read lives ONLY in the test); new comments must not contain the
strings condition_ld_matrix / write_conditioned_ld_npz / nan_to_num (:1787-1796 scans
the source text); no absolute paths (:501). fail_fast needs NO code change (:1099
already halts on any non-'ok'); only the --fail-fast help text gains one sentence:
"Deferrals (deferred_infeasible_square / deferred_occlusion_anomaly) also halt —
deliberate: Stages A/B contain only feasible, far-below-ceiling regions; Stage C runs
without --fail-fast so deferrals continue the loop."
Run verify command 1 — ALL selected tests green. Commit 2:
  `git add src/python/run_native_ld_panel.py tests/m3/test_run_native_ld_panel.py`
  msg: `feat(260813-t21): wire clause-(d) anomaly gate + --max-n-var feasibility ceiling into the square path`

**Step 3 — MUTATION-PROOF the two no-change pins** (on top of the CLEAN committed tree,
so revert = git checkout — [[feedback_green_assertion_needs_a_negative_control]]). For
each: edit src/python/run_native_ld_panel.py, run verify command 1, OBSERVE the named
test red, then `git checkout -- src/python/run_native_ld_panel.py` and re-run to
confirm green again:
  (i) `_OCCLUSION_ANOMALY_FRACTION = 0.0005` -> `1e-9`  =>
      test_occlusion_anomaly_gate_open_at_subceiling_rate MUST go red (gate fires on
      1/2001). If it stays green the pin is vacuous — STOP and fix before proceeding.
  (ii) gate comparison `>` -> `>=`  =>
      test_occlusion_anomaly_gate_boundary_strict_greater MUST go red (fires at
      count == ceiling).
Each mutation is a real file edit with a byte-length change (no .pyc-staleness trap —
[[feedback_negative_control_defeated_by_bytecode_cache]]); record both observed-red
outputs verbatim in the SUMMARY evidence block. All other new assertions carry their
negative control as the Step-1 observed RED.
  </action>
  <verify>
    <automated>/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_run_native_ld_panel.py tests/m3/test_gate.py -k "anomaly_gate or feasibility_ceiling or max_n_var_default or fail_fast_halts_on_deferral or occlusion or per_region or fail_fast" -q</automated>
    Run this SAME command three times with three different expected outcomes:
    (1) after Step 1: exactly 5 of the 7 new tests FAIL (named above), tests 2-3 pass,
        all pre-existing selected tests pass;
    (2) after Step 2: everything selected passes;
    (3) during Step 3 mutations: the single named test fails per mutation, then all
        pass again after git checkout.
  </verify>
  <done>
    7 new tests green; the 5 RED observations + 2 mutation-proof observations recorded;
    both gates live in the square path with the LOCKED status strings; --max-n-var
    threaded main() -> run_native_ld_panel() -> process_region() with default
    _DEFAULT_MAX_N_VAR = 120000; the 9 existing fixture tests pinned open and green;
    commits 1 (RED) and 2 (GREEN) landed with explicit paths; working tree clean except
    untracked planning files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full-suite verification + Stage-C hold lift docs</name>
  <files>.planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-AGENT-PROMPT.md, .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-BROWSER-PASTE.md, .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md, .planning/HANDOFF.json, .planning/STATE.md, .planning/quick/260813-t21-wire-the-two-pre-fire-producer-gates-trs/260813-t21-SUMMARY.md</files>
  <action>
**Step 1 — full suites** (order matters: suites first, docs only if green):
  `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3 -q`
    -> MUST be exactly `914 passed, 31 skipped` (907 baseline + 7 new), 0 failed.
  `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/phase2 -q`
    -> MUST be exactly `136 passed, 1 skipped`, 0 failed.
  SKIP DISCIPLINE: skips must stay EXACTLY 31 and 1 — a new test landing as a SKIP is
  NOT evidence; check the skip count, not just the failure count. If m3 shows 32+
  skips, a new test is being skipped: find it and fix before proceeding.
  Then restore the jitter file: `git checkout -- tests/m3/sparse_parent_benchmark.tsv`
  and confirm `git status --porcelain` shows no unexpected modifications (no perimeter
  files, no frozen files, nothing outside the declared files_modified set).

**Step 2 — Stage-C hold lifts.** These three files live in a DATED QUICK DIR, which is
normally off-limits — they are the CONTEXT-LOCKED exception ("After the code lands"
section names them explicitly). Edit ONLY these three files there; capture the GREEN
commit SHA from Task 1 (`git rev-parse --short HEAD` after commit 2) as {SHA}:
  (a) 260812-ox1-AGENT-PROMPT.md — REPLACE the hold paragraph at :161-168
      ("⚠ STAGE C IS ADDITIONALLY HELD (2026-08-13) pending two producer-side gates
      ... via git pull).") with a dated lifted note:
      "✅ STAGE C HOLD LIFTED (2026-08-13, commit {SHA}): both producer gates are wired
      in run_native_ld_panel.py — the pre-registered clause-(d) anomaly gate
      (0.0005 x n_var, defer-not-exclude) and the --max-n-var feasibility ceiling
      (default 120000 = the consumer's m3_convert_max_n_var). `git pull` on the VM
      before Stage C. In the panel TSV, `deferred_infeasible_square` and
      `deferred_occlusion_anomaly` rows are THE GATES WORKING — expected for ~29+
      regions above the ceiling; the bankable target is 276 MINUS deferrals, and no
      deferral count is a pre-committed expectation (the count emerges at fire time).
      The STEP-10 monitoring rollup already keys by status: report ok /
      deferred_infeasible_square / deferred_occlusion_anomaly / error counts
      SEPARATELY. The fire invocation is unchanged (no new flag needed; the default
      ceiling is the gate) and Stage C still runs WITHOUT --fail-fast — with it,
      the first deferral would halt the loop."
  (b) 260812-ox1-BROWSER-PASTE.md — REPLACE the "## ⚠ STAGE C HOLD (added 2026-08-13)"
      section body at :282-288 with the same dated lifted note (keep the heading,
      retitled "## ✅ STAGE C HOLD LIFTED (2026-08-13)").
  (c) 260812-ox1-READY-TO-FIRE.md — item 10 (## 10. STEP B — THE FIRE): ADD one short
      dated paragraph (do not restructure the item): deferral vocabulary as above,
      `deferred_*` rows = gates working, bankable target = 276 minus deferrals, rollup
      reports the four status classes separately; PLUS the post-fire disclosure duty
      (note only — NOT implemented here): per clause (d), the measured deferral list is
      disclosed as a deviation at STEP E/F time.
  DO NOT touch the poll commands, the never-prefix rule, the 276-is-not-a-pass-bar
  block, or anything else in these files.

**Step 3 — bookkeeping + commit 3.**
  - .planning/HANDOFF.json: update suite_baselines "tests/m3" to
    "914 passed / 31 skipped / 0 failed — raised from 907 by quick-260813-t21 (+7
    producer-gate tests, 2026-08-13); prior: raised from 902 by quick-260812-ox1".
    Leave the tests/phase2 line and the skip rule untouched.
  - .planning/STATE.md: append/refresh the current-state note (t21 landed: both
    pre-fire producer gates wired + hold lifted; Stage C unblocked pending Carter's
    fire-gate sequence) — atomic with this commit ([[feedback_state_md_keep_current]]).
  - Write 260813-t21-SUMMARY.md in this task's quick dir: what landed (both gates,
    status strings, threading), the RED evidence (5 named red outputs), the mutation
    evidence (2 named red outputs + restore-green), fixture-density adaptation
    rationale (5/11 vs real 4.9e-5), suite counts (914/31/0 + 136/1/0), the three doc
    lifts, commit SHAs. Frame: pre-registered policy implemented; no scientific
    decision made.
  - Commit 3 (explicit paths only):
    `git add .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-AGENT-PROMPT.md .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-BROWSER-PASTE.md .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md .planning/HANDOFF.json .planning/STATE.md .planning/quick/260813-t21-wire-the-two-pre-fire-producer-gates-trs/260813-t21-PLAN.md .planning/quick/260813-t21-wire-the-two-pre-fire-producer-gates-trs/260813-t21-CONTEXT.md .planning/quick/260813-t21-wire-the-two-pre-fire-producer-gates-trs/260813-t21-SUMMARY.md`
    msg: `docs(260813-t21): lift Stage-C hold — both producer gates landed; suites 914/31 + 136/1`
  </action>
  <verify>
    <automated>/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3 tests/phase2 -q</automated>
    Expect exactly: tests/m3 914 passed / 31 skipped / 0 failed AND tests/phase2 136
    passed / 1 skipped / 0 failed (combined run acceptable if totals reconcile:
    1050 passed / 32 skipped). Then:
    `git checkout -- tests/m3/sparse_parent_benchmark.tsv`
    `grep -c "STAGE C HOLD LIFTED" <each of the two browser packages>` -> 1 each;
    `grep -c "deferred_infeasible_square" 260812-ox1-READY-TO-FIRE.md` -> >= 1;
    `grep -n "ADDITIONALLY HELD\|Wait for the repo update" <the two packages>` -> no hits;
    `git status --porcelain` clean after commit 3.
  </verify>
  <done>
    Both suites green at 914/31/0 and 136/1/0 with skips exactly 31/1; benchmark TSV
    restored; the two hold blocks replaced by dated lifted notes carrying the GREEN
    commit SHA and the deferral vocabulary; READY-TO-FIRE item 10 carries the
    deferral + clause-(d) disclosure paragraph; HANDOFF.json baseline = 914;
    STATE.md current; SUMMARY.md written with full RED/mutation evidence; commit 3
    landed with explicit paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NCSU repo -> AoU fire VM (git pull) | the gates guard a $385-1,084 billed run; a silent gate regression re-opens the OOM / pre-registration-violation exposure |
| driver -> pre-registered OSF policy | clause (d) is a public commitment; code that auto-excludes above the fraction is a disclosure violation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-t21-01 | Tampering | _OCCLUSION_ANOMALY_FRACTION | mitigate | module constant with clause-(d) citation, NOT CLI-tunable; boundary + subceiling tests mutation-proven failable |
| T-t21-02 | Tampering | --max-n-var default vs consumer ceiling | mitigate | YAML-reading PIN test (never two hardcoded 120000s) — drift between producer and consumer fails the suite |
| T-t21-03 | Repudiation | deferred regions | mitigate | panel row appended on every deferral (status string carries counts); stderr disclosure line; clause-(d) deviation disclosure duty noted in READY-TO-FIRE item 10 |
| T-t21-04 | DoS | dense n_var^2 load on the fire VM | mitigate | GATE 1 defers before any plink/allocation; empirically the 2026-06-28 fire died here |
| T-t21-05 | Info disclosure | test fixtures / docs | accept | synthetic .bim rows only; no genotypes, no perimeter contact, $0 |
</threat_model>

<verification>
- Verify command 1 (Task 1) run three ways: RED split observed (5 red / 2 green-before),
  GREEN after implementation, single-red per mutation then green after checkout.
- Full suites: tests/m3 914/31/0, tests/phase2 136/1/0 — skips EXACTLY 31 and 1.
- `git log --oneline -3` shows the test/feat/docs commit triple with the t21 scope tag.
- No diffs outside the declared files_modified set; the three PY_FROZEN_RELS,
  run_susie_rss.R, .planning/amendments/, docs/manuscript/, results/, and every OTHER
  dated quick dir untouched (`git status --porcelain` + `git diff --stat HEAD~3..HEAD`).
- Zero perimeter contact: no gsutil/gcloud/wb invocation anywhere in this task
  (_MockGsutil only).
</verification>

<success_criteria>
- Clause-(d) anomaly gate live: over-fraction regions defer with the LOCKED status
  string, nothing excluded, no artifacts, loop continues — proven by a RED-first test.
- Feasibility ceiling live and ordered FIRST: over-ceiling regions defer pre-detect,
  no plink — proven by RED-first tests including the ordering spy.
- The consumer's 120,000 carried back to the producer and PINNED against
  config/pipeline.yaml by a YAML-reading test.
- fail_fast halts on deferrals (unchanged code, now documented + tested).
- Suites 914/31/0 + 136/1/0; skips 31/1; benchmark TSV restored.
- Stage-C hold lifted in AGENT-PROMPT, BROWSER-PASTE, and READY-TO-FIRE item 10 with
  the deferral vocabulary + disclosure duty; HANDOFF baseline recorded.
- Remaining human step unchanged: Carter's fire-gate sequence (never an agent).
</success_criteria>

<output>
After completion, create
`.planning/quick/260813-t21-wire-the-two-pre-fire-producer-gates-trs/260813-t21-SUMMARY.md`
(content per Task 2 Step 3), committed in commit 3.
</output>
