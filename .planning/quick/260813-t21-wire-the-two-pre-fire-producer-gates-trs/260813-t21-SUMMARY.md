---
phase: quick-260813-t21
plan: 1
subsystem: m3-aou-afr-ld-panel-build (native-plink LD loop driver)
tags: [trsx5-clause-d, anomaly-gate, feasibility-ceiling, max-n-var, tdd, pre-fire, stage-c-hold-lift]
requires:
  - trsx5 clause (d) (.planning/amendments/osf-amendment-afr-occlusion-exclude-UPDATE-2026-07-10.md:61 — READ-ONLY, unchanged)
  - .planning/debug/m3-producer-unbounded-dense-read.md (standing diagnosis — remedied here)
  - config/pipeline.yaml:393 m3_convert_max_n_var: 120000 (READ by the pin test, never edited)
provides:
  - clause-(d) anomaly gate live in process_region square path (defer-not-exclude)
  - --max-n-var feasibility ceiling (default 120000 = consumer ceiling, YAML-pinned)
  - Stage-C hold LIFTED in the three 260812-ox1 runbook docs
affects: [m3-04c Task 3 Stage C fire, 260812-ox1 browser packages]
key-files:
  modified:
    - src/python/run_native_ld_panel.py
    - tests/m3/test_run_native_ld_panel.py
    - .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-AGENT-PROMPT.md
    - .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-BROWSER-PASTE.md
    - .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md
    - .planning/HANDOFF.json
decisions: []   # pre-registered policy implemented; NO new scientific decision made
metrics:
  duration: ~50 min (commit 1 2026-08-13T21:22 ET -> commit 3)
  tasks: 2/2
  tests: 907 -> 914 (+7), skips 31 (unchanged)
completed: 2026-08-13
---

# Quick 260813-t21: Wire the two pre-fire producer gates — Summary

**One-liner:** Both missing pre-fire producer gates are now live in
`run_native_ld_panel.py`'s square path — the pre-registered trsx5 clause-(d)
occlusion anomaly gate (`> 0.0005 x n_var` DEFERS, never auto-excludes) and the
`--max-n-var` feasibility ceiling (default 120000, YAML-pinned to the consumer's
`m3_convert_max_n_var`) — TDD RED-first with mutation-proven no-change pins, and
the Stage-C hold is lifted in all three 260812-ox1 runbook docs.

This task implemented pre-registered policy; **no new scientific decision was
made** (CONTEXT LOCKED; verified during execution — no step required one).

## What landed

**GATE 1 — feasibility ceiling** (`process_region`, square path, immediately
after the `pre_window_n_var` count, BEFORE the raw_rows parse / occlusion
detect): `pre_window_n_var > max_n_var` → status
`deferred_infeasible_square: n_var={N} > ceiling={M}` (LOCKED format), panel row
appended, early return mirroring the `skipped_idempotent` shape, loop continues.
No plink, no artifacts, `n_var`/`n_dropped_*` stay `None` (detail-in-status,
the `error: ...` precedent).

**GATE 2 — clause-(d) anomaly gate** (after `detect_occluded_variants`, BEFORE
the excludelist write / manifests / plink): strict `>` per clause (d)
("exceeds"); status
`deferred_occlusion_anomaly: {k} occluded of {N} (ceiling {int(f*N)})`.
NOTHING excluded, no excludelist, no manifests, no uploads; panel row appended;
loop continues. `_OCCLUSION_ANOMALY_FRACTION = 0.0005` is a module constant with
the clause-(d) citation, deliberately NOT CLI-tunable, read as a MODULE-GLOBAL at
evaluation time (monkeypatchable — the pins depend on it).

**Threading:** `_DEFAULT_MAX_N_VAR = 120000` → `--max-n-var` argparse flag →
`run_native_ld_panel(max_n_var=...)` → `process_region(max_n_var=...)`.
`--fail-fast` code UNCHANGED (it already halts on any non-`ok` status, which now
includes deferrals); only its help text gained the documented-semantics sentence
(Stages A/B feasible-only; Stage C runs WITHOUT `--fail-fast`).

**7 new tests** (section 19; `-k` substrings `anomaly_gate` /
`feasibility_ceiling` / `max_n_var_default` / `fail_fast_halts_on_deferral`):
defer-never-excludes (gs mode, fire-faithful), subceiling-rate no-change pin,
boundary strict-greater no-change pin, infeasible-defers (LOCKED status format),
ceiling-before-detect ordering spy (with built-in spy control), the
carried-back-number YAML pin, fail-fast-halts-on-deferral.

## RED evidence (Step 1, observed verbatim — 5 red / 2 green-before)

Command: `pytest tests/m3/test_run_native_ld_panel.py tests/m3/test_gate.py -k
"anomaly_gate or feasibility_ceiling or max_n_var_default or
fail_fast_halts_on_deferral or occlusion or per_region or fail_fast" -q`
→ **`5 failed, 10 passed, 59 deselected`** (10 = 8 pre-existing selected + the
2 no-change pins, all passing):

1. `test_occlusion_anomaly_gate_defers_never_excludes` —
   `tests/m3/test_run_native_ld_panel.py:2179: AssertionError: assert False`
   on `res[0]["status"].startswith("deferred_occlusion_anomaly")` — the
   pre-gate driver excluded and returned "ok", exactly the clause-(d) violation.
2. `test_feasibility_ceiling_defers_infeasible_region` — `:2286: TypeError:
   run_native_ld_panel() got an unexpected keyword argument 'max_n_var'`
3. `test_feasibility_ceiling_runs_before_occlusion_detect` — `:2331: TypeError:
   run_native_ld_panel() got an unexpected keyword argument 'max_n_var'`.
   NOTE (checker-predicted): the OBSERVED first-RED mode is this TypeError
   against the pre-implementation signature, NOT the plan-predicted spy-count
   failure; the 5-red/2-green split held regardless.
4. `test_max_n_var_default_pins_consumer_ceiling` — `:2366: KeyError:
   'max_n_var'` (absent from the captured kwargs)
5. `test_fail_fast_halts_on_deferral` — `:2388: TypeError: run_native_ld_panel()
   got an unexpected keyword argument 'max_n_var'`

Tests 2 and 3 of section 19 (`subceiling_rate`, `boundary_strict_greater`) pin
NO-CHANGE behavior, so RED-first was structurally impossible; their failability
was mutation-proven below. The only permitted `raising=False` monkeypatch is the
section-19 ordering test's gate pin (self-heals at GREEN).

## GREEN (Step 2)

Same command → **`15 passed, 59 deselected`**. Full-file check:
`tests/m3/test_run_native_ld_panel.py + tests/m3/test_gate.py` → `74 passed`.

## Mutation evidence (Step 3, on the clean committed tree; revert = git checkout)

**(i) `_OCCLUSION_ANOMALY_FRACTION = 0.0005` → `1e-9`** →
`2 failed, 13 passed`:
`test_occlusion_anomaly_gate_open_at_subceiling_rate` AND
`test_occlusion_anomaly_gate_boundary_strict_greater` both RED
(`:2289: AssertionError: assert 'deferred_occ...0 (ceiling 0)' == 'ok'`;
stderr: `region m2_region_00001: DEFERRED (occlusion anomaly, trsx5 clause (d))
— deferred_occlusion_anomaly: 1 occluded of 2000 (ceiling 0)`). The double-red
is the checker-predicted behavior of the mutation working (1 > 2e-6 fires the
boundary test too), not an anomaly. After `git checkout --
src/python/run_native_ld_panel.py` → `15 passed`.

**(ii) gate-2 comparison `>` → `>=`** → `1 failed, 14 passed`: exactly
`test_occlusion_anomaly_gate_boundary_strict_greater` RED
(`:2289: AssertionError: assert 'deferred_occ...0 (ceiling 1)' == 'ok'`;
stderr: `deferred_occlusion_anomaly: 1 occluded of 2000 (ceiling 1)` — fired at
count == ceiling). After checkout → `15 passed`. Both mutations were real file
edits with byte-length changes (no .pyc-staleness trap).

## Fixture-density adaptation (planned, not discovered mid-task)

The region-1 TEST fixture is topology-dense: 5 occluded / 11 rows = 45%, vs the
REAL region 1 at 5/102,421 = 4.9e-5 (ceiling 51 — 10x headroom; the gate must
not and does not fire there). With the pre-registered fraction the gate FIRES on
the fixture, so the 9 existing exclude-in-lockstep tests (5 in section 16, 4 in
section 18) each pin the gate OPEN with one line —
`monkeypatch.setattr(drv, "_OCCLUSION_ANOMALY_FRACTION", 0.5)` — patching the
CONSTANT, never the clause; every asserted driver behavior stays byte-unchanged.

## Suites (Task 2 Step 1)

- `tests/m3`: **914 passed, 31 skipped, 0 failed** (838.74s) — 907 + 7, skips
  EXACTLY 31 (no new test landed as a skip)
- `tests/phase2`: **136 passed, 1 skipped, 0 failed** (2.17s) — skips exactly 1
- `git checkout -- tests/m3/sparse_parent_benchmark.tsv` run after the suite;
  `git status --porcelain` clean of unexpected modifications; zero perimeter
  contact throughout ($0; `_MockGsutil`/`_MockPlink` only)

## Stage-C hold lifts (Task 2 Step 2 — the CONTEXT-LOCKED dated-quick-dir exception)

All three carry the GREEN commit SHA `d9fbc63` and the deferral vocabulary
(`deferred_*` rows = THE GATES WORKING; bankable target = 276 minus deferrals;
no deferral count pre-committed; rollup reports ok /
deferred_infeasible_square / deferred_occlusion_anomaly / error SEPARATELY;
fire invocation unchanged; Stage C still runs WITHOUT `--fail-fast`):

- **AGENT-PROMPT.md** — hold paragraph (was :161-168) REPLACED by the dated
  lifted note.
- **BROWSER-PASTE.md** — hold section retitled `## ✅ STAGE C HOLD LIFTED
  (2026-08-13)` with the same note as body.
- **READY-TO-FIRE.md item 10** — one dated paragraph ADDED (item not
  restructured): deferral vocabulary + the clause-(d) post-fire disclosure duty
  (note only — the measured deferral list is disclosed as a deviation at STEP
  E/F time). Poll commands, never-prefix rule, and the 276-is-not-a-pass-bar
  block untouched.

Verify: `grep -c "STAGE C HOLD LIFTED"` = 1 in each browser package;
`grep "ADDITIONALLY HELD\|Wait for the repo update"` = no hits;
`grep -c deferred_infeasible_square READY-TO-FIRE.md` = 2 (>= 1).

## Deviations from plan

1. **[per orchestrator constraint] STATE.md NOT updated in commit 3.** The plan's
   Task 2 Step 3 called for an atomic STATE.md refresh, but the executing
   orchestrator's constraints explicitly reserve STATE.md/ROADMAP.md updates to
   the orchestrator. Skipped here; the orchestrator carries the position update
   (t21 landed: both pre-fire producer gates wired + hold lifted; Stage C
   unblocked pending Carter's fire-gate sequence — NEVER an agent).
2. **[Rule 1 - verify-gate reconciliation] BROWSER-PASTE body first line.**
   Following the plan's construction literally (retitled heading AND the note
   body both opening with "STAGE C HOLD LIFTED") made
   `grep -c "STAGE C HOLD LIFTED"` = 2, violating the plan's own verify gate
   (`-> 1 each`). The body now opens `✅ Lifted 2026-08-13, commit d9fbc63: ...`
   (content otherwise identical) so the gate passes as written. No weaker check
   substituted.

## Known Stubs

None — both gates are fully wired to the fire path; no placeholder values, no
unwired data sources introduced.

## Threat Flags

None — no new security-relevant surface beyond the plan's threat model
(T-t21-01..05 all addressed: constant not CLI-tunable + mutation-proven;
YAML-pin test; panel row + stderr on every deferral + disclosure duty noted;
GATE 1 defers before any allocation; synthetic fixtures only, $0).

## Commits

- `08dea40` — `test(260813-t21): RED — clause-(d) anomaly gate + n_var
  feasibility ceiling (5 red observed; 2 no-change pins deferred to mutation
  proof)`
- `d9fbc63` — `feat(260813-t21): wire clause-(d) anomaly gate + --max-n-var
  feasibility ceiling into the square path` ← the {SHA} in the hold-lift notes
- commit 3 — `docs(260813-t21): lift Stage-C hold — both producer gates landed;
  suites 914/31 + 136/1` (this file + the three ox1 docs + HANDOFF.json +
  PLAN/CONTEXT)

## Self-Check: PASSED

All 7 declared artifacts exist; commits 08dea40 + d9fbc63 present;
`_OCCLUSION_ANOMALY_FRACTION` in the driver and `deferred_occlusion_anomaly` in
the tests; modified tracked files exactly the declared set; the three
PY_FROZEN_RELS, run_susie_rss.R, .planning/amendments/, results/,
docs/manuscript/, DECISIONS.md and every other dated quick dir untouched.
