---
title: "Route A Step 2.2.b — Introduction R1 surgical edit pass + walk-through brief for 2.2.e / 2.2.f / 2.3 / 2.4"
quick_id: 260424-j64
mode: quick
status: ready
priority: high
phase: quick-260424-j64
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/manuscript/track_a_pivot.md
  - .planning/quick/260424-j64-route-a-step-2-2-b-introduction-rewrite-/260424-j64-NEXT-STEPS.md
autonomous: true
requirements:
  - ROUTE-A-2.2.b  # Introduction R1 surgical edit against TRACK-A-PIVOT.md §4.5
  - ROUTE-A-HANDOFF-2.2.e-2.4  # walk-through brief for remaining Route A steps
estimated_effort: "60–120 min total (Task 1: 45–90 min, Task 2: 15–30 min)"
must_haves:
  truths:
    - "Introduction paragraph 1 is trimmed ~20% with no 'debated for decades' rhetoric; 50%-T2D-hypertension comorbidity stat and refs 1–3 retained."
    - "Introduction paragraph 2 names Wallace 2021, Zou 2022, Weissbrod 2020 as the three identity-LD / LD-mismatch inflation references (via a placeholder convention resolvable at 2.2.f), replacing the opaque 20⁻22 range citation."
    - "Introduction paragraph 3 explicitly states that single-causal-variant coloc.abf with identity-LD can *produce* apparent overlap (not merely fail to resolve it); SuSiE-RSS + coloc.susie framed as the rigorous alternative; 'Systematic re-analysis ... has not been performed at scale' closing sentence preserved."
    - "Introduction paragraph 4 retains the candid AFR-as-limitation framing and Track B forward pointer; no sentence leads with health-equity-as-primary-motivation."
    - "Introduction paragraph 5 (numbered aims) matches §4.5 P5 exactly; curated candidate-locus validation subset language at line 48 is intact."
    - "Introduction contains zero residual 'thrifty-gene', 'antagonistic pleiotropy', 'Neel 1962', 'Williams 1957', 'machine learning', 'ML', 'classifier', or 'predictive model' terms (grep-verified on lines 32–48)."
    - "A next-steps walk-through brief exists at .planning/quick/260424-j64-.../260424-j64-NEXT-STEPS.md covering Route A Steps 2.2.e, 2.2.f, 2.3, and 2.4, each with scope / source-of-truth / dependencies / proposed command / success gate."
  artifacts:
    - path: "docs/manuscript/track_a_pivot.md"
      provides: "Introduction R1 surgical edits applied (lines 32–48 region)"
      contains: "Wallace 2021"
    - path: ".planning/quick/260424-j64-route-a-step-2-2-b-introduction-rewrite-/260424-j64-NEXT-STEPS.md"
      provides: "Hand-off note for Route A Steps 2.2.e / 2.2.f / 2.3 / 2.4"
      min_lines: 80
  key_links:
    - from: "docs/manuscript/track_a_pivot.md Introduction"
      to: ".planning/amendments/TRACK-A-PIVOT.md §4.5"
      via: "paragraph-level spec compliance"
      pattern: "Wallace 2021|Zou 2022|Weissbrod 2020"
    - from: ".planning/quick/260424-j64-.../260424-j64-NEXT-STEPS.md"
      to: "/home/ckclinto/.claude/plans/snappy-humming-pine.md §2.2.e–2.4"
      via: "per-step section with proposed /gsd-quick command"
      pattern: "2\\.2\\.e|2\\.2\\.f|2\\.3|2\\.4"
---

<objective>
Route A Step 2.2.b, R1 (first revision pass): apply surgical edits to the Introduction of `docs/manuscript/track_a_pivot.md` (current lines 32–48) per `.planning/amendments/TRACK-A-PIVOT.md` §4.5. The Introduction is already ~80% aligned after Step 2.2.a/c/d (commit 05a701a). This is a refinement pass, not a rewrite — specific paragraph-level edits, citation swaps, and tone tightening only. In the same session, also produce a concise hand-off brief for the four remaining Route A steps (2.2.e Discussion rewrite, 2.2.f References + supplementary, 2.3 Figures, 2.4 bioRxiv preprint) so future /gsd-quick sessions have scope / dependencies / success gates captured in writing.

Purpose: keep the manuscript moving along the critical path (2.2.b → 2.2.e → 2.2.f → 2.3 → 2.4) without losing context between /gsd-quick invocations, and produce an Introduction that reviewers at *Genome Medicine* can read without encountering residual pre-pivot framing.

Output:
  1. Edited `docs/manuscript/track_a_pivot.md` (Introduction section only, lines 32–48 region) — original research framing, fidelity to §4.5 spec, consistent numerics with TRACK-A-FROZEN-NUMBERS.md.
  2. New `260424-j64-NEXT-STEPS.md` — one section per remaining Route A step, structured as a hand-off note to future-Carter.

Out of scope (for this quick):
  - Abstract, Methods, Results, Discussion, Conclusion, References section edits.
  - Reference-list renumbering (that is Step 2.2.f).
  - Figure generation (Step 2.3).
  - Any docs commit (the orchestrator commits at workflow Step 8; do NOT `git add` or `git commit` inside the plan tasks).
  - ROADMAP.md updates (quick tasks are separate from planned phases).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@docs/manuscript/track_a_pivot.md
@.planning/amendments/TRACK-A-PIVOT.md
@.planning/amendments/TRACK-A-FROZEN-NUMBERS.md
@/home/ckclinto/.claude/plans/snappy-humming-pine.md

<editing_rules>
<!-- These are project-wide invariants that constrain every task in this plan. -->

1. **Original research framing.** This manuscript is hypothesis-driven original research. Any text visible in public-facing artifacts (docs, SUMMARY.md, commit messages if any) must NOT use "revision", "cleanup", "fix", or any language that frames the pivot as a correction. "R1" in internal planning = "first revision pass of my own draft". This is not a revision of a published paper. OK inside this plan's task descriptions, not in manuscript prose.

2. **Surgical, not wholesale.** The Introduction is 80% there after commits bde60e2 + 05a701a. Do NOT rewrite from scratch. Read the paragraph, identify the §4.5-mandated delta, apply the minimum edit. If a paragraph is already correct, state that explicitly in the SUMMARY.md and move on.

3. **Citation placeholder convention for R1.** Step 2.2.f will renumber the full reference list. During R1, insert new refs as bracketed author-year placeholders: `[Wallace 2021]`, `[Zou 2022]`, `[Weissbrod 2020]`. The existing `²⁰⁻²²` range citation on line 36 is replaced with these three placeholders. Step 2.2.f will resolve placeholders to numeric superscripts when it builds the final References section. Do NOT attempt partial renumbering now — it creates conflicts 2.2.f will have to undo.

4. **Numeric consistency with TRACK-A-FROZEN-NUMBERS.md.** Introduction prose contains only aims/framing numbers (50 regions, 3 aims). No Stage 2 numerics (51/96 CS, 0/0/9 tiers, 1274 QTL) appear in Introduction — those belong in Abstract and Results. Verify this remains true after edits.

5. **Evolutionary-medicine / ML scrub.** Per §4.17 and §4.5, the Introduction is NOT the place for thrifty-gene / antagonistic-pleiotropy framing (that is demoted to a single ~120-word Discussion paragraph) and NOT the place for ML framing (removed entirely from the paper). Grep lines 32–48 for: `thrifty`, `antagonistic`, `Neel 1962`, `Williams 1957`, `machine learning`, `\bML\b`, `classifier`, `predictive model`. All should return zero matches inside the Introduction after R1. (Grep on the full file will still match lines 114/118/204/230 — those are Methods/Discussion and out of scope.)

6. **GPFS, no worktree, solo-author, public-data.** Per CLAUDE.md: edit the file in place on `main` (no branch isolation). No wet-lab, no proprietary data, no R/Snakemake/Python code changes in this quick — this is a prose-only task.
</editing_rules>

<source_of_truth>
Authoritative editing brief: `.planning/amendments/TRACK-A-PIVOT.md` §4.5 (lines 65–73).
Authoritative references list: `.planning/amendments/TRACK-A-PIVOT.md` §9 (lines 280–301).
Frozen numerics anchor: `.planning/amendments/TRACK-A-FROZEN-NUMBERS.md`.
Downstream step scopes: `/home/ckclinto/.claude/plans/snappy-humming-pine.md` §2.2.e, §2.2.f, §2.3, §2.4.
</source_of_truth>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduction R1 surgical edit pass against TRACK-A-PIVOT.md §4.5</name>
  <files>docs/manuscript/track_a_pivot.md</files>
  <action>
Apply paragraph-by-paragraph surgical edits to the Introduction (currently at lines 32–48). Work in the order below; for each paragraph, re-read it first, decide whether it already satisfies §4.5, and apply only the minimum delta.

**Step 1 — Pre-edit scrub (grep guardrails).**
Run from the repo root to confirm the starting state of the Introduction region:
  `sed -n '32,48p' docs/manuscript/track_a_pivot.md`
  `grep -n -E "thrifty|antagonistic|Neel 1962|Williams 1957|machine learning|\\bML\\b|classifier|predictive model" docs/manuscript/track_a_pivot.md | awk -F: '$1 >= 32 && $1 <= 48'`
Expected: the second grep returns zero matches inside lines 32–48 (already clean per commit 05a701a — this task confirms, does not re-clean).

**Step 2 — Paragraph 1 (line 34, opening comorbidity framing).**
Per §4.5 P1: *stay with minor edits, trim ~20%, cut "debated for decades" rhetoric, keep the 50% T2D/hypertension comorbidity stat and refs 1–3.*
Action: read line 34. If "debated for decades" (or thrifty-gene / antagonistic-pleiotropy bleed from the pre-pivot source) is present, remove it. If not present, tighten any redundant rhetoric to hit the ~20% reduction target — aim for a paragraph of ~80–100 words vs the current ~110–130. Keep: (a) the "cardiometabolic conditions co-occur" opener, (b) the 50%-T2D-hypertension-obesity stat, (c) refs 1–3. If the paragraph already meets the spec, document as "no-op" in the SUMMARY and proceed.

**Step 3 — Paragraph 2 (line 36, identity-LD inflation framing + citation swap).**
Per §4.5 P2: *the three inflation settings (a)/(b)/(c) are correct; cite Wallace 2021, Zou 2022, Weissbrod 2020.*
Current paragraph cites `²⁰⁻²²` (opaque range). Action:
  (a) Verify the three settings are intact: tight-LD credible sets, LD-reference mismatch, multi-signal true architecture. They already are per the current text — do not restructure.
  (b) Replace the `²⁰⁻²²` range citation at the end of the inflation sentence with three bracketed author-year placeholders: `[Wallace 2021] [Zou 2022] [Weissbrod 2020]`. These placeholders are the resolve-at-2.2.f convention documented in the `<editing_rules>` block above.
  (c) Do NOT attempt to renumber the full reference list — that is Step 2.2.f's job. Leave all other superscript citations in the Introduction as they are.

**Step 4 — Paragraph 3 (line 38, produces-apparent-overlap inflection).**
Per §4.5 P3: *strengthen from "fails to resolve" to "can produce apparent overlap".*
Current text says SuSiE-RSS + coloc.susie is "a more rigorous alternative". Action: rewrite the opening sentence of this paragraph to lead with the stronger claim: single-causal-variant `coloc.abf` applied under identity-LD can *produce* apparent colocalization signals that do not exist in the real-LD data (not merely fail to resolve them). Then introduce SuSiE-RSS + `coloc.susie` as the rigorous alternative. Retain the closing sentence about systematic re-analysis not having been performed at scale. Target paragraph length: unchanged (~3 sentences, 80–100 words). The inflection is tone, not length.

**Step 5 — Paragraph 4 (line 40, AFR as candid limitation).**
Per §4.5 P4: *keep cross-ancestry data-equity framing; de-emphasize health equity as the primary motivation; AFR arm is a candid limitation, not a primary finding.*
Action: read line 40 as-is. If any sentence reads as "health equity is our primary motivation" (rather than "we acknowledge the ancestry gap but frame AFR as a limitation"), rewrite that sentence. The current text already has "honest underpowered replication subset" language and a Track B forward pointer — that framing is correct. Most likely this paragraph is a no-op; document in SUMMARY.

**Step 6 — Paragraph 5 (lines 42–48, three numbered aims).**
Per §4.5 P5: three aims — (1) quantify real-LD survival of PP.H4 ≥ 0.8 claims at 50 curated regions, (2) magnitude of identity-LD inflation via within-locus comparison, (3) pathway-level biological reframing.
Action: read lines 42–48. The three aims already match §4.5 P5 verbatim per the current draft. Verify numbering (1/2/3 present), verify the closing paragraph (line 48) preserves the "curated candidate-locus validation subset" language and the "audit of published claims, not an expansion of them" sentence. If exact — no-op. If any deviation — tighten to match §4.5.

**Step 7 — Post-edit verification.**
After edits, run these grep + line-count checks (Bash tool, from repo root):
  (a) `sed -n '32,48p' docs/manuscript/track_a_pivot.md` — eyeball the edited Introduction for prose quality.
  (b) `grep -c "Wallace 2021\\|Zou 2022\\|Weissbrod 2020" docs/manuscript/track_a_pivot.md` — expect ≥ 3 (may be more once 2.2.f adds Methods citations).
  (c) `awk 'NR>=32 && NR<=48' docs/manuscript/track_a_pivot.md | grep -E -c "thrifty|antagonistic|Neel 1962|Williams 1957|machine learning|\\bML\\b|classifier|predictive model"` — expect exactly 0.
  (d) `awk 'NR>=32 && NR<=48' docs/manuscript/track_a_pivot.md | wc -w` — expect ≤ word count before edits (the trim in P1 net-dominates the inflection in P3; the aim is a leaner Introduction, not a longer one).

**Step 8 — Do NOT commit.**
Per the `<constraints>` block in the planning context: the orchestrator commits docs artifacts at workflow Step 8. Do not run `git add` or `git commit` inside this task. Leave the working tree dirty with the Introduction edit staged for the orchestrator.

**Style notes (apply throughout):**
  - Prose-aware edits only — think at the paragraph and sentence level, not the character-diff level. Use the Edit tool with enough surrounding context to be unambiguous.
  - Preserve all existing superscript citations except the `²⁰⁻²²` range in paragraph 2.
  - Preserve the "original research" tone — no "we previously thought X but now think Y" framing. This is a first-submission manuscript, not a revision of a published paper.
  - If any paragraph is already correct per §4.5, explicitly record "no-op, already compliant" in the SUMMARY; do not manufacture edits for their own sake.
  </action>
  <verify>
    <automated>
# From repo root. All four checks must pass.
test "$(awk 'NR>=32 && NR<=48' docs/manuscript/track_a_pivot.md | grep -E -c "thrifty|antagonistic|Neel 1962|Williams 1957|machine learning|\bML\b|classifier|predictive model")" = "0" \
  && grep -q "Wallace 2021" docs/manuscript/track_a_pivot.md \
  && grep -q "Zou 2022" docs/manuscript/track_a_pivot.md \
  && grep -q "Weissbrod 2020" docs/manuscript/track_a_pivot.md \
  && echo "R1-INTRO-VERIFY: OK"
    </automated>
  </verify>
  <done>
Introduction (lines 32–48 region) satisfies §4.5 paragraph-by-paragraph: P1 trimmed, P2 cites Wallace/Zou/Weissbrod via bracketed placeholders (replacing the ²⁰⁻²² range), P3 leads with "can produce apparent overlap", P4 retains candid AFR-limitation framing, P5 preserves three numbered aims + curated-candidate-locus closing. Introduction region is free of thrifty-gene / ML terminology. No commit performed (orchestrator handles at workflow Step 8).
  </done>
</task>

<task type="auto">
  <name>Task 2: Walk-through brief for Route A Steps 2.2.e, 2.2.f, 2.3, 2.4</name>
  <files>.planning/quick/260424-j64-route-a-step-2-2-b-introduction-rewrite-/260424-j64-NEXT-STEPS.md</files>
  <action>
Write a single markdown hand-off document at the path above, addressed to future-Carter (not past-Carter — this is a forward-looking navigation note, not a retrospective). Tone: concise, command-ready, no marketing language. No "revision" / "cleanup" framing — each downstream step is the continuation of original hypothesis-driven research.

**Document structure (use exactly this section order):**

```
# Route A — Remaining Steps After 2.2.b (Hand-Off Brief)

_Generated during /gsd-quick 260424-j64 (2026-04-24). Parent plan:
`/home/ckclinto/.claude/plans/snappy-humming-pine.md` §2.2.e–2.4. Source-of-truth
edit specs live in `.planning/amendments/TRACK-A-PIVOT.md`._

## Critical path summary

| Order | Step | Blocking input | Parallel-feasible with |
| --- | --- | --- | --- |
| 1 | 2.2.b (this session, DONE) | — | — |
| 2 | 2.2.e Discussion rewrite | 2.2.b tone | — |
| 3 | Pathway Results re-compute (may need separate /gsd-quick) | real-LD pathway outputs on disk | 2.3 figure 1/2 scripts |
| 4 | 2.2.f References + supplementary | 2.2.e + Methods placeholders | 2.3 figure 3 script |
| 5 | 2.3 Figures × 3 | frozen numbers + pathway re-compute | 2.2.f |
| 6 | 2.4 bioRxiv preprint package + submit | 2.2.e, 2.2.f, 2.3 all DONE | — |

All sumstats and coloc outputs are already on disk; no re-download needed.

---

## 2.2.e — Discussion rewrite (R1)

**Source of truth:** `.planning/amendments/TRACK-A-PIVOT.md` §4.17 (lines 137–144).

**Scope:** refine-don't-rebuild pass over Discussion subsections in
`docs/manuscript/track_a_pivot.md`:
  - Opening paragraph — identity-LD inflation headline; SH2B3 BMI–stroke collapse.
  - Metabolic Syndrome — retain only if pathway enrichment survives real-LD.
  - Asthma-Metabolic Axis — likely remove (NEGR1/FTO/FADS1 identity-LD-sourced).
  - Variant Mechanisms — drop drug-repurposing claims
    (MC4R-setmelanotide, PCSK9-evolocumab, KCNJ11-sulfonylureas, FADS-PUFA).
  - Evolutionary Medicine — demote three paragraphs to a single ~120-word
    speculative paragraph.
  - Cross-Ancestry Health Equity — remove the "concordant null = shared
    biology" claim; replace with the underpowering-disclaimer framing.
  - Strengths / Limitations / Future Directions — rewrite per §4.17.

**Dependencies:** 2.2.b Introduction R1 (for tone consistency — Discussion
opening must match Introduction inflection).

**Estimated effort:** 90–150 min (larger than 2.2.b — more subsections,
some require pathway-result look-ups).

**Proposed command:**
`/gsd-quick Route A Step 2.2.e Discussion rewrite (R1) for docs/manuscript/track_a_pivot.md against TRACK-A-PIVOT.md §4.17`

**Success gate:**
  - `grep -E "setmelanotide|evolocumab|sulfonylurea|omega-3 supplementation"
     docs/manuscript/track_a_pivot.md` returns zero matches in the Discussion
    line range.
  - Evolutionary Medicine subsection in Discussion is ≤ 1 paragraph and ≤ 150 words.
  - Cross-Ancestry subsection contains the "concordant nulls are
    indistinguishable from both-ancestry underpowering" disclaimer verbatim.
  - Discussion opening paragraph cites the SH2B3 BMI–stroke collapse
    (identity-LD PP.H4 = 1.00 → real-LD trait-pair absent from Stage 2
    `coloc.susie` output manifest).

---

## 2.2.f — References + supplementary

**Source of truth:** `.planning/amendments/TRACK-A-PIVOT.md` §9 (lines 280–301).

**Scope:**
  - **Add:** Giambartolomei 2014 (coloc.abf primary), Wallace 2020
    (eliciting priors), Wallace 2021 PLoS Genet 17:e1009440 (coloc.susie),
    Zou 2022 PLoS Genet 18:e1010299 (SuSiE-RSS — promote to primary),
    Weissbrod 2020 Nat Genet 52:1355 (functional fine-mapping / LD-mismatch),
    Benner 2016 (FINEMAP comparator), Foley 2021 Nat Commun 12:764 (multi-trait
    coloc), Kanai 2022 Cell Genom 2:100210 (meta-analysis fine-mapping
    calibration / LD mismatch), plus one 2023–2025 LD-reference-panel review
    (Carter to pick at freeze).
  - **Demote / drop:** refs 4–5 (Neel 1962 thrifty-gene, Williams 1957
    antagonistic-pleiotropy) if Evolutionary Medicine is compressed to a
    single paragraph per §4.17; drop all refs formerly supporting "ML-based"
    framing (review refs 37–41 — retain only as annotation sources).
  - **Retain:** GIANT / DIAMANTE / ICBP / GIGASTROKE / TAGC / MEDIA / SIREN /
    CAAPA GWAS sources (these are the provenance of the claims under audit);
    Open Targets, GWAS Catalog, CADD, PolyPhen-2, SIFT, KEGG, Reactome, GO,
    OMIM, ClinVar, Roadmap, STRING, DGIdb, ChEMBL, gnomAD (annotation
    sources); Martin 2019 PRS-transportability (Cross-Ancestry limitation).
  - **Resolve placeholders:** every `[Wallace 2021]` / `[Zou 2022]` /
    `[Weissbrod 2020]` inline bracket left by 2.2.b becomes the final
    superscript number.
  - **Renumber:** every inline citation in Abstract, Introduction, Methods,
    Results, Discussion, Conclusion is re-walked.
  - **Supplementary tables S1–S6 sanity check:** Table S1 provenance refs,
    Table S6 harmonization-diagnostics per §4.12.

**Dependencies:** 2.2.b (Introduction placeholders to resolve); 2.2.e
(Discussion citations may shift after the rewrite).

**Estimated effort:** 60–120 min (mostly mechanical but error-prone;
recommend a final `grep -n "\[.*20[0-9][0-9]\]" docs/manuscript/track_a_pivot.md`
sweep to catch orphan placeholders).

**Proposed command:**
`/gsd-quick Route A Step 2.2.f References renumber + supplementary check against TRACK-A-PIVOT.md §9`

**Success gate:**
  - `grep -c "Wallace 2021\|Zou 2022\|Weissbrod 2020\|Benner 2016\|Kanai 2022"
     docs/manuscript/track_a_pivot.md` ≥ 4 unique matches (Methods +
    Introduction + Discussion coverage expected).
  - Zero bracketed author-year placeholders remain:
    `grep -c "\\[Wallace\\|\\[Zou\\|\\[Weissbrod\\]" docs/manuscript/track_a_pivot.md`
    equals 0.
  - Every numeric superscript in the manuscript body has a matching entry in
    the References section (manual spot-check of the first, middle, last
    five numbers is sufficient).

---

## 2.3 — Figures (three R scripts)

**Source of truth:** `/home/ckclinto/.claude/plans/snappy-humming-pine.md`
§2.3; `.planning/amendments/TRACK-A-PIVOT.md` §5 (figure legends).

**Scope:** three R/ggplot2 build scripts under `src/R/figures/`:
  - **Fig 1 — CS yield (identity-LD vs real-LD).** Inputs:
    `results/fine_mapping/finemap_summary.tsv`. Headline numbers:
    12/96 identity-LD vs 51/96 real-LD non-empty credible sets at admissible
    regions (4.25× yield).
  - **Fig 2 — SH2B3 12q24 locus plot.** Inputs:
    `results/multitrait/coloc_susie/*SH2B3*.json` (or equivalent locus
    file), Stage 1d identity-LD PP.H4 = 1.00 BMI–hypertension / hypertension–stroke
    vs Stage 2 real-LD absence from manifest. LocusZoom-style panel
    (GWAS Manhattan, fine-map credible-set shading, recombination overlay).
  - **Fig 3 — Pathway enrichment reconfiguration.** Inputs:
    `results/pathway/` (requires pathway Results re-compute on real-LD
    surviving gene set — this re-compute may itself need a separate /gsd-quick
    session before Fig 3 is buildable). Side-by-side fold-enrichment (KEGG /
    Reactome / GO) identity-LD vs real-LD.

**Outputs:** PDF + PNG per figure under `docs/manuscript/figures/`.

**R environment:** first check whether `envs/smoke_dev` already contains
ggplot2, patchwork, scales, and a LocusZoom-compatible panel library (e.g.
`locuszoomr` or a hand-rolled panel constructor). If not, create a new
`envs/figures_R.yml` (R 4.3+, ggplot2, patchwork, scales, cowplot,
data.table, locuszoomr or equivalent). Do NOT add heavy Python deps —
figures are pure R.

**Dependencies:**
  - `TRACK-A-FROZEN-NUMBERS.md` numerics (must not drift).
  - Pathway Results re-compute (Fig 3 only — may run as a separate /gsd-quick
    before Fig 3 script is written).

**Estimated effort:** ~45–75 min per figure → 3 figures × 3 /gsd-quick
sessions ≈ 2.5–4 hours total. Split across sessions to keep each atomic.

**Proposed commands (three separate sessions):**
  - `/gsd-quick Route A Step 2.3 build Figure 1 (CS yield identity-LD vs real-LD)`
  - `/gsd-quick Route A Step 2.3 build Figure 2 (SH2B3 12q24 locus collapse)`
  - `/gsd-quick Route A Step 2.3 build Figure 3 (pathway enrichment reconfiguration)`

**Success gate (per figure):**
  - `src/R/figures/fig{1,2,3}.R` exits 0 under the pinned R env.
  - Writes both `docs/manuscript/figures/fig{1,2,3}.pdf` AND
    `docs/manuscript/figures/fig{1,2,3}.png`.
  - Headline numbers match TRACK-A-FROZEN-NUMBERS.md verbatim (no drift).

---

## 2.4 — bioRxiv preprint (package + manual submit)

**Source of truth:** `/home/ckclinto/.claude/plans/snappy-humming-pine.md` §2.4.

**Scope:**
  - Build a submission-ready PDF from the Markdown draft (pandoc →
    Genome Medicine-compatible template, LaTeX fallback if pandoc chokes on
    figure embedding).
  - Embed Figures 1–3 at Markdown-referenced anchor points.
  - Prepare cover letter, competing-interests statement, funding statement,
    author affiliations (ASHES Lab + NCSU Biological Sciences) — per
    bioRxiv submission UI field names.
  - Manual web-UI submission at biorxiv.org (Carter action — bioRxiv does
    not expose a scripted submission API; `/gsd-quick` stops at package
    preparation).
  - Log the returned DOI + page URL to `.planning/amendments/BIORXIV-TRACK-A.md`
    (create the file if absent) + STATE.md row + repo tag
    `M1-BIORXIV-TRACK-A-POSTED-YYYY-MM-DD` (tag format TBD per
    project conventions).

**Prerequisites (all gate 2.4):**
  - 2.2.b Introduction R1 DONE (this session).
  - 2.2.e Discussion R1 DONE.
  - 2.2.f References + supplementary DONE.
  - 2.3 Figures 1–3 DONE.
  - Every `[EXTRACT: …]` placeholder in `track_a_pivot.md` either resolved
    OR explicitly deferred to venue-specific submission-only fill.

**Subject categories (bioRxiv web UI):** primary = Genetics; secondary =
Genomics.

**Dependencies:** 2.2.e, 2.2.f, 2.3 all complete. Carter's bioRxiv
account + institutional affiliation must be pre-registered (check before
starting 2.4).

**Estimated effort:**
  - Package prep (/gsd-quick): 60–120 min.
  - Carter web-UI submission: 30–60 min (outside the /gsd-quick command).

**Proposed command:**
`/gsd-quick Route A Step 2.4 prepare bioRxiv submission package for track_a_pivot.md`

**Success gate:**
  - `docs/manuscript/track_a_pivot.pdf` exists and is human-readable.
  - All three figures embedded at marker locations.
  - `docs/manuscript/cover_letter.pdf`, `docs/manuscript/coi_statement.md`,
    `docs/manuscript/funding_statement.md` exist.
  - After Carter's manual web-UI submission: bioRxiv DOI + page URL logged
    to `.planning/amendments/BIORXIV-TRACK-A.md`; STATE.md row appended;
    repo tag pushed.

---

## Parallelism notes

  - 2.2.f References renumber and 2.3 Figure 3 pathway script are both
    gated on the pathway Results re-compute. Sequence: pathway re-compute
    first → then 2.2.f and Fig 3 in parallel is safe (disjoint file sets).
  - Fig 1 and Fig 2 have no shared inputs with 2.2.f or 2.2.e — buildable
    in parallel with those prose edits if a second terminal is convenient.
  - 2.4 is a hard fan-in — all upstream steps must DONE.

## STATE.md integration

Each step closeout should:
  1. Append a row to `.planning/STATE.md` under the same Session Continuity
     pattern used by 260423-osk and 260424-mxp.
  2. Append a DEC entry to `.planning/DECISIONS.md` IF a substantive
     scope/framing decision was made inside the step (not for routine
     edits).
  3. Update `.planning/STATE.md` `Last session:` timestamp and
     `Stopped at:` line per the project convention.
```

**Writing rules (apply while authoring NEXT-STEPS.md):**
  - Use GitHub-Flavored Markdown. One top-level `#` heading; `##` for each
    step; `###` only if a step has multiple sub-deliverables.
  - Bold the **Success gate:** label on every step.
  - Keep each step's copy concise — aim for 200–400 words per step, not
    essay-length.
  - The "Proposed command" line on every step must be a single-line
    `/gsd-quick …` invocation that future-Carter can paste verbatim.
  - Tone: hand-off note to future-Carter. Not a retrospective summary.
    Not sales copy. Plain, technical, actionable.
  - No "revision" / "cleanup" framing anywhere in the prose.
  </action>
  <verify>
    <automated>
# From repo root. Output file exists and contains per-step hand-off sections.
test -f .planning/quick/260424-j64-route-a-step-2-2-b-introduction-rewrite-/260424-j64-NEXT-STEPS.md \
  && grep -q "## 2.2.e" .planning/quick/260424-j64-route-a-step-2-2-b-introduction-rewrite-/260424-j64-NEXT-STEPS.md \
  && grep -q "## 2.2.f" .planning/quick/260424-j64-route-a-step-2-2-b-introduction-rewrite-/260424-j64-NEXT-STEPS.md \
  && grep -q "## 2.3" .planning/quick/260424-j64-route-a-step-2-2-b-introduction-rewrite-/260424-j64-NEXT-STEPS.md \
  && grep -q "## 2.4" .planning/quick/260424-j64-route-a-step-2-2-b-introduction-rewrite-/260424-j64-NEXT-STEPS.md \
  && grep -c "Proposed command" .planning/quick/260424-j64-route-a-step-2-2-b-introduction-rewrite-/260424-j64-NEXT-STEPS.md | awk '$1 >= 4' | grep -q . \
  && grep -c "Success gate" .planning/quick/260424-j64-route-a-step-2-2-b-introduction-rewrite-/260424-j64-NEXT-STEPS.md | awk '$1 >= 4' | grep -q . \
  && test "$(wc -l < .planning/quick/260424-j64-route-a-step-2-2-b-introduction-rewrite-/260424-j64-NEXT-STEPS.md)" -ge 80 \
  && echo "NEXT-STEPS-VERIFY: OK"
    </automated>
  </verify>
  <done>
`.planning/quick/260424-j64-route-a-step-2-2-b-introduction-rewrite-/260424-j64-NEXT-STEPS.md`
exists, contains four top-level sections (2.2.e, 2.2.f, 2.3, 2.4) each with
scope / source-of-truth / dependencies / proposed `/gsd-quick` command /
bolded success gate; a critical-path summary table sits at the top; a
parallelism-and-STATE-integration epilogue sits at the bottom. Minimum
length 80 lines. Tone is forward-looking hand-off, not retrospective;
no "revision" / "cleanup" framing appears.
  </done>
</task>

</tasks>

<verification>
Plan-level success requires both tasks completed AND these file-state checks (run
once at the end of the /gsd-quick execution, before returning to the orchestrator):

1. Introduction hygiene:
   `awk 'NR>=32 && NR<=48' docs/manuscript/track_a_pivot.md | grep -E -c "thrifty|antagonistic|Neel 1962|Williams 1957|machine learning|\bML\b|classifier|predictive model"`
   → must return `0`.

2. Citation placeholders present:
   `grep -c "Wallace 2021\|Zou 2022\|Weissbrod 2020" docs/manuscript/track_a_pivot.md`
   → must return ≥ 3.

3. Hand-off brief exists:
   `test -f .planning/quick/260424-j64-route-a-step-2-2-b-introduction-rewrite-/260424-j64-NEXT-STEPS.md`
   → exit 0.

4. Hand-off brief coverage:
   Each of `## 2.2.e`, `## 2.2.f`, `## 2.3`, `## 2.4` appears exactly once as a top-level step
   section header in NEXT-STEPS.md.

5. No commit performed inside plan execution:
   `git log -1 --format=%s` — must still be `26cc9e2 docs(quick-260424-mxp): ...` (or later
   commit only if another session committed between now and quick execution). The
   orchestrator's workflow Step 8 performs the commit for this quick's outputs; plan
   tasks must leave the working tree dirty, not cleaned.
</verification>

<success_criteria>
  - Introduction (lines 32–48 region of `docs/manuscript/track_a_pivot.md`) satisfies
    `.planning/amendments/TRACK-A-PIVOT.md` §4.5 paragraph-by-paragraph (P1 trimmed, P2
    cites Wallace 2021 / Zou 2022 / Weissbrod 2020, P3 leads with "can produce apparent
    overlap", P4 candid AFR-limitation retained, P5 three numbered aims + curated-candidate
    closing preserved).
  - Introduction contains zero residual pre-pivot evolutionary-medicine or ML terminology.
  - `260424-j64-NEXT-STEPS.md` exists and covers 2.2.e, 2.2.f, 2.3, 2.4 each with scope,
    source-of-truth, dependencies, proposed /gsd-quick command, and bolded success gate.
  - Working tree remains uncommitted — orchestrator performs the docs commit at
    workflow Step 8.
  - No ROADMAP.md modification, no branch creation (solo / GPFS / no-worktree mode
    per CLAUDE.md).
</success_criteria>

<output>
After completion, the quick workflow's summary step writes
`.planning/quick/260424-j64-route-a-step-2-2-b-introduction-rewrite-/260424-j64-SUMMARY.md`
with:
  - Per-paragraph diff summary for Introduction edits (which paragraphs were edited,
    which were no-op).
  - Pointer to the new NEXT-STEPS.md file.
  - STATE.md Session Continuity row appended by the orchestrator
    (no action required inside this plan).
</output>
