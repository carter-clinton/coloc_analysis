---
quick_id: 260428-vt2
description: m3-W1-portal-gate-clear-and-aux-spec — record M3 Wave 1 portal pre-conditions cleared (NCSU faculty controlled-tier basis) + pre-stage AUX path verification spec
date: 2026-04-29
related_phase: m3-aou-afr-ld-panel-build
related_plan: m3-01-W1-aou-cohort-and-hard-gates (pre-fire pre-conditions)
ruling_token: m3-W1-portal-cleared
commit_strategy: single atomic commit per Carter directive
---

# Quick Task 260428-vt2: M3 Wave 1 Portal-Gate Clear + AUX Spec Pre-Stage

## Goal

Record three Carter-delivered facts (2026-04-28 coordinator chat) that
close the Wave 1 portal pre-condition stack, except for AUX path
verification which requires a Workbench session:

1. NCSU faculty status grants Carter controlled-tier AoU access; the
   P1 / P2 / P3 / P4 / P6 portal gates auto-approve at his access tier
   (workspace created; billing attached; DUS / RPS / P&P all PASS).
2. R1 (egress classification of variant×variant LD matrices) is handled
   implicitly under standard AoU egress review (no per-data-class
   custom ruling letter required); the HARD GATE row in
   `aou-egress-audit-log.md` flips from PENDING to PASS.
3. Only AUX path verification remains before Wave 1 can fire — and that
   verification can ONLY run from inside an AoU Workbench Jupyter
   terminal session, so the orchestrator pre-stages a paste-ready spec
   that Carter executes when he's in the Workbench shell.

## Inputs Read

1. `.planning/amendments/aou-egress-audit-log.md` — existing structure
   (Status header, Egress Classification Ruling section with table row,
   Per-Bundle Audit Entries section, Audit log policy, Last updated
   line).
2. `src/python/aou_ld_panel.py` — `AUX_BASE` constant (line 58),
   `ANCESTRY_PREDS_PATH` constant (line 61), `WORKSPACE_BUCKET`
   environment-variable usage (lines 217, 375), Q9/O3 cross-references
   in docstring header (lines 17–28).
3. `.planning/STATE.md` — frontmatter `stopped_at` + `last_*` fields,
   `## Session Continuity` section structure (top "Last session" block
   + reverse-chronological "### Prior session" entries).
4. Carter coordinator-chat directive (this task's prompt) — the three
   deliverables and the single-atomic-commit constraint.

## Tasks

| # | Action | Files | Done when |
|---|--------|-------|-----------|
| 1 | Flip aou-egress-audit-log.md HARD GATE row PENDING → PASS; rewrite Status header to PASS framing; add structured ruling block (Status / Date / Basis / Provenance / Re-open conditions / Cross-reference); refresh Last-updated line | `.planning/amendments/aou-egress-audit-log.md` | `grep "PASS" .planning/amendments/aou-egress-audit-log.md \| wc -l` ≥ 5; `grep "PENDING" .planning/amendments/aou-egress-audit-log.md` returns 0 hits in the HARD GATE section |
| 2 | Create paste-ready AUX path verification spec — load-bearing literal `gsutil ls` command + 3 failure modes with remediation (incl. `(m3-W1-aux-path-fix)` commit recipe) + 3-item Carter-signoff verification checklist + Run Log template; cross-references all relevant files | `.planning/phases/m3-aou-afr-ld-panel-build/m3-W1-AUX-PATH-VERIFICATION.md` | File exists; `grep -c "gsutil ls" m3-W1-AUX-PATH-VERIFICATION.md` ≥ 3 (Step 2 canonical + Step 3 backup + Failure Mode (iii) listing); Carter-signoff 3-checkbox verification checklist present |
| 3 | Refresh STATE.md frontmatter `stopped_at` + `last_updated` + `last_activity`; append new `### Prior session` entry under `## Session Continuity` recording the m3-W1 portal-gate clear (without displacing the existing top "Last session" block, per "append" semantics) | `.planning/STATE.md` | `grep "Wave 1 portal pre-conditions all PASS" .planning/STATE.md` ≥ 2 (frontmatter + Session Continuity entry); existing top "Last session" block intact (preserves Terminal C's potential future top-block update) |
| 4 | Write quick-task PLAN.md + SUMMARY.md artifacts (this PLAN; sibling SUMMARY) | `.planning/quick/260428-vt2-.../260428-vt2-PLAN.md` + `260428-vt2-SUMMARY.md` | Both files exist with the standard quick-task frontmatter + body |
| 5 | Single atomic commit with token `(m3-W1-portal-cleared)` in subject (per Carter directive — NOT the standard /gsd-quick two-commit pattern) | All four files above | `git log --oneline --grep "(m3-W1-portal-cleared)"` returns exactly one commit |

## Constraints (non-negotiable per Carter directive)

* **Single atomic commit.** All deliverables land in one commit with
  token `(m3-W1-portal-cleared)` in subject. Do not split into
  load-bearing + docs-closeout commits.
* **File scope is bounded:** only files under `.planning/` plus
  optionally `src/python/aou_ld_panel.py` may be touched. This task
  does NOT touch `src/python/aou_ld_panel.py` (the path-fix-up edit is
  deferred to Carter's Workbench session per Failure Mode (ii) of the
  AUX spec; if the inferred path turns out to be wrong, that triggers
  a separate `(m3-W1-aux-path-fix)` commit with its own scope).
* **Disjoint with Terminal C's `ta-sh2b3-canonical-and-cache-refresh`
  scope.** Do NOT touch `docs/manuscript/`,
  `.planning/phases/ta-sh2b3-canonical-and-cache-refresh/`, or any
  other M0-M6 spine file outside m3.
* **STATE.md concurrency:** check `git log -1 -- .planning/STATE.md`
  before commit. Terminal C's last STATE.md write was `d2affb2`
  (2026-04-28 20:56 EDT, > 2 h ago); no live concurrent write at
  staging time. The Session Continuity append is below the existing
  top "Last session" block (preserving room for Terminal C to update
  the top block later if needed).
* **Wave 0 SUMMARY checklist intentionally NOT updated.** Per
  phase-artifact discipline, the m3-00 Wave 0 SUMMARY is a frozen
  artifact of when Wave 0 closed (status: complete since
  quick-260428-stv `b7beef8`); updating its readiness checklist now
  would rewrite history. Current portal-gate state is canonically
  recorded in `aou-egress-audit-log.md` + `.planning/STATE.md`
  frontmatter; that is sufficient.

## Verification (must-haves)

* [ ] aou-egress-audit-log.md Status header reads "HARD GATE PASS as of
  2026-04-28"
* [ ] aou-egress-audit-log.md HARD GATE table row shows
  `2026-04-28 | ... | PASS | ...` (not `TBD-Wave-1 | ... | PENDING | TBD`)
* [ ] aou-egress-audit-log.md ruling block contains all 6 fields:
  Status, Date, Basis, Provenance, Re-open conditions, Cross-reference
* [ ] m3-W1-AUX-PATH-VERIFICATION.md exists; Step 2 carries the
  literal canonical path
  (`gs://fc-aou-datasets-controlled/v7/wgs/short_read/snpindel/aux/ancestry/`);
  3 failure modes (i) / (ii) / (iii) all present with remediation
* [ ] STATE.md frontmatter `stopped_at` matches Carter directive verbatim
* [ ] STATE.md `## Session Continuity` has new entry below the
  existing top "Last session" block
* [ ] Single commit with `(m3-W1-portal-cleared)` token in subject

## Deliverable scope clarification — what this task does NOT do

* Does **NOT** fire Wave 1 (`/gsd-execute-phase m3-aou-afr-ld-panel-build`).
  Wave 1 fires AFTER AUX path verification clears.
* Does **NOT** edit `src/python/aou_ld_panel.py`. The
  `ANCESTRY_PREDS_PATH` constant remains at its inferred value pending
  Carter's Workbench session per Failure Mode (ii).
* Does **NOT** flip the Wave 0 SUMMARY readiness checkboxes. The
  m3-00 SUMMARY is a frozen Wave-0-close artifact; current state is
  recorded canonically in egress log + STATE.md.
* Does **NOT** touch any Track A R2 (`ta-sh2b3-canonical-and-cache-refresh`)
  scope, in active use by Terminal C.
* Does **NOT** flip the m3-01 ROADMAP plan-list line (m3-01 still
  `[ ]`); that flips when Wave 1 actually fires and m3-01 plan
  completes.
