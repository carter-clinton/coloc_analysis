---
status: complete
phase: quick-260715-sqe
plan: 1
source: [260715-sqe-SUMMARY.md]
started: 2026-07-15
updated: 2026-07-15
verification_mode: automated
note: >
  quick-260715-sqe is a ONE-LINE TEST FIX in a headless HPC genomics pipeline — no UI,
  no server, no CLI output change, no user-clickable surface. The verify-work workflow
  extracts USER-OBSERVABLE tests and explicitly skips non-observable internals; by that
  rule this deliverable yields ZERO interactive tests. So verification is AUTOMATED
  (Claude runs each check and records pass/fail), following the m3-07a precedent
  (m3-07a-UAT.md, also verification_mode: automated). Carter was not available for
  interactive UAT.

  GOAL UNDER TEST (achievement, not task completion): close the test-vs-test
  contradiction WITHOUT weakening the m3-07a RED and WITHOUT dropping the
  OSF-pre-registered n_dropped_occluded provenance column, leaving m3-07c a clean
  baseline.

  RESULT: 13/13 automated checks pass, 0 issues, 0 gaps. Two checks initially reported
  FAIL and BOTH were defects in the CHECK SCRIPT, not the work (recorded verbatim at
  tests 4 and 12 rather than silently re-run — a green obtained by fixing the ruler
  must be visible as such).
---

## Current Test

[testing complete]

## Tests

### 1. The contradiction is CLOSED
expected: `test_panel_tsv_append_resume_safe` — the test that failed at m3-07b close with
  `At index 7 diff: 'n_dropped_occluded' != 'n_dropped_monomorphic'` — now passes.
observed: `1 passed in 1.12s`.
result: pass

### 2. Driver suite fully GREEN
expected: tests/m3/test_run_native_ld_panel.py = 52 passed / 0 failed (was 1F/51P).
observed: `52 passed`. Re-run twice this session, stable.
result: pass

### 3. The m3-07a RED is INTACT — not weakened, not skipped, not deleted
expected: `test_panel_columns_include_n_dropped_occluded` is byte-unchanged and PASSES.
  This is the load-bearing check: the HANDOFF `do_not` forbids resolving the conflict by
  weakening the RED, and a SKIP would fake green.
observed: diff of the RED body since a76ebe5 = NO lines touched. Whole-file diff since
  a76ebe5 = `1 file changed, 1 insertion(+)` — a pure single insertion, which
  STRUCTURALLY proves the RED (and every other test in the file) is byte-identical.
  PASSED count 2 / SKIPPED count 0 across the two guard tests.
result: pass

### 4. n_dropped_occluded remains in production _PANEL_COLUMNS at index 7
expected: 9 entries; `n_dropped_occluded` at [7]; `n_dropped_monomorphic` still last
  (the `[-1]` pin at :1281 is why it was inserted rather than appended).
observed: FIRST RUN reported `❌ FAIL No module named 'pandas'` — a CHECK-SCRIPT DEFECT
  (invoked system `python3` instead of the smoke_dev py3.11 interpreter), NOT a code
  failure. Re-run with /rs1/.../smoke_dev/bin/python:
    [0]region_id [1]chr [2]n_var [3]wall_min [4]peak_ram_gib [5]output_gib [6]status
    [7]n_dropped_occluded [8]n_dropped_monomorphic
  len=9, [7]=='n_dropped_occluded', [-1]=='n_dropped_monomorphic'.
result: pass

### 5. The OSF-pre-registered provenance column was NOT dropped
expected: the alternative fix (dropping the column) is forbidden — it would fail the 07a
  RED and discard the per-region occlusion provenance osf.io/az52u pre-registers.
observed: column present in production at index 7 (test 4); 07a RED green (test 3).
result: pass

### 6. Test-only — no production code touched
expected: `git diff --stat a76ebe5..HEAD -- src/` EMPTY.
observed: EMPTY.
result: pass

### 7. Frozen contracts 0-line diff
expected: plink_ld_to_npz.py, ld_npz_to_rds.R, condition_ld_matrix.py unchanged vs d172dfa.
observed: EMPTY diff.
result: pass

### 8. Zero regressions across the full suite
expected: full tests/m3 improves by exactly 1 and nothing else moves.
observed: 15 failed / 395 passed / 31 skipped in 411.34s, vs the 16F/394P/31S baseline —
  exactly 1 newly GREEN, 0 regressions.
result: pass

### 9. Every remaining failure is an EXPECTED m3-07c ModuleNotFoundError
expected: no unexplained reds. Verified by counting the ACTUAL exception, not by
  trusting the file names.
observed: 9 × `ModuleNotFoundError: No module named 'drop_occluded_from_sumstats'` (07c T4)
  + 6 × `No module named 'occlusion_present_rate_scan'` (07c T3) = 15 = the full failure
  count. Zero unexplained failures.
result: pass

### 10. Artifacts exist
expected: 260715-sqe-PLAN.md and 260715-sqe-SUMMARY.md present in the quick-task dir.
observed: both present.
result: pass

### 11. STATE.md current
expected: the 260715-sqe quick-task row is appended (6-col format, matching the existing
  table), and `Last activity` is refreshed to this task.
observed: both present.
result: pass

### 12. HANDOFF.json is valid JSON and records the gate as CLOSED
expected: the resume entry point parses, and gates.panel_column_conflict reads CLOSED —
  a stale OPEN would send a future session to re-litigate a settled decision.
observed: valid JSON, 24 top-level keys, 4 resume_on_reconnect items, 7 do_not items;
  gates.panel_column_conflict starts `✅ CLOSED`.
result: pass

### 13. SUMMARY provenance claim is accurate
expected: the SUMMARY must NOT claim Carter ruled on this. The decision was made by the
  ORCHESTRATING AGENT on verified precedent evidence (1a9d170); Carter's resume args
  named "decide the column conflict" as the next step. An executor draft wrongly wrote
  "pre-settled by Carter" and was corrected.
observed: FIRST RUN reported `❌ FAIL SUMMARY does NOT say 'pre-settled by Carter'` — a
  CHECK-SCRIPT DEFECT: the phrase survives ONLY inside the correction itself, as a
  QUOTATION of the claim being refuted (`The executor's draft recorded this decision as
  "pre-settled by Carter." That is **not accurate**…`). A naive grep cannot distinguish
  an assertion from a quotation. Manual read of :79 confirms the SUMMARY asserts the
  correct provenance and carries the ⚠ PROVENANCE CORRECTION block.
result: pass

### 14. Durability
expected: origin tip == local HEAD (NCSU commonly runs ahead of origin; ls-remote success
  is not proof of sync).
observed: origin == local == 558ed44.
result: pass

## Summary

total: 14
passed: 14
issues: 0
pending: 0
skipped: 0

check_script_defects_found: 2 (tests 4 and 12/13 — wrong interpreter; grep cannot
  distinguish quotation from assertion). Both recorded rather than silently re-run.

## Gaps

[none — the goal is achieved]

## Notes — scope boundary (NOT gaps in this task)

A 4-agent blast-radius sweep run immediately after this UAT confirmed the fix is correct
and orthogonal to the surrounding hazards, and surfaced PRE-EXISTING issues that this
one-line test fix neither introduced nor was scoped to solve. They are recorded in
HANDOFF.json, not here, because they do not block this task's goal:

- **PRODUCTION PRECONDITIONS (block the ~11-day fire, NOT this task, NOT merge):** the
  panel-TSV append has no header/arity reconciliation (`run_native_ld_panel.py:487-493`),
  so a stale pre-07b (7- or 8-column) panel TSV at the `gs://` path — plausibly left by
  the June/July fires, which DID append error rows (`:808`, unconditional, outside the
  per-region try/except) — would either dedup-swallow the good rows' provenance or crash
  the fire on a ParserError. NOTE: "0/276 banked" is measured in `.npz`s and says NOTHING
  about a leftover panel TSV. Separately, a transient gsutil error can truncate the bucket
  panel TSV to one row (`:514-524`) with no version skew required.
- **BLOCKS m3-07c:** `scan_present_rate` is pinned by its RED to return a `(chr,pos)`
  GRCh37-keyed dict, while the shipped 07b consumer `enrich_occlusion_manifest` looks up
  by GRCh38 `variant_id` string (`occlusion_manifest.py:331`) — `.get(vid, {})` misses
  silently → `pd.NA` across all 276 regions, both suites green. Zero tests cover the seam.

Neither is attributable to 957d5a1; `git diff --stat a76ebe5..HEAD -- src/` is EMPTY.
