---
phase: quick-260812-ot2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260812-ot2-e-2-obligation-1-placement-draft-and-jou/260812-ot2-placement-draft-ms-correction-v2.md
  - .planning/quick/260812-ot2-e-2-obligation-1-placement-draft-and-jou/260812-ot2-journal-selection-memo.md
  - .planning/DECISIONS.md
autonomous: true
requirements: [QUICK-260812-OT2]

must_haves:
  truths:
    - "Carter can place ms-correction-v2 into the Track A manuscript in <=2 minutes using ONLY the placement draft (exact heading anchors + full inserted text + the Limitations pointer sentence)"
    - "The draft's quoted paste block is byte-identical to the v2 source's PASTE-BEGIN/END block, proven by a cmp command recorded in the draft itself, and the cmp was observed to FAIL on a perturbed copy before the green run was trusted"
    - "The closing-sentence coherence consequence (OSF sentence false at placement with obligation (2) skipped) is surfaced once, plainly, with a P-1 (recommended) / P-2 (marked deviation) fork"
    - "The journal memo presents a Nature-first ladder with the transfer cascade, an honest fit ranking, a per-venue pre-placement-check column, and the conditional Track-A-pending paragraph — every fact traceable to RESEARCH.md with its verified/model-knowledge flag intact"
    - "DECISIONS.md carries one appended entry recording both 2026-08-12 directives (obligation (2) skipped-by-direction and stays UNDISCHARGED; venue re-target Nature-first) with zero deleted lines"
    - "docs/manuscript/id-vs-ref-LD.md is byte-unchanged by this task"
  artifacts:
    - path: ".planning/quick/260812-ot2-e-2-obligation-1-placement-draft-and-jou/260812-ot2-placement-draft-ms-correction-v2.md"
      provides: "Placement-ready SPEC: anchors, byte-locked paste block, P-1/P-2 fork, Limitations pointer, verification record"
      contains: "<!-- PASTE-BEGIN: ms-correction-v2 -->"
    - path: ".planning/quick/260812-ot2-e-2-obligation-1-placement-draft-and-jou/260812-ot2-journal-selection-memo.md"
      provides: "Nature-first venue ladder + per-venue table incl. pre-placement-check column + conditional Track-A paragraph"
      contains: "Nature Communications"
    - path: ".planning/DECISIONS.md"
      provides: "DEC-2026-08-12-e2-obligation1-venue-and-obligation2-skip appended entry"
      contains: "DEC-2026-08-12-e2-obligation1-venue-and-obligation2-skip"
  key_links:
    - from: ".planning/quick/260812-ot2-e-2-obligation-1-placement-draft-and-jou/260812-ot2-placement-draft-ms-correction-v2.md"
      to: ".planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-SELECTED-PAIR-correction-v2.md"
      via: "byte-identical PASTE-BEGIN/END block, cmp-verified"
      pattern: "PASTE-BEGIN: ms-correction-v2"
    - from: ".planning/DECISIONS.md (new entry)"
      to: "DEC-2026-08-11-e2-framing-correction"
      via: "cross-reference"
      pattern: "DEC-2026-08-11-e2-framing-correction"
    - from: ".planning/quick/260812-ot2-e-2-obligation-1-placement-draft-and-jou/260812-ot2-journal-selection-memo.md"
      to: ".planning/quick/260812-ot2-e-2-obligation-1-placement-draft-and-jou/260812-ot2-RESEARCH.md"
      via: "facts + confidence flags carried from research"
      pattern: "model knowledge"
---

<objective>
Prepare E-2 obligation (1) for Carter's discharge: (a) a placement-ready SPEC of
`ms-correction-v2` for the Track A manuscript (a paste-ready document — NOT a
manuscript edit), (b) a journal-selection memo implementing Carter's Nature-first
directive with an honest fit ranking, and (c) one appended DECISIONS.md entry
recording both 2026-08-12 directives.

Purpose: obligation (1) discharges only at Carter's own placement. This task
makes that placement a <=2-minute mechanical action, gives the venue decision an
honest factual substrate, and puts both directives on the ledger. Obligation (2)
is SKIPPED by Carter's direction — no OSF work of any kind; the obligation stays
UNDISCHARGED (deferred, not discharged).
Output: two docs in the ot2 quick dir + one append-only DECISIONS.md entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260812-ot2-e-2-obligation-1-placement-draft-and-jou/260812-ot2-CONTEXT.md
@.planning/quick/260812-ot2-e-2-obligation-1-placement-draft-and-jou/260812-ot2-RESEARCH.md
@.planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-SELECTED-PAIR-correction-v2.md
@docs/manuscript/id-vs-ref-LD.md
@.planning/DECISIONS.md

<interfaces>
<!-- Measured 2026-08-12 by the planner. Executor: use these directly; re-verify only the marker greps in Task 1. -->

**v2 source block** (`260812-09a-SELECTED-PAIR-correction-v2.md`):
- `<!-- PASTE-BEGIN: ms-correction-v2 -->` at line 68; `<!-- PASTE-END: ms-correction-v2 -->` at line 82.
- Each marker line appears EXACTLY ONCE in the source (`grep -cx` = 1 for each; the OSF block uses distinct marker names `osf-correction-v2`, so anchored sed ranges cannot bleed).
- Extraction including both marker lines = 15 lines / 3,429 bytes. 13 content sentences, one sentence per line.
- The block's final content line (source line 81) is the OSF cross-reference sentence ("...recorded in the paired entry posted as a supplementary file on osf.io/az52u.") — this is the P-1/P-2 fork target.
- The ms block does NOT contain the pooled 5.29% (that figure is only in the OSF body, which this task never touches). The block carries both corpus units correctly labelled on single lines.

**Manuscript anchors** (`docs/manuscript/id-vs-ref-LD.md`, 453 lines, READ-ONLY):
- `### Ethics Statement` heading at :130, its one-paragraph body at :132.
- `## Results` at :134 — the default insertion point is between :132 and :134.
- `### Limitations` at :250; its single numbered paragraph (items (1)-(6)) at :252.
- Status header :3 and Target-venue line :7 currently name *Genome Medicine* — stale relative to the new memo; NOTING this is in scope, editing it is NOT.

**DECISIONS.md** (1,741 lines as of planning): prior entry `DEC-2026-08-11-e2-framing-correction` begins at :1252 — mirror its house style (`## YYYY-MM-DD — DEC-...: title`, Decision paragraph, grounds, discharge-status table, How to apply, Cross-refs). Append target = end of file. Its discharge table (:1318-1322) is the status baseline: (3) DISCHARGED, (1) OPEN, (2) OPEN.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Placement-ready draft of ms-correction-v2 (byte-verified)</name>
  <files>.planning/quick/260812-ot2-e-2-obligation-1-placement-draft-and-jou/260812-ot2-placement-draft-ms-correction-v2.md</files>
  <action>
Create the placement SPEC. ⛔ `docs/manuscript/id-vs-ref-LD.md` is READ-ONLY —
the standing no-agent-edits rule HOLDS; obligation (1) discharges only at
Carter's placement. Required sections, in order:

1. **Header** — what this file is (a paste-ready SPEC Carter applies; nothing
   here edits the manuscript), the no-agent-edits rule, the discharge condition
   (v2 file §1). State that the v2 pair supersedes v1; never quote from
   `260811-tf3` or `260811-oku`.

2. **Pre-placement check status** — per RESEARCH.md's cross-venue finding: on a
   FRESH submission no venue's correction machinery attaches (corrections/errata
   workflows apply only to published/in-press articles), so the check does not
   fire and the §1 default placement (Methods note + Limitations pointer)
   stands. Carry the conditional: if this manuscript is in a PENDING submission
   anywhere, the in-submission reading re-triggers — keep the CONTENT, use
   framing A's PLACEMENT (Limitations section) per the v2 file's pre-placement
   check. Assert NOTHING about whether a pending submission exists.

3. **Placement instructions (the <=2-minute action)** — anchor by heading text,
   with as-of-2026-08-12 line numbers as a courtesy only:
   - Step 1: after the `### Ethics Statement` body paragraph (:132) and before
     `## Results` (:134), insert a blank line, the new heading
     `### Correction and Disclosure: Variant-Orientation Exposure`, a blank
     line, then the paste block of section 4 — content lines only; state
     explicitly that the two HTML-comment marker lines are extraction anchors,
     not manuscript text.
   - Step 2: append the pointer sentence of section 6 to the end of the
     `### Limitations` paragraph (:252), as item (7).

4. **THE PASTE BLOCK** — the full 15-line block INCLUDING both marker lines,
   byte-identical to the v2 source. ⛔ Transcription is forbidden: produce it by
   machine extraction (anchored sed range over the marker lines, exactly as in
   the verify command) and insert the extraction verbatim. Each marker line must
   appear exactly once in this draft.

5. **Closing-sentence fork** — state the coherence consequence ONCE, plainly,
   without relitigating: the block's final sentence cross-references "the paired
   entry posted as a supplementary file on osf.io/az52u"; with obligation (2)
   skipped by Carter's direction, that sentence is FALSE at placement time.
   Then the two options, Carter chooses at placement:
   - **Option P-1 (pair-coherent, RECOMMENDED):** place the block byte-intact;
     treat (2) as deferred-to-submission — the OSF entry must exist by the time
     the manuscript is submitted. Grounds: the rigor-over-speed standing rule,
     and the v2 file §3 item 4 pair-matching check assumes the two halves agree.
   - **Option P-2 (skip-permanent):** replace ONLY the final sentence with the
     variant below. Mark the variant visibly: "P-2 VARIANT — NOT part of the
     gated v2 pair; Carter-directed deviation". State explicitly that under P-2
     the placed text is no longer "the v2 pair half", and that the deviation is
     recorded in DEC-2026-08-12-e2-obligation1-venue-and-obligation2-skip.
   - Draft the P-2 variant sentence yourself: ONE sentence, references the
     project's internal version-controlled record and pre-registration
     osf.io/pvb5j, claims NO posted supplementary entry, quotes no numbers.
     It must live OUTSIDE the PASTE-BEGIN/END markers so the byte check stays
     clean.
   - One line noting: the E-4 public-commitment obligation is NOT registered by
     this task (its trigger is posting, which is skipped).

6. **Limitations pointer sentence** — write one sentence for item (7) of the
   Limitations paragraph pointing to "Methods §Correction and Disclosure:
   Variant-Orientation Exposure". It quotes NO figures (safest under the number
   rules), uses original-research framing, never "revision", never unnegated
   "salvage".

7. **Verification record** — paste the ACTUAL commands run and their observed
   output:
   - The byte comparison (SRC = the v2 file, DRAFT = this file):
     `cmp <(sed -n '/^<!-- PASTE-BEGIN: ms-correction-v2 -->$/,/^<!-- PASTE-END: ms-correction-v2 -->$/p' SRC) <(sed -n '/^<!-- PASTE-BEGIN: ms-correction-v2 -->$/,/^<!-- PASTE-END: ms-correction-v2 -->$/p' DRAFT) && echo BYTE-IDENTICAL`
   - The NEGATIVE CONTROL: run the same cmp against a one-character-perturbed
     copy of the extraction (perturbed copy lives in the session scratchpad,
     never committed) and record that it exited NON-ZERO — green is evidence
     only after it has been seen to fail.
   - Marker-uniqueness: `grep -cx` = 1 for each marker line in this draft.
   - The number/framing greps of the verify block, with their observed output.

8. **Stale-header note** — manuscript Status (:3) and Target-venue (:7) lines
   name *Genome Medicine*; venue selection is now governed by the ot2 journal
   memo; updating the header is Carter's own edit, out of scope for any agent.

Number/framing rules bind ALL SPEC prose: never the pooled 5.29% at all (the ms
block does not contain it and the SPEC has no need for it); any corpus figure
only with its unit on the same line; never cite the E-2 numbers as real-LD
exposure (the block's own identity-LD-stub sentence covers the block; SPEC prose
must not undo it); hypothesis-driven original research framing; never
"revision"; "salvage" only in negated form.

Commit (explicit path only, never `git add -A` / `.`):
`git add .planning/quick/260812-ot2-e-2-obligation-1-placement-draft-and-jou/260812-ot2-placement-draft-ms-correction-v2.md && git commit -m "docs(quick-260812-ot2): placement-ready SPEC for ms-correction-v2 — byte-verified paste block, P-1/P-2 closing fork"`
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && D=.planning/quick/260812-ot2-e-2-obligation-1-placement-draft-and-jou/260812-ot2-placement-draft-ms-correction-v2.md && S=.planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-SELECTED-PAIR-correction-v2.md && cmp <(sed -n '/^<!-- PASTE-BEGIN: ms-correction-v2 -->$/,/^<!-- PASTE-END: ms-correction-v2 -->$/p' "$S") <(sed -n '/^<!-- PASTE-BEGIN: ms-correction-v2 -->$/,/^<!-- PASTE-END: ms-correction-v2 -->$/p' "$D") && [ "$(grep -cx -- '<!-- PASTE-BEGIN: ms-correction-v2 -->' "$D")" = 1 ] && [ "$(grep -cx -- '<!-- PASTE-END: ms-correction-v2 -->' "$D")" = 1 ] && ! grep -n '5\.29' "$D" && [ -z "$(grep -in 'salvage' "$D" | grep -iv 'not a salvage')" ] && ! grep -in 'revision' "$D" && grep -q 'P-2 VARIANT' "$D" && grep -q 'Correction and Disclosure: Variant-Orientation Exposure' "$D" && git status --porcelain -- docs/manuscript/id-vs-ref-LD.md | grep -c . | grep -qx 0 && echo TASK1-PASS</automated>
  </verify>
  <done>Draft exists with all 8 sections; cmp proves the paste block byte-identical to the v2 source (negative control observed red first, recorded in the draft); markers unique; number/framing greps clean; P-1 recommended and P-2 variant clearly marked as NOT part of the gated v2 pair; `docs/manuscript/id-vs-ref-LD.md` untouched; committed with explicit path.</done>
</task>

<task type="auto">
  <name>Task 2: Journal-selection memo (Nature-first ladder)</name>
  <files>.planning/quick/260812-ot2-e-2-obligation-1-placement-draft-and-jou/260812-ot2-journal-selection-memo.md</files>
  <action>
Create the memo. ⛔ Every fact comes from `260812-ot2-RESEARCH.md` ONLY — carry
each fact's confidence flag (verified 2026-08-12 / MEDIUM — secondary source,
confirm before submission / model knowledge — verify) with it; do not add new
venue facts, do not fetch anything, do not soften the honest realism notes. The
memo does not quote, evaluate, or touch the E-2 correction texts — so no E-2
figures appear anywhere in it; any figure of any other kind (APCs, word caps)
keeps its unit/currency and flag. Required sections:

1. **Header + scope** — Carter's 2026-08-12 directive (Nature-first, "we can
   adjust for others"); recommend-don't-flatter; $0, nothing posted anywhere;
   facts frozen as of RESEARCH.md's 2026-08-12 session, with its valid-until
   note (~2026-11 for APCs).
2. **Manuscript profile** — from RESEARCH.md: solo-author original research
   audit of 50 curated cardiometabolic pleiotropy loci; identity-LD vs real-LD;
   largely corrective/negative findings with one surviving Tier-A anchor
   (SH2B3); ~15,400-word Markdown file, abstract above every venue cap; Day-1
   bioRxiv planned regardless; incumbent *Genome Medicine* header; README
   cover-letter list.
3. **The ladder (the recommendation)** — Nature flagship first per directive
   (expect fast desk reject; real value = near-zero cost of satisfying the
   directive + the formal Manuscript Transfer Service with editor referral and
   optional referee-report transfer) → Nature Genetics as an optional single
   rung (low odds) → Nature Communications (the realistic Nature-brand landing;
   receives Nature transfers with referee reports) → AJHG / Genome Biology (the
   two STRONG-fit rungs) → Genome Medicine (incumbent; fully viable, below GB
   on methods-nativeness) → PLOS Genetics / HGG Advances (high-probability
   floor). eLife = parallel-philosophy option, NOT a rung (no accept gate, no
   current JIF since the model change — a real cost against the "most
   reputable" criterion). Nature Metabolism: pruned, weakest scope fit. State
   the honest top-3 by reputation-per-realistic-odds (AJHG, Nature
   Communications, Genome Biology) even where it cuts against the Nature-first
   aim — Carter said "we can adjust for others".
4. **Per-venue table** — one row per venue carrying RESEARCH.md's columns:
   (a) scope fit + acceptance realism (honest); (b) format vs the current
   453-line full article (restructure needed?); (c) **THE PRE-PLACEMENT-CHECK
   column**: how a "Methods correction-and-disclosure note" inside a FRESH
   submission reads at that venue — include the structural distinction (the v2
   file's check concerns an in-submission manuscript; a fresh submission has no
   live editorial record for correction machinery to act on), flag Nature
   flagship as the one venue where the Limitations-adjacent read is safer
   (Methods ~3,000-word cap, verified), and note the Cell Press venues' (AJHG,
   Cell Genomics, HGG Advances) designated "Limitations of the study"
   subsection mapping for the pointer-sentence pattern; (d) Day-1 bioRxiv
   compatibility; (e) OA/APC with figures + flags ($0 subscription routes where
   they exist); (f) dual-submission / transfer rule.
5. **Conditional Track-A-pending paragraph** — in substance from CONTEXT/
   RESEARCH: if a Genome Medicine (or any) submission of this manuscript is
   currently pending, no second submission may be made anywhere until it is
   withdrawn or decided — every listed venue prohibits simultaneous submission —
   and placing the correction note into a PENDING submission re-triggers the
   in-submission reading of the pre-placement check. Assert nothing about
   whether such a submission exists.
6. **Sources + flags** — carry RESEARCH.md's verified-URL list, the
   MEDIUM-confidence items (Cell Genomics ~$8,900; PLOS Genetics ~$2,655; AJHG
   OA amount — confirm on publisher pages before submission), the
   model-knowledge items, and the valid-until note.

Framing: hypothesis-driven original research throughout; the words "revision"
and "salvage" must not appear at all (rephrase any rule-statement so the greps
stay trivially clean).

Commit (explicit path only):
`git add .planning/quick/260812-ot2-e-2-obligation-1-placement-draft-and-jou/260812-ot2-journal-selection-memo.md && git commit -m "docs(quick-260812-ot2): journal-selection memo — Nature-first ladder, honest fit ranking, per-venue pre-placement check"`
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && M=.planning/quick/260812-ot2-e-2-obligation-1-placement-draft-and-jou/260812-ot2-journal-selection-memo.md && grep -qi 'pre-placement' "$M" && grep -q 'Nature Communications' "$M" && grep -q 'AJHG' "$M" && grep -q 'Genome Biology' "$M" && grep -q 'Genome Medicine' "$M" && grep -qi 'pending' "$M" && grep -qi 'model knowledge' "$M" && grep -qi 'verified' "$M" && grep -qi 'bioRxiv' "$M" && ! grep -n '5\.29\|18\.41\|23\.80\|17\.82\|0\.4234\|38\.68' "$M" && ! grep -in 'revision\|salvage' "$M" && echo TASK2-PASS</automated>
  </verify>
  <done>Memo exists with all 6 sections; ladder is Nature-first with the transfer cascade; honest top-3 stated; per-venue table carries the pre-placement-check column; conditional Track-A paragraph present with no assertion of submission status; every fact flag carried; no E-2 figures anywhere; committed with explicit path.</done>
</task>

<task type="auto">
  <name>Task 3: Append the DECISIONS.md ledger entry</name>
  <files>.planning/DECISIONS.md</files>
  <action>
APPEND (never edit or delete existing lines) one entry at the end of
`.planning/DECISIONS.md`, house style per the :1252 entry:

Heading: `## 2026-08-12 — DEC-2026-08-12-e2-obligation1-venue-and-obligation2-skip: obligation (1) prepped placement-ready with a Nature-first venue ladder; obligation (2) SKIPPED by direction (deferred, not discharged)`

Body must record:

1. **The directive, verbatim** (Carter, 2026-08-12, 17:48 EDT): "For E-2
   obligation (1), we will write a draft and then determine the best/most
   rigorous/reputable journal to submit to (we will aim for nature but we can
   adjust for others). skip this E-2 obligation (2)."
2. **Directive 1 — obligation (1) prep + venue re-target.** The placement-ready
   SPEC (`260812-ot2-placement-draft-ms-correction-v2.md`) carries the
   byte-verified `ms-correction-v2` block, exact anchors, and the Limitations
   pointer; venue selection is re-targeted Nature-first per the memo
   (`260812-ot2-journal-selection-memo.md`), superseding the manuscript
   header's incumbent *Genome Medicine* line as ADVICE (the header itself is
   untouched; updating it is Carter's edit). Obligation (1) remains OPEN — it
   discharges only at Carter's placement; this entry records preparation, not
   discharge.
3. **Directive 2 — obligation (2) SKIPPED by direction.** No OSF drafting,
   posting prep, or `osf_deviations.md` work was performed. Status:
   skipped-by-direction = DEFERRED, NOT DISCHARGED — the v2 file §4 row for (2)
   stays UNDISCHARGED and its discharge condition (posting + URL/timestamp
   record) is unchanged. The E-4 public-commitment obligation is NOT registered
   (its trigger is posting, which is skipped).
4. **The coherence consequence + the P-1/P-2 fork.** The v2 block's final
   sentence claims a posted supplementary entry on osf.io/az52u; with (2)
   skipped that claim is false at placement time. Fork recorded: **P-1
   (pair-coherent, RECOMMENDED)** — place byte-intact, treat (2) as
   deferred-to-submission (OSF entry must exist by submission time); grounds:
   rigor-over-speed + the v2 §3 item 4 pair-matching check. **P-2
   (skip-permanent)** — replace ONLY the final sentence with the marked variant
   in the SPEC; a Carter-directed deviation from the gated v2 pair, after which
   the placed text is no longer "the v2 pair half". Carter chooses at
   placement.
5. **Discharge-status table** (unchanged statuses restated for the record):
   (3) DISCHARGED (`DEC-2026-08-11-e2-framing-correction`); (1) OPEN —
   placement-ready, discharges at Carter's placement; (2) OPEN — deferred by
   direction.
6. **Cross-refs:** `DEC-2026-08-11-e2-framing-correction`;
   `DEC-2026-08-07-e2-orientation-disposition`; the v2 pair file
   `.planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-SELECTED-PAIR-correction-v2.md`;
   the two ot2 deliverables; `260812-ot2-CONTEXT.md` / `260812-ot2-RESEARCH.md`.

Number/framing rules bind the entry: no E-2 figures needed — quote none (if any
is quoted, the pooled-alone / unit / identity-LD-stub rules bind); never
"revision"; "salvage" only negated.

Commit (explicit path only):
`git add .planning/DECISIONS.md && git commit -m "docs(quick-260812-ot2): ledger — DEC-2026-08-12-e2-obligation1-venue-and-obligation2-skip (obligation (2) deferred, not discharged; Nature-first venue re-target; P-1/P-2 fork)"`
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && grep -q 'DEC-2026-08-12-e2-obligation1-venue-and-obligation2-skip' .planning/DECISIONS.md && grep -q 'DEC-2026-08-11-e2-framing-correction' .planning/DECISIONS.md && sed -n '/DEC-2026-08-12-e2-obligation1-venue-and-obligation2-skip/,$p' .planning/DECISIONS.md | grep -q 'UNDISCHARGED\|NOT DISCHARGED' && [ "$(git log -1 --numstat --format= -- .planning/DECISIONS.md | awk '{print $2}')" = "0" ] && git status --porcelain -- docs/manuscript/id-vs-ref-LD.md | grep -c . | grep -qx 0 && echo TASK3-PASS</automated>
  </verify>
  <done>One entry appended (0 lines deleted, proven by the commit's numstat); records both directives, the deferred-not-discharged status of (2), the non-registration of the E-4 public commitment, the P-1/P-2 fork with P-1 recommended, and all cross-refs; committed with explicit path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → manuscript/journal | text drafted here is destined for a public, career-bearing artifact |
| repo → ledger | DECISIONS.md is the append-only decision record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ot2-01 | Tampering | paste block in placement draft | mitigate | machine extraction + recorded `cmp` vs the v2 source, with an observed-red negative control before trusting green |
| T-ot2-02 | Repudiation | closing OSF-reference sentence | mitigate | P-1/P-2 fork surfaced in the draft and recorded in the DECISIONS entry; no silent false claim reaches placement |
| T-ot2-03 | Information disclosure | Track A submission status | mitigate | conditional paragraph only; neither deliverable asserts the status either way |
| T-ot2-04 | Tampering | DECISIONS.md history | mitigate | append-only enforced by commit numstat (deletions = 0) |
| T-ot2-05 | Elevation of privilege | agent editing manuscript/OSF | mitigate | no task touches `docs/manuscript/` or OSF; Task 1/3 verifies assert the manuscript is porcelain-clean |
</threat_model>

<verification>
1. All three task verifies green (TASK1-PASS, TASK2-PASS, TASK3-PASS).
2. `git status --porcelain -- docs/manuscript/ src/ tests/ config/ Snakefile results/ .planning/amendments/` → empty (nothing outside the declared surface changed).
3. Three commits exist, each staging only its declared explicit paths.
4. Decision coverage: every LOCKED item in `260812-ot2-CONTEXT.md` <decisions> maps to a section of a deliverable (no-agent-edits → Task 1 header + T-ot2-05; byte-lock → Task 1 §4/§7; (2)-skip + consequence → Task 1 §5 + Task 3; memo scope → Task 2; number/framing rules → greps in all three verifies; process constraints → commit discipline).
</verification>

<success_criteria>
- Carter can discharge obligation (1) with a <=2-minute paste guided solely by the draft, choosing P-1 or P-2 at placement.
- The venue decision has an honest, flag-carrying factual substrate implementing the Nature-first directive.
- The ledger records both 2026-08-12 directives; obligation (2) remains UNDISCHARGED by explicit record, not by omission.
- Zero changes to the manuscript, OSF surfaces, or any code/results path; $0 spent; nothing posted.
</success_criteria>

<output>
After completion, create `.planning/quick/260812-ot2-e-2-obligation-1-placement-draft-and-jou/260812-ot2-SUMMARY.md`
(quick-task summary: deliverable paths, the three commit SHAs, the observed
verification outputs including the negative-control red run, and the standing
next step: Carter's placement per the draft, then the fire gate sequence owned
by Carter — never an agent).
</output>
