---
phase: quick/260806-b77
plan: 01
type: execute
wave: 1
depends_on: [quick/260805-23d, quick/260805-o7o, quick/260805-w7u]
subsystem: m3-ld-read-path
tags: [ld-panel, snakemake, crosswalk, provenance, blast-radius, aou, trans, receipt]
autonomous: true
requirements: [BR-G, BR-J, BR-K, BR-L, BR-M]
baseline_rev: 6b427bc
files_modified:
  - src/python/ld_panel.py
  - config/pipeline.yaml
  - src/snakemake/rules/finemap.smk
  - src/python/build_curated_m2_crosswalk.py
  - tests/m3/test_ld_panel_aou_orphan_and_strict.py
  - tests/m3/test_finemap_receipt_early_exit.py
  - tests/m3/test_curated_m2_crosswalk_drift.py
  - .planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md
user_setup: []

must_haves:
  truths:
    - "With strict_aou_only ON, a TRANS resolution whose TRANS_aou_eur head is absent RAISES FileNotFoundError instead of silently walking to EUR_1kg."
    - "With the SHIPPED config (strict_aou_only false), every curated region x ancestry resolves to the byte-identical path it resolved to at 6b427bc -- Track A cannot move."
    - "A NEW AoU-sourced ld_panel chain entry cannot be added without either a producing Snakemake rule or an explicit orphan-registry entry naming the consequence."
    - "The per-region receipt distinguishes an early-exit region (no_variants / too_many_variants) from a genuine LD-field regression -- the two render DIFFERENT tokens."
    - "A committed config/curated_to_m2_region_map.tsv that has drifted from a fresh rebuild FAILS the suite."
    - "A curated region present in config/regions_curated.csv but ABSENT from the crosswalk produces a named, loud WARN at DAG-parse time."
    - "A crosswalk row with status=partial is NOT handed to resolve_ld_path; the pre-change loader IS shown to hand it over."
    - "Finding K is registered as a PREPARED DEFERRAL carrying the exact minimal diff, its blast radius, its re-freeze obligation, and the two authorizations it needs."
  artifacts:
    - path: "src/python/ld_panel.py"
      provides: "token-based is_aou_source() predicate; strict-mode guard no longer blind to TRANS_aou_eur"
      contains: "def is_aou_source"
    - path: "src/snakemake/rules/finemap.smk"
      provides: "status-aware per-region receipt; coverage-aware crosswalk WARN"
      contains: "d.get('ld_allele_catalog_join')"
    - path: "src/python/build_curated_m2_crosswalk.py"
      provides: "contained-only load allow-list (M) + crosswalk drift/coverage reader (L)"
      contains: "def load_curated_to_m2"
    - path: "tests/m3/test_ld_panel_aou_orphan_and_strict.py"
      provides: "G -- predicate verdict table, orphan registry, strict-mode proof, shipped-config invariance"
    - path: "tests/m3/test_finemap_receipt_early_exit.py"
      provides: "J -- differential receipt test extracted from git, early-exit vs regression distinguishability"
    - path: "tests/m3/test_curated_m2_crosswalk_drift.py"
      provides: "L/M -- committed-vs-rebuild drift, coverage, partial-row rejection with a pre-change differential"
    - path: ".planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md"
      provides: "G-2 (TRANS scientific decision) and K-1 (prepared deferral with exact diff)"
      contains: "K-1"
  key_links:
    - from: "src/python/ld_panel.py::resolve_ld_path"
      to: "src/python/ld_panel.py::is_aou_source"
      via: "the strict_aou_only guard predicate"
      pattern: "is_aou_source\\(entry\\["
    - from: "tests/m3/test_ld_panel_aou_orphan_and_strict.py"
      to: "config/pipeline.yaml ld_panel block"
      via: "yaml.safe_load of the REAL shipped config, never a structural copy"
      pattern: "pipeline\\.yaml"
    - from: "tests/m3/test_finemap_receipt_early_exit.py"
      to: "src/snakemake/rules/finemap.smk shell block"
      via: "extraction of the live PYTHON_BIN -c receipt line, executed against fixtures"
      pattern: "PYTHON_BIN"
    - from: "src/snakemake/rules/finemap.smk module scope"
      to: "src/python/build_curated_m2_crosswalk.py drift reader"
      via: "import plus a named WARN listing the missing curated slugs"
      pattern: "crosswalk"
---

<objective>
Close the remaining autonomous blast-radius findings **G, J, L, M** and register **K** as a
prepared deferral, discharging **two** of the three named gate rows in
`.planning/phases/m3-aou-afr-ld-panel-build/m3-04c-BLAST-RADIUS.md:133-144`.

**Purpose:** every one of these is a silent-wrong-answer defect that the 745-test `tests/m3`
suite is currently structurally incapable of observing. None blocks what has merged; all
five block a class of *conclusion*.

**Output:** three source edits, one config comment, three NEW test modules, two registry
entries. `$0`, NC State, zero perimeter contact, no AoU fire.

## THE HONEST GATE ACCOUNTING -- read before reporting anything

| Gate row (`m3-04c-BLAST-RADIUS.md`) | Blocked by | After this plan |
|---|---|---|
| **Any TRANS fit** | G, F | **CLEARED** -- G closed here; **F re-derived, not assumed** (T1 STEP 0) |
| **Publishing the panel provenance** | I, J, K | **PARTIAL -- NOT cleared.** I closed by `260805-o7o`, J closed here, **K DEFERRED** |
| **Growing the curated region set** | L, M | **CLEARED** |

**TWO gate rows clear, not three.** The task brief anticipated three; K's remedy sits inside
a re-frozen file **and** is separately mandated by a pre-existing test (both proven in T2
STEP 0). Per `<the_freeze_constraint>` that makes K a prepared deferral, which is a success
-- but it is **not** a closure and must never be reported as one.

## WHAT G's CLOSURE DOES AND DOES NOT DO

It does **not** make TRANS work. After this plan a TRANS fit still resolves to
`data/processed/ld_reference/EUR/{region_safe}.rds` -- the legacy 1kG EUR panel -- exactly as
it does today. What changes is that the orphan stops being **silent**: `strict_aou_only` can
now see it, the suite fails if a *new* orphaned AoU entry is added, and the consequence is
written where the next editor will read it. Whether a TRANS fit on a 1kG EUR panel is
reportable at all is a scientific question, registered as **G-2**, not answered here.

Likewise **the AoU panel does not exist** (0/276 `.npz`; `data/processed/ld_reference/` is
absent from this node entirely) and `resolve_ld_path` returns the first EXISTING path. Every
acceptance criterion below is satisfiable on **fixtures and source text**. **No criterion may
require a real panel on disk** -- two prior plans in this arc shipped exactly that
unsatisfiable shape. Say plainly in the SUMMARY which parts are correct wiring that is INERT
today, exactly as `260805-w7u` did for finding E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/m3-aou-afr-ld-panel-build/m3-04c-BLAST-RADIUS.md
@.planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md
@.planning/quick/260805-o7o-close-blast-radius-findings-h-and-i-alle/260805-o7o-SUMMARY.md
@.planning/quick/260805-w7u-close-blast-radius-finding-e-crosswalk-t/260805-w7u-SUMMARY.md
@src/python/ld_panel.py
@src/python/build_curated_m2_crosswalk.py
@src/snakemake/rules/finemap.smk
@config/pipeline.yaml
</context>

<verified_anchors>
**Every line number below was RE-DERIVED at HEAD `6b427bc` during planning.** The
blast-radius doc's anchors are stale and were not trusted.

| Finding | Live anchor at `6b427bc` | Verified fact |
|---|---|---|
| **G** | `src/python/ld_panel.py:89` | `if panel_cfg.get("strict_aou_only", False) and entry["source"].endswith("_aou"):` |
| **G** | `config/pipeline.yaml:221-223` | TRANS chain; `:222` head is `{source: "TRANS_aou_eur", path: "data/processed/ld_reference/EUR_aou/{region_id}.rds"}` |
| **G** | `src/snakemake/rules/m3_convert_npz_rds.smk:304-326` | the `build_ld_rds_aou_eur` RETIREMENT note (every line a `#` comment) |
| **G** | `src/snakemake/rules/m3_convert_npz_rds.smk:138` | `rule build_ld_rds_aou_afr:` -- the LIVE `AFR_aou/*.rds` producer |
| **J** | `src/snakemake/rules/finemap.smk:441` | the single `{PYTHON_BIN} -c "..."` receipt line (15 `d.get(...)` reads) |
| **J** | `run_susie_rss.R:919-943` and `:946-978` | the `no_variants` / `too_many_variants` early-exit writers -- **FROZEN**, emit no `ld_*` key |
| **K** | `run_susie_rss.R:787`, `:1013`, `:1208` | init FALSE / Path-2 `variant_catalog_fallback <- TRUE` / success-JSON emit -- **FROZEN** |
| **K** | `tests/m3/test_ld_read_path.py:451-453` | a **PRE-EXISTING test MANDATES** `variant_catalog_fallback <- TRUE` inside the Path-2 branch |
| **L** | `src/snakemake/rules/finemap.smk:160-175` | crosswalk loaded at module scope; the WARN at `:167` fires ONLY on `not CURATED_TO_M2` |
| **L** | `tests/m3/test_curated_m2_crosswalk.py:181-203` | `_build_into` / `_production_rows` -- **every** test rebuilds into a tmpdir; **zero** read the committed file |
| **L** | `config/curated_to_m2_region_map.tsv` | 15 columns, 12 data rows: **11 `contained` + 1 `unmapped`** (BMI_Xq24) |
| **M** | `src/python/build_curated_m2_crosswalk.py:485` | `if row.get("status") == "unmapped": continue` -- a DENY-list of one |
| **M** | `src/python/build_curated_m2_crosswalk.py:48` and `:257` | the promise "A partial match is NEVER promoted to a containment"; `"partial"` is a really-emitted status |

**Environment confirmed present on this node:** `data/external/liftover/hg38ToHg19.over.chain.gz`
(1,246,411 bytes) and `pyliftover` inside `smoke_dev` -- so the crosswalk rebuild in T3 is
runnable here and is **not** one of the 8 `chain file not present` skips.

**Pre-existing tests that CONSTRAIN the fixes (all re-derived; none may be edited):**

* `tests/m3/test_ld_panel_resolver.py:184` requires `EUR_aou` to remain **in the EUR chain**
  (`eur_sources.index("EUR_aou")` would raise `ValueError` if removed). **Do not touch the
  EUR chain.**
* `tests/m3/test_ld_read_path.py:472-476` requires the literals `ld_overlap_zero_fallback`
  and `variant_catalog_fallback` to survive in `finemap.smk`'s **comment-stripped** `shell:`
  body.
* `tests/m3/test_ld_allele_aware_wiring.py:281-287` requires the literal `d.get('<key>')` for
  all seven `ld_allele_*` keys in the comment-stripped shell.
* `tests/m3/test_ld_read_path_ancestry_gate.py:380-393` (`_rscript_argv`) cuts the argv at the
  **first `{PYTHON_BIN}` line after `Rscript`**. **Introducing a new `{PYTHON_BIN}` line
  between the `Rscript` invocation and the receipt truncates the argv and breaks the
  argv-delta test.** All J work stays on / after the existing receipt line.
* `tests/m3/test_curated_m2_crosswalk.py:470-503` pins `set(load_curated_to_m2(...))` to the
  mapped oracle -- all 11 mapped rows are `contained` today, so T3's allow-list change keeps
  this green.
</verified_anchors>

<hard_rules>
1. **A GREEN ASSERTION IS EVIDENCE ONLY IF YOU HAVE SEEN IT FAIL.** Every new assertion ships
   with a negative control that was **OBSERVED RED** and is quoted raw in the SUMMARY. Eleven
   assertions in this arc were structurally incapable of failing; `260805-w7u` caught three
   more in its own tests before committing.
2. **The `.pyc` trap.** `SourceFileLoader` validates cached bytecode on `(mtime_seconds, size)`.
   Load any module-under-differential-test by `compile()`ing source text read at call time
   (the `_load_module_from_text` pattern already in `tests/m3/test_qtl_coloc_ld_resolution.py`),
   **or** make every revert change the byte length. Never rely on a same-second restore.
3. **TRACK A MUST NOT MOVE.** Prove invariance with whole-object / whole-string equality plus
   an inverted control. `ld_status` / `ld_overlap_fraction` / `status` are **DISQUALIFIED** as
   evidence -- m3-04c proved EUR numerics move while all three stay byte-identical.
4. **NO PRE-EXISTING TEST FILE MAY BE EDITED.** `AUTH-o7o-01` is **not** inherited. A failing
   pre-existing test is a **STOP-and-surface**, never an edit and never a weakened assertion.
   Do not touch `run_finemap.params.region_id`. All three new modules are NEW files.
5. **FROZEN, 0-diff:** `src/legacy/region_analysis/scripts/run_susie_rss.R` at `dc4bbd2`;
   `plink_ld_to_npz.py`; `ld_npz_to_rds.R`; `condition_ld_matrix.py`; the m3-07 occlusion
   modules. m3-06 stays **HELD** (no NaN->0, no `condition_ld_matrix.py`). Forward gate,
   asserted at the end of every task:
   `git diff --exit-code dc4bbd2 -- src/legacy/region_analysis/scripts/run_susie_rss.R`
6. **Schema:** `src/snakemake/schemas/pipeline.schema.yaml` is `additionalProperties: false`
   **at the TOP LEVEL ONLY** (measured in `260805-o7o`; a new SUB-key needs no entry).
   **This plan adds NO config key at all** -- T1's `config/pipeline.yaml` edit is
   **comment-only** -- so **no schema edit is required or permitted.** If you find yourself
   editing the schema you have deviated; stop and re-read.
7. **Environment.** Never `conda activate`.
   Snakemake `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/snakemake`;
   Python `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python`; R env `m3-r-ld`.
8. **Git:** explicit paths only, never `git add -A` or `git add .`. No worktree isolation.
9. **BOTH suites gate** (T3 STEP 5): `tests/m3` **>= 745 passed / == 31 skipped / 0 failed**;
   `tests/phase2` **136 passed / 1 skipped / 0 failed**. **A new test landing as a SKIP is not
   evidence** -- skips must stay at 31 and 1. Reconcile the delta arithmetically per module.
10. `tests/m3/sparse_parent_benchmark.tsv` is rewritten by the suite on every run (timing
    jitter only). Restore with `git checkout --`. **Never commit it.**
11. **NEVER trigger the AoU fire.** No `gsutil` / `gcloud` / `bq` / `wb` / `dataproc` /
    `hailctl` anywhere in the session or the diff. Grep before each commit.
12. `finemap.smk` is touched by **T2 and T3**. T2 edits the `shell:` block only; T3 edits
    module scope only. Run them in order and re-read the file before T3.
</hard_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: G -- un-blind the strict guard, register the orphan, prove neither direction over-fires</name>
  <files>src/python/ld_panel.py, config/pipeline.yaml, tests/m3/test_ld_panel_aou_orphan_and_strict.py, .planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md</files>

  <behavior>
  - The predicate returns **True** for `EUR_aou`, `AFR_aou`, `TRANS_aou_eur` and **False** for
    `EUR_ukbb_pub`, `EUR_ukbb`, `EUR_1kg`, `AFR_hgdp`, `AFR_1kg` -- the complete shipped
    source set. Before the change `TRANS_aou_eur` is False; that is finding G.
  - It is **token-based**, not substring-based: `EUR_aoudad` returns **False**.
  - With `strict_aou_only: True` and the TRANS head absent, `resolve_ld_path(..., "TRANS")`
    raises `FileNotFoundError` matching `strict_aou_only`. Before the change it returns the
    `EUR_1kg` path.
  - With `strict_aou_only: True` and a chain of **only non-AoU** entries, resolution does
    **not** raise when any entry exists -- the guard must not newly fire on a legitimate entry.
  - Under the **SHIPPED** config (`strict_aou_only: false`) the resolved path for every
    (curated slug x {EUR, AFR, TRANS}) equals `6b427bc`'s, over a fixture tree.
  - The AoU-sourced set in the shipped chains is exactly
    `{AFR_aou (produced), EUR_aou (orphan), TRANS_aou_eur (orphan)}`; a fourth fails the suite.
  </behavior>

  <action>
**STEP 0 -- RE-DERIVE FINDING F BEFORE CLAIMING THE GATE ROW.** The gate row "Any TRANS fit"
is blocked by **G and F**. F is "the crosswalk has no ancestry gate". Verify -- do not assume
-- that F is already closed by `260805-23d`: confirm `finemap.smk`'s `input.ld_matrix` routes
`region_id` through `ld_read_path.ld_matrix_region_id(...)` (live at `finemap.smk:310-323`),
that `ld_matrix_region_id` applies `CURATED_TO_M2` only for `config.ld_read_path.ancestries`,
and that `tests/m3/test_ld_read_path_ancestry_gate.py` pins EUR/TRANS to `3f431ab`'s resolved
string with a **non-vacuous** negative control (it does -- `:305-318`). Quote the evidence
verbatim in the SUMMARY. **If F is NOT closed, STOP and surface** -- do not claim the gate row
on G alone.

**STEP 1 -- the predicate.** In `src/python/ld_panel.py`, add a module-level public helper
immediately above `resolve_ld_path` and export it in `__all__`:

```python
#: Token that marks a chain source as AoU-derived.
_AOU_SOURCE_TOKEN = "aou"


def is_aou_source(source: str) -> bool:
    """True when *source* names an AoU-derived panel, ANYWHERE in the name.

    DELIBERATELY NOT ``endswith("_aou")``. The TRANS chain head is
    ``TRANS_aou_eur`` -- ancestry-of-panel LAST -- so the suffix test returned
    False and ``strict_aou_only`` was PROVABLY BLIND to it (m3-04c blast
    radius, finding G). The removal note for ``build_ld_rds_aou_eur`` verified
    that the RULE NAME had no references; it never checked the ARTIFACT PATH,
    so the TRANS head outlived its producer.

    Split on ``_`` and test for the EXACT token, so this cannot widen
    accidentally onto a source that merely CONTAINS the letters (``EUR_aoudad``).

    Verdicts on the shipped chains (config/pipeline.yaml:212-223)::

        EUR_ukbb_pub  False | EUR_aou        True
        EUR_ukbb      False | AFR_aou        True
        EUR_1kg       False | TRANS_aou_eur  True   <- was False before finding G
        AFR_hgdp      False | AFR_1kg        False
    """
    return _AOU_SOURCE_TOKEN in str(source).split("_")
```

Change **only** the guard at `ld_panel.py:89` to

```python
        if panel_cfg.get("strict_aou_only", False) and is_aou_source(entry["source"]):
```

and update the `strict_aou_only` sentence in the `resolve_ld_path` docstring (`:40-52`) so it
says "an AoU-sourced entry (see ``is_aou_source``)" instead of implying a suffix. **No other
line of `ld_panel.py` moves.**

**STEP 2 -- the config disclosure (COMMENT ONLY).** Insert a comment block immediately above
`  TRANS:` at `config/pipeline.yaml:221`. **Change no parsed value.**

`TRANS_aou_eur` **STAYS** in the chain. Deleting it would leave TRANS with no AoU entry at
all, which would make `strict_aou_only` structurally unable to ever flag TRANS again -- that
*deepens* the silence rather than closing it, and it is the same "a fallback chain hides an
unreachable artifact" shape the project has already been bitten by. Record in the comment:

* `EUR_aou/*.rds` has **no producer** -- `build_ld_rds_aou_eur` was retired 2026-08-05
  (`src/snakemake/rules/m3_convert_npz_rds.smk:304-326`) and the AoU perimeter computes no
  EUR LD at all, so `data/interim/aou_ld_exports/EUR_aou/` is never populated;
* TRANS therefore resolves to `EUR_1kg` -- the legacy 1kG EUR panel -- on every run today and
  after the fire;
* `strict_aou_only: true` is now the lever that converts that silence into a raise (finding G,
  closed by `quick-260806-b77`);
* **do not set `pin.TRANS`** as a containment: `pin` short-circuits the chain **ahead of**
  strict mode (`ld_panel.py:78-83`), so pinning would re-hide exactly what this fix exposes;
* the scientific question "may a TRANS fit be reported on a 1kG EUR panel at all" is
  registered as **G-2** in `.planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md`.

**STEP 3 -- the test module `tests/m3/test_ld_panel_aou_orphan_and_strict.py` (NEW).**
Load the **REAL** `config/pipeline.yaml` with `yaml.safe_load`. Do **not** hand-copy the
chains: a structural copy is precisely why `test_ld_read_path_ancestry_gate.py::_panel_cfg`
cannot observe a shipped-config drift, and that limitation must be inherited deliberately,
never by accident. Deep-copy the loaded block and re-root paths under `tmp_path` for the
resolution tests.

  a. **Verdict table, exhaustive.** For every `source` in every shipped chain assert
     `is_aou_source(source)` matches the docstring table. Assert the True-set is **exactly**
     `{"EUR_aou", "AFR_aou", "TRANS_aou_eur"}` -- an equality, not a superset, so a widened
     predicate fails here.
  b. **Token, not substring.** `is_aou_source("EUR_aoudad") is False`;
     `is_aou_source("aou") is True`; `is_aou_source("TRANS_aou_eur") is True`.
  c. **Strict mode, TRANS, end to end.** Re-rooted shipped block, `strict_aou_only = True`,
     **no** files created: `pytest.raises(FileNotFoundError, match="strict_aou_only")` for
     `TRANS`.
  d. **The guard must NOT newly fire (the P3 / `quick-260715-vxz` lesson).** With
     `strict_aou_only = True` and a chain of only non-AoU sources (`EUR_ukbb_pub` ->
     `EUR_1kg`) where the second exists, assert the call **returns** that path and does not
     raise. Separately assert that under the shipped `strict_aou_only: false` **no ancestry
     raises** when a tail entry exists.
  e. **Orphan registry -- the durable half.** Two module-level dicts carrying prose reasons:
     `PRODUCED_AOU_SOURCES = {"AFR_aou": ("src/snakemake/rules/m3_convert_npz_rds.smk", "build_ld_rds_aou_afr")}`
     and `KNOWN_ORPHANED_AOU_SOURCES = {"EUR_aou": "<reason>", "TRANS_aou_eur": "<reason>"}`.
     Assert:
     * the shipped AoU-source set equals `set(PRODUCED) | set(ORPHANS)` -- **a fourth AoU
       entry added to the config with no registry entry fails the suite**;
     * for each PRODUCED source, `rule <name>:` occurs in the named `.smk`;
     * `"rule build_ld_rds_aou_eur"` occurs in **no** `.smk` under `src/snakemake/rules/`;
     * **the artifact half, which the retirement note skipped:** no **non-comment** line in
       any `src/snakemake/rules/*.smk` nor in `Snakefile` contains `ld_reference/EUR_aou`.
       Strip lines whose `lstrip()` starts with `#` before searching (the
       `_shell_command_block` discipline -- a comment satisfying its own regex broke two
       assertions in `260805-w7u`). **Pre-verified during planning: the only hits are inside
       the `#`-commented retirement note.** If you measure a non-comment hit, **STOP and
       surface** -- do not weaken the assertion to accommodate it.
  f. **Shipped-config resolution invariance (Track A, hard rule 3).** Load `6b427bc`'s
     `ld_panel.py` via `git show 6b427bc:src/python/ld_panel.py` plus `compile()` (hard rule
     2 -- do **not** import the on-disk module twice). Build a fixture tree that touches a
     plausible file for **every** chain entry, then for the 12 curated slugs x
     `{EUR, AFR, TRANS}` assert `str(new(...)) == str(old(...))` for **every** cell under the
     shipped `strict_aou_only: false`. **Non-vacuity guard:** at least one cell must resolve
     to a real path -- an all-raise fixture makes the equality meaningless.
     **INVERTED CONTROL, permanent and in-suite:** the same comparison with
     `strict_aou_only = True` and the TRANS head absent must **DIFFER** (new raises, old
     returns the `EUR_1kg` path). Without it the equality above is a tautology.

**STEP 4 -- register G-2.** Append to
`.planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md` under a new heading
`# Deferred items -- discovered during quick-260806-b77 execution (2026-08-06)`:

> **G-2 -- TRANS has no AoU panel and never will; is a TRANS fit on a 1kG EUR panel
> reportable?**

State the measured facts (no producer; resolves to `EUR_1kg`; `strict_aou_only` now catches
it; `pin.TRANS` would *defeat* the guard because `pin` short-circuits ahead of strict mode),
give a neutral A/B options table in the style of the existing E-2 entry, and say plainly this
is Carter's scientific call, not an executor's. **Not blocking.**

**NEGATIVE CONTROLS -- run all three, record each RED output raw.**
* **NC-G1 (the finding itself).** Revert `is_aou_source`'s body to
  `return str(source).endswith("_aou")` (byte length changes -- hard rule 2). (c), the
  verdict table (a) and the inverted control (f) must go RED. Restore and re-run green.
* **NC-G2 (over-fire).** Stub `is_aou_source` to `return True`. (d) must go RED -- a
  legitimate non-AoU entry now raises under strict mode. This is the false-trip direction and
  it is the one the P3 lesson says gets missed.
* **NC-G3 (registry non-vacuity), PERMANENT AND IN-SUITE.** Inject a synthetic
  `{"source": "AFR_aou_v2", "path": ".../AFR_aou_v2/{region_id}.rds"}` into an **in-memory**
  copy of the loaded config and assert the registry check in (e) reports it as unregistered.
  This makes "a new orphan cannot be added silently" a CI-enforced property rather than a claim.
  </action>

  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_ld_panel_aou_orphan_and_strict.py tests/m3/test_ld_panel_resolver.py tests/m3/test_ld_read_path_ancestry_gate.py tests/m3/test_finemap_loader_contract.py tests/m3/test_m3_ingest_convert_destale.py -q -rs</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/snakemake --snakefile Snakefile --configfile config/pipeline.yaml --list > /dev/null && echo LIST_OK</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && git diff --exit-code dc4bbd2 -- src/legacy/region_analysis/scripts/run_susie_rss.R && echo FREEZE_OK && git diff --stat HEAD -- src/snakemake/schemas/pipeline.schema.yaml | wc -l</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -c "import yaml,sys; a=yaml.safe_load(open('config/pipeline.yaml')); import subprocess; b=yaml.safe_load(subprocess.run(['git','show','6b427bc:config/pipeline.yaml'],capture_output=True,text=True).stdout); print('PARSED_CONFIG_IDENTICAL', a==b); sys.exit(0 if a==b else 1)"</automated>
  </verify>

  <done>
  `is_aou_source` exists, is exported and is token-based; the strict guard uses it; the
  `config/pipeline.yaml` edit is **comment-only** and the parsed config is byte-equal to
  `6b427bc`'s (4th verify command prints `PARSED_CONFIG_IDENTICAL True`); the new module
  passes and every touched pre-existing module stays green; `--list` is rc 0; the freeze gate
  is clean; the schema is 0-diff; NC-G1 / NC-G2 were observed RED and NC-G3 is permanent
  in-suite; G-2 is registered; finding F's closure was re-derived and quoted (or the task
  STOPPED). Commit with explicit paths:
  `feat(260806-b77-T1): un-blind strict_aou_only to TRANS_aou_eur and register the orphan (FINDING G)`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: J -- make the receipt distinguish an early exit from a regression; K -- prepared deferral</name>
  <files>src/snakemake/rules/finemap.smk, tests/m3/test_finemap_receipt_early_exit.py, .planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md</files>

  <behavior>
  - On a `no_variants` or `too_many_variants` JSON, the receipt renders every LD field as a
    single explicit token naming the cause (e.g. `NA_EARLY_EXIT`) and a verdict
    `early_exit:<status>` -- **never** a bare `None`.
  - On a JSON whose `status` is a real fit status but whose `ld_matrix` key is **absent**, the
    receipt renders a **different** token (e.g. `ABSENT`) and the verdict
    `ALARM_LD_FIELDS_MISSING`. **The early-exit token and the alarm token must not be equal**
    -- that inequality is the whole of finding J.
  - On a full success JSON the receipt prints the real `ld_matrix` path and the verdict
    `ld_fields_present`; no token substitution fires.
  - The pre-change receipt, executed on the same early-exit fixture, prints
    `ld_matrix None ld_file_declared None` and carries no verdict -- observed, not asserted.
  - `run_susie_rss.R` is **0-diff** throughout, asserted mid-test.
  </behavior>

  <action>
**STEP 0 -- ESTABLISH, ON THE RECORD, THAT K CANNOT BE CLOSED HERE.** Two independent
blockers, both re-derived at HEAD during planning; re-confirm both and quote them:

1. Every `variant_catalog_fallback` assignment site lives in
   `src/legacy/region_analysis/scripts/run_susie_rss.R` (`:787` init, `:1013` the Path-2
   mutation, `:1208` the success-JSON emit). That file is **RE-FROZEN at `dc4bbd2`** and the
   `260805-o7o` unfreeze is **SPENT**.
2. `tests/m3/test_ld_read_path.py:451-453` is a **PRE-EXISTING** test that **requires**
   `"variant_catalog_fallback <- TRUE"` to be inside the Path-2 brace block. Removing the
   mutation makes it RED, and hard rule 4 forbids editing it.

So K needs **two** authorizations (an unfreeze **and** a pre-existing-test edit). **Do not
attempt it.** Go to STEP 4.

**STEP 1 -- confirm J's premise mechanically, then fix it in `finemap.smk` only.** The early
exits are `run_susie_rss.R:919-943` (`no_variants`) and `:946-978` (`too_many_variants`).
Their `result <- list(...)` payloads carry `status`, `notes`, `ld_dir`,
`variant_catalog_path`, `variant_catalog_attempted`, `variant_catalog_used`,
`variant_catalog_fallback` -- and **no** `ld_matrix`, `ld_file_declared`, `ld_authoritative`
or `ld_allele_*` key. **The receipt is the non-frozen half of the pair, so J closes with a
`finemap.smk` edit alone. Do not touch the R file.**

**STEP 2 -- the receipt edit, IN PLACE on `finemap.smk:441`.** Extend the existing single
`{PYTHON_BIN} -c "..."` line. **Hard constraints, all from pre-existing tests:**

* **Do not** move the receipt into a script, and **do not** add any new `{PYTHON_BIN}` line
  before it -- `_rscript_argv` cuts the argv at the first `{PYTHON_BIN}` line after `Rscript`,
  so either change truncates the argv-delta comparison.
* Every existing literal `d.get('<key>')` must survive verbatim for the seven `ld_allele_*`
  keys, plus `variant_catalog_fallback` and `ld_overlap_zero_fallback`.
* `--ld-allele-aware {params.ld_allele_aware}` must still appear exactly once in the
  comment-stripped shell.

Shape (single line; keep the existing key order and add `status` first):

```python
import json,sys; d=json.load(open(sys.argv[1])); st=d.get('status'); EARLY={'no_variants','too_many_variants'}; na=('NA_EARLY_EXIT' if st in EARLY else 'ABSENT'); vd=('early_exit:'+str(st) if st in EARLY else ('ld_fields_present' if 'ld_matrix' in d else 'ALARM_LD_FIELDS_MISSING')); print('region', sys.argv[2], 'ancestry', sys.argv[3], 'status', st, 'ld_receipt_verdict', vd, 'ld_z_consistency_s', (d.get('d3b_ld_z_consistency_s') if 'd3b_ld_z_consistency_s' in d else na), ... )
```

Apply the same `(d.get('<key>') if '<key>' in d else na)` wrapper to **every** LD field in the
line -- `d3b_ld_z_consistency_s`, `ld_source_mismatch_flag`, `ld_matrix`, `ld_file_declared`,
`ld_authoritative`, the two fallback flags and the eight allele fields -- so the literal
`d.get('<key>')` is preserved for the pre-existing assertions while an absent key can never
render as a bare `None` again.

**Rationale to put in the comment block above the line** (comments are stripped by
`_shell_command_block`, so this cannot satisfy or break any assertion):
`HLA_6p21` and `PYHIN1_1q23` are named `too_many_variants` regions, so before this change the
receipt printed `None None` on **real inputs today**, indistinguishable from a genuine
declare-vs-read regression -- the exact defect class BLOCKER-1 was.

**Also add, in the same comment block, K's decoder ring** (the $0 non-frozen partial for K):
`variant_catalog_fallback=True` **with** `ld_overlap_zero_fallback=True` is the **Path-2**
revert (`ld_overlap == 0`) and has **no numeric cause**; `variant_catalog_fallback=True` with
`ld_overlap_zero_fallback=False` is the legacy Path-1 AFR empty-subset revert. All 1,957
legacy JSONs carry `variant_catalog_fallback: false` and no `ld_overlap_zero_fallback` key at
all -- point the reader at **K-1** in `deferred-items.md` rather than letting them chase the
flip. **This is a partial mitigation of K, not its closure.**

**STEP 3 -- the test module `tests/m3/test_finemap_receipt_early_exit.py` (NEW).** No R and no
Snakemake execution needed; fixtures are JSON files in `tmp_path`.

* **Extract the receipt from the LIVE source, never hand-copy it** (the `260805-w7u`
  body-walk discipline). Read `finemap.smk`, take the `shell:` block with comment lines
  stripped, isolate the `{PYTHON_BIN} -c "..."` line, pull out the quoted program, substitute
  `{output.json}` / `{wildcards.region}` / `{wildcards.ancestry}`, and run it with the
  `smoke_dev` interpreter via `subprocess`. **Non-vacuity guard: assert exactly ONE such line
  exists and that the extracted program is non-empty and contains `json.load`** -- otherwise
  a failed extraction silently tests nothing.
* **Fixtures, four:** (1) a `no_variants` payload, (2) a `too_many_variants` payload -- both
  built from the exact key list emitted by the frozen R blocks; (3) a `status="ok"` payload
  **missing** `ld_matrix` (the real-regression case); (4) a full success payload carrying
  `ld_matrix`, `ld_file_declared` and the eight allele fields.
* **The load-bearing assertion:** the token printed for fixture (1)/(2) is **NOT EQUAL** to
  the token printed for fixture (3), and the verdicts differ (`early_exit:*` vs
  `ALARM_LD_FIELDS_MISSING`). Assert inequality explicitly -- an implementation that renders
  both as `MISSING` must fail here.
* **No false alarm:** fixture (4) prints the real `ld_matrix` path verbatim and the verdict
  `ld_fields_present`; assert neither token string appears anywhere in its output.
* **Premise pin against the FROZEN source.** Brace-walk `run_susie_rss.R` for the two
  `result <- list(...)` blocks inside `if (nrow(subset) == 0)` and
  `if (nrow(subset) > SUSIE_MAX_VARIANTS)` and assert **none** of `ld_matrix`,
  `ld_file_declared`, `ld_authoritative`, `ld_allele_exact` appears in either. If a future
  unfreeze adds them, this test tells you the receipt's premise moved. Assert the anchor
  strings occur **exactly once** each so the walk cannot silently match nothing. Assert
  `git diff --exit-code dc4bbd2 -- run_susie_rss.R` is clean **mid-test**.

**NEGATIVE CONTROLS -- both PERMANENT AND IN-SUITE (differential, no revert needed).**
* **NC-J1.** Extract the receipt from `git show 6b427bc:src/snakemake/rules/finemap.smk` by
  the same extractor and run it on fixtures (1) and (2): assert its output contains
  `ld_matrix None` and `ld_file_declared None`, and that it is **identical** on fixture (3) --
  i.e. the pre-change receipt genuinely **cannot** tell an early exit from a regression. The
  new receipt on the same three fixtures must produce **three** distinguishable outputs. This
  is finding J reproduced, permanently, rather than argued.
* **NC-J2.** Assert `_receipt_program(new) != _receipt_program(old)` with a named message, so
  a no-op edit cannot pass NC-J1 by accident.

**STEP 4 -- register K-1, the PREPARED DEFERRAL.** Append to `deferred-items.md`, under the
same `quick-260806-b77` heading Task 1 created:

> **K-1 DEFERRED -- `variant_catalog_fallback` is a MUTATED pre-existing key, and restoring
> its meaning needs TWO words from Carter.**

The entry must carry all four of the elements `<the_freeze_constraint>` requires:

1. **The exact minimal diff**, written out as a fenced diff against `dc4bbd2`: delete the
   single line `    variant_catalog_fallback <- TRUE` from the Path-2 branch at
   `run_susie_rss.R:1013`, keeping `ld_overlap_zero_fallback <- TRUE` at `:1014`, the
   `subset <- copy(subset_base)` revert and the `attempt <- attempt + 1` retry untouched.
   State that `:787` and `:1208` do **not** move.
2. **The blast radius of applying it.** Path-2 stops setting the legacy key, so the key
   recovers its legacy meaning ("the AFR variant-catalog empty-subset revert fired") and a
   before/after diff of the 1,957 legacy JSONs no longer shows a `false -> true` flip with no
   numeric cause. **Science unchanged** -- the branch still reverts to `subset_base` and still
   retries once; only the reporting flag moves. `ld_overlap_zero_fallback` remains the Path-2
   discriminator, so nothing becomes invisible.
3. **What re-freezing means afterward.** The unfreeze is single-use: re-pin
   `run_susie_rss.R` at the new SHA and the forward gate becomes
   `git diff --exit-code <new-sha> -- src/legacy/region_analysis/scripts/run_susie_rss.R`.
   Note that the still-outstanding `ld_allele_join.R` extraction follow-up recorded in
   `260805-w7u-SUMMARY.md` should be considered for the **same** unfreeze window rather than
   spending a second one.
4. **The two authorizations, named.** (a) an unfreeze of `run_susie_rss.R`; (b) an
   `AUTH`-style authorization to edit the pre-existing
   `tests/m3/test_ld_read_path.py::test_path2_ld_overlap_zero_fallback_is_observable_and_read`,
   whose `:451-453` parity assertion **mandates** the very line being deleted -- and a
   requirement that the edit **strengthens** rather than weakens it (assert
   `ld_overlap_zero_fallback <- TRUE` **and** assert `variant_catalog_fallback <- TRUE` is
   **absent** from the branch, so the new semantics are pinned in both directions), mirroring
   the two-part shape of `AUTH-o7o-01`.

Close with the neutral A/B options table (A = leave it, documented via the receipt decoder
ring landed in STEP 2; B = restore the legacy semantics) and the sentence: **this needs one
decision from Carter and is otherwise ready to execute.** Not blocking.
  </action>

  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_finemap_receipt_early_exit.py tests/m3/test_ld_read_path.py tests/m3/test_ld_allele_aware_wiring.py tests/m3/test_ld_read_path_ancestry_gate.py tests/m3/test_ld_declared_authoritative.py tests/m3/test_finemap_summary_panel_visible.py -q -rs</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/snakemake --snakefile Snakefile --configfile config/pipeline.yaml --list > /dev/null && echo LIST_OK</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && git diff --exit-code dc4bbd2 -- src/legacy/region_analysis/scripts/run_susie_rss.R && echo FREEZE_OK && git diff 6b427bc HEAD -- src/snakemake/rules/finemap.smk | grep -c "region_id" </automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && grep -c -- "--ld-allele-aware {params.ld_allele_aware}" src/snakemake/rules/finemap.smk && grep -c "{PYTHON_BIN} -c" src/snakemake/rules/finemap.smk</automated>
  </verify>

  <done>
  The receipt renders `early_exit:*` / `ALARM_LD_FIELDS_MISSING` / `ld_fields_present` and the
  early-exit and alarm tokens are proven **unequal**; NC-J1 shows the pre-change receipt
  cannot distinguish the three fixtures and is permanent in-suite; every pre-existing
  `finemap.smk`-shell assertion stays green; the third verify command prints `0` for
  `region_id` in the diff; the fourth prints `1` and `1`; the freeze gate is clean;
  `run_susie_rss.R` is 0-diff; **K-1 is registered with all four required elements and K is
  reported as DEFERRED, never as closed.** Commit:
  `feat(260806-b77-T2): make the per-region receipt distinguish an early exit from a regression (FINDING J); register K-1`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: L + M -- drift-detect the committed crosswalk, refuse partial rows, then gate both suites</name>
  <files>src/python/build_curated_m2_crosswalk.py, src/snakemake/rules/finemap.smk, tests/m3/test_curated_m2_crosswalk_drift.py</files>

  <behavior>
  - `load_curated_to_m2` admits **only** rows whose `status == "contained"`. A synthetic
    `status="partial"` row carrying a real `m2_region_id` is **excluded**; the `6b427bc`
    loader **includes** it (differential, observed).
  - On the SHIPPED `config/curated_to_m2_region_map.tsv` the returned dict is **exactly equal**
    to `6b427bc`'s -- 11 entries, non-empty (non-vacuity guard). Nothing moves today.
  - Any row skipped for a reason **other than** `unmapped` emits a named stderr warning
    listing the region and its status.
  - `crosswalk_missing_region_safes(regions_curated_csv, tsv)` returns the curated slugs with
    **no row at all** in the crosswalk. On the shipped pair it is empty; drop a row from a temp
    copy and that slug is returned.
  - The committed TSV is **byte-identical** to a fresh rebuild from the real inputs.
  - `finemap.smk` warns loudly and by name when the crosswalk exists but is incomplete -- not
    only when it is entirely empty.
  </behavior>

  <action>
**STEP 1 -- M: the allow-list.** In `src/python/build_curated_m2_crosswalk.py`, replace the
deny-list at `:485` (`if row.get("status") == "unmapped": continue`) with a **contained-only
allow-list**, and make every non-`unmapped` rejection audible:

```python
_LOADABLE_STATUSES = ("contained",)
```

For each row: keep it only when `row.get("status") in _LOADABLE_STATUSES`; `continue`
silently on `"unmapped"` (today's behaviour, and the byte-identity that `260805-23d` relies
on); for **any other** status collect `(region_safe, status)` and, after the loop, emit ONE
`print(..., file=sys.stderr)` naming them. Update the `load_curated_to_m2` docstring
(`:469-478`) to state that a `partial` row is **NOT** promoted -- quoting the builder's own
promise at `:48` -- and that a skipped row falls through to `REGION_SAFE_TO_ID` exactly as an
`unmapped` one does, which is the **fail-safe direction for this caller**: the DAG keeps
today's legacy path instead of silently fine-mapping on a panel that covers only part of the
locus. Cross-reference `:257`, where `"partial"` is genuinely emitted.

**STEP 2 -- L: the drift reader.** Add to the same module, reusing its **existing** curated-CSV
reader and `_safe_slug` (do **not** re-implement either):

```python
def crosswalk_covered_region_safes(path=DEFAULT_OUT_TSV) -> set:
    """Every ``region_safe`` the crosswalk has a ROW for, mapped or not.

    Distinct from ``load_curated_to_m2``, which returns only the USABLE rows.
    An ``unmapped`` row is COVERAGE -- a decision was recorded. An ABSENT row is
    DRIFT: the artifact predates the region. Finding L: the WARN in finemap.smk
    fired only on a FULLY EMPTY dict, so a 13th curated region added without a
    rebuild was silently legacy-routed.
    """


def crosswalk_missing_region_safes(regions_curated_csv, path=DEFAULT_OUT_TSV) -> list:
    """Curated slugs with NO crosswalk row at all, sorted. Empty == in sync."""
```

Both return `set()` / the full curated list respectively when the TSV is absent, so a fresh
clone still builds a DAG (the `:475-477` contract).

**STEP 3 -- L: the WARN.** In `src/snakemake/rules/finemap.smk`, extend the module-scope block
at `:160-175`. **Module scope only -- do not touch the `shell:` block Task 2 edited.** Import
`crosswalk_missing_region_safes` alongside `load_curated_to_m2` at `:85`, then after the
existing `if not CURATED_TO_M2:` WARN add an `else:` branch that computes the missing slugs
against `config["paths"]["regions_curated"]` and, when non-empty, prints a WARN that **names
the slugs** and gives the rebuild command
(`python src/python/build_curated_m2_crosswalk.py`).

Derive the curated set from `config["paths"]["regions_curated"]`, **not** from
`REGION_SAFE_TO_ID`: that name is built in `Snakefile:45-62` and is currently referenced only
from inside deferred lambdas, so its availability at `finemap.smk` parse time is **not
proven**. Using the config path has no include-order dependency. **Keep it a WARN, not a
raise** -- a raise at DAG-parse time would change `--list` behaviour for every caller and
risks tripping pre-existing DAG-building tests; the suite test in STEP 4 is the actual gate.

**STEP 4 -- the test module `tests/m3/test_curated_m2_crosswalk_drift.py` (NEW).**
This module is the **first** in the repo to read the **committed**
`config/curated_to_m2_region_map.tsv`; every test in `tests/m3/test_curated_m2_crosswalk.py`
rebuilds into a tmpdir (`:181-203`), which is exactly why drift is currently undetectable.

  a. **Drift (L).** Rebuild into `tmp_path` with the real builder over the real inputs
     (`config/regions_curated.csv`, `config/ld_regions.tsv`,
     `data/external/liftover/hg38ToHg19.over.chain.gz`) and assert
     `Path("config/curated_to_m2_region_map.tsv").read_bytes() == rebuilt.read_bytes()`.
     **Non-vacuity guard:** the committed file must have more than one line and its header
     must equal the module's `EXPECTED_COLUMNS` -- a byte comparison of two empty files proves
     nothing. Do **not** `pytest.skip` if the chain file is missing: it is present on this
     node (verified during planning) and a skip here would reproduce the very failure mode
     `test_curated_m2_crosswalk.py:328` warns against. **If the chain is genuinely absent,
     STOP and surface.**
  b. **Coverage (L).** `crosswalk_missing_region_safes(real_csv, committed_tsv) == []`.
  c. **Partial rejection (M), DIFFERENTIAL.** Write a synthetic TSV with the real header plus
     one `contained` row and one `partial` row (real-looking `m2_region_id`,
     `overlap_frac=0.30`). Load `6b427bc`'s module via
     `git show 6b427bc:src/python/build_curated_m2_crosswalk.py` plus `compile()` (hard rule
     2). Assert the **old** loader returns both keys and the **new** one returns only the
     `contained` key. The old-loader half is the negative control and is **permanent
     in-suite**, so "partial is now excluded" can never become vacuous.
  d. **Nothing moves today (M).** `load_curated_to_m2(committed)` new **==** old, whole-dict
     equality, with a non-vacuity guard `len(...) == 11` and
     `d["FTO_16q12"] == "m2_region_00067"` and `"BMI_Xq24" not in d`.
  e. **The warning is real.** Capture stderr and assert the `partial` row's slug and status
     both appear.

**NEGATIVE CONTROLS -- all three OBSERVED RED, quoted raw.**
* **NC-L1.** Copy the committed TSV to `tmp_path`, flip one byte of `overlap_frac`, point the
  drift test at the copy: it must go RED with a byte-level diff. (Perturb the **copy**, never
  the committed file.)
* **NC-L2.** Copy the committed TSV minus one data row and assert
  `crosswalk_missing_region_safes` returns exactly that slug -- and that it returns `[]` for
  the unmodified committed file. This is the "13th curated region" scenario, reproduced.
* **NC-M1.** Revert `_LOADABLE_STATUSES` to the deny-list (byte length changes -- hard rule
  2): test (c) must go RED because the new loader now admits the `partial` row. Restore and
  re-run green.

**STEP 5 -- THE FULL-SUITE GATE (this task carries it for the whole plan).** One clean,
INDEPENDENT run of each suite, after all three tasks are committed:

* `tests/m3`: require `failed == 0`, `passed >= 745`, `skipped == 31`. Run with `-rs` and
  prove **zero** skips originate from the three new modules; every skip must be one of the
  pre-existing `hail` / `chain file not present` / `M2 union BED` / SKELETON / AoU-perimeter
  gates. Reconcile the `passed` delta arithmetically per new module (budget, not target:
  roughly +50).
* `tests/phase2`: require exactly `136 passed / 1 skipped / 0 failed`, the one skip being the
  `bedtools not available` skip.
* `snakemake --list` rc 0 on `config/pipeline.yaml` **and** on
  `config/pipeline_lsweep_L{15,20,30}_overlay.yaml`.
* Scope + freeze by diff: `git diff --name-only 6b427bc HEAD -- src config tests | sort` must
  equal this plan's `files_modified` (source subset) and **nothing else** -- **no pre-existing
  test file**. `git diff --exit-code dc4bbd2 -- run_susie_rss.R`. 0-diff on
  `plink_ld_to_npz.py`, `ld_npz_to_rds.R`, `condition_ld_matrix.py` and the four m3-07
  occlusion modules. `grep -inE 'condition_ld_matrix|nan_to_num'` over the diff must be empty
  (m3-06 stays HELD). `grep -inE "gsutil|gcloud|\bbq\b|dataproc|hailctl| wb "` over the diff
  must be empty.
* `git checkout -- tests/m3/sparse_parent_benchmark.tsv` and confirm it is **not** staged.
  </action>

  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_curated_m2_crosswalk_drift.py tests/m3/test_curated_m2_crosswalk.py tests/m3/test_ld_read_path_ancestry_gate.py -q -rs</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3 -q -rs 2>&1 | tail -40</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/phase2 -q -rs 2>&1 | tail -15</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && for c in config/pipeline.yaml config/pipeline_lsweep_L15_overlay.yaml config/pipeline_lsweep_L20_overlay.yaml config/pipeline_lsweep_L30_overlay.yaml; do /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/snakemake --snakefile Snakefile --configfile $c --list > /dev/null && echo "OK $c" || echo "FAIL $c"; done</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && git diff --exit-code dc4bbd2 -- src/legacy/region_analysis/scripts/run_susie_rss.R && echo FREEZE_OK; git diff --name-only 6b427bc HEAD -- src config tests | sort; git diff 6b427bc HEAD -- src config tests | grep -icE "condition_ld_matrix|nan_to_num|gsutil|gcloud|dataproc|hailctl"</automated>
  </verify>

  <done>
  `load_curated_to_m2` is a `contained`-only allow-list and a `partial` row is proven excluded
  against the `6b427bc` loader (differential, permanent in-suite); the committed crosswalk is
  proven byte-identical to a fresh rebuild with a non-vacuity guard; `finemap.smk` warns by
  NAME on an incomplete crosswalk; NC-L1 / NC-L2 / NC-M1 were observed RED; the whole-dict
  identity for today's shipped file holds with `len == 11`; **both suites are green at
  `tests/m3` >= 745 passed / 31 skipped / 0 failed and `tests/phase2` 136 / 1 / 0, with zero
  new skips**; `--list` is rc 0 on all four configs; the scope + freeze diff shows exactly
  this plan's `files_modified` and no pre-existing test file; `sparse_parent_benchmark.tsv` is
  restored and unstaged. Commit:
  `feat(260806-b77-T3): drift-detect the committed crosswalk and refuse partial rows (FINDINGS L, M)`
  </done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| `config/pipeline.yaml` -> `resolve_ld_path` | a chain entry names an artifact nothing produces; the resolver silently walks past it |
| committed `config/curated_to_m2_region_map.tsv` -> `finemap.smk` module scope | a hand-run, DAG-absent artifact is trusted at DAG-build time with no comparison to its own inputs |
| `run_susie_rss.R` JSON -> the per-region receipt | an absent key and a regressed key render identically |
| the new test modules -> the frozen R source | a test that READS a frozen file must never write it |

## STRIDE threat register

| ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T-b77-01 | Spoofing | `strict_aou_only` guard | mitigate | token-based `is_aou_source`; verdict table asserted as an EQUALITY over the real shipped chains, so a widened predicate fails (T1a) |
| T-b77-02 | Denial of service | `strict_aou_only` guard | mitigate | NC-G2 proves the guard does not newly fire on a legitimate non-AoU entry -- the P3 / `quick-260715-vxz` false-trip direction |
| T-b77-03 | Tampering | `config/pipeline.yaml` | mitigate | T1's edit is comment-only; a verify command asserts the PARSED config is equal to `6b427bc`'s |
| T-b77-04 | Repudiation | per-region receipt | mitigate | early-exit and regression render provably UNEQUAL tokens; NC-J1 reproduces the pre-change ambiguity permanently in-suite |
| T-b77-05 | Information disclosure | receipt / `deferred-items.md` | accept | receipts carry file paths and counts only; no PII, no credentials; artifacts stay on the NC State node |
| T-b77-06 | Tampering | committed crosswalk TSV | mitigate | byte-identity against a fresh rebuild, plus a coverage check, with NC-L1 / NC-L2 observed RED |
| T-b77-07 | Elevation of privilege | `load_curated_to_m2` | mitigate | `contained`-only allow-list; a `partial` row can no longer be handed to `resolve_ld_path` as if contained; proven differentially against `6b427bc` |
| T-b77-08 | Tampering | frozen `run_susie_rss.R` | mitigate | every task's verify runs `git diff --exit-code dc4bbd2`; T2's premise pin asserts freeze cleanliness MID-TEST; K is deferred rather than edited |
| T-b77-09 | Tampering | pre-existing test files | mitigate | scope diff in T3 STEP 5 must list no pre-existing test; `AUTH-o7o-01` explicitly not inherited; any red pre-existing test is a STOP |
| T-b77-10 | Spoofing | new assertions | mitigate | every assertion carries a negative control; three are PERMANENT in-suite (NC-G3, NC-J1, T3c's old-loader half) so they cannot decay into claims |
| T-b77-11 | Denial of service | the `.pyc` cache | mitigate | differential loads use `compile()` on source read at call time; every revert control changes the byte length |
| T-b77-12 | Elevation of privilege | AoU perimeter / billing | mitigate | `$0`, NC State only; a grep for `gsutil|gcloud|bq|dataproc|hailctl|wb` over the whole diff must be empty; the ~11-day fire is Carter's terminal gate |
</threat_model>

<verification>
**Every number below must be MEASURED and quoted raw in the SUMMARY, one run each.**

1. `tests/m3`: `failed == 0`, `passed >= 745`, `skipped == 31`. `-rs` output must show zero
   skips originating from the three new modules, with each remaining skip attributed to a
   pre-existing gate.
2. `tests/phase2`: exactly `136 passed / 1 skipped / 0 failed`; the skip is `bedtools not
   available`.
3. `snakemake --list` rc 0 on `config/pipeline.yaml` and the three `lsweep` overlays.
4. `git diff --exit-code dc4bbd2 -- src/legacy/region_analysis/scripts/run_susie_rss.R`.
5. `git diff --name-only 6b427bc HEAD -- src config tests | sort` equals exactly the source /
   test subset of `files_modified`; **no pre-existing test file** appears.
6. Parsed-YAML equality of `config/pipeline.yaml` against `6b427bc` (comment-only edit).
7. `grep -inE 'condition_ld_matrix|nan_to_num'` over the diff is empty (m3-06 HELD).
8. `grep -inE "gsutil|gcloud|\bbq\b|dataproc|hailctl| wb "` over the diff is empty.
9. `tests/m3/sparse_parent_benchmark.tsv` restored via `git checkout --` and not committed.
10. Every negative control listed in the three tasks was **OBSERVED RED**, quoted raw, and
    the code restored to green afterwards.

**DELIBERATE NON-CRITERIA -- stated as limits, not sold as coverage.**
* `snakemake --dry-run` is **NOT** an acceptance criterion. `data/processed/ld_reference/`
  does not exist, so `resolve_ld_path` raises `FileNotFoundError` for every AFR region
  independently of this plan (registered as `D-04b-03`). `--list` is the achievable
  substitute and **does not evaluate input functions**, so it cannot observe the resolver
  either way.
* No criterion asserts anything about a real `.npz` or `.rds` on disk. **0/276 banked.**
* G's closure is proven on **fixtures and source text**; the strict-mode behaviour it unlocks
  is INERT under the shipped `strict_aou_only: false`. Say so.
</verification>

<success_criteria>
1. **G CLOSED.** `is_aou_source` is token-based and exported; the strict guard uses it; the
   verdict table is an equality over the REAL shipped chains; strict mode raises for TRANS and
   does not newly raise for any legitimate entry; shipped-config resolution is proven equal to
   `6b427bc` for all 12 curated slugs x 3 ancestries with a working inverted control; the
   orphan registry fails the suite on an unregistered fourth AoU entry; the `EUR_aou` artifact
   path is proven to have no non-comment producer reference.
2. **F RE-DERIVED**, with quoted evidence, before the "Any TRANS fit" gate row is claimed.
3. **J CLOSED** entirely inside `finemap.smk` -- no frozen-file edit -- with the early-exit and
   regression tokens proven UNEQUAL and the pre-change ambiguity reproduced permanently.
4. **K DEFERRED, NOT CLOSED**, registered as `K-1` with the exact minimal diff, the blast
   radius, the re-freeze obligation, and **both** authorizations named. The gate row
   "Publishing the panel provenance" is reported **PARTIAL**.
5. **L CLOSED**: the committed crosswalk is compared against a fresh rebuild for the first
   time in this repo, and an incomplete crosswalk is named loudly at DAG-parse time.
6. **M CLOSED**: `partial` can no longer be promoted, proven differentially against the
   pre-change loader.
7. **Two gate rows cleared, one PARTIAL** -- reported exactly that way, in the SUMMARY's
   gate-binding table, against `m3-04c-BLAST-RADIUS.md:133-144`. **A partial closure reported
   as a full one is the exact failure class this arc keeps catching.**
8. Both suites green at the required numbers with **no new skip**; no pre-existing test
   edited; `run_susie_rss.R` 0-diff at `dc4bbd2`; frozen contracts 0-diff; m3-06 HELD;
   `$0`, zero perimeter contact, **the AoU fire NOT triggered**.
9. Anything discovered and not fixed is registered in `deferred-items.md` -- never absorbed
   silently.
</success_criteria>

<explicitly_out_of_scope>
* **E-2** (QTL-beta / panel-ALT orientation) -- Carter's decision; it MOVES Track A numbers.
* **E-3** (two stale schema comments) and **E-4** (`_ancestry_for_region` hardcoded `"EUR"`).
* **BLOCKER-D's MC4R / FTO / HLA large-region classes** -- needs a producer-side change to a
  frozen file plus a big-memory-node decision.
* **A / B / C / E / H / I** -- all closed and verified. **Do not re-open any of them.**
* The `ld_allele_join.R` extraction follow-up from `260805-w7u` -- referenced in K-1 only as a
  candidate for the same unfreeze window; **not executed here**.
* `.planning/STATE.md` and `.planning/HANDOFF.json` are **Carter's** (standing directive) --
  **do not write them.** `ROADMAP.md` is untouched: quick tasks are separate from phases.
</explicitly_out_of_scope>

<output>
On completion write
`.planning/quick/260806-b77-close-the-remaining-blast-radius-finding/260806-b77-SUMMARY.md`
using the GSD summary template, and include:

* the **gate-binding table** against `m3-04c-BLAST-RADIUS.md:133-144`, showing **two CLEARED
  and one PARTIAL** -- with the reason K is PARTIAL stated in the row itself;
* **every negative control, quoted RAW** as observed RED;
* a plain statement of what is **INERT today** versus what changes behaviour now;
* the two-suite numbers with an arithmetic per-module delta reconciliation;
* the freeze re-pin paragraph (`run_susie_rss.R` still `dc4bbd2`; the unfreeze still SPENT);
* the cost line: `$0`, NC State only, zero perimeter contact, AoU fire NOT triggered;
* a `Deviations from the plan` section -- **plan facts found wrong must be reported as
  deviations, not silently worked around**;
* a `Lessons` section.
</output>
