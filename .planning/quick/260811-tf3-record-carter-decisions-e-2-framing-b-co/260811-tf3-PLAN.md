---
phase: quick/260811-tf3
plan: 01
type: execute
wave: 1
depends_on: []
subsystem: decision-record
tags: [e2, sr4, decisions, handoff, disclosure, track-a, id-vs-ref-ld, m3, docs-only]
autonomous: true
requirements: [E2-OBL-3-DISCHARGE, E2-SELECTED-PAIR-EXTRACTION, SR4-OPEN-DISPOSITION, DECISION-SURFACE-SYNC]
baseline_rev: 0e7e309
branch: m3-W2-aou-deltas
files_modified:
  - .planning/DECISIONS.md
  - .planning/HANDOFF.json
  - .planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md
  - .planning/quick/260811-tf3-record-carter-decisions-e-2-framing-b-co/260811-tf3-check.sh
  - .planning/quick/260811-tf3-record-carter-decisions-e-2-framing-b-co/260811-tf3-SELECTED-PAIR-correction.md
user_setup: []

must_haves:
  truths:
    - "A reader of .planning/DECISIONS.md can see that E-2's framing question is ANSWERED — framing B (CORRECTION), selecting the matched pair ms-correction + osf-correction — that Carter DELEGATED the choice to the standing recommendation on 2026-08-11, and that obligation (3) is DISCHARGED while (1) manuscript placement and (2) OSF posting remain Carter's external actions with their discharge conditions restated."
    - "The same entry records the journal-policy question as a PRE-PLACEMENT VERIFICATION STEP owned by Carter (if the target journal reads 'correction' on an in-submission manuscript as a formal correction notice, keep B's CONTENT with A's PLACEMENT), not as an unresolved question and not as a reason to reopen the framing."
    - "The same entry states in terms that framing B is NOT disposition option B: the code is still NOT changed, DEC-2026-08-07-e2-orientation-disposition's option-A disposition stands, and only the framing axis moved."
    - "A reader of .planning/DECISIONS.md can see SR4-OPEN answered NEVER ACTUALLY FROZEN, with the evidence cited by path and by commit (260811-pmv dossier; f78bbc1, 399c50f, 2b13dce), BOTH contrary-evidence caveats restated rather than smoothed (the 2bda675 pre-drift COLLECTIVE label for F1-F4 was a status report not a prohibition; F5 was never in the roster and its only label postdates its drift), and the explicit statement that NO NEW PIN is created by the decision."
    - "HANDOFF.json's carter_decisions_outstanding no longer presents SR4-OPEN as an open '▶' question and no longer presents E-2 obligation (3) as open; the file still parses as JSON, the E-2 entry's original text survives as a byte-exact PREFIX of the new text, and every other value in the document is structurally identical to its baseline."
    - "deferred-items.md's E-2 and SR4-OPEN entries carry the 2026-08-11 disposition ADJACENT to the superseded claims they correct, with ZERO deleted or modified lines anywhere in the file."
    - "A paste-ready SELECTED-PAIR file exists carrying the ms-correction paragraph and the osf-correction body BYTE-IDENTICAL to their oku source blocks (proven by cmp against a machine extraction, not by inspection), each labeled with its destination and its discharge condition, plus the pre-placement journal-policy check and the ⛔ no-agent-posts rule."
    - "Every mechanical gate in this plan was OBSERVED RED on a fixture COPY before being trusted green: the DECISIONS.md append-only check, the HANDOFF.json parse gate, the HANDOFF.json out-of-scope containment check, the deferred-items zero-deletion check, and the SELECTED-PAIR byte-identity check."
    - "Nothing was posted to OSF, no manuscript file was touched, and no posted OSF amendment body was edited or re-posted. Obligations (1) and (2) discharge only by Carter's external actions and every artifact written here says so on its face."
    - "The change set is EXACTLY four paths: .planning/DECISIONS.md (append-only), .planning/HANDOFF.json (two array entries), .planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md (insertions only), and new files under this quick directory. No src/, no tests/, no config/, no results/, no .planning/amendments/, no STATE.md, no ROADMAP.md, and nothing under the oku or pmv quick directories."
  artifacts:
    - path: ".planning/quick/260811-tf3-record-carter-decisions-e-2-framing-b-co/260811-tf3-check.sh"
      provides: "the acceptance harness for all three edits — clause groups dec|handoff|pair, each with negative controls that must be OBSERVED red on fixture copies"
      contains: "--self-test"
      min_lines: 200
    - path: ".planning/quick/260811-tf3-record-carter-decisions-e-2-framing-b-co/260811-tf3-SELECTED-PAIR-correction.md"
      provides: "the paste-ready selected pair — ms-correction + osf-correction extracted verbatim, with destinations, discharge conditions and the pre-placement journal-policy check"
      contains: "PASTE-BEGIN: osf-correction"
      min_lines: 80
    - path: ".planning/DECISIONS.md"
      provides: "DEC-2026-08-11-e2-framing-correction and DEC-2026-08-11-sr4-disposition, appended without touching a single existing byte"
      contains: "DEC-2026-08-11-sr4-disposition"
    - path: ".planning/HANDOFF.json"
      provides: "carter_decisions_outstanding[0] (E-2 obligation list updated) and [2] (SR4-OPEN flipped ▶ -> ✅), everything else preserved"
      contains: "SR4-OPEN — DECIDED 2026-08-11"
    - path: ".planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md"
      provides: "the E-2 framing-decided record and the SR4-OPEN disposition banner, both as pure insertions adjacent to what they supersede"
      contains: "E-2 FRAMING DECIDED"
  key_links:
    - from: ".planning/quick/260811-tf3-record-carter-decisions-e-2-framing-b-co/260811-tf3-SELECTED-PAIR-correction.md"
      to: ".planning/quick/260811-oku-discharge-e-2-disclosure-obligations-dra/260811-oku-e2-manuscript-limitation-drafts.md and 260811-oku-e2-osf-entry-drafts.md"
      via: "machine extraction of the ms-correction / osf-correction PASTE blocks, proven byte-identical by cmp — which is how the extraction INHERITS the oku harness's clause evidence"
      pattern: "PASTE-BEGIN: (ms|osf)-correction"
    - from: ".planning/DECISIONS.md DEC-2026-08-11-e2-framing-correction"
      to: ".planning/DECISIONS.md DEC-2026-08-07-e2-orientation-disposition obligation (3)"
      via: "named discharge of obligation (3); obligations (1) and (2) restated as still open with their conditions"
      pattern: "DEC-2026-08-07-e2-orientation-disposition"
    - from: ".planning/DECISIONS.md DEC-2026-08-11-sr4-disposition"
      to: ".planning/quick/260811-pmv-sr4-open-evidence-dossier-were-the-five-/260811-pmv-DOSSIER.md"
      via: "evidence citation by path and by commit (f78bbc1, 399c50f, 2b13dce)"
      pattern: "260811-pmv"
    - from: ".planning/HANDOFF.json carter_decisions_outstanding"
      to: "both new DEC ids"
      via: "entry [0] appended with the framing-B update, entry [2] replaced with the ✅ SR4-OPEN disposition"
      pattern: "DEC-2026-08-11-(e2-framing-correction|sr4-disposition)"
---

<objective>
Record two decisions Carter made in-session on **2026-08-11 (~21:09 EDT)**:

> *"Based on your recommendation, choose the pair for E-2, and for SR4-OPEN,
> correct the handoff language"*

**(A) E-2 FRAMING = B (CORRECTION).** Carter delegated the choice to the standing
recommendation in the `260811-oku` decision surface, selecting the matched pair
`ms-correction` + `osf-correction`. This **discharges obligation (3)** of
`DEC-2026-08-07-e2-orientation-disposition`. Obligations **(1) manuscript
placement** and **(2) OSF posting** remain Carter's external actions.

**(B) SR4-OPEN = NEVER ACTUALLY FROZEN.** The handoff's "frozen/pinned at
`bf16289`" language for the five files was **wrong**; correct it. Evidence basis
is the `260811-pmv` dossier: all 8 drift commits traceable, zero `DECISIONS.md`
declarations, `bf16289` touched none of the eight — with the two contrary-evidence
caveats (the F1–F4 pre-drift COLLECTIVE narrative label; F5 never in the roster)
preserved rather than smoothed.

Purpose: both decisions currently exist **only in a chat turn**. Until they are in
`DECISIONS.md`, `HANDOFF.json` and `deferred-items.md`, the next session resumes
into three surfaces that all say the questions are still open — and one of them
(`deferred-items.md`) is the very file the handoff points readers to. A decision
that lives nowhere the resume path reads is not a decision; it is a memory.

Output: two appended `DECISIONS.md` entries, two targeted `HANDOFF.json` array
edits, two insert-only `deferred-items.md` records, and one paste-ready
SELECTED-PAIR file whose two bodies are proven byte-identical to the oku sources.

⛔ **This plan discharges obligation (3) ONLY.** It posts nothing to OSF, edits no
manuscript file, and does not touch the body of any posted OSF amendment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/DECISIONS.md
@.planning/HANDOFF.json
@.planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md
@.planning/quick/260811-oku-discharge-e-2-disclosure-obligations-dra/260811-oku-e2-framing-decision-surface.md
@.planning/quick/260811-oku-discharge-e-2-disclosure-obligations-dra/260811-oku-e2-manuscript-limitation-drafts.md
@.planning/quick/260811-oku-discharge-e-2-disclosure-obligations-dra/260811-oku-e2-osf-entry-drafts.md
@.planning/quick/260811-oku-discharge-e-2-disclosure-obligations-dra/260811-oku-check-drafts.sh
@.planning/quick/260811-pmv-sr4-open-evidence-dossier-were-the-five-/260811-pmv-DOSSIER.md

**Targeted reads (do not read these files end to end):**
- `DECISIONS.md` from line **1039** (`DEC-2026-08-06-sr4-freeze-scope`) and line
  **1173** (`DEC-2026-08-07-e2-orientation-disposition`) — these two are the
  entry-format precedent AND the two entries the new ones cross-reference.
- `deferred-items.md` §`E-2` (line **139**, incl. the `▶ E-2 EVIDENCE UPDATE`
  block at **231** and its end at **324**) and §`SR4-OPEN` (line **989**).
- `HANDOFF.json` key `carter_decisions_outstanding` (line **115**): `[0]` = E-2,
  `[1]` = K-1, `[2]` = SR4-OPEN. **This index mapping is load-bearing** — the
  containment gate names `[0]` and `[2]` as the only permitted deltas.
- The oku decision surface §`Recommendation` (line **132**), its flip conditions
  (line **157**) and its discharge table (line **177**).
- The pmv dossier §5 (line **340**), the "one fact that cuts the other way"
  subsection (line **382**), §6 (line **413**) and §7 LIMITS (line **509**).
</context>

<interfaces>
<!-- Contracts the executor needs. Measured at planning time on this node,
     2026-08-11, at HEAD 0e7e309. Do not re-derive by exploration. -->

**Paste-block extractor** (identical to `260811-oku-check-drafts.sh::extract_block`;
the `sub()` calls strip leading/trailing whitespace from the MARKER match only,
so block bytes come through untouched):

```bash
extract_block() { # file block_id -> block body on stdout, marker lines removed
  awk -v id="$2" '
    { line = $0; sub(/^[ \t]+/, "", line); sub(/[ \t]+$/, "", line) }
    line == "<!-- PASTE-BEGIN: " id " -->" { inb = 1; next }
    line == "<!-- PASTE-END: "   id " -->" { inb = 0; next }
    inb { print }
  ' "$1"
}
```

**Measured block sizes at planning time** (use as a sanity floor, not as a pin):
`ms-correction` = 15 lines / 1,180 bytes; `osf-correction` = 34 lines / 6,322 bytes.

**oku harness status, measured at planning time:**
`./260811-oku-check-drafts.sh` (all three groups, no `--only`) exits **0**. That
is the evidence the extraction inherits — see `<inherited_evidence>`.

**HANDOFF.json containment walker** (verified at planning time: baseline vs
current with no edits yields `DIFFERING PATHS: (none)`, rc 0):

```python
import json, sys
old = json.load(open(sys.argv[1])); new = json.load(open(sys.argv[2]))
def walk(a, b, path=""):
    if type(a) is not type(b): yield path; return
    if isinstance(a, dict):
        for k in sorted(set(a) | set(b)):
            if k not in a or k not in b: yield f"{path}.{k}"
            else: yield from walk(a[k], b[k], f"{path}.{k}")
    elif isinstance(a, list):
        if len(a) != len(b): yield f"{path}[len {len(a)}->{len(b)}]"; return
        for i, (x, y) in enumerate(zip(a, b)): yield from walk(x, y, f"{path}[{i}]")
    elif a != b: yield path
diffs = sorted(walk(old, new))
allowed = {".carter_decisions_outstanding[0]", ".carter_decisions_outstanding[2]"}
extra = [d for d in diffs if d not in allowed]
print("DIFFERING PATHS:", diffs or "(none)")
print("OUT-OF-SCOPE PATHS:", extra or "(none)")
sys.exit(1 if extra else 0)
```

**Python interpreter:** `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python`
(`/usr/bin/python3` also parses JSON fine; use smoke_dev for consistency with the
project's other gates). Do **not** `conda activate` anything.

**Append-only mechanics, both verified at planning time on fixture copies:**
- byte prefix: `N=$(wc -c < baseline); head -c "$N" current | cmp - baseline`
  — passes on an unmodified file, **RED** on a copy with one line deleted.
- zero deleted lines (works for insert-in-the-middle edits too):
  `diff baseline current | grep -c '^<'` must be `0`.

**Both `.planning/DECISIONS.md` and `deferred-items.md` end with a trailing
newline** (measured), so an append starts cleanly on a new line. `DECISIONS.md`
line 1172 is blank and 1173 is the `## 2026-08-07 — DEC-...` heading: the house
separator between entries is **one blank line, no `---` rule** (the `---` rules
earlier in the file are older style; match the most recent precedent).
</interfaces>

<locked_facts>
<!-- The ONLY numbers, ids and quotations permitted. Re-derive nothing; retype
     nothing from memory. Every item below was read from the cited file. -->

**Carter's instruction, verbatim (2026-08-11, ~21:09 EDT, in-session):**
> "Based on your recommendation, choose the pair for E-2, and for SR4-OPEN,
> correct the handoff language"

**E-2 — the four recommendation grounds** (oku decision surface §Recommendation):
1. **Accuracy.** A limitation is something the data cannot do; this is something
   the pipeline **did wrongly** — matching on coordinates while ignoring alleles.
   Filing it under Limitations describes it inaccurately, and inaccuracy in the
   **reassuring** direction is the class of error this arc already committed
   twice (the 100× tile-3 misread; the `46/182` fixture quoted as a measurement).
2. **The magnitude claim is bounded either way.** Both bodies carry the same
   identity-LD-stub caveat and the same "population, not realised errors"
   sentence. B corrects the **convention** (provable in code, substrate-
   independent) while bounding the **magnitude** (stub-bound).
3. **B's extra obligation is one the E-4 bundle already carries** — the real-LD
   re-measurement, the before/after comparison and the further OSF update. B puts
   them on the public record instead of in a planning file.
4. **The asymmetry favours B.** Over-correcting costs a paragraph. Under-calling
   a measured 18–24% exposure that a real panel later confirms costs the record
   showing the softer word was chosen **after** measuring 18.41% and 23.80%.
   (Plus standing posture: rigor over speed in any gray-area trade-off.)

**E-2 — the flip condition that survives as a PRE-PLACEMENT CHECK** (oku surface,
"What would flip this recommendation to A", second bullet): if the target
journal's process makes a "correction" framing on a manuscript **in submission**
procedurally costly or ambiguous (e.g. it is read as a **post-publication
correction notice**), then **keep the correction CONTENT and use A's PLACEMENT**.
It is "the one input to this decision that is not in this repository", so it is
**Carter's check, before placement** — not an open question and not a reason to
reopen the framing.

**E-2 — the discharge table** (oku surface §"What discharges the obligations"):

| Obligation | Discharges when |
|---|---|
| (1) manuscript paragraph | the selected paragraph (`ms-correction`) is placed in the Track A manuscript |
| (2) OSF record entry | the selected body (`osf-correction`) is posted as a **new supplementary file** on `osf.io/az52u` **and** its URL + timestamp are recorded in `.planning/osf_deviations.md` |
| (3) LIMITATION vs CORRECTION | the framing is chosen and recorded in `.planning/DECISIONS.md` |

⚠ The oku OSF draft's **post-paste** checklist item 3 sequences the same
recording *after* posting and asks it to cite the entry's URL. **These do not
conflict and must not be reported as conflicting:** (3) discharges here, per the
discharge table (chosen + recorded); the URL cross-reference is added when **(2)**
discharges and does not un-discharge (3). Say so explicitly in the entry.

**E-2 — what becomes PUBLIC on posting.** The `osf-correction` body's closing
commitment (join made allele-aware; exposure re-measured on the real panel;
affected African-ancestry results **regenerated and re-reported** with a
before-and-after comparison; both posted as a further update **whether or not the
reported conclusions change**) is today an internal E-4 obligation. **Posting
converts it into a public commitment**, and it must then be registered as a
tracked obligation of the E-4 work (oku post-paste checklist item 6).

**E-2 — the axis guard.** *Framing B is NOT disposition option B.*
`DEC-2026-08-07-e2-orientation-disposition` disposed E-2 as **option A** — the
code is **still NOT changed**. This entry moves only the framing axis. The oku
surface flags this collision itself ("option B of the disposition, not to be
confused with framing B here"). An entry that lets the two blur would read as an
undeclared reversal of a Carter decision.

**E-2 — standing number rules** (unchanged, restate, do not re-derive):
`APOL1_22q12` **18.41%**, `FTO_16q12` **23.80%**, `SH2B3_12q24` **2.74%** with
md5-pinned anchor tiles **0.00%** and tile 3 **20.33%**, `CXADR_F2RL1_6p21`
**0.06%**, `MC4R_18q21` **0.07%**, pooled **5.29%**; corpus median **17.82%**,
max **38.68%**, **195 of 206** affected. ⛔ The pooled **5.29%** may never be
quoted alone — it is **dragged down** by the two clean large regions. ⛔ These are
the **catalog↔panel-frame transposition rate for variant bookkeeping** on an
**identity-LD stub** tree, never the real-LD exposure.

**SR4-OPEN — the four agreeing measurements** (pmv dossier §5):
1. `bf16289` is a `docs(handoff)` session-close commit that changed **one** file
   (`.continue-here.md`, +19/−3) and touched **none** of the eight. A pin that
   never touched what it pinned was a **bookmark**, not an act of freezing.
2. The freeze convention did not exist in the register until after all the drift:
   `DEC-2026-08-06-sr4-freeze-scope` is the only freeze entry;
   `grep -c '<basename>' .planning/DECISIONS.md` = **0** for all five; and
   `bf16289` matched **zero** files under `tests/ src/ config/ Snakefile` at
   **every one of the 8 drift commits**.
3. **All 8 distinct drift commits are traceable** — the task token resolves to a
   real artifact directory **and** the short SHA is named inside it (13 grep
   hits). No untraceable commit ⇒ nothing is forced to `DRIFT-NEEDS-REVIEW`.
4. The label was **COLLECTIVE** and was a **status report** ("All 7 pinned files
   0-line diff vs bf16289"), not a prohibition.

**SR4-OPEN — the two caveats, mandatory, stated not buried:**
- **F1–F4:** that collective label (`2bda675`, **2026-08-03**) **DID predate the
  drift** — `git merge-base --is-ancestor` confirms it precedes `3bb8783`,
  `bf963df`, `fac9a93`, `57b381f`. It is **genuine contrary evidence**. It
  carries limited weight because it is a **status report** in a **handoff
  narrative** rather than the register, and it was enforced by **measured zero**
  at each drift commit — but it existed, and the entry must not pretend otherwise.
- **F5 (`src/snakemake/schemas/pipeline.schema.yaml`) was NEVER in the "7 pinned
  files" roster at all** (that roster is 4 m3-07 modules + 3 frozen contracts),
  and its only individual label (`HANDOFF.json` `freeze_state` at `63453db`,
  2026-08-06) **POSTDATES all of its drift**. It was called frozen only after it
  stopped moving, so **there is nothing to review**.

**SR4-OPEN — the five files and their drift** (identical to the sr4-era figures):
`occlusion_manifest.py` +46/−8 · `occlusion_present_rate_scan.py` +154/−21 ·
`drop_occluded_from_sumstats.py` +97/−24 · `ld_npz_to_rds.R` +313/−62 ·
`pipeline.schema.yaml` +119/−0. The three genuinely 0-diff controls:
`plink_ld_to_npz.py`, `condition_ld_matrix.py`, `occlusion_span_filter.py`.

**SR4-OPEN — the 8 deduplicated drift commits:** `3bb8783`, `bf963df`, `fac9a93`,
`aeed8c0`, `57b381f`, `64f420a`, `2563451`, `d7dfa67` (tasks `260804-rtc`,
`260805-23d`, `260805-o7o`, `260805-w7u`, `m3-04b`).

**SR4-OPEN — the dossier's own commits:** `f78bbc1` (T1), `399c50f` (T2),
`2b13dce`. Path:
`.planning/quick/260811-pmv-sr4-open-evidence-dossier-were-the-five-/260811-pmv-DOSSIER.md`.

**SR4-OPEN — stated limits** (pmv §7, mandatory in the entry): `NEVER-FROZEN` is
a statement about **process and intent**, **not a code review** — nobody read the
`+46/−8` or the `+313/−62` for correctness. The narrative first-appearance dates
are **LOWER BOUNDS** (17 `HANDOFF.json`, 29 `STATE.md`, 22 `.continue-here.md`
revisions unreadable; the unreadable set skews **old**). **Recovering an earlier
label would not move this disposition**, because nothing enforced it at any drift
commit and every drift commit is traceable to a reviewed SUMMARY.

**SR4-OPEN — the residual language sites, OUT OF THIS TASK'S WRITE SCOPE**
(pmv §6, live current-state assertions, to be corrected in a follow-up):
`.planning/STATE.md:15` and `.planning/ROADMAP.md:1077`, both of which still
assert `ld_npz_to_rds.R` is frozen/unchanged when it is **+313/−62**. ⚠ The dated
historical `>` blocks (`STATE.md:266,278,297,301,311,349,362`;
`.continue-here.md:57,113,205,217,233,249,252`) are **NOT correction sites** —
they correctly describe what was believed when written, and rewriting them would
destroy the record this disposition rests on.
</locked_facts>

<inherited_evidence>
The constraint offers a choice: re-run the oku harness clauses against the
extraction, **or** prove byte-identity and inherit the harness's evidence.
**This plan takes the byte-identity route, and states why.**

`260811-oku-check-drafts.sh` is **structurally unable** to run against the
SELECTED-PAIR file: `group_ms` and `group_osf` read hard-coded paths (`MS_FILE`,
`OSF_FILE`) and every clause requires **both** ids (`ms-limitation` **and**
`ms-correction`) to be present exactly once. The SELECTED-PAIR file deliberately
carries only the correction halves, so a direct run would fail on `MS-01`/`OSF-01`
for a reason that has nothing to do with the extraction's fidelity. Bending the
harness to accept a half-file would weaken a gate that is currently green.

**The inheritance chain, stated so a reader can check each link:**
1. `./260811-oku-check-drafts.sh` exits **0** over all three oku deliverables
   (re-run in Task 3 at execution time, not taken from this plan).
2. The SELECTED-PAIR bodies are produced by the **same extractor** the harness
   uses, from those same files, and `cmp` proves them **byte-identical**.
3. Therefore every clause the harness asserts about the `ms-correction` and
   `osf-correction` blocks holds of the extraction **by identity**, not by
   re-assertion.

⚠ What this does **not** inherit: the harness's *file-level* clauses (`MS-10`,
`MS-11`, `OSF-12`, `OSF-13` etc.) are assertions about the **oku draft files**,
not about the blocks. The SELECTED-PAIR file gets its own `SP-04`..`SP-08`
clauses for the parts it must carry itself.
</inherited_evidence>

<explicitly_out_of_scope>
- ⛔ **No OSF posting, of anything, by any agent.** Obligation (2) discharges only
  when **Carter** posts and records the URL + timestamp in
  `.planning/osf_deviations.md`.
- ⛔ **No manuscript file is created, opened for edit, or touched.** Obligation (1)
  discharges only at Carter's placement.
- ⛔ **No edit or re-post of the BODY of any posted OSF amendment** (`trsx5`,
  `tcujq`). OSF bodies are append-only.
- ⛔ **No writes outside the four allowed paths.** Specifically NOT: `src/`,
  `tests/`, `config/`, `results/`, `.planning/amendments/`, `.planning/ROADMAP.md`,
  `.planning/STATE.md` (the orchestrator owns STATE), and nothing at all inside
  `.planning/quick/260811-oku-.../` or `.planning/quick/260811-pmv-.../` — both are
  **read-only** here.
- ⛔ **No new source-freeze pin.** `PY_FROZEN_RELS` is not touched;
  `tests/m3/test_source_freeze_pins.py` is not touched. Adding any of the five is
  its own task with its own recorded decision and its own observed-red control.
- ⛔ **No `git add -A` / `git add .`** on this GPFS tree — explicit paths only.
- **DEFERRED, NOT DONE — and disclosed rather than silently absorbed:**
  1. `HANDOFF.json`'s `status`, `headline` and `wave` strings still describe
     SR4-OPEN as outstanding and the E-2 obligations as three-undischarged. They
     are **deliberately left byte-identical** because the constraint is "targeted
     edits to the two `carter_decisions_outstanding` entries; preserve every other
     byte", and the containment gate mechanically enforces exactly that. Flag this
     in the SUMMARY so the next session-close refresh picks it up.
  2. `.planning/STATE.md:15` and `.planning/ROADMAP.md:1077` still carry the false
     "frozen/unchanged" claim for `ld_npz_to_rds.R`. Out of write scope here;
     registered in the new SR4 decision entry as the residual follow-up.
</explicitly_out_of_scope>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the harness's `dec` group RED-first, then append the two DECISIONS.md entries</name>
  <files>
.planning/quick/260811-tf3-record-carter-decisions-e-2-framing-b-co/260811-tf3-check.sh,
.planning/DECISIONS.md
  </files>
  <behavior>
The gate is written and **proven able to fail** before the entries exist.
`[[feedback_green_assertion_needs_a_negative_control]]`.

**Harness contract** (shared by all three tasks; Tasks 2 and 3 EXTEND this file,
they do not rewrite it):

```
./260811-tf3-check.sh [--only dec|handoff|pair] [--self-test [dec|handoff|pair]]
```

- exit 0 = every clause in the selected group(s) passed; non-zero = at least one
  failed, or a required file is absent (absence is a LOUD FAILURE, never a skip).
- one line per clause: `PASS <id>  <desc>` / `FAIL <id>  <desc> -- <problems>`.
- `REPO="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)"`.
- `BASELINE_REV="0e7e309"` with the comment
  `#: DIFFERENTIAL SUBSTRATE — the pre-task HEAD. NEVER re-pin.`
  (the project's `#:` bucket convention, applied here by choice).
- **Baseline validity guard, run first, every invocation:** assert
  `git diff --numstat $BASELINE_REV <the three tracked files>` is EMPTY **for the
  files whose group has not yet been executed**, and otherwise materialize each
  baseline into a temp dir via `git show $BASELINE_REV:<path>`. If the baseline
  is stale on a file no task has touched yet, **FAIL LOUDLY and stop** — do not
  silently re-pin. (A stale baseline is the only way this harness can grade its
  own edit against itself.)
- **Group functions take FILE PATHS, never git refs**, so `--self-test` can hand
  them fixture pairs. Signature: `group_dec <baseline_file> <current_file>`.
- ⚠ Every clause is **line-scoped** (grep is line-oriented). Required multi-word
  phrases must sit on ONE line in `DECISIONS.md` — this bit the oku harness once.
  Where a required phrase risks wrapping, assert against the whole appended
  region with `tr '\n' ' '` folded into a single line for that clause only, and
  say so in the clause comment.

**`dec` clause group** — `<baseline_file>` = `DECISIONS.md` at `BASELINE_REV`,
`<current_file>` = the working-tree file. The **appended region** is
`tail -c +$(( $(wc -c < baseline) + 1 )) current`.

- **DEC-01** append-only, byte prefix: `head -c $(wc -c < baseline) current`
  is `cmp`-identical to `baseline`.
- **DEC-02** zero deleted lines: `diff baseline current | grep -c '^<'` == 0.
- **DEC-03** exactly one occurrence each (`grep -cF`) of
  `## 2026-08-11 — DEC-2026-08-11-e2-framing-correction` and
  `## 2026-08-11 — DEC-2026-08-11-sr4-disposition`, both at line start.
- **DEC-04** the E-2 entry carries its identifying content: appended region
  matches all of `framing B`, `CORRECTION`, `ms-correction`, `osf-correction`,
  `Based on your recommendation`, `obligation \(3\)`, `DISCHARGED`, `az52u`,
  `osf_deviations\.md`, `DEC-2026-08-07-e2-orientation-disposition`.
- **DEC-05** the journal-policy PRE-PLACEMENT step: matches `journal` AND
  `polic` AND (`pre-placement`|`before placement`) AND `placement`.
- **DEC-06** the axis guard: matches `option A` AND `framing B` AND, case-
  insensitively, `code is (still )?not changed`.
- **DEC-07** the numbers guard: matches `identity` AND `bookkeeping`; and if
  `5\.29` appears then `dragged`, `18\.41` and `23\.80` must all appear.
- **DEC-08** the SR4 entry carries its evidence: matches all of
  `NEVER ACTUALLY FROZEN`, `260811-pmv`, `f78bbc1`, `399c50f`, `2b13dce`,
  `bf16289`, `2bda675`, `status report`, `pipeline\.schema\.yaml`,
  `source_freeze\.py`, `negative control`, and case-insensitive `no new pin`.
- **DEC-09** framing guard: **zero** case-insensitive standalone occurrences of
  `revision`, `revisions`, `salvage`, `cleanup` in the appended region
  (`grep -ciE '\b(revisions?|salvage|cleanup)\b'` == 0). Project-standing
  original-research framing.

**`--self-test dec` negative controls** — fixtures are COPIES in
`mktemp -d "${TMPDIR:-/tmp}/tf3-selftest.XXXXXX"`, **never the real files**:
- **NC-0 (positive control)** the real baseline + real current pair is GREEN.
- **NC-D1** current fixture with one mid-file line **deleted** ⇒ `DEC-01` and
  `DEC-02` RED. (Verified feasible at planning time: `sed '500d'` on a copy makes
  the byte-prefix check go red.)
- **NC-D2** current fixture with the `DEC-2026-08-11-sr4-disposition` heading
  line removed ⇒ `DEC-03` RED.
- **NC-D3** current fixture with the journal-policy sentence removed ⇒ `DEC-05`
  RED, and RED **alone** (assert exactly one FAIL line) — a control that fires
  three clauses proves nothing about the one it names.
- Each control prints its FAIL lines; `(no FAIL lines -- CONTROL DEFEATED)` if
  it passed, and the self-test then exits non-zero.
  </behavior>
  <action>
**Step 1 — write the harness with the `dec` group only, and observe it RED.**
Run `./260811-tf3-check.sh --only dec` **before** touching `DECISIONS.md`:
`DEC-03`..`DEC-08` MUST fail (the entries do not exist). Record the FAIL ids in
the SUMMARY as the birth negative control. `DEC-01`/`DEC-02`/`DEC-09` pass
trivially at this point, which is correct and must be reported as such rather
than counted as evidence.

**Step 2 — append the two entries to `.planning/DECISIONS.md`.** APPEND ONLY:
one blank line, then the entry, matching the `DEC-2026-08-07` precedent (`## ` heading,
no `---` rule). Do not touch, reflow, or re-indent one existing byte.

**Entry 1 — `## 2026-08-11 — DEC-2026-08-11-e2-framing-correction: E-2 is framed
as a CORRECTION (framing B); the matched pair ms-correction + osf-correction is
selected`**

- **Decision:** framing **B — CORRECTION**, selecting the complete matched pair
  `ms-correction` (manuscript paragraph) + `osf-correction` (OSF entry) from the
  `260811-oku` drafts. Carter's call, **2026-08-11**, **delegated to the standing
  recommendation** — quote the instruction verbatim from `<locked_facts>`. State
  that this resolves **obligation (3)** of `DEC-2026-08-07-e2-orientation-disposition`.
- **⚠ The axis guard, in its own short paragraph:** framing B is **NOT**
  disposition option B. The code is **still NOT changed**; the option-A
  disposition stands unmoved; only the framing axis moved here.
- **Why — the four grounds**, summarized from `<locked_facts>` (accuracy;
  magnitude bounded either way by the identity-stub caveat; B's extra obligation
  already carried by the E-4 bundle; the asymmetry favours over-correcting), each
  as a numbered bullet. Keep every required phrase on ONE line.
- **⚠ PRE-PLACEMENT VERIFICATION STEP — Carter's, before placement.** The target
  journal's policy is the one input to this decision that is not in this
  repository. If the journal reads "correction" on an **in-submission** manuscript
  as a formal **post-publication correction notice**, keep **B's CONTENT** with
  **A's PLACEMENT** (the Limitations section), per the oku flip condition. State
  plainly that this is a **placement** change only: it does not reopen the
  framing, does not change a number, and does not change a sentence of either
  selected body.
- **What becomes public on posting:** the `osf-correction` closing commitment
  (allele-aware join, real-panel re-measurement, affected African-ancestry results
  regenerated and re-reported with a before/after comparison, both posted as a
  further update whether or not conclusions change) is an internal E-4 obligation
  **today** and becomes a **public commitment** on posting; register it against
  E-4 at that point.
- **Discharge status table** — (3) ✅ DISCHARGED by this entry; (1) ⛔ OPEN,
  discharges at placement of `ms-correction` in the Track A (`id-vs-ref-LD`)
  manuscript, after the journal-policy check; (2) ⛔ OPEN, discharges when
  `osf-correction` is posted as a **NEW supplementary file** on `osf.io/az52u`
  (never a new version of `trsx5`/`tcujq`) **and** its URL + timestamp are
  recorded in `.planning/osf_deviations.md`. Add the one-sentence reconciliation
  of the oku post-paste checklist item 3 (the URL cross-reference is added when
  (2) discharges and does not un-discharge (3)).
- **How to apply:** the paste-ready extraction path (Task 3's file); ⛔ no agent
  posts to OSF, ⛔ no agent edits a manuscript, ⛔ OSF bodies are append-only;
  ⛔ never quote the pooled `5.29%` alone (it is **dragged down** by the two
  clean large regions — name `18.41%` and `23.80%` in the same breath);
  ⛔ never cite these as the real-LD exposure (identity-LD stub, variant
  **bookkeeping** only); the unselected `ms-limitation`/`osf-limitation` texts
  stay in the oku directory as the record of what was considered and are **not**
  to be posted; the oku harness was **green at selection time** and the tf3
  extraction is byte-identical to its blocks, so it inherits that evidence.
- **Alternatives considered:** (a) framing **A — LIMITATION**, rejected on
  grounds 1 and 4 — not a wrong description of the *magnitude*, a wrong
  description of the *defect*; (b) **defer the framing until a real panel
  exists**, rejected — obligations (1)/(2) cannot be written in no framing at
  all, Track A is in submission now, and the one future fact that would have
  argued for A (a near-0% real-panel measurement) is already covered as a further
  OSF update rather than a retraction; (c) **B's content with A's placement** —
  **NOT rejected**, held as the conditional outcome of the pre-placement check.
- **Cross-refs:** `DEC-2026-08-07-e2-orientation-disposition`; the three oku
  deliverable paths; `deferred-items.md` E-2 and E-4;
  `[[feedback_a_count_is_a_claim_scope_and_reconcile]]`,
  `[[feedback_rigor_over_speed]]`.

**Entry 2 — `## 2026-08-11 — DEC-2026-08-11-sr4-disposition: SR4-OPEN is answered
NEVER ACTUALLY FROZEN; the handoff language was wrong and is corrected`**

- **Decision:** the five files were **NEVER ACTUALLY FROZEN**. The handoff's
  "frozen/pinned at `bf16289`" language for them was **wrong** and is corrected,
  not defended. Carter's call, **2026-08-11** (quote the instruction). **No drift
  review is required.** Name the five files with their drift figures.
- **Evidence:** the pmv dossier by path, landed by `f78bbc1`, `399c50f`,
  `2b13dce`; then the four agreeing measurements from `<locked_facts>` as
  numbered bullets (including the "bookmark, not an act of freezing" reading of
  `bf16289`, and the measured-zero enforcement at every one of the 8 drift
  commits).
- **⚠ The two caveats, each in its own bullet, stated not buried:** the F1–F4
  pre-drift COLLECTIVE label at `2bda675` (2026-08-03) — genuine contrary
  evidence, weighted by the three bounds; and F5 never in the roster with a label
  that postdates its drift.
- **Stated limits:** not a code review of the drift; first-appearance dates are
  LOWER BOUNDS from the unreadable-object census; and the explicit sentence that
  recovering an earlier label **would not move this disposition**.
- **Consequences:** no drift review (name the 8 commits so the *not*-review set
  is explicit); the three genuinely-0-diff files **stay gated** by
  `tests/m3/source_freeze.py` at `PY_CODE_REF`, unchanged by this entry;
  **NO NEW PIN is created** — none of the five is added to `PY_FROZEN_RELS`, and
  `test_the_handoff_frozen_claim_is_recorded_as_partly_false` stays green
  asserting they are OUT; any future pin is **its own task** and costs a recorded
  decision, a `#:` bucket annotation, a **negative control OBSERVED red**, a
  change to the handoff-claim test which goes RED the moment a file is added,
  and — for F5 only — a YAML code-stripper `source_freeze.py` does not have.
  Add the honest note that a pin at `bf16289` is **not available** for these five
  (red at birth), so any pin would be the weaker "must not change from here"
  claim — legitimate to want, legitimate to decline.
- **How to apply:** the `HANDOFF.json` retraction and `STATE.md:39` already carry
  the correction — **do not repeat the retracted claim**; the residual live sites
  `.planning/STATE.md:15` and `.planning/ROADMAP.md:1077` are **out of this
  task's write scope** and are registered here as the follow-up; the dated
  historical `>` blocks are **NOT** correction sites.
- **Alternatives considered:** (a) **FROZEN-AND-DRIFTED** (review the 8 commits),
  rejected on the evidence — F5 would be its least defensible member;
  (b) **answer NEVER-FROZEN and pin the five at today's HEAD in the same breath**,
  rejected as a bundled decision — declining today forecloses nothing.
- **Cross-refs:** `DEC-2026-08-06-sr4-freeze-scope`; `deferred-items.md`
  SR4-OPEN; the pmv dossier;
  `[[feedback_a_claimed_invariant_needs_a_named_enforcer]]`,
  `[[feedback_green_assertion_needs_a_negative_control]]`,
  `[[feedback_fixed_sha_whole_file_pin_is_a_timebomb]]`.

**Step 3 — run `--only dec` (must be GREEN) and `--self-test dec` (every control
OBSERVED red on its named clause).** Commit both files with explicit paths.
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis/.planning/quick/260811-tf3-record-carter-decisions-e-2-framing-b-co && ./260811-tf3-check.sh --self-test dec && ./260811-tf3-check.sh --only dec && cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && git status --porcelain | grep -Ev '^\?\?' | grep -Ev '^ ?M ?\.planning/(DECISIONS\.md|quick/260811-tf3)' | wc -l | grep -qx 0</automated>
  </verify>
  <done>
`.planning/DECISIONS.md` carries both new entries; `git diff --numstat 0e7e309 --
.planning/DECISIONS.md` shows **0 deletions**; `--only dec` exits 0; every `dec`
negative control was OBSERVED red on its named clause (NC-D3 red **alone**); the
pre-append RED run is recorded in the SUMMARY with its FAIL ids; no tracked file
outside `DECISIONS.md` and this quick directory is modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync the two decision surfaces — HANDOFF.json (2 array entries) and deferred-items.md (insert-only)</name>
  <files>
.planning/quick/260811-tf3-record-carter-decisions-e-2-framing-b-co/260811-tf3-check.sh,
.planning/HANDOFF.json,
.planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md
  </files>
  <action>
**Step 1 — extend the harness with the `handoff` group, and observe it RED**
before editing either file. Clauses (`group_handoff <base_json> <cur_json>
<base_deferred> <cur_deferred>`):

- **HJ-01** `json.load(cur_json)` succeeds under
  `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python`. **This is the
  gate**; a `HANDOFF.json` that does not parse is a broken resume path.
- **HJ-02** containment: run the `<interfaces>` walker; **only**
  `.carter_decisions_outstanding[0]` and `[2]` may differ. Any other path ⇒ FAIL,
  printing the offending paths.
- **HJ-03** `[2]` starts with `✅ SR4-OPEN — DECIDED 2026-08-11` and contains
  `NEVER ACTUALLY FROZEN`, `DEC-2026-08-11-sr4-disposition`, `260811-pmv`,
  `2bda675`, `pipeline.schema.yaml`, `source_freeze.py`, and case-insensitive
  `no new pin`.
- **HJ-04** `[0]`: the **baseline string is a byte-exact PREFIX of the new
  string** (pure append — nothing overwritten), and the new tail contains
  `DEC-2026-08-11-e2-framing-correction`, `framing B`, `ms-correction`,
  `osf-correction`, `az52u`, `osf_deviations.md`, `(1)`, `(2)` and `(3)`.
- **HJ-05** `deferred-items.md` zero deleted lines:
  `diff base_deferred cur_deferred | grep -c '^<'` == 0. (Byte-prefix does **not**
  apply here — these are mid-file insertions by design.)
- **HJ-06** `deferred-items.md` contains `DEC-2026-08-11-e2-framing-correction`,
  `DEC-2026-08-11-sr4-disposition`, a heading matching `E-2 FRAMING DECIDED`, and
  a line matching `✅ DISPOSED 2026-08-11`.
- **HJ-07** E-2 **adjacency**: the line number of the first
  `DEC-2026-08-11-e2-framing-correction` occurrence is **less than** the line
  number of `**Logged:** 2026-08-06 (quick-260805-w7u)` — i.e. the update sits
  inside the E-2 banner, where a reader hits it before the superseded
  "THREE OBLIGATIONS SURVIVE" claim can be read alone.
- **HJ-08** SR4 **adjacency**: the `✅ DISPOSED 2026-08-11` line number is
  **greater than** the `## ⚠ SR4-OPEN` heading line and **less than** the
  `**Logged:** 2026-08-06 (`quick-260806-sr4`)` line.

`--self-test handoff` controls, all on fixture COPIES:
- **NC-H1** JSON fixture with the final `}` removed ⇒ **HJ-01** RED. (Verified
  feasible at planning time: yields `JSONDecodeError: Expecting ',' delimiter`.)
- **NC-H2** JSON fixture with an **out-of-scope** value mutated (e.g. the
  top-level `"phase"` string) ⇒ **HJ-02** RED, naming `.phase`.
- **NC-H3** JSON fixture where `[0]` is **rewritten** rather than appended (drop
  its first sentence) ⇒ **HJ-04** RED on the prefix check.
- **NC-H4** deferred fixture with one line **deleted** ⇒ **HJ-05** RED.
- **NC-H5** deferred fixture where the E-2 update is moved **after** the
  `**Logged:**` line ⇒ **HJ-07** RED (the adjacency clause is not decorative).

**Step 2 — edit `.planning/HANDOFF.json`.** Two targeted string edits, nothing
else. **Preserve every other byte**; do not reformat, re-indent, or re-order keys.

- `carter_decisions_outstanding[2]` — **replace** the `▶ SR4-OPEN (a QUESTION,
  not a blocker) …` string with a `✅` entry style-matched to the existing
  `✅ E-2` / `✅ K-1` entries (same voice, same ⚠ callout convention). It must
  carry: `✅ SR4-OPEN — DECIDED 2026-08-11: NEVER ACTUALLY FROZEN
  (DEC-2026-08-11-sr4-disposition)`; the correction of the language claim; the
  evidence (dossier path + `f78bbc1`/`399c50f`/`2b13dce` + the four measurements
  in compressed form); **both caveats** (the `2bda675` pre-drift collective
  status report; F5 never in the roster, label postdates drift); and the
  consequences — no drift review, the 3 genuinely-0-diff files stay gated by
  `tests/m3/source_freeze.py`, **NO NEW PIN**, any future pin is its own task
  with a negative control observed red; plus the ⚠ residual `STATE.md:15` /
  `ROADMAP.md:1077` sites and the "historical `>` blocks are NOT correction
  sites" rule.
- `carter_decisions_outstanding[0]` — **APPEND** to the existing E-2 string
  (leave every existing character in place; **HJ-04** enforces this):
  `▶ UPDATE 2026-08-11 — OBLIGATION (3) IS DISCHARGED` … framing **B
  (CORRECTION)** chosen (`DEC-2026-08-11-e2-framing-correction`), selecting
  `ms-correction` + `osf-correction`, paste-ready at the tf3 SELECTED-PAIR path;
  **(1) and (2) REMAIN OPEN and are CARTER'S EXTERNAL ACTIONS** with their
  discharge conditions restated (placement in the Track A manuscript after the
  pre-placement journal-policy check — if the journal reads "correction" on an
  in-submission manuscript as a formal correction notice, keep B's CONTENT with
  A's PLACEMENT; posting as a NEW supplementary file on `osf.io/az52u` plus URL +
  timestamp in `.planning/osf_deviations.md`); `⚠ framing B is NOT disposition
  option B — the code is still NOT changed`; and `⚠ posting makes the E-4-bundled
  re-report commitment PUBLIC`.

⚠ **JSON string hygiene:** these values are JSON strings on one physical line.
Use `\"` for any interior double quote, keep the em dashes and the ✅/⚠/▶/⛔
glyphs as literal UTF-8 (the file already carries them), and introduce **no raw
newline** inside a value. `HJ-01` is the mechanical gate on this.

**Step 3 — edit `deferred-items.md`. INSERTIONS ONLY — zero lines deleted or
modified anywhere in the file.** Two insertion points:

1. **E-2 banner update** — insert a short blockquote continuation immediately
   after the line ending `…may read it as the latter.` (the end of the
   "THREE OBLIGATIONS SURVIVE" paragraph, ~line 161), *before* the
   `> **B becomes right** …` paragraph:
   `> **▶ UPDATE 2026-08-11 — OBLIGATION (3) IS DISCHARGED.** Framing **B
   (CORRECTION)** chosen …` citing `DEC-2026-08-11-e2-framing-correction`,
   naming the selected pair, and stating that (1) and (2) remain open. Point to
   the new section below.
2. **`### ▶ E-2 FRAMING DECIDED (2026-08-11) — B (CORRECTION)`** — a full section
   inserted immediately **before** `## E-3 (minor)` (~line 326), i.e. at the end
   of the E-2 material and after the dated `▶ E-2 EVIDENCE UPDATE (2026-08-07)`
   block, so the file reads chronologically. Content: the decision + delegation
   quote; the selected pair and where each half goes; the four grounds in one
   compressed paragraph; the pre-placement journal-policy check; the axis guard
   (framing B ≠ disposition option B); the discharge table (3 ✅ / 1 ⛔ / 2 ⛔);
   the paste-ready path; and the public-commitment-on-posting note with its E-4
   registration.
3. **SR4-OPEN banner** — insert a `> **✅ DISPOSED 2026-08-11 — NEVER ACTUALLY
   FROZEN.**` blockquote immediately **after** the `## ⚠ SR4-OPEN …` heading
   (~line 989) and **before** the `**Logged:** 2026-08-06 (`quick-260806-sr4`)`
   line, mirroring the E-2 `✅ DISPOSED` banner shape. It must state explicitly
   that **the `Status: OPEN` line immediately below is SUPERSEDED and is
   preserved verbatim for the record**, cite `DEC-2026-08-11-sr4-disposition` and
   the dossier path, restate **both caveats** in one line each, and state the
   consequence: no drift review, three files stay gated, **no new pin**.

⚠ This third insertion is a **deliberate, disclosed addition** beyond the
literal content list in the task brief. Justification: `HANDOFF.json` and
`DECISIONS.md` will both say SR4-OPEN is decided while `deferred-items.md` — the
file the handoff *points readers to* — would still say *"THE QUESTION FOR CARTER
— not answered here."* Creating that false surface is worse than the scope
addition. It is a **pure insertion** of one blockquote; if the orchestrator
objects it is a single-hunk revert that nothing else depends on. Note it in the
SUMMARY as a deviation.

**Step 4 —** run `--only handoff` (GREEN) and `--self-test handoff` (every
control OBSERVED red on its named clause). Commit with explicit paths.
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis/.planning/quick/260811-tf3-record-carter-decisions-e-2-framing-b-co && ./260811-tf3-check.sh --self-test handoff && ./260811-tf3-check.sh --only dec && ./260811-tf3-check.sh --only handoff && cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -c "import json;d=json.load(open('.planning/HANDOFF.json'));a=d['carter_decisions_outstanding'];assert len(a)==3;assert a[2].startswith('✅ SR4-OPEN — DECIDED 2026-08-11');assert 'DEC-2026-08-11-e2-framing-correction' in a[0];print('HANDOFF OK')"</automated>
  </verify>
  <done>
`HANDOFF.json` parses; the containment walker reports **only**
`.carter_decisions_outstanding[0]` and `[2]` as differing; `[0]`'s baseline text
survives as a byte-exact prefix; `deferred-items.md` has **0 deleted lines**
(`git diff --numstat 0e7e309 -- <path>` deletion column = 0) and both adjacency
clauses pass; `--only dec` still GREEN (no regression from Task 1); every
`handoff` negative control was OBSERVED red, including the parse gate on a
deliberately broken fixture **copy** — never the real file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extract the SELECTED PAIR verbatim and prove byte-identity against the oku sources</name>
  <files>
.planning/quick/260811-tf3-record-carter-decisions-e-2-framing-b-co/260811-tf3-check.sh,
.planning/quick/260811-tf3-record-carter-decisions-e-2-framing-b-co/260811-tf3-SELECTED-PAIR-correction.md
  </files>
  <action>
**Step 1 — extend the harness with the `pair` group, and observe it RED** before
the file exists. `group_pair <selected_pair_file>` — the source blocks are always
re-extracted from the **read-only** oku drafts at run time:

- **SP-01** `<!-- PASTE-BEGIN: ms-correction -->` / `-END` and the same for
  `osf-correction` appear **exactly once each** (`grep -cF`).
- **SP-02** ★ **BYTE IDENTITY — the load-bearing clause.** For each id, extract
  the block from the SELECTED-PAIR file and from its oku source with the SAME
  extractor and `cmp` them. Any difference ⇒ FAIL, printing the first differing
  byte offset.
- **SP-03** the unselected halves are **absent**: `grep -cF
  '<!-- PASTE-BEGIN: ms-limitation -->'` == 0 and the same for `osf-limitation`.
  (Prose *mentioning* the ids is fine and expected — the clause is marker-scoped.)
- **SP-04** destination + discharge labeling: the file matches `az52u`,
  `osf_deviations\.md`, `DEC-2026-08-11-e2-framing-correction`, `Track A`,
  `new supplementary file`, `append-only`, `obligation \(1\)`,
  `obligation \(2\)`, `obligation \(3\)`, and case-insensitive
  `no agent posts` (the ⛔ rule).
- **SP-05** the pre-placement journal-policy check: matches `journal` AND
  `polic` AND (`pre-placement`|`before placement`) AND `placement`.
- **SP-06** framing guard: zero case-insensitive `\b(revisions?|salvage|cleanup)\b`.
- **SP-07** pooled-alone guard: if `5\.29` appears then `dragged` must appear.
- **SP-08** the file states on its face that it **discharges nothing by
  existing**: matches `DISCHARGED` and (`Carter` + `external`) on one line
  (fold to one line for this clause if needed).

`--self-test pair` controls, on fixture COPIES:
- **NC-P1** `18.41` → `1.841` inside the `ms-correction` block ⇒ **SP-02** RED.
- **NC-P2** the journal-policy sentence deleted ⇒ **SP-05** RED, and RED alone.
- **NC-P3** one `PASTE-END` marker removed ⇒ **SP-01** RED (and SP-02, since the
  extraction then runs past the block — report both, expect SP-01 named).

**Step 2 — build `260811-tf3-SELECTED-PAIR-correction.md` BY MACHINE
EXTRACTION, never by retyping.** Write the frame with sentinel lines, then splice:

```bash
sed -e '/^@@MS_CORRECTION_BLOCK@@$/r <tmp>/ms-correction.src' \
    -e '/^@@MS_CORRECTION_BLOCK@@$/d' \
    -e '/^@@OSF_CORRECTION_BLOCK@@$/r <tmp>/osf-correction.src' \
    -e '/^@@OSF_CORRECTION_BLOCK@@$/d' frame.md > 260811-tf3-SELECTED-PAIR-correction.md
```

The two `.src` files come from the extractor in `<interfaces>` run against the
oku drafts. **The blocks go in at column 0, unfenced, unindented, with their
`PASTE-BEGIN`/`PASTE-END` marker lines around them** — any wrapper (a code fence,
a blockquote, an indent) changes the bytes the reader would paste and breaks
SP-02 by design.

**File content, around the two spliced blocks:**

- **Header:** what this file is — the **SELECTED PAIR** for E-2 under
  `DEC-2026-08-11-e2-framing-correction` (framing **B — CORRECTION**), extracted
  verbatim from the `260811-oku` drafts. State that the bodies are **byte-
  identical to their sources, verified mechanically by `260811-tf3-check.sh
  --only pair`**, and that this inheritance — not a re-run of the oku harness — is
  what carries the oku clause evidence, with the one-paragraph reason from
  `<inherited_evidence>` (the oku harness requires both framings in one file and
  cannot be pointed at a half-file without weakening a green gate). Also record
  that `./260811-oku-check-drafts.sh` was re-run in this task and exited 0 —
  quote the observed exit code, do not copy it from this plan.
- **⚠ PRE-PLACEMENT CHECK, before anything else is done — Carter's.** The target
  journal's policy. If "correction" on an **in-submission** manuscript is read as
  a formal post-publication correction notice, **keep this content, use A's
  placement** (the Limitations section). Placement only; no number and no
  sentence changes.
- **§1 — the manuscript paragraph (`ms-correction`).** Destination: the Track A
  manuscript (`id-vs-ref-LD`), as a **Methods correction-and-disclosure note**
  with a pointer from Limitations (or, under the flip condition, in Limitations).
  Discharge condition for **obligation (1)**, and that it is **Carter's external
  action**. Then the spliced block.
- **§2 — the OSF entry body (`osf-correction`).** Destination: a **NEW
  supplementary file** on `osf.io/az52u` — **never** a new version of `trsx5` or
  `tcujq`; OSF bodies are append-only. Discharge condition for **obligation (2)**
  (posted **and** URL + timestamp recorded in `.planning/osf_deviations.md`), and
  that it is **Carter's external action**. Then the spliced block.
- **§3 — pre-paste checklist**, pointing to the oku OSF draft's Pre-Paste
  Reference rather than copying it (a second copy is a second source of truth
  waiting to drift): fill the `Date:` line; fill the commit gate (baseline
  `7d575a5` → HEAD at posting) and confirm no E-2 **code** change landed (option A
  leaves the code unchanged); confirm the manuscript half placed is the matching
  `ms-correction`; post as a new supplementary file. Plus the ⚠ scope boundary:
  this does **not** discharge the separate Check-2 amendment-update obligation —
  do not fold the two into one posting.
- **§4 — status.** Obligation (3) **DISCHARGED** (`DEC-2026-08-11-e2-framing-correction`).
  Obligations (1) and (2) **UNDISCHARGED**, and **⛔ no agent posts to OSF, edits a
  manuscript, or edits a posted amendment body — this file discharges nothing by
  existing.** Plus the two standing number rules (never the pooled 5.29% alone;
  never as the real-LD exposure).

**Step 3 —** run `./260811-oku-check-drafts.sh` (expect exit 0; if it is not 0,
**STOP** — the sources moved and the extraction's inherited evidence is void),
then `--self-test pair`, then the **full** `./260811-tf3-check.sh` (all three
groups green). Commit with explicit paths.
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis/.planning/quick/260811-oku-discharge-e-2-disclosure-obligations-dra && ./260811-oku-check-drafts.sh && cd ../260811-tf3-record-carter-decisions-e-2-framing-b-co && ./260811-tf3-check.sh --self-test && ./260811-tf3-check.sh && cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && git status --porcelain | grep -Ev '^\?\?' | grep -Ev '\.planning/(DECISIONS\.md|HANDOFF\.json|quick/260811-tf3|phases/m3-aou-afr-ld-panel-build/deferred-items\.md)' | wc -l | grep -qx 0</automated>
  </verify>
  <done>
`260811-tf3-SELECTED-PAIR-correction.md` exists; both bodies are `cmp`-identical
to a fresh extraction from the oku sources; the unselected limitation blocks are
absent; the pre-placement journal-policy check, both destinations, both discharge
conditions and the ⛔ no-agent-posts rule are present; the oku harness exited 0 at
execution time and that exit code is recorded in the file; all three tf3 clause
groups are GREEN and every negative control across all three groups was OBSERVED
red; the working tree shows modifications to **exactly** the four allowed paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| a chat turn → the permanent record | Carter's two decisions exist only as an in-session instruction. Everything downstream depends on transcribing intent without widening it. |
| planning-file prose → the resume path | `HANDOFF.json` / `deferred-items.md` are what the next session reads. A stale or contradictory surface here is indistinguishable from an undecided question. |
| oku draft blocks → the paste-ready extraction | Any byte drift between source and extraction silently un-inherits the oku harness's clause evidence. |
| this task's writes → the repo | Four allowed paths. `src/`, `tests/`, `config/`, `results/`, `.planning/amendments/`, `STATE.md`, `ROADMAP.md` and the oku/pmv quick dirs are read-only. |
| this task's writes → OSF / the manuscript | ⛔ Hard boundary. No agent crosses it. Obligations (1)+(2) discharge only by Carter's external actions. |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-tf3-01 | Tampering (a decision record that silently rewrites history) | `.planning/DECISIONS.md` | mitigate | Append-only, enforced two ways: `DEC-01` byte-prefix `cmp` against `BASELINE_REV` and `DEC-02` zero-`^<` diff. Both OBSERVED red on a line-deleted fixture copy (NC-D1). |
| T-tf3-02 | Denial of service (an unparseable resume file) | `.planning/HANDOFF.json` | mitigate | `HJ-01` `json.load` gate, OBSERVED red on a deliberately broken fixture **copy** (NC-H1). The real file is never corrupted to test the gate. |
| T-tf3-03 | Tampering (an edit that widens beyond the two array entries) | `.planning/HANDOFF.json` | mitigate | `HJ-02` structural containment walker: only `.carter_decisions_outstanding[0]` and `[2]` may differ; NC-H2 mutates an out-of-scope key and is OBSERVED red. `HJ-04` additionally requires `[0]`'s baseline text to survive as a byte-exact prefix (NC-H3). |
| T-tf3-04 | Spoofing (a "verbatim" paste body that is not verbatim) | `260811-tf3-SELECTED-PAIR-correction.md` | mitigate | `SP-02` `cmp` against a fresh machine extraction; the file is BUILT by splice, never retyped; NC-P1 corrupts `18.41` inside the block and is OBSERVED red. |
| T-tf3-05 | Elevation of privilege (an agent discharging an obligation that is Carter's) | OSF record, Track A manuscript | mitigate | No posting/manuscript tool call exists in any task. Every artifact states (1)+(2) are UNDISCHARGED and are Carter's external actions; `SP-04`/`SP-08` assert the ⛔ rule and the undischarged status are on the file's face. |
| T-tf3-06 | Repudiation (a green gate nobody has seen fail) | all three clause groups | mitigate | Every group is written and OBSERVED red before its edit lands, plus per-clause `--self-test` fixture controls; two controls (NC-D3, NC-P2) must fire **alone** so a clause cannot pass by proxy. `[[feedback_green_assertion_needs_a_negative_control]]`. |
| T-tf3-07 | Tampering (a stale baseline grading the edit against itself) | `BASELINE_REV` | mitigate | The harness asserts the baseline is still clean for any file whose group has not yet run, and FAILS LOUDLY rather than re-pinning. `#: DIFFERENTIAL SUBSTRATE — NEVER re-pin` annotation per `[[feedback_fixing_a_split_unpins_what_it_pinned]]`. |
| T-tf3-08 | Information disclosure (an over-stated public claim) | both DECISIONS entries, the SELECTED-PAIR file | mitigate | `DEC-07`/`SP-07` enforce the identity-LD-stub caveat and the never-quote-5.29%-alone rule; the SR4 entry must carry the "not a code review" and "LOWER BOUND" limits and both contrary-evidence caveats (`DEC-08` asserts `2bda675` and `status report` are present). |
| T-tf3-09 | Elevation of privilege (a disposition quietly creating a new pin) | `tests/m3/test_source_freeze_pins.py` | mitigate | Out of write scope; the SR4 entry must contain case-insensitive `no new pin` (`DEC-08`), and the final verify asserts zero modified tracked files outside the four allowed paths. |
| T-tf3-10 | Denial of service / cost | n/a | accept | Local, docs-only, `$0`, no LSF, no network, no AoU perimeter contact, no fire path touched, no test suite required. |
| T-tf3-11 | Information disclosure (data exposure) | n/a | accept | No PII, no genotypes, no controlled-tier data. Every number written here is already committed to this repo in `DECISIONS.md` and the oku/pmv artifacts. |
</threat_model>

<verification>
1. `./260811-tf3-check.sh` exits **0** with all three groups green, and
   `./260811-tf3-check.sh --self-test` exits **0** with **every** negative
   control printed as OBSERVED red on its named clause.
2. `git diff --numstat 0e7e309 HEAD -- .planning/DECISIONS.md` → deletion column
   **0**. Same for
   `.planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md`.
3. `git diff --name-only 0e7e309 HEAD` lists **exactly** the four allowed paths
   (`.planning/DECISIONS.md`, `.planning/HANDOFF.json`, the phase
   `deferred-items.md`, and files under
   `.planning/quick/260811-tf3-record-carter-decisions-e-2-framing-b-co/`) and
   nothing else. ⚠ If SUMMARY/STATE writes by the orchestrator appear, name them;
   do not fold them into the four.
4. `/rs1/.../smoke_dev/bin/python -c "import json; json.load(open('.planning/HANDOFF.json'))"`
   exits 0.
5. `./260811-oku-check-drafts.sh` exits 0 (the sources have not moved), and the
   two extracted blocks `cmp` clean against the SELECTED-PAIR file.
6. **Nothing was posted, placed or edited externally.** `.planning/amendments/`
   and `results/` show **zero** modified tracked files
   (`git status --porcelain .planning/amendments/ results/ | grep -v '^??'` is
   empty).
</verification>

<success_criteria>
- Both decisions are recorded in `.planning/DECISIONS.md` in house format, and
  the file is provably append-only against `0e7e309`.
- E-2 obligation **(3)** reads DISCHARGED on all three surfaces (`DECISIONS.md`,
  `HANDOFF.json`, `deferred-items.md`); **(1)** and **(2)** read OPEN on all
  three, each with its discharge condition restated.
- SR4-OPEN reads **NEVER ACTUALLY FROZEN** on all three surfaces, with the
  evidence cited by path **and** by commit, and **both** contrary-evidence
  caveats present rather than smoothed.
- No agent posted to OSF, touched a manuscript file, edited a posted amendment
  body, or added a source-freeze pin.
- Every mechanical gate in the plan was OBSERVED red on a fixture copy before it
  was trusted green — and the SUMMARY names each control and the clause it fired.
- The SUMMARY discloses the two deferred items: the untouched stale
  `HANDOFF.json` `status`/`headline`/`wave` strings, and the residual
  `STATE.md:15` / `ROADMAP.md:1077` correction sites.
</success_criteria>

<output>
After completion, create
`.planning/quick/260811-tf3-record-carter-decisions-e-2-framing-b-co/260811-tf3-SUMMARY.md`.

It must carry: the two DEC ids and one-line statements of each decision; the
RED-first accounting for all three clause groups (which clauses were red before
each edit, which controls were observed red and on which clause); the
append-only / containment / byte-identity numbers as measured (not as claimed);
the disclosed deviation in Task 2 Step 3 (the SR4 banner insertion into
`deferred-items.md`, beyond the literal content list); and the two deferred items
from `<explicitly_out_of_scope>`. ⚠ Do **not** assert any invariant the SUMMARY
cannot name an enforcer for
(`[[feedback_a_claimed_invariant_needs_a_named_enforcer]]`).
</output>
