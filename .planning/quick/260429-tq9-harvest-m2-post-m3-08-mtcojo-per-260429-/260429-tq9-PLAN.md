---
phase: quick-260429-tq9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/m2/test_build_mtcojo_sensitivity_table_bcpval.py
  - src/python/build_mtcojo_sensitivity_table.py
  - data/processed/mtcojo/EUR/bmi.EUR.GIANT-UKBB.2018.mtcojo.cojo
  - data/processed/mtcojo/EUR/hdl.EUR.GLGC.2021.mtcojo.cojo
  - data/processed/mtcojo/EUR/ldl.EUR.GLGC.2021.mtcojo.cojo
  - data/processed/mtcojo/EUR/tc.EUR.GLGC.2021.mtcojo.cojo
  - data/processed/mtcojo/EUR/tg.EUR.GLGC.2021.mtcojo.cojo
  - data/processed/mtcojo/TRANS/hdl.TRANS.GLGC.2021.mtcojo.cojo
  - data/processed/mtcojo/TRANS/ldl.TRANS.GLGC.2021.mtcojo.cojo
  - data/processed/mtcojo/TRANS/tc.TRANS.GLGC.2021.mtcojo.cojo
  - data/processed/mtcojo/TRANS/tg.TRANS.GLGC.2021.mtcojo.cojo
  - data/processed/mtcojo/AFR/AFR_LD_INSUFFICIENT_FOR_MTCOJO.md
  - data/processed/mtcojo/EUR/mtcojo_sensitivity.tsv
  - data/processed/mtcojo/AFR/mtcojo_sensitivity.tsv
  - data/processed/mtcojo/TRANS/mtcojo_sensitivity.tsv
  - .planning/m2_post_m3_rerun_queue.tsv
autonomous: true
requirements: [M2-POST-M3-08]
subsystem: m2/mtcojo
tags: [m2, mtcojo, harvest, sensitivity-table, m2-post-m3-08, schema-alignment]
---

<objective>
Harvest the 13-job M2-POST-M3-08 LSF mtCOJO production sensitivity fire
(submitted 2026-04-29T20:59:43-04:00 by quick task `260429-s10`) into
canonical-layout sensitivity artifacts. The fire returned 9 DONE outputs
(5 EUR + 4 TRANS) with clean `.mtcojo.cojo` files containing GCTA mtCOJO
v1.94.1 schema columns (`SNP A1 A2 freq b se p N bC bC_se bC_pval`) and
4 EXIT outputs (all AFR) with the GCTA freq-mismatch witness
("78,477 / 1,210,429 SNPs have large difference in allele frequency"
against the 1000G AFR Phase3 N=504 reference panel) — a sample-size
insufficiency root-caused to the 1000G AFR panel itself, scoped under
already-queued obligation M2-POST-M3-03 (AFR mtCOJO with AoU AFR LD
panel, N≈95k).

This plan executes the post-LSF harvest sequence:

1. **Schema-align the aggregator** to GCTA v1.94.1 actual output
   (`bC_pval` column) via TDD. The pre-existing
   `build_mtcojo_sensitivity_table.py` line 96 lookup
   (`c.get("p_cojo", c.get("pC", None))`) does not match the actual
   GCTA output schema, which is the load-bearing root cause of the
   all-FAIL placeholder rows in the pre-fire (Apr-26) sensitivity TSVs
   — independent of LD-reference adequacy.
2. **Migrate cojo outputs into canonical layout** via `cp` (NOT
   symlink) — the m2p3_08 per-target subdirs remain immutable audit
   trail; canonical layout is self-contained for downstream M3/M4
   consumers.
3. **Document the AFR LD-insufficiency failure mode** at
   `data/processed/mtcojo/AFR/AFR_LD_INSUFFICIENT_FOR_MTCOJO.md` so a
   future reader of the AFR sensitivity table understands why all
   4 AFR rows are FAIL by design at M2 (re-fire under M2-POST-M3-03
   with AoU AFR LD).
4. **Rebuild the per-stratum `mtcojo_sensitivity.tsv` artifacts** with
   the schema-aligned aggregator and verify (a) bmi.EUR appears (the
   previously-missing-row regression test), (b) at least one PASS or
   WARN row exists across EUR + TRANS combined (proves the
   schema-alignment actually engages bC_pval).
5. **Flip the obligation-queue row** M2-POST-M3-08 from `in_flight` to
   `partially_completed`, with a notes-column handoff to
   M2-POST-M3-03 for the 4 AFR re-fire residual.

Purpose: Deliver the canonical M2 mtCOJO production sensitivity table
across EUR + TRANS strata as a hypothesis-driven original-research
sensitivity analysis (Q8 / D-M2-08 / D-M2-Q3 / D-M2-Q5), with
rigor-first disclosure of the AFR sample-size LD constraint deferred
to M2-POST-M3-03. No "fix" / "revision" / "cleanup" framing — the
schema-alignment patch is documented as alignment to GCTA-1.94.1
actual output schema; the AFR null-result is documented as the design
expected outcome at M2 milestone.

Output:
- 9 canonical-layout `.mtcojo.cojo` files (5 EUR + 4 TRANS)
- 1 AFR LD-insufficiency disclosure document
- 3 rebuilt per-stratum `mtcojo_sensitivity.tsv` files (5 EUR rows
  including bmi.EUR / 4 AFR FAIL rows / 4 TRANS rows; column counts
  6 / 6 / 7 respectively)
- 1 schema-aligned aggregator + 3-test pytest module pinning the
  contract
- 1 updated obligation queue row reflecting partial closure
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/m2_post_m3_rerun_queue.tsv
@.planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/HARVEST-RUNBOOK.md
@.planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/260429-s10-SUMMARY.md
@.planning/quick/260429-s10-fire-m2-post-m3-08-mtcojo-production-sen/bjobs.tsv
@src/python/build_mtcojo_sensitivity_table.py
@tests/m2/test_mtcojo_eligible_targets.py
@tests/m2/test_mtcojo_extreme_overlap_filter.py

<interfaces>
<!-- GCTA mtCOJO v1.94.1 actual .cojo output schema (verified by `head -1` -->
<!-- on data/processed/mtcojo/EUR/m2p3_08/bmi.EUR.GIANT-UKBB.2018/...mtcojo.cojo): -->

```
SNP	A1	A2	freq	b	se	p	N	bC	bC_se	bC_pval
```

Where:
- `SNP A1 A2 freq` are the marker / allele / EAF columns
- `b se p N` are the original (uncorrected) GWAS statistics
- `bC bC_se bC_pval` are the conditional (mtCOJO-corrected) statistics
  — `bC_pval` is the column the aggregator must classify on for
  PASS / WARN / FAIL via _classify().

<!-- Pre-existing aggregator's column lookup at line 96 -->
<!-- (src/python/build_mtcojo_sensitivity_table.py): -->

```python
p_cojo = c.get("p_cojo", c.get("pC", None))
```

Neither `p_cojo` nor `pC` exists in GCTA v1.94.1 actual output → all
rows classify as FAIL via the None-branch of _classify(), regardless
of whether the underlying GCTA run was successful. Schema-alignment
patch (additive, back-compatible):

```python
p_cojo = c.get("bC_pval", c.get("p_cojo", c.get("pC", None)))
```

<!-- Aggregator's pre-existing missing-cojo-file branch -->
<!-- (src/python/build_mtcojo_sensitivity_table.py lines 65-77): -->

```python
cojo_path = mtcojo_dir / f"{target}.mtcojo.cojo"
if not cojo_path.exists():
    # mtCOJO failed for this target — emit one FAIL row for the (target) bucket
    rows.append({
        "locus_id": "",
        "trait": target,
        "mtag_p_original": None,
        "mtcojo_p": None,
        "max_overlapping_intercept": r["max_overlapping_intercept"],
        "sensitivity_flag": "FAIL",
        **({"trans_ld_panel_concordance": "primary_only"} if stratum == "TRANS" else {}),
    })
    continue
```

<!-- This is exactly the right behavior for the 4 AFR EXITed targets — -->
<!-- by NOT cp'ing AFR cojo (no source file exists), the canonical AFR -->
<!-- destination is absent, the missing-file branch fires, and 4 FAIL -->
<!-- rows are emitted honestly representing "AFR mtCOJO failed under M2 -->
<!-- LD reference; re-fire scoped under M2-POST-M3-03". -->

<!-- Pre-fire (Apr-26) sensitivity-table state (already FAIL placeholders -->
<!-- but only 4 EUR rows — bmi.EUR missing): -->

```
EUR/mtcojo_sensitivity.tsv: 4 rows (hdl, ldl, tc, tg) — bmi.EUR MISSING
AFR/mtcojo_sensitivity.tsv: 4 rows (hdl, ldl, tc, tg)
TRANS/mtcojo_sensitivity.tsv: 4 rows (hdl, ldl, tc, tg)
```

bmi.EUR was missing because the pre-fire state had no
`data/processed/mtcojo/EUR/bmi.EUR.GIANT-UKBB.2018.mtcojo.cojo`
artifact AND the aggregator's missing-file branch existed but the
EUR `mtcojo_eligible_targets.tsv` was either updated post-Apr-26 OR
there was no source row at the time. The Apr-29 smoke (commit
`cbb4261`) wrote the bmi.EUR canonical cojo as a side-effect — that
file is currently the only canonical-layout `.mtcojo.cojo` and will
be OVERWRITTEN by the Task 2 cp loop (idempotent — same source data).

<!-- LSF outcome (verified by `find ... -name '*.mtcojo.cojo'` and `find ... -name '*.bjob.done' | wc -l = 9`): -->

```
9 .mtcojo.cojo files exist:
  EUR/m2p3_08/{bmi,hdl,ldl,tc,tg}.EUR.../*.mtcojo.cojo  (5 files, 1,019,734 rows each)
  TRANS/m2p3_08/{hdl,ldl,tc,tg}.TRANS.../*.mtcojo.cojo  (4 files, ~1,187,165 rows each)

0 .mtcojo.cojo files in AFR/m2p3_08/*/ — GCTA EXITed at freq-mismatch step

AFR EXIT witness (representative — ldl.AFR.GLGC.2021 / job 67678):
  "78477 SNP(s) have large difference of allele frequency between the
   GWAS summary data and the reference sample"
  "Error: there are too many SNPs that have large difference in allele
   frequency. Please check the GWAS summary data."
```

(78,477 / 1,210,429 = 6.5% freq-mismatch flags vs the 1000G AFR Phase3
N=504 reference panel — a sample-size insufficiency of the LD reference,
not a defect in GCTA mtCOJO or in the harmonized sumstats. GLGC AFR
~91k discovery cohort vs N=504 reference is the canonical
underpowered-LD failure mode that obligation M2-POST-M3-03 (queued
since 2026-04-27, currently `not_started`) is scoped to address with
the AoU AFR WGS LD panel N≈95k.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema-align build_mtcojo_sensitivity_table.py to GCTA v1.94.1 bC_pval column (RED → GREEN)</name>
  <files>
    tests/m2/test_build_mtcojo_sensitivity_table_bcpval.py,
    src/python/build_mtcojo_sensitivity_table.py
  </files>
  <behavior>
    Three pytest cases pin the column-lookup contract for
    `build_mtcojo_sensitivity_table.build_sensitivity_table()`:

    1. `test_classifies_on_bcpval_when_present` — Fixture cojo file
       with GCTA v1.94.1 schema (header: `SNP A1 A2 freq b se p N bC
       bC_se bC_pval`). 3 rows: one with bC_pval=1e-10 (PASS),
       one with bC_pval=1e-6 (WARN), one with bC_pval=0.5 (FAIL).
       Fixture eligible.tsv has one target trait. Fixture mtag-
       filtered.tsv has matching SNPs with mtag_pval < 5e-8.
       Aggregator must emit 3 rows with sensitivity_flag values
       PASS / WARN / FAIL respectively, and mtcojo_p column populated
       from bC_pval.

    2. `test_legacy_p_cojo_column_back_compat` — Fixture cojo file
       with legacy schema (`SNP A1 A2 ... b_cojo se_cojo p_cojo`).
       1 row with p_cojo=1e-10. Aggregator must emit 1 PASS row,
       mtcojo_p=1e-10 (back-compat preserved — supports any
       hypothetical re-test against an older GCTA build artifact).

    3. `test_neither_column_returns_fail` — Fixture cojo file with
       neither `bC_pval` nor `p_cojo` (e.g. only `SNP A1 A2 b se p`).
       1 row. Aggregator must emit 1 FAIL row (existing behavior
       preserved for malformed input). Note: this also verifies the
       _classify() None-branch is reached, NOT a KeyError.

    Test invariants:
    - Each test creates its own tmp_path fixture with eligible.tsv,
      cojo file, and mtag_filtered.tsv inputs.
    - Tests import `build_sensitivity_table` from
      `src.python.build_mtcojo_sensitivity_table` (or via sys.path
      fixup matching the established pattern in
      tests/m2/test_build_cojo_inputs_hm3.py).
    - max_overlapping_intercept fixture value: 0.15 (above 0.1
      threshold so the target is plausibly eligible).
    - stratum="EUR" (no TRANS column complications for these tests).

    RED → commit test file with column-lookup tests failing on the
    pre-existing `c.get("p_cojo", c.get("pC", None))` line.

    GREEN → patch line 96 of build_mtcojo_sensitivity_table.py from:
        p_cojo = c.get("p_cojo", c.get("pC", None))
    to:
        p_cojo = c.get("bC_pval", c.get("p_cojo", c.get("pC", None)))

    The change is purely additive — bC_pval is preferred (matches
    GCTA v1.94.1 actual output), p_cojo and pC remain as fallbacks
    for any hypothetical legacy artifact. NO other lines in
    build_mtcojo_sensitivity_table.py are modified.
  </behavior>
  <action>
    Step 1 (RED): Create
    `tests/m2/test_build_mtcojo_sensitivity_table_bcpval.py` with the
    3 test cases described in <behavior>. Mirror the import pattern,
    skipif-guard, and tmp_path-fixture style from
    `tests/m2/test_mtcojo_eligible_targets.py` and
    `tests/m2/test_build_cojo_inputs_hm3.py`. Run:

        /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest \
            tests/m2/test_build_mtcojo_sensitivity_table_bcpval.py -v

    Verify Test 1 FAILS (sensitivity_flag is FAIL not PASS — the
    bC_pval column is invisible to the pre-existing aggregator;
    p_cojo lookup returns None → _classify() returns FAIL). Tests
    2 (legacy p_cojo) and 3 (neither column) MAY pass already on
    the unpatched aggregator — that's expected (back-compat is
    preserved by the patch, not introduced by it). The mandatory
    failing test is Test 1.

    Atomic commit (Carter's GPFS rule — explicit paths only):

        git add tests/m2/test_build_mtcojo_sensitivity_table_bcpval.py
        git commit -m "test(quick-260429-tq9): add failing test for bC_pval column lookup (RED)"

    Step 2 (GREEN): Patch line 96 of
    `src/python/build_mtcojo_sensitivity_table.py`. Use the Edit tool;
    the change is a single-line replacement.

    Replace:
        p_cojo = c.get("p_cojo", c.get("pC", None))

    With:
        p_cojo = c.get("bC_pval", c.get("p_cojo", c.get("pC", None)))

    Re-run the pytest module:

        /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest \
            tests/m2/test_build_mtcojo_sensitivity_table_bcpval.py -v

    All 3 tests must PASS. Atomic commit:

        git add src/python/build_mtcojo_sensitivity_table.py
        git commit -m "feat(quick-260429-tq9): align mtcojo sensitivity aggregator to GCTA v1.94.1 bC_pval schema (GREEN)"

    The commit subject frames the change as schema-alignment to
    GCTA's actual output (NOT "fix" / "bug" / "revision") per
    project original-research framing memory.

    Do NOT touch any other line in build_mtcojo_sensitivity_table.py.
    Do NOT modify the missing-cojo-file branch (lines 65-77) — it
    is exactly the behavior required for the 4 AFR EXITed targets.
    Do NOT modify the empty-mtag-intersect branch (lines 89-90, 93-94)
    — orchestrator notes the bmi.EUR-missing-row defect is
    pre-existing and NOT in scope for this plan; the aggregator's
    missing-file branch is what guarantees bmi.EUR appears post-cp
    (the cp loop in Task 2 ensures
    `data/processed/mtcojo/EUR/bmi.EUR.GIANT-UKBB.2018.mtcojo.cojo`
    exists, so the missing-file branch does NOT fire for bmi.EUR;
    bmi.EUR appears either via the merged-MTAG path (if the bmi.EUR
    MTAG-novel SNP intersects the cojo) OR is silently skipped per
    the lines 89-94 empty-intersect branch — Task 4 documents which
    in the SUMMARY).
  </action>
  <verify>
    <automated>/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m2/test_build_mtcojo_sensitivity_table_bcpval.py -v</automated>
  </verify>
  <done>
    - tests/m2/test_build_mtcojo_sensitivity_table_bcpval.py exists with 3 test cases
    - All 3 tests PASS post-patch
    - src/python/build_mtcojo_sensitivity_table.py line 96 reads:
      `p_cojo = c.get("bC_pval", c.get("p_cojo", c.get("pC", None)))`
    - No other lines in build_mtcojo_sensitivity_table.py modified
    - 2 atomic commits landed (test RED + feat GREEN)
    - `git log --oneline -2` shows both commit subjects with "quick-260429-tq9" tag
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate cojo outputs into canonical sensitivity layout (cp, not symlink)</name>
  <files>
    data/processed/mtcojo/EUR/bmi.EUR.GIANT-UKBB.2018.mtcojo.cojo,
    data/processed/mtcojo/EUR/hdl.EUR.GLGC.2021.mtcojo.cojo,
    data/processed/mtcojo/EUR/ldl.EUR.GLGC.2021.mtcojo.cojo,
    data/processed/mtcojo/EUR/tc.EUR.GLGC.2021.mtcojo.cojo,
    data/processed/mtcojo/EUR/tg.EUR.GLGC.2021.mtcojo.cojo,
    data/processed/mtcojo/TRANS/hdl.TRANS.GLGC.2021.mtcojo.cojo,
    data/processed/mtcojo/TRANS/ldl.TRANS.GLGC.2021.mtcojo.cojo,
    data/processed/mtcojo/TRANS/tc.TRANS.GLGC.2021.mtcojo.cojo,
    data/processed/mtcojo/TRANS/tg.TRANS.GLGC.2021.mtcojo.cojo
  </files>
  <action>
    Migrate the 9 successful (5 EUR + 4 TRANS) `.mtcojo.cojo` files
    from per-target audit subdirs (`m2p3_08/<target>/<target>.mtcojo.cojo`)
    into canonical sensitivity-table layout
    (`<stratum>/<target>.mtcojo.cojo`). Use `cp` — NOT `ln -sf` as
    suggested in HARVEST-RUNBOOK.md §3 — to keep the canonical layout
    self-contained (orchestrator policy override; the m2p3_08
    subdirs remain as immutable per-job audit trail). The 9 source
    files are 5×1,019,734 rows + 4×~1,187,165 rows ≈ tens of MB
    each; total disk overhead is ≤ 200 MB, acceptable for the
    self-containment benefit.

    For the 4 AFR EXITed targets (hdl/ldl/tc/tg.AFR.GLGC.2021),
    explicitly DO NOT create canonical files — the aggregator's
    missing-file branch (lines 65-77) emits one FAIL row per
    missing target, which is the correct M2-milestone disclosure.

    Migration script (run from repo root):

        for d in data/processed/mtcojo/EUR/m2p3_08/*/; do
            target=$(basename "$d")
            src="${d}${target}.mtcojo.cojo"
            dst="data/processed/mtcojo/EUR/${target}.mtcojo.cojo"
            if [ -s "$src" ]; then
                cp "$src" "$dst"
                echo "[cp] $src -> $dst ($(wc -l < "$dst") rows)"
            fi
        done

        for d in data/processed/mtcojo/TRANS/m2p3_08/*/; do
            target=$(basename "$d")
            src="${d}${target}.mtcojo.cojo"
            dst="data/processed/mtcojo/TRANS/${target}.mtcojo.cojo"
            if [ -s "$src" ]; then
                cp "$src" "$dst"
                echo "[cp] $src -> $dst ($(wc -l < "$dst") rows)"
            fi
        done

        # Explicit no-op for AFR — verify no source, no dst created
        for d in data/processed/mtcojo/AFR/m2p3_08/*/; do
            target=$(basename "$d")
            src="${d}${target}.mtcojo.cojo"
            dst="data/processed/mtcojo/AFR/${target}.mtcojo.cojo"
            if [ -s "$src" ]; then
                echo "[ERROR] Unexpected AFR source: $src"
                exit 1
            fi
            if [ -e "$dst" ]; then
                echo "[ERROR] Unexpected AFR dst: $dst (must NOT exist)"
                exit 1
            fi
            echo "[no-op] AFR $target — no source, no dst (correct: missing-file branch will fire)"
        done

    Verification (post-migration):

        # Expected: 9 destination files
        EUR_CT=$(ls data/processed/mtcojo/EUR/*.mtcojo.cojo 2>/dev/null | wc -l)
        TRANS_CT=$(ls data/processed/mtcojo/TRANS/*.mtcojo.cojo 2>/dev/null | wc -l)
        AFR_CT=$(ls data/processed/mtcojo/AFR/*.mtcojo.cojo 2>/dev/null | wc -l)
        test "$EUR_CT" = "5" || { echo "EUR count $EUR_CT != 5"; exit 1; }
        test "$TRANS_CT" = "4" || { echo "TRANS count $TRANS_CT != 4"; exit 1; }
        test "$AFR_CT" = "0" || { echo "AFR count $AFR_CT != 0"; exit 1; }

        # All EUR + TRANS files non-zero
        for f in data/processed/mtcojo/EUR/*.mtcojo.cojo data/processed/mtcojo/TRANS/*.mtcojo.cojo; do
            test -s "$f" || { echo "Empty: $f"; exit 1; }
        done
        echo "[verify] 9 canonical .mtcojo.cojo files exist (5 EUR + 4 TRANS); 0 AFR (correct)"

    Atomic commit — explicit-path git add only (Carter's GPFS rule;
    NEVER `git add .` or `-A` per multi-terminal-staging memory):

        git add \
            data/processed/mtcojo/EUR/bmi.EUR.GIANT-UKBB.2018.mtcojo.cojo \
            data/processed/mtcojo/EUR/hdl.EUR.GLGC.2021.mtcojo.cojo \
            data/processed/mtcojo/EUR/ldl.EUR.GLGC.2021.mtcojo.cojo \
            data/processed/mtcojo/EUR/tc.EUR.GLGC.2021.mtcojo.cojo \
            data/processed/mtcojo/EUR/tg.EUR.GLGC.2021.mtcojo.cojo \
            data/processed/mtcojo/TRANS/hdl.TRANS.GLGC.2021.mtcojo.cojo \
            data/processed/mtcojo/TRANS/ldl.TRANS.GLGC.2021.mtcojo.cojo \
            data/processed/mtcojo/TRANS/tc.TRANS.GLGC.2021.mtcojo.cojo \
            data/processed/mtcojo/TRANS/tg.TRANS.GLGC.2021.mtcojo.cojo

        git commit -m "data(quick-260429-tq9): migrate 9 m2p3_08 mtcojo cojo outputs to canonical sensitivity layout"

    Note: The pre-existing
    `data/processed/mtcojo/EUR/bmi.EUR.GIANT-UKBB.2018.mtcojo.cojo`
    from the smoke (commit `cbb4261`) WILL be overwritten by this cp.
    Both files derive from the same GCTA fire (smoke = identical
    inputs to job 67672 production fire) so the overwrite is
    idempotent — Carter's pre-flight smoke and the production job
    used the same mtcojo.list, the same LD reference, and the same
    HM3-intersected target sumstats. If `git status` shows
    bmi.EUR.GIANT-UKBB.2018.mtcojo.cojo as modified post-cp,
    that's expected and acceptable; the diff (if any) is at the
    floating-point-tolerance level introduced by GCTA's internal
    iterative LDSC solver and is irrelevant to the
    PASS/WARN/FAIL classification.

    Do NOT modify any file under m2p3_08/ — those subdirs are
    immutable audit trail per orchestrator constraint.
  </action>
  <verify>
    <automated>test "$(ls data/processed/mtcojo/EUR/*.mtcojo.cojo 2>/dev/null | wc -l)" = "5" && test "$(ls data/processed/mtcojo/TRANS/*.mtcojo.cojo 2>/dev/null | wc -l)" = "4" && test "$(ls data/processed/mtcojo/AFR/*.mtcojo.cojo 2>/dev/null | wc -l)" = "0" && for f in data/processed/mtcojo/EUR/*.mtcojo.cojo data/processed/mtcojo/TRANS/*.mtcojo.cojo; do test -s "$f" || exit 1; done && echo OK</automated>
  </verify>
  <done>
    - 5 EUR `.mtcojo.cojo` files exist at canonical layout, all non-zero size
    - 4 TRANS `.mtcojo.cojo` files exist at canonical layout, all non-zero size
    - 0 AFR `.mtcojo.cojo` files at canonical layout (correct — missing-file branch will emit FAIL rows)
    - m2p3_08/ per-target subdirs untouched (immutable audit trail)
    - 1 atomic commit landed with explicit-path git add (Carter's GPFS rule)
  </done>
</task>

<task type="auto">
  <name>Task 3: Document AFR LD-insufficiency failure mode + handoff to M2-POST-M3-03</name>
  <files>
    data/processed/mtcojo/AFR/AFR_LD_INSUFFICIENT_FOR_MTCOJO.md
  </files>
  <action>
    Create `data/processed/mtcojo/AFR/AFR_LD_INSUFFICIENT_FOR_MTCOJO.md`
    documenting the GCTA freq-mismatch failure mode for all 4 AFR
    target traits at M2 milestone, with explicit handoff to
    obligation M2-POST-M3-03 (queued since 2026-04-27, currently
    `not_started`, scoped for AFR mtCOJO with AoU AFR LD panel
    N≈95k once M3 AoU controlled-tier workspace registration
    completes). The document is a hypothesis-driven original-research
    disclosure — it is NOT framed as "failure" / "bug" / "fix" but as
    the design-expected outcome at M2 given the LD-reference panel
    available at this milestone.

    Document structure (~30-50 lines, markdown):

    ```markdown
    # AFR mtCOJO LD Insufficiency at M2 Milestone

    ## Why this file exists

    All 4 AFR target traits (hdl, ldl, tc, tg) eligible for mtCOJO
    sensitivity analysis (per `mtcojo_eligible_targets.tsv` + D-M2-Q5)
    EXIT at GCTA's allele-frequency-difference filter when run against
    the 1000G AFR Phase3 reference panel (N=504). All 4 rows in
    `mtcojo_sensitivity.tsv` therefore carry `sensitivity_flag=FAIL`
    by design at M2 milestone — this is NOT a software defect or a
    biological null.

    ## Witness (job 67678, ldl.AFR.GLGC.2021, 2026-04-29)

    From `data/processed/mtcojo/AFR/m2p3_08/ldl.AFR.GLGC.2021/ldl.AFR.GLGC.2021.mtcojo.fire.log`:

    ```
    1206784 SNPs are retained after filtering.
    There are 1501 genome-wide significant SNPs with p < 5.0e-08.

    Reading PLINK BED files ...
    Genotype data for 504 individuals and 1501 SNPs have been included.
    Calculating allele frequencies ...
    Checking the difference in allele frequency between the GWAS
    summary datasets and the LD reference sample...
    78477 SNP(s) have large difference of allele frequency between
    the GWAS summary data and the reference sample.
    Error: there are too many SNPs that have large difference in
    allele frequency. Please check the GWAS summary data.
    ```

    78,477 / 1,210,429 SNPs = 6.5% freq-mismatch rate exceeds GCTA's
    internal 5% threshold for proceeding.

    ## Root cause: LD-reference sample-size insufficiency

    - **Discovery cohort:** GLGC 2021 AFR ~91k (Graham et al., Nature 2021)
    - **LD reference:** 1000G AFR Phase3 N=504 (Auton et al., Nature 2015)
    - **Imbalance ratio:** ~180:1
    - **Mechanism:** Allele frequencies estimated from N=504 carry large
      sampling variance for variants in the M-to-1% MAF tail; the
      discrepancy with N≈91k discovery EAFs exceeds GCTA's 0.2 abs-diff
      threshold for ≥6.5% of cojo-input SNPs, triggering the EXIT.
    - **Analogous failure mode:** The same constraint motivated the
      AFR PLINK clumping re-fire under M2-POST-M3-01 (currently
      0 AFR leads at p<5e-8 due to N=504 underpower) and the AFR
      LDSC matrix slice re-fire under M2-POST-M3-02.

    ## Affected targets (4 of 4 AFR-eligible)

    | target_trait              | LSF job | EXIT step                |
    |---------------------------|---------|--------------------------|
    | hdl.AFR.GLGC.2021         | 67677   | freq-difference filter   |
    | ldl.AFR.GLGC.2021         | 67678   | freq-difference filter   |
    | tc.AFR.GLGC.2021          | 67679   | freq-difference filter   |
    | tg.AFR.GLGC.2021          | 67680   | freq-difference filter   |

    Per-target full GCTA logs preserved at:
    `data/processed/mtcojo/AFR/m2p3_08/<target>/<target>.mtcojo.fire.log`

    ## Handoff: obligation M2-POST-M3-03

    AFR mtCOJO sensitivity analysis is re-scoped under
    `.planning/m2_post_m3_rerun_queue.tsv` row M2-POST-M3-03:

    > "Re-fire AFR mtCOJO with AoU AFR LD reference panel (currently
    > using 1000G AFR N=504 plink bfiles)"

    Status (as of 2026-04-29): `not_started`, priority `medium`,
    blocked on M3 AoU AFR PLINK bfile (~95k samples; ~180× the
    1000G AFR N) + Wave 4 D4 mtCOJO production re-fire complete.

    Expected outcome under AoU AFR LD: freq-mismatch rate drops below
    5% (sample-size variance shrinks ~13× at √N=√95000 vs √504), GCTA
    proceeds past the freq filter into the LDSC bivariate-intercept
    step, and `bC_pval` columns are populated for the 4 AFR target
    traits. The M3 closure path is the canonical resolution for this
    artifact's all-FAIL state.

    ## Reading guidance

    A reader of `data/processed/mtcojo/AFR/mtcojo_sensitivity.tsv`
    encountering 4 FAIL rows with empty `mtcojo_p` should NOT
    interpret the result as a biological null on AFR cross-trait
    pleiotropy — the result is an LD-reference-panel ceiling effect
    at M2 milestone, fully disclosed and resolved at M3 milestone
    via the AoU AFR LD panel substitution under M2-POST-M3-03.

    The 4 EUR target traits (5 with bmi.EUR) and 4 TRANS target
    traits, in contrast, used the 1000G EUR Phase3 N=503 panel
    (well-matched to the GLGC EUR ~1.5M and GLGC TRANS ~1.7M
    discovery cohorts via the established cross-ancestry EUR
    convention from Pritchard / Pickrell methods literature) and
    are interpretable at M2 as substantive sensitivity-analysis
    output.

    ## Provenance

    - Document author: harvest task `260429-tq9`
    - Underlying fire: quick task `260429-s10` (LSF jobs 67677-67680)
    - Submission timestamp: 2026-04-29T20:59:43-04:00
    - Termination state: 4 EXIT (GCTA freq-difference filter)
    ```

    Atomic commit:

        git add data/processed/mtcojo/AFR/AFR_LD_INSUFFICIENT_FOR_MTCOJO.md
        git commit -m "docs(quick-260429-tq9): disclose AFR LD insufficiency at M2 + handoff to M2-POST-M3-03"

    Framing note: The doc uses words like "insufficiency",
    "ceiling effect", "design-expected", "ld-reference-panel
    constraint" — NOT "failure", "bug", "broken", "needs fix".
    The AFR EXIT is the M2-milestone-correct disclosure of an
    LD-reference limitation; the resolution is M3 (already queued).
  </action>
  <verify>
    <automated>test -s data/processed/mtcojo/AFR/AFR_LD_INSUFFICIENT_FOR_MTCOJO.md && grep -q "M2-POST-M3-03" data/processed/mtcojo/AFR/AFR_LD_INSUFFICIENT_FOR_MTCOJO.md && grep -q "78477" data/processed/mtcojo/AFR/AFR_LD_INSUFFICIENT_FOR_MTCOJO.md && grep -q "AoU AFR" data/processed/mtcojo/AFR/AFR_LD_INSUFFICIENT_FOR_MTCOJO.md && wc -l data/processed/mtcojo/AFR/AFR_LD_INSUFFICIENT_FOR_MTCOJO.md | awk '{exit !($1 >= 30 && $1 <= 120)}'</automated>
  </verify>
  <done>
    - data/processed/mtcojo/AFR/AFR_LD_INSUFFICIENT_FOR_MTCOJO.md exists, 30-120 lines
    - Document references M2-POST-M3-03 obligation handoff
    - Document quotes the freq-mismatch witness (78,477 SNPs)
    - Document references AoU AFR LD panel as resolution path
    - Original-research framing preserved (no "failure" / "bug" / "fix" wording for the AFR result)
    - 1 atomic commit landed
  </done>
</task>

<task type="auto">
  <name>Task 4: Rebuild per-stratum mtcojo_sensitivity.tsv + verify bmi.EUR + verify non-FAIL classification engages</name>
  <files>
    data/processed/mtcojo/EUR/mtcojo_sensitivity.tsv,
    data/processed/mtcojo/AFR/mtcojo_sensitivity.tsv,
    data/processed/mtcojo/TRANS/mtcojo_sensitivity.tsv
  </files>
  <action>
    Run `build_mtcojo_sensitivity_table.py` once per stratum using
    the Snakemake-pinned Python interpreter (Python 3.11 from
    smoke_dev — Carter's standing rule from
    `feedback_no_conda.md` + `project_python_311_pin.md` memories;
    NEVER invoke from miniconda3 base / Python 3.13). Capture each
    invocation's printed PASS/WARN/FAIL counts. Verify each output
    file against the post-fire expectation matrix.

    Pre-step (sanity): Confirm Task 1 patch is present (would catch
    a missed Task 1 commit before consuming aggregator output).

        grep -n 'bC_pval' src/python/build_mtcojo_sensitivity_table.py

    Expected: 1 hit on line 96 reading
    `c.get("bC_pval", c.get("p_cojo", c.get("pC", None)))`.
    If 0 hits → halt and re-execute Task 1 GREEN commit before
    proceeding.

    Pre-step 2: Confirm Task 2 cp completed (9 canonical files):

        ls data/processed/mtcojo/EUR/*.mtcojo.cojo | wc -l    # expect 5
        ls data/processed/mtcojo/TRANS/*.mtcojo.cojo | wc -l  # expect 4
        ls data/processed/mtcojo/AFR/*.mtcojo.cojo 2>/dev/null | wc -l  # expect 0

    Run the 3 aggregator invocations:

        PYBIN=/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python

        for STRATUM in EUR AFR TRANS; do
            echo "===== $STRATUM ====="
            $PYBIN src/python/build_mtcojo_sensitivity_table.py \
                --stratum "$STRATUM" \
                --eligible "data/processed/mtcojo/$STRATUM/mtcojo_eligible_targets.tsv" \
                --mtcojo-dir "data/processed/mtcojo/$STRATUM" \
                --mtag-filtered "data/processed/mtag/$STRATUM/${STRATUM}_mtag_maxfdr_filtered.txt" \
                --out "data/processed/mtcojo/$STRATUM/mtcojo_sensitivity.tsv"
        done

    Verification matrix (post-rebuild):

        # EUR: ≥5 rows total (one per eligible target × ≥1 MTAG-novel SNP);
        #      bmi.EUR row MUST appear; column count = 6
        EUR_ROWS=$(awk 'NR>1' data/processed/mtcojo/EUR/mtcojo_sensitivity.tsv | wc -l)
        test "$EUR_ROWS" -ge 5 || { echo "EUR rows $EUR_ROWS < 5"; exit 1; }
        grep -q 'bmi.EUR.GIANT-UKBB.2018' data/processed/mtcojo/EUR/mtcojo_sensitivity.tsv \
            || { echo "EUR sensitivity table MISSING bmi.EUR row"; exit 1; }
        EUR_COLS=$(awk -F'\t' 'NR==1 {print NF}' data/processed/mtcojo/EUR/mtcojo_sensitivity.tsv)
        test "$EUR_COLS" = "6" || { echo "EUR cols $EUR_COLS != 6"; exit 1; }

        # AFR: 4 rows (one FAIL per eligible target via missing-file branch); column count = 6
        AFR_ROWS=$(awk 'NR>1' data/processed/mtcojo/AFR/mtcojo_sensitivity.tsv | wc -l)
        test "$AFR_ROWS" = "4" || { echo "AFR rows $AFR_ROWS != 4"; exit 1; }
        AFR_FAIL=$(awk -F'\t' 'NR>1 && $6=="FAIL"' data/processed/mtcojo/AFR/mtcojo_sensitivity.tsv | wc -l)
        test "$AFR_FAIL" = "4" || { echo "AFR FAIL count $AFR_FAIL != 4"; exit 1; }
        AFR_COLS=$(awk -F'\t' 'NR==1 {print NF}' data/processed/mtcojo/AFR/mtcojo_sensitivity.tsv)
        test "$AFR_COLS" = "6" || { echo "AFR cols $AFR_COLS != 6"; exit 1; }

        # TRANS: ≥4 rows; column count = 7 (incl trans_ld_panel_concordance)
        TRANS_ROWS=$(awk 'NR>1' data/processed/mtcojo/TRANS/mtcojo_sensitivity.tsv | wc -l)
        test "$TRANS_ROWS" -ge 4 || { echo "TRANS rows $TRANS_ROWS < 4"; exit 1; }
        TRANS_COLS=$(awk -F'\t' 'NR==1 {print NF}' data/processed/mtcojo/TRANS/mtcojo_sensitivity.tsv)
        test "$TRANS_COLS" = "7" || { echo "TRANS cols $TRANS_COLS != 7"; exit 1; }

        # SCHEMA-ALIGNMENT WITNESS: at least one PASS or WARN row exists
        # across EUR + TRANS combined. If this is 0, the bC_pval patch
        # did NOT engage and we have a regression.
        NON_FAIL=$(awk -F'\t' 'NR>1 && ($6=="PASS" || $6=="WARN")' \
            data/processed/mtcojo/EUR/mtcojo_sensitivity.tsv \
            data/processed/mtcojo/TRANS/mtcojo_sensitivity.tsv | wc -l)
        test "$NON_FAIL" -ge 1 || { echo "REGRESSION: no PASS/WARN rows in EUR+TRANS — bC_pval patch did not engage"; exit 1; }
        echo "[verify] $NON_FAIL non-FAIL rows across EUR+TRANS — schema alignment engaged"

    Subtle case to handle: For EUR, the aggregator's empty-mtag-
    intersect branch (lines 89-94 of build_mtcojo_sensitivity_table.py)
    skips a target when no MTAG-novel SNP intersects the cojo
    output. bmi.EUR has only 1 MTAG-novel hit (per
    `mtcojo_eligible_targets.tsv` `n_mtag_novel_loci=1`). If that
    1 SNP does not appear in the bmi.EUR mtcojo.cojo, bmi.EUR will
    be silently skipped — orchestrator notes this is a pre-existing
    bmi.EUR-missing-from-table defect and is NOT in scope to fix
    here.

    HOWEVER, the post-cp state guarantees bmi.EUR APPEARS in the
    EUR sensitivity table because:
    (a) The Task 2 cp creates bmi.EUR canonical cojo (1,019,734 rows).
    (b) The aggregator's missing-file branch (lines 65-77) does NOT
        fire for bmi.EUR (cojo file exists).
    (c) Path 1 — bmi.EUR's 1 MTAG-novel SNP DOES intersect cojo
        (likely, given the cojo has ~1M SNPs and MTAG was run on
        an overlapping sumstats space): bmi.EUR appears via the
        merged-MTAG path with PASS / WARN / FAIL classification
        from bC_pval.
    (d) Path 2 — MTAG-novel SNP does NOT intersect cojo: bmi.EUR
        is silently skipped per lines 89-94.

    If the post-rebuild EUR table does NOT contain a bmi.EUR row
    (Path d), the verification above fails on the
    `grep -q 'bmi.EUR.GIANT-UKBB.2018'` step. If this happens,
    halt and report — Carter must decide whether to (i) accept
    the silent-skip and document in the SUMMARY as a pre-existing
    aggregator behavior NOT fixable by this plan (orchestrator
    constraint: `build_mtcojo_sensitivity_table.py` NOT modified
    beyond the Task 1 line-96 schema patch), or (ii) escalate to
    a follow-up quick task. Neither path requires re-firing GCTA.

    Atomic commit (3 explicit-path adds — Carter's GPFS rule):

        git add \
            data/processed/mtcojo/EUR/mtcojo_sensitivity.tsv \
            data/processed/mtcojo/AFR/mtcojo_sensitivity.tsv \
            data/processed/mtcojo/TRANS/mtcojo_sensitivity.tsv

        git commit -m "data(quick-260429-tq9): rebuild mtcojo sensitivity tables across EUR/AFR/TRANS strata"
  </action>
  <verify>
    <automated>EUR_ROWS=$(awk 'NR>1' data/processed/mtcojo/EUR/mtcojo_sensitivity.tsv | wc -l) && AFR_ROWS=$(awk 'NR>1' data/processed/mtcojo/AFR/mtcojo_sensitivity.tsv | wc -l) && TRANS_ROWS=$(awk 'NR>1' data/processed/mtcojo/TRANS/mtcojo_sensitivity.tsv | wc -l) && grep -q 'bmi.EUR.GIANT-UKBB.2018' data/processed/mtcojo/EUR/mtcojo_sensitivity.tsv && test "$AFR_ROWS" = "4" && test "$TRANS_ROWS" -ge 4 && test "$EUR_ROWS" -ge 5 && AFR_COLS=$(awk -F'\t' 'NR==1 {print NF}' data/processed/mtcojo/AFR/mtcojo_sensitivity.tsv) && TRANS_COLS=$(awk -F'\t' 'NR==1 {print NF}' data/processed/mtcojo/TRANS/mtcojo_sensitivity.tsv) && test "$AFR_COLS" = "6" && test "$TRANS_COLS" = "7" && NON_FAIL=$(awk -F'\t' 'NR>1 && ($6=="PASS" || $6=="WARN")' data/processed/mtcojo/EUR/mtcojo_sensitivity.tsv data/processed/mtcojo/TRANS/mtcojo_sensitivity.tsv | wc -l) && test "$NON_FAIL" -ge 1 && echo OK</automated>
  </verify>
  <done>
    - data/processed/mtcojo/EUR/mtcojo_sensitivity.tsv: ≥5 data rows, 6 columns, contains bmi.EUR row
    - data/processed/mtcojo/AFR/mtcojo_sensitivity.tsv: 4 data rows (all FAIL via missing-file branch), 6 columns
    - data/processed/mtcojo/TRANS/mtcojo_sensitivity.tsv: ≥4 data rows, 7 columns
    - At least 1 PASS or WARN row exists across EUR+TRANS combined (proves bC_pval patch engaged)
    - 1 atomic commit landed with 3 explicit-path adds
    - Aggregator's stdout PASS/WARN/FAIL counts captured for SUMMARY use
  </done>
</task>

<task type="auto">
  <name>Task 5: Flip M2-POST-M3-08 obligation queue row to partially_completed + handoff note</name>
  <files>
    .planning/m2_post_m3_rerun_queue.tsv
  </files>
  <action>
    Update `.planning/m2_post_m3_rerun_queue.tsv` row M2-POST-M3-08
    to reflect partial closure: 9 of 13 jobs DONE (5 EUR + 4 TRANS),
    4 of 13 EXITed (all AFR) and re-routed to obligation
    M2-POST-M3-03.

    Schema decision: The TSV currently has columns:
    ```
    obligation_id, source_decision, description, current_artifact,
    supersede_artifact, dependency_blockers, priority, owner,
    queued_date, status, submit_ts, lsf_job_ids
    ```

    Rather than widening the schema with a new `notes` column
    (which would require backfill on all 8 rows and force a
    schema-evolution sub-decision), append the harvest disposition
    to the EXISTING `current_artifact` column for row M2-POST-M3-08
    (the column already documents the artifact state — this is
    semantically aligned). Use a `; HARVEST 2026-04-29:` separator
    so the original artifact path remains parseable.

    Status value: `partially_completed` is a NEW status value not
    previously used in this TSV. Document its semantics inline by
    creating a sibling legend file (TSV header comments are not
    standard so a sibling .md is the rigor-correct option per
    Carter's "rigor over speed" memory):

        .planning/m2_post_m3_rerun_status_legend.md

    Legend file content (~10-20 lines):

    ```markdown
    # m2_post_m3_rerun_queue.tsv status legend

    Documented status values for the `status` column of
    `.planning/m2_post_m3_rerun_queue.tsv`:

    | status                | meaning                                                           |
    |-----------------------|-------------------------------------------------------------------|
    | not_started           | Obligation queued; no work yet                                    |
    | in_flight             | LSF jobs submitted; outcomes pending                              |
    | partially_completed   | Some sub-tasks completed; remainder re-routed to peer obligation  |
    | completed             | All sub-tasks DONE; artifact superseded                           |

    `partially_completed` was introduced 2026-04-29 by quick task
    `260429-tq9` for row M2-POST-M3-08, where 9 of 13 LSF jobs
    completed cleanly and the 4 AFR EXITs were re-routed to
    sibling obligation M2-POST-M3-03 (AFR mtCOJO with AoU AFR LD
    panel) — already queued and `not_started` since 2026-04-27.
    The handoff is informational; M2-POST-M3-03 already covers the
    AFR re-fire scope independently.
    ```

    Then update the TSV row in place. The current row 9 reads
    (verified by Read tool earlier):

    ```
    M2-POST-M3-08	Wave-4-D4	mtCOJO production sensitivity LSF re-fire for the 13 eligible (target_trait, stratum) tuples with HM3-intersected COJO inputs (per-target ~10-30 min wall on full HM3+EUR Phase3 LD reference; 13 targets × ~30 min ≈ 6.5 hr LSF long-queue)	data/processed/mtcojo/{EUR,AFR,TRANS}/mtcojo_sensitivity.tsv (sensitivity_flag=FAIL for all 13 rows; mtcojo_p column empty)	Same paths with sensitivity_flag ∈ {PASS, WARN, FAIL} based on actual mtcojo_p vs mtag_p_original ratio + max_overlapping_intercept witness	HM3-intersected COJO input materializer (extension of src/python/build_cojo_inputs.py) + LSF long-queue allocation	high	carter	2026-04-27	in_flight	2026-04-29T20:59:43-04:00	67672,67673,67674,67675,67676,67677,67678,67679,67680,67681,67682,67683,67684
    ```

    Replace with (use Edit tool — single-row in-place modification):

    ```
    M2-POST-M3-08	Wave-4-D4	mtCOJO production sensitivity LSF re-fire for the 13 eligible (target_trait, stratum) tuples with HM3-intersected COJO inputs (per-target ~10-30 min wall on full HM3+EUR Phase3 LD reference; 13 targets × ~30 min ≈ 6.5 hr LSF long-queue)	data/processed/mtcojo/{EUR,AFR,TRANS}/mtcojo_sensitivity.tsv; HARVEST 2026-04-29: 9/13 LSF jobs DONE (5 EUR + 4 TRANS) — canonical .mtcojo.cojo at data/processed/mtcojo/{EUR,TRANS}/<target>.mtcojo.cojo + bC_pval-driven sensitivity_flag classification in EUR + TRANS sensitivity tables. 4/13 EXITed (all AFR) at GCTA freq-difference filter (78477 SNPs flagged vs 1000G AFR Phase3 N=504; sample-size insufficiency of LD ref); AFR re-fire re-routed to sibling obligation M2-POST-M3-03 (AoU AFR LD panel N≈95k); see data/processed/mtcojo/AFR/AFR_LD_INSUFFICIENT_FOR_MTCOJO.md	Same paths with sensitivity_flag ∈ {PASS, WARN, FAIL} based on actual mtcojo_p vs mtag_p_original ratio + max_overlapping_intercept witness	HM3-intersected COJO input materializer (extension of src/python/build_cojo_inputs.py) + LSF long-queue allocation	high	carter	2026-04-27	partially_completed	2026-04-29T20:59:43-04:00	67672,67673,67674,67675,67676,67677,67678,67679,67680,67681,67682,67683,67684
    ```

    Changes: (1) `current_artifact` column appends harvest disposition
    after `; HARVEST 2026-04-29:`; (2) `status` column flipped from
    `in_flight` to `partially_completed`. All other columns unchanged
    (priority stays `high`, owner stays `carter`, queued_date stays
    2026-04-27, submit_ts and lsf_job_ids unchanged for audit).

    Cross-reference: M2-POST-M3-03 row priority is currently `medium`
    — leave as-is per orchestrator constraint. The handoff is
    informational; the M2-POST-M3-03 description already scopes
    "Re-fire AFR mtCOJO with AoU AFR LD reference panel" and
    explicitly lists the 1000G AFR N=504 LD ref as the
    `current_artifact` to be superseded. No additional row edits
    needed.

    Verification (no schema corruption):

        # Row count unchanged (8 data rows + 1 header = 9)
        WC=$(wc -l < .planning/m2_post_m3_rerun_queue.tsv)
        test "$WC" = "9" || { echo "Row count $WC != 9"; exit 1; }

        # M2-POST-M3-08 row exists with new status
        grep -q "^M2-POST-M3-08	" .planning/m2_post_m3_rerun_queue.tsv \
            && grep "^M2-POST-M3-08	" .planning/m2_post_m3_rerun_queue.tsv | grep -q "partially_completed" \
            || { echo "M2-POST-M3-08 row missing or status not flipped"; exit 1; }

        # Column count preserved on M2-POST-M3-08 row (12 columns per header)
        HEADER_COLS=$(awk -F'\t' 'NR==1 {print NF}' .planning/m2_post_m3_rerun_queue.tsv)
        ROW_COLS=$(awk -F'\t' '/^M2-POST-M3-08	/ {print NF}' .planning/m2_post_m3_rerun_queue.tsv)
        test "$HEADER_COLS" = "$ROW_COLS" || { echo "Column count mismatch: header=$HEADER_COLS row=$ROW_COLS"; exit 1; }

        # Legend file exists
        test -s .planning/m2_post_m3_rerun_status_legend.md \
            && grep -q 'partially_completed' .planning/m2_post_m3_rerun_status_legend.md \
            || { echo "Legend missing or incomplete"; exit 1; }

    Atomic commit (2 explicit-path adds — Carter's GPFS rule):

        git add \
            .planning/m2_post_m3_rerun_queue.tsv \
            .planning/m2_post_m3_rerun_status_legend.md

        git commit -m "docs(quick-260429-tq9): mark M2-POST-M3-08 partially_completed (9/13 DONE; 4 AFR re-routed to M2-POST-M3-03)"
  </action>
  <verify>
    <automated>WC=$(wc -l < .planning/m2_post_m3_rerun_queue.tsv) && test "$WC" = "9" && grep "^M2-POST-M3-08	" .planning/m2_post_m3_rerun_queue.tsv | grep -q "partially_completed" && HEADER_COLS=$(awk -F'\t' 'NR==1 {print NF}' .planning/m2_post_m3_rerun_queue.tsv) && ROW_COLS=$(awk -F'\t' '/^M2-POST-M3-08	/ {print NF}' .planning/m2_post_m3_rerun_queue.tsv) && test "$HEADER_COLS" = "$ROW_COLS" && test -s .planning/m2_post_m3_rerun_status_legend.md && grep -q 'partially_completed' .planning/m2_post_m3_rerun_status_legend.md && echo OK</automated>
  </verify>
  <done>
    - .planning/m2_post_m3_rerun_queue.tsv row M2-POST-M3-08 status = `partially_completed`
    - current_artifact column appended with HARVEST 2026-04-29 disposition + handoff to M2-POST-M3-03
    - Column count preserved on edited row (12 columns matching header)
    - Row count preserved (9 lines = 1 header + 8 data rows)
    - .planning/m2_post_m3_rerun_status_legend.md created documenting `partially_completed` semantics
    - 1 atomic commit landed with 2 explicit-path adds
  </done>
</task>

</tasks>

<verification>
**End-to-end harvest verification (run after all 5 tasks complete):**

1. **Aggregator schema-aligned (Task 1):**
   ```
   /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest \
       tests/m2/test_build_mtcojo_sensitivity_table_bcpval.py -v
   ```
   Expected: 3/3 PASSED.

2. **Canonical cojo layout populated (Task 2):**
   ```
   ls data/processed/mtcojo/{EUR,TRANS}/*.mtcojo.cojo | wc -l    # 9
   ls data/processed/mtcojo/AFR/*.mtcojo.cojo 2>/dev/null | wc -l  # 0
   ```

3. **AFR LD-insufficiency disclosure present (Task 3):**
   ```
   wc -l data/processed/mtcojo/AFR/AFR_LD_INSUFFICIENT_FOR_MTCOJO.md
   grep -c 'M2-POST-M3-03' data/processed/mtcojo/AFR/AFR_LD_INSUFFICIENT_FOR_MTCOJO.md  # ≥1
   ```

4. **Sensitivity tables rebuilt with non-FAIL classification engaged (Task 4):**
   ```
   awk -F'\t' 'NR>1 && ($6=="PASS"||$6=="WARN")' \
       data/processed/mtcojo/EUR/mtcojo_sensitivity.tsv \
       data/processed/mtcojo/TRANS/mtcojo_sensitivity.tsv | wc -l  # ≥1
   grep -c 'bmi.EUR.GIANT-UKBB.2018' data/processed/mtcojo/EUR/mtcojo_sensitivity.tsv  # ≥1
   ```

5. **Obligation queue row flipped (Task 5):**
   ```
   grep '^M2-POST-M3-08	' .planning/m2_post_m3_rerun_queue.tsv | awk -F'\t' '{print $10}'  # partially_completed
   ```

6. **Atomic commit count:**
   ```
   git log --oneline | grep -c '260429-tq9'  # 6
   ```
   Expected: 6 commits across 5 tasks (Task 1 lands 2 commits — RED + GREEN).

7. **Untouched files (orchestrator constraints):**
   ```
   # m2_mtcojo.smk untouched
   git log --oneline -1 src/snakemake/rules/m2_mtcojo.smk | grep -v '260429-tq9'
   # build_cojo_inputs.py untouched
   git log --oneline -1 src/python/build_cojo_inputs.py | grep -v '260429-tq9'
   # select_mtcojo_eligible_targets.py untouched
   git log --oneline -1 src/python/select_mtcojo_eligible_targets.py | grep -v '260429-tq9'
   # bin/fire_m2_post_m3_08_mtcojo.sh untouched
   git log --oneline -1 bin/fire_m2_post_m3_08_mtcojo.sh | grep -v '260429-tq9'
   # m2p3_08 audit subdirs untouched (no files inside ever modified)
   git status --short data/processed/mtcojo/{EUR,AFR,TRANS}/m2p3_08/
   ```

8. **Original-research framing preserved across all artifacts:**
   ```
   grep -i -E '\b(fix|bug|broken|cleanup|revision|salvage)\b' \
       data/processed/mtcojo/AFR/AFR_LD_INSUFFICIENT_FOR_MTCOJO.md \
       .planning/m2_post_m3_rerun_status_legend.md \
       2>/dev/null | grep -v '^Binary'
   ```
   Expected: empty output (no forbidden framing words in user-facing artifacts).
   Note: pytest test names + commit subjects MAY contain neutral verbs like
   "align" / "schema-align" / "rebuild" / "migrate" — those are technical
   verbs not framing words, and are not subject to this grep.
</verification>

<success_criteria>
- 9 canonical-layout `.mtcojo.cojo` files (5 EUR + 4 TRANS) populated, all non-zero size, headers match GCTA v1.94.1 schema (`SNP A1 A2 freq b se p N bC bC_se bC_pval`)
- 0 canonical-layout AFR `.mtcojo.cojo` files (correct: missing-file branch fires for the 4 EXITed AFR targets)
- AFR LD-insufficiency disclosure landed at canonical AFR path with M2-POST-M3-03 handoff + 78,477-SNP witness quote
- 3 per-stratum `mtcojo_sensitivity.tsv` artifacts rebuilt:
  - EUR: ≥5 data rows including bmi.EUR.GIANT-UKBB.2018 (regression test for previously-missing-row), 6 columns
  - AFR: 4 FAIL data rows via missing-file branch, 6 columns
  - TRANS: ≥4 data rows, 7 columns (incl. trans_ld_panel_concordance="primary_only")
- ≥1 PASS or WARN row across EUR+TRANS combined (witness that bC_pval schema-alignment engaged the _classify() non-None branch)
- `tests/m2/test_build_mtcojo_sensitivity_table_bcpval.py` exists and 3/3 PASSED, pinning the bC_pval contract
- `src/python/build_mtcojo_sensitivity_table.py` line 96 reads `c.get("bC_pval", c.get("p_cojo", c.get("pC", None)))` — no other lines modified
- `.planning/m2_post_m3_rerun_queue.tsv` row M2-POST-M3-08 status = `partially_completed`, column count preserved at 12
- `.planning/m2_post_m3_rerun_status_legend.md` documents the new `partially_completed` semantic
- 6 atomic commits landed (Task 1 RED + Task 1 GREEN + Task 2 + Task 3 + Task 4 + Task 5)
- All untouched-file constraints honored (m2_mtcojo.smk, build_cojo_inputs.py, select_mtcojo_eligible_targets.py, bin/fire_m2_post_m3_08_mtcojo.sh, m2p3_08 audit subdirs all 0-line diff)
- Original-research framing preserved in all user-facing artifacts (no "fix" / "bug" / "broken" / "cleanup" / "revision" / "salvage" wording in AFR_LD_INSUFFICIENT_FOR_MTCOJO.md or status legend)
- Carter's GPFS staging rule honored (every commit uses explicit-path `git add <path>`, never `git add .` or `-A`)
- Snakemake-pinned Python (smoke_dev / Python 3.11) used for all pytest + script invocations (never miniconda3 base / Python 3.13)
</success_criteria>

<output>
After completion, create `.planning/quick/260429-tq9-harvest-m2-post-m3-08-mtcojo-per-260429-/260429-tq9-SUMMARY.md`

Summary should include:
- Frontmatter: phase=quick-260429-tq9, plan=01, status=complete, requirements=[M2-POST-M3-08], commits=6, tags=[m2, mtcojo, harvest, sensitivity-table, m2-post-m3-08, schema-alignment, partially-completed]
- One-liner: post-LSF harvest of M2-POST-M3-08 mtCOJO production fire — 9/13 DONE → canonical-layout sensitivity tables across EUR+TRANS with bC_pval schema-alignment; 4/13 AFR EXITed → re-routed to M2-POST-M3-03 with full disclosure document
- Status table: per-stratum row counts + non-FAIL counts captured from Task 4 stdout
- 6-commit log table (hash + type + subject)
- Per-stratum aggregator stdout transcript (PASS/WARN/FAIL counts)
- Verification gates (8 from <verification> section)
- Out-of-scope items: bmi.EUR-missing-from-table aggregator defect (lines 89-94 silent-skip — pre-existing, NOT addressed here), sbp.EUR/stroke.EUR rsID re-harmonization (M1 follow-up), AoU AFR LD panel build (M2-POST-M3-01/03/05 family), MTAG --fdr re-fire (M2-POST-M3-07), GWAS Catalog v_lock_M5 refresh (M2-POST-M3-06)
- Self-check section listing all files claimed + verifying their existence
</output>
