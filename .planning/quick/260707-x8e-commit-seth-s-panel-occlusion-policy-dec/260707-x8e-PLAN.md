# Quick 260707-x8e — Commit the panel-occlusion policy decision (byte-verified)

**Type:** docs-only, byte-faithful commit of an externally-authored (Seth) artifact. No code,
no perimeter, no spend. Mirrors the n11 / 0uf byte-verification pattern.

## Goal

Land Seth's authoritative policy-decision record in-repo, byte-verified against his SHA-256
anchor, as the doc that CLOSES the exclude-vs-flag open question (supersedes the verdict's
"Carter policy call" section).

## Task

1. Commit `.planning/amendments/m3_panel_occlusion_policy_decision.md` = a self-locating `>`
   header + Seth's verbatim body. **Body (header excluded) verified: SHA-256
   `42d701677ac8bc85…ab209ef`, 5247 B == Seth's anchor** (`tail -c 5247 | sha256sum`).
2. Update STATE.md: Quick Tasks Completed row.

## must_haves

- `m3_panel_occlusion_policy_decision.md` exists; its last 5247 bytes hash to Seth's anchor.
- STATE.md carries a `260707-x8e` row.
- No source code / OSF artifact / loop state changed; m3-06 stays HELD.

## Decision recorded (for coherence)

Exclude-in-lockstep (panel + sumstats) + mandatory provenance manifest. NaN→0 dead;
panel-only-exclude unsafe (orphans sumstats-present rs182965575); upstream span-filter for all
276 regions + sumstats-side drop at m3-04; OSF amendment-update = exclusion+provenance, never
zeroing. Companions: `m3_nan_conditioning_scientific_review.md` (WHY), the pending
`m3_region1_nan_geometry_verdict.md` (WHAT/mechanism), `m3_region1_occlusion_hinge_check.md`
(join-impact, `c4e0875`).
