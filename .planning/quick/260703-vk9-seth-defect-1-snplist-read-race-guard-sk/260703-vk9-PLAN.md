---
quick_id: 260703-vk9
slug: seth-defect-1-snplist-read-race-guard-sk
description: "Seth Defect 1 — snplist-read race guard + skip intersection when no monomorphic drop (run_native_ld_panel.py)"
date: 2026-07-04
mode: quick
must_haves:
  truths:
    - "The snplist read in _retained_window_bim is guarded against a transient short read, MIRRORING the sibling raw-window .bim guard (_window_bim_n_var_retry_on_zero)."
    - "When --mac 1 drops nothing (bin_n_var == raw_window_n_var, the observed AFR regime), the square path SKIPS the snplist∩.bim intersection entirely — so the race that caused the live region-1 'window .bim has 0 rows' error cannot occur there."
    - "A genuinely-empty snplist still returns 0 retained ids after the bounded retries, so the caller's byte-identical n_var mismatch ValueError still fires (no semantics loosened)."
  artifacts:
    - "src/python/run_native_ld_panel.py — _needs_retained_subset helper + expect_nonzero retry guard + conditional caller"
    - "tests/m3/test_defect1.py — 3 failing-first cases"
  key_links:
    - "src/python/run_native_ld_panel.py:352 (_retained_window_bim)"
    - "src/python/run_native_ld_panel.py:591 (process_region square branch)"
    - "src/python/run_native_ld_panel.py:114 (_WINDOW_BIM_RETRIES / _WINDOW_BIM_RETRY_SLEEP_S)"
---

# Quick Task 260703-vk9: Seth Defect 1 — snplist-read race guard

## Root cause (Seth, offline-verified)

The live region-1 failure (`.ld.bin implies 102421 but the window .bim has 0 rows`)
was a **RACE, not a logic bug**. `_retained_window_bim` read `{out_prefix}.snplist`
with a bare `read_text()` and **NO** retry-on-zero guard — unlike its sibling
raw-window `.bim` read (`_window_bim_n_var_retry_on_zero`, 27af416). It raced
plink's flush, read an un-flushed (empty) snplist → 0 retained ids → a false
`n_var` mismatch. Offline the same call returns `102421 == 102421` and the
snplist ids match the `.bim` col-2 ids byte-for-byte — the logic and join are
sound.

## Fix (Seth's Changes A/B/C — edits ONLY `src/python/run_native_ld_panel.py`)

- **A.** Add pure helper `_needs_retained_subset(bin_n_var, raw_window_n_var) -> bool`
  (returns `bin_n_var != raw_window_n_var`) immediately before `_retained_window_bim`.
- **B.** `_retained_window_bim`: add kw-only `expect_nonzero: bool = False`; after the
  snplist read, if `not retained_ids and expect_nonzero`, retry up to
  `_WINDOW_BIM_RETRIES` (sleep `_WINDOW_BIM_RETRY_SLEEP_S`), loud WARN to stderr on
  recovery. Persistently-empty still returns `[]` → caller's mismatch still fires.
- **C.** `process_region` square branch: do the snplist∩.bim intersection ONLY when
  `_needs_retained_subset(bin_n_var, raw_window_n_var)` (a real drop, with
  `expect_nonzero=(bin_n_var > 0)`); else use the (already race-guarded) raw window
  `.bim` directly. The `if bin_n_var != window_n_var:` ValueError + `n_var` + the
  `n_dropped` provenance block are UNCHANGED (n_dropped is correctly 0 in the skip path).

## Task 1 — TDD Defect 1

- **files:** `tests/m3/test_defect1.py` (new), `src/python/run_native_ld_panel.py`
- **action:** Write `tests/m3/test_defect1.py` with 3 cases (RED first: helper + param
  don't exist yet) → apply Changes A/B/C → GREEN.
- **verify:** `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/pytest -q tests/m3/`
  — full suite stays green (baseline 333 passed / 30 skipped → 336 / 30).
- **done:** 3 new cases pass; no regression; `test_plink_ld_to_npz.py` / `test_nan_guard.py`
  / `test_gate.py` and `plink_ld_to_npz.py` UNTOUCHED (o0m-authoritative).

## Out of scope (carry-forwards, do NOT fold in)

1. **NaN→0 + PSD conditioning policy** — the true region-1 substrate fix (a downstream
   `.npz`/`.rds`/pre-SuSiE step, recorded `n_zeroed` provenance). Separate backlog ticket.
2. **No loop re-fire** — the fixes are NC-State-side; a re-fire before the NaN policy lands
   still (correctly) raises on the 12 NaN cells.
