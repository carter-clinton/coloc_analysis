---
phase: 260425-wa2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/R/figures/fig_h3_ld_overlap_dose_response.R
  - docs/manuscript/figures/fig_h3_ld_overlap_dose_response.pdf
  - docs/manuscript/figures/fig_h3_ld_overlap_dose_response.png
autonomous: true
requirements:
  - AUDIT-HQ-3
user_setup: []
must_haves:
  truths:
    - "A reader can see, on a single composite figure, how SuSiE-RSS credible-set yield depends on the ld_overlap_fraction of the real 1000G EUR LD panel (Panel A, 60 EUR fits)."
    - "A reader can see, on the same composite figure, how successful QTL-coloc PP.H4 estimates depend on the GWAS-side ld_overlap_fraction at the same (region, ancestry) cell (Panel B, 32 EUR successful attempts)."
    - "Both panels show a vertical reference line at ld_overlap_fraction = 0.5 (Benner et al. 2017 calibration threshold), so a reader can locate every fit relative to that calibration boundary."
    - "Panel B additionally shows horizontal reference lines at PP.H4 = 0.5 (Tier B) and PP.H4 = 0.8 (Tier A); the FTO_16q12 EUR IRX3/Pancreas headline (PP.H4=0.3099, GWAS-side min ld_overlap_fraction=0) sits in the suspect top-left quadrant."
    - "The figure caption discloses the trait-ambiguity caveat for Panel B (qtl_coloc per-attempt JSON does not record which GWAS-side trait was the input; the script reports min ld_overlap_fraction across the 5 GWAS-side trait fits at the same region × ancestry cell as a worst-case bound)."
    - "The script renders 600-dpi PDF and PNG via cairo_pdf using the la_multitrait_r conda env, mirroring fig2/fig3/fig5 conventions."
    - "The single atomic git commit stages exactly three paths (R script + PDF + PNG); no manuscript files, no STATE.md, no FROZEN-NUMBERS.md edits."
  artifacts:
    - path: "src/R/figures/fig_h3_ld_overlap_dose_response.R"
      provides: "Builder script for the audit High-Quality #3 LD-reference-quality dose-response figure."
      min_lines: 250
    - path: "docs/manuscript/figures/fig_h3_ld_overlap_dose_response.pdf"
      provides: "Cairo-rendered PDF of the 2-panel composite (Panel A: SuSiE CS yield × ld_overlap_fraction; Panel B: QTL-coloc PP.H4 × ld_overlap_fraction)."
    - path: "docs/manuscript/figures/fig_h3_ld_overlap_dose_response.png"
      provides: "600-dpi PNG companion of the same composite for slide / preprint preview use."
  key_links:
    - from: "src/R/figures/fig_h3_ld_overlap_dose_response.R"
      to: "results/fine_mapping/susie/*.json"
      via: "list.files() + jsonlite::fromJSON() per-fit loop"
      pattern: "results/fine_mapping/susie"
    - from: "src/R/figures/fig_h3_ld_overlap_dose_response.R"
      to: "results/fine_mapping/finemap_summary.tsv"
      via: "readr::read_tsv() of the Stage 2 production-fire summary"
      pattern: "finemap_summary.tsv"
    - from: "src/R/figures/fig_h3_ld_overlap_dose_response.R"
      to: "results/qtl_coloc/*.json"
      via: "list.files() + jsonlite::fromJSON() per-attempt loop, filtered to status=success"
      pattern: "results/qtl_coloc"
    - from: "src/R/figures/fig_h3_ld_overlap_dose_response.R"
      to: "docs/manuscript/figures/fig_h3_ld_overlap_dose_response.{pdf,png}"
      via: "ggsave() with cairo_pdf + 600-dpi PNG"
      pattern: "ggsave\\(.*cairo_pdf"
  diagnostic_scalars:
    - "N_REAL_LD_FITS = 96 (cross-check vs nrow(finemap_summary.tsv))"
    - "N_REAL_LD_NONEMPTY = 51 (cross-check vs sum(credible_sets > 0))"
    - "N_REAL_LD_EUR_WITH_OVERLAP = 60 (Panel A data; the 36 AFR fits have ld_overlap_fraction = NULL because no AFR LD panel is loaded)"
    - "N_QTL_SUCCESS = 32 (cross-check vs sum(status == 'success') across qtl_coloc/*.json)"
    - "BENNER_THRESHOLD = 0.5"
    - "TIER_B_THRESHOLD = 0.5; TIER_A_THRESHOLD = 0.8"
    - "FTO_HEADLINE_PPH4 = 0.3099 (annotated point in Panel B)"
    - "N_EUR_FITS_BELOW_BENNER (derived at runtime; emitted as diagnostic; not yet in TRACK-A-FROZEN-NUMBERS.md — orchestrator will record after first render)"
---

<objective>
Build the audit High-Quality #3 LD-reference-quality dose-response figure: a 2-panel composite that visualizes (Panel A) SuSiE-RSS credible-set yield as a function of `ld_overlap_fraction` across the 60 EUR real-LD region × trait fits, and (Panel B) successful QTL-coloc PP.H4 as a function of GWAS-side `ld_overlap_fraction` across the 32 EUR successful qtl_coloc attempts. The figure answers the audit-author's "what LD-reference quality is needed for a coloc.susie PP.H4 to be trustworthy?" question on the disk truth that already exists at `results/fine_mapping/susie/*.json` and `results/qtl_coloc/*.json`.

Purpose: Address Carter's audit Eval 3.3 (FTO_16q12 EUR ld_overlap_fraction=0 disclosure) at the figure level — extending the per-row Tier-C-table disclosure that quick-260425-kki already published in the manuscript prose to a dose-response visualization across all admissible fits. The figure is built at the level the audit specifically requested ("plot PP.H4 vs ld_overlap_fraction to show the dose–response of LD-reference quality on the inferred coloc signal"); manuscript caption integration (slot decision: Fig 4 reclaim vs new Fig 6 vs supplementary) is OUT OF SCOPE and will be handled in a separate /gsd-quick after Carter reviews the rendered composite.

Output: One R builder script (~280 lines) + cairo_pdf (~25–60 KB) + 600-dpi PNG (~150–400 KB) at the canonical figure paths. One atomic commit staging exactly those three files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/amendments/AUDIT-REVIEW-2026-04-25.md
@.planning/amendments/TRACK-A-FROZEN-NUMBERS.md
@src/R/figures/fig2_cs_yield.R
@src/R/figures/fig3_sh2b3_eur_collapse_forest.R
@src/R/figures/fig5_variant_mech_scorecard.R
@results/fine_mapping/finemap_summary.tsv
@results/fine_mapping/susie/asthma.EUR.SH2B3_12q24.json
@results/qtl_coloc/FTO_16q12_ENSG00000177508_gtex_eqtl_Pancreas.json
@results/qtl_coloc/tier_assignments.tsv

<interfaces>
<!--
Key data shapes the executor needs. Extracted from disk truth (recon by orchestrator + planner).
Executor does not need to grep for column names or JSON keys — they are below.
-->

## results/fine_mapping/finemap_summary.tsv (96 rows + header; tab-separated)
Columns (verified from `head -1`):
```
trait  ancestry  method  region_id  status  credible_sets  credible_set_sizes
variants_in_cs  pip_nonzero  top_chr  top_pos  top_pip  top_beta  top_se
sumstats  ld_dir  output_path
```
Notes:
- `credible_sets` is the integer count (not a list) — used for Panel A's y-axis.
- `status` ∈ {ok, too_many_variants, non_converged, no_variants} — disk-verified counts: 48 / 24 / 18 / 6.
- DOES NOT contain ld_overlap_fraction; that field is in the per-fit JSONs.

## results/fine_mapping/susie/{trait}.{ancestry}.{region_id}.json (96 files, one per fit)
Top-level keys actually used by this builder:
```r
trait                # e.g. "asthma"
ancestry             # "EUR" | "AFR"
region_id            # e.g. "SH2B3_12q24"
status               # "ok" | "too_many_variants" | "non_converged" | "no_variants"
ld_status            # diagnostic string e.g. "ld_loaded;partial_overlap;147;0.038"
ld_overlap           # integer count of overlapping variants (e.g. 147)
ld_overlap_fraction  # numeric in [0, 1]; PRESENT for EUR fits (60), MISSING for AFR fits (36) since no AFR panel loaded
credible_sets        # named list (CS1, CS2, …) of variant arrays; length() gives n_CS
convergence_status   # may be "converged_primary", "non_converged", etc. (also exposed at status)
L_used / L_saturated # SuSiE-RSS L diagnostic fields
```

## results/qtl_coloc/{region}_{gene_id}_{qtl_source}_{tissue}.json (1,274 files)
Status distribution (disk-verified): 1005 too_few_snps / 235 no_qtl_cs / 32 success / 2 qtl_susie_failed.
Top-level keys actually used by this builder:
```r
status         # filter to "success" — 32 attempts
region         # e.g. "FTO_16q12" (matches region_id in SuSiE JSONs)
ancestry       # "EUR" | "AFR" — disk-verified all 32 successes are EUR
gene_id        # ENSG…
tissue         # e.g. "Pancreas"
qtl_source     # "gtex_eqtl" | "gtex_sqtl"
n_snps_overlap # integer
summary$PP.H4.abf  # 0–1; the y-axis for Panel B
```
**Critical absence:** there is NO `trait` field. The qtl_coloc per-attempt JSON does not record which of the 5 GWAS-side trait SuSiE fits was the input. This is the trait-ambiguity caveat — see Panel B disclosure rules in the action block.

## results/qtl_coloc/tier_assignments.tsv (234 rows; informational only — not joined into figure)
Used only as a sanity check that the FTO_16q12 EUR IRX3 / Pancreas point is the unique 0.3099 row.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author fig_h3_ld_overlap_dose_response.R builder script</name>
  <files>src/R/figures/fig_h3_ld_overlap_dose_response.R</files>
  <behavior>
    The script must produce a 2-panel composite via patchwork that mirrors fig5_variant_mech_scorecard.R conventions (jsonlite multi-JSON parse, cairo_pdf render, 600-dpi PNG, locked-scalar + assertion pattern, diagnostic stdout block at end).

    Panel A — SuSiE CS yield × ld_overlap_fraction:
      - Data: join `finemap_summary.tsv` (for credible_sets count + status) to per-fit SuSiE JSONs at results/fine_mapping/susie/*.json (for ld_overlap_fraction). Filter to fits where ld_overlap_fraction is non-NULL — disk truth = 60 EUR fits (the 36 AFR fits have ld_overlap_fraction=NULL because no AFR LD panel is loaded; this is documented in TRACK-A-FROZEN-NUMBERS.md "Scope caveat" block).
      - x-axis = ld_overlap_fraction (0–1)
      - y-axis = credible_sets (capped visually at 10; SuSiE-RSS L=10 ceiling)
      - color = status (ok / too_many_variants / non_converged / no_variants — 4 levels with a stable palette; reuse fig3's #3B6AA0 for "ok" and #C0392B for "non_converged" for visual coherence)
      - Vertical reference line at ld_overlap_fraction = 0.5 (Benner et al. 2017 AJHG calibration threshold — cited in caption)
      - Annotate (with ggrepel or geom_text) two specific points: SH2B3_12q24 EUR asthma (the lone "ok" SH2B3 EUR fit at ld_overlap_fraction = 0.0385) and FTO_16q12 EUR fits (the headline-Tier-C locus where ld_overlap_fraction = 0). Use small italicized labels.
      - Title: "SuSiE-RSS credible-set yield as a function of LD-reference overlap"
      - Subtitle: "All 60 EUR real-LD region × trait fits with measured ld_overlap_fraction; reference at Benner 2017 0.5 calibration threshold"

    Panel B — QTL-coloc PP.H4 × GWAS-side ld_overlap_fraction (worst-case bound):
      - Data: read all 1,274 results/qtl_coloc/*.json, filter to status == "success" (32 rows; all EUR per disk truth). For each (region, ancestry) cell, compute `min_ld_overlap_fraction` = min across the 5 GWAS-side trait SuSiE fits at the same (region_id, ancestry). Join this min to each qtl_coloc success row by (region, ancestry).
      - x-axis = min_ld_overlap_fraction across the 5 GWAS-side trait fits (worst-case LD-quality bound)
      - y-axis = summary$PP.H4.abf (0–1)
      - color = qtl_source (gtex_eqtl, gtex_sqtl) — 2 levels (alternative: shape by qtl_source if all 32 are gtex_eqtl on disk; check at runtime and pick whichever gives more information)
      - Reference vertical line at ld_overlap_fraction = 0.5 (Benner threshold)
      - Reference horizontal lines at PP.H4 = 0.5 (Tier B, dashed orange #D95F02) and PP.H4 = 0.8 (Tier A, dashed purple #7570B3) — reuse fig5 palette
      - Annotate the FTO_16q12 EUR IRX3 / Pancreas point (PP.H4=0.3099, min_ld_overlap_fraction expected = 0) with a labelled arrow / repel callout
      - Title: "QTL-coloc PP.H4 as a function of GWAS-side LD-reference overlap"
      - Subtitle: "32 successful QTL-coloc attempts (status=success); GWAS-side ld_overlap_fraction = min across 5 trait fits at the same region × ancestry cell"

    Composite layout:
      - Use patchwork: `panel_a / panel_b` (vertical stack) with plot_layout(heights = c(1, 1))
      - Plot dimensions: 170 × 200 mm cairo_pdf + 600-dpi PNG (taller than fig3's 180×160 to give two scatters room to breathe)
      - plot_annotation(caption = ...) — see caption text below.

    In-figure caption text (assembled via paste0 in plot_annotation):
      "Figure H3. LD-reference-quality dose-response on coloc inference at admissible Track A loci.
       Panel A — SuSiE-RSS credible-set yield × ld_overlap_fraction. Each point = one EUR (region, trait) SuSiE-RSS fit (n=60; the 36 AFR fits are excluded because no AFR LD panel was loaded — see TRACK-A-FROZEN-NUMBERS.md scope caveat). Color = SuSiE status. Vertical reference line at ld_overlap_fraction = 0.5 (Benner et al. 2017 AJHG calibration threshold).
       Panel B — QTL-coloc PP.H4 × GWAS-side ld_overlap_fraction. Each point = one successful (region, gene, tissue, qtl_source) QTL-coloc attempt (n=32, all EUR). x-axis = MIN ld_overlap_fraction across the 5 GWAS-side trait fits at the same (region, ancestry) cell — a conservative worst-case bound, because the qtl_coloc per-attempt JSON does not record which trait's GWAS-side SuSiE fit was the input. The headline FTO_16q12 EUR IRX3/Pancreas Tier-C signal (PP.H4 = 0.3099, summary block at hit2 = rs11075995) sits at min ld_overlap_fraction = 0 — the structural inflation flag.
       Sources: results/fine_mapping/finemap_summary.tsv + results/fine_mapping/susie/*.json (Stage 2 production fire 2026-04-22); results/qtl_coloc/*.json (per-attempt 2026-04-22 fire); reference threshold from Benner et al. 2017 AJHG 101:539–551; tier thresholds from .planning/amendments/TRACK-A-FROZEN-NUMBERS.md."

    Locked scalars block (mirroring fig2/fig3 hard-fail discipline; cite TRACK-A-FROZEN-NUMBERS.md as authoritative ledger):
      - N_REAL_LD_FITS_TOTAL <- 96L
      - N_REAL_LD_NONEMPTY <- 51L
      - N_QTL_SUCCESS <- 32L
      - BENNER_THRESHOLD <- 0.5
      - TIER_B_THRESHOLD <- 0.5
      - TIER_A_THRESHOLD <- 0.8
      - FTO_HEADLINE_PPH4 <- 0.3099  (cross-checked against the FTO_16q12 EUR IRX3 Pancreas qtl_coloc JSON)
    Each scalar gets a stopifnot() with an informative fail message that names TRACK-A-FROZEN-NUMBERS.md as the source-of-truth.

    Diagnostic stdout block at end of script (read by Task 2 verification):
      - Print N_REAL_LD_FITS_TOTAL parsed, EUR vs AFR counts, status distribution, n with ld_overlap_fraction non-NULL (expect 60), n below Benner threshold (DERIVED, no locked scalar yet — this is N_EUR_FITS_BELOW_BENNER, surfaced for orchestrator to add to FROZEN-NUMBERS.md in a follow-on commit).
      - Print N_QTL_SUCCESS parsed, FTO IRX3 Pancreas PP.H4 + min_ld_overlap_fraction at FTO_16q12 EUR (expect 0.3099 and 0 respectively), SH2B3 asthma EUR ld_overlap_fraction at SH2B3_12q24 (expect 0.0385).
      - Print output PDF + PNG paths and byte sizes.

    Render env: /rs1/researchers/c/ckclinto/conda_envs/la_multitrait_r/bin/Rscript. Carter has feedback_no_conda memory — call binary directly, do NOT instruct conda activate.

    Required libraries (all pre-installed per fig5/fig3 precedents): jsonlite, readr, dplyr, tidyr, ggplot2, patchwork, scales. Optionally ggrepel for callouts (verify availability with requireNamespace at script top; if absent, fall back to geom_text with carefully chosen hjust/vjust + nudge_x/nudge_y).

    Header comment block (mirror fig2/fig3/fig5 structure):
      - Title: "fig_h3_ld_overlap_dose_response.R — Track A audit High-Quality #3 LD-reference-quality dose-response figure"
      - Purpose: 4-line summary citing AUDIT-REVIEW-2026-04-25.md High-Quality #3 verbatim recommendation.
      - Provenance: cite AUDIT-REVIEW-2026-04-25.md High-Quality #3 + the disk-truth files used.
      - Outputs: paths + render env + invocation.
      - Author: "Carter K. Clinton — 2026-04-25 (quick-260425-wa2)"
      - Filename neutral: this is the audit's "H3" tag; no figure-number assignment to the manuscript yet (slot decision deferred to follow-on /gsd-quick per orchestrator brief).

    Framing discipline (per feedback_original_research_framing user memory): zero "revision/cleanup/fix-up/mistake/v1/simplified/placeholder/TBD" tokens in script comments or caption text. The framing is "the audit identified an under-explored disk-truth question (the dose-response of LD-quality on coloc inference); we built the figure that answers it." Use neutral language: "extending", "surfacing", "visualizing", "answering".
  </behavior>
  <action>
    Write a new R script at `src/R/figures/fig_h3_ld_overlap_dose_response.R` (~280 lines).

    Skeleton (follow exactly; populate per behavior block above):

    1. Header comment block (~50 lines) — title, purpose, audit-citation provenance, data sources, outputs, render env, invocation, author, framing discipline.
    2. `suppressPackageStartupMessages({ library(...) })` — jsonlite, readr, dplyr, tidyr, ggplot2, patchwork, scales. ggrepel optional via requireNamespace.
    3. Locked scalars block with stopifnot assertions citing TRACK-A-FROZEN-NUMBERS.md.
    4. Path constants: REAL_TSV / SUSIE_DIR / QTL_DIR / OUT_DIR / OUT_PDF / OUT_PNG.
    5. Helper: `load_susie_overlap_record(json_path)` — reads JSON, returns tibble row with trait/ancestry/region_id/status/ld_overlap_fraction (NA if NULL)/credible_sets count.
    6. Helper: `load_qtl_success_record(json_path)` — reads JSON, returns NULL if status != "success", else tibble row with region/ancestry/gene_id/tissue/qtl_source/PP.H4.
    7. Read finemap_summary.tsv via readr; assert nrow == 96; assert sum(credible_sets > 0) == 51.
    8. Walk results/fine_mapping/susie/*.json (96 files); bind_rows to assemble susie_disclosure tibble. Cross-check: 60 EUR fits with non-NA ld_overlap_fraction; 36 AFR with NA.
    9. Walk results/qtl_coloc/*.json (1,274 files); bind_rows successes only (filter at the helper). Cross-check: nrow == 32; all ancestry == "EUR".
    10. Compute per-(region_id, ancestry) `min_ld_overlap_fraction` from susie_disclosure (group_by + summarise(min(ld_overlap_fraction, na.rm=TRUE))).
    11. Left-join the qtl_success tibble to that min via (region == region_id, ancestry == ancestry).
    12. Cross-check: FTO_16q12 EUR IRX3 / Pancreas / gtex_eqtl row exists with PP.H4 within 0.0001 of 0.3099; min_ld_overlap_fraction at FTO_16q12 EUR is 0 (or near-zero). Hard-fail with informative message if not.
    13. Build Panel A (panel_a, ggplot scatter as specified).
    14. Build Panel B (panel_b, ggplot scatter as specified).
    15. Compose via `composite <- panel_a / panel_b + plot_layout(heights = c(1, 1)) + plot_annotation(caption = ...)`.
    16. Render: cairo capability assert; ggsave PDF (170 × 200 mm, cairo_pdf); ggsave PNG (170 × 200 mm, 600 dpi).
    17. Diagnostic stdout block — print all the scalars + annotated point coordinates + output byte sizes per the behavior block.

    Code quality: every block has a `# --- {section name} ---` header; library imports at top inside suppressPackageStartupMessages; helper functions before the main flow; no global state mutation; no source() of other scripts.

    DO NOT modify any other file. DO NOT modify .planning/STATE.md, .planning/amendments/TRACK-A-FROZEN-NUMBERS.md, or .planning/config.json. The N_EUR_FITS_BELOW_BENNER scalar surfaced by the diagnostic block is for orchestrator follow-on; the executor does NOT update FROZEN-NUMBERS.md in this commit (per user constraints).
  </action>
  <verify>
    <automated>test -f src/R/figures/fig_h3_ld_overlap_dose_response.R &amp;&amp; wc -l src/R/figures/fig_h3_ld_overlap_dose_response.R | awk '$1 &gt;= 200 &amp;&amp; $1 &lt;= 450 {exit 0} {exit 1}' &amp;&amp; grep -q "ld_overlap_fraction" src/R/figures/fig_h3_ld_overlap_dose_response.R &amp;&amp; grep -q "BENNER_THRESHOLD" src/R/figures/fig_h3_ld_overlap_dose_response.R &amp;&amp; grep -q "cairo_pdf" src/R/figures/fig_h3_ld_overlap_dose_response.R &amp;&amp; grep -q "patchwork" src/R/figures/fig_h3_ld_overlap_dose_response.R &amp;&amp; grep -q "AUDIT-REVIEW-2026-04-25" src/R/figures/fig_h3_ld_overlap_dose_response.R &amp;&amp; ! grep -iE "revision|cleanup|fix-up|mistake|v1|simplified|placeholder|TBD" src/R/figures/fig_h3_ld_overlap_dose_response.R</automated>
  </verify>
  <done>
    The R script exists at `src/R/figures/fig_h3_ld_overlap_dose_response.R`; is between 200 and 450 lines; references `ld_overlap_fraction`, `BENNER_THRESHOLD`, `cairo_pdf`, `patchwork`, and the AUDIT-REVIEW-2026-04-25 source citation; contains zero scope-reduction tokens. The script has not yet been run.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render the composite figure to PDF + PNG</name>
  <files>docs/manuscript/figures/fig_h3_ld_overlap_dose_response.pdf, docs/manuscript/figures/fig_h3_ld_overlap_dose_response.png</files>
  <action>
    Run the script via the la_multitrait_r conda env Rscript binary FROM THE PROJECT ROOT (paths in the script are repo-relative, mirroring fig2/fig3/fig5):

    ```
    /rs1/researchers/c/ckclinto/conda_envs/la_multitrait_r/bin/Rscript src/R/figures/fig_h3_ld_overlap_dose_response.R 2>&amp;1 | tee /tmp/fig_h3_render.log
    ```

    Expected stdout (capture for SUMMARY):
      - "Total real-LD fits parsed: 96 (expected 96)"
      - "EUR fits with measured ld_overlap_fraction: 60"
      - "AFR fits (no measured ld_overlap_fraction): 36"
      - "Status distribution: ok=48 too_many_variants=24 non_converged=18 no_variants=6"
      - "EUR fits below Benner threshold (ld_overlap_fraction &lt; 0.5): N_EUR_FITS_BELOW_BENNER" — record this number for the orchestrator follow-on commit; on disk pre-render this is approximately 39 across all 96 (39 below + 27 ≥0.5 + 30 NULL); restricted to EUR (60 fits) the expected count is 39 - 0 = 39 if all NULL fits are AFR (verify at runtime).
      - "Total qtl_coloc successes parsed: 32 (expected 32; all EUR)"
      - "FTO_16q12 EUR IRX3/Pancreas: PP.H4=0.3099 min_ld_overlap_fraction=0"
      - "SH2B3_12q24 EUR asthma: ld_overlap_fraction=0.0385"
      - "wrote docs/manuscript/figures/fig_h3_ld_overlap_dose_response.pdf (XXXXX bytes)"
      - "wrote docs/manuscript/figures/fig_h3_ld_overlap_dose_response.png (XXXXX bytes)"

    Hard-fail conditions to address by editing the script + re-running (NOT by force-passing):
      - cairo capability missing → script aborts (do not fall back to pdf()).
      - PP.H4 cross-check fails → audit the FTO JSON parse; do not relax the assertion.
      - File-size sanity gate fails (PDF &lt; 10 KB or PNG &lt; 50 KB) → almost certainly a render error or empty data join; debug.

    Do not edit any other file. Do not stage anything yet (the commit is Task 3).
  </action>
  <verify>
    <automated>test -f docs/manuscript/figures/fig_h3_ld_overlap_dose_response.pdf &amp;&amp; test -f docs/manuscript/figures/fig_h3_ld_overlap_dose_response.png &amp;&amp; test $(stat -c%s docs/manuscript/figures/fig_h3_ld_overlap_dose_response.pdf) -ge 10000 &amp;&amp; test $(stat -c%s docs/manuscript/figures/fig_h3_ld_overlap_dose_response.png) -ge 50000 &amp;&amp; file docs/manuscript/figures/fig_h3_ld_overlap_dose_response.pdf | grep -q "PDF document"</automated>
  </verify>
  <done>
    Both PDF and PNG output files exist at `docs/manuscript/figures/fig_h3_ld_overlap_dose_response.{pdf,png}`. PDF ≥ 10,000 bytes; PNG ≥ 50,000 bytes. PDF is recognized as a valid PDF by `file(1)`. The render log records expected diagnostic scalars (96 fits, 60 EUR with ld_overlap, 32 qtl successes, FTO 0.3099 / 0, SH2B3 asthma 0.0385). Render log is captured at `/tmp/fig_h3_render.log` for the SUMMARY scribe.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Single atomic commit staging exactly the 3 load-bearing files</name>
  <files>(staged paths: src/R/figures/fig_h3_ld_overlap_dose_response.R, docs/manuscript/figures/fig_h3_ld_overlap_dose_response.pdf, docs/manuscript/figures/fig_h3_ld_overlap_dose_response.png)</files>
  <action>
    Stage the 3 files explicitly by path (NEVER use `git add -A` or `git add .` — pre-existing dirty files at `.claude/settings.json`, `.planning/STATE.md`, `.planning/config.json`, `.claude/scheduled_tasks.lock` must NOT be staged):

    ```
    git add \
      src/R/figures/fig_h3_ld_overlap_dose_response.R \
      docs/manuscript/figures/fig_h3_ld_overlap_dose_response.pdf \
      docs/manuscript/figures/fig_h3_ld_overlap_dose_response.png
    ```

    Verify staging is correct (exactly 3 files, none of the dirty pre-existing files):

    ```
    git diff --cached --name-only | sort
    ```

    Expect output (sorted):
    ```
    docs/manuscript/figures/fig_h3_ld_overlap_dose_response.pdf
    docs/manuscript/figures/fig_h3_ld_overlap_dose_response.png
    src/R/figures/fig_h3_ld_overlap_dose_response.R
    ```

    Commit with this exact message (HEREDOC; no trailing co-author line per Carter's commit style — match the recent quick-260425-* commits which do not include Co-Authored-By):

    ```
    git commit -m "$(cat &lt;&lt;'EOF'
    docs(quick-260425-wa2): build audit High-Quality #3 LD-reference-quality dose-response figure

    Builds the dose-response figure that AUDIT-REVIEW-2026-04-25.md High-Quality #3
    specifically asked for: a 2-panel composite visualizing how SuSiE-RSS credible-set
    yield (Panel A, 60 EUR fits) and QTL-coloc PP.H4 (Panel B, 32 EUR successes)
    depend on the ld_overlap_fraction of the real 1000G EUR LD panel.

    Panel A — SuSiE-RSS credible-set yield x ld_overlap_fraction across the 60 EUR
    real-LD region x trait fits. The 36 AFR fits are excluded because no AFR LD
    panel was loaded (TRACK-A-FROZEN-NUMBERS.md scope caveat). Vertical reference
    line at ld_overlap_fraction = 0.5 (Benner et al. 2017 AJHG calibration
    threshold). Color = SuSiE status (ok / too_many_variants / non_converged /
    no_variants). Annotated points: SH2B3_12q24 EUR asthma (ld_overlap_fraction =
    0.0385, the lone "ok" SH2B3 EUR fit) and FTO_16q12 EUR (ld_overlap_fraction = 0,
    the headline-Tier-C locus).

    Panel B — QTL-coloc PP.H4 x GWAS-side min ld_overlap_fraction across the 32
    successful qtl_coloc attempts (all EUR per disk truth). Because the qtl_coloc
    per-attempt JSON does not record which trait's GWAS-side SuSiE fit was the
    input, the x-axis uses the MIN ld_overlap_fraction across the 5 GWAS-side
    trait fits at the same (region, ancestry) cell — a conservative worst-case
    bound, disclosed in the in-figure caption. Reference vertical line at
    ld_overlap_fraction = 0.5 (Benner threshold); reference horizontal lines at
    PP.H4 = 0.5 (Tier B) and 0.8 (Tier A). Annotated point: FTO_16q12 EUR IRX3 /
    Pancreas / gtex_eqtl (PP.H4 = 0.3099, min ld_overlap_fraction = 0) — sits in
    the suspect top-left quadrant where headline posterior probability is highest
    yet GWAS-side LD-quality is lowest, the structural inflation flag the audit
    surfaced.

    Filename uses the audit's neutral "H3" tag; manuscript slot assignment (Fig 4
    reclaim vs new Fig 6 vs supplementary) is deferred to a follow-on /gsd-quick
    after Carter reviews the rendered composite. Mirrors quick-260425-1vy /
    quick-260425-ie0 build-then-integrate cadence.

    Disk-truth scalars cross-checked at runtime (hard-fail on drift):
      - 96 real-LD fits (cross-check vs nrow(finemap_summary.tsv))
      - 51 / 96 non-empty credible sets (Stage 2 production fire 2026-04-22)
      - 60 EUR fits with measured ld_overlap_fraction (Panel A data)
      - 32 successful qtl_coloc attempts, all EUR (Panel B data)
      - FTO_16q12 EUR IRX3 / Pancreas: PP.H4 = 0.3099, min_ld_overlap_fraction = 0
      - SH2B3_12q24 EUR asthma: ld_overlap_fraction = 0.0385

    Files staged in this atomic commit (3):
      - src/R/figures/fig_h3_ld_overlap_dose_response.R
      - docs/manuscript/figures/fig_h3_ld_overlap_dose_response.pdf
      - docs/manuscript/figures/fig_h3_ld_overlap_dose_response.png

    No manuscript edits in this commit. No FROZEN-NUMBERS.md edits. No STATE.md
    edits. The N_EUR_FITS_BELOW_BENNER scalar surfaced by the script's diagnostic
    block is recorded in the SUMMARY for orchestrator follow-on.

    Sources: AUDIT-REVIEW-2026-04-25.md High-Quality #3; results/fine_mapping/
    finemap_summary.tsv + per-fit results/fine_mapping/susie/*.json (Stage 2
    2026-04-22); results/qtl_coloc/*.json (per-attempt 2026-04-22 fire); Benner
    et al. 2017 AJHG 101:539-551 (calibration threshold); .planning/amendments/
    TRACK-A-FROZEN-NUMBERS.md (locked scalars).
    EOF
    )"
    ```

    Then verify the commit landed:

    ```
    git log -1 --stat
    ```

    Expect: 3 files changed, no other paths.
  </action>
  <verify>
    <automated>git log -1 --name-only --pretty=format: | sort | grep -v '^$' | tr '\n' ' ' | grep -qE "docs/manuscript/figures/fig_h3_ld_overlap_dose_response.pdf docs/manuscript/figures/fig_h3_ld_overlap_dose_response.png src/R/figures/fig_h3_ld_overlap_dose_response.R" &amp;&amp; test $(git log -1 --name-only --pretty=format: | grep -c '^.') -eq 3 &amp;&amp; git log -1 --pretty=%s | grep -q "docs(quick-260425-wa2): build audit High-Quality #3 LD-reference-quality dose-response figure"</automated>
  </verify>
  <done>
    The HEAD commit message subject is `docs(quick-260425-wa2): build audit High-Quality #3 LD-reference-quality dose-response figure`. The HEAD commit changes exactly 3 files: the R script, the PDF, and the PNG. No pre-existing dirty files (.claude/settings.json, .planning/STATE.md, .planning/config.json, .claude/scheduled_tasks.lock) are included. `git status` after the commit still shows those 4 files as M / ?? (untouched).
  </done>
</task>

</tasks>

<verification>
End-to-end verification (composes the per-task gates above):

1. **R script exists, well-formed:** `wc -l src/R/figures/fig_h3_ld_overlap_dose_response.R` returns 200–450 lines; grep finds `ld_overlap_fraction`, `BENNER_THRESHOLD`, `cairo_pdf`, `patchwork`, `AUDIT-REVIEW-2026-04-25`; grep does NOT find any of `revision|cleanup|fix-up|mistake|v1|simplified|placeholder|TBD` (case-insensitive).

2. **Render artifacts exist + valid:** `file docs/manuscript/figures/fig_h3_ld_overlap_dose_response.pdf` reports "PDF document"; PDF size ≥ 10 KB; PNG size ≥ 50 KB.

3. **Locked-scalar discipline preserved:** the script aborts with an informative message if disk-derived counts ever drift from N_REAL_LD_FITS_TOTAL=96, N_REAL_LD_NONEMPTY=51, N_QTL_SUCCESS=32, FTO_HEADLINE_PPH4=0.3099, SH2B3 EUR asthma ld_overlap_fraction=0.0385.

4. **Commit hygiene:** HEAD commit has exactly 3 files staged (R script + PDF + PNG); commit subject matches the prescribed `docs(quick-260425-wa2): ...` form; no pre-existing dirty files swept into the commit; `git status` post-commit still shows `.claude/settings.json`, `.planning/STATE.md`, `.planning/config.json`, `.claude/scheduled_tasks.lock` as untouched.

5. **Render log captured:** `/tmp/fig_h3_render.log` contains the diagnostic stdout block that the SUMMARY scribe will lift verbatim — including the runtime-derived `N_EUR_FITS_BELOW_BENNER` scalar (the new scalar surfaced by this build that the orchestrator will add to TRACK-A-FROZEN-NUMBERS.md in a follow-on commit).
</verification>

<success_criteria>
- `src/R/figures/fig_h3_ld_overlap_dose_response.R` exists, runs cleanly under la_multitrait_r Rscript, and produces a valid 2-panel composite cairo_pdf + 600-dpi PNG.
- The figure visualizes the audit-author's High-Quality #3 dose-response question on the disk truth that already exists at `results/fine_mapping/susie/*.json` (60 EUR fits with measured ld_overlap_fraction) and `results/qtl_coloc/*.json` (32 successful EUR attempts).
- All locked scalars cross-check against disk; the FTO_16q12 EUR IRX3 / Pancreas point is correctly annotated at PP.H4=0.3099 / min_ld_overlap_fraction=0; the SH2B3_12q24 EUR asthma point is correctly annotated at ld_overlap_fraction=0.0385.
- One atomic commit at HEAD staging exactly 3 files; subject matches `docs(quick-260425-wa2): build audit High-Quality #3 LD-reference-quality dose-response figure`; pre-existing dirty files preserved untouched.
- Manuscript caption integration is OUT OF SCOPE — explicitly deferred to a follow-on /gsd-quick after Carter reviews the rendered composite.
- Framing discipline upheld: zero "revision/cleanup/fix-up/mistake/v1/simplified/placeholder/TBD" tokens anywhere in the R script comments, caption, or commit message.
</success_criteria>

<output>
After completion, create `.planning/quick/260425-wa2-audit-h3-ld-overlap-dose-response-figure/260425-wa2-SUMMARY.md` recording:
- HEAD commit SHA + subject
- The 3 files staged
- The runtime-derived `N_EUR_FITS_BELOW_BENNER` scalar (new — orchestrator will record in FROZEN-NUMBERS.md follow-on)
- The render log diagnostic block verbatim (96 fits, 60 EUR with overlap, 32 qtl successes, FTO 0.3099/0, SH2B3 asthma 0.0385)
- File byte sizes (PDF + PNG) so reviewers can confirm render quality
- Slot-decision deferral note: "Manuscript caption integration deferred to follow-on /gsd-quick after Carter reviews the rendered composite. Candidate slots: Fig 4 reclaim, new Fig 6, or supplementary — to be decided based on figure-roster fit."
</output>
