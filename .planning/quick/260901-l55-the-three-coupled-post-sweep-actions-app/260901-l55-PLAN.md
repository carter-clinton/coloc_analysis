---
phase: 260901-l55
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [A1-apply-parked-patch, A2-rescope-step0-gate, A3-delete-1b-deferral, A4-land-deferred-secondary-test, A5-record]
files_modified:
  - .planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md
  - src/python/pairwise_completeness_scan.py
  - src/python/pcs_panelwide_reclassify.py
  - tests/m3/test_pairwise_completeness_scan.py
  - tests/m3/test_pcs_panelwide_reclassify.py
  - .planning/debug/260901-l55-step0-gate-rescope-off-the-byte-proxy.md
  - .planning/STATE.md

must_haves:
  truths:
    - "The false claim at src/python/pairwise_completeness_scan.py:122 is GONE and the corrected ANCHOR-RELATIVE semantics are in its place."
    - "The runbook STEP 0 gate no longer pins the scanner by whole-file md5 or byte size; it pins the scanner's CODE against an immutable git ref, unparsed on BOTH sides by the SAME interpreter in the SAME process."
    - "A docstring/comment-only correction to the scanner costs NOTHING: the rescoped gate is GREEN across the parked patch (MEASURED, both sides 540 code lines)."
    - "A one-character CODE change to the scanner turns the rescoped gate RED, with a first-difference message naming the moved line (MEASURED before the gate is trusted)."
    - "The §(1b) deferral note in pcs_panelwide_reclassify.py is deleted; the TRUE semantics it carried survive in §(1) and are still machine-enforced against the scanner ON DISK."
    - "No pre-registered number moves: 353089/353090, 15 rows, 13 pairs, 10/3, offsets {-14:1,-9:1,-6:1,-3:1,-1:1,0:10}, panel-wide 12/1 pairs and 14/1 rows are untouched."
    - "The full tests/m3 suite is GREEN and reconciles component-exact against the 1167/33/0 baseline."
  artifacts:
    - path: ".planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md"
      provides: "STEP 0 gate check (ii) rescoped to a git-ref CODE pin; (iii) demoted to a FIELD RECORD; HOW TO REGENERATE rewritten"
      contains: "SCANNER_CODE_REF"
    - path: "src/python/pairwise_completeness_scan.py"
      provides: "corrected RETAINED-SET PARITY docstring (md5 fc1d68dff1f493f6eb57dd427bed638a / 78843 bytes)"
      contains: "ANCHOR-RELATIVE"
    - path: "src/python/pcs_panelwide_reclassify.py"
      provides: "module docstring with §(1) true semantics and NO §(1b) deferral note"
    - path: "tests/m3/test_pairwise_completeness_scan.py"
      provides: "the gate enforcer (AST-parsed + executed + negative-controlled) replacing the md5 recompute test, and the deferred secondary docstring-absence test"
    - path: "tests/m3/test_pcs_panelwide_reclassify.py"
      provides: "the replacement semantics test — still live against the scanner on disk, no deferral asserted"
    - path: ".planning/debug/260901-l55-step0-gate-rescope-off-the-byte-proxy.md"
      provides: "the record: four actions, the git-ref design, why the literal-hash design was rejected, the reuse-vs-inline argument, the 2026-08-06 repeat CLOSED"
  key_links:
    - from: ".planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md"
      to: "tests/m3/source_freeze.py::assert_code_frozen"
      via: "sys.path.insert(0, 'tests/m3') inside the STEP 0 heredoc"
      pattern: "assert_code_frozen"
    - from: "tests/m3/test_pairwise_completeness_scan.py"
      to: ".planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md"
      via: "extract the gate heredoc, ast.parse it, read SCANNER_CODE_REF, then EXECUTE it and require exit 0"
      pattern: "SCANNER_CODE_REF"
    - from: "tests/m3/test_pcs_panelwide_reclassify.py"
      to: "src/python/pairwise_completeness_scan.py"
      via: "ast.get_docstring read at call time — the tool's claims about the scanner stay LIVE"
      pattern: "ast.get_docstring"
---

<objective>
Land the three coupled post-sweep actions — apply the parked docstring patch, rescope
the STEP 0 gate off the whole-file byte proxy onto a git-ref CODE pin, delete the
self-invalidating §(1b) deferral note — plus the fourth action the repo's own record
demands: the DEFERRED SECONDARY docstring-absence test that the comment block at
`tests/m3/test_pairwise_completeness_scan.py:4149-4152` declares is "NOT in this file
yet, because the sentence is still present."

Purpose: close `feedback_scope_a_guard_to_the_property_not_a_proxy` as a REPEAT
(2026-08-06, now again 2026-08-31) by removing the byte proxy that made correcting a
known-false docstring cost an edit to a live runbook.

Output: one commit; a debug record; a green, component-exact suite.

**NOTHING IS FIRED.** No enclave, no VM, no Dataproc, no OSF, no `gsutil`, no
`gcloud`, no network. The VM is stopped; it stays stopped. $0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/debug/260831-DEFERRED-pairwise-completeness-scan-docstring.patch
@.planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md
@src/python/pairwise_completeness_scan.py
@src/python/pcs_panelwide_reclassify.py
@tests/m3/source_freeze.py
@tests/m3/test_pairwise_completeness_scan.py
@tests/m3/test_pcs_panelwide_reclassify.py
@tests/m3/test_source_freeze_pins.py

Python is `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python` (3.11.15).
</context>

<measurements_already_taken>

These were MEASURED by the planner on this tree at HEAD `04a9b2b`. They are the
premises the design rests on. **Re-derive them before relying on them** — every one
is a one-command check.

| # | Measurement | Value |
|---|-------------|-------|
| M1 | `git apply --check` of the parked patch at HEAD | PASSES |
| M2 | scanner md5 / size at HEAD | `e03078ff73502c3c877b0d2ebf93941d` / `73772` |
| M3 | scanner md5 / size WITH the patch applied | `fc1d68dff1f493f6eb57dd427bed638a` / `78843` |
| M4 | `code_lines(scanner, py)` at HEAD **vs** `git show cb199b6:scanner` | **IDENTICAL**, 540 code lines each |
| M5 | `code_lines(PATCHED scanner)` vs the same ref | **IDENTICAL**, 540 code lines — *the patch moves zero CODE* |
| M6 | perturbing the CODE line `already_occluded=bool(deletion.pos < partner.pos <= ...)` to `<=` | `code_lines` **DIFFERS**; first-difference names the `pairs.append(CandidatePair(...))` line |
| M7 | perturbing the SAME phrase where it occurs **inside the module docstring** | `code_lines` **IDENTICAL** — correctly green, it is prose |
| M8 | last CODE-moving commit of the scanner | `cb199b6` (quick-260828-uej T1) |
| M9 | `test_the_already_occluded_rename_is_declined_while_the_sweep_artifact_contract_stands` — the enforcer the patch NAMES | **EXISTS** at `tests/m3/test_pairwise_completeness_scan.py:4232` |
| M10 | gate slice `_GATE_OPEN`..`_GATE_CLOSE` in the runbook | lines 61-149; `HOW TO REGENERATE` (line 141) is **INSIDE** the slice |
| M11 | per-file collect counts | scanner test **114**, reclassify test **21**, source_freeze_pins **39** |

⚠ **M7 is a trap that already caught the planner once.** A naive negative control that
does `text.replace(PHRASE, PERTURBED, 1)` hits the **docstring** occurrence first and
"proves" the gate green — proving nothing. The NC must target the **code** occurrence
(the one inside `already_occluded=bool(`). Both directions are required.

</measurements_already_taken>

<perturbation_audit>

Every perturbation in this plan, audited for the one question that matters: **does it
assert the perturbation actually landed, and did it land where the test claims?**

| # | Perturbation | Landed? | Where it claims? | Fix applied |
|---|--------------|---------|------------------|-------------|
| Test 4(a) | `already_occluded=bool(` → `already_occluded_NC=bool(`, in memory | `assert pert != text` | `assert not (DS <= i < DE)` **and** `ast.get_docstring` UNCHANGED | uniqueness precondition (`count == 1`) + both landing assertions added |
| Test 4(b) | `already_occluded` inside the `[DS:DE]` docstring slice, in memory | `assert pert != text` | `ast.get_docstring` **CHANGED** | **was the defect**: retargeted from a phrase absent from the scanner's module docstring; AST-scoped; precondition + non-vacuity + landing added |
| Test 5 | absence of an md5 token in the gate slice | n/a (absence) | slice non-empty **and** the same regex matches a control string | already correct — kept as the pattern the others now follow |
| Task 1e | on-disk, line-anchored operator flip | `git diff --numstat` == `1 1`, md5 moved | module docstring UNCHANGED after the edit | **was unsafe**: `deletion.pos <` matches 3 lines at HEAD / 4 after the patch, one of which yields `<==` (SyntaxError). Now derives the line from the unique `already_occluded=bool(` anchor |
| Task 1e restore | `git checkout --` | md5 back to `e03078ff…`, `git status` EMPTY | n/a | restore proof added |
| Task 2b | patch applied | md5 `fc1d68df…` + non-zero `--numstat` **before** the green run | n/a | precondition added — a GREEN assertion needs its premise proved |
| Task 2f | revert → RED → re-apply → GREEN | md5 asserted at BOTH file states, for BOTH assertions (4 captures) | n/a | landing assertions added; byte lengths differ so `.pyc` cannot mask it |
| Task 3b | collect-diff by name | diff MUST be non-empty and equal the named set | n/a | non-vacuity added — an empty diff must not read as agreement |

**A2 (MEASURED, HEAD).** `grep -n "deletion\.pos <"` → `:468` (docstring), `:617`
(code), `:658` (`start_bp <= deletion.pos <= end_bp`). Patched adds `:124` (docstring).
`already_occluded=bool(` → exactly **one** line in both states (`:616` / `:687`).

**A1 (MEASURED, both states).** Module-docstring span `[0, 9052]` unpatched /
`[0, 12874]` patched; it contains `already_occluded` **1x / 9x**; the anchor offset
(27791 / 32270) is OUTSIDE that span in both. All **six** Test-4 assertions verified
green — non-vacuity, landing and result, in BOTH directions.

**One residual, recorded rather than omitted.** Test 3 executes the runbook block by
subprocess and asserts exit 0 — a GREEN assertion. Its RED counterpart cannot be
committed without writing the working tree (`assert_code_frozen`'s `actual_text` seam
is banned in every `tests/m3/` module but `test_source_freeze.py`). The RED is covered
in memory by Test 4(a) and on disk by the Task 1e session control. Bounded and stated.

</perturbation_audit>

<the_design_decision>

## The gate compares against a GIT REF, not a literal hash

**Rejected — printing a docstring-insensitive CODE-HASH LITERAL into the runbook.**
`tests/m3/source_freeze.py`'s module docstring forbids it in terms:

> *"`ast.unparse` output can differ across Python minor versions. That is harmless
> **because both sides of every comparison are unparsed by the same interpreter in
> the same process**. Do not 'fix' it by freezing an unparse string."*

A literal in the runbook IS a frozen unparse string, and the enclave's interpreter is
not ours. It would also be a fresh instance of
`feedback_fixed_sha_whole_file_pin_is_a_timebomb`.

**Adopted — `assert_code_frozen(rel_path, ref, LANG_PY)`.** It reads the working tree
and `git show <ref>:<path>` and unparses BOTH in the same process, so interpreter
drift cancels by construction. The enclave has git and the repo (STEP 0 already does
`git pull --ff-only`), and `cb199b6` is an ancestor of the run branch.

**The ref is `cb199b6` — and it does not move when this plan lands.** M4/M5: the
scanner's CODE is identical to `cb199b6` both before AND after the parked patch. That
is the acceptance demonstration for the whole rescope: *a prose correction costs
nothing.*

## REUSE (import `tests/m3/source_freeze.py`) — RECOMMENDED over an inline copy

**Recommendation: REUSE.** Argument and the failure analysis both directions:

1. **The property we need is exactly the property that module implements**, and its
   own docstring says a hand-rolled stripper "makes a real code change **invisible**,
   which is strictly worse than the byte pin it replaces." The scanner has f-strings
   and `#` inside string literals. `source_freeze`'s Python path uses `ast` (comment-
   insensitivity true *by construction*) and is cross-checked in
   `test_source_freeze.py` against an independent hand-written scanner
   (`_mask_strip_py`), with two synthetic mask invariants (4a/4b) pinned because no
   live file exercises them. A ten-line paste-block copy inherits none of that.
2. **`feedback_extract_reusable_utilities`** is the standing rule for a recurrent bug
   class. This is the **second** time the byte-proxy class has bitten (2026-08-06,
   2026-08-31). A second copy is precisely what that memory forbids.
3. **`_SHA_RE` structurally forbids the cheapest weakening.** `assert_code_frozen`
   refuses a symbolic ref, so nobody can green a red gate by pointing it at `HEAD`.
   An inline ten-liner has no such guard.
4. **The apparent counter-precedent is distinguishable, and must be argued not
   waved.** `feedback_freeze_economy_is_not_a_reason_to_take_risk` records that we
   DECLINED extracting `ld_allele_join.R` because it would put a *first-of-its-kind
   runtime `source()` dependency on the **$385-1,084 fire path*** while removing
   duplication that was **already drift-guarded**. Two material differences here:
   (a) STEP 0 is a **pre-flight gate**, not the fire path — it runs before anything
   billable and its failure mode is **STOP**, which is the safe direction; (b) there
   is no existing copy and no drift guard, so here *creating* one is the risk.

**What breaks under REUSE:** a refactor of `source_freeze.py` (rename, move, changed
signature) breaks the enclave gate. **Bounded and safe** — it fails as an import error
or an assertion, i.e. STOP, and Task 1's enforcer test *executes the runbook's own
block* at NCSU, so the refactor turns a committed test RED long before anyone reaches
the enclave. Also: `ast.unparse` needs Python ≥ 3.9; the gate self-checks
`sys.version_info` with an explicit STOP rather than dying on an `AttributeError`.

**What breaks under INLINE:** the copy drifts from the audited stripper **silently**,
and the dangerous direction is *gate green on changed code* — unbounded, undetectable,
and exactly the class that voided the 2026-08-26 sweep. Restoring safety would require
a committed test proving the copy agrees with `_strip_to_code_py`, which re-creates the
dependency **plus** the copy.

## Why ONE commit

The four actions are **mutually invalidating**:

* Landing the patch first turns `test_pending_paste_step0_pins_the_scanner_by_a_CURRENT_content_hash`
  and the §(1b) self-invalidating test **RED**. Forbidden by the brief.
* Landing the gate rescope first is green — but it ships a **committed falsehood**:
  §(1b) states "the live runbook STEP 0 gate pins the scanner's WHOLE-FILE md5 and
  byte size" and names a test that no longer exists. Nothing would catch it (§(1b)'s
  own test only checks that the md5/size *appear* in the note). Shipping a known-false
  sentence to save process is the exact trade the byte-proxy episode punished.

There is no ordering that is both green and truthful. **One commit.** The *work* is
still sequenced (Tasks 1 → 2) so every transition is measured in the working tree
before anything is staged.

</the_design_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rescope the STEP 0 gate onto a git-ref CODE pin, with the scanner still UNPATCHED — and prove the gate can fail</name>
  <files>.planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md, tests/m3/test_pairwise_completeness_scan.py</files>
  <behavior>
    New enforcer `test_pending_paste_step0_pins_the_scanner_CODE_against_a_git_ref`
    (replacing `test_pending_paste_step0_pins_the_scanner_by_a_CURRENT_content_hash`):

    - Test 1 (STRUCTURE): slice the gate `_GATE_OPEN`.._GATE_CLOSE`; extract every
      heredoc body by `^python3 - <<'EOF'$` / `^EOF$` line anchors; `ast.parse` each;
      select the block whose module body assigns `SCANNER_CODE_REF`. Read
      `SCANNER_CODE_REF` and `SCANNER_REL` from their `ast.Assign` nodes. Assert the
      block contains a `Call` to `assert_code_frozen` and that NO keyword named
      `actual_text` and no `**` unpack is passed to it. (Parsed structure, never a
      grep.)
    - Test 2 (VALUE): the extracted `SCANNER_REL` == `src/python/pairwise_completeness_scan.py`
      and `SCANNER_CODE_REF` matches `^[0-9a-f]{7,40}$` — never a symbolic name.
    - Test 3 (RUNTIME): `subprocess.run([sys.executable, "-c", block], cwd=PROJECT_ROOT)`
      returns 0 and stdout contains `CODE PIN PASSED`. The exact text an operator
      pastes is EXECUTED, not pattern-matched.
    - Test 4 (COMMITTED NEGATIVE CONTROL, in memory, no disk write). Both
      perturbations are **AST-SCOPED BY BYTE RANGE**, never a raw first-match
      `.replace()` over the file. Compute the module docstring's exact span once:

      ```
      tree = ast.parse(text)
      node = tree.body[0]                      # must be Expr(Constant(str))
      starts = cumulative line-start offsets of `text`
      DS = starts[node.lineno - 1] + node.col_offset
      DE = starts[node.end_lineno - 1] + node.end_col_offset
      ref_lines = code_lines(git_show(SCANNER_CODE_REF, SCANNER_REL), LANG_PY)
      ```

      **(a) CODE side — must make `code_lines` DIFFER.** Anchor on
      `already_occluded=bool(`, which occurs **exactly once** in the file in BOTH the
      unpatched and patched states (MEASURED, A1).
      - PRECONDITION: `assert text.count("already_occluded=bool(") == 1`
      - LANDING: `assert not (DS <= i < DE)` — the anchor is OUTSIDE the docstring
      - perturb: `already_occluded=bool(` -> `already_occluded_NC=bool(` at offset `i`
      - NON-VACUITY: `assert pert_code != text`
      - LANDING: `assert ast.get_docstring(ast.parse(pert_code)) == ast.get_docstring(tree)`
        — the docstring was NOT what moved
      - RESULT: `assert code_lines(pert_code, LANG_PY) != ref_lines`

      **(b) PROSE side — must leave `code_lines` IDENTICAL.** Perturb the token
      `already_occluded` **inside the `[DS:DE]` slice only**. That token is present in
      the module docstring in BOTH states (MEASURED, A1: 1x unpatched, 9x patched);
      the full phrase `deletion.pos < partner.pos <= deletion.span_end` is **NOT** and
      must not be used — it is absent from the scanner's module docstring at HEAD.
      - PRECONDITION: `assert text[DS:DE].count("already_occluded") >= 1` — the target
        exists WHERE the test claims it does
      - perturb: `text[:DS] + text[DS:DE].replace("already_occluded", "already_occluded_PERTURBED_BY_NC", 1) + text[DE:]`
      - NON-VACUITY: `assert pert_doc != text`
      - LANDING: `assert ast.get_docstring(ast.parse(pert_doc)) != ast.get_docstring(tree)`
        — it landed in the MODULE DOCSTRING, structurally, not merely "somewhere"
      - RESULT: `assert code_lines(pert_doc, LANG_PY) == ref_lines`

      Both directions, or the green means nothing. All **six** assertions (non-vacuity,
      landing and result, each direction) MEASURED green by the planner against BOTH
      the unpatched and the patched scanner (A1), so this
      test is state-independent: it passes at Task 1 (unpatched) and in the committed
      final tree (patched).
    - Test 5 (BYTE PROXY IS GONE): the gate slice contains NO 32-hex-char md5 token
      (regex `(?<![0-9a-f])[0-9a-f]{32}(?![0-9a-f])`) and does not `_has_number` the
      scanner's current byte size. Non-vacuity: assert the slice is non-empty and
      that the same md5 regex DOES match a control string.
    - Test 6 (CARRIED FORWARD from the deleted test): the gate still contains
      `git status --porcelain src/python/pairwise_completeness_scan.py`, and the
      commit-subject spoof guard `"quick-260825-qpf" not in text or "769afa6" in text`
      still holds.
  </behavior>
  <action>
**⚠ Do this task with the scanner UNPATCHED.** The whole point is to demonstrate the
new gate is green BEFORE the patch and still green AFTER it (Task 2), i.e. that the
insensitivity is a measured transition rather than an assertion.

**1a — rewrite gate check (ii)** in `.planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md`
(currently the `md5sum` + `stat -c '%s'` block at ~:86-96). Replace the two commands
and their `EXPECT exactly:` values with a heredoc block. Keep the label `(ii)` and
give it a heading naming what it pins. The block, verified working by the planner
(exit 0, prints `SCANNER CODE PIN ref: cb199b6` / `CODE PIN PASSED`):

```
(ii) THE CODE PIN. Pin what the file DOES, not its BYTES. A comment or docstring
correction must cost NOTHING; a one-character CODE change must STOP the run.

python3 - <<'EOF'
import sys
if sys.version_info < (3, 9):
    raise SystemExit(
        "STOP: this gate needs Python 3.9+ (ast.unparse). Got %s" % sys.version
    )
sys.path.insert(0, "tests/m3")
from source_freeze import assert_code_frozen, LANG_PY

SCANNER_REL = "src/python/pairwise_completeness_scan.py"
SCANNER_CODE_REF = "cb199b6"

try:
    assert_code_frozen(SCANNER_REL, SCANNER_CODE_REF, LANG_PY)
except AssertionError as exc:
    print("STOP -- the scanner's CODE has MOVED off the STEP 0 pin.")
    print(exc)
    print(
        "REMEDY: see HOW TO REGENERATE THIS GATE below. IGNORE the "
        "'tests/m3/test_source_freeze_pins.py' clause in the message above: that "
        "is source_freeze's GENERIC re-pin sentence and it does NOT apply to this "
        "gate. The constant to update is SCANNER_CODE_REF, in this block."
    )
    raise SystemExit(1)

print("SCANNER CODE PIN ref:", SCANNER_CODE_REF)
print("CODE PIN PASSED")
EOF

EXPECT exactly these two lines (the ref is a FIELD RECORD -- paste it back):

  SCANNER CODE PIN ref: cb199b6
  CODE PIN PASSED
```

Add short prose under it stating, in the runbook's own voice:
  - **Why a ref and not a hash literal.** Quote `source_freeze.py` verbatim: *"Do not
    'fix' it by freezing an unparse string."* Both sides are unparsed by the SAME
    interpreter in the SAME process, so `ast.unparse` version drift cancels; a literal
    in this document would not.
  - **What it ignores on purpose:** comments, docstrings, blank lines, trailing
    whitespace. Correcting a wrong sentence in the scanner costs nothing.
  - **What it still catches:** any real code change, reported as a first-difference
    naming the moved line.
  - **Why check (i) survives and is not redundant:** the code pin reads the WORKING
    TREE, so it already sees an uncommitted *code* edit — but it is deliberately BLIND
    to an uncommitted *prose* edit. `git status --porcelain` is the only check that
    sees one, and a prose edit still means this clone is not byte-identical to what was
    reviewed. It STAYS.
  - **If `git show` fails** (shallow / partial clone), the assertion re-raises git's
    stderr: **STOP.**

**1b — demote check (iii)** from a STOP to a FIELD RECORD. It currently EXPECTs the
short hash `cb199b6`. That is the byte proxy one level up: a docstring-only commit
legitimately moves the last-touching commit and would false-STOP the operator. Keep the
command, delete the `EXPECT`/`STOP`, and label it a FIELD RECORD in the runbook's
existing vocabulary ("report whatever it says; only the CODE PIN carries a STOP").

**1c — rewrite `HOW TO REGENERATE THIS GATE`** (~:141, inside the gate slice). It
currently says "the only legitimate way to change (i)-(iii)" and lists four hash/stat
commands. Replace with: the ONLY legitimate change is `SCANNER_CODE_REF` → the SHA of
the commit that landed an **authorized CODE change**; a comment or docstring change
updates **NOTHING**; and the way to confirm is `pytest tests/m3/test_pairwise_completeness_scan.py -k step0`
at NCSU on a clean tree. Cross-reference `DEC-2026-08-06-sr4-freeze-scope`.

**1d — replace the enforcer** in `tests/m3/test_pairwise_completeness_scan.py`. Delete
`test_pending_paste_step0_pins_the_scanner_by_a_CURRENT_content_hash` (~:2775) and add
`test_pending_paste_step0_pins_the_scanner_CODE_against_a_git_ref` implementing the six
checks in `<behavior>`. Import `code_lines`, `git_show`, `LANG_PY` from `source_freeze`
(the same `sys.path` insert the file already uses for tests/m3 imports).

⚠ **You may NOT pass `actual_text=`.** `tests/m3/test_source_freeze.py::test_no_production_call_site_supplies_the_control_seam`
AST-walks every module in `tests/m3/` and rejects it. Use `code_lines` / `git_show`
directly for Test 4 — they are unguarded — and never write the scanner to disk.

⚠ **Do not introduce the token `--ancestry` anywhere in the runbook.**
`test_pending_paste_step3_carries_no_ancestry_flag_so_the_default_is_load_bearing`
asserts `text.count("--ancestry") == 0` over the whole document.

⚠ Leave `(i)`, `(iv)` and the `WHAT EACH FAILURE MEANS` block **verbatim** —
`test_pending_paste_step0_capability_numbers_are_the_real_manifest_numbers` asserts on
`276`, the exact `manifest windows: ... distinct region ids: ...` line and
`CAPABILITY CHECK PASSED` inside this same slice.

**1e — the SESSION NEGATIVE CONTROL (run it; do not skip it).** A green gate is
evidence only if it has been seen RED.

⚠ **DO NOT use a blind `sed` on `deletion.pos <`.** MEASURED at HEAD (A2): that string
matches **THREE** lines — `:468` (a DOCSTRING), `:617` (the code), and `:658`
(`start_bp <= deletion.pos <= end_bp`, where the naive substitution produces
`deletion.pos <==` → `SyntaxError`). After the Task 2 patch it matches **FOUR**,
including `:124` in the module docstring. A first-match edit perturbs PROSE and then
reports the gate "failed to catch it" — a false alarm dressed as a finding, and the
M7 trap again.

  1. Run the extracted block → expect exit 0 / `CODE PIN PASSED`. Record the scanner
     md5 first: it MUST be `e03078ff73502c3c877b0d2ebf93941d`.
  2. Derive the target LINE, do not pattern-match the file: `LN=$(grep -n 'already_occluded=bool(' src/python/pairwise_completeness_scan.py | cut -d: -f1)`
     and `assert` there is exactly ONE such line. Perturb **line `LN+1` only**
     (`deletion.pos <` → `deletion.pos <=` on that line alone, e.g.
     `sed -i "$((LN+1))s/deletion\.pos </deletion.pos <=/" <file>`).
  3. PROVE THE PERTURBATION LANDED, AND LANDED IN CODE:
     - `git diff --numstat -- src/python/pairwise_completeness_scan.py` MUST read
       `1<TAB>1<TAB><path>` — exactly one line changed;
     - `md5sum` MUST have moved off `e03078ff…`;
     - `python3 -c "import ast,sys; t=open(...).read(); print(ast.get_docstring(ast.parse(t))==<captured docstring>)"` MUST print `True` —
       the module docstring is UNCHANGED, so what moved was code.
     Then `find . -path '*/src/*' -name '__pycache__' -prune -exec rm -rf {} +`
     (`feedback_negative_control_defeated_by_bytecode_cache`).
  4. Re-run → expect exit **1**, `STOP -- the scanner's CODE has MOVED`, and a
     first-difference naming the `pairs.append(CandidatePair(...))` line.
  5. `git checkout -- src/python/pairwise_completeness_scan.py`. PROVE THE RESTORE:
     md5 back to `e03078ff73502c3c877b0d2ebf93941d` and `git status --porcelain` on
     that path EMPTY. Re-run → exit 0.
  6. Record steps 1-5 verbatim; they go in the Task 3 record.
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis &amp;&amp; /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_pairwise_completeness_scan.py -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - `tests/m3/test_pairwise_completeness_scan.py` passes; collect count is **114**
      (one test deleted, one added — net 0; confirm by a NAME-level collect diff vs
      HEAD, never by count alone).
    - The gate slice contains **no** 32-hex md5 token and **no** `73772`.
    - `git status --porcelain` shows exactly the two files this task touches.
    - The 1e negative control was observed **RED then GREEN**, with the output captured.
    - `git diff --stat -- src/python/ ` is **EMPTY** (the scanner is still unpatched).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the parked patch, delete the §(1b) deferral, and land the deferred secondary docstring test</name>
  <files>src/python/pairwise_completeness_scan.py, src/python/pcs_panelwide_reclassify.py, tests/m3/test_pcs_panelwide_reclassify.py, tests/m3/test_pairwise_completeness_scan.py</files>
  <behavior>
    - Replacement for `test_the_tool_docstring_carries_the_true_semantics_and_flags_the_scanners_false_claim`,
      renamed `test_the_tool_and_the_scanner_agree_that_already_occluded_is_anchor_relative`:
      whitespace-normalised `ast.get_docstring` reads of BOTH files AT CALL TIME (never
      an import — bytecode-cache rule). Asserts (1) `pcs_panelwide_reclassify` §(1)
      still states `ANCHOR-RELATIVE` / `PANEL-WIDE` / `does NOT mean` +
      `survives \`\`--exclude\`\``; (2) the DEFERRAL framing is GONE — the note no
      longer quotes `already visible as \`\`already_occluded\`\``, no longer says
      `THAT SENTENCE IS FALSE`, and no longer names the parked patch file; (3) the
      **scanner's own docstring now carries the correction** — contains
      `ANCHOR-RELATIVE` and `does NOT mean`, and does NOT contain the false phrase.
      Claim (3) is what keeps this test LIVE against another file after the deferral
      is gone.
    - New `test_the_scanner_docstring_no_longer_claims_already_occluded_is_the_exclude_side`
      in `tests/m3/test_pairwise_completeness_scan.py` — the SECONDARY test the comment
      block at :4149-4152 declares DEFERRED. `ast.get_docstring` on the scanner,
      whitespace-normalised: the false sentence is ABSENT; `ANCHOR-RELATIVE`,
      `pcs_panelwide_reclassify` and `detect_occluded_variants` are PRESENT.
      Non-vacuity: assert the docstring is non-empty and > 1000 chars first, so a
      vanished docstring cannot green it.
  </behavior>
  <action>
**2a — apply the parked patch, unchanged:**
`git apply .planning/debug/260831-DEFERRED-pairwise-completeness-scan-docstring.patch`
(M1: verified clean at HEAD). Then MEASURE and record: `md5sum` and `stat -c '%s'` MUST
read `fc1d68dff1f493f6eb57dd427bed638a` / `78843` (M3). Do **not** hand-edit the
scanner; if the patch does not apply, STOP and report.

**2b — prove the rescoped gate did not move.** PRECONDITION, asserted first, because
this is a GREEN assertion whose meaning depends entirely on the patch having landed:
`md5sum` MUST read `fc1d68dff1f493f6eb57dd427bed638a` and `git diff --numstat` on the
scanner MUST be non-zero. Only then re-run Task 1's extracted heredoc block: it MUST
still print `CODE PIN PASSED` with the scanner now patched. This is the acceptance
demonstration for the entire rescope (M5). Capture the output.

**2c — delete §(1b)** from `src/python/pcs_panelwide_reclassify.py`'s module docstring:
the whole block from `(1b) ⚠ THE SCANNER'S OWN DOCSTRING CURRENTLY CONTRADICTS (1)` down
to and including the paragraph ending `...is why the true statement is placed here.`,
stopping immediately before `(2) WHY VID-KEYED`. **KEEP §(1) exactly as it is** — it
carries the TRUE semantics and it is what the replacement test enforces. Do not
renumber §(2)/§(3)/etc.

**2d — replace the §(1b) test** in `tests/m3/test_pcs_panelwide_reclassify.py` (~:890)
per `<behavior>`. Delete `_SCANNER_FALSE_CLAIM`'s use as a REQUIRED presence and reuse
it as a REQUIRED ABSENCE. Delete self-invalidation #2 (the md5 / byte-size quotes) —
with §(1b) gone nothing quotes them and the runbook no longer pins bytes. Rewrite the
comment banner above it (currently "THE MITIGATION THAT COSTS NO PINNED BYTES") to
record that the mitigation is DISCHARGED and why.

**2e — land the deferred SECONDARY test** in `tests/m3/test_pairwise_completeness_scan.py`
per `<behavior>`, and rewrite the comment block at :4140-4166: delete the
`⚠ DEFERRED, DELIBERATELY` paragraph and the
`e03078ff73502c3c877b0d2ebf93941d / 73772` sentence; state that the correction LANDED
in `quick-260901-l55` together with the gate rescope, and NAME both enforcers (the
behavioural `test_already_occluded_is_anchor_relative_and_is_not_the_exclude_side` and
the new phrase test) — `feedback_a_claimed_invariant_needs_a_named_enforcer`.

**2f — RED-first for 2e.** Before trusting the new phrase test: `git checkout --
src/python/pairwise_completeness_scan.py`, then **PROVE THE REVERT LANDED** — md5
`e03078ff73502c3c877b0d2ebf93941d` / size `73772` — because a revert that silently did
not happen would make the following RED unattributable. Run the new test → must be
**RED** (the false sentence is present). Re-apply the patch, **prove it landed** (md5
`fc1d68dff1f493f6eb57dd427bed638a` / `78843`), re-run → **GREEN**. Do the mirror for
2d's claim (3) the same way. Capture md5 + test outcome at BOTH file states (reverted,
re-applied) for BOTH assertions (2e's test and 2d's claim (3)) — four captures over two
file states. The byte
lengths differ (73772 vs 78843) so the `.pyc` (mtime, size) validation cannot mask
either transition — but clear `__pycache__` anyway.

⚠ The patch NAMES two enforcers. M9 confirms both exist
(`test_the_already_occluded_rename_is_declined_while_the_sweep_artifact_contract_stands`
at :4232 and `test_already_occluded_is_anchor_relative_and_is_not_the_exclude_side`).
Re-confirm by grep before committing — a named enforcer that does not exist is the
2026-08-06 `a_claimed_invariant_needs_a_named_enforcer` defect.

⚠ **Do NOT touch** `src/python/occlusion_span_filter.py`, `run_native_ld_panel.py`,
`fire_verifier.py`, `aou_ld_panel.py`, or `.planning/amendments/**`.
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis &amp;&amp; /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_pairwise_completeness_scan.py tests/m3/test_pcs_panelwide_reclassify.py -q 2>&amp;1 | tail -5 &amp;&amp; md5sum src/python/pairwise_completeness_scan.py &amp;&amp; stat -c '%s' src/python/pairwise_completeness_scan.py &amp;&amp; git diff --stat -- src/python/occlusion_span_filter.py src/python/run_native_ld_panel.py src/python/fire_verifier.py src/python/aou_ld_panel.py .planning/amendments/</automated>
  </verify>
  <done>
    - Scanner md5 `fc1d68dff1f493f6eb57dd427bed638a`, size `78843`.
    - The rescoped gate block still exits 0 with the scanner patched (2b captured).
    - `grep -c 'DEFERRED' src/python/pcs_panelwide_reclassify.py` is **0**.
    - Both test files green; collect counts scanner **115** (114 + 1), reclassify **21**
      (1 replaced, net 0).
    - 2f observed RED then GREEN for both new assertions.
    - The forbidden-surface `git diff --stat` is **EMPTY**.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full suite, component-exact reconciliation, the record, and ONE commit</name>
  <files>.planning/debug/260901-l55-step0-gate-rescope-off-the-byte-proxy.md, .planning/STATE.md</files>
  <action>
**3a — full suite.** `cd` to the repo and run
`/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3 -q`
with a timeout of **≥ 1,200,000 ms** (~14 min). Afterwards, unconditionally:
`git checkout -- tests/m3/sparse_parent_benchmark.tsv`.

**3b — reconcile COMPONENT-EXACT, both bases, and by NAME.** Baseline
**1167 passed / 33 skipped / 0 failed**; collected 1200.
  - Passed basis: `1167 + 1 = 1168` — the single net-new test is Task 2e's
    `test_the_scanner_docstring_no_longer_claims_already_occluded_is_the_exclude_side`.
    Task 1 deleted 1 and added 1 (net 0); Task 2d replaced 1 with 1 (net 0).
  - Collected basis: `1200 + 1 = 1201 = 1168 + 33`. **Skips must stay at exactly 33** —
    no new test may land as a SKIP.
  - Per-file control: scanner test `114 → 115`, reclassify test `21 → 21`.
  - ⚠ **Diff `--collect-only -q` BY NAME against HEAD, not by count.** A count match can
    hide a rename plus a deletion (`feedback_aggregate_agreement_hides_component_errors`).
    Report added/removed names explicitly. **NON-VACUITY:** the name diff MUST be
    non-empty and MUST equal exactly the expected set — **2 removed**
    (`…step0_pins_the_scanner_by_a_CURRENT_content_hash`,
    `…carries_the_true_semantics_and_flags_the_scanners_false_claim`, plus nothing else)
    and **3 added** (`…step0_pins_the_scanner_CODE_against_a_git_ref`,
    `…tool_and_the_scanner_agree_that_already_occluded_is_anchor_relative`,
    `…scanner_docstring_no_longer_claims_already_occluded_is_the_exclude_side`, plus
    nothing else). An EMPTY diff means the edits did not land and must not read as
    agreement.
  - If the arithmetic does not close, **STOP and report the discrepancy** — do not
    adjust the expectation to fit.

**3c — write the record** at
`.planning/debug/260901-l55-step0-gate-rescope-off-the-byte-proxy.md`:
  1. **The four coupled actions** and why they are coupled (each, alone, makes a
     committed statement in another file false or a committed test red).
  2. **Why ONE commit** — reproduce the argument from `<the_design_decision>`,
     including that the gate-first ordering is green but ships a false sentence.
  3. **The git-ref design**, and **why the literal-hash design was REJECTED**, quoting
     `source_freeze.py` verbatim: *"`ast.unparse` output can differ across Python minor
     versions. That is harmless because both sides of every comparison are unparsed by
     the same interpreter in the same process. Do not 'fix' it by freezing an unparse
     string."*
  4. **The REUSE vs INLINE decision**, the argument, and **what breaks under each** —
     including the honest treatment of the apparent counter-precedent
     (`feedback_freeze_economy_is_not_a_reason_to_take_risk`) and the two axes on which
     it is distinguishable.
  5. **Every measurement**, as a table: M1-M11 re-derived, plus the 1e and 2f
     negative-control transcripts (RED then GREEN, both directions), plus the M7 trap —
     *the planner's own first negative control perturbed the DOCSTRING occurrence and
     "passed" while proving nothing.* Record it; it is the most transferable finding.
  6. **The byte-proxy episode CLOSED** as a REPEAT of
     `feedback_scope_a_guard_to_the_property_not_a_proxy` (baked 2026-08-06, repeated
     2026-08-31): what the proxy was, what it cost (deletion of a known falsehood
     became expensive), what replaced it, and the named enforcer that now fails if it
     regresses.
  7. **What did NOT change:** every pre-registered number — `353089` / `353090`, 15
     rows, 13 pairs, 10/3, offsets `{-14:1,-9:1,-6:1,-3:1,-1:1,0:10}`, and the
     panel-wide 12/1 pairs and 14/1 rows. Prove it: `git diff --stat` over
     `.planning/amendments/` and the pre-registration file is EMPTY.
  8. **NOTHING FIRED:** $0, zero enclave / VM / Dataproc / OSF / `gsutil` / `gcloud` /
     network contact. The VM was already stopped and stays stopped.

**3d — STATE.md.** Add a dated block at the top marking the three (four) coupled
deferrals **DISCHARGED**, the new suite baseline, and what remains open
(`samepos_missingness_probe.py` BUILT/TESTED/**NOT RUN**; Seth gets the measurement
beside his sealed prediction). `feedback_state_md_keep_current` — write it as part of
the atomic commit, do not defer.

**3e — ONE commit, explicit paths.** ⚠ NEVER `git add -A` or `git add .`
(`feedback_multi_terminal_staging`). Prepend node to PATH for the GSD CLI
(`feedback_node_path`):

```
export PATH="$HOME/miniconda3/bin:$PATH"
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
  "fix(quick-260901-l55): rescope the STEP 0 gate off the WHOLE-FILE BYTE PROXY onto a git-ref CODE pin, land the parked docstring correction, and delete the deferral it was blocking — one commit, because each action alone makes another file lie or another test red" \
  --files src/python/pairwise_completeness_scan.py src/python/pcs_panelwide_reclassify.py \
          tests/m3/test_pairwise_completeness_scan.py tests/m3/test_pcs_panelwide_reclassify.py \
          .planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md \
          .planning/debug/260901-l55-step0-gate-rescope-off-the-byte-proxy.md \
          .planning/STATE.md
```

If the commit fails with `invalid object` / `Error building trees`, that is the GPFS
loose-object loss — see `reference_gpfs_git_object_store_loss` — recover, do not retry
blindly.
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis &amp;&amp; /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3 -q 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <done>
    - Full `tests/m3`: **1168 passed / 33 skipped / 0 failed**, reconciled component-exact
      on BOTH bases and by a NAME-level collect diff vs HEAD.
    - `git checkout -- tests/m3/sparse_parent_benchmark.tsv` run afterwards; tree clean
      apart from the seven committed paths.
    - The record exists and contains: the four actions, the ONE-commit reason, the
      git-ref design, the verbatim `source_freeze` quote rejecting a frozen unparse
      string, the REUSE-vs-INLINE argument with what breaks under each, M1-M11, both
      negative-control transcripts, the M7 trap, and the 2026-08-06 repeat marked
      **CLOSED**.
    - `.planning/STATE.md` carries a dated block marking the deferrals DISCHARGED.
    - Exactly **one** commit, staged by explicit paths.
    - `git diff --stat` over the forbidden surfaces is EMPTY; no pre-registered number
      moved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NCSU repo → AoU enclave | The runbook is PASTED into an external, billed, non-reproducible environment. The only thing crossing is text; the thing at risk is the operator's belief about **which instrument ran**. |
| Working tree → committed record | A claim about another file's contents (the §(1b) note, the STEP 0 pins) can decay into a lie with nothing turning red. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l55-01 | Spoofing | STEP 0 gate check (ii) | mitigate | The gate resolves BOTH sides in-process (`assert_code_frozen`), so a stale/forged literal cannot stand in for the reference; `_SHA_RE` refuses a symbolic ref, closing the "point it at HEAD" weakening. |
| T-l55-02 | Tampering | scanner working tree vs the reviewed instrument | mitigate | Check (i) `git status --porcelain` EMPTY catches an uncommitted **prose** edit that the CODE pin deliberately ignores; the CODE pin catches an uncommitted **code** edit. Both retained — neither subsumes the other. |
| T-l55-03 | Repudiation | "which code produced the sweep?" | mitigate | The gate PRINTS `SCANNER CODE PIN ref: <sha>` as a FIELD RECORD pasted back, so the attribution is what the run actually resolved, not what the document claimed. |
| T-l55-04 | Information disclosure | none — no genotype, no `.bed`, no egress | accept | This task touches only NCSU-local source, tests and planning docs. Nothing is fired; no perimeter contact. |
| T-l55-05 | Denial of service | gate errors in the enclave (missing `tests/m3`, Python &lt; 3.9, shallow clone) | mitigate | All three fail **CLOSED** → STOP, the safe direction. An explicit `sys.version_info &lt; (3, 9)` STOP replaces an obscure `AttributeError`; `git_show`'s assertion re-raises git's stderr. |
| T-l55-06 | Elevation of privilege | n/a (no auth surface, no network) | accept | Single-user HPC tree; no credential or privilege boundary is involved. |
</threat_model>

<verification>
1. `git apply --check` of the parked patch passes **before** Task 2 (M1 re-derived).
2. Scanner md5/size move `e03078ff…`/`73772` → `fc1d68df…`/`78843` and nothing else about
   the scanner moves: `code_lines` vs `cb199b6` is IDENTICAL both before and after.
3. The rescoped gate block executes to exit 0 with the scanner **unpatched** (Task 1)
   AND **patched** (Task 2) — the same block, both states.
4. The gate has been seen **RED**: a one-character CODE perturbation produces exit 1
   plus a first-difference naming the moved line; a docstring perturbation does NOT.
5. `grep -c 'DEFERRED' src/python/pcs_panelwide_reclassify.py` → **0**; §(1) survives.
6. The gate slice contains no 32-hex md5 token and no `73772`.
7. Runbook `--ancestry` token count is still **0**.
8. Forbidden surfaces (`occlusion_span_filter.py`, `run_native_ld_panel.py`,
   `fire_verifier.py`, `aou_ld_panel.py`, `.planning/amendments/`) `git diff --stat`
   **EMPTY**.
9. Full `tests/m3` = 1168 / 33 / 0, reconciled component-exact and by NAME.
10. Nothing fired: `$0`, zero enclave / VM / Dataproc / OSF / `gsutil` / `gcloud` /
    network contact.
</verification>

<success_criteria>
- The false claim at `pairwise_completeness_scan.py:122` is GONE, replaced by the
  corrected ANCHOR-RELATIVE statement, and its absence is machine-enforced.
- The STEP 0 gate pins the scanner's **CODE against `cb199b6`**, not its bytes; a prose
  correction costs nothing and a code change STOPs the run — **both demonstrated**.
- The §(1b) deferral is deleted; its TRUE semantics survive in §(1) and remain live
  against the scanner on disk.
- One commit, seven explicit paths, suite 1168/33/0, record written, STATE.md current.
- No pre-registered number moved. Nothing fired.
</success_criteria>

<output>
After completion, create
`.planning/quick/260901-l55-the-three-coupled-post-sweep-actions-app/260901-l55-SUMMARY.md`.
</output>
