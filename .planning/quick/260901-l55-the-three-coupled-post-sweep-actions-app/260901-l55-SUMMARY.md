---
phase: 260901-l55
plan: 01
subsystem: testing
tags: [source-freeze, ast, code-pin, pytest, runbook-gate, negative-control, git-ref]

requires:
  - phase: quick-260828-uej
    provides: "the STEP 0 behavioural freshness gate (and its whole-file md5 + byte-size content pin, which this plan replaces)"
  - phase: quick-260831-kw8
    provides: "the parked docstring patch, the §(1b) self-invalidating deferral note, and the DEFERRED secondary-test declaration"
  - phase: quick-260806-sr4
    provides: "tests/m3/source_freeze.py — assert_code_frozen / code_lines / git_show, and DEC-2026-08-06-sr4-freeze-scope"
provides:
  - "STEP 0 gate check (ii) rescoped from a whole-file BYTE proxy onto a git-ref CODE pin (assert_code_frozen vs cb199b6), resolved in-process on both sides"
  - "STEP 0 gate check (iii) demoted from a STOP to a FIELD RECORD"
  - "the corrected ANCHOR-RELATIVE semantics in pairwise_completeness_scan.py's module docstring (the parked patch, applied unchanged)"
  - "§(1b) of pcs_panelwide_reclassify.py deleted; §(1) strengthened to carry the ANCHOR-RELATIVE vs PANEL-WIDE contrast"
  - "the SECONDARY docstring-absence enforcer that quick-260831-kw8 declared DEFERRED"
  - "a committed, AST-scoped, two-direction negative control for the gate"
affects: [AoU enclave STEP 0 pre-flight, any future scanner edit, any future freeze/pin design]

tech-stack:
  added: []
  patterns:
    - "PIN THE PROPERTY, NOT A PROXY: a CODE pin (ast-unparsed, comment/docstring-insensitive) against an IMMUTABLE git ref, never a frozen hash literal"
    - "AST-SCOPED-BY-BYTE-RANGE negative controls: perturb by computed docstring span, assert non-vacuity AND that the perturbation landed where the test claims"
    - "EXECUTE the pasted runbook block in a subprocess rather than pattern-matching its text"

key-files:
  created:
    - .planning/debug/260901-l55-step0-gate-rescope-off-the-byte-proxy.md
  modified:
    - .planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md
    - src/python/pairwise_completeness_scan.py
    - src/python/pcs_panelwide_reclassify.py
    - tests/m3/test_pairwise_completeness_scan.py
    - tests/m3/test_pcs_panelwide_reclassify.py
    - .planning/STATE.md

key-decisions:
  - "The gate compares against a GIT REF, not a code-hash LITERAL — source_freeze.py's own docstring forbids freezing an unparse string, and the enclave's interpreter is not ours"
  - "REUSE tests/m3/source_freeze.py rather than inlining a copy of the stripper; the failure mode of REUSE is STOP (safe), the failure mode of a drifting INLINE copy is green-on-changed-code (unbounded)"
  - "source_freeze.py's imprecise generic re-pin sentence is WRAPPED by runbook prose, not fixed in place — it is a shared utility with four other consumers"
  - "ONE commit: no ordering of the four actions is both green and truthful"
  - "Line-number citations replaced by SYMBOL citations, which cannot rot silently"

patterns-established:
  - "A guard scoped to a proxy is a scoping bug report the moment the correct action becomes the expensive one"
  - "A green assertion is evidence only after its RED has been observed — in BOTH directions when the guard claims insensitivity"

requirements-completed: [A1-apply-parked-patch, A2-rescope-step0-gate, A3-delete-1b-deferral, A4-land-deferred-secondary-test, A5-record]

duration: ~95min
completed: 2026-09-01
---

# quick-260901-l55: the three (four) coupled post-sweep actions Summary

**The STEP 0 runbook gate no longer pins `pairwise_completeness_scan.py` by whole-file md5 + byte size; it pins the file's CODE against `cb199b6` via `source_freeze.assert_code_frozen`, and the parked docstring correction that the byte pin had been blocking landed in the same commit — MEASURED: the patch moved 5,071 bytes and the md5, and moved ZERO code (540 code lines identical on both sides, before and after).**

## Performance

- **Duration:** ~95 min (incl. a 14m39s full-suite run)
- **Tasks:** 3 of 3
- **Files modified:** 6 modified + 1 created = 7
- **Commit:** `f9425b7` (ONE commit, seven explicit paths; amended in place from `1ff812b` — see deviation 3)

## Accomplishments

1. **A1 — the parked patch applied unchanged.** `git apply` of
   `.planning/debug/260831-DEFERRED-pairwise-completeness-scan-docstring.patch`,
   89 insertions / 10 deletions. The false RETAINED-SET PARITY claim
   (*"the `--exclude` side is already visible as `already_occluded`"*) is gone;
   the ANCHOR-RELATIVE semantics, the consequence, and a pointer to
   `pcs_panelwide_reclassify.py` are in its place.
2. **A2 — the gate rescoped.** Check **(ii)** is now a `python3` heredoc that
   imports `tests/m3/source_freeze` and calls
   `assert_code_frozen("src/python/pairwise_completeness_scan.py", "cb199b6", LANG_PY)`,
   printing `SCANNER CODE PIN ref: cb199b6` / `CODE PIN PASSED`. Check **(iii)**
   is demoted from a STOP to a FIELD RECORD. `HOW TO REGENERATE THIS GATE` now
   names exactly one legitimate change (`SCANNER_CODE_REF`) and states that a
   comment or docstring change updates NOTHING.
3. **A3 — §(1b) deleted** from `pcs_panelwide_reclassify.py` (2,519 chars).
   `grep -c DEFERRED` → **0**. §(1) survives and was strengthened.
4. **A4 — the deferred SECONDARY test landed**:
   `test_the_scanner_docstring_no_longer_claims_already_occluded_is_the_exclude_side`.
5. **A5 — the record**: `.planning/debug/260901-l55-step0-gate-rescope-off-the-byte-proxy.md` (363 lines).

## Verification gates — literal output

| # | Gate | Result |
|---|------|--------|
| 1 | `git apply --check` of the parked patch at base | **PASSES** |
| 2 | scanner md5 / size | `e03078ff73502c3c877b0d2ebf93941d` / `73772` → `fc1d68dff1f493f6eb57dd427bed638a` / `78843` |
| 2 | `code_lines(scanner)` vs `git show cb199b6:scanner` | **IDENTICAL, 540 / 540, before AND after the patch** |
| 3 | extracted gate block, scanner UNPATCHED | `SCANNER CODE PIN ref: cb199b6` / `CODE PIN PASSED` / `EXIT=0` |
| 3 | extracted gate block, scanner PATCHED | `SCANNER CODE PIN ref: cb199b6` / `CODE PIN PASSED` / `EXIT=0` |
| 4 | gate observed RED (code) / not-red (prose) | see "RED observations" below |
| 5 | `grep -c 'DEFERRED' src/python/pcs_panelwide_reclassify.py` | **0** |
| 6 | gate slice 32-hex md5 tokens / `73772` / `78843` | `[]` / `False` / `False` |
| 7 | `grep -c -- '--ancestry'` in the runbook | **0** |
| 8 | `git diff --stat` over the five forbidden surfaces | **EMPTY** |
| 9 | full `tests/m3` | **`1168 passed, 33 skipped, 4 warnings in 879.35s (0:14:39)`** |
| 10 | anything fired | **nothing** — no enclave, VM, Dataproc, OSF, `gsutil`, `gcloud` or network contact. **$0** |

## RED observations — both directions, with what was actually seen

### On disk, the gate block (Task 1e), scanner unpatched

* **GREEN before:** md5 `e03078ff…`; block → `CODE PIN PASSED`, `EXIT=0`.
* **Perturbation, derived from the UNIQUE anchor, not a blind `sed`:**
  `grep -c 'already_occluded=bool('` = **1** → `LN=616`; perturbed **line 617 only**
  (`deletion.pos <` → `deletion.pos <=`).
* **Proof it landed, and landed in CODE:** `git diff --numstat` → `1	1	src/python/pairwise_completeness_scan.py`;
  md5 moved to `bc8fa060ce736690e91ac4da15e69a30`; `ast.get_docstring(...) == <captured>` → **True**
  (so what moved was code). `__pycache__` cleared.
* **RED:**
  ```
  STOP -- the scanner's CODE has MOVED off the STEP 0 pin.
  the CODE of src/python/pairwise_completeness_scan.py (whole file) has MOVED off its pin cb199b6.
  first difference at code-line index 187:
    -   ... already_occluded=bool(deletion.pos < partner.pos <= deletion.span_end) ...
    +   ... already_occluded=bool(deletion.pos <= partner.pos <= deletion.span_end) ...
  (reference: 540 code lines / actual: 540 code lines)
  EXIT=1
  ```
  The wrapper prose also fired: *"IGNORE the 'tests/m3/test_source_freeze_pins.py' clause … The constant to update is SCANNER_CODE_REF, in this block."*
* **Restore proved:** md5 back to `e03078ff…`; `git status --porcelain <scanner>` **EMPTY**; block → `EXIT=0`.

### On disk, the PROSE direction (Task 2b) — the acceptance demonstration

The real patch (89+/10−, md5 and 5,071 bytes moved) left the block at
`CODE PIN PASSED` / `EXIT=0`. **A docstring correction costs nothing.**

### Session controls on the committed ENFORCER test (each reverted, then green)

| Control | Perturbation | Observed |
|---------|--------------|----------|
| A | `SCANNER_CODE_REF` → `1333f3f` (different scanner CODE) | RED at check (3): executed block exits 1, `assert 1 == 0` |
| B | `SCANNER_CODE_REF` → `HEAD` | RED at check (2): *"the STEP 0 code pin 'HEAD' is not an IMMUTABLE revision … the cheapest possible weakening"* |
| C | md5 + size re-inserted into the gate slice | RED at check (5): *"the STEP 0 gate carries a 32-hex content hash again: ['e03078ff73502c3c877b0d2ebf93941d']"* |

### Task 2f — RED-first for the two new docstring assertions (4 captures, 2 file states)

| # | File state (proved) | Assertion | Result |
|---|---------------------|-----------|--------|
| 1 | REVERTED, md5 `e03078ff…` / `73772`, `git status` empty | `test_the_scanner_docstring_no_longer_claims_…` | **RED** at `"already visible as ``already_occluded``" not in flat` |
| 2 | same | `test_the_tool_and_the_scanner_agree_…` claim (3) | **RED** at `_SCANNER_FALSE_CLAIM not in scanner_flat` |
| 3 | RE-APPLIED, md5 `fc1d68df…` / `78843` | as #1 | **GREEN** |
| 4 | same | as #2 | **GREEN** |

`__pycache__` cleared at every transition. **Bonus:** with the scanner reverted, the
rescoped CODE PIN enforcer was still GREEN (`1 passed`) — the gate is indifferent to
which prose state the scanner is in.

### In memory, committed (Test 4a / 4b inside the new enforcer)

* **4a CODE side:** anchor unique (`count == 1`), asserted OUTSIDE the docstring span,
  perturbation non-vacuous, `ast.get_docstring` UNCHANGED → `code_lines` **DIFFERS**.
* **4b PROSE side:** perturbation confined to `text[DS:DE]`, non-vacuous,
  `ast.get_docstring` **CHANGED** → `code_lines` **IDENTICAL**.

## Suite reconciliation — BY NAME, non-vacuous, both bases

**`1168 passed / 33 skipped / 0 failed`** (baseline `1167 / 33 / 0`).
Collected **1200 → 1201**; `1168 + 33 = 1201`. ✔

**REMOVED (exactly 2, nothing else):**
- `tests/m3/test_pairwise_completeness_scan.py::test_pending_paste_step0_pins_the_scanner_by_a_CURRENT_content_hash`
- `tests/m3/test_pcs_panelwide_reclassify.py::test_the_tool_docstring_carries_the_true_semantics_and_flags_the_scanners_false_claim`

**ADDED (exactly 3, nothing else):**
- `tests/m3/test_pairwise_completeness_scan.py::test_pending_paste_step0_pins_the_scanner_CODE_against_a_git_ref`
- `tests/m3/test_pairwise_completeness_scan.py::test_the_scanner_docstring_no_longer_claims_already_occluded_is_the_exclude_side`
- `tests/m3/test_pcs_panelwide_reclassify.py::test_the_tool_and_the_scanner_agree_that_already_occluded_is_anchor_relative`

**Per-file:** `test_pairwise_completeness_scan` 114 → **115**;
`test_pcs_panelwide_reclassify` 21 → **21**; `test_source_freeze_pins` 39 → **39**.
**Skips held at exactly 33** — no new test landed as a SKIP. The name diff is
non-empty and equals exactly the expected set.

`git checkout -- tests/m3/sparse_parent_benchmark.tsv` was run after the suite.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Bug] §(1) did not actually contain the token `PANEL-WIDE`**
- **Found during:** Task 2c
- **Issue:** the plan's replacement test asserts §(1) states `ANCHOR-RELATIVE` **and** `PANEL-WIDE`. MEASURED immediately after deleting §(1b): `ANCHOR-RELATIVE` True, **`PANEL-WIDE` False** — the uppercase token lived only inside the deleted note; §(1) said "panel-wide" in lower case once, in a trailing sentence.
- **Fix:** strengthened §(1) rather than weakening the assertion. §(1) is now the only place in that module carrying the correction, so it states the ANCHOR-RELATIVE vs PANEL-WIDE contrast explicitly, spells out the consequence, and names the runtime enforcer that demonstrates the disagreement. Weakening the assertion to a case-insensitive match would have been fixing the thermometer.
- **Files modified:** `src/python/pcs_panelwide_reclassify.py`
- **Commit:** `f9425b7`

**2. [Rule 1 — Bug] Three line-number citations falsified by the patch**
- **Found during:** Task 2c/2d
- **Issue:** the patch moves `already_occluded=bool(` from `:616` to `:687` (MEASURED), silently falsifying `pcs_panelwide_reclassify.py:6`, `tests/m3/test_pcs_panelwide_reclassify.py:7` and the `:122` reference in the kw8 banner.
- **Fix:** replaced with **symbol** citations (`pairwise_completeness_scan.enumerate_candidates`) rather than re-transcribing `:687` — a line number is a proxy that decays again on the next edit above it, which is the same scoping lesson this whole task is about. The third was inside the banner that Task 2e rewrote whole.
- **Files modified:** `src/python/pcs_panelwide_reclassify.py`, `tests/m3/test_pcs_panelwide_reclassify.py`, `tests/m3/test_pairwise_completeness_scan.py`
- **Commit:** `f9425b7`

**3. [Rule 1 — Bug] A quotation labelled "verbatim" was not verbatim**
- **Found during:** Task 3 self-check
- **Issue:** both the record and the runbook rendered `source_freeze.py`'s sentence as `Do not 'fix' it by freezing an unparse string` where the original says `Do not "fix" it ...`, and the runbook had also dropped the `**...**` emphasis while calling the quote verbatim. Caught by a byte-containment check against `tests/m3/source_freeze.py`, not by eye — the first grep for it produced a MISLEADING "MISSING", itself a text-vs-meaning artifact of the search string's own quoting.
- **Fix:** both documents now carry the clause byte-exact (verified by whitespace-normalised containment against `tests/m3/source_freeze.py`); the runbook's inline quote became an indented block quote so the nested double quotes are unambiguous. The record states that this fidelity is checked by measurement in-session, NOT by a committed enforcer — `feedback_a_claimed_invariant_needs_a_named_enforcer`.
- **Why it mattered enough to redo the commit:** this task exists because a guard let a known-false sentence stand. Shipping an inexact "verbatim" quotation inside the record of that lesson is the same defect one level up.
- **Files modified:** `.planning/debug/260901-l55-step0-gate-rescope-off-the-byte-proxy.md`, `.planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md`
- **Commit:** `f9425b7` — the fix was folded in by `git commit --amend` on the UNPUSHED `1ff812b` (verified `origin/m3-W2-aou-deltas == 04a9b2b`, i.e. nothing had been published and no other clone could hold the old hash), so the plan's ONE-COMMIT contract stays literally true. Affected files re-verified afterwards: gate slice still carries no 32-hex token and no byte size, `--ancestry` count still 0, `136 passed`, and the full suite re-run on the final committed tree.

### Bounded residual, recorded rather than omitted

Check (3) of the new enforcer (execute the block, require exit 0) is a GREEN
assertion whose committed RED counterpart would require writing the working tree
(`actual_text` is banned in every `tests/m3/` module but `test_source_freeze.py`).
Its RED is covered in memory by control 4a, on disk by the Task 1e control, and at
test level by SESSION CONTROL A.

## Authentication Gates

None. Nothing was fired; no credential or perimeter was touched.

## What did NOT change

`353089` / `353090`, 15 rows, 13 pairs, 10/3, offsets
`{-14:1, -9:1, -6:1, -3:1, -1:1, 0:10}`, panel-wide 12/1 pairs and 14/1 rows, the
3,094-row (0.876%) tail — **all untouched**. `git diff --stat` over
`.planning/amendments/`, `occlusion_span_filter.py`, `run_native_ld_panel.py`,
`fire_verifier.py`, `aou_ld_panel.py` is **EMPTY**. Gate check (iv) and the
`WHAT EACH FAILURE MEANS` block are verbatim.

## Known Stubs

None.

## Still open (unchanged by this task)

- `src/python/samepos_missingness_probe.py` — **BUILT / TESTED / NOT RUN**.
- Seth gets the measurement beside his sealed brief-blind prediction.

## Self-Check: PASSED

- All created files verified present on disk.
- Commit `f9425b7` verified present in `git log --all`.
- Full `tests/m3` re-run on the FINAL committed tree: **1168 passed / 33 skipped / 0 failed** in 816.92s.
