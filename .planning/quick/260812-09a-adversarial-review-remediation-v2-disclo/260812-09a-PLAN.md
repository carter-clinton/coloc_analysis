---
phase: quick/260812-09a
plan: 01
type: execute
wave: 1
depends_on: []
subsystem: disclosure-record-and-fire-surface
tags: [e2, disclosure, osf, manuscript, track-a, id-vs-ref-ld, rcw, pre-fire-gate, handoff, decisions, state, skill, m3, docs-only, adversarial-review]
autonomous: true
requirements:
  - A-BLOCKER-1-MECHANISM-CORRECT
  - A-BLOCKER-2-REMEDY-AND-SCOPE-CORRECT
  - A-BLOCKER-3-MS-BOUNDING-RESTORED
  - A-HIGH-1-UNIT-DISAMBIGUATION
  - A-MEDIUM-PROVENANCE-TRSX5-FRAMING-TILES
  - A-HARNESS-V2-DEFEAT-RESISTANT
  - B-BLOCKER-1-LIVENESS-POLL-COMMAND
  - B-HIGH-1-NO-276-PASS-BAR
  - B-MEDIUM-OPEN-ITEMS-URI-BIM-LASTKNOWN-BRANCH
  - B-LOW-LINE-ANCHORS-AND-LABEL-SCOPING
  - C-HIGH-LIVE-FIELD-CORRECTIONS
  - C-MEDIUM-CLAIM-LEVEL-SWEEP
  - C-DEC-2026-08-12-CANONICAL-TABLE
baseline_rev: 42c060e
branch: m3-W2-aou-deltas
files_modified:
  - .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-SELECTED-PAIR-correction-v2.md
  - .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-check-v2-pair.sh
  - .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-check-review.sh
  - .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-check-sweep.sh
  - .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-stale-claim-sweep.tsv
  - .planning/quick/260811-rcw-assemble-the-m3-04c-task-3-pre-fire-gate/260811-rcw-PRE-FIRE-GATE-REVIEW.md
  - .planning/HANDOFF.json
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/DECISIONS.md
  - .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md
  - .claude/skills/aou-ld-pipeline/SKILL.md
user_setup: []

must_haves:
  truths:
    - "A reader of the v2 disclosure pair learns WHICH join is which: that the eleven disclosed percentages were produced BY the shipped allele-aware 4-key matcher's own transposition counter (ld_allele_join.R:205-269, exact + swap keys, palindromes dropped, duplicated 4-keys removed from the match table), NOT by an allele-blind join; that the disclosed defect is that the computed orientation is measured and reported but DELIBERATELY NOT APPLIED to the QTL beta (run_qtl_coloc.R:478-479, a recorded decision — DEC-2026-08-07-e2-orientation-disposition option A); and that the allele-blind CHR:POS description belongs to the PRE-260805-o7o fine-map-side sumstats-to-panel join, which o7o fixed behind an `allele_aware` gate that config restricts to AFR — so EUR and TRANS fine-map joins are allele-blind TODAY and the fix is never described in repo-wide past tense."
    - "The v2 commitment names the REAL remedy (apply the measured orientation to the QTL beta, plus a GRCh38-to-GRCh37 allele reconciliation on the QTL side, plus an ancestry gate if Track A must not move) rather than 're-committing' to an allele-aware join that already shipped, and scopes the re-report to ALL affected ancestries' results that exist at remediation time — explicitly including the EUR results that exist today, because zero AFR coloc jobs exist (E-4: _ancestry_for_region returns EUR unconditionally) and applying the orientation moves PP.H4 for any pair with transposed variants including EUR. The commitment stays condition-bounded (bundled with E-4, once a real non-identity panel exists) and carries no schedule."
    - "The v2 manuscript paragraph carries all three bounding elements that v1 dropped: the population-not-realised-errors sentence; an explicit statement that the ANALYSIS CODE IS UNCHANGED by this disclosure (the correction is to the record and the forward analysis plan — framing B is not disposition option B, per DEC-2026-08-11-e2-framing-correction); and the statement that no posterior probability of colocalization is shown to be wrong. A Methods reader can no longer infer that a fix was made and results were regenerated."
    - "Both outgoing v2 bodies state BOTH measurement units with explicit labels and never call the tile median 'per-region': per TILE-ROW, 195 of 206 measured tile-rows affected with median 17.82% and maximum 38.68%; per LOCUS (collapsing the __tile/__sub suffix), 49 of 51 loci affected with median 0.4234% and maximum 38.6824%; and the five-region table is labeled as locus-unit."
    - "The v2 OSF body reasons the trsx5 interaction instead of asserting it is 'unaffected': it states that the posted 2026-07-10 occlusion amendment premises a POSITION-BASED panel-to-sumstats join and commits to lockstep exclusion so that no variant is present on one side and absent on the other, and that an allele-aware join with palindrome-dropping introduces a NEW one-sided drop class relative to that premise — stated either as bounded-compatible with the reason given, or as a noted premise update, with the palindrome-drop magnitude re-derived from e2-exposure-real-corpus.tsv and the fact that the class is COUNTED (dropped_palindromic) and therefore auditable."
    - "The v2 texts additionally carry every element the five-way review CLEARED in v1 and did not ask to change: all eleven figures re-derived to the digit from the two e2-exposure TSVs; the pooled 5.29% never appearing without the per-region figures and the dragged-down sentence; the identity-LD-stub caveat in both bodies; the 46/182=25.3% synthetic-fixture and 20.33%-vs-0.20% 100x disclosures correctly labeled and never presented as externally reported; the bindable (exact + flipped) denominator; the no-pre-registered-number-moved and what-does-not-change statements; the append-only new-supplementary-file destination discipline; and the separate-from-Check-2 scope boundary."
    - "The v2 harness is DEFEAT-RESISTANT where the v1 harness was defeated: figures are asserted adjacent to THEIR OWN region label rather than merely present in the file (an APOL1-to-CXADR label swap goes RED); the pooled-alone guard is scoped to the PASTE BLOCK so that the word 'dragged' in an out-of-block comment cannot satisfy it; (UN)DISCHARGED assertions use word boundaries so a DISCHARGED-for-UNDISCHARGED flip goes RED; no POSIX ERE bracket expression contains [^\\n]; and EVERY clause group carries expect_red coverage rather than the ms group alone."
    - "The rcw PRE-FIRE gate review carries the corrected liveness-poll command AT EVERY POINT OF USE (the §4 bucket-count row, the liveness-arbiter block, and STEP B) in the literal-bucket primary form, with the env-variable alternate quoted correctly and an explicit warning that WORKSPACE_BUCKET already contains gs:// and must never be prefixed — so the pre-fire 'expected 0' row can no longer FALSE-PASS on a gsutil usage error and a healthy fire can no longer be read as dead."
    - "The corrected rcw review states that 276 is NOT a pass bar: a partial bank is a real and reportable outcome, because verify_failed regions never upload and the loop continues on error."
    - "Every correction made to the rcw review is listed, with its finding ID, in a dated '## Corrections (2026-08-12)' changelog section — and every line-number and URI the corrections introduce was RE-ANCHORED against the tree at HEAD before being written down, per that document's own §2.1(8) discipline."
    - "The stale sweep is CLAIM-LEVEL: for each stale claim there is a recorded grep pattern, the full hit list across the record surfaces, and a per-hit disposition (fixed / left with reason) — so no site is missed the way a file-by-file registration missed six."
    - "The three live HANDOFF.json fields that assert falsehoods are corrected rather than left as history: gates.m3_04b no longer repeats the retracted 'all 7 pinned files 0-line diff' clause and points at the retraction instead; resume_on_reconnect carries a new dated 2026-08-12 entry at index 0 with the current state while the 2026-08-07 entry survives demoted in place; and resume_entry_point routes a reader to both 2026-08-11 decisions, the corrected rcw review, and this task's directory."
    - "DECISIONS.md carries an appended DEC-2026-08-12 entry recording the five-way review, the corrections applied, the SCOPED restatement of the SR4 grep counts (0 hits measured at 0e7e309, before the disposition entry itself named the basenames; at HEAD the same grep counts at least 1 by self-reference), the oku task-local-harness caveat (4 of 29 clauses observed red, ms group only; task-local, never CI), and the ONE CANONICAL residual/stale-site table that supersedes the three divergent lists — including the sites deliberately left alone with their reasons."
    - "Every mechanical gate in this plan was OBSERVED RED on a fixture COPY before its real green was trusted, and the named controls specifically include the APOL1-to-CXADR label swap, the block-scoped pooled-alone isolator, the UNDISCHARGED-to-DISCHARGED flip, the double-prefixed gsutil poll form, the HANDOFF.json parse and containment walkers, the DECISIONS.md byte-prefix append-only gate, and the STATE.md frontmatter byte-identity gate."
    - "Nothing was posted to OSF, no manuscript file was touched, no posted amendment body was edited, no file under src/ tests/ config/ Snakefile results/ moved, no perimeter call was made, and nothing was fired. The two 2026-08-11 decisions (framing B; SR4 never-frozen) are recorded as standing and are not reopened."
  artifacts:
    - path: ".planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-SELECTED-PAIR-correction-v2.md"
      provides: "the v2 outgoing pair — ms-correction-v2 and osf-correction-v2 paste blocks, with destinations, discharge conditions, the pre-placement journal-policy check, the no-agent-posts rule, and a v1-to-v2 delta table naming each finding ID"
      contains: "PASTE-BEGIN: osf-correction-v2"
      min_lines: 120
    - path: ".planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-check-v2-pair.sh"
      provides: "the v2 acceptance harness for the outgoing pair — clause groups ms and osf, every group with expect_red coverage, including the label-swap, block-scoped pooled-alone and UNDISCHARGED-flip controls"
      contains: "--self-test"
      min_lines: 250
    - path: ".planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-check-review.sh"
      provides: "the acceptance harness for the corrected rcw PRE-FIRE gate review — poll-command form, no-276-pass-bar, changelog completeness, re-anchored pointers, each with a control observed red"
      contains: "--self-test"
      min_lines: 150
    - path: ".planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-check-sweep.sh"
      provides: "the acceptance harness for the record-surface sweep — HANDOFF.json parse + containment walker, DECISIONS.md byte-prefix append-only gate, STATE.md frontmatter byte-identity, continue-here demote convention, SKILL.md no-deletion, and per-claim residual-hit assertions, each with a control observed red"
      contains: "--self-test"
      min_lines: 200
    - path: ".planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-stale-claim-sweep.tsv"
      provides: "the machine-readable claim-level sweep — one row per (claim_id, grep_pattern, file, line, verbatim_excerpt, disposition, reason) — and the SOURCE the DECISIONS.md canonical table is rendered from rather than retyped"
      contains: "claim_id"
      min_lines: 20
    - path: ".planning/quick/260811-rcw-assemble-the-m3-04c-task-3-pre-fire-gate/260811-rcw-PRE-FIRE-GATE-REVIEW.md"
      provides: "the corrected fire surface — poll command fixed at every point of use, no-276-pass-bar caveat, refreshed open-items table, real panel-TSV URI, honest real-.bim row, last-known labels, the branch (ii)/(iii) contradiction, the B-LOW re-anchors, and a dated Corrections changelog"
      contains: "## Corrections (2026-08-12)"
    - path: ".planning/DECISIONS.md"
      provides: "the appended DEC-2026-08-12 remediation entry carrying the review record, the corrections, the scoped SR4 grep restatement, the oku harness caveat, and the canonical stale-site table"
      contains: "DEC-2026-08-12"
    - path: ".planning/HANDOFF.json"
      provides: "corrected live fields — gates.m3_04b retraction pointer, a new dated resume_on_reconnect[0], a refreshed resume_entry_point, and dated STALE markers on four superseded gate rows"
      contains: "2026-08-12"
    - path: ".planning/STATE.md"
      provides: "dated supersession annotations on the RESUME-HERE bullets and the oku line, the corrected ld_npz_to_rds.R claim, a refreshed Session Continuity, and the oku enforced-by scoping fix — body only, frontmatter untouched"
      contains: "2026-08-12"
    - path: ".planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md"
      provides: "a prepended dated 2026-08-12 LATEST block with the 2026-08-07 marker demoted in place, per the close-session prepend-demote convention"
      contains: "2026-08-12"
    - path: ".claude/skills/aou-ld-pipeline/SKILL.md"
      provides: "dated banners marking the Wave-2 GATE 2/GATE 3 rows, the 322 = 161 x 2 region-set line and the 44-export egress line as describing the RETIRED Hail A.3 producer, with the current native-plink path and gate surface named; GATE 0/1/1.5 rows and every historical row preserved"
      contains: "2026-08-12"
  key_links:
    - from: ".planning/quick/260812-09a-.../260812-09a-SELECTED-PAIR-correction-v2.md"
      to: "src/snakemake/scripts/ld_allele_join.R and src/snakemake/scripts/run_qtl_coloc.R"
      via: "the mechanism sentences name the shipped allele-aware matcher as the SOURCE of the numbers and the not-applied orientation at the coloc call site as the disclosed defect, so a reviewer holding the code finds exact agreement"
      pattern: "ld_allele_join_indices|run_qtl_coloc"
    - from: ".planning/quick/260812-09a-.../260812-09a-SELECTED-PAIR-correction-v2.md"
      to: ".planning/phases/m3-aou-afr-ld-panel-build/e2-exposure-real-corpus.tsv and e2-exposure-track-a-regions.tsv"
      via: "every figure in both bodies, in BOTH units, re-derived from the TSVs by the v2 harness rather than copied from v1 prose"
      pattern: "e2-exposure-(real-corpus|track-a-regions)"
    - from: ".planning/quick/260811-rcw-.../260811-rcw-PRE-FIRE-GATE-REVIEW.md"
      to: "src/python/run_native_ld_panel.py"
      via: "the corrected poll command and the panel-TSV URI are anchored to the producer's actual write paths and default panel name, re-verified at HEAD"
      pattern: "AFR_aou|m3-W2-native-plink-panel"
    - from: ".planning/DECISIONS.md DEC-2026-08-12 canonical stale-site table"
      to: ".planning/quick/260812-09a-.../260812-09a-stale-claim-sweep.tsv"
      via: "the table is RENDERED from the sweep TSV, so the register and the evidence cannot diverge into a fourth list"
      pattern: "260812-09a-stale-claim-sweep"
    - from: ".planning/HANDOFF.json resume_entry_point and resume_on_reconnect[0]"
      to: "DEC-2026-08-11-e2-framing-correction, DEC-2026-08-11-sr4-disposition, the corrected rcw review, and this quick directory"
      via: "the resume surface routes a cold reader to the current decisions and the corrected fire surface instead of two stale 2026-08-07 blocks"
      pattern: "DEC-2026-08-11-(e2-framing-correction|sr4-disposition)"
---

<objective>
Remediate every finding from the 2026-08-11/12 five-way adversarial review of commit
range `7d575a5..42c060e` (Codex CLI v0.141.0 + four blind read-only investigators),
per Carter's "Run it".

**Purpose.** Three surfaces are wrong in ways that cost real things. (A) The outgoing
E-2 disclosure pair misstates which join was defective, commits to work that already
shipped, mis-scopes the re-report to an ancestry with zero jobs, dropped its bounding
sentences, and equivocates tile units for locus units — and its harness could not have
caught any of it. (B) The PRE-FIRE gate review hands Carter a liveness-poll command
that FALSE-PASSES its own pre-fire check and would read a healthy $385–1,084 fire as
dead. (C) The resume surfaces still carry a retracted claim in a LIVE status field, plus
three divergent stale-site lists.

**Output.** A v2 disclosure pair gated by a new defeat-resistant harness; the rcw review
corrected in place with a dated changelog; and a claim-level sweep of the record surfaces
landing one canonical stale-site table in a DEC-2026-08-12 entry.

**⛔ Out of scope, absolutely.** No OSF posting. No manuscript file edited. No posted
amendment body edited. No change under `src/`, `tests/`, `config/`, `Snakefile`,
`results/`. No perimeter contact of any kind (no `gsutil`, `gcloud`, `bq`, `wb` — not
even read-only control-plane). Nothing fired. The two 2026-08-11 decisions
(`DEC-2026-08-11-e2-framing-correction` framing B; `DEC-2026-08-11-sr4-disposition`
never-frozen) STAND and are not reopened — this task fixes texts and surfaces, never
decisions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-REVIEW-FINDINGS.md
@.planning/quick/260811-tf3-record-carter-decisions-e-2-framing-b-co/260811-tf3-SELECTED-PAIR-correction.md
@.planning/quick/260811-oku-discharge-e-2-disclosure-obligations-dra/260811-oku-e2-osf-entry-drafts.md
@.planning/quick/260811-oku-discharge-e-2-disclosure-obligations-dra/260811-oku-e2-manuscript-limitation-drafts.md
@.planning/quick/260811-oku-discharge-e-2-disclosure-obligations-dra/260811-oku-check-drafts.sh
@.planning/quick/260811-rcw-assemble-the-m3-04c-task-3-pre-fire-gate/260811-rcw-PRE-FIRE-GATE-REVIEW.md
@.planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md
@.planning/HANDOFF.json
@.claude/skills/aou-ld-pipeline/SKILL.md

**THE FINDINGS FILE IS THE LOCKED CONTRACT.** `260812-09a-REVIEW-FINDINGS.md` carries every
requirement, evidence pointer, fix form and constraint. Do not weaken any requirement to
make a task fit. If a finding proves unfixable AS SPECIFIED, **REPORT IT** in the SUMMARY
with the evidence — do not improvise a smaller version of it.

<interfaces>
<!-- Facts VERIFIED at HEAD 42c060e during planning. The executor RE-VERIFIES each before
     writing it down (the rcw review's own §2.1(8) re-anchoring discipline), and reports
     any that fail rather than propagating them. -->

**Which join is which (this is A-BLOCKER-1's whole substance):**

- `src/snakemake/scripts/ld_allele_join.R::ld_allele_join_indices()` — the COLOC-side
  allele-aware 4-key matcher. `k4_pan` at `:205`; duplicated 4-keys nulled out of the
  match TABLE at `:211-219`; `k4_exact` / `k4_swap` at `:221-222`; `m_exact` / `m_swap`
  at `:227-230`; palindromes (AT/TA/CG/GC) computed at `:232-234` and EXCLUDED by
  `keep_mask` at `:239`; `orient` = +1 exact / −1 swap at `:241`; the counters
  `exact` / `flipped` / `dropped_ambiguous` / `dropped_palindromic` / `dropped_mismatch` /
  `dropped_unusable` at `:257-269`. **It is this `flipped` counter that produced every
  percentage in the disclosure.** Sole production call site: `run_qtl_coloc.R:471`.

- `src/snakemake/scripts/run_qtl_coloc.R:478-479` — the verbatim comment
  `⚠ join$orient IS MEASURED AND REPORTED, AND DELIBERATELY NOT APPLIED.` continuing at
  `:479-484` with the reason (pre-existing on the legacy 1kG/EUR path; correcting it would
  move Track-A EUR numbers in submission). **This is the E-2 defect the disclosure is
  about** — a recorded decision (`DEC-2026-08-07-e2-orientation-disposition`, option A),
  not a bug nobody noticed.

- `src/legacy/region_analysis/scripts/run_susie_rss.R::match_indices_allele_aware` (defined
  ~`:220`, `k4_swap` `:274`, `dropped_palindromic` `:312`, dispatch
  `if (isTRUE(allele_aware))` `:331-332`) — the FINE-MAP-side twin landed by
  quick-260805-o7o for FINDING H. It is gated on `allele_aware`, which
  `config/pipeline.yaml`'s `ld_read_path` restricts to `ancestries: ['AFR']` (rcw `L-07`
  measured `{'enabled': True, 'ancestries': ['AFR'], 'allele_aware': True, 'coloc': True}`).
  **So EUR and TRANS fine-map joins take the legacy CHR:POS first-hit path TODAY.** The
  allele-blind CHR:POS description belongs HERE, in the past tense for AFR only — never
  repo-wide.

**The measured figures (re-derived during planning from the shipped TSVs; RE-DERIVE, do
not copy):**

- Substrate: `.planning/phases/m3-aou-afr-ld-panel-build/e2-exposure-real-corpus.tsv`
  (618 rows = 206 tile-rows x 3 ancestry arms; columns
  `ancestry region exact flipped dropped_ambiguous dropped_palindromic dropped_mismatch
  dropped_unusable reject n_catalog n_panel`) and `e2-exposure-track-a-regions.tsv`.
- **Per TILE-ROW (EUR arm):** 206 rows; 195 with at least one transposed pair;
  median 17.82%; max 38.68%.
- **Per LOCUS (EUR arm, collapsing `__tileN` / `__subNN`):** **51** loci; **49** affected;
  median **0.4234%**; max **38.6824%**. Derivation that reproduces it exactly:
  `awk -F'\t' 'NR>1 && $1=="EUR"{r=$2; sub(/__tile[0-9]+$/,"",r); sub(/__sub[0-9]+$/,"",r);
  e[r]+=$3; f[r]+=$4} END{for(k in e) printf "%s\t%.4f\n", k, f[k]/(e[k]+f[k])*100}'`.
- **Palindrome-drop magnitude (EUR arm, needed for the trsx5 reasoning):** `exact` 714,382 /
  `flipped` 31,152 / `dropped_palindromic` **144,176** = **19.34%** of the bindable set.
  It is COUNTED, therefore auditable — that is the honest bound on the new drop class.

**Producer facts for Part B (verified at HEAD):**

- `src/python/run_native_ld_panel.py:122` `_DEFAULT_PANEL_NAME = "m3-W2-native-plink-panel.tsv"`;
  `:734` `panel_tsv = panel_tsv or _gs_join(gs_out_dir, _DEFAULT_PANEL_NAME)`; the module
  docstring `:72` shows `--panel-tsv gs://<bucket>/ld/AFR_aou/m3-W2-native-plink-panel.tsv`.
  With the canonical bucket from `SKILL.md:12` this resolves to
  `gs://rw-migration-aou-rw-476cdac2/ld/AFR_aou/m3-W2-native-plink-panel.tsv`.
  **Codex guessed the same string — do not take that as confirmation; the executor
  re-derives it from the producer and the m3 records and reports any mismatch.**
- `SKILL.md:43` — `echo $WORKSPACE_BUCKET` "must be `gs://rw-migration-aou-rw-476cdac2`".
  **The variable ALREADY CARRIES `gs://`.** That is why
  `gsutil ls gs://${WORKSPACE_BUCKET}/...` errors to stderr, prints nothing to stdout, and
  `wc -l` returns 0 — a FALSE PASS against an "expected: 0" row and a false "dead" reading
  during STEP B polling. Same defect class already fixed once in quick-260611-tbw (gap C3).

**GNU grep dialect (measured on this node during planning):**

- `/usr/bin/grep --version` -> **GNU grep 3.6**. A script run through its shebang gets
  `/usr/bin/grep`; the interactive `ugrep` seen by an agent shell is a **CLI wrapper
  artifact** and must not be cited as the runtime dialect.
- **D4-07 confirmed empirically:** `printf 'aXb\n' | grep -cE 'a[^\n]b'` -> **1** and
  `printf 'anb\n' | grep -cE 'a[^\n]b'` -> **0**. Inside a POSIX ERE bracket expression
  `[^\n]` means "not backslash, not the letter n". **Never use it.** Use `.` with
  line-oriented grep.

**SR4 grep self-reference (Codex #4, verified):** at HEAD,
`grep -c '<basename>' .planning/DECISIONS.md` returns 1 / 1 / 1 / 2 / 2 for
`occlusion_manifest.py`, `occlusion_present_rate_scan.py`,
`drop_occluded_from_sumstats.py`, `ld_npz_to_rds.R`, `pipeline.schema.yaml` — because
`DEC-2026-08-11-sr4-disposition` itself names them. The dossier's "0 hits" was measured at
`0e7e309` (`git cat-file -t 0e7e309` -> commit), BEFORE that entry existed. The restatement
must be scoped that way, not silently left to read false at HEAD.

**The `.continue-here.md` prepend-demote convention (observed in the file):** the live top
block reads `> **2026-08-07 SESSION CLOSE (★★ LATEST — SUPERSEDES every block below ★★): ...**`
(`:11`) and the block below it reads
`> **2026-08-06 POST-sr4 (SUPERSEDED by the 2026-08-07 block above): ...**` (`:44`).
Demotion = rewrite the parenthetical, never delete the block.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the v2 outgoing disclosure pair and gate it with a new defeat-resistant harness (PART A)</name>

  <files>
.planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-SELECTED-PAIR-correction-v2.md (new)
.planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-check-v2-pair.sh (new, chmod +x)
  </files>

  <action>
**Write the v2 pair FRESH in this quick directory. The v1 artifacts stay byte-untouched as
history** — do not edit `260811-tf3-SELECTED-PAIR-correction.md`, the two `260811-oku` draft
files, or `260811-oku-check-drafts.sh`. v2 supersedes v1 by existing and by being named in
the DEC entry (Task 3); v1 remains the record of what was considered.

**Step 1 — re-derive every figure before writing any prose.** Compute, from
`e2-exposure-real-corpus.tsv` and `e2-exposure-track-a-regions.tsv`, both unit sets (the
`<interfaces>` block gives the exact awk collapse and the values planning obtained). Write
the derivation commands into the v2 file's author-notes section. If any value disagrees
with `<interfaces>`, **the TSV wins and you REPORT the discrepancy** — do not silently
adopt either.

**Step 2 — write `ms-correction-v2` and `osf-correction-v2` as PASTE blocks** (markers
`<!-- PASTE-BEGIN: ms-correction-v2 -->` / `<!-- PASTE-END: ms-correction-v2 -->` and the
`osf-correction-v2` pair). Content requirements, all mandatory:

*A-BLOCKER-1 — the mechanism, stated so a reviewer holding the code finds exact agreement.*
Both bodies must (i) attribute the disclosed percentages to the SHIPPED allele-aware 4-key
matcher's own transposition counter — exact + swap keys, palindromic sites dropped,
duplicated 4-keys removed from the match table — i.e. the numbers come from the CORRECT
join measuring the population, not from a defective one; (ii) state that the disclosed
property is that the computed orientation is measured and reported but DELIBERATELY NOT
APPLIED to the QTL beta in the current pipeline, and that this is a recorded decision
(`DEC-2026-08-07-e2-orientation-disposition`, option A), not an oversight; (iii)
distinguish the joins by name/role, and confine the allele-blind CHR:POS description to
the PRE-260805-o7o fine-map-side sumstats-to-panel join — **noting it is fixed for the
African-ancestry arm only and remains allele-blind on the European arm today**, because
the `allele_aware` gate is ancestry-scoped. ⛔ **Delete outright** v1's claim that the
catalog-to-panel join "matched on coordinates alone and ignored the alleles" — it is false
of that join.

*A-BLOCKER-2 — the remedy and the re-report scope.* The `osf-correction-v2` closing
commitment must name the REAL remedy: apply the measured orientation to the QTL beta; a
GRCh38-to-GRCh37 allele reconciliation on the QTL side (the QTL frame is GRCh38, the panel
and catalog are GRCh37, so there is no position join available on the QTL side at all);
and an ancestry gate if Track A must not move. It must scope the re-report to **all
affected ancestries' results that exist at remediation time, explicitly including the
European-ancestry results that exist today** — because zero African-ancestry colocalization
jobs exist (E-4) and applying the orientation moves PP.H4 for any pair with transposed
variants, including EUR. Keep it condition-bounded (bundled with E-4, once a real
non-identity panel exists) and give NO schedule. ⛔ **Delete** v1's "the join is made
allele-aware" commitment (already shipped) and v1's "the affected African-ancestry results
are regenerated" scoping (an ancestry with no results).

*A-BLOCKER-3 — the manuscript paragraph's bounding.* `ms-correction-v2` must carry: the
population-in-which-an-orientation-error-can-occur / not-a-count-of-realised-errors
sentence; an explicit statement that **the analysis code is unchanged by this disclosure**
and that the correction is to the record and to the forward analysis plan (framing B is not
disposition option B); and the sentence that no posterior probability of colocalization is
shown to be wrong. A Methods reader must not be able to infer that a fix was made and
results regenerated.

*A-HIGH-1 — units.* Both bodies state BOTH units with explicit labels and never call the
tile median "per-region": per TILE-ROW 195 of 206 affected, median 17.82%, max 38.68%
(206 tile-rows per arm across 51 loci); per LOCUS 49 of 51 affected, median 0.4234%, max
38.6824%. Label the five-region table as locus-unit.

*A-MEDIUM (all five).* (a) `ms-correction-v2` states the measurement basis — the shipped
`ld_allele_join_indices()` run over the 207 real region variant catalogs — and carries a
provenance pointer to the paired OSF entry. (b) "published direction of effect" ->
"**reported** direction of effect" (the manuscript is in submission), everywhere in both
bodies. (c) **Reason the trsx5 interaction** rather than asserting it is "unaffected": the
posted 2026-07-10 occlusion amendment premises a POSITION-BASED panel-to-sumstats join and
commits to lockstep exclusion so no variant is present on one side and absent on the other;
an allele-aware join with palindrome-dropping introduces a NEW one-sided drop class
relative to that premise. State it honestly — either bounded-compatible **with the reason
given**, or as a noted premise update — and bound it with the re-derived
`dropped_palindromic` magnitude and the fact that the class is COUNTED and therefore
auditable. Verify the amendment's premise lines yourself in
`.planning/amendments/osf-amendment-afr-occlusion-exclude-UPDATE-2026-07-10.md` before
characterising them. (d) Add an original-research framing sentence per the posted 2026-07-03
precedent, adapted because this IS a correction entry (e.g. hypothesis-driven original
research disclosing a measured property of its own pipeline; not a salvage of prior work).
⛔ The word "revision" must appear nowhere. (e) `FTO_16q12 (3 cells)` -> `(3 tiles)`.

*Everything the review CLEARED must survive.* Carry forward, unchanged in substance: all
eleven figures; the pooled 5.29% never appearing without the per-region figures and the
dragged-down sentence; the identity-LD-stub caveat in both bodies; the 46/182 = 25.3%
synthetic-fixture disclosure and the 20.33%-vs-0.20% 100x-error disclosure, both correctly
labeled as internal-record corrections never externally reported; the bindable
(exact + flipped) denominator with its reason; the "we checked and it is immaterial is not
defensible for APOL1_22q12 or FTO_16q12" sentence; the two-of-five-materially-exposed
statement and the SH2B3 anchor-0.00%/tile-3-20.33% split; the no-pre-registered-number-moved
and what-does-not-change paragraphs; the new-supplementary-file-on-az52u append-only
destination discipline; and the "does not discharge the separate Check-2 obligation" scope
boundary. Also carry the v1 wrapper material: destinations, discharge conditions,
obligation status table, the PRE-PLACEMENT journal-policy check (Carter's), the standing
two number rules, and the ⛔ no-agent-posts-or-places rule.

⚠ **On length.** The v1 `ms` clause capped a block at 120–200 words. The required v2 content
(mechanism precision + both units + code-unchanged + measurement basis + provenance pointer)
will exceed it. **Set the v2 bound to what the content demands and RECORD the change with
its reason in the harness header.** ⛔ Never drop a required clause to fit an inherited
bound.

Add a `## v1 -> v2 delta` table to the v2 file: one row per finding ID (A-BLOCKER-1/2/3,
A-HIGH-1, the five A-MEDIUM items) naming what changed and the evidence pointer.

**Step 3 — write `260812-09a-check-v2-pair.sh`** (bash, `set -uo pipefail`, own shebang,
`--only ms|osf`, `--self-test`, exit 0 only when every clause passes; a missing deliverable
is a LOUD FAIL, never a skip). It reads ONLY the v2 file. Required properties:

- **D4-01 number-to-region fidelity, not token presence.** For each (figure, region-label)
  pair, assert the figure occurs within N characters of ITS OWN label — not merely present
  in the block. Implement adjacency with a line-oriented or window-oriented extraction; do
  NOT reach for `[^\n]` (see D4-07).
- **D4-02 block-scoped pooled-alone guard.** The pooled-5.29% guard operates on the
  EXTRACTED PASTE BLOCK, never on the whole file, so the word "dragged" in an out-of-block
  comment cannot satisfy it.
- **D4-03 word-boundary (UN)DISCHARGED.** Where UNDISCHARGED is meant, require
  `\bUNDISCHARGED\b` present AND reject a standalone `\bDISCHARGED\b` (guard the preceding
  character so the UN- form does not self-match).
- **D4-07 dialect hygiene.** No `[^\n]` in any bracket expression anywhere in the script.
  Add a self-check clause that greps the script itself for the forbidden construct.
- **Grep provenance note** in the header, CORRECT: runtime is `/usr/bin/grep` GNU 3.6 via
  the shebang; the interactive `ugrep` is a CLI wrapper artifact and must not be propagated
  as the runtime dialect. Cite the two measured directions.
- **Clause coverage.** Clauses for every requirement above: both-unit presence with labels;
  the mechanism triple (i)/(ii)/(iii); the code-unchanged sentence; the not-realised-errors
  bounding; the real-remedy tokens; the all-ancestries-including-EUR re-report scope;
  the measurement basis + provenance pointer; "reported" not "published" direction of
  effect; the trsx5 reasoning tokens; the original-research framing sentence; absence of
  "revision"; `(3 tiles)` not `(3 cells)`; absence of the deleted false mechanism phrasing;
  absence of the deleted already-shipped commitment; every cleared-in-v1 element; and every
  figure re-derived from the TSVs at run time rather than hard-coded where feasible.
- **expect_red coverage for EVERY clause group** — not the ms group alone. Reuse the v1
  `expect_red` shape (fixture COPIES in a scratch dir; assert non-zero exit AND that the
  failure lands on the NAMED clause; `sole=yes` where the mutation isolates one clause).
  **Named controls that MUST exist and MUST be observed red:** (1) an APOL1<->CXADR label
  swap; (2) pooled kept + the dragged-down sentence removed INSIDE the block, with a
  companion control proving the word "dragged" in an out-of-block comment does NOT satisfy
  the clause; (3) an UNDISCHARGED -> DISCHARGED flip; plus at least one control per
  remaining clause group.
- The self-test must print each control's FAIL lines and end with an explicit
  `SELF-TEST PASSED/FAILED` verdict; a control that passes is reported as
  `CONTROL DEFEATED` and fails the self-test.

**All controls operate on fixture COPIES in a scratch directory. Never mutate the real v2
file.**
  </action>

  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis/.planning/quick/260812-09a-adversarial-review-remediation-v2-disclo && chmod +x 260812-09a-check-v2-pair.sh && ./260812-09a-check-v2-pair.sh --self-test && ./260812-09a-check-v2-pair.sh && cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && git diff --exit-code -- .planning/quick/260811-oku-discharge-e-2-disclosure-obligations-dra .planning/quick/260811-tf3-record-carter-decisions-e-2-framing-b-co && ! grep -q '\[\^\\n\]' .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-check-v2-pair.sh</automated>
  </verify>

  <done>
`--self-test` exits 0 with the APOL1<->CXADR label swap, the block-scoped pooled-alone
isolator, the UNDISCHARGED flip and at least one control per remaining clause group each
printed as OBSERVED RED on its named clause; the real run exits 0 against the v2 file;
`git diff --exit-code` over the oku and tf3 directories is EMPTY (v1 untouched); the
script contains no `[^\n]` bracket construct; and both v2 bodies carry the corrected
mechanism, the real remedy with the all-ancestries-including-EUR re-report scope, the three
restored bounding elements, both labeled unit sets, the reasoned trsx5 interaction, the
original-research framing sentence, `(3 tiles)`, and every element the review cleared in v1.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the rcw PRE-FIRE gate review IN PLACE, at point-of-use, with a dated changelog (PART B)</name>

  <files>
.planning/quick/260811-rcw-assemble-the-m3-04c-task-3-pre-fire-gate/260811-rcw-PRE-FIRE-GATE-REVIEW.md (modified)
.planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-check-review.sh (new, chmod +x)
  </files>

  <action>
Corrections go **IN PLACE at point-of-use**, because this is the document Carter reads
immediately before a ~11-day / $385–1,084 fire and an addendum at the bottom is one he may
never reach. A dated `## Corrections (2026-08-12)` changelog section is added **in
addition**, listing every edit with its finding ID — never instead of the in-place fix.

⚠ **Re-anchor before you write.** This document's own §2.1(8) rule is that any line number
from an older record must be re-verified against the tree before it is acted on. That rule
now applies to the corrections themselves: **verify every line number, path and URI below
against HEAD before writing it down.** If any proves wrong, use the measured value and say
so in the changelog. If a finding proves unfixable as specified, **REPORT it** — do not
improvise.

**B-BLOCKER-1 — the liveness arbiter (the false pass).** The command
`gsutil ls gs://${WORKSPACE_BUCKET}/ld/AFR_aou/*.npz | wc -l` double-prefixes: per
`SKILL.md:43` the variable ALREADY contains `gs://`, so gsutil errors to stderr, stdout is
empty and `wc -l` prints 0 — a FALSE PASS on the pre-fire "expected: 0" row, and a healthy
fire read as dead during STEP B polling. Fix **both forms at all three points of use** —
the §4 bucket-`.npz`-count row (~`:281`), the ⚠ THE LIVENESS ARBITER FOR THE FIRE block
(~`:287-296`), and STEP B (~`:418-421`):

- **primary, literal bucket:** `gsutil ls gs://rw-migration-aou-rw-476cdac2/ld/AFR_aou/*.npz | wc -l`
- **alternate, env form correctly quoted:** `gsutil ls "${WORKSPACE_BUCKET}/ld/AFR_aou/"*.npz | wc -l`
- an explicit warning at each site: **the variable already contains `gs://` — never prefix it**
- note it is the same defect class the project already fixed in quick-260611-tbw (gap C3)
- note the producer path is verified correct: `run_native_ld_panel.py:925-926` writes
  `{bucket}/ld/AFR_aou/{region_id}.npz`

**B-HIGH-1 — 276 is not a pass bar.** Restore the m3-04c PLAN's caveat (PLAN `:1494-1497`):
do NOT hardcode 276 as a pass bar — **a partial bank is a real, reportable outcome**, because
`status="verify_failed"` regions never upload (`:920-926`) and the loop continues on error
(`:942`). Add it to the liveness-arbiter paragraph **and** to STEP B.

**B-MEDIUM (five items).**
1. Refresh the §6 fire-time open-items table (~`:476-477`) and **date the refresh**: the
   E-2 row's obligation **(3) is DISCHARGED** (`DEC-2026-08-11-e2-framing-correction`) while
   **(1) and (2) remain open and are Carter's external actions**; the SR4-OPEN row is
   **DECIDED** (`DEC-2026-08-11-sr4-disposition`, never-frozen). Point the E-2 row at the v2
   pair from Task 1 as the current outgoing text.
2. §4 panel-TSV row: replace the `<panel-uri>` placeholder with the **actual URI**. Derive
   it from the repo — `run_native_ld_panel.py` `_DEFAULT_PANEL_NAME` and the gs default
   join, plus the canonical bucket — **not from Codex's guess.** Record in the changelog how
   you derived it.
3. §4 real-`.bim` row: it currently says "Byte-check..." with no executable command. Either
   supply a real command **with its path**, or state honestly that the exact command is
   determined in-perimeter and that **the 0-vs-1-based index-origin question is OPEN** (see
   the m3-07/16 record). Do not leave an un-runnable instruction wearing the shape of one.
4. §2 rows for the egress ruling, the CDR pin and the cohort rebuild: label each
   **"last-known (dated record; not re-verifiable from NC-State)"**. Then EITHER add a §4
   gate-time recheck row for the cohort MTs (`du` + `count_cols`/`count_rows` per the skill's
   invariant 1) OR state explicitly why fire-time re-verification is not required.
5. Add a new §2.1 divergence for the **PLAN's branch-(ii)/(iii) self-contradiction**: PLAN
   `:1391-1393` says degraded only under branch (iii) — which agrees with the code
   (`assemble_occlusion_catalog.py:230` scalar provenance, `:415` GATE 1, `:460` GATE 2) —
   while PLAN `:1505-1509` and `:1527` say branch (ii). State the **code-correct reading**
   so a post-fire auditor is not hit cold.

**B-LOW (four items).**
1. Fix three wrong line corrections inside §2.1(8) and their §5 PRE-FIRE 1 echo: the
   `occlusion_manifest.py` write is `append_region_manifest` `:195-196` (fresh-file path
   `:181`), **not** `:203-208`; `run_native_ld_panel.py:507` is a **docstring** (say so, or
   cite the code line instead); the `run_susie_rss.R` thresholds are `:715-718` — the cited
   `:713-716` excludes two of the three.
2. "four `test_negative_control_pre_change_*` tests" -> **three** (`:935`, `:1018`, `:1296`).
3. Annotate the L-row labels so each claims exactly what its command proves: **L-11** is a
   file-wide grep (the SCOPED proof is L-13's
   `test_run_finemap_shell_passes_the_declared_ld_matrix`); **L-09** reads a config value
   only (the fail-fast ENFORCEMENT lives at `m3_convert_npz_rds.smk:132/163/180` +
   `ld_npz_to_rds.R:272` and is not measured by the L-set).
4. Annotate **L-16**'s `2>/dev/null` missing-directory blindness (an absent directory and an
   empty one both print 0).

**The changelog.** Append `## Corrections (2026-08-12)` at the end: one row/bullet per edit
carrying the finding ID (B-BLOCKER-1, B-HIGH-1, B-MEDIUM-1..5, B-LOW-1..4), what changed,
where it changed, and the evidence (measured at HEAD / named record). Note the review's
2026-08-11 anchor and evidence log are UNCHANGED — this is a corrections layer over a dated
measurement, not a re-measurement.

**Write `260812-09a-check-review.sh`** (same shape and dialect discipline as Task 1;
`--self-test`, exit 0 only on all-pass, missing file = LOUD FAIL). Clauses at minimum:
the literal-bucket primary poll form present at **all three** point-of-use sites; the
double-prefixed form `gs://${WORKSPACE_BUCKET}` present **nowhere** except inside an
explicitly-marked warning context; the never-prefix warning present at each site; the
no-276-pass-bar caveat present in both the liveness paragraph and STEP B; the `<panel-uri>`
placeholder gone and the derived URI present; the changelog section present and naming
**every** finding ID; the corrected line anchors present and the superseded ones absent;
the E-2/SR4 open-items rows carrying their DEC ids. **expect_red coverage for every clause
group** on fixture COPIES, including a control that reintroduces the double-prefixed poll
form and is OBSERVED RED.

⛔ No perimeter contact — every command in this document is an instruction FOR CARTER; none
may be run. ⛔ Modify no file other than the rcw review and the new script.
  </action>

  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis/.planning/quick/260812-09a-adversarial-review-remediation-v2-disclo && chmod +x 260812-09a-check-review.sh && ./260812-09a-check-review.sh --self-test && ./260812-09a-check-review.sh && cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && ! grep -q '\[\^\\n\]' .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-check-review.sh && test "$(git diff --name-only 42c060e -- . | grep -cv -e '^\.planning/quick/260812-09a-' -e '^\.planning/quick/260811-rcw-')" = "0"</automated>
  </verify>

  <done>
`--self-test` exits 0 with every control — including the reintroduced double-prefixed poll
form — printed as OBSERVED RED on its named clause; the real run exits 0; the literal-bucket
poll command and the never-prefix warning appear at all three points of use; the
double-prefixed form survives nowhere as an instruction; the no-276-pass-bar caveat is in
both the liveness paragraph and STEP B; `<panel-uri>` is replaced by a URI derived from the
producer and that derivation is recorded; the real-`.bim` row is either runnable or honestly
labeled; the three §2 rows carry last-known labels; the branch (ii)/(iii) contradiction is
recorded with the code-correct reading; all four B-LOW items are fixed or annotated; a dated
`## Corrections (2026-08-12)` section names every finding ID; and the only files changed
versus `42c060e` are the rcw review and this quick directory.
  </done>
</task>

<task type="auto">
  <name>Task 3: Claim-level stale sweep of the record surfaces + the DEC-2026-08-12 canonical addendum (PART C)</name>

  <files>
.planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-stale-claim-sweep.tsv (new)
.planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-check-sweep.sh (new, chmod +x)
.planning/HANDOFF.json (modified)
.planning/STATE.md (modified, BODY ONLY)
.planning/ROADMAP.md (modified)
.planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md (modified)
.claude/skills/aou-ld-pipeline/SKILL.md (modified)
.planning/DECISIONS.md (APPENDED ONLY)
  </files>

  <action>
**Sweep by CLAIM, not by file.** The v1 sweep registered sites file-by-file and missed six
or more. For EACH stale claim below, run a grep for the claim across all record surfaces
(`.planning/*.md`, `.planning/*.json`, `.planning/phases/m3-aou-afr-ld-panel-build/*.md`
including `.continue-here.md`, `.claude/skills/**`), enumerate EVERY hit, and give each hit
an explicit disposition. Write the result to `260812-09a-stale-claim-sweep.tsv` with columns
`claim_id  grep_pattern  file  line  excerpt  disposition  reason`. **The DECISIONS.md
canonical table in this task is RENDERED FROM THIS TSV, never retyped** — that is what stops
a fourth divergent list from being born.

⚠ `grep -r` does not follow symlinks on this tree. Scope the sweep to the named record
surfaces explicitly rather than recursing from the repo root, and state the scope in the TSV
header so the counts are reproducible.

**The claims to sweep** (at minimum): C1 the retracted "all 7 pinned files 0-line diff vs
`bf16289`"; C2 "ZERO Carter decisions outstanding"; C3 "THREE E-2 obligations / all
UNDISCHARGED"; C4 the "open LIMITATION-vs-CORRECTION question"; C5 SR4-OPEN presented as an
open question; C6 `ld_npz_to_rds.R` asserted frozen / unchanged / byte-unchanged; C7 the
superseded suite figure `548P/31S/0F`; C8 the unit conflation "per-region median 17.82%";
C9 the superseded gate narratives (`panel_reachability` OPEN, `blocker1_ld_read_path` NOT
YET IMPLEMENTED, `aou_loop_refire` BLOCKED on panel reachability); C10 the oku "enforced by"
overclaim read as standing 29-clause enforcement.

**Then apply the fixes. HANDOFF.json — parse after EVERY edit.**
- `gates.m3_04b` (~`:47`): **STRIKE** the verbatim retracted clause and replace it with a
  pointer to the retraction (`verified_this_session_firsthand[5]` /
  `DEC-2026-08-11-sr4-disposition`). This is a correction to a LIVE status field, not
  history-rewriting — say so in the replacement text.
- `resume_on_reconnect` (~`:24`): **prepend a new dated `▶ #0 (2026-08-12 — SUPERSEDES every
  earlier #0)`** carrying the current state (nothing running, $0, no perimeter contact,
  `origin == 42c060e` plus this task's commits; both 2026-08-11 decisions recorded; E-2
  obligations (1) and (2) open as Carter's external actions with (3) discharged; the v2 pair
  is the current outgoing text; the corrected rcw review is the fire surface; the fire is
  Carter's terminal gate). **Demote the old entry in place** — retitle it, do not delete it.
- `resume_entry_point` (~`:131`): add `DEC-2026-08-11-e2-framing-correction`,
  `DEC-2026-08-11-sr4-disposition`, the corrected rcw review path, and this quick directory.
- Gate rows `m3_04c` (548P, `:48`), `panel_reachability` (`:49`), `aou_loop_refire` (`:50`),
  `blocker1_ld_read_path` (`:53`): **prefix each with a dated
  `⚠ STALE 2026-08-12 — superseded by ...`** marker naming the superseding record (the rcw
  review §2 / `suite_baselines`). **Do not delete the bodies.**
- `timestamp` (`:3`): update to the sweep time **with the sweep as the named reason**, or
  leave it for close-session — executor's call, **disclosed either way** in the SUMMARY and
  the DEC entry.

**STATE.md — BODY ONLY; the frontmatter must stay byte-identical.**
- `:55` (the line THIS RANGE ADDED into the ★★ RESUME HERE ★★ block, "ALL THREE E-2
  obligations remain UNDISCHARGED", falsified 17 commits later): dated supersession
  annotation in place.
- `:47-49` (the "ZERO decisions outstanding" / "three undischarged" / "SR4-OPEN question"
  bullets): dated supersession annotations.
- `:15`: correct the false "`ld_npz_to_rds.R` byte-unchanged" claim (it is +313/−62 and
  never-frozen per `DEC-2026-08-11-sr4-disposition`).
- `:17` `last_activity` "2026-08-04 (LATEST)": this is the FRONTMATTER zone and the YAML is
  pre-existing-broken. **Do not attempt frontmatter surgery.** Record it as a body-side note
  in the sweep record and in the DEC entry.
- `:1633` Session Continuity: refresh to current (it still reads SR4-OPEN as new and NEXT =
  Carter decides E-2).
- The two oku "enforced by" surfaces: rescope to
  **"gated at selection time by (task-local, not CI)"**. ⛔ Do not edit the closed oku
  SUMMARY/VERIFICATION — the harness caveat is recorded in the DEC entry instead.
- ⚠ **Reconciliation you must state, not stumble over:** `DEC-2026-08-11-sr4-disposition`
  protects the dated historical `>` blocks (`STATE.md:266,:278,:297,:301,:311,:349,:362`;
  `.continue-here.md:57,:113,:205,:217,:233,:249,:252`) from rewriting. The lines edited here
  (`:15`, `:47-49`, `:55`, `:1633`) are **not** in that protected set. Verify that before
  editing and record the check.

**ROADMAP.md `:1077`:** correct the "leave the raw panel `.npz` contract frozen
(`ld_npz_to_rds.R` unchanged)" claim — it is +313/−62 and never-frozen.

**`.continue-here.md`:** prepend a new dated `2026-08-12` LATEST block per the observed
close-session prepend-demote convention (current state: both decisions recorded; obligations
(1)+(2) open and Carter's; the v2 pair is the outgoing text; the corrected rcw review is the
fire surface; the fire gate is Carter's), and **demote the 2026-08-07 marker in place** by
rewriting `(★★ LATEST — SUPERSEDES every block below ★★)` to
`(SUPERSEDED by the 2026-08-12 block above)`. Update the `status:` frontmatter field
(`:7`). ⚠ `:39-40` are inside the dated historical block: **the fix for them IS the
demotion**, not an in-place rewrite — state that reasoning in the sweep record.

**`.claude/skills/aou-ld-pipeline/SKILL.md`:** add **dated 2026-08-12 banners** — no
deletions of historical rows — to the Wave-2 gate table and the LD-compute section, marking
**GATE 2 / GATE 3**, the `322 = 161 M2 regions × 2 ancestries` region-set line and the
`Egress = 44 export requests` line as describing the **RETIRED Hail A.3 producer**. The
banner states the current path: native plink (`src/python/run_native_ld_panel.py`,
Hail-free, AFR-only, **276** AFR regions — `config/ld_regions.tsv` 552 rows = 276 × 2
ancestries), egress redefined to at most 22 AFR chromosome groups plus size splits, and the
current gate surface = m3-04c Task 3 + the corrected rcw review. **GATE 0 / GATE 1 / GATE
1.5 rows remain LIVE and must be marked as such.**

**DECISIONS.md — APPEND ONLY.** Append `## 2026-08-12 — DEC-2026-08-12-...` following the
file's existing entry shape, carrying: (a) the five-way review record (Codex CLI v0.141.0 +
four blind read-only investigators; range `7d575a5..42c060e`; what it CLEARED as well as
what it found — the clears are load-bearing); (b) the corrections applied, by part and
finding ID, with the v2 pair named as the current outgoing text and v1 named as superseded
history; (c) **the scoped SR4 grep restatement** — the dossier's 0 hits were measured at
`0e7e309`, before the disposition entry itself named the basenames; at HEAD the same grep
counts ≥1 by self-reference (state the measured per-basename counts). This does not weaken
the disposition; it scopes its evidence honestly; (d) **the ONE CANONICAL residual/stale-site
table**, rendered from `260812-09a-stale-claim-sweep.tsv`, listing sites FIXED and sites
DELIBERATELY LEFT with their reasons — including `STATE.md:233` and `:1625/:1626` (dated
historical ledger rows), the HANDOFF entry [0] double-✅ skim risk (cosmetic), the protected
dated `>` blocks, and the STATE.md frontmatter (`:17`). **This table supersedes the three
divergent lists across DECISIONS / HANDOFF / STATE; say so explicitly**; (e) **the oku
task-local-harness caveat** — the true figure is **4 observed-red of 29 clauses, ms group
only**, and the harness is **task-local, never CI**; the v2 harness from Task 1 is the
defeat-resistant replacement and the closed oku SUMMARY/VERIFICATION are deliberately NOT
edited; (f) an explicit statement that the two 2026-08-11 decisions STAND unchanged.

**Write `260812-09a-check-sweep.sh`** (same shape and dialect discipline). Gates:
`json.load` on HANDOFF.json; a **containment walker** proving the changed JSON paths are
EXACTLY the intended set and every other value is structurally identical to `42c060e`;
DECISIONS.md **byte-prefix append-only** (the first N bytes, N = baseline byte length, are
byte-identical to `git show 42c060e:.planning/DECISIONS.md`, and 0 deleted lines);
STATE.md **frontmatter byte-identity** against baseline (guard the both-empty case);
`.continue-here.md` demote-convention assertions (new dated block present, old marker
retitled, zero deleted historical lines); SKILL.md no-deletion (every pre-existing gate/row
token still present) plus banner presence; ROADMAP.md claim corrected; and **per-claim
residual assertions** — for each claim id, the surviving hit set equals the
`disposition=left` rows of the sweep TSV. **expect_red coverage for every clause group** on
fixture COPIES, each OBSERVED RED — at minimum: a deleted DECISIONS.md byte (append-only
gate red), a mutated HANDOFF value outside the intended paths (containment walker red), a
mutated STATE.md frontmatter byte (byte-identity red), a deleted SKILL.md historical row
(no-deletion red), a broken JSON comma (parse gate red), and a re-inserted stale claim
(residual assertion red).

⛔ No `src/`, `tests/`, `config/`, `Snakefile`, `results/`, `.planning/amendments/` edits.
⛔ No OSF posting, no manuscript file, no posted amendment body. ⛔ No perimeter contact.
⛔ The two 2026-08-11 decisions are recorded as standing and are not reopened.
  </action>

  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis/.planning/quick/260812-09a-adversarial-review-remediation-v2-disclo && chmod +x 260812-09a-check-sweep.sh && ./260812-09a-check-sweep.sh --self-test && ./260812-09a-check-sweep.sh && cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && ! grep -q '\[\^\\n\]' .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-check-sweep.sh && python3 -c "import json; json.load(open('.planning/HANDOFF.json')); print('HANDOFF parses')" && test "$(git diff --numstat 42c060e -- .planning/DECISIONS.md | cut -f2)" = "0" && test "$(git diff --name-only 42c060e -- src tests config Snakefile results .planning/amendments | wc -l)" = "0"</automated>
  </verify>

  <done>
`--self-test` exits 0 with every named control — deleted DECISIONS byte, out-of-scope
HANDOFF mutation, mutated STATE frontmatter byte, deleted SKILL row, broken JSON, re-inserted
stale claim — printed as OBSERVED RED on its named clause; the real run exits 0; HANDOFF.json
parses and its diff is confined to the intended paths; DECISIONS.md shows 0 deleted lines and
a byte-identical prefix; STATE.md frontmatter is byte-identical to baseline; the sweep TSV
enumerates every hit of all ten claims with a disposition and reason; the DEC-2026-08-12
entry carries the review record, the corrections, the scoped SR4 grep restatement, the
canonical stale-site table rendered from the TSV, the oku harness caveat and the standing
2026-08-11 decisions; `.continue-here.md` carries a new dated block with the old marker
demoted and zero deleted history; SKILL.md carries dated banners with GATE 0/1/1.5 marked
live and no historical row deleted; and `git diff --name-only 42c060e` shows nothing under
`src/`, `tests/`, `config/`, `Snakefile`, `results/` or `.planning/amendments/`.
  </done>
</task>

</tasks>

<verification>
Phase-level checks, all $0, all NC-State, zero perimeter contact:

1. **All three harnesses green, all self-tests green.**
   `./260812-09a-check-v2-pair.sh --self-test && ./260812-09a-check-v2-pair.sh`
   `./260812-09a-check-review.sh --self-test && ./260812-09a-check-review.sh`
   `./260812-09a-check-sweep.sh  --self-test && ./260812-09a-check-sweep.sh`
   Every control printed as OBSERVED RED on its named clause before any real green is
   claimed. A control that passes is a DEFEATED control and fails the task.

2. **Containment.** `git diff --name-only 42c060e` returns exactly the twelve paths in
   `files_modified` and nothing else. Specifically zero hits under `src/`, `tests/`,
   `config/`, `Snakefile`, `results/`, `.planning/amendments/`, and zero changes inside the
   `260811-oku`, `260811-tf3` and `260811-pmv` directories.

3. **Append-only and parse gates.** `git diff --numstat 42c060e -- .planning/DECISIONS.md`
   shows 0 deletions; the baseline byte prefix is byte-identical.
   `python3 -c "import json; json.load(open('.planning/HANDOFF.json'))"` succeeds.
   STATE.md frontmatter is byte-identical to baseline.

4. **Zero perimeter contact, proven not promised.** No `gsutil`, `gcloud`, `bq` or `wb`
   invocation appears in any command run by this task; record the proof the way the rcw
   review does (`grep -cE '^\$ .*(gsutil|gcloud|bq |wb )' <evidence log>` = 0).

5. **Dialect hygiene.** `grep -n '\[\^\\n\]'` over all three new scripts returns nothing.

6. **Figure fidelity.** Every figure in the v2 pair re-derives from
   `e2-exposure-real-corpus.tsv` / `e2-exposure-track-a-regions.tsv` in BOTH units, and the
   derivation commands are recorded in the v2 file.

7. **No decision reopened.** `DEC-2026-08-11-e2-framing-correction` and
   `DEC-2026-08-11-sr4-disposition` are cited as standing; no text proposes revisiting
   either.
</verification>

<success_criteria>
- Every finding in `260812-09a-REVIEW-FINDINGS.md` is either FIXED as specified, or REPORTED
  as unfixable-as-specified with evidence. None is silently narrowed, deferred, or reduced to
  a "v1 / simplified / for now" form.
- The v2 disclosure pair is safe to hand to Carter: the mechanism is code-accurate, the
  remedy is real, the re-report scope covers the ancestries that actually have results, the
  bounding sentences are present, both units are labeled, and the trsx5 interaction is
  reasoned rather than asserted.
- The rcw PRE-FIRE gate review can no longer FALSE-PASS its own liveness arbiter, and the
  corrected command sits where the reader will actually meet it.
- One canonical stale-site table exists, rendered from machine-readable evidence, and the
  live fields that asserted falsehoods now point at their retractions.
- Every green in this task was preceded by an observed red.
</success_criteria>

<output>
After completion, create
`.planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-SUMMARY.md`.

It must state explicitly: which findings were fixed and which (if any) were reported rather
than fixed, and why; the HANDOFF `timestamp` call and its disclosure; the observed-red roll
call for every control by name; the exact change set versus `42c060e`; and the standing
⛔ facts — nothing posted to OSF, no manuscript touched, no posted amendment body edited, no
source drift, zero perimeter contact, nothing fired, and both 2026-08-11 decisions unmoved.
</output>
</content>
</invoke>
