---
phase: quick/260812-09a
plan: 01
subsystem: disclosure-record-and-fire-surface
tags: [e2, disclosure, osf, manuscript, track-a, id-vs-ref-ld, rcw, pre-fire-gate, handoff, decisions, state, skill, m3, docs-only, adversarial-review]
status: COMPLETE
baseline_rev: 42c060e
branch: m3-W2-aou-deltas
commits:
  - 20ffe03  # PART A - the v2 E-2 disclosure pair + its defeat-resistant harness
  - 8c247bc  # PART B - the rcw PRE-FIRE gate review corrected in place + its harness
  - 1a4a455  # PART C - the claim-level record-surface sweep + DEC-2026-08-12
requires: [DEC-2026-08-11-e2-framing-correction, DEC-2026-08-11-sr4-disposition, DEC-2026-08-07-e2-orientation-disposition]
provides: [260812-09a-SELECTED-PAIR-correction-v2, DEC-2026-08-12-adversarial-review-remediation, corrected-fire-surface, canonical-stale-site-table]
affects: [.planning/HANDOFF.json, .planning/STATE.md, .planning/ROADMAP.md, .planning/DECISIONS.md, .claude/skills/aou-ld-pipeline/SKILL.md]
key-files:
  created:
    - .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-SELECTED-PAIR-correction-v2.md
    - .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-check-v2-pair.sh
    - .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-check-review.sh
    - .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-check-sweep.sh
    - .planning/quick/260812-09a-adversarial-review-remediation-v2-disclo/260812-09a-stale-claim-sweep.tsv
  modified:
    - .planning/quick/260811-rcw-assemble-the-m3-04c-task-3-pre-fire-gate/260811-rcw-PRE-FIRE-GATE-REVIEW.md
    - .planning/HANDOFF.json
    - .planning/STATE.md
    - .planning/ROADMAP.md
    - .planning/DECISIONS.md
    - .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md
    - .claude/skills/aou-ld-pipeline/SKILL.md
metrics:
  tasks: 3
  files_changed: 14   # the 12 in files_modified + PLAN.md + REVIEW-FINDINGS.md (untracked at baseline)
  harness_clauses: 93
  harness_controls_observed_red: 41
  sweep_hits: 130
completed: 2026-08-12
---

# Quick 260812-09a: adversarial-review remediation — v2 disclosure pair, corrected fire surface, canonical stale-site table

**One-liner.** Remediated all 22 findings of the 2026-08-11/12 five-way
adversarial review: a **v2** E-2 disclosure pair whose mechanism now matches the
code, a PRE-FIRE gate review whose liveness poll no longer FALSE-PASSES its own
check, and a claim-level sweep landing **one** canonical stale-site table —
gated by three harnesses carrying **41 controls, every one OBSERVED RED first**.

---

## Findings: fixed vs reported

**21 of 22 findings FIXED AS SPECIFIED.** One is **REPORTED, not fixed** (below),
and it is reported because it is **unfixable as specified**, not narrowed.

| Part | Findings | Disposition |
|---|---|---|
| **A** | A-BLOCKER-1/2/3, A-HIGH-1, A-MEDIUM (a)–(e), A-HARNESS | **all FIXED** |
| **B** | B-BLOCKER-1, B-HIGH-1, B-MEDIUM-1..5, B-LOW-1..4 | **all FIXED** |
| **C** | C-HIGH (5 items), C-MEDIUM (9 items), C-LOW (2 items) | **all FIXED except `STATE.md:15`** |

### ⚠ THE ONE ITEM REPORTED RATHER THAN FIXED

**`STATE.md:15` — the false *"Frozen contracts byte-unchanged (… `ld_npz_to_rds.R`
…)"* claim was NOT edited.**

The plan's `<interfaces>` described `:15` as *"the `#` comment block"*, implying
it sits in the body. **Measured at HEAD, it does not.** STATE.md's frontmatter
fence runs **lines 1–24** (`---` at `:1`, `---` at `:24`); `:15` is a YAML comment
**inside** that fence, as is `:17` `last_activity`. Editing it would break the
**frontmatter byte-identity** requirement this task is explicitly bound by
(*"STATE.md frontmatter byte-identical (body edits only)"*), in a frontmatter
whose YAML is already pre-existing-unparseable — the exact surgery the plan
forbids for `:17`.

**Rather than improvise a weaker fix**, the correction is recorded as a
**body-side note** in three places: STATE.md's Session Continuity, HANDOFF.json
`carter_decisions_outstanding[2]`, and the `DEC-2026-08-12` entry's
*"ONE ITEM REPORTED RATHER THAN FIXED"* section. The claim itself is false:
`ld_npz_to_rds.R` is **+313 / −62** and **never-frozen** per
`DEC-2026-08-11-sr4-disposition`.

---

## PART A — the v2 disclosure pair (`20ffe03`)

The v1 pair said the catalog↔panel join *"matched on coordinates alone and
ignored the alleles."* **That is false of that join.**
`src/snakemake/scripts/ld_allele_join.R` is a 4-key allele-aware matcher —
`k4_pan` `:205`, duplicated 4-keys nulled out of the match table `:211-219`,
`k4_exact`/`k4_swap` `:221-222`, palindromes computed `:232-234` and excluded by
`keep_mask` `:239`, `orient` `:241`, counters `:257-270` — and **its own
`flipped` counter produced every percentage we disclosed**. Sole production call
site: `run_qtl_coloc.R:471`.

v2 states, so a reviewer holding the code finds exact agreement:

1. the numbers come **from the correct join measuring the population**;
2. the disclosed property is that the computed orientation is **measured and
   reported but deliberately NOT APPLIED** to the QTL beta at
   `run_qtl_coloc.R:478-479` — a **recorded decision**
   (`DEC-2026-08-07-e2-orientation-disposition`, option A), not an oversight;
3. the allele-blind CHR:POS description belongs to the **fine-map-side** join
   (`run_susie_rss.R:220/:274/:312/:331-332`), **fixed for AFR only** and **still
   position-only on EUR today**, because `config/pipeline.yaml`'s `ld_read_path`
   measures `{'enabled': True, 'ancestries': ['AFR'], 'allele_aware': True,
   'coloc': True}`. Never described in repo-wide past tense.

**The real remedy** replaces v1's already-shipped commitment: apply the measured
orientation to the QTL beta; a **GRCh38→GRCh37 allele reconciliation on the QTL
side** (there is no position join available there at all); an ancestry gate if
Track A must not move. **Re-report scoped to every affected ancestry that HAS
results at remediation time, explicitly including EUR** — because AFR has zero
coloc jobs (`build_qtl_coloc_manifest.py:250,:276` returns `"EUR"`
unconditionally). Condition-bounded on E-4 + a real non-identity panel, **no
schedule**.

**All figures re-derived from the TSVs, agreeing with `<interfaces>` to the
digit — no discrepancy found:**

| Unit | Affected | Median | Max |
|---|---|---|---|
| per **TILE-ROW** (EUR arm) | **195 / 206** | **17.82%** (17.8240) | **38.68%** (38.6824) |
| per **LOCUS** (collapsing `__tile`/`__sub`) | **49 / 51** | **0.4234%** | **38.6824%** |

Locus table: CXADR 28,415/18 = 0.06%; MC4R 14,141/10 = 0.07%; SH2B3 11,826/333 =
2.74% (anchor tiles 10,521/0 = 0.00%, tile 3 1,305/333 = 20.33%); APOL1
4,910/1,108 = 18.41%; FTO 7,188/2,245 = 23.80%; pooled 66,480/3,714 = 5.29%.
Palindrome bound: 144,176 dropped against 714,382 exact + 31,152 flipped =
**19.34%** of the bindable set.

The **trsx5 interaction is reasoned, not asserted**: the posted 2026-07-10 body
premises a position-based join (`:47`) and lockstep exclusion (`:57`); palindrome
dropping is a **new one-sided drop class** relative to that premise, recorded as
a **premise update**, bounded at 19.34% and **counted** in `dropped_palindromic`,
therefore auditable.

⚠ **Block-length bound raised deliberately** (ms 120–200 → 420–700 words), with
the reason recorded in the harness header. **No required clause was dropped to
fit an inherited bound.**

---

## PART B — the fire surface (`8c247bc`)

**B-BLOCKER-1 is the finding that could have cost money.** The document Carter
reads immediately before a ~11-day / **$385–1,084** fire told him to poll with
`gsutil ls gs://${WORKSPACE_BUCKET}/ld/AFR_aou/*.npz | wc -l`. `WORKSPACE_BUCKET`
**already carries `gs://`** (`SKILL.md:43`), so gsutil errors to stderr, stdout
is empty, and `wc -l` prints **0** — which is exactly what the pre-fire
*"expected: 0"* row EXPECTS (a **FALSE PASS**) and which during STEP B polling
reads a **healthy fire as dead**. Same defect class as **quick-260611-tbw**
(`fix-aou-2-workspace-bucket-double-prefix`, gap C3).

### The corrected poll command, as landed

**Primary form (literal bucket), present at all three points of use:**

```
gsutil ls gs://rw-migration-aou-rw-476cdac2/ld/AFR_aou/*.npz | wc -l
```

**Alternate (env form, correctly quoted):**

```
gsutil ls "${WORKSPACE_BUCKET}/ld/AFR_aou/"*.npz | wc -l
```

Each site carries **⚠⚠ `WORKSPACE_BUCKET` ALREADY CONTAINS `gs://` — NEVER PREFIX
IT.** The three sites are the **§4 bucket-`.npz`-count row**, the **⚠ THE
LIVENESS ARBITER** block and **§5 STEP B** — **the latter two previously carried
no command at all.**

Also: **276 is not a pass bar** in both the liveness paragraph and STEP B; the
`<panel-uri>` placeholder replaced by a URI **derived from the producer**; the
real-`.bim` row made honest; three §2 rows labelled last-known with an explicit
re-verification ruling each plus a **new §4 row 5** for the cohort MTs; a new
**§2.1(9)** for the PLAN's branch (ii)/(iii) contradiction with the code-correct
reading; and all four B-LOW anchor fixes with their echoes chased. Dated
`## Corrections (2026-08-12)` names every finding ID.

⚠ **Re-anchoring found four of the review order's own pointers wrong at HEAD**
(`:1494-1497` → `:1503-1504`; `:920-926` → `:917`/`:922`/`:939-940`; `:942` →
`:943-945`; `:715-718` → `:716-718`). **The measured values were used and every
disagreement is recorded in the changelog** rather than propagated.

⚠ **The harness found two residual stale anchors** buried in the review's own
2026-08-11 Reconciliation log (`occlusion_manifest.py:203-208` and `:713-716`)
that the finding list had not named. Both annotated in place — **demoted, not
rewritten**. The 2026-08-11 anchor, all **20 of 20** L-rows, and the evidence
log/TSV are **unchanged**.

---

## PART C — the claim-level sweep (`1a4a455`)

Swept **by CLAIM, not by file** — that is the whole point, and it worked.

### The canonical stale-site table (rendered from the TSV, never retyped)

| | fixed | left | eliminated | total |
|---|---|---|---|---|
| **10 claims over 83 scanned surfaces** | **42** | **86** | **2** | **130** |

Per claim: C1 3/38/0 · C2 8/0/0 · C3 3/0/0 · C4 3/3/0 · C5 13/11/0 · C6 4/32/0 ·
C7 3/1/0 · C8 2/0/0 · C9 3/1/0 · C10 0/0/2.

**Sites left, by class:** dated phase records (PLAN/SUMMARY/BLAST-RADIUS/UAT/
CONTEXT/RESEARCH/VALIDATION); the dated `>` blocks in STATE.md and
`.continue-here.md` **explicitly protected by `DEC-2026-08-11-sr4-disposition`**;
`STATE.md:233` and the `:1625/:1626` ledger rows (**no action needed**, listed so
their absence from the fix set is deliberate); `DECISIONS.md` itself
(append-only, and every hit is a retraction/decision entry stating the claim in
order to answer it); STATE.md's frontmatter; HANDOFF's explicitly **dated** keys;
and the HANDOFF entry-[0] double-✅ skim risk (cosmetic).

**Counts reconcile:** written 130 == read back 130 == 42 + 86 + 2. ⚠ An earlier
pass reported **118** — `csv.DictReader`'s default `QUOTE_MINIMAL` had silently
**merged rows** on the quote characters in the excerpts. The TSV is now declared
tab-separated-not-CSV in its own header, and both the generator and the harness
split on TAB. **[[feedback_a_count_is_a_claim_scope_and_reconcile]]**

### What sweeping by CLAIM caught that a file sweep would not

Five **live** sites the file-by-file scope had not named:

1. **`ROADMAP.md:213`** — a *second* `ld_npz_to_rds.R … UNCHANGED` assertion.
2. **`STATE.md:28`** — the live RESUME-HERE heading still self-labelling
   2026-08-07 as ★★ LATEST ★★.
3. **`HANDOFF.json` `headline`** — an **undated, live** key asserting *"THREE
   UNDISCHARGED OBLIGATIONS and one open question"* **and** the per-region unit
   error.
4. **`HANDOFF.json` `carter_decisions_outstanding[0]`** — **still routing a
   reader to the SUPERSEDED v1 paste-ready pair.** This is the one that could
   have caused a wrong OSF posting.
5. **`HANDOFF.json` `carter_decisions_outstanding[2]`** — its *"RESIDUAL LIVE
   SITES"* note, now discharged (one by disclosure, not by edit).

### The HANDOFF `timestamp` call, disclosed

**UPDATED**, deliberately, to `2026-08-12T00:00:00Z`, with the sweep as the
**named reason** in a new sibling key `timestamp_reason_2026_08_12`. Reasoning: a
2026-08-07 timestamp on a file whose **live** fields were corrected on 2026-08-12
is exactly the staleness class this sweep exists to remove. All 2026-08-07 bodies
are **preserved in place, demoted rather than deleted**.

### The DEC id as landed

**`DEC-2026-08-12-adversarial-review-remediation`**, appended at
`.planning/DECISIONS.md` — **232 lines added, 0 deleted**, baseline byte prefix
byte-identical to `42c060e`. It carries the review record **including what it
CLEARED**, the corrections by finding ID, the scoped SR4 grep restatement, the
oku harness caveat, the canonical table, the reported-not-fixed item, and an
explicit statement that both 2026-08-11 decisions **stand**.

### The scoped SR4 grep restatement (measured, not asserted)

| basename | @ `0e7e309` | @ HEAD |
|---|---|---|
| `occlusion_manifest.py` | 0 | 1 |
| `occlusion_present_rate_scan.py` | 0 | 1 |
| `drop_occluded_from_sumstats.py` | 0 | 1 |
| `ld_npz_to_rds.R` | 0 | 2 |
| `pipeline.schema.yaml` | 0 | 2 |

Every HEAD hit is **self-reference by the disposition entry**, which did not
exist at `0e7e309` (`git cat-file -t 0e7e309` → commit). **This does not weaken
the disposition** — it scopes its evidence so no future reader re-runs the grep,
gets a non-zero count and concludes the dossier was wrong.

---

## OBSERVED-RED ROLL CALL — 41 controls, every one red on its named clause

**`260812-09a-check-v2-pair.sh`** — 45 clauses, 3 groups, **16 controls**:

| Control | Clause | Sole |
|---|---|---|
| **NC-A — APOL1↔CXADR label swap (ms prose)** | MSV-04 | ✔ |
| NC-B — unit labels replaced by the v1 per-region equivocation | MSV-06 | |
| NC-C — the code-unchanged statement deleted | MSV-08 | ✔ |
| NC-L — mechanism (ii) "measured but not applied" removed | MSV-07 | ✔ |
| NC-M — v1's false mechanism phrasing re-introduced | MSV-15 | |
| NC-D — APOL1↔CXADR **table** label swap | OSV-04 | |
| **NC-E — pooled kept, "dragged" removed INSIDE the block** | OSV-06 | ✔ |
| **NC-E2 — "dragged" present ONLY out of block (block scoping)** | OSV-06 | ✔ |
| NC-F — v1's already-shipped "the join is made allele-aware" | OSV-09 | |
| NC-G — re-report scope reverted to AFR-only | OSV-10 | ✔ |
| NC-H — trsx5 no longer recorded as a premise update | OSV-11 | ✔ |
| NC-N — palindrome magnitude corrupted vs the TSV | OSV-11 | ✔ |
| **NC-I — UNDISCHARGED → DISCHARGED flip** | WRV-02 | ✔ |
| NC-J — a delta-table row deleted | WRV-04 | ✔ |
| NC-K — the no-agent-posts rule removed | WRV-06 | ✔ |
| NC-P — the word "revision" re-introduced | WRV-11 | ✔ |

**`260812-09a-check-review.sh`** — 28 clauses, 5 groups, **14 controls**:
**NC-1 the double-prefixed poll form reintroduced as an instruction** (POLL-05,
sole) · NC-2 command deleted from STEP B only (POLL-03) · NC-3 never-prefix
warning removed (POLL-03) · NC-4 no-276 caveat deleted from the liveness block
(BAR-01, sole) · NC-5 "climbing **to** 276" restored (BAR-03, sole) · NC-6
`<panel-uri>` restored (ITEM-03) · NC-7 obligation (3) un-discharged (ITEM-01,
sole) · NC-8 cohort-MT recheck row deleted (ITEM-05, sole) · NC-9 manifest anchor
reverted (ANCH-01) · NC-10 the "three" count removed (ANCH-03) · NC-11 L-11
rescoping removed (ANCH-04, sole) · NC-12 a finding ID dropped (LOG-02) · NC-13
changelog heading undated (LOG-01) · NC-14 zero-perimeter declaration removed
(LOG-05, sole).

**`260812-09a-check-sweep.sh`** — 20 clauses, 7 groups, **11 controls**:
NC-1 a deleted DECISIONS.md byte (DEC-01) · NC-2 a HANDOFF value mutated outside
the intended paths (JSON-02, sole) · NC-3 a mutated STATE.md frontmatter byte
(STATE-01, sole) · NC-4 a deleted SKILL.md historical row (SKILL-01, sole) ·
NC-5 a broken JSON comma (JSON-01) · NC-6 a re-inserted stale claim (CLAIM-02) ·
NC-7 the 2026-08-07 marker left un-demoted (CONT-01, sole) · NC-8 the ROADMAP
correction reverted (ROAD-01, sole) · NC-9 the canonical table heading removed
(DEC-03, sole) · NC-10 the retracted clause restored to a live field (JSON-02) ·
NC-11 the oku overclaim restored (STATE-03).

**Plus, outside the harnesses:** the **v1-untouched** gate was observed red on a
deliberate mutation of `260811-tf3-SELECTED-PAIR-correction.md` (then restored,
`git diff --exit-code` clean), and the **STATE.md frontmatter byte-identity**
gate was observed red on a mutated frontmatter byte before its real green.

### ⚠ Four guards were found BROKEN BY THEIR OWN CONTROLS and fixed

1. **`marked_context` was VACUOUS** — markers joined with spaces but split on
   `|`, so every marker collapsed into one unmatchable string. It reported false
   failures until fixed; both of its clauses now carry an observed red.
2. **The NC-5 parse control was DEFEATED** — it deleted the **space** before
   `"phase"`, not the comma, so the JSON stayed valid and the parse gate stayed
   green *looking correct*. Now it deletes the real comma **and asserts the
   mutation actually broke the parse** before trusting itself.
3. **`pyh` merged stderr into the problem string**, so a Python
   `DeprecationWarning` read as a clause failure — noise masquerading as a
   finding.
4. **An empty problem string turned every Python-backed clause red** (a bare
   newline survived `tr`). `verdict` is now immune to whitespace-only problems.

**[[feedback_green_assertion_needs_a_negative_control]]** — four of this task's
own guards were wrong, and the controls are what said so.

---

## Containment gate results

| Gate | Result |
|---|---|
| Files changed vs `42c060e` | **exactly 14**, reconciled: the **12** in `files_modified`, **plus** `260812-09a-PLAN.md` and `260812-09a-REVIEW-FINDINGS.md`, which were untracked at baseline and were committed with Task 1 as instructed. Nothing else. |
| `src/`, `tests/`, `config/`, `Snakefile`, `results/`, `.planning/amendments/` | **0 files** |
| `260811-oku`, `260811-tf3`, `260811-pmv` directories | **`git diff --exit-code` CLEAN** (gate observed red on a deliberate mutation) |
| `python3 -c "json.load(open('.planning/HANDOFF.json'))"` | **parses** (after every edit) |
| HANDOFF containment walker | **PASS** — changed paths exactly the intended set; older `resume_on_reconnect` entries byte-identical |
| `git diff --numstat 42c060e -- DECISIONS.md` | **232 added / 0 deleted**; baseline byte prefix identical |
| STATE.md frontmatter (lines 1–24) | **byte-identical**; `last_activity` untouched |
| Protected dated `>` blocks (STATE ×7) | **all present verbatim** |
| `.continue-here.md` | new dated block on top, 2026-08-07 marker **retitled**, **0** historical body lines deleted |
| SKILL.md | **0** historical rows deleted; GATE 0/1/1.5 marked LIVE, GATE 2/3 marked RETIRED |
| Dialect hygiene — forbidden bracket construct | **0 hits in all three scripts** |
| Perimeter contact | **ZERO** — no `gsutil`, `gcloud`, `bq` or `wb` invoked; grep for an invocation in this task's scripts = **0** |

---

## Deviations from plan

**1. [Rule 2 — missing critical correction] Five extra live sites corrected.**
The claim-level sweep found live surfaces the plan's file-by-file list did not
name: `ROADMAP.md:213`, `STATE.md:28`, and `HANDOFF.json` `headline`,
`carter_decisions_outstanding[0]` and `[2]`. `[0]` was still routing a reader to
the **superseded v1 pair** — a live misrouting with a real consequence. The
HANDOFF containment walker's intended-path set was widened to match, and the
widening is disclosed in the walker source, the DEC entry and here.

**2. [Scope note — reported, not silently taken] `STATE.md:1625` sits in the
Quick-Tasks ledger table.** The orchestrator's constraint fences that table; the
plan and finding **D4-10** both mandate rescoping *"the two oku 'enforced by'
surfaces"*, and the second surface is that row. **What was done:** the
enforcement claim inside the existing row was corrected. **What was NOT done:**
no row was added for this task, and **`last_activity` was not touched** (proven
byte-identical) — both remain the orchestrator's.

**3. [Scope note] `STATE.md:28` is not one of the plan's named lines.** It was
annotated because the live RESUME-HERE heading still self-labelled 2026-08-07 as
★★ LATEST ★★, which is precisely the claim class C2 exists to catch. Reported
here rather than folded in silently.

**4. [Scope decision, recorded in the DEC entry] The sweep excludes the
`DEC-2026-08-12` entry itself.** That entry is the **record of** the enumeration,
not a subject of it — it necessarily quotes every stale claim in order to
register it, so counting it would make the sweep circular and self-inflating.
Same discipline the `260811-rcw` review states for its own Reconciliation log.
The generator and the harness apply the **identical** cut; everything before that
heading **is** swept and contributes 9 of the 86 left rows.

---

## ⛔ Standing facts

- **Nothing was posted to OSF.**
- **No manuscript file was touched.**
- **No posted amendment body was edited** (`.planning/amendments/` = 0 changes).
- **No source drift** — 0 files under `src/`, `tests/`, `config/`, `Snakefile`,
  `results/`.
- **ZERO perimeter contact** — no `gsutil`, `gcloud`, `bq` or `wb`, not even
  read-only control-plane.
- **Nothing was fired.** m3-04c Task 3 remains **Carter's terminal gate**.
- **`DEC-2026-08-11-e2-framing-correction` and `DEC-2026-08-11-sr4-disposition`
  are UNMOVED** and are recorded as standing. This task fixed texts and surfaces,
  never decisions.
- **The v1 artifacts are byte-untouched** and remain the record of what was
  considered. They must **not** be placed or posted.

## What Carter still owns

1. **E-2 obligation (1)** — place `ms-correction-v2` in the Track A manuscript,
   after the **pre-placement journal-policy check**.
2. **E-2 obligation (2)** — post `osf-correction-v2` as a **new supplementary
   file** on `osf.io/az52u` **and** record its URL + timestamp in
   `.planning/osf_deviations.md`. Posting alone does not discharge it.
3. **m3-04c Task 3** — the ~11-day / $385–1,084 fire. **Read the corrected rcw
   review's `## Corrections (2026-08-12)` section before acting on any command
   in it.**

## Known Stubs

None. This task produced documents and harnesses only; every figure in every
outgoing body is re-derived from a shipped measurement TSV at harness run time,
and no component was left wired to placeholder data.

## Self-Check: PASSED

All 5 deliverables + the SUMMARY exist on disk; all three commits (20ffe03, 8c247bc, 1a4a455) resolve in `git log`. The 14-file change set was re-counted and reconciled against `files_modified` after an initial mis-count of 12.
