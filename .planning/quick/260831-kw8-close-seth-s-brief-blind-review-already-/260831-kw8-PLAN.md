---
phase: quick-260831-kw8
plan: 01
type: execute
wave: 1
depends_on: []
subsystem: m3-afr-ld-panel
tags: [pairwise-completeness, occlusion, already-occluded, anchor-relative, panel-wide-excludelist, post-hoc-reclassification, false-docstring-claim, same-position-multiallelic, split-multi-hts, brief-blind-review, m3-07, prereg-unchanged, nothing-fired]

files_modified:
  - src/python/pcs_panelwide_reclassify.py
  - tests/m3/test_pcs_panelwide_reclassify.py
  - src/python/pairwise_completeness_scan.py
  - tests/m3/test_pairwise_completeness_scan.py
  - src/python/samepos_missingness_probe.py
  - tests/m3/test_samepos_missingness_probe.py
  - .planning/debug/260831-PENDING-PASTE-POSTHOC-panelwide-reclass-and-samepos-probe.md
  - .planning/debug/260831-seth-brief-blind-review-already-occluded-is-anchor-relative.md
  - .planning/STATE.md
  - .planning/quick/260831-kw8-close-seth-s-brief-blind-review-already-/260831-kw8-PLAN.md
  - .planning/quick/260831-kw8-close-seth-s-brief-blind-review-already-/260831-kw8-SUMMARY.md

autonomous: true

requirements:
  - PCS-PANELWIDE-RECLASSIFY-POSTHOC-NO-RERUN
  - PCS-PANELWIDE-REUSES-THE-FROZEN-DETECTOR
  - PCS-PANELWIDE-TWO-COUNTS-NEVER-COLLAPSED
  - PCS-PANELWIDE-VID-KEYED-MATCHES-PRODUCTION
  - PCS-PANELWIDE-ROWSET-PROVENANCE-AND-MONOTONICITY
  - PCS-PANELWIDE-WRONG-BIM-RAISES
  - PCS-DOCSTRING-ALREADY-OCCLUDED-IS-ANCHOR-RELATIVE
  - PCS-RENAME-ADJUDICATED-AND-DECLINED-WITH-REASONS
  - PCS-SAMEPOS-PROBE-BUILT-AND-STAGED-NOT-FIRED
  - PCS-SAMEPOS-INFERENCE-LABELLED-UNVERIFIED
  - PCS-REVIEW-RECORDED-WITH-ACCEPTED-NO-ACTION-ITEMS
  - PCS-PREREG-NUMBERS-DO-NOT-MOVE
  - PCS-FROZEN-SURFACES-UNCHANGED
  - PCS-SUITE-REBASELINE-COMPONENT-EXACT
  - PCS-NOTHING-FIRED

user_setup: []

must_haves:
  truths:
    - "A POST-HOC TOOL ANSWERS THE PANEL-WIDE QUESTION WITHOUT RE-RUNNING THE SWEEP. `src/python/pcs_panelwide_reclassify.py` takes an ALREADY-EMITTED `pcs_pairs.tsv` plus the cohort `.bim` and the region manifest, and reports per undefined pair whether either member lands on the production excludelist. It reads NO `.bed`, computes NO genotype, and re-runs NOTHING: the ~4h20m sweep in flight is untouched and its output is the tool's INPUT. THIS IS MACHINE-CHECKED, NOT NARRATED — an AST gate in Task 1's `<verify>` fails on any `BedReader`/`Genotypes` import, any `BedReader` reference, any `read_variant` call, or any `.bed` string literal outside a docstring (AST, not `grep -c '\\.bed'`: a docstring that merely MENTIONS `.bed` must not false-fire), and it must be SEEN RED before it is trusted."
    - "THE OCCLUSION RULE IS NOT RE-IMPLEMENTED — THE FROZEN DETECTOR IS CALLED. The verdict comes from `occlusion_span_filter.detect_occluded_variants` over the region's FULL in-window row set, selected exactly as production selects it (`[from_bp, to_bp]` on `chrom`, NO padding — `run_native_ld_panel.py:872-878` builds `raw_rows` from `_window_bim_n_var`'s in-window subset). A named enforcer asserts function IDENTITY (`reclassify.detect_occluded_variants is occlusion_span_filter.detect_occluded_variants`, and likewise for `iter_bim_windows` / `_read_regions_tsv`), so a future second copy of the rule fails a test instead of drifting silently."
    - "THE TWO POPULATIONS ARE REPORTED SEPARATELY AND ARE NEVER COLLAPSED — AT BOTH ROW AND PAIR LEVEL. `n_pairs_member_occluded_panelwide` (the pair never reaches the matrix) and `n_pairs_neither_member_occluded_panelwide` (the genuine residual) are both emitted, and so are their ROW-level twins, because `n_undefined_not_already_occluded = 3` is a KNOWN UNDERCOUNT of the row-level 5 (`.planning/debug/260826-PCS-...md` §THE PAIR-LEVEL UNDERCOUNT). A third, strictest tier — `n_pairs_neither_occluded_and_no_globally_invariant_member` — subtracts the `--mac 1` side using the TSV's own `del_globally_invariant` / `partner_globally_invariant` columns. The tool RAISES if `member_occluded + neither != n_undefined_distinct_pairs_in` (and the row twin likewise): a count is a claim and it reconciles arithmetically or it stops."
    - "THE RECLASSIFICATION IS VID-KEYED BECAUSE PRODUCTION IS, AND THE AMBIGUITY THAT CREATES IS PRODUCTION'S — SURFACED, NOT FIXED. `detect_occluded_variants` returns col-2 ids because that is what plink `--exclude` consumes, and `--exclude` on a duplicated id drops EVERY row carrying it. A member vid appearing on >1 row of the region window is counted in `n_pairs_with_ambiguous_member_id` and NAMED in `ambiguous_member_ids` — never `fixed` by switching to indices. The tool's docstring states the contrast with `_pair_key` (index-keyed, `pairwise_completeness_scan.py:446`, for the opposite reason: two rows sharing a `.` id are DISTINCT pairs and a vid key would UNDERCOUNT them). Pair-level rollups use `pair_key`; occlusion verdicts use vid; a test pins that both are live in one fixture."
    - "A NOT-OCCLUDED VERDICT CARRIES THE ROW SET IT IS RELATIVE TO, AND THE ASYMMETRY IS PINNED. Occlusion is MONOTONE in the row set — for a variant present in both, `R ⊆ R'` implies `occluded(v, R) ⟹ occluded(v, R')` — so an OCCLUDED verdict computed on a subset is SOUND while a NOT-OCCLUDED verdict on a subset is NOT. The summary's `provenance` block records `bim_path`, `bim_sha256`, `bim_n_lines`, `regions_tsv_sha256`, `ancestry`, the region ids NAMED (not counted), per-region `n_rows_in_window`, and a literal `verdict_scope` sentence saying exactly this. `tests/m3/test_pcs_panelwide_reclassify.py::test_occlusion_is_monotone_in_the_row_set` proves the monotonicity the soundness argument rests on."
    - "HANDING THE TOOL THE WRONG `.bim` IS LOUD, NEVER A SILENT `genuine residual`. Every pair member vid in the TSV for region R must appear in R's window row set; a missing one RAISES `ValueError` naming the vid and the region. The pairs-TSV header must EQUAL `pairwise_completeness_scan.TSV_COLUMNS` or the tool RAISES — a drifted header means the artifact was not produced by this scanner and its pair semantics are unverified. Both have negative controls that were observed RED."
    - "THE FALSE DOCSTRING CLAIM IS GONE AND ITS REPLACEMENT IS ENFORCED BY BEHAVIOUR, NOT BY TEXT. `src/python/pairwise_completeness_scan.py:122` no longer says the `--exclude` side is `already visible as already_occluded`. It says `already_occluded` is ANCHOR-RELATIVE (`deletion.pos < partner.pos <= deletion.span_end` against THE ANCHOR ONLY, `:616`), names the panel-wide question as a SEPARATE quantity, and points at `pcs_panelwide_reclassify.py`. The enforcer is a RUNTIME test that CONSTRUCTS a window in which a pair carries `already_occluded == False` while `detect_occluded_variants` over the same rows DOES occlude a member — the two predicates are demonstrated to disagree, which is what falsifies the deleted sentence."
    - "THE RENAME WAS ADJUDICATED, DECLINED, AND THE DECLINE IS ITSELF ENFORCED. `already_occluded` is NOT renamed, for three MEASURED reasons: (i) a sweep is MID-FLIGHT and will emit a header carrying that exact column — a rename desynchronizes the reader from the artifact; (ii) the pre-registration names `n_undefined_already_occluded` / `n_undefined_not_already_occluded` (`.planning/debug/260826-PCS-...md` §(e)), and renaming a pre-registered key in response to a review is the move pre-registration exists to prevent; (iii) `TSV_COLUMNS` IS `PairResult._fields`, so a rename moves the emitted header, the summary keys, the banked BLOCK 2 identity pull and every consumer at once. The decline carries a TRIGGER (revisit only at the next OSF version, after the sweep lands and its artifacts are banked) and a named enforcer that fails if the field leaves `TSV_COLUMNS` while the in-flight artifact contract stands."
    - "THE SAME-POSITION PROBE IS A TESTED INSTRUMENT, NOT A PASTE BLOB — AND IT IS STAGED, NOT FIRED. `src/python/samepos_missingness_probe.py` decides EMPIRICALLY from `.bim`/`.bed` whether same-position rows are CO-CALLED (site called ⟹ both rows called) or COMPLEMENTARY (one row's ALT carriers are MISSING at its same-position sibling), using the scanner's own `BedReader` — no Hail, no plink, no network. Labels are neutral (`co_called` / `complementary` / `mixed`); they describe the MEASUREMENT, never the mechanism. Egress is aggregate + variant ids only. It is invoked ONLY from a staged doc whose first line says it has not been run."
    - "THE INFERENCE AND ITS UNVERIFIED STATUS SIT BESIDE THE MEASUREMENT, SO NEITHER CAN BE MISTAKEN FOR THE OTHER. The staged doc states: the pipeline uses `hl.split_multi_hts` (`src/python/aou_ld_panel.py:2138`), whose documented behaviour DOWNCODES other-ALT carriers to REFERENCE — which would make the same-position class EMPTY — and that this is an INFERENCE FROM DOCUMENTATION, NOT A MEASUREMENT, because Hail is not installed on this node and cannot be verified here. The inference-derived expectation is written as a labelled PREDICTION with the standing rule: a mismatch is a finding to report, never a number to adjust."
    - "THE PRE-REGISTERED NUMBERS DID NOT MOVE, AND THAT IS ENFORCED BY NOT TOUCHING THE FILE. 15 rows / 13 distinct pairs / 10 already-occluded / 3 not-already-occluded and the offset histogram `{-14: 1, -9: 1, -6: 1, -3: 1, -1: 1, 0: 10}` are UNCHANGED. `.planning/debug/260826-PCS-ancestry-blind-manifest-read-8x-duplication-and-the-prereg-prediction.md` is NOT edited by this plan (`git status --porcelain` on it stays empty), and `test_prereg_pooled_row_prediction_reconciles_with_the_afr_pass` stays green. This task ADDS a derived quantity and CORRECTS an interpretation; it does not revise a prediction, and the record says so and says why."
    - "THE DERIVED QUANTITY AVAILABLE TODAY IS BANKED WITH ITS SCOPE. MEASURED during planning against the FROZEN detector on rows synthesized from the BANKED vids: of the 5 upstream un-occluded ROWS, THREE (-14, -9, -6) belong to pairs with a member on the panel-wide excludelist (`chr1:7492693:ACAAACACACACGCAGG:A` occluded by `chr1:7492679:...` span 7492679-7492709; `chr4:80782565:TATACAT…:T` occluded by `chr4:80782556:…:G` span 80782556-80782628; `chr1:5922718:G:A` occluded by `chr1:5922716:ACGGTGG:A` span 5922716-5922722), and TWO (-3 `m2_region_00008`, -1 `m2_region_00149`) are NOT occluded relative to the banked rows and are therefore UNKNOWN pending the full-window run. Row level 5 = 3 + 2; pair level 3 = 1 + 2. Both reconcile and BOTH are stated; the positives are sound by monotonicity, the negatives are explicitly scope-limited."
    - "NOTHING WAS FIRED. Zero enclave / VM / Dataproc / OSF / `gsutil` / `gcloud` / network contact; $0. No per-sample data is created, read or moved. The in-flight sweep is neither touched nor required to re-run, and the live runbook `.planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md` is NOT edited (an operator may be mid-paste against it) — the new staging goes in a SEPARATE doc."
    - "NO CRITERION, THRESHOLD OR POLICY MOVED. `git status --porcelain` is EMPTY for `src/python/occlusion_span_filter.py`, `src/python/run_native_ld_panel.py`, `src/python/fire_verifier.py`, `src/python/aou_ld_panel.py`, `.planning/amendments/`, and `.planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md`. The posted predicate is NOT widened and the NaN-raise is NOT weakened. ⚠ THE PRICES CARRY THEIR PROVENANCE: +-5bp = 6.5x the current rule and +-50bp = 12.66% of the panel are SETH'S OWN computations (2026-08-29 reply §6) that appear NOWHERE else in `.planning/` and that we have NOT independently re-derived — re-derivation needs region 1's in-perimeter `.bim` (MEASURED: no local copy) and was not attempted. The DECLINE rests on the geometry argument, not on those two values."
    - "THE SUITE IS RE-BASELINED COMPONENT-EXACT. Baseline: 1133 passed / 33 skipped / 0 failed; scanner file 112; non-scanner collect 1054 (112 + 1054 = 1166 = 1133 + 33 — MEASURED at HEAD ff94bb3). After the work: 0 failed, skipped STAYS exactly 33 (a new test landing as a SKIP is a BLOCKER — `feedback_skip_guard_masks_not_fixes`), and the new total is reconciled per FILE, not merely in aggregate (`feedback_aggregate_agreement_hides_component_errors`)."
    - "THE SHARED GPFS TREE WAS NOT TRAMPLED. Every commit stages EXPLICIT paths (never `git add .` / `-A` — `feedback_multi_terminal_staging`); no worktree isolation; branch stays `m3-W2-aou-deltas`. The pre-existing untracked results/ and targeted_rerun_* clutter is left alone."
    - "THE RECORD DOES NOT INHERIT A CLAIM ITS OWN AUTHOR DEMOTED. The reason to keep the exclude-the-occluded asymmetry is the MECHANISM — an in-span variant is callable only on non-deletion haplotypes, so its AF is computed on a haplotype-restricted subsample and is biased BY CONSTRUCTION — which is a DERIVATION and needs no data. The MAF ratio (~0.0078 on the pairwise-complete subset vs ~0.014 in the sumstats = 0.557, ours, `.planning/amendments/m3_panel_occlusion_policy_decision.md:52`) is recorded as DIRECTIONALLY CONSISTENT, NOT EVIDENCE, citing Seth's own 2026-08-18 ruling (`260818-uoi-SETH-COURIER-d-acceptance-as-received.md:60`) that the 0.014 denominator is a different cohort and 'a red here would be ambiguous'. His new Q5 re-cites the ratio; the earlier, more conservative ruling GOVERNS, and the record says so."
    - "`_build_parser() -> argparse.ArgumentParser` IS A DECLARED CROSS-TASK CONTRACT. Tasks 1 and 3 both expose it (matching `pairwise_completeness_scan.py:1343`) because Task 4's verify feeds the STAGED argv to those exact functions; a differently-named builder would break Task 4 with `AttributeError` instead of catching a staged typo."
    - "EVERY PROSE ASSERTION IS WHITESPACE-NORMALISED BEFORE MATCHING. Debug docs and docstrings in this repo wrap at ~80-100 chars, so a phrase split across a newline would make a NEGATIVE assertion false-PASS — reporting a deleted claim as gone when it is merely wrapped, which is the dangerous direction. Every prose gate applies `' '.join(text.split())` first, beside the comma normalisation the numeric gates already use."
  artifacts:
    - path: "src/python/pcs_panelwide_reclassify.py"
      provides: "post-hoc panel-wide `--exclude` reclassification of an already-emitted pcs_pairs.tsv; coordinate-only, no .bed, no re-run"
      contains: "detect_occluded_variants"
      min_lines: 200
    - path: "tests/m3/test_pcs_panelwide_reclassify.py"
      provides: "known-answer (the region-1 tangle), monotonicity, wrong-.bim RAISE, header RAISE, id-ambiguity counter, two-counts reconciliation, and their negative controls"
      contains: "chr1:5922718:G:A"
      min_lines: 250
    - path: "src/python/samepos_missingness_probe.py"
      provides: "empirical co_called vs complementary decision for same-position .bim rows from .bim/.bed; aggregate egress only"
      contains: "complementary"
      min_lines: 180
    - path: "tests/m3/test_samepos_missingness_probe.py"
      provides: "synthetic-.bed known answers for BOTH labels, the mixed negative control, determinism, census honesty, egress-scalar check"
      contains: "co_called"
      min_lines: 200
    - path: "src/python/pairwise_completeness_scan.py"
      provides: "corrected RETAINED-SET PARITY docstring: already_occluded is ANCHOR-RELATIVE, the panel-wide question is named separately and routed to the new tool"
      contains: "ANCHOR-RELATIVE"
    - path: ".planning/debug/260831-PENDING-PASTE-POSTHOC-panelwide-reclass-and-samepos-probe.md"
      provides: "STAGED, NOT-RUN post-hoc invocations (reclassifier + same-position probe) with egress rules, the decision rule stated BEFORE the run, and the Hail inference labelled UNVERIFIED"
      contains: "STATUS: STAGED — NOT RUN"
    - path: ".planning/debug/260831-seth-brief-blind-review-already-occluded-is-anchor-relative.md"
      provides: "the review as received, what it changed, what it did NOT change (the pre-registered numbers, and why), and the accepted-without-action items with their prices"
      contains: "never a number to adjust"
  key_links:
    - from: "src/python/pcs_panelwide_reclassify.py"
      to: "src/python/occlusion_span_filter.py"
      via: "from occlusion_span_filter import detect_occluded_variants — FUNCTION IDENTITY asserted by test, never a second copy of the rule"
      pattern: "from occlusion_span_filter import"
    - from: "src/python/pcs_panelwide_reclassify.py"
      to: "src/python/pairwise_completeness_scan.py"
      via: "iter_bim_windows(pad_bp=0) + _read_regions_tsv + TSV_COLUMNS — the SAME window selection and the SAME header contract the scanner used"
      pattern: "iter_bim_windows"
    - from: "src/python/samepos_missingness_probe.py"
      to: "src/python/pairwise_completeness_scan.py"
      via: "BedReader / Genotypes.called — the scanner's own validated .bed reader, so the byte contract has one implementation"
      pattern: "BedReader"
    - from: ".planning/debug/260831-PENDING-PASTE-POSTHOC-panelwide-reclass-and-samepos-probe.md"
      to: "the two argparse parsers"
      via: "every staged argv is fed to the real `_build_parser().parse_args` at verify time — a staged typo fails HERE, not in the perimeter. `_build_parser` is therefore a DECLARED contract in Tasks 1 and 3, not an incidental helper."
      pattern: "_build_parser"
    - from: ".planning/debug/260831-seth-brief-blind-review-already-occluded-is-anchor-relative.md"
      to: ".planning/debug/260826-PCS-ancestry-blind-manifest-read-8x-duplication-and-the-prereg-prediction.md"
      via: "REFERENCED, never modified — the prereg file's git status stays empty"
      pattern: "260826-PCS"
---

<objective>
Close Seth's brief-blind review. It attacked our framing and was right twice, and
both findings are ALREADY CONFIRMED in code and against our own data — this plan
does not re-investigate them, it closes them.

FINDING A: `pairwise_completeness_scan.py:122` asserts that the `--exclude` side
is "already visible as `already_occluded`". It is not. At `:616` the field is
computed against THE ANCHOR DELETION ONLY, while the production excludelist is
built by `detect_occluded_variants` over EVERY deletion in the window. So
`already_occluded=False` means "not inside THIS anchor's span" — NOT "survives
`--exclude`" — and `n_undefined_not_already_occluded` does not count pairs that
survive filtering.

FINDING B: the geometry sentence in our own courier was backwards. The invariant
party is the DELETION, not the neighbour — already recorded at
`.planning/STATE.md:735` from the July 2x2 and re-confirmed by today's STEP 2.

Purpose: make the panel-wide question ANSWERABLE post-hoc from the artifact the
in-flight sweep is about to produce; make the field's semantics impossible to
misread again; stage — without firing — the measurement that decides whether the
same-position class is real; and record the whole thing so the pre-registration
is visibly untouched.

Output: one new post-hoc tool, one corrected docstring with a behavioural
enforcer, one new tested probe, one STAGED-NOT-RUN paste doc, one debug record.

⚠ A ~4h20m SWEEP IS IN FLIGHT ON THIS CODE. NOTHING IN THIS PLAN FIRES, requires
a re-run, edits the live runbook, or moves a pre-registered number. $0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260831-kw8-close-seth-s-brief-blind-review-already-/260831-kw8-COURIER-SENT-to-seth.md
@.planning/debug/260826-PCS-ancestry-blind-manifest-read-8x-duplication-and-the-prereg-prediction.md
@src/python/pairwise_completeness_scan.py
@src/python/occlusion_span_filter.py
@tests/m3/test_pairwise_completeness_scan.py

<interfaces>
<!-- Everything below was MEASURED during planning on 2026-08-31 at HEAD ff94bb3. -->
<!-- Do NOT re-derive it and do NOT re-investigate the two confirmed findings.    -->
<!-- Re-verify only where a task explicitly says so.                              -->

PYTHON (the ONLY interpreter for every command in this plan):
  /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python      (3.11.15, numpy 2.4.4)

STARTING STATE (MEASURED):
  HEAD                                          ff94bb3   (origin == local, tree clean)
  branch                                        m3-W2-aou-deltas   (no worktree isolation)
  md5sum pairwise_completeness_scan.py          e03078ff73502c3c877b0d2ebf93941d   (73772 B, last touched cb199b6)
  md5sum occlusion_span_filter.py               f6d35524476e751481e08ce3a0de590c   (11746 B, last touched 7734725)
  pytest tests/m3/test_pairwise_completeness_scan.py -q     112 passed in 3.25s
  pytest tests/m3 --collect-only (minus that file)          1054 collected
  full tests/m3 baseline (given)                1133 passed / 33 skipped / 0 failed, ~14 min
  RECONCILES: 112 + 1054 = 1166 = 1133 + 33

FREEZE STATUS (MEASURED — this is why one file may be edited and one may not):
  occlusion_span_filter.py       IS code-frozen (tests/m3/test_source_freeze_pins.py:86, PY_FROZEN_RELS)  -> IMPORT ONLY
  pairwise_completeness_scan.py  is NOT in PY_FROZEN_RELS                                                 -> editable
  source_freeze IGNORES docstrings/comments, so a docstring correction costs no re-pin even if it were.

THE CODE SITES (line numbers as of ff94bb3; RE-LOCATE BY SYMBOL, never by number):
  the FALSE bullet                          :116-135, the sentence at :122
  the ANCHOR-ONLY computation               :616   already_occluded=bool(deletion.pos < partner.pos <= deletion.span_end)
  _pair_key (INDEX-keyed, on purpose)       :446   "NEVER the variant ids ... an id-keyed pair key COLLAPSES two DISTINCT pairs"
  CandidatePair.already_occluded            :442
  PairResult.already_occluded               :793
  TSV_COLUMNS = PairResult._fields          :1031
  SUMMARY_KEYS (carries both n_undefined_*) :1037-1053
  summarize's occluded_keys rollup          :1169, emitted at :1208-1209
  iter_bim_windows(bim, windows, pad_bp=0)  :707  -> {region_id: [(GLOBAL_index, row6), ...]}
  _read_regions_tsv(path, ids, ancestry=)   :1294 -> [(region_id, chrom, start_bp, end_bp), ...]
  BedReader(prefix).read_variant(global_ix) :277/:383 -> Genotypes(dosage int8, .called = dosage >= 0)
  DEFAULT_WINDOW_BP = 25 / DEFAULT_ANCESTRY = "AFR"          :209 / :1246

PRODUCTION'S EXCLUDELIST ROW SET (MEASURED — the reclassifier MUST match it):
  run_native_ld_panel.py:851  pre_window_n_var, pre_window_bim = _window_bim_n_var(bim_path, chrom, from_bp, to_bp)
  run_native_ld_panel.py:872  raw_rows = [ln.split()[:6] for ln in Path(pre_window_bim).read_text().splitlines() if ln.strip()]
  run_native_ld_panel.py:878  occluded_ids, occlusion_edges = detect_occluded_variants(raw_rows)
  => EXACTLY the in-window rows for [from_bp, to_bp] on chrom. NO PADDING.
  => the reclassifier uses iter_bim_windows(..., pad_bp=0) with the ld_regions.tsv bounds. Anything else
     is a different population and would answer a different question.

THE `--exclude` WIRING THAT MAKES VID-KEYING THE PRODUCTION SEMANTICS (read-only, do not edit):
  aou_ld_panel.py:2910-2913   if exclude is not None: ... cmd += ["--exclude", str(exclude)]
  The excludelist is a file of col-2 IDS, one per line. plink `--exclude` on a duplicated id drops
  EVERY row carrying it -- which is why the reclassifier keys on VID and why any id ambiguity is
  PRODUCTION'S ambiguity, surfaced rather than "fixed".

THE FROZEN DETECTOR'S CONTRACT (read, not inferred):
  detect_occluded_variants(rows) -> (occluded_ids SORTED+UNIQUE col-2 ids, edges [OcclusionEdge(occluder_id, occluded_id)])
  rows are 6-field [chr, snp_id, cm, bp, A1=ALT, A2=REF]; REF length IS the footprint.
  RULE: d.index != v.index and d.pos < v.pos <= d.pos + len(REF_d) - 1, over the ORIGINAL window, non-iterative.
  RAISES ValueError on a malformed row. Only the DOWNSTREAM occluded V is excluded; the occluder D is KEPT.

MONOTONICITY (the property the soundness argument rests on — PROVE IT, do not assume it):
  For a variant v present in both row sets, R subset-of R' implies occluded(v,R) => occluded(v,R').
  Adding rows can only ADD covering deletions; the self-guard is index-based and recomputed per call.
  CONSEQUENCE, and it is the whole reason the planning measurement below is usable:
    an OCCLUDED verdict on a SUBSET is SOUND;  a NOT-OCCLUDED verdict on a SUBSET is NOT.

MEASURED DURING PLANNING — the frozen detector run on rows SYNTHESIZED FROM THE BANKED vids
(vid = chr:pos:REF:ALT, so row6 = [chr, vid, "0", pos, ALT, REF]). This is a SUBSET row set:
  region 1 banked rows -> occluded_ids:
      chr1:1980475:G:A            <- chr1:1980423:CCTC…CAGA:C
      chr1:5733487:C:T            <- chr1:5733474:TCCC…GTCC:T
      chr1:5922718:G:A            <- chr1:5922716:ACGGTGG:A          (span 5922716-5922722)
      chr1:7492693:ACAAACACACACGCAGG:A  <- chr1:7492679:…:A          (span 7492679-7492709)
      chr1:8375822:A:T            <- chr1:8375794:TTCC…TGCA:T
  region 120__sub03 -> chr4:80782565:TATACAT…GGC:T occluded by chr4:80782556:…:G (span 80782556-80782628)
  region 8  (3 banked rows)      -> occluded: [chr1:155856788:G:GGGG…AAA]   (the INTERIOR pair's partner)
  region 149 (2 banked rows)     -> occluded: []

  THE DERIVED SPLIT, RECONCILED BOTH WAYS:
    ROW level   5 upstream un-occluded rows = 3 whose pair has a panel-wide-occluded member (-14, -9, -6)
                                            + 2 UNKNOWN on the banked subset (-3 region 8, -1 region 149)
    PAIR level  3 not-already-occluded pairs = 1 member-occluded (46714|46715, the -6)
                                            + 2 UNKNOWN (924401|924402, 9776035|9776036)
    The three positives are SOUND by monotonicity. The two negatives are NOT — they are
    UNKNOWN until the tool runs against the FULL window .bim in-perimeter.
    ⚠ THIS DOES NOT MOVE 15 / 13 / 10 / 3 OR THE HISTOGRAM. It is a NEW DERIVED QUANTITY beside them.

THE PRE-REGISTERED TABLE THAT MUST NOT MOVE (.planning/debug/260826-PCS-…md §(e)):
  n_undefined_rows 15 · n_undefined_distinct_pairs 13 · already_occluded 10 · not_already_occluded 3
  offset histogram {-14: 1, -9: 1, -6: 1, -3: 1, -1: 1, 0: 10} (sums to 15)
  POOLED candidate rows 353089 · wc -l 353090
  ENFORCER already in the suite: test_prereg_pooled_row_prediction_reconciles_with_the_afr_pass

ACCEPTED FROM THE REVIEW — RECORD ONLY, NO CODE ACTION (do not re-litigate, do not re-price).
⚠ EVERY FIGURE BELOW CARRIES ITS SOURCE. Two of them are NOT ours and one of them our own reviewer
previously ruled unusable as evidence. A permanent record may not inherit an untraceable number.

  widening the predicate is DECLINED: +-5bp costs 6.5x the current rule; +-50bp costs 12.66% of
    the panel; and it would encode a caller artifact as geometry.
    PROVENANCE: both figures are SETH'S OWN computations from region-1 dimensions, in his 2026-08-29
    reply §6. They appear NOWHERE in `.planning/` (MEASURED: `grep -rn` for both returns only this
    plan) and we have NOT independently re-derived them. Re-derivation is not cheap here and was NOT
    attempted: it needs region 1's `.bim`, which is in-perimeter (MEASURED: no `afr_cohort*.bim` /
    `*region_00001*.bim` anywhere under the repo). Record them as SETH-COMPUTED, NOT INDEPENDENTLY
    RE-DERIVED. The DECISION does not rest on their exact values — it rests on the geometry argument.

  the retained NaN-raise is the necessary-condition residual detector and MUST NOT be weakened.

  the exclude-the-occluded asymmetry is principled — BUT SPLIT THE MECHANISM FROM THE RATIO:
    THE MECHANISM (a DERIVATION; stands on its own, no data needed): an in-span variant is callable
      only on non-deletion haplotypes, so its AF is computed on a haplotype-restricted subsample and
      is biased BY CONSTRUCTION, while the deletion is well-called. THIS is the reason to keep the
      asymmetry, and it is what Seth's new Q5 argues.
    THE RATIO (MAF ~0.0078 vs ~0.014 = 0.557) is OURS — `.planning/amendments/
      m3_panel_occlusion_policy_decision.md:52` ("the occluded SNP's panel MAF on the
      pairwise-complete subset was ~0.0078 (pairs 3/4), vs ~0.014 in the sumstats").
    ⚠ IT IS NOT CLEAN SUPPORT, BY ITS OWN AUTHOR'S EARLIER RULING. Seth, 2026-08-18
      (`.planning/quick/260818-uoi-bank-seth-d-acceptance-courier-register-/
      260818-uoi-SETH-COURIER-d-acceptance-as-received.md:60`): "the GWAS AFR cohort is not the AoU
      AFR cohort. Region 1's ratio is 0.0078 / 0.014 = 0.557 — the direction the mechanism predicts,
      but ordinary between-cohort AF differences at MAF ~0.01 are easily that large on their own. So
      a red here would be ambiguous, and an ambiguous gate is one people learn to ignore." His new Q5
      cites the same ratio as corroboration; the 2026-08-18 ruling is EARLIER and MORE CONSERVATIVE
      and therefore governs. RECORD IT AS: directionally consistent, NOT evidence — the 0.014
      denominator is a different cohort.
  FINDING B's geometry: invariant_member = 'deletion'; del_carriers_lost_frac = 1.0;
    confounding_pattern = 'perfect_deletion_confounding'; consistent with STATE.md:735 (5/6 asymmetric,
    occluder carriers ~100% missing at partner).

SAME-POSITION CLASS — WHAT IS KNOWN AND WHAT IS AN INFERENCE:
  KNOWN (posted, public, .planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md:228-244):
    same-position rows are ~7-11% of rows in every region sampled; region 1 has 8,358 duplicate-position
    rows at 2,645 duplicate sites, mean multiplicity 3.16, max 21. The surviving scoped claim is
    "zero of the SIX NaN pairs were same-position"; any window-scale reading of it is FALSE.
  KNOWN (DECISIONS.md:2155-2172, DEC-2026-08-19): split-biallelic same-position rows are the OBLIGATORY
    representation — a .bim row is biallelic by construction; the NORMALIZATION branch is WITHDRAWN.
  KNOWN (code): the posted predicate's left bound is STRICT (d.pos < v.pos), so v.pos == d.pos rows are
    invisible to it BY CONSTRUCTION — not mis-measured, unlooked-at.
  INFERENCE, NOT MEASUREMENT: the pipeline uses hl.split_multi_hts (src/python/aou_ld_panel.py:2138),
    whose DOCUMENTED behaviour downcodes other-ALT carriers to REFERENCE (=> the class would be EMPTY)
    rather than to MISSING (=> the class would be REAL).
    ⚠ HAIL IS NOT INSTALLED ON THIS NODE. This CANNOT be verified here, and the reviewer asked for a
    measurement rather than an inference. Hence the probe.

FORBIDDEN SURFACES (read freely, NEVER write):
  src/python/occlusion_span_filter.py · src/python/run_native_ld_panel.py · src/python/fire_verifier.py
  src/python/aou_ld_panel.py · .planning/amendments/** (PUBLIC on OSF)
  .planning/debug/260826-PCS-…-prereg-prediction.md   (the pre-registration — reference it, never edit it)
  .planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md
      (the LIVE runbook; an operator may be mid-paste against it while the sweep runs. New staging goes
       in a NEW doc. Its git status must stay empty.)

TEST-SUITE CONVENTIONS TO MATCH (from tests/m3/test_pairwise_completeness_scan.py):
  module under test imported INSIDE each test body (never at module top) so collection stays clean and
  RED-for-the-right-reason is an assert failure, not a collection error;
  PROJECT_ROOT = Path(__file__).resolve().parents[2]; src/python prepended to sys.path;
  synthetic .bed built from 2-bit codes (00 hom-A1 / 01 MISSING / 10 het / 11 hom-A2), low sample in low bits.

GATE DISCIPLINE (binding — this repo has been bitten ELEVEN times by text-matching gates):
  every verify gate asserts on RUNTIME BEHAVIOUR or PARSED STRUCTURE;
  bare substring tests are FORBIDDEN for short tokens and numbers;
  numerics use (?<!\d)<n>(?!\d) after comma normalisation;
  a green assertion is evidence ONLY if it has been seen RED — every new gate needs a negative control.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The POST-HOC panel-wide reclassifier — answer the question the scanner never asked, from the artifact the sweep is already producing</name>
  <files>src/python/pcs_panelwide_reclassify.py, tests/m3/test_pcs_panelwide_reclassify.py</files>
  <behavior>
    RED FIRST. Write `tests/m3/test_pcs_panelwide_reclassify.py` before the module,
    run it, and RECORD the observed failure text in the SUMMARY. Minimum 12 tests:

    - test_the_frozen_detector_is_called_not_reimplemented
        `pcs_panelwide_reclassify.detect_occluded_variants is occlusion_span_filter.detect_occluded_variants`
        AND the same identity assertion for `iter_bim_windows` and `_read_regions_tsv`
        against `pairwise_completeness_scan`. Function IDENTITY, not name equality.
    - test_occlusion_is_monotone_in_the_row_set
        Build R (2 rows: a SNP and a non-covering deletion) and R' = R + the covering
        deletion. Assert the SNP is NOT occluded in R and IS occluded in R'. This is the
        NEGATIVE CONTROL for the whole soundness argument: it shows a not-occluded verdict
        on a subset can flip, which is exactly why the summary must carry row-set provenance.
    - test_the_region1_tangle_is_reclassified_member_occluded    (KNOWN ANSWER)
        Fixture .bim synthesized from the SEVEN banked region-1 vids; fixture pairs TSV with the
        `46714|46715` undefined row (`already_occluded == False`, offset -6). Assert the pair is
        classified member-occluded, that the occluded member is `chr1:5922718:G:A`, and that the
        attributed occluder is `chr1:5922716:ACGGTGG:A`.
    - test_dropping_the_occluding_deletion_flips_the_region1_verdict   (NEGATIVE CONTROL)
        Same fixture minus the `chr1:5922716:ACGGTGG:A` row -> the SAME pair now classifies
        neither-member-occluded. Proves the green assertion above can go red.
    - test_the_two_unknown_pairs_classify_as_neither_on_the_banked_subset
        The region-8 (-3) and region-149 (-1) fixtures classify neither-occluded, AND the summary's
        `verdict_scope` sentence + `bim_n_lines` are present, so the verdict is never readable as
        panel-wide-final on a subset row set.
    - test_a_pair_member_missing_from_the_bim_raises_naming_the_vid_and_region   (NEGATIVE CONTROL)
        pytest.raises(ValueError) with the vid AND the region id in the message. The fixture file is
        NOT named after the vid (a filename must never be able to satisfy the assertion —
        `feedback_green_assertion_needs_a_negative_control`).
    - test_a_drifted_pairs_tsv_header_raises
        Header with a renamed/reordered column -> ValueError naming the mismatch. Second case:
        the exact `TSV_COLUMNS` header parses clean.
    - test_the_in_flight_artifact_column_name_is_pinned
        `"already_occluded" in pairwise_completeness_scan.TSV_COLUMNS` (tuple membership on a
        structured object, not a substring grep) with a comment naming the in-flight sweep.
    - test_pair_rollups_are_pair_key_keyed_while_occlusion_is_vid_keyed
        One fixture in which two rows share a duplicated col-2 id: assert the pair count uses
        `pair_key` (index-keyed, no collapse) AND `n_pairs_with_ambiguous_member_id == 1` with the
        id NAMED in `ambiguous_member_ids`. Assert the tool did NOT switch to indices for the
        occlusion verdict.
    - test_the_two_counts_reconcile_or_the_tool_raises
        member_occluded + neither == n_undefined_distinct_pairs_in, and the ROW twin likewise;
        plus a monkeypatched inconsistency that RAISES.
    - test_the_third_tier_subtracts_the_globally_invariant_members
        A pair whose member carries `partner_globally_invariant == True` lands OUTSIDE
        `n_pairs_neither_occluded_and_no_globally_invariant_member` while still being counted in
        `n_pairs_neither_member_occluded_panelwide`. Both are emitted; neither is collapsed.
    - test_summary_key_sets_are_exact
        Exact tuple/set equality for the `provenance`, `pooled` and per-region key sets, and
        `region_ids` is a NAMED list (a count of 21 is satisfiable by the wrong 21).
    - test_only_undefined_rows_are_reclassified
        Defined rows in the input TSV are ignored and `n_undefined_rows_in` matches the count of
        `undefined == True` rows.
  </behavior>
  <action>
    Create `src/python/pcs_panelwide_reclassify.py`. It is a POST-HOC, COORDINATE-ONLY tool:
    it reads a `pcs_pairs.tsv` and a `.bim` and NOTHING ELSE. It opens no `.bed`, decodes no
    genotype, and CANNOT require the sweep to be re-run — the sweep's output is its input.

    Module docstring must state, in this order:
      (1) WHY IT EXISTS — `already_occluded` is ANCHOR-RELATIVE
          (`pairwise_completeness_scan.py:616`, against the anchor deletion only) while the
          production excludelist is built by `detect_occluded_variants` over EVERY deletion in the
          window (`run_native_ld_panel.py:878`). `already_occluded == False` therefore means
          "not inside THIS anchor's span", NOT "survives `--exclude`", and
          `n_undefined_not_already_occluded` does not count pairs that survive filtering.
      (2) WHY VID-KEYED — `detect_occluded_variants` returns col-2 ids because that is what plink
          `--exclude` consumes, and `--exclude` on a duplicated id drops EVERY row carrying it.
          A vid key therefore MATCHES PRODUCTION SEMANTICS and any id ambiguity is PRODUCTION'S
          ambiguity: it is surfaced and counted, never "fixed" by switching to indices.
          ⚠ Do NOT conflate this with `_pair_key` (`:446`), which is deliberately INDEX-keyed for
          the opposite reason — two rows sharing a `.` id are DISTINCT pairs and a vid key would
          UNDERCOUNT them. Both conventions are correct; they answer different questions.
      (3) MONOTONICITY AND SCOPE — occlusion is monotone in the row set, so an OCCLUDED verdict on
          a subset is SOUND and a NOT-OCCLUDED verdict on a subset is NOT. Every not-occluded
          verdict is therefore RELATIVE TO the row set named in `provenance`.
      (4) POST-HOC ONLY — state in words that this tool opens NO `.bed`, decodes no genotype, and
          cannot require the sweep to be re-run. (Saying it in the docstring also EXERCISES the
          AST gate's docstring-exclusion path: the gate must tolerate a docstring that MENTIONS
          `.bed` while rejecting a `.bed` string literal in code.)
      (5) WHAT IT DOES NOT ESTABLISH — no prevalence, no policy change, no criterion moves; it
          adds a DERIVED quantity beside the pre-registered ones and revises none of them.

    Implementation:
      - `from occlusion_span_filter import detect_occluded_variants` and
        `from pairwise_completeness_scan import iter_bim_windows, _read_regions_tsv, TSV_COLUMNS,
        DEFAULT_ANCESTRY` — REUSE, never a second copy. Comment the `_read_regions_tsv` import
        "PRIVATE BY NAME, SHARED BY DESIGN: a local copy would diverge from the scanner's own
        window selection the day either moves."
      - Windows come from `_read_regions_tsv(regions_tsv, region_ids, ancestry=...)`; rows come from
        `iter_bim_windows(bim_path, windows, pad_bp=0)`. `pad_bp=0` is LOAD-BEARING and must carry a
        comment naming `run_native_ld_panel.py:851-878` as the population being matched.
      - Parse the pairs TSV BY HEADER (`csv.DictReader`), and RAISE if the header does not equal
        `TSV_COLUMNS` exactly. Never parse it positionally.
      - Per region: `occluded_ids, edges = detect_occluded_variants([row for _ix, row in rows])`;
        build `occluded = set(occluded_ids)` and `attributed = {occluded_id: occluder_id}` from edges.
      - Per UNDEFINED row: `del_occluded_panelwide`, `partner_occluded_panelwide`,
        `occluding_deletion_id` (from `attributed`, empty string when not occluded),
        `pair_reaches_matrix = not (del_occluded or partner_occluded)`.
      - LOUD GUARD: every `del_vid` / `partner_vid` for region R must be in R's row-id multiset;
        otherwise `ValueError` naming the vid AND the region ("the .bim you supplied is not the .bim
        these pairs came from").
      - AMBIGUITY: count and NAME member vids occurring on >1 row of the region window.
      - Emit `--out` (one row per undefined input row, carrying the input identity columns plus the
        new verdict columns) and `--summary` JSON with `provenance` / `pooled` / `per_region`.
      - SELF-CHECK BEFORE WRITE: member_occluded + neither == n_undefined_distinct_pairs_in, and the
        row twin; RAISE naming both sides on disagreement.
      - CLI: `--pairs-tsv`, `--bfile-prefix` (reads `{prefix}.bim` ONLY; FileNotFoundError names the
        missing `.bim`), `--regions-tsv`, `--ancestry` (default AFR), `--region-ids` (optional,
        comma-separated; an empty-after-strip value is an ERROR, matching the scanner's ruling),
        `--out`, `--summary`. Expose the parser as `_build_parser() -> argparse.ArgumentParser`
        and `main(argv: "list[str] | None" = None) -> int`, matching the existing convention at
        `pairwise_completeness_scan.py:1343`. ⚠ `_build_parser` IS A DECLARED CROSS-TASK CONTRACT:
        Task 4's verify feeds the STAGED argv to this exact function, so a differently-named builder
        breaks Task 4 with `AttributeError`.
      - `provenance` carries `pairs_tsv_path/sha256/n_lines`, `bim_path/sha256/n_lines`,
        `regions_tsv_path/sha256`, `ancestry`, `region_ids` (NAMED list), and a literal
        `verdict_scope` sentence stating the monotonicity asymmetry.

    Commit with EXPLICIT paths only.
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_pcs_panelwide_reclassify.py -q 2>&1 | tail -3</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_pcs_panelwide_reclassify.py -v -k "monotone or flips or raises or region1 or ambiguous" 2>&1 | tail -15</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -c "
import sys; sys.path.insert(0,'src/python')
import pcs_panelwide_reclassify as R, occlusion_span_filter as O, pairwise_completeness_scan as P
assert R.detect_occluded_variants is O.detect_occluded_variants, 'RULE WAS RE-IMPLEMENTED'
assert R.iter_bim_windows is P.iter_bim_windows, 'WINDOW SELECTION WAS RE-IMPLEMENTED'
assert 'already_occluded' in P.TSV_COLUMNS, 'IN-FLIGHT ARTIFACT COLUMN LOST'
print('IDENTITY OK; header column present')"</automated>
    <automated>/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -c "
import ast
# THE POST-HOC-ONLY GATE -- the single property that keeps this task from requiring a 4h20m
# re-run, so it is MACHINE-CHECKED, never narrated. AST, not grep: a docstring that merely
# MENTIONS .bed must not false-fire (this repo has been burned eleven times by exactly that).
p='src/python/pcs_panelwide_reclassify.py'
tree=ast.parse(open(p,encoding='utf-8').read())
doc_ids=set()
for nd in ast.walk(tree):
    body=getattr(nd,'body',None)
    if isinstance(nd,(ast.Module,ast.FunctionDef,ast.AsyncFunctionDef,ast.ClassDef)) and body:
        f=body[0]
        if isinstance(f,ast.Expr) and isinstance(f.value,ast.Constant) and isinstance(f.value.value,str):
            doc_ids.add(id(f.value))
imported=set()
for nd in ast.walk(tree):
    if isinstance(nd,ast.ImportFrom): imported.update(a.asname or a.name for a in nd.names)
    elif isinstance(nd,ast.Import):   imported.update((a.asname or a.name).split('.')[0] for a in nd.names)
for banned in ('BedReader','Genotypes','MISSING_DOSAGE'):
    assert banned not in imported, 'GENOTYPE SURFACE IMPORTED: '+banned
names={nd.id for nd in ast.walk(tree) if isinstance(nd,ast.Name)}
attrs={nd.attr for nd in ast.walk(tree) if isinstance(nd,ast.Attribute)}
assert 'BedReader' not in names and 'BedReader' not in attrs, 'BedReader REFERENCED'
assert 'read_variant' not in attrs, 'GENOTYPE READ CALLED'
lits=[nd.value for nd in ast.walk(tree)
      if isinstance(nd,ast.Constant) and isinstance(nd.value,str) and id(nd) not in doc_ids]
bad=[s for s in lits if '.bed' in s]
assert not bad, 'BED PATH LITERAL OUTSIDE A DOCSTRING: '+repr(bad)
assert any('.bim' in s for s in lits), 'NO .bim LITERAL -- is it reading the right file at all?'
print('POST-HOC-ONLY GATE OK: no BedReader/Genotypes import, no BedReader reference, no')
print('read_variant, no .bed literal outside docstrings, .bim literal present -> cannot re-run')"</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && git status --porcelain -- src/python/occlusion_span_filter.py src/python/run_native_ld_panel.py src/python/fire_verifier.py src/python/aou_ld_panel.py .planning/amendments/ .planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md .planning/debug/260826-PCS-ancestry-blind-manifest-read-8x-duplication-and-the-prereg-prediction.md; echo "FORBIDDEN-SURFACE STATUS ABOVE MUST BE EMPTY"</automated>
  </verify>
  <done>
    `tests/m3/test_pcs_panelwide_reclassify.py` reports >= 12 passed, 0 skipped, 0 failed.
    The observed RED output from before the module existed is quoted in the SUMMARY.
    The identity check prints `IDENTITY OK`; the forbidden-surface status block is EMPTY.
    The region-1 known answer and its flipped negative control both executed (visible in -v output).
    THE POST-HOC-ONLY GATE PRINTS `POST-HOC-ONLY GATE OK`, and it was SEEN RED: temporarily add
    `from pairwise_completeness_scan import BedReader` to the module, re-run the gate, confirm it
    FAILS with `GENOTYPE SURFACE IMPORTED: BedReader`, revert, and quote BOTH outputs in the SUMMARY.
    (The gate parses SOURCE READ AT CALL TIME, so it is immune to a stale-bytecode revert --
    `feedback_negative_control_defeated_by_bytecode_cache` -- but say that rather than assume it.)
    Nothing was fired. The no-re-run property is MACHINE-CHECKED, not self-attested.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct the false docstring claim, audit the module and tests for the panel-wide reading, and adjudicate the rename in writing</name>
  <files>src/python/pairwise_completeness_scan.py, tests/m3/test_pairwise_completeness_scan.py</files>
  <behavior>
    RED FIRST, and the primary enforcer is BEHAVIOURAL, not textual. Minimum 3 new tests in
    `tests/m3/test_pairwise_completeness_scan.py`:

    - test_already_occluded_is_anchor_relative_and_is_not_the_exclude_side   (PRIMARY)
        Construct ONE window's rows containing TWO deletions and a SNP such that the SNP is
        covered by deletion A but NOT by deletion B, then enumerate candidates anchored on B.
        Assert: the emitted pair carries `already_occluded is False`, AND
        `detect_occluded_variants` over the SAME rows returns the SNP's vid in `occluded_ids`.
        The two predicates are DEMONSTRATED to disagree at runtime — which is precisely what
        falsifies the deleted sentence. Add the mirror assertion for the anchored-on-A pair
        (`already_occluded is True`) so the test cannot pass vacuously.
    - test_the_module_docstring_no_longer_claims_already_occluded_is_the_exclude_side   (SECONDARY)
        `ast.get_docstring` on the parsed module source READ AT CALL TIME (never an import —
        `feedback_negative_control_defeated_by_bytecode_cache`). Assert the exact long phrase
        "already visible as ``already_occluded``" is ABSENT (a long phrase, not a short token),
        and that the RETAINED-SET PARITY section names both "ANCHOR-RELATIVE" and the new tool
        module name. Demonstrate this test RED against the pre-fix docstring and quote it.
    - test_the_already_occluded_rename_is_declined_while_the_sweep_artifact_contract_stands
        Structural, not textual: assert `"already_occluded" in TSV_COLUMNS`,
        `"n_undefined_already_occluded" in SUMMARY_KEYS`,
        `"n_undefined_not_already_occluded" in SUMMARY_KEYS`, and that `TSV_COLUMNS ==
        PairResult._fields`. Docstring names the DECLINE and its trigger.
  </behavior>
  <action>
    1. Rewrite the RETAINED-SET PARITY bullet at `:116-135`. The false sentence
       ("the ``--exclude`` side is already visible as ``already_occluded``") is DELETED and replaced
       with a statement that says, explicitly:
         - `already_occluded` is ANCHOR-RELATIVE: it is `deletion.pos < partner.pos <=
           deletion.span_end` against THE ANCHOR DELETION ONLY (`:616`);
         - the PANEL-WIDE question — does either member land on the production excludelist,
           which `detect_occluded_variants` builds over EVERY deletion in the window
           (`run_native_ld_panel.py:878`) — is a SEPARATE quantity this scanner does not compute;
         - `already_occluded == False` therefore does NOT mean "survives `--exclude`", and
           `n_undefined_not_already_occluded` does NOT count pairs that survive filtering;
         - the panel-wide quantity is computed POST-HOC by `src/python/pcs_panelwide_reclassify.py`
           from an emitted `pcs_pairs.tsv` + the cohort `.bim`, with no re-run;
         - the `--mac 1` side is unchanged (`del_globally_invariant` /
           `partner_globally_invariant`), and the RETAINED-SET gap now has TWO named halves
           instead of one true half and one false one.
       Keep the correction scoped to prose. Do NOT change any computation, threshold, criterion,
       column, key or default in this task.
    2. AUDIT. Search the module AND `tests/m3/test_pairwise_completeness_scan.py` for every other
       place that asserts or IMPLIES the panel-wide reading — start from `summarize`'s docstring at
       `:1149` ("already covered"), `CandidatePair.already_occluded` `:442`,
       `PairResult.already_occluded` `:793`, and `span_offset`'s `:477-480` note. Correct any that
       overstate; leave correct ones alone. LIST every site inspected in the SUMMARY, including the
       ones judged already-correct — an audit that reports only its hits is not an audit.
    3. ADJUDICATE THE RENAME, IN THE DOCSTRING, AND DECLINE IT. A name is a claim and this one has
       already misled a reader, so the decline must be argued, not assumed. Record all three
       measured reasons: (i) a sweep is MID-FLIGHT and will emit a header carrying this exact
       column — renaming desynchronizes every reader (including Task 1's strict header check) from
       the artifact; (ii) the pre-registration names `n_undefined_already_occluded` /
       `n_undefined_not_already_occluded`, and renaming a pre-registered key in response to a
       review is the exact move pre-registration exists to prevent; (iii) `TSV_COLUMNS IS
       PairResult._fields`, so the rename would move the emitted header, `SUMMARY_KEYS`, the banked
       BLOCK 2 identity pull and every consumer in one step. State WHAT WOULD BREAK, and state the
       TRIGGER for revisiting: the next OSF version, after the sweep lands and its artifacts are
       banked — not before. Name the enforcer test.
    4. Do NOT touch `src/python/occlusion_span_filter.py` (FROZEN). `source_freeze` ignores
       docstrings, so nothing here needs a re-pin — verify that claim rather than assert it, by
       running the freeze-pin tests.

    Commit with EXPLICIT paths only.
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_pairwise_completeness_scan.py -q 2>&1 | tail -3</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_pairwise_completeness_scan.py -v -k "anchor_relative or docstring_no_longer or rename_is_declined" 2>&1 | tail -12</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_source_freeze_pins.py tests/m3/test_occlusion_span_filter.py -q 2>&1 | tail -3</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -c "
import ast,sys,re
src=open('src/python/pairwise_completeness_scan.py',encoding='utf-8').read()
doc=ast.get_docstring(ast.parse(src)) or ''
# WHITESPACE-NORMALISE BEFORE ANY PROSE ASSERTION. Docstrings wrap at ~80 chars, so a phrase
# split across a newline would make the NEGATIVE assertion below false-PASS -- i.e. report the
# false claim as deleted when it is merely wrapped. That is the dangerous direction.
flat=' '.join(doc.split())
assert 'already visible as' not in flat, 'FALSE CLAIM STILL PRESENT'
assert 'ANCHOR-RELATIVE' in flat, 'REPLACEMENT MISSING'
assert 'pcs_panelwide_reclassify' in flat, 'POST-HOC TOOL NOT NAMED'
import importlib.util as u
spec=u.spec_from_file_location('pcs','src/python/pairwise_completeness_scan.py'); m=u.module_from_spec(spec)
sys.path.insert(0,'src/python'); spec.loader.exec_module(m)
assert 'already_occluded' in m.TSV_COLUMNS and m.TSV_COLUMNS==m.PairResult._fields
print('DOCSTRING CORRECTED; COLUMN CONTRACT INTACT')"</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && git status --porcelain -- src/python/occlusion_span_filter.py src/python/run_native_ld_panel.py src/python/fire_verifier.py src/python/aou_ld_panel.py .planning/amendments/ .planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md .planning/debug/260826-PCS-ancestry-blind-manifest-read-8x-duplication-and-the-prereg-prediction.md; echo "FORBIDDEN-SURFACE STATUS ABOVE MUST BE EMPTY"</automated>
  </verify>
  <done>
    The scanner test file passes with >= 115 tests (112 baseline + >= 3 new), 0 failed, 0 skipped.
    The behavioural disagreement test executed and is quoted in the SUMMARY with both halves
    (anchored-on-B `False` while the detector occludes; anchored-on-A `True`).
    The docstring test was observed RED against the pre-fix docstring, and that output is quoted.
    Freeze-pin and frozen-detector suites are green. The audit list names every site inspected,
    hits and non-hits alike. The rename decline, its three reasons, what would break, its trigger
    and its enforcer are all in the docstring. Forbidden-surface status is EMPTY.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the same-position missingness probe as a tested instrument — the measurement the reviewer asked for, in place of our inference</name>
  <files>src/python/samepos_missingness_probe.py, tests/m3/test_samepos_missingness_probe.py</files>
  <behavior>
    RED FIRST. Minimum 8 tests in `tests/m3/test_samepos_missingness_probe.py`, all on SYNTHETIC
    `.bed`/`.bim`/`.fam` trios built in `tmp_path` from the 2-bit codes (no perimeter, no Hail,
    no plink). Follow the scanner test file's import-inside-the-body convention.

    - test_co_called_signature_when_both_rows_are_called_in_every_sample
        Two same-position rows, every sample called at both -> per-pair
        `frac_carriers_a_missing_at_b == 0.0`, `n_both == n_called_a == n_called_b`,
        label `co_called`.
    - test_complementary_signature_when_each_rows_alt_carriers_are_missing_at_the_sibling
        Two same-position rows with complementary missingness -> `frac_carriers_a_missing_at_b == 1.0`,
        `n_both < n_called_a`, label `complementary`, and the pair is reported UNDEFINED on the
        both-called subset.
    - test_the_mixed_case_is_labelled_mixed_and_not_forced_into_either_bucket   (NEGATIVE CONTROL)
        An intermediate fraction -> label `mixed`. Proves the two labels above can disagree and are
        not returned unconditionally.
    - test_labels_describe_the_measurement_not_the_mechanism
        The emitted label vocabulary is EXACTLY {`co_called`, `complementary`, `mixed`} — assert set
        equality against the module's exported constant. No label may name split_multi, Hail,
        reference, downcode, or a conclusion.
    - test_single_row_sites_are_not_measured_and_are_not_counted_as_same_position
        A site with multiplicity 1 contributes nothing.
    - test_the_sample_is_deterministic_and_the_census_is_reported_beside_it
        Two runs on the same input produce byte-identical output; with a cap applied,
        `n_sites_total` and `n_sites_measured` are BOTH emitted and differ, and
        `n_groups_skipped_over_max_multiplicity` is emitted. A sample must never be readable as a census.
    - test_the_summary_carries_no_per_sample_data
        Walk the summary JSON: every leaf is an int/float/str/bool, or a list of str, or a dict of
        those. Assert no leaf has length >= n_samples. EGRESS is aggregate + variant ids only.
    - test_the_bed_reader_is_the_scanners_own
        `samepos_missingness_probe.BedReader is pairwise_completeness_scan.BedReader` — the byte
        contract has ONE implementation.
  </behavior>
  <action>
    Create `src/python/samepos_missingness_probe.py`. It answers ONE question EMPIRICALLY:
    at a same-position `.bim` site, are the sibling rows CO-CALLED (the site is called => both rows
    are called) or COMPLEMENTARY (a row's ALT carriers are MISSING at its sibling)?

    Docstring must state, in this order:
      (1) WHY — the posted predicate's left bound is STRICT (`d.pos < v.pos`), so `v.pos == d.pos`
          rows are invisible to it BY CONSTRUCTION, and same-position rows are ~7-11% of rows per
          region (region 1: 8,358 rows at 2,645 sites, mean multiplicity 3.16, max 21 — the POSTED
          scope statement, `.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md:228-244`).
      (2) THE DECISION RULE, STATED BEFORE ANY RUN —
            `co_called`     => the same-position class CANNOT produce an undefined pair by
                               co-location alone => the class is EMPTY;
            `complementary` => `called(a) ∩ called(b)` strips a's carriers => the class is REAL and
                               the strict left bound is a genuine blind spot.
      (3) THE INFERENCE AND ITS STATUS — the pipeline uses `hl.split_multi_hts`
          (`src/python/aou_ld_panel.py:2138`), whose DOCUMENTED behaviour downcodes other-ALT
          carriers to REFERENCE (which would yield `co_called` and an EMPTY class) rather than to
          MISSING. ⚠ THIS IS AN INFERENCE FROM DOCUMENTATION, NOT A MEASUREMENT: Hail is not
          installed on this node and it cannot be verified here. That is why this probe exists.
          A mismatch between the inference and the measurement is a FINDING TO REPORT, never a
          number to adjust.
      (4) WHAT IT IS NOT — changes no criterion, no threshold, no policy; not on the fire path;
          states no prevalence; a capped run is a SAMPLE and reports its census beside itself.
      (5) EGRESS — aggregate counts, fractions, histogram bins and variant ids only. No per-sample
          vector, no sample identifier, no dosage. The per-pair TSV stays in-perimeter.

    Implementation:
      - Reuse `from pairwise_completeness_scan import BedReader, iter_bim_windows,
        _read_regions_tsv, DEFAULT_ANCESTRY` — one `.bed` byte contract, one window selection.
      - Windows via `_read_regions_tsv(..., ancestry=...)`; rows via `iter_bim_windows(..., pad_bp=0)`.
      - Group in-window rows by `(chrom, pos)`; keep groups with >= 2 rows.
      - Skip groups above `--max-multiplicity` (default 8) and COUNT the skips — a silent skip is a
        masked measurement (`feedback_skip_guard_masks_not_fixes`).
      - Deterministic PREFIX sample of at most `--max-sites-per-region` (default 200) groups in file
        order. No RNG. Report `n_sites_total` and `n_sites_measured` side by side, always.
      - Per ordered within-group pair (a, b): `called_a`, `called_b`, `both`; `carriers_a =
        dosage_a > 0`; emit `n_samples`, `n_called_a`, `n_called_b`, `n_both`, `n_carriers_a`,
        `n_carriers_a_called_at_b`, `frac_carriers_a_missing_at_b`, `a_invariant_on_both`,
        `b_invariant_on_both`, `undefined`, and `label` in {co_called, complementary, mixed} with
        the two thresholds as named module constants (documented as REPORTING BINS, never gates).
      - Emit `--out` per-pair TSV and `--summary` JSON: a per-region record plus a POOLED block with
        a HISTOGRAM of `frac_carriers_a_missing_at_b` (do not average a possibly-bimodal quantity
        away — report the histogram AND both tail counts), the label counts, `n_undefined_pairs`,
        and the vids of any UNDEFINED same-position pair.
      - CLI mirrors the scanner's: `--bfile-prefix`, `--regions-tsv`, `--ancestry` (default AFR),
        `--region-ids`, `--window`-free (this probe is same-position only), `--max-multiplicity`,
        `--max-sites-per-region`, `--out`, `--summary`. Expose the parser as
        `_build_parser() -> argparse.ArgumentParser` and
        `main(argv: "list[str] | None" = None) -> int`, matching the existing convention at
        `pairwise_completeness_scan.py:1343`. ⚠ `_build_parser` IS A DECLARED CROSS-TASK CONTRACT:
        Task 4's verify feeds the STAGED argv to this exact function, so a differently-named builder
        breaks Task 4 with `AttributeError`.

    THIS TASK RUNS THE PROBE ON SYNTHETIC FIXTURES ONLY. It is NOT executed against the cohort,
    the perimeter, or any real `.bed`. Staging is Task 4's job.

    Commit with EXPLICIT paths only.
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_samepos_missingness_probe.py -q 2>&1 | tail -3</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_samepos_missingness_probe.py -v -k "co_called or complementary or mixed or no_per_sample or deterministic" 2>&1 | tail -15</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -c "
import sys; sys.path.insert(0,'src/python')
import samepos_missingness_probe as S, pairwise_completeness_scan as P, ast
assert S.BedReader is P.BedReader, 'SECOND .bed READER'
doc=ast.get_docstring(ast.parse(open('src/python/samepos_missingness_probe.py',encoding='utf-8').read())) or ''
flat=' '.join(doc.split())   # NORMALISE FIRST: an ~80-char wrap inside 'not installed' would false-negative
for needle in ('INFERENCE','not installed','split_multi_hts','never a number to adjust'):
    assert needle in flat, 'MISSING: '+needle
banned=('downcode','reference_downcode','hail_says')
labels=set(S.LABELS)
assert labels=={'co_called','complementary','mixed'}, labels
assert not (labels & set(banned))
print('PROBE CONTRACT OK; labels', sorted(labels))"</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && git status --porcelain -- src/python/occlusion_span_filter.py src/python/run_native_ld_panel.py src/python/fire_verifier.py src/python/aou_ld_panel.py .planning/amendments/ .planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md .planning/debug/260826-PCS-ancestry-blind-manifest-read-8x-duplication-and-the-prereg-prediction.md; echo "FORBIDDEN-SURFACE STATUS ABOVE MUST BE EMPTY"</automated>
  </verify>
  <done>
    `tests/m3/test_samepos_missingness_probe.py` reports >= 8 passed, 0 skipped, 0 failed.
    The RED output from before the module existed is quoted in the SUMMARY.
    Both label known-answers AND the `mixed` negative control executed (visible in -v output).
    The probe contract check prints the exact three-label vocabulary.
    The probe was NOT run against any real data. Forbidden-surface status is EMPTY.
  </done>
</task>

<task type="auto">
  <name>Task 4: Stage the two post-hoc runs (NOT fired), record the review and what it did and did NOT change, and re-baseline the suite component-exact</name>
  <files>.planning/debug/260831-PENDING-PASTE-POSTHOC-panelwide-reclass-and-samepos-probe.md, .planning/debug/260831-seth-brief-blind-review-already-occluded-is-anchor-relative.md, .planning/STATE.md</files>
  <action>
    A. THE STAGED DOC — `.planning/debug/260831-PENDING-PASTE-POSTHOC-panelwide-reclass-and-samepos-probe.md`.
       ⚠ A NEW file. Do NOT edit `.planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md`:
       a sweep is mid-flight and an operator may be pasting against it right now; a doc that changes
       under a running operator is its own defect class.

       Line 1 after the title must be exactly:  `STATUS: STAGED — NOT RUN`
       followed by: this fires nothing; run ONLY after the in-flight sweep LANDS and its artifacts
       are banked; neither block re-runs the sweep — both READ its output.

       BLOCK A — the panel-wide reclassification. Exact argv for
       `src/python/pcs_panelwide_reclassify.py` against
       `/home/jupyter/occ_measure/pcs_pairs.tsv` + `/home/jupyter/afr_cohort` +
       `config/ld_regions.tsv`, `--ancestry AFR`, writing
       `/home/jupyter/occ_measure/pcs_panelwide_reclass.{tsv,json}`. State the PRE-FLIGHT (refuse if
       either output path is occupied; ROTATE, never delete — the standing project ruling) and the
       EGRESS (the summary JSON's aggregate counts + the named variant ids only; the per-pair TSV
       stays in-perimeter). State the COST NOTE: the `.bim` sha256 is a full streamed pass over
       ~20.7M lines and takes seconds-to-minutes; that is provenance, not overhead.
       State WHAT TO LOOK FOR — and that it is NOT a prediction: the two counts, at BOTH row and pair
       level, plus the ambiguity counter and the third `--mac 1`-subtracted tier.
       ⚠ Carry the PLANNING MEASUREMENT with its scope: on rows synthesized from the BANKED vids,
       three of the five upstream un-occluded rows (-14, -9, -6) belong to pairs with a
       panel-wide-occluded member and two (-3, -1) are UNKNOWN; the positives are SOUND by
       monotonicity, the negatives are NOT, and the full-window run is what settles them.

       BLOCK B — the same-position probe. Exact argv for `src/python/samepos_missingness_probe.py`.
       State the DECISION RULE BEFORE the run (`co_called` => class EMPTY; `complementary` =>
       class REAL). State the INFERENCE AND ITS UNVERIFIED STATUS immediately beside it:
       `hl.split_multi_hts` (`src/python/aou_ld_panel.py:2138`) is DOCUMENTED to downcode other-ALT
       carriers to REFERENCE, which predicts `co_called` and an EMPTY class — an INFERENCE FROM
       DOCUMENTATION, NOT A MEASUREMENT, unverifiable on this node because Hail is not installed,
       and the reviewer asked for a measurement rather than an inference. Label the expectation a
       PREDICTION and carry the standing rule verbatim: a mismatch is a finding to report, never a
       number to adjust. State the EGRESS (aggregate + variant ids only; no genotypes).

    B. THE RECORD — `.planning/debug/260831-seth-brief-blind-review-already-occluded-is-anchor-relative.md`.
       Sections, in this order:
       (a) THE REVIEW AS RECEIVED — what was asked (link the courier), what was withheld and why the
           blind framing mattered; Seth was right twice.
       (b) FINDING A, CONFIRMED IN CODE — `:122` vs `:616` vs `run_native_ld_panel.py:878`; the
           consequence for `n_undefined_not_already_occluded`; the region-1 `46714|46715` case with
           `chr1:5922718:G:A` covered by `del@5922716` (REF `ACGGTGG`, span 5922716-5922722).
       (c) FINDING B, CONFIRMED AGAINST OUR OWN DATA — the invariant party is the DELETION; our own
           courier sentence was backwards; `invariant_member: 'deletion'`,
           `del_carriers_lost_frac: 1.0`, `confounding_pattern: 'perfect_deletion_confounding'`;
           consistent with `.planning/STATE.md:735` (July 2x2, 5/6 asymmetric).
       (d) WHAT CHANGED — the new post-hoc tool, the corrected docstring and its behavioural
           enforcer, the rename DECLINED with its three reasons and its trigger, the staged probe.
       (e) WHAT DID NOT CHANGE, AND WHY — ⚠ the pre-registered 15 / 13 / 10-3 and the offset
           histogram `{-14: 1, -9: 1, -6: 1, -3: 1, -1: 1, 0: 10}` DO NOT MOVE. This task ADDS a
           derived quantity and CORRECTS an interpretation; it does not revise a prediction.
           Say WHY in one sentence that cannot be misread: adjusting a pre-registered number in
           response to a review is the exact move the pre-registration exists to prevent, and the
           enforcement is that `.planning/debug/260826-PCS-…-prereg-prediction.md` is not edited at
           all. Also unchanged: the posted predicate, every threshold, the NaN policy.
       (f) ACCEPTED FROM THE REVIEW, NO ACTION BEYOND THIS RECORD — a table with FOUR columns for
           each item: ITEM · DISPOSITION · REASON · **SOURCE (and whether WE re-derived it)**.
           ⚠ A permanent record may not inherit an untraceable number, and may not inherit an
           empirical claim its own author previously ruled ambiguous. Three rows:

           1. WIDENING THE PREDICATE — DECLINED. Reason: +-5bp costs 6.5x the current rule, +-50bp
              costs 12.66% of the panel, and it would encode a caller artifact as geometry.
              SOURCE: **SETH-COMPUTED**, his 2026-08-29 reply §6, from region-1 dimensions.
              **NOT INDEPENDENTLY RE-DERIVED BY US**, and re-derivation was NOT attempted: it needs
              region 1's `.bim`, which is in-perimeter (MEASURED: no `afr_cohort*.bim` or
              `*region_00001*.bim` anywhere under the repo). Neither figure appears anywhere else in
              `.planning/` (MEASURED by `grep -rn`). Say so in the row. State plainly that the
              DECISION does not rest on their exact values — it rests on the geometry argument, so
              a later correction to either number would not reopen the decline.

           2. THE RETAINED NaN-RAISE — NOT WEAKENED. Reason: it is the necessary-condition residual
              detector. SOURCE: ours, already posted.

           3. THE EXCLUDE-THE-OCCLUDED ASYMMETRY — KEPT, **but split the mechanism from the ratio**:
              • THE MECHANISM IS THE REASON, and it is a DERIVATION needing no data: an in-span
                variant is callable only on non-deletion haplotypes, so its AF is computed on a
                haplotype-restricted subsample and is biased BY CONSTRUCTION, while the deletion is
                well-called. This is what Seth's new Q5 argues and it stands on its own.
              • THE RATIO (panel MAF ~0.0078 on the pairwise-complete subset vs ~0.014 in the
                sumstats = 0.557) is OURS —
                `.planning/amendments/m3_panel_occlusion_policy_decision.md:52`. Record it as
                **DIRECTIONALLY CONSISTENT, NOT EVIDENCE**, and cite WHY: Seth himself ruled it
                unusable on 2026-08-18 —
                `.planning/quick/260818-uoi-bank-seth-d-acceptance-courier-register-/260818-uoi-SETH-COURIER-d-acceptance-as-received.md:60`
                — "the GWAS AFR cohort is not the AoU AFR cohort … ordinary between-cohort AF
                differences at MAF ~0.01 are easily that large on their own. So a red here would be
                ambiguous, and an ambiguous gate is one people learn to ignore."
              • His new Q5 cites the same ratio as corroboration. The 2026-08-18 ruling is EARLIER
                and MORE CONSERVATIVE and therefore GOVERNS. Say that explicitly, so the record
                cannot be read as quietly promoting a claim its author had already demoted.
       (g) THE DERIVED QUANTITY AND ITS SCOPE — row level 5 = 3 + 2, pair level 3 = 1 + 2, the
           monotonicity argument spelled out, and the explicit statement that the two negatives are
           UNKNOWN until the full-window run.
       (h) WHAT THIS RECORD DOES NOT ESTABLISH — no prevalence, no boundary width, no
           partial-confounding tail size, and no answer for the same-position class (staged, not run).

    C. STATE.md — update the ★RESUME★ lead in place: the review is closed; the tool, the docstring
       correction and the probe exist; the probe and the reclassification are STAGED, NOT RUN; the
       sweep is still in flight and was not touched; the pre-registered numbers did not move. Keep
       it current in the same commit (`feedback_state_md_keep_current`).

    D. RE-BASELINE COMPONENT-EXACT. Run the full `tests/m3` suite (allow >= 1,200,000 ms), then
       `git checkout -- tests/m3/sparse_parent_benchmark.tsv`. Reconcile per FILE, not in aggregate:
       report the new counts for `test_pairwise_completeness_scan.py`,
       `test_pcs_panelwide_reclassify.py` and `test_samepos_missingness_probe.py`, and show that
       `new_total == 1166 + Δscanner + Δreclassify + Δprobe`, that FAILED is 0, and that SKIPPED is
       still EXACTLY 33. A new test landing as a SKIP is a BLOCKER.

    Commit with EXPLICIT paths only. Branch stays `m3-W2-aou-deltas`; no worktree isolation.
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -c "
import re,sys,shlex
sys.path.insert(0,'src/python')
import pcs_panelwide_reclassify as R, samepos_missingness_probe as S
p='.planning/debug/260831-PENDING-PASTE-POSTHOC-panelwide-reclass-and-samepos-probe.md'
t=open(p,encoding='utf-8').read()
assert any(l.strip()=='STATUS: STAGED — NOT RUN' for l in t.splitlines()), 'NOT-RUN MARKER MISSING'
# every staged argv must parse against the REAL parsers -- a typo fails HERE, not in the perimeter
def argv_for(script):
    m=re.findall(r'python3\s+src/python/'+re.escape(script)+r'((?:\s+\\\n|[^\n])*(?:\n\s+[^\n]*)*)', t)
    assert m, 'no staged invocation for '+script
    raw=m[0].replace('\\\n',' ')
    return shlex.split(raw)
R._build_parser().parse_args(argv_for('pcs_panelwide_reclassify.py'))
S._build_parser().parse_args(argv_for('samepos_missingness_probe.py'))
try:
    R._build_parser().parse_args(['--pairs-tsv'])   # NEGATIVE CONTROL: a broken argv must be rejected
except SystemExit: pass
else: raise AssertionError('parser accepted a broken argv')
flat=' '.join(t.split())   # NORMALISE BEFORE PROSE ASSERTIONS -- every debug doc here wraps at ~80-100 chars
for needle in ('INFERENCE','not installed','split_multi_hts','never a number to adjust','co_called','complementary'):
    assert needle in flat, 'STAGED DOC MISSING: '+needle
print('STAGED ARGVS PARSE; NOT-RUN MARKER PRESENT; INFERENCE LABELLED')"</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -c "
import re
p='.planning/debug/260831-seth-brief-blind-review-already-occluded-is-anchor-relative.md'
t=open(p,encoding='utf-8').read()
flat=' '.join(t.split())        # WHITESPACE normalisation, for the PROSE assertions
nflat=flat.replace(',','')      # COMMA normalisation, for the NUMERIC assertions
def num(x):
    assert re.search(r'(?<!\d)'+re.escape(x)+r'(?!\d)', nflat), 'MISSING NUMBER: '+x
for x in ('15','13','10','3','6.5','12.66','0.0078','0.014','5922716','5922718','5922722'):
    num(x)
for phrase in ('never a number to adjust','DO NOT MOVE','anchor','ANCHOR','deletion'):
    assert phrase in flat, 'MISSING: '+phrase
assert '{-14: 1, -9: 1, -6: 1, -3: 1, -1: 1, 0: 10}' in flat, 'HISTOGRAM NOT RESTATED'
# PROVENANCE GATE -- no untraceable number may enter a permanent record, and no empirical claim
# may be inherited that its own author previously ruled ambiguous.
for cite in ('260818-uoi','m3_panel_occlusion_policy_decision.md','2026-08-29'):
    assert cite in flat, 'MISSING CITATION: '+cite
for label in ('SETH-COMPUTED','not independently re-derived','directionally consistent','not evidence'):
    assert label.lower() in flat.lower(), 'MISSING PROVENANCE LABEL: '+label
print('RECORD OK: prereg numbers restated unchanged; prices labelled SETH-COMPUTED;')
print('the 0.0078/0.014 ratio recorded as directionally consistent, NOT evidence, with its citation')"</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_pairwise_completeness_scan.py -q -k "prereg" -v 2>&1 | tail -8</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3 -q 2>&1 | tail -3; git checkout -- tests/m3/sparse_parent_benchmark.tsv; git status --porcelain tests/; echo "--- per-file reconciliation ---"; for f in test_pairwise_completeness_scan test_pcs_panelwide_reclassify test_samepos_missingness_probe; do printf '%s ' "$f"; /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/$f.py -q --collect-only 2>&1 | tail -1; done</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && git status --porcelain -- src/python/occlusion_span_filter.py src/python/run_native_ld_panel.py src/python/fire_verifier.py src/python/aou_ld_panel.py .planning/amendments/ .planning/debug/260825-PENDING-PASTE-pairwise-completeness-sweep.md .planning/debug/260826-PCS-ancestry-blind-manifest-read-8x-duplication-and-the-prereg-prediction.md; echo "FORBIDDEN-SURFACE STATUS ABOVE MUST BE EMPTY"; git log --oneline ff94bb3..HEAD; git branch --show-current</automated>
  </verify>
  <done>
    The staged doc's `STATUS: STAGED — NOT RUN` marker is present, BOTH staged argvs parse against
    the REAL argparse parsers, and the broken-argv negative control was rejected.
    The record restates 15 / 13 / 10 / 3 and the exact offset histogram UNCHANGED, carries the
    accepted-without-action prices (6.5x, 12.66%, MAF 0.0078 vs 0.014), and says in one unmissable
    sentence why a pre-registered number is not adjusted in response to a review.
    `.planning/debug/260826-PCS-…-prereg-prediction.md` and the live runbook are BOTH untouched
    (git status empty), and the existing prereg enforcer test is green.
    Full `tests/m3`: 0 FAILED, SKIPPED exactly 33, and the new total reconciles per FILE as
    `1166 + Δscanner + Δreclassify + Δprobe` with each Δ reported.
    `tests/m3/sparse_parent_benchmark.tsv` is restored and `git status --porcelain tests/` is clean.
    STATE.md ★RESUME★ is current in the same commit. Branch is still `m3-W2-aou-deltas`.
    Nothing was fired: $0, no enclave / VM / OSF / gsutil / gcloud / network contact, and the
    in-flight sweep was neither touched nor required to re-run.
  </done>
</task>

</tasks>

<verification>
Phase-level checks, run after Task 4:

1. **Nothing fired, nothing re-run.** No enclave / VM / Dataproc / OSF / `gsutil` / `gcloud` /
   network contact anywhere in the plan; $0. The in-flight ~4h20m sweep is untouched and its
   output is an INPUT to the new tool, never a thing to regenerate.
2. **Frozen and public surfaces unchanged.** `git status --porcelain` EMPTY for
   `occlusion_span_filter.py`, `run_native_ld_panel.py`, `fire_verifier.py`, `aou_ld_panel.py`,
   `.planning/amendments/`, the live runbook `260825-PENDING-PASTE-…md`, and the pre-registration
   `260826-PCS-…-prereg-prediction.md`.
3. **The pre-registered numbers did not move**, and the enforcement is structural: the file that
   carries them was not edited, and `test_prereg_pooled_row_prediction_reconciles_with_the_afr_pass`
   is green.
4. **The post-hoc-only property is machine-checked.** Task 1's AST gate (no `BedReader`/`Genotypes`
   import, no `BedReader` reference, no `read_variant`, no `.bed` literal outside a docstring) passes,
   and its RED demonstration is quoted. This is the property that keeps the plan from requiring a
   4h20m re-run, so it is not left to narration.
5. **Every figure in the permanent record carries its source.** The two widening prices are labelled
   SETH-COMPUTED and NOT INDEPENDENTLY RE-DERIVED; the MAF ratio is labelled DIRECTIONALLY CONSISTENT,
   NOT EVIDENCE, citing the 2026-08-18 ruling that governs it.
6. **Every prose assertion normalises whitespace first**; every numeric assertion normalises commas
   first and uses `(?<!\d)n(?!\d)`.
7. **Every new gate was seen RED.** Each of the three new/extended test files has at least one
   negative control that flips a green assertion, and the pre-implementation RED output is quoted
   in the SUMMARY. No gate is a bare substring match on a short token or a number; numerics use
   `(?<!\d)<n>(?!\d)` after comma normalisation.
8. **The occlusion rule has exactly one implementation.** Function-identity assertions tie the new
   tool to `occlusion_span_filter.detect_occluded_variants` and to the scanner's
   `iter_bim_windows` / `_read_regions_tsv` / `BedReader`.
9. **Suite re-baselined component-exact**: 0 failed, skipped exactly 33, new total reconciled per
   file against the measured 112 + 1054 = 1166 baseline.
10. **Explicit-path staging only** on the shared GPFS tree; branch `m3-W2-aou-deltas`; no worktree
   isolation; pre-existing untracked clutter left alone.
</verification>

<success_criteria>
- `src/python/pcs_panelwide_reclassify.py` answers the panel-wide `--exclude` question from an
  already-emitted `pcs_pairs.tsv` + the cohort `.bim`, calls the FROZEN detector, reports the two
  populations separately at BOTH row and pair level plus the `--mac 1`-subtracted third tier, keys
  on VID with production's semantics, counts and NAMES id ambiguity, carries row-set provenance
  with the monotonicity scope sentence, and RAISES on a mismatched `.bim` or a drifted header.
- `src/python/pairwise_completeness_scan.py:116-135` no longer claims `already_occluded` is the
  `--exclude` side; it says ANCHOR-RELATIVE, names the panel-wide quantity separately, points at
  the new tool, and the correction is enforced by a RUNTIME test that demonstrates the two
  predicates disagreeing. The rename is adjudicated, DECLINED with three measured reasons, given a
  trigger, and enforced.
- `src/python/samepos_missingness_probe.py` is a tested instrument with a neutral three-label
  vocabulary, deterministic sampling, census-beside-sample reporting, aggregate-only egress, and
  the Hail inference recorded beside it as an INFERENCE that this node cannot verify.
- `.planning/debug/260831-PENDING-PASTE-POSTHOC-…md` stages both post-hoc runs with a machine-checked
  `STATUS: STAGED — NOT RUN` marker and argvs that parse against the real parsers.
- `.planning/debug/260831-seth-brief-blind-review-…md` records the review, both findings, what
  changed, what did NOT change and why, and the accepted-without-action items with their prices.
- Full `tests/m3`: 0 failed, 33 skipped, total reconciled per file.
- Nothing fired. The pre-registered 15 / 13 / 10-3 and the offset histogram are unchanged.
</success_criteria>

<output>
After completion, create
`.planning/quick/260831-kw8-close-seth-s-brief-blind-review-already-/260831-kw8-SUMMARY.md`.

It must carry: the quoted RED output for every new test file before implementation; the full audit
list from Task 2 (sites inspected, hits AND non-hits); the per-file test-count reconciliation
arithmetic; the derived row-level 5 = 3 + 2 and pair-level 3 = 1 + 2 split WITH its scope caveat;
the rename decline with its three reasons and its trigger; the RED and GREEN outputs of Task 1's
post-hoc-only AST gate; the provenance resolution for all three accepted-without-action figures
(which are Seth's, which are ours, which we re-derived — none — and why re-derivation was not
attempted); and an explicit statement that nothing was fired, the sweep was not touched, and no
pre-registered number moved.
