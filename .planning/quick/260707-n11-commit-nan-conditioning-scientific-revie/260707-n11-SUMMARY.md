# Quick Task 260707-n11 — SUMMARY

**Commit Seth's NaN-conditioning scientific-review memo in-repo beside the amendment**

**Date:** 2026-07-07 · **Branch:** m3-W2-aou-deltas · **Mode:** quick (inline) · **DOCS-only**

## What & why

Seth's peer review found the NaN mechanism behind the AFR-panel conditioning is
mis-identified (see the memo). This task commits that review in-repo as the **durable
record of WHY m3-06 is held landed-but-not-trusted**, beside the amendment it gates.

## Integrity — byte-verified, not reconstructed-and-trusted

Seth supplied the memo with a full-file SHA-256 anchor. I reconstructed the body to
scratch and **confirmed `e82201147c…` / 7364 B before committing**; the repo file is the
`> Seth review …` self-locating header note + the byte-verified body (body-after-header
re-hashed to the anchor). Same discipline that byte-verified the OSF amendment.

## Landed

- **`.planning/amendments/m3_nan_conditioning_scientific_review.md`** (new, 7540 B =
  header note + blank + 7364-B verified body) — beside
  `osf-amendment-afr-native-ld-nan-psd-2026-07-03.md`.
- **STATE.md reconciled** to ONE coherent hold record (no duplicate block): the existing
  SCIENTIFIC HOLD (`21a5642`) memo reference now points to the in-repo path + SHA, and
  the pair distance is corrected `5-8 bp` → `8-52 bp` per memo v3. Quick-task row +
  last_activity added.

## The finding this memo records (headline)

The amendment's "pairwise-undefined `r` (0/0)" premise is arithmetically impossible —
pairwise-complete monomorphism is ~10⁻⁹⁴⁷–10⁻³⁷²⁹ improbable at the reported
F_MISS/MAF (I independently reproduced this). The NaN requires **correlated missingness**
at co-located records (index-adjacent, 8–52 bp), the fingerprint of a
**variant-representation artifact**. `NaN→0` asserts independence for high-LD neighbors —
directionally wrong. **Gate:** the region-1 2×2 diagnostic must resolve the mechanism
before any conditioned region feeds a fit.

## Do-NOTs honored (verified git-diff-clean)

- OSF amendment file / coverage flag / tag **UNTOUCHED** — this memo is a NEW gate on
  top, not a revision of the (correct) posting record.
- `read_square_bin` NaN-raise intact; `condition_ld_matrix.py` **FROZEN** — the third
  topology category (co-located → variant normalization) is designed AFTER the 2×2 result.
- No OSF re-post / amendment (memo §5: the 2×2 result decides upstream-normalization note
  vs narrow amendment-update).

## Thread state

Two paste blocks were in flight: the **region-1 2×2 diagnostic** (→ AoU browser agent,
egress the 6-row JSON only) and **this memo commit** (done). The 2×2 is the unblocking
move and is NOT loop-gated.
