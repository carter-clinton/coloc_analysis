---
quick_id: 260707-n11
slug: commit-nan-conditioning-scientific-revie
description: "Commit the NaN-conditioning scientific-review memo (Seth) in-repo beside the amendment as the durable HOLD record"
date: 2026-07-07
mode: quick
must_haves:
  truths:
    - "The scientific-review memo is committed in-repo, byte-verified against Seth's SHA-256 anchor (not reconstructed-and-trusted)."
    - "The existing SCIENTIFIC HOLD (21a5642) is reconciled to one coherent record — no duplicate hold block."
    - "The OSF amendment file, coverage flag, and tag are UNTOUCHED; read_square_bin NaN-raise + condition_ld_matrix.py are unchanged (memo is a NEW gate, not a revision)."
  artifacts:
    - ".planning/amendments/m3_nan_conditioning_scientific_review.md (byte-verified body e82201147c + header note)"
    - ".planning/STATE.md (hold pointer + bp reconcile + quick row)"
  key_links:
    - ".planning/amendments/osf-amendment-afr-native-ld-nan-psd-2026-07-03.md (companion — the amendment this memo gates)"
    - ".planning/STATE.md:21 (the existing SCIENTIFIC HOLD block being reconciled)"
---

# Quick Task 260707-n11: commit the NaN-conditioning scientific-review memo

DOCS-only. Commit Seth's scientific-review memo as the durable record of WHY m3-06 is
held landed-but-not-trusted, beside the amendment it gates. No code, no re-fire, no OSF
change.

## Integrity (verify, don't trust)

Seth supplied the memo with a full-file SHA-256 anchor. I reconstructed the body to
scratch and confirmed it hashes to `e82201147c…` / 7364 B BEFORE committing; the final
repo file = `> Seth review …` header note + blank + the byte-verified body (body-after-
header re-hashed to the anchor). Same discipline as the OSF amendment (quick-260704-0uf).

## Task 1 — commit the memo + reconcile the hold

- **files:** `.planning/amendments/m3_nan_conditioning_scientific_review.md` (new),
  `.planning/STATE.md`
- **action:** save the byte-verified memo; reconcile the existing SCIENTIFIC HOLD block
  (`21a5642`) — point the memo ref at the in-repo path, fix the pair distance
  `5-8 bp` → `8-52 bp` (memo v3); add the quick-task row + last_activity. Do NOT add a
  second hold block.
- **verify:** `tail -n +3 <memo> | sha256sum` == `e82201147c…`; OSF amendment /
  coverage flag / tag / `read_square_bin` / `condition_ld_matrix.py` git-diff-clean.
- **done:** committed with explicit paths; the gate (region-1 2×2 diagnostic before any
  conditioned region feeds a fit) is recorded and stands.

## Do NOT

- Do not weaken `read_square_bin`'s NaN-raise.
- Do not unfreeze / edit `condition_ld_matrix.py` — the third topology category
  (co-located → normalization) is designed AFTER the 2×2 result.
- Do not change / re-post / amend the OSF amendment, coverage flag, or tag — they
  recorded the posting correctly; this memo is a NEW gate on top.
