# Quick task 260821-jcs — BRIEF (orchestrator-authored, the single source the planner / checker / executor / verifier all read)

Bank Seth's FINAL PASS (no blocking objection) as the seventh supporting record, execute the amendment's own RE-CONFIRMED-AT-POSTING step (PRE_EXECUTE_COMMIT advance via the engine, POSTING_DATE unchanged), refresh anchors, and write Carter's POSTING CARD. Docs-only; NO code change; NO OSF contact; an agent never posts and never fires.

## CONTEXT (all verified read-only 2026-08-21 13:53 EDT at HEAD 241515b, tree clean, origin == local)

- Seth's final pass on `.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md` (42,213 B / e1b4a11d18ad2907af4f0a93fd5747d2, 591 lines) arrived 13:42 EDT: §2 blocking item CLEARED, §3 CLEARED, §4 companion condition ADOPTED (he recomputed 3.42x / 1.91x / med+3σ=1.407 / med+4σ=1.496 independently), §6 CLEARED, advanced gate commit + guard 2x2 accepted. "No blocking objection remains." He verified both file anchors byte-exact himself (read the Mac repo copy directly). He explicitly names two things NOT his: Carter posts; POSTING_DATE re-confirmation.
- The amendment's pre-paste table row (line 63) carries a STANDING instruction: "RE-CONFIRMED AT POSTING: re-read HEAD, confirm no `_OCCLUSION_ANOMALY_FRACTION` or gate-constant change has landed since, and update this value if the branch has advanced." The branch HAS advanced past 2689cae by four DOCS-ONLY commits (b4263e7, a364d19, cd0cdfd, 241515b; `git diff --stat 2689cae HEAD -- src/ tests/ config/` is EMPTY; `src/python/run_native_ld_panel.py:133` still reads `_OCCLUSION_ANOMALY_FRACTION = 0.0005`). Banking Seth's verdict adds one more. So the row's own rule requires moving PRE_EXECUTE_COMMIT, by the ENGINE (Class-P force-substitution), never by hand.
- Engine: `.planning/quick/260820-s2x-instantiate-the-occlusion-recalibration-/260820-s2x-instantiate.py`. Prior (u6i) invocation, verbatim from its SUMMARY Appendix 2: `python3 <engine> --second-pass <amendment> --pre-execute-commit <40-hex> --posting-date 2026-08-21`. Under --second-pass: every Class-M slot (19) is VERIFIED-IN-PLACE against fresh recomputation from the banked records (drift ABORTS); the 2 Class-P slots are FORCE-SUBSTITUTED document-wide. Expected this run: 19 VERIFIED-IN-PLACE, 0 SUBSTITUTED, 2 FORCE-SUBSTITUTED, `RECONCILIATION: OK`, exit 0.
- Guard: `bash .planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh all <amendment>` — exit 0 now; must be exit 0 after.
- PRE_EXECUTE_COMMIT 40-hex currently occurs at exactly 2 places (line 63 pre-paste row, line 92 SLOT_LEDGER); the name only at line 140. POSTING_DATE `2026-08-21` occurs at 3 (line 64 row, line 91 ledger, line 168 paste-block `**Date:**`) and stays 2026-08-21 (Carter intends to post today).
- THE POSTED BODY MUST NOT MOVE. The paste block (between the marker lines `--- PASTE INTO OSF FROM HERE ---` at line 164 and `--- PASTE ENDS HERE ---` at line 498, EXCLUSIVE of both markers) is currently 22,945 B / md5 422f1f28d6a3b76c7657fadec05a0237, extracted with: `awk '/^--- PASTE INTO OSF FROM HERE ---$/{f=1;next} /^--- PASTE ENDS HERE ---$/{f=0} f' <amendment>`. PRE_EXECUTE_COMMIT does not occur inside the paste block, so this md5 MUST be byte-identical after the task — that identity is what proves Carter posts exactly the body Seth verified. Assert it.
- Seth's verdict is already transcribed byte-faithfully (with an as-received provenance header) at the SCRATCHPAD path `/gpfs_common/share01/clintonlab/ckclinto/tmp/claude-410819/-gpfs-common-share01-clintonlab-ckclinto-coloc-analysis/19c6a323-a3ca-4c18-bffc-bfcb052f7fe3/scratchpad/260821-SETH-FINAL-PASS-no-blocking-objection-as-received.md`. Bank it by `cp` + `cmp` (byte-identical), NOT by re-typing; do not edit its body.
- Carter's Mac: login `ckclinto@login.hpc.ncsu.edu`; destination dir `/Users/cc/Documents/NCSU/1.ASHES_Lab_Research/Analyses/coloc_analysis/`; macOS hashing is `md5` (not `md5sum`).

## TASKS (atomic commits, explicit paths only — never `git add -A`/`.`; no worktree isolation on this GPFS tree)

1. BANK the verdict: `cp` the scratchpad file to `.planning/debug/260821-SETH-FINAL-PASS-no-blocking-objection-as-received.md`; `cmp` must be silent; report wc -c / md5sum. Commit: `docs(debug): bank Seth's final pass as-received — no blocking objection; seventh supporting record`.

2. RE-CONFIRM AT POSTING (the amendment's own rule). (a) Capture HEAD AFTER the Task-1 commit as the full 40-hex (`git rev-parse HEAD`) — this is the HEAD against which the "docs-only since" check is made; record `git log --oneline 2689cae..HEAD` and the EMPTY `git diff --stat 2689cae HEAD -- src/ tests/ config/` and the `grep -n '_OCCLUSION_ANOMALY_FRACTION = 0.0005' src/python/run_native_ld_panel.py` hit in the SUMMARY. (b) Run the engine `--second-pass` with `--pre-execute-commit <that 40-hex> --posting-date 2026-08-21`. Capture stdout verbatim into the SUMMARY (as u6i did). (c) Assert: `grep -c 2689cae <amendment>` == 0; `grep -c <new40hex> <amendment>` == 2; `grep -c '{{'` == 0; POSTING_DATE still at exactly 3 occurrences; paste-block md5 == 422f1f28d6a3b76c7657fadec05a0237 and 22,945 B (the invariant). (d) Guard `all` exit 0. (e) NEGATIVE CONTROL, re-executed not transcript-trusted: on a COPY in the scratchpad, re-introduce one `{{SLOT}}` sentinel → guard `all` must exit non-zero; and run the engine `--dry-run --control-source <perturbed copy of the banked site-basis record with one table value changed>` and show the RECONCILIATION fails (the engine's documented control mode). Seen-red evidence goes in the SUMMARY. Commit: `docs(amendment): RE-CONFIRMED AT POSTING — PRE_EXECUTE_COMMIT advanced to <short> by engine Class-P pass; paste block byte-identical (422f1f28…)`.

3. PROSE outside the paste block, minimal and accurate (hand edits are allowed ONLY outside the markers and ONLY to prose, never to a number): (a) line 63's description sentence currently says the hash is "the HEAD … captured before the first commit of the REVISING task. It SUPERSEDED the first instantiation's value…" — update so it is TRUE of the new value: captured at the 2026-08-21 posting-prep re-confirmation, after Seth's final pass was banked; all commits between 2689cae and it are docs-only (enumerate the short SHAs); keep the standing RE-CONFIRMED-AT-POSTING sentence intact. (b) item 4 of the instantiation record (line ~140, "PRE_EXECUTE_COMMIT is the full 40-hex HEAD captured before the REVISING task's first commit") — same correction in one clause. (c) pre-paste checklist item 3 (line 157): "six supporting records" → "seven", appending the new path `.planning/debug/260821-SETH-FINAL-PASS-no-blocking-objection-as-received.md` (Seth's final pass). Do NOT touch lines 215/230/555 — those "six" are the six NaN PAIRS, a different quantity. Re-run guard `all` (exit 0) and re-assert the paste-block md5 invariant after these edits. Report final whole-file wc -c and md5sum. May be folded into the Task-2 commit or its own commit.

4. POSTING CARD for Carter at `.planning/debug/260821-POSTING-CARD-for-carter.md`, written for a human driving a browser, no agent in the loop. Must contain: (i) the fresh whole-file anchors and the paste-block anchor (22,945 B / 422f1f28…); (ii) the Mac-side commands: `scp ckclinto@login.hpc.ncsu.edu:<abs amendment path> /Users/cc/Documents/NCSU/1.ASHES_Lab_Research/Analyses/coloc_analysis/`, then extract the paste block with the awk command above into `osf-amendment-occlusion-gate-recalibration-2026-08-21.md` (the OSF filename carries the POSTING date per the prepared deviation entry; the repo basename keeps 2026-08-20), then `wc -c` + `md5` (macOS `md5`, not `md5sum`) must read 22945 / 422f1f28d6a3b76c7657fadec05a0237; (iii) the OSF procedure: osf.io/az52u → Files → UPLOAD the extracted file as a NEW file (never "upload new version" on trsx5; never paste into a wiki/text box — the 2026-07-10 hand-paste is exactly how trsx5's body came out truncated, see `.planning/osf_deviations.md` lines 190-300); (iv) the post-upload captures the prepared deviation entry needs: new file GUID + URL, the authoritative UTC timestamp from OSF Recent Activity (NOT the file page "Date created"), re-download the posted file and `md5` it (must be 422f1f28…), and confirm trsx5 still shows exactly 1 revision (2026-07-10 13:32); (v) what to paste back to the NCSU session so the record step can run; (vi) the if-it-slips rule: if posting is not today, do NOT hand-edit — ask the session to re-run the engine Class-P pass with the new `--posting-date` (3 occurrences move together) and re-issue anchors. End with the status line verbatim: "measurement banked; amendment drafted, not posted; code constant unchanged; fire HELD; an agent never posts and never fires."

5. STATE: update the top ★ RESUME HERE ★ block of `.planning/STATE.md` and `.planning/HANDOFF.json` → `resume_on_reconnect[0]` (2026-08-21 AFTERNOON): Seth final pass RECEIVED and banked; pre-paste re-confirmation DONE with new PRE_EXECUTE_COMMIT and fresh anchors; NEXT = Carter posts from the posting card, then the record quick task (append the prepared deviation entry with GUID/URL/UTC, DEC-…-occlusion-recalibration-posted, HANDOFF gates.osf_pre_registration, tag), THEN the remediation batch (two-condition gate: site-basis ceiling 0.5056% AND inflation 3.42x — BOTH, per amendment Post-Paste item 6 and Seth's closing line), then Stage A. Final commit + `git push` (SSH, no PAT). Report the final HEAD.

## VERIFICATION the verifier must re-execute (not read)

guard `all` exit 0 on the final file; paste-block md5 == 422f1f28d6a3b76c7657fadec05a0237 / 22,945 B; `grep -c 2689cae` == 0 and new-hash count == 2; `{{` count 0; banked record `cmp`-identical to the scratchpad source; `git diff --stat 2689cae HEAD -- src/ tests/ config/` empty; `_OCCLUSION_ANOMALY_FRACTION = 0.0005` unchanged; the negative controls were actually run in this task (command + non-zero exit shown); `git status` clean and `origin == local` after push.
