# Quick 260628-244 — SUMMARY

**Completed:** 2026-06-28
**Branch:** m3-W2-aou-deltas
**Status:** ✅ Done — fix + failing-first regression landed, full suite green

## What changed

- **`src/python/run_native_ld_panel.py`** — added `_chrom_match_key()` (strips a
  leading `chr`/`CHR`, mirroring plink1.9's contig normalization) and applied it to
  **both** sides of the chrom compare in `_window_bim_n_var`. Kept-line content stays
  verbatim; the temp window-`.bim` filename now uses the bare key. This unblocks the
  banking path: with a `chr`-prefixed AoU GRCh38 cohort `.bim` and a bare-numeric
  manifest `chr`, the window count now agrees with the `.ld.bin` `n_var` so regions
  verify → bank → reclaim.
- **`tests/m3/test_run_native_ld_panel.py`** — `test_window_bim_n_var_chr_prefix_agnostic`,
  parametrized over all four prefix combinations. Failing-first: the `chr12`/`12` and
  `12`/`chr12` cases returned 0 pre-fix; all four pass post-fix.

## Verification

- `pytest tests/m3/test_run_native_ld_panel.py` → **28 passed** (24 prior + 4 new).
- `pytest tests/m3/test_plink_ld_to_npz.py` → **14 passed** (downstream consumer
  unaffected — it already strips `chr` in `ld_npz_to_rds.R`).
- No stray `chrom_s` references remain.

## Why CI missed it

The fake-plink stub `_MockPlink._n_var_in_window` used the identical literal
`str(r[0]) == str(chrom)` compare against a bare-numeric `.bim`, so the stub and the
buggy verify agreed and the prefix mismatch was never exercised. The new test bypasses
the stub and unit-tests `_window_bim_n_var` directly with a `chr`-prefixed `.bim`.

## Next (operational — not in this commit)

1. Push to origin `m3-W2-aou-deltas`; verify origin tip == local HEAD.
2. AoU agent: `git pull` → confirm fix present → real-data pre-flight (region-1 window
   count ≈ 102,421) → re-fire loop (resume-safe; 0 banked → clean restart at region 1).
3. Liveness = bucket `.npz` count climbing → 276; reclaim keeps scratch flat per region.
