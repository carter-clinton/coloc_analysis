---
phase: quick-260819-u8d
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [DEC-2026-08-19-occlusion-recalibration-adopted]
files_modified:
  - .planning/DECISIONS.md
  - .planning/HANDOFF.json
  - .planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-XX.md
  - .planning/amendments/note-same-position-collinearity-2026-08-19.md
  - .planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh
  - .planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-guard-controls-transcript.txt
  - .planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-SUMMARY.md

must_haves:
  truths:
    - "A reader of .planning/DECISIONS.md finds DEC-2026-08-19-occlusion-recalibration-adopted with Carter's verbatim adoption and the five adopted elements (calibration branch, gate-on-SITES/report-ROWS, 3x-site-median ceiling, instantiate-once sequencing, new-version-never-silent-swap)."
    - "A reader of .planning/HANDOFF.json gates sees the adopted recalibration path appended to both osf_pre_registration and producer_pre_fire_gates, with every pre-existing byte of those two strings intact and every other key deep-equal to the pre-edit file."
    - "A paste-ready amendment body exists that (a) corrects the false line-45 sentence against the measured inventory, (b) states the corrected empirical claim, (c) re-defines clause (d) as gate-on-SITES / account-on-ROWS with a purpose-anchored 3x-site-median ceiling, (d) carries Seth's §8 provenance verbatim, and (e) enumerates what is UNCHANGED."
    - "Not one number that PENDING PASTE #3 must supply appears in the amendment as a literal — every one is a named {{SLOT}} sentinel, and the slot roster is fixed and enumerated."
    - "Running the guard's paste-ready and arith sections against the current uninstantiated draft FAILS, and running them against a test-instantiated copy PASSES — both behaviours observed and transcribed."
    - "The §6 same-position collinearity caveat is recorded in its own note file, not folded into the amendment."
    - "The prepared osf_deviations.md entry text exists inside the amendment file marked NOT-YET-APPENDED, and .planning/osf_deviations.md itself is byte-unchanged."
  artifacts:
    - path: ".planning/DECISIONS.md"
      provides: "The appended DEC-2026-08-19-occlusion-recalibration-adopted entry (append-only; prior 2143 lines byte-intact)"
      contains: "DEC-2026-08-19-occlusion-recalibration-adopted"
    - path: ".planning/HANDOFF.json"
      provides: "Gate-ledger note of the adopted recalibration path on two gate fields"
      contains: "quick-260819-u8d"
    - path: ".planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-XX.md"
      provides: "The paste-ready OSF amendment-update draft with grep-able slot sentinels, drafting notes, slot ledger, and the prepared (NOT-YET-APPENDED) osf_deviations entry text"
      min_lines: 180
      contains: "--- PASTE INTO OSF FROM HERE ---"
    - path: ".planning/amendments/note-same-position-collinearity-2026-08-19.md"
      provides: "The separate §6 record: same-position rows retained, near-collinear predictors at multiallelic sites, split-representation property not a defect, fine-mapping caveat for the manuscript"
      min_lines: 30
      contains: "same-position"
    - path: ".planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh"
      provides: "Re-runnable enforcer: draft / paste-ready / arith / quote / all sections; non-zero exit while any sentinel remains"
      min_lines: 90
      contains: "paste-ready"
    - path: ".planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-guard-controls-transcript.txt"
      provides: "Verbatim seen-red and seen-green output for every guard section, including the vacuity and arithmetic negative controls"
      min_lines: 40
      contains: "SEEN RED"
  key_links:
    - from: ".planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-XX.md"
      to: ".planning/debug/260819-PENDING-PASTE-3-site-basis-sweep.md"
      via: "slot sentinels named for the quantities that sweep prints"
      pattern: "\\{\\{SITE_MEDIAN_PCT\\}\\}"
    - from: ".planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-XX.md"
      to: ".planning/debug/260819-SETH-VERDICT-adjudication-confirmed-as-received.md"
      via: "§8 provenance carried verbatim as a blockquote"
      pattern: "calibrated against observed NaN count"
    - from: ".planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh"
      to: ".planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-XX.md"
      via: "guard reads the amendment path passed as argv and fails on any remaining sentinel"
      pattern: "\\{\\{"
    - from: ".planning/DECISIONS.md"
      to: ".planning/debug/260819-SETH-C1C2C3-convergence-as-received.md"
      via: "Cross-refs block naming the three banked adjudication files"
      pattern: "260819-SETH-C1C2C3-convergence-as-received"
---

<objective>
Record the adopted occlusion-gate recalibration decision and draft — draft only, no OSF
contact — the amendment package that will correct the public record once PENDING PASTE #3
supplies the site-basis numbers.

Purpose: Carter adopted the recalibration branch at 21:45 EDT 2026-08-19. Three things must
exist before the next fire-morning step: (1) the decision in the ledger where a future
session will look for it; (2) a paste-ready amendment body whose every unmeasured number is
a named, grep-able sentinel so it physically cannot reach OSF uninstantiated; (3) a named
enforcer that FAILS while any sentinel remains. Item (3) is the R4-gate lesson applied
directly: an obligation with no failing check lapses silently.

Output:
- `.planning/DECISIONS.md` — DEC-2026-08-19-occlusion-recalibration-adopted (appended)
- `.planning/HANDOFF.json` — two gate strings extended, append-only, byte round-trip preserved
- `.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-XX.md` — the package
- `.planning/amendments/note-same-position-collinearity-2026-08-19.md` — the §6 note
- `.planning/quick/260819-u8d-.../260819-u8d-placeholder-guard.sh` + controls transcript
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/debug/260819-SETH-C1C2C3-convergence-as-received.md
@.planning/debug/260819-SETH-VERDICT-adjudication-confirmed-as-received.md
@.planning/debug/260819-occ-measure-sweep-results-as-received.md
@.planning/debug/260819-supplement-results-as-received.md
@.planning/debug/260819-PENDING-PASTE-3-site-basis-sweep.md
@.planning/amendments/osf-amendment-afr-occlusion-exclude-UPDATE-2026-07-10.md

Read on demand (large; do not load whole):
- `.planning/debug/fire-morning-occlusion-oracle-vs-geometry.md` — read §2 (lines 425-531)
  and §4 (lines 611-702) only. Those two ranges carry every rationale fact the amendment needs.
- `.planning/DECISIONS.md` — read the LAST entry only (lines 2030-2143,
  `DEC-2026-08-17-trsx5-gate-released`) as the house format template. Do not read the file whole.
- `.planning/osf_deviations.md` — read lines 133-189 (the 2026-07-10 trsx5 entry) as the
  FORMAT TEMPLATE for the prepared entry text. **This file is NOT edited in this task.**
</context>

<interfaces>
<!-- Contracts fixed here so Tasks 2 and 3 agree without exploration. -->

## THE SLOT ROSTER — canonical, 13 slots, fixed by this plan

Class M — instantiated ONCE from PENDING PASTE #3 stdout (11 slots):

| Slot | Source | Rendered form |
|---|---|---|
| `{{SITE_MIN_PCT}}` | sweep `SITE-BASIS SUMMARY … min=` | `0.1102%` |
| `{{SITE_MEDIAN_PCT}}` | sweep `… median=` | `0.1330%` |
| `{{SITE_MAX_PCT}}` | sweep `… max=` | `0.2500%` |
| `{{SITE_ROBUST_SIGMA_PCT}}` | sweep `robust_sigma(1.4826*MAD)=` | `0.0300%` |
| `{{MEAN_ROW_SITE_INFLATION}}` | sweep `mean row/site inflation` | `1.45x` |
| `{{MED_PLUS_3SIG_PCT}}` | DERIVED = median + 3·sigma | `0.2230%` |
| `{{MED_PLUS_4SIG_PCT}}` | DERIVED = median + 4·sigma | `0.2530%` |
| `{{TWO_X_MEDIAN_PCT}}` | DERIVED = 2·median | `0.2660%` |
| `{{TWO_X_MAX_PCT}}` | DERIVED = 2·max | `0.5000%` |
| `{{CEILING_3X_MEDIAN_PCT}}` | sweep `CANDIDATE CEILING …` = 3·median | `0.3990%` |
| `{{CEILING_MARGIN_X}}` | sweep `margin over observed site-basis max` = ceiling/max | `1.60x` |

Class P — instantiated at posting time by Carter (2 slots):

| Slot | Rendered form |
|---|---|
| `{{POSTING_DATE}}` | `2026-08-21` |
| `{{PRE_EXECUTE_COMMIT}}` | `a1b2c3d` |

(The "rendered form" column shows SHAPE only. Those digits are illustrative, not values.)

## THE SLOT LEDGER — exact block, lives OUTSIDE the paste markers

Two-space indent, single spaces around `=`, one slot per line, roster order above:

```
SLOT_LEDGER
  SITE_MIN_PCT = {{SITE_MIN_PCT}}
  SITE_MEDIAN_PCT = {{SITE_MEDIAN_PCT}}
  SITE_MAX_PCT = {{SITE_MAX_PCT}}
  SITE_ROBUST_SIGMA_PCT = {{SITE_ROBUST_SIGMA_PCT}}
  MEAN_ROW_SITE_INFLATION = {{MEAN_ROW_SITE_INFLATION}}
  MED_PLUS_3SIG_PCT = {{MED_PLUS_3SIG_PCT}}
  MED_PLUS_4SIG_PCT = {{MED_PLUS_4SIG_PCT}}
  TWO_X_MEDIAN_PCT = {{TWO_X_MEDIAN_PCT}}
  TWO_X_MAX_PCT = {{TWO_X_MAX_PCT}}
  CEILING_3X_MEDIAN_PCT = {{CEILING_3X_MEDIAN_PCT}}
  CEILING_MARGIN_X = {{CEILING_MARGIN_X}}
  POSTING_DATE = {{POSTING_DATE}}
  PRE_EXECUTE_COMMIT = {{PRE_EXECUTE_COMMIT}}
```

Post-instantiation the ledger is the machine-readable record of what was substituted. The
ledger is what makes DELETING a sentinel (instead of filling it) detectable — a deleted slot
leaves a ledger line that matches no filled-value pattern.

## GUARD CONTRACT — sections, exit codes, filled-value patterns

`bash 260819-u8d-placeholder-guard.sh {draft|paste-ready|arith|quote|all} <amendment_path>`

Exit 0 = every check in the requested section PASSED. Exit 1 = at least one FAILED.
Every section starts with a VACUITY floor and fails on a missing/short file or block.

- `draft` — file exists; ≥ 6,000 bytes; exactly one `--- PASTE INTO OSF FROM HERE ---` and
  one `--- PASTE ENDS HERE ---`, opener before closer; extracted paste block ≥ 3,000 bytes;
  all 13 roster names present; the SLOT_LEDGER block present with exactly 13 ledger lines;
  `(site basis)` appears ≥ 3× and `(row basis)` appears ≥ 3× in the paste block.
  **Expected GREEN in draft state.**
- `paste-ready` — the `draft` vacuity + marker + block-size checks, THEN: zero occurrences of
  `{{` and zero of `}}` anywhere in the file; the basename contains no `XX`; every ledger line
  matches its filled-value pattern. **Expected RED in draft state.**
  Filled-value patterns (GNU ERE, anchored):
  - `*_PCT`: `^  NAME = [0-9]+\.[0-9]+%$`
  - `MEAN_ROW_SITE_INFLATION`, `CEILING_MARGIN_X`: `^  NAME = [0-9]+\.[0-9]+x$`
  - `POSTING_DATE`: `^  POSTING_DATE = 20[0-9]{2}-[0-9]{2}-[0-9]{2}$`
  - `PRE_EXECUTE_COMMIT`: `^  PRE_EXECUTE_COMMIT = [0-9a-f]{7,40}$`
- `arith` — parses the ledger and asserts the six derived identities plus ordering. FAILS
  loudly (never skips) if any value is still a sentinel. **Expected RED in draft state.**
  - `MED_PLUS_3SIG_PCT == SITE_MEDIAN_PCT + 3*SITE_ROBUST_SIGMA_PCT`
  - `MED_PLUS_4SIG_PCT == SITE_MEDIAN_PCT + 4*SITE_ROBUST_SIGMA_PCT`
  - `TWO_X_MEDIAN_PCT == 2*SITE_MEDIAN_PCT`
  - `TWO_X_MAX_PCT == 2*SITE_MAX_PCT`
  - `CEILING_3X_MEDIAN_PCT == 3*SITE_MEDIAN_PCT`
  - `CEILING_MARGIN_X == CEILING_3X_MEDIAN_PCT / SITE_MAX_PCT`
  - ordering: `SITE_MIN_PCT <= SITE_MEDIAN_PCT <= SITE_MAX_PCT`
  Tolerances: absolute **0.001** percentage points for the pct identities (inputs print at
  4 dp, so worst-case propagated rounding is 4 × 0.00005 + 0.00005 = 0.00025 — the tolerance
  is 4× that headroom and is deliberately NOT written 0.0005, to avoid visual confusion with
  the dead constant); **0.02** absolute for the ratio.
- `quote` — Seth's §8 is carried verbatim. Source of truth =
  `.planning/debug/260819-SETH-VERDICT-adjudication-confirmed-as-received.md` **lines
  102-107**. For every non-blank source line in that range, the line's stripped text must
  appear as a substring of the amendment; the count of matched lines must equal the count of
  non-blank source lines (a count identity, not a threshold).
  **Expected GREEN once Task 2 lands.**
- `all` — runs every section; exit 1 if any fails.

Implementation notes (dialect hazards, learned here):
- The guard executes under its own shebang, so it gets `/usr/bin/grep` (GNU), not the
  interactive wrapper. Use `grep -c -F` for `{{` / `}}` and `grep -c -E` with the anchored
  patterns above. Do not use `[^\n]` in a bracket expression.
- Implement `arith` and `quote` as embedded `python3 - <<'PY'` blocks called from the bash
  guard. Do not add a second script.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Record the adoption — DECISIONS.md entry + HANDOFF.json gate notes</name>
  <files>.planning/DECISIONS.md, .planning/HANDOFF.json</files>
  <action>
Two append-only record edits, one commit.

**(A) `.planning/DECISIONS.md` — APPEND a new entry at end of file.** Match the house format
measured at lines 2030-2143 (`DEC-2026-08-17-trsx5-gate-released`): an `## YYYY-MM-DD — DEC-ID:
one-line headline` heading, a `**Decision (CARTER, <time> EDT, verbatim):**` block, `###`
subsections, and a closing `**Cross-refs:**` block. Do not renumber, reorder, or edit any
existing line.

Heading: `## 2026-08-19 — DEC-2026-08-19-occlusion-recalibration-adopted: the CALIBRATION branch is ADOPTED — the gate moves to occluded SITES with ROWS accounting, the ceiling is re-derived as 3x the measured site-basis median, and the correction goes to OSF as a new version`

Content, all five adopted elements explicitly (this entry is the thing a future session reads
instead of re-deriving the adjudication):

1. **Carter's adoption**, verbatim: `"adopt"` at 21:45 EDT 2026-08-19. State plainly that this
   is the formal branch adoption Seth's convergence note was awaiting.
2. **What was adopted** — five numbered commitments:
   (i) the CALIBRATION branch; the NORMALIZATION branch is WITHDRAWN by Seth (C1) because a
   plink `.bim` row is biallelic by construction, so split-multiallelic same-position rows are
   the obligatory representation for this substrate — `bcftools norm -m +` is the wrong
   direction;
   (ii) the anomaly GATE is re-defined on occluded **SITES** (representation-invariant), while
   the exclusion ACCOUNTING stays on **ROWS** (the manifest records rows; the lockstep sumstats
   drop is row-keyed on (CHR,POS)); **both numbers are reported** (C2);
   (iii) the ceiling = **3x the measured SITE-BASIS median**, purpose-anchored per Seth's C3
   derivation, with the explicit **never-calibrate-to-pass** clause;
   (iv) the site-basis re-measurement (PENDING PASTE #3) instantiates the numbers **ONCE**,
   before Seth's brief-blind review — draft basis-agnostic now, instantiate later, never draft
   twice (C3 §5);
   (v) the amendment states a **corrected empirical claim**, not a threshold tweak, and posts as
   a **NEW OSF version/file — never a silent swap**.
3. **The measured basis** (literals, all already banked; every one carries an explicit basis label):
   - STEP 7 first real-data contact: **231** occluded rows vs the settled 5-member oracle; oracle
     ⊆ observed at **exact** indices (index-origin VALIDATED); **7,951** multi-base-REF rows in
     the region-1 window vs the asserted 7; clause-(d) ceiling **51.2** exceeded **4.5x**.
   - Pre-committed systematic 21-region sample, **row basis**: min **0.1323%**, median
     **0.1888%**, max **0.3527%**; **21/21 defer** at 0.0005; flat across small/medium/large.
     Harness cross-check: region 1 reproduced **231 exactly** or all results discarded.
   - Same-position composition measured LARGE everywhere: dup rows **~7-11%** of `n_rows`,
     multiplicities to **21**, region-1 mean multiplicity **3.16** (8,358 rows at 2,645 sites) —
     a representation convention, not a defect.
   - §4: **span-dominant 37/39** single-occluder runs (Seth's chain prediction refuted; one
     deletion span over a same-position stack yields a long consecutive-index run).
4. **The §8 provenance, as Seth stated it** — the 0.0005 was his figure, derived as "~10x
   headroom over the observed 6 NaN pairs at n_var 102,421", **n=1**, calibrated on NaN counts
   and then re-purposed to geometric exclusions **without re-derivation**; premise low by
   **~38x** (0.0059% asserted vs 0.2255% measured, row basis). Note it goes into the amendment
   VERBATIM (Task 2), not paraphrased.
5. **The false sentence in the POSTED record** — `osf-amendment-afr-occlusion-exclude-UPDATE-
   2026-07-10.md:45`: *"Region 1 alone contains 7 distinct overlapping deletions"* — the
   NaN-implicated subset written as window inventory; measured window inventory is **7,951**.
   Record that the *"Zero pairs are same-position"* half of that same bullet was correctly scoped
   to the six NaN pairs and **survives**. Correction is REQUIRED regardless of branch.
6. **What is NOT changed and why it stays untouched** — clause (a) the occlusion CRITERION
   (`:77` fences changing the criterion to obtain a result; recalibrating the GATE is a
   different object and is not fenced); clause (b) exclude-in-lockstep; clause (c) the manifest;
   the defer-not-exclude protocol (it fired on first real-data contact and stopped the fire
   before a banked byte — that is the machinery working); clause (e) present-rate reporting; the
   three `BRANCH_AFR_OCC_*` tokens; the PSD retention; the raw-panel NaN-raise contract.
7. **The honest limitation, recorded rather than buried** — n=21 of 276; the upper tail is
   unmeasured; a full sweep is ~39 h of VM time and is DELIBERATELY not spent pre-amendment;
   per-region counts fold in during the fire, which computes them anyway.
8. **The §6 collinearity caveat** is recorded SEPARATELY (Task 2's note file), by Seth's own
   preference — "I would rather it be recorded somewhere than folded in awkwardly."

`**Cross-refs:**` — `.planning/debug/260819-SETH-C1C2C3-convergence-as-received.md`;
`…/260819-SETH-VERDICT-adjudication-confirmed-as-received.md`;
`…/260819-occ-measure-sweep-results-as-received.md`;
`…/260819-supplement-results-as-received.md`;
`…/260819-PENDING-PASTE-3-site-basis-sweep.md`;
`…/fire-morning-occlusion-oracle-vs-geometry.md`;
`DEC-2026-08-17-trsx5-gate-released` (the new-version-never-a-silent-swap commitment this
decision inherits); the two Task-2 artifacts by path.

**(B) `.planning/HANDOFF.json` — extend exactly two gate strings, append-only.**

Both `gates.osf_pre_registration` and `gates.producer_pre_fire_gates` are plain strings. Append
a clause to each in the house continuation style already used in
`producer_pre_fire_gates` (` ⟶ <date> (<quick-id>): …`):

- `gates.osf_pre_registration` — append: the clause-(d) recalibration is ADOPTED
  (`DEC-2026-08-19-occlusion-recalibration-adopted`); a NEW obligation exists — draft posted as
  a new OSF version/file, agent DRAFTS + Carter POSTS, never a silent swap; the posted `:45`
  factual sentence is FALSE and requires correction; the draft is UNINSTANTIATED until PENDING
  PASTE #3 lands, enforced by `260819-u8d-placeholder-guard.sh paste-ready`; this is a SECOND
  open OSF obligation alongside the still-open Check-2 amendment-update — neither discharges the
  other.
- `gates.producer_pre_fire_gates` — append: the shipped clause-(d) gate constant
  `_OCCLUSION_ANOMALY_FRACTION` = 0.0005 is row-basis and its calibration premise is DEAD
  (21/21 defer); it is NOT to be edited in code until the recalibrated amendment is POSTED
  (silent modification excluded by `:81`, by `:98`, and by the shipped source's own comment at
  `run_native_ld_panel.py:129-130`); the recalibrated gate will count occluded SITES with rows
  reported alongside; Stage A stays unreachable.

Recipe — exact byte round-trip (verified: `json.dumps(d, indent=2, ensure_ascii=False)` currently
reproduces the file EXACTLY, and the file has **no trailing newline**):

```python
import json, pathlib
p = pathlib.Path(".planning/HANDOFF.json")
raw = p.read_text()
d = json.loads(raw)
d["gates"]["osf_pre_registration"] += " ⟶ 2026-08-19 (quick-260819-u8d): ..."
d["gates"]["producer_pre_fire_gates"] += " ⟶ 2026-08-19 (quick-260819-u8d): ..."
p.write_text(json.dumps(d, indent=2, ensure_ascii=False))   # NO trailing newline
```

⛔ Do NOT touch `status`, `resume_on_reconnect`, `headline`, or any other key — those are
close-session surfaces owned by the orchestrator. **Before editing, copy the pre-edit file to**
`/gpfs_common/share01/clintonlab/ckclinto/tmp/claude-410819/-gpfs-common-share01-clintonlab-ckclinto-coloc-analysis/19c6a323-a3ca-4c18-bffc-bfcb052f7fe3/scratchpad/HANDOFF.pre-edit.json`
— the verify step reads it at exactly that path to prove deep-equality of everything else.

**GPFS commit contingency** (this repo loses loose objects ~3× in 3 weeks): if `git commit`
fails with `invalid object` / `Error building trees`, run the recovery loop — for each
`git ls-files -s` entry test `git cat-file -e <sha>`, and for misses re-hash the intact
working-tree file via `git hash-object -w <path>` — then retry the commit ONCE.

Commit with explicit paths only (never `git add .` / `-A` on this shared GPFS tree):
`git add .planning/DECISIONS.md .planning/HANDOFF.json`
`docs(quick-260819-u8d): record DEC-2026-08-19-occlusion-recalibration-adopted + append the adopted recalibration path to the two HANDOFF gate ledgers`
  </action>
  <verify>
    <automated>
cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && python3 - <<'PY'
import json, pathlib, subprocess, sys
SCRATCH = "/gpfs_common/share01/clintonlab/ckclinto/tmp/claude-410819/-gpfs-common-share01-clintonlab-ckclinto-coloc-analysis/19c6a323-a3ca-4c18-bffc-bfcb052f7fe3/scratchpad/HANDOFF.pre-edit.json"   # copy taken BEFORE the edit
fails = []
# --- DECISIONS.md: appended, prior bytes intact ---
dec = pathlib.Path(".planning/DECISIONS.md").read_text()
if "DEC-2026-08-19-occlusion-recalibration-adopted" not in dec: fails.append("DEC id missing")
for s in ['"adopt"', "231", "7,951", "0.1888%", "0.3527%", "37/39", "38x",
          "260819-SETH-C1C2C3-convergence-as-received"]:
    if s not in dec: fails.append("DECISIONS missing: %s" % s)
old = subprocess.run(["git","show","HEAD~1:.planning/DECISIONS.md"],capture_output=True,text=True)
if old.returncode == 0 and not dec.startswith(old.stdout):
    fails.append("DECISIONS.md is NOT append-only (prior bytes changed)")
# --- HANDOFF.json: round-trip, append-only, everything else deep-equal ---
raw = pathlib.Path(".planning/HANDOFF.json").read_text()
new = json.loads(raw)
if raw.endswith("\n"): fails.append("HANDOFF gained a trailing newline")
if json.dumps(new, indent=2, ensure_ascii=False) != raw: fails.append("HANDOFF byte round-trip BROKEN")
pre = json.loads(pathlib.Path(SCRATCH).read_text())
for k in ("osf_pre_registration", "producer_pre_fire_gates"):
    if not new["gates"][k].startswith(pre["gates"][k]): fails.append("gates.%s not append-only" % k)
    if "quick-260819-u8d" not in new["gates"][k]: fails.append("gates.%s missing the new clause" % k)
    if len(new["gates"][k]) <= len(pre["gates"][k]): fails.append("gates.%s did not grow" % k)
    pre["gates"][k] = new["gates"][k]          # normalise the two intended edits
if new != pre: fails.append("HANDOFF changed OUTSIDE the two gate fields")
print("FAIL: " + "; ".join(fails) if fails else "TASK 1 VERIFY: PASS")
sys.exit(1 if fails else 0)
PY
    </automated>
  </verify>
  <done>
`.planning/DECISIONS.md` carries DEC-2026-08-19-occlusion-recalibration-adopted with Carter's
verbatim adoption, all five adopted elements, the measured literals, the §8 provenance summary,
the `:45` false-sentence record, the unchanged-list, and cross-refs — appended, prior bytes
byte-identical. `.planning/HANDOFF.json` re-parses, round-trips byte-exactly with
`json.dumps(indent=2, ensure_ascii=False)`, still has no trailing newline, differs from the
pre-edit file in exactly the two gate strings, and each of those two is a strict prefix-extension
of its old value. One commit, explicit paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: Draft the amendment package + the separate §6 collinearity note</name>
  <files>.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-XX.md, .planning/amendments/note-same-position-collinearity-2026-08-19.md</files>
  <action>
Two NEW files. **DRAFT ONLY — no OSF contact, no posting, no edits to any existing amendment.**

⛔ Do NOT edit `.planning/amendments/osf-amendment-afr-occlusion-exclude-UPDATE-2026-07-10.md`.
The correction happens ON OSF via a new file; the repo copy is the historical record of what was
posted. Annotate NOTHING there in this task. Likewise do NOT touch `tests/`, `src/`, `config/`,
`Snakefile`, the fire verifier, or the `260812-ox1` runbooks.

⛔ Do NOT append to `.planning/osf_deviations.md` (the live, pre-registered file — note the
`.planning/amendments/osf_deviations.md` sibling is a DIFFERENT, Track-A-scoped file; neither is
edited). The prepared entry text lives inside the amendment file, marked NOT-YET-APPENDED.

**FILE A — `.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-XX.md`**

Follow the project's paste-block convention, measured from the July file: a Pre-Paste Reference
block, then `--- PASTE INTO OSF FROM HERE ---`, the body, then `--- PASTE ENDS HERE ---`, then a
Post-Paste Reference block. Content between the markers is exactly what Carter pastes to OSF.

Use the slot roster and SLOT_LEDGER from `<interfaces>` verbatim. **Every quantity PENDING
PASTE #3 will produce is a `{{SLOT}}`. Row-basis numbers already measured appear as literals.**
Label every percentage `(site basis)` or `(row basis)` — mixing bases is the single easiest way
to ship a wrong number here; add a one-paragraph "Basis conventions" statement in the body saying
so, and use both labels at least three times each inside the paste block.

BEFORE the opening marker (drafting notes, never pasted):
- `> **DRAFT — NOT POSTED. Agent DRAFTS, Carter POSTS** (m3-07a discipline).` plus a one-line
  statement that the body is uninstantiated and that
  `bash .planning/quick/260819-u8d-.../260819-u8d-placeholder-guard.sh all <this file>` is the
  gate that must be GREEN before any paste.
- Pre-Paste Reference table modeled on the July file: target `osf.io/az52u` as a **NEW
  supplementary file** (append-only; M1/r3/tcujq/trsx5 pattern), amendment kind = methods
  correction + gate recalibration, prior record = `trsx5` (posted 2026-07-10T13:32:22Z), original
  pre-registration `osf.io/pvb5j` (DOI 10.17605/OSF.IO/PVB5J), what changes, what does not,
  posting gate, substrate, pre-execute commit `{{PRE_EXECUTE_COMMIT}}`, expected date
  `{{POSTING_DATE}}`. State explicitly that this is **NOT** a re-version of `trsx5` and that
  `trsx5` must still show exactly 1 revision after posting (the same append-only check that
  cleared the July record) — "new OSF version, never a silent swap" means a new dated record in
  the chain, never an in-place edit of a posted body.
- The SLOT_LEDGER block, exactly as specified in `<interfaces>`.
- Instantiation instructions: paste PENDING PASTE #3 stdout, substitute each Class-M slot ONCE
  everywhere it occurs (including its ledger line), compute the four DERIVED slots by the stated
  formulas, then run the guard's `paste-ready` and `arith` sections and require GREEN. Note that
  the filename's `XX` is itself instantiated at posting (the guard fails while `XX` remains in
  the basename).

BETWEEN THE MARKERS — the body, in this order:

1. **Title + `**Date:** {{POSTING_DATE}}` + Investigator** (Carter K. Clinton, NCSU ASHES Lab,
   ORCID 0000-0003-2669-8200 — copy the July file's line).
2. **Purpose of amendment-update.** One paragraph: this corrects a factual error in the
   2026-07-10 record (az52u file `trsx5`) and recalibrates that record's clause-(d) anomaly gate
   against measurement. The mechanism, the exclusion policy, the manifest requirement and the
   defer-not-exclude protocol are UNCHANGED.
3. **(a) Factual correction.** Quote the false sentence verbatim — *"Region 1 alone contains 7
   distinct overlapping deletions (60/29/7/31/31/17/29 bp)"* — and state: that is the inventory
   of the deletions implicated in the six NaN pairs, written as if it were the window inventory.
   The measured region-1 window inventory is **7,951 multi-base-REF rows** in 102,421 records
   (**7.76%**), which is the ordinary WGS figure (gnomAD v3: indels ≈14.8% of variants, deletions
   ≈half of indels). The asserted 7-in-102,421 is 0.0068% — ~1,140× below the measured value and
   three orders of magnitude below any published WGS callset. State plainly that the error was
   an unflagged scope promotion — a pair-indexed enumeration written as a window-indexed one —
   and that the *same bullet's* other claim, *"Zero pairs are same-position multiallelic
   records"*, was correctly scoped to the six NaN pairs and **stands**; what does NOT stand is
   any window-scale reading of it (same-position rows are ~7-11% of rows window-wide).
4. **(b) The corrected empirical claim.** Seth's framing, stated as the amendment's headline:
   *we did not discover that our policy was wrong; we discovered that our estimate of how often
   the policy applies was wrong by a factor of 38.* Spell out that the posted clause (d) carried
   an implicit factual assertion — that occlusion is rare enough for 0.05% to be generous
   headroom — and that measurement falsifies it by ~38x. Geometric occlusion at **0.13-0.35% of
   rows (row basis)** is a population-scale property of this substrate, not an anomaly.
5. **(c) The evidence.** The pre-committed systematic-by-span 21-region sample with the full
   row-basis distribution as literals (min **0.1323%**, median **0.1888%**, max **0.3527%**,
   robust sigma **0.0393%**, **21/21 defer** at 0.0005, flat across small/medium/large); the
   harness cross-check (region 1 must reproduce **231** exactly or all results discarded — it
   did); the region-1 first-contact numbers (**231** occluded rows vs the 5-member oracle, oracle
   ⊆ observed at exact indices, ceiling 51.2 exceeded 4.5x); the same-position composition
   (dup rows ~7-11% of n_rows, multiplicities to 21, region-1 mean multiplicity 3.16 = 8,358 rows
   at 2,645 sites); span-dominance 37/39. State the mechanism conclusion: a `.bim` row is
   biallelic by construction, so a correctly-normalized split-multiallelic callset necessarily
   renders one k-allelic site as k same-position rows — this is the obligatory representation for
   this substrate, and `bcftools norm -m +` would move in the wrong direction. Therefore the
   correction is a **calibration**, not an upstream normalization change.
6. **(d) The recalibrated clause (d).** Replace the posted clause (d) with, in this order:
   - **Purpose statement first** (the thing whose absence caused the original error): the gate
     exists to detect a region whose occlusion count is qualitatively unlike the measured
     population — a representation or LD-construction failure — with a low false-defer rate,
     because every false defer discards a scientifically usable region. It does NOT exist to
     certify that occlusion is rare.
   - **Metric definition:** the gate counts occluded **SITES** as a fraction of the region's
     total distinct sites (representation-invariant — a row-basis gate fires differently on
     identical biology depending on how the caller split multiallelics, counting 3 at
     multiplicity 3 and 18 at multiplicity 18). The exclusion **ACCOUNTING remains on ROWS**
     (rows are what leave the panel; the manifest records rows; the lockstep sumstats drop is
     row-keyed on (CHR,POS)); reporting exclusions in sites would understate what left the panel
     and break the manifest's audit purpose. **Both numbers are reported for every region**, with
     the measured mean row/site inflation `{{MEAN_ROW_SITE_INFLATION}}` across the sample.
   - **The ceiling:** `n_occluded_sites <= {{CEILING_3X_MEDIAN_PCT}} × n_sites`, i.e. **3x the
     measured site-basis median** ({{SITE_MEDIAN_PCT}}, site basis), giving
     `{{CEILING_MARGIN_X}}` margin over the observed site-basis maximum
     ({{SITE_MAX_PCT}}, site basis).
   - **The derivation, including what was rejected and why** — reproduce Seth's C3 table on the
     site basis: `median + 3 sigma_rob = {{MED_PLUS_3SIG_PCT}}` REJECT (a normal region defers);
     `median + 4 sigma_rob = {{MED_PLUS_4SIG_PCT}}` REJECT; `2x median = {{TWO_X_MEDIAN_PCT}}`
     REJECT (too tight for n=21); `2x observed max = {{TWO_X_MAX_PCT}}` CANDIDATE;
     `3x median = {{CEILING_3X_MEDIAN_PCT}}` ADOPTED. Give the reasoning: anchored on a location
     statistic rather than where the sample happened to stop, so extending the sample moves it
     only if the population's centre moves; the margin respects an unmeasured upper tail; it
     still fires on anything ≥3x typical, which is the regime where "representation problem
     beyond isolated occlusion" actually lives. Note the row-basis analogue Seth derived
     (**0.5664%**, row basis) so the two bases are reconcilable by a reader — and label both.
   - **The no-calibrate-to-pass clause, on the public record:** the multiplier was NOT chosen
     because it clears the sample. x8 and x10 also pass 21/21 and are rejected precisely because
     they would have been chosen for clearing the data — the original error inverted. That
     3x-median happens to pass 21/21 is a consequence of the derivation, never its justification.
     *"If a future region legitimately sits at 0.6%, the gate should fire and we should
     investigate, not widen the gate."* Re-widening the ceiling in response to a firing region is
     prohibited without a new amendment.
   - **The limitation, stated honestly:** n=21 of 276. The sample was pre-committed and
     systematic-by-span and the distribution is flat across size classes, but the upper tail is
     unmeasured. A full 276-region sweep is ~39 h of VM time (8.6 min/region measured) and is
     deliberately not spent ahead of this amendment; every region computes its own count during
     the run, so the full distribution folds in at closeout and is reported there.
   - **Unchanged within (d):** deferral remains NOT auto-exclusion — a region over the ceiling is
     deferred for re-diagnosis and disclosed as a deviation. Nothing about that protocol moves.
7. **(e) Provenance of the withdrawn constant — Seth's §8, VERBATIM.** Carry lines 102-107 of
   `.planning/debug/260819-SETH-VERDICT-adjudication-confirmed-as-received.md` as a blockquote,
   word-for-word, attributed as the methodological reviewer's own account. Put the
   premise/measured arithmetic in a fenced code block so markdown cannot mangle it. Do NOT
   paraphrase, soften, or reorder — the guard's `quote` section checks every non-blank source
   line by containment and by count identity.
8. **(f) Methods provenance.** How the numbers were obtained: the frozen
   `occlusion_span_filter` detector via its own `load_bim_rows` — the identical code path as the
   failed gated test; a pre-committed systematic-by-span 21-region sample chosen before any
   result was seen; a harness cross-check that discards all results if region 1 does not
   reproduce 231 exactly; read-only, coordinate-only, in-perimeter probes with aggregate-count
   egress only — the same evidentiary class the 2026-07-10 record cites at its `:43`. No
   genotypes, no LD, no banking, no fine-mapping output was produced or consulted.
9. **(g) WHAT IS UNCHANGED — enumerate, do not gesture.** Clause (a) the occlusion CRITERION
   (`[POS, POS+len(REF)-1]` covers a neighbour's POS) — untouched, and note the distinction the
   record itself fences: `:77` prohibits changing the CRITERION to obtain a result; the GATE is a
   different object and recalibrating it is not prohibited. Clause (b) exclude-in-lockstep across
   panel and harmonized sumstats; panel-only exclusion and NaN→0 both still prohibited. Clause
   (c) the mandatory per-variant provenance manifest — a lockstep exclusion without a manifest
   entry remains prohibited. The defer-not-exclude protocol. Clause (e) genome-wide present-rate
   reporting. The three outcome tokens `BRANCH_AFR_OCC_NONE` / `BRANCH_AFR_OCC_EXCLUDED` /
   `BRANCH_AFR_OCC_DEFERRED`, reproduced verbatim and unchanged. PSD regularization and lambda
   (eigclip lambda_floor=1e-6 primary; ridge lambda in {0.001, 0.01, 0.1} robustness). The
   fully-NaN-row drop rule. The raw-panel NaN-raise contract. Pre-registration discipline and
   deviation logging in `.planning/osf_deviations.md`. AoU controlled-tier handling.
   Close with a sentence recording that the pre-registered machinery worked: a gate calibrated on
   a false premise fired at a free local known-answer test, on first real-data contact, before a
   single byte was banked.

AFTER the closing marker (never pasted):
- **Post-Paste checklist** modeled on the July file's: confirm the OSF timestamp precedes any
  commit containing recalibrated-gate outputs; capture the new file GUID + authoritative UTC
  timestamp; confirm `trsx5` still shows exactly 1 revision (append-only, no silent swap);
  append the prepared deviation entry; tag the record commit; only then may
  `_OCCLUSION_ANOMALY_FRACTION` be changed in code.
- **PREPARED `.planning/osf_deviations.md` ENTRY — ⚠ NOT-YET-APPENDED.** Fenced block, in the
  format measured at that file's lines 133-189 (an `## YYYY-MM-DD — <title>` heading with
  `- **Posted:** … - **OSF timestamp (authoritative, UTC):** … - **Project-side copy:** …
  - **What it CORRECTS:** … - **What it RECALIBRATES:** … - **What it RETAINS unchanged:** …
  - **Amends:** …` bullets). Leave the GUID/URL/timestamp as explicit `<TO BE FILLED AT POSTING>`
  markers — those are Carter-observed values, NOT slots (do not add them to the roster; they are
  not knowable from PENDING PASTE #3 and the ledger must stay a measurement ledger).
  Head the block with a bold line: appends to `.planning/osf_deviations.md` ONLY when Carter has
  posted and the URL + timestamp are in hand.

**FILE B — `.planning/amendments/note-same-position-collinearity-2026-08-19.md`**

Short standalone note (~40-70 lines). Recorded separately at Seth's own preference. Content:
- Provenance header: surfaced by the §5 same-position measurement on 2026-08-19, raised in
  `260819-SETH-C1C2C3-convergence-as-received.md` §6; recorded here rather than folded into the
  amendment.
- The observation: same-position rows are alternate ALT alleles at one site; a person cannot
  carry two different ALTs of the same site on the same haplotype, so the dosages of two such
  rows are structurally anti-correlated — their r is partly determined by the representation, not
  purely by population LD.
- The measured scale: ~7-11% of rows across all 21 sampled regions, multiplicities up to 21,
  region-1 mean multiplicity 3.16 (8,358 rows at 2,645 sites).
- What it is NOT, stated as carefully as Seth stated it: not fabrication (the dosages really are
  anti-correlated; the LD is real as computed); not the occlusion problem; it changes no
  conclusion in the recalibration.
- What it IS: a fine-mapping consideration. SuSiE on a submatrix containing several same-position
  rows sees near-collinear predictors — the regime where credible sets inflate or split. This is
  a known property of split-multiallelic representation, not a defect introduced here.
- The disposition: same-position rows are RETAINED (merging them back to multiallelic records is
  not representable in plink and would be the wrong direction). The manuscript carries the caveat
  for fine-mapping at multiallelic sites. Recorded now because an unrecorded observation becomes
  an unanswerable reviewer question later.
- Explicit status line: this note is NOT part of the OSF amendment and is NOT posted. If it is
  ever promoted to the public record that is a separate, dated decision.

Commit both files, explicit paths, one commit. GPFS contingency as in Task 1.
`docs(quick-260819-u8d): draft the occlusion-recalibration OSF amendment package (slot-sentinelled, uninstantiated) + the separate §6 same-position collinearity note`
  </action>
  <verify>
    <automated>
cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && python3 - <<'PY'
import pathlib, re, subprocess, sys
A = pathlib.Path(".planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-XX.md")
B = pathlib.Path(".planning/amendments/note-same-position-collinearity-2026-08-19.md")
ROSTER = ["SITE_MIN_PCT","SITE_MEDIAN_PCT","SITE_MAX_PCT","SITE_ROBUST_SIGMA_PCT",
          "MEAN_ROW_SITE_INFLATION","MED_PLUS_3SIG_PCT","MED_PLUS_4SIG_PCT","TWO_X_MEDIAN_PCT",
          "TWO_X_MAX_PCT","CEILING_3X_MEDIAN_PCT","CEILING_MARGIN_X","POSTING_DATE",
          "PRE_EXECUTE_COMMIT"]
fails = []
if not A.exists(): fails.append("amendment file missing")
if not B.exists(): fails.append("collinearity note missing")
if fails: print("FAIL: " + "; ".join(fails)); sys.exit(1)
a, b = A.read_text(), B.read_text()
# vacuity floors
if len(a) < 6000: fails.append("amendment too short (%d B)" % len(a))
if len(b) < 1200: fails.append("note too short (%d B)" % len(b))
# markers + block floor
if a.count("--- PASTE INTO OSF FROM HERE ---") != 1: fails.append("opener count != 1")
if a.count("--- PASTE ENDS HERE ---") != 1: fails.append("closer count != 1")
i, j = a.find("--- PASTE INTO OSF FROM HERE ---"), a.find("--- PASTE ENDS HERE ---")
if not (0 <= i < j): fails.append("markers missing or out of order")
else:
    blk = a[i:j]
    if len(blk) < 3000: fails.append("paste block too short (%d B)" % len(blk))
    if blk.count("(site basis)") < 3: fails.append("<3 '(site basis)' labels in paste block")
    if blk.count("(row basis)") < 3: fails.append("<3 '(row basis)' labels in paste block")
# roster: every slot present as a sentinel; ledger has exactly 13 lines
for s in ROSTER:
    if ("{{%s}}" % s) not in a: fails.append("slot missing: %s" % s)
led = re.findall(r"(?m)^  ([A-Z0-9_]+) = ", a)
if sorted(led) != sorted(ROSTER): fails.append("SLOT_LEDGER lines != roster (got %d)" % len(led))
# no measured-from-PASTE-3 number smuggled in as a literal placeholder
for bad in ("TBD", "TODO", "XXX", "<fill", "FIXME"):
    if bad in a: fails.append("placeholder-of-another-kind present: %s" % bad)
# load-bearing content
for s in ['"Region 1 alone contains 7 distinct overlapping deletions',
          "7,951", "0.1323%", "0.1888%", "0.3527%", "0.0393%", "21/21", "231",
          "factor of 38", "BRANCH_AFR_OCC_NONE", "BRANCH_AFR_OCC_EXCLUDED",
          "BRANCH_AFR_OCC_DEFERRED", "NOT-YET-APPENDED", "trsx5", "pvb5j"]:
    if s not in a: fails.append("amendment missing: %s" % s)
for s in ["same-position", "SuSiE", "not a defect", "RETAINED"]:
    if s.lower() not in b.lower(): fails.append("note missing: %s" % s)
# Seth §8 carried verbatim (lines 102-107), per-line containment + count identity
src = pathlib.Path(".planning/debug/260819-SETH-VERDICT-adjudication-confirmed-as-received.md")
lines = [l.strip() for l in src.read_text().splitlines()[101:107] if l.strip()]
hit = sum(1 for l in lines if l in a)
if hit != len(lines): fails.append("SETH §8 verbatim: %d/%d lines matched" % (hit, len(lines)))
# forbidden edits
diff = subprocess.run(["git","diff","--name-only","HEAD~1","HEAD"],capture_output=True,text=True).stdout.split()
for f in diff:
    if f.startswith(("tests/","src/","config/")) or f in (
        "Snakefile",
        ".planning/osf_deviations.md",
        ".planning/amendments/osf_deviations.md",
        ".planning/amendments/osf-amendment-afr-occlusion-exclude-UPDATE-2026-07-10.md"):
        fails.append("FORBIDDEN file modified: %s" % f)
print("FAIL: " + "; ".join(fails) if fails else "TASK 2 VERIFY: PASS")
sys.exit(1 if fails else 0)
PY
    </automated>
  </verify>
  <done>
The amendment file exists with exactly one PASTE opener and one closer in order, a paste block
≥3,000 B carrying all seven required sections (factual correction, corrected empirical claim,
evidence, recalibrated clause (d) with purpose/metric/ceiling/derivation/no-calibrate-to-pass/
limitation, §8 verbatim, methods provenance, enumerated unchanged-list), all 13 roster slots
present as sentinels, a 13-line SLOT_LEDGER, ≥3 `(site basis)` and ≥3 `(row basis)` labels, and
the prepared osf_deviations entry marked NOT-YET-APPENDED outside the markers. Seth's §8 matches
line-for-line by count identity. The §6 note exists as its own file. No forbidden file
(`tests/`, `src/`, `config/`, `Snakefile`, either `osf_deviations.md`, the July amendment) appears
in the commit's diff. One commit, explicit paths.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build the placeholder guard and prove it red before trusting it green</name>
  <files>.planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh, .planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-guard-controls-transcript.txt</files>
  <action>
Build the enforcer specified in `<interfaces>` (GUARD CONTRACT). Bash with `#!/usr/bin/env bash`,
`set -uo pipefail`, sections `draft | paste-ready | arith | quote | all`, amendment path as `$2`,
exit 0 = all checks in the section passed, exit 1 = any failed. Print one `PASS:` / `FAIL:` line
per check so a red is legible without re-reading the script. Implement `arith` and `quote` as
embedded `python3 - <<'PY'` blocks inside the same file — do not add a second script.

Header comment must state, in this order:
1. What it enforces and why it exists: the amendment draft must not reach OSF uninstantiated;
   an obligation with no failing check lapses silently.
2. That `paste-ready` and `arith` are EXPECTED RED in draft state, and that a red there is the
   guard working, not a defect.
3. That its green is evidence ONLY because the controls were seen red — pointing at the
   transcript file by name.
4. ⛔ FORBIDDEN REPAIR: never satisfy a check by deleting a sentinel, loosening the anchored
   filled-value patterns, widening the arithmetic tolerance, or trimming the §8 quote. If a
   check is wrong, fix the check and re-run its control.

VACUITY FIRST, in every section: a missing file, a file under the byte floor, missing or
out-of-order markers, or a paste block under its floor is a FAIL — checked BEFORE any content
assertion. An empty block satisfies every content assertion trivially; that silent-vacuity class
is exactly what this guard exists to catch.

Then OBSERVE both behaviours and transcribe them verbatim into
`260819-u8d-guard-controls-transcript.txt`. Each entry: the exact command, the exact output,
the exit code, and a one-line statement of what it proves.

SEEN RED (all against the real draft or a deliberately-broken copy):
- `R1` — `paste-ready` against the real uninstantiated draft → FAIL (sentinels present).
  **This is the headline red: it is the state the guard exists for.**
- `R2` — `arith` against the real uninstantiated draft → FAIL loudly with "cannot verify — draft
  not instantiated", NOT a skip. (A guard that skips below a threshold hides the bug.)
- `R3` — vacuity: `draft` and `paste-ready` against an EMPTY file in the scratchpad → FAIL.
  Proves the guard cannot go green on a deleted/renamed amendment.
- `R4` — marker vacuity: a scratchpad copy with the paste block gutted to a few bytes
  (markers intact) → `draft` FAIL. Proves an emptied block is not silently green.
- `R5` — deleted-not-filled: an instantiated scratchpad copy with ONE sentinel deleted rather
  than filled (its ledger line left as `  SITE_MAX_PCT = `) → `paste-ready` FAIL on the anchored
  pattern. Proves deletion is not a valid instantiation.
- `R6` — arithmetic: an instantiated scratchpad copy with `CEILING_3X_MEDIAN_PCT` perturbed off
  `3 × SITE_MEDIAN_PCT` by more than tolerance → `arith` FAIL naming the broken identity.
- `R7` — quote: a scratchpad copy with one word changed inside the §8 blockquote → `quote` FAIL
  reporting a count identity miss.
- `R8` — filename: rename the instantiated scratchpad copy so its basename still contains `XX`
  → `paste-ready` FAIL. Proves the filename placeholder is gated too.

SEEN GREEN:
- `G1` — `draft` against the real draft → PASS (this is the draft-state check).
- `G2` — `quote` against the real draft → PASS.
- `G3` — `paste-ready` and `arith` against a fully test-instantiated scratchpad copy → PASS.

TEST-INSTANTIATED COPY — rules, and they matter:
- It lives ONLY in the scratchpad dir
  (`/gpfs_common/share01/clintonlab/ckclinto/tmp/claude-410819/-gpfs-common-share01-clintonlab-ckclinto-coloc-analysis/19c6a323-a3ca-4c18-bffc-bfcb052f7fe3/scratchpad`).
  **It is NEVER written into `.planning/` and NEVER committed.** A file full of plausible-looking
  fake numbers sitting beside the real draft is a real hazard.
- Its first line is a shouting banner: `⛔ FAKE NUMBERS — GUARD CONTROL ONLY — NEVER POST`.
- The fake value set is arithmetically self-consistent so `arith` can pass honestly:
  `SITE_MIN_PCT=0.1000%`, `SITE_MEDIAN_PCT=0.1300%`, `SITE_MAX_PCT=0.2500%`,
  `SITE_ROBUST_SIGMA_PCT=0.0300%`, `MED_PLUS_3SIG_PCT=0.2200%`, `MED_PLUS_4SIG_PCT=0.2500%`,
  `TWO_X_MEDIAN_PCT=0.2600%`, `TWO_X_MAX_PCT=0.5000%`, `CEILING_3X_MEDIAN_PCT=0.3900%`,
  `CEILING_MARGIN_X=1.56x`, `MEAN_ROW_SITE_INFLATION=1.45x`, `POSTING_DATE=2026-08-21`,
  `PRE_EXECUTE_COMMIT=0000000`. (`0.3900/0.2500 = 1.56`; every identity holds.)
- The copy's basename must NOT contain `XX` for `G3` (that is the point of `R8`).

Finally, add one line to the amendment file's drafting notes (the only edit to that file in this
task) recording the guard path and the exact `all` invocation, so a future session finds the
enforcer from the artifact rather than from memory. Then write
`260819-u8d-SUMMARY.md` per the GSD summary template, recording every red and green observation
explicitly — the SUMMARY must state that `paste-ready` and `arith` were observed RED against the
real draft and GREEN against the instantiated control.

Commit, explicit paths, one commit. GPFS contingency as in Task 1.
`docs(quick-260819-u8d): add the placeholder guard (draft/paste-ready/arith/quote), seen red 8 ways and green 3, + task summary`
  </action>
  <verify>
    <automated>
cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && bash -c '
Q=.planning/quick/260819-u8d-record-occlusion-recalibration-adoption-
G=$Q/260819-u8d-placeholder-guard.sh
A=.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-XX.md
T=$Q/260819-u8d-guard-controls-transcript.txt
fail=0
[ -f "$G" ] || { echo "FAIL: guard missing"; exit 1; }
[ -f "$T" ] || { echo "FAIL: transcript missing"; exit 1; }
[ "$(wc -l < "$G")" -ge 90 ] || { echo "FAIL: guard under 90 lines"; fail=1; }
[ "$(wc -l < "$T")" -ge 40 ] || { echo "FAIL: transcript under 40 lines"; fail=1; }
grep -qF "SEEN RED" "$T" || { echo "FAIL: transcript has no SEEN RED section"; fail=1; }
grep -qF "SEEN GREEN" "$T" || { echo "FAIL: transcript has no SEEN GREEN section"; fail=1; }
for r in R1 R2 R3 R4 R5 R6 R7 R8 G1 G2 G3; do
  grep -qF "$r" "$T" || { echo "FAIL: control $r not transcribed"; fail=1; }
done
# the guard is RED on the uninstantiated draft — the whole point
bash "$G" paste-ready "$A" >/dev/null 2>&1 && { echo "FAIL: paste-ready GREEN on an uninstantiated draft"; fail=1; } || echo "PASS: paste-ready RED on the draft (expected)"
bash "$G" arith       "$A" >/dev/null 2>&1 && { echo "FAIL: arith GREEN on an uninstantiated draft"; fail=1; } || echo "PASS: arith RED on the draft (expected)"
# vacuity: an empty file must not be green
E=$(mktemp); : > "$E"
bash "$G" draft       "$E" >/dev/null 2>&1 && { echo "FAIL: draft GREEN on an EMPTY file"; fail=1; } || echo "PASS: draft RED on an empty file"
bash "$G" paste-ready "$E" >/dev/null 2>&1 && { echo "FAIL: paste-ready GREEN on an EMPTY file"; fail=1; } || echo "PASS: paste-ready RED on an empty file"
rm -f "$E"
# the sections that must be green now
bash "$G" draft "$A" || { echo "FAIL: draft section RED on the real draft"; fail=1; }
bash "$G" quote "$A" || { echo "FAIL: quote section RED on the real draft"; fail=1; }
# no fake-number control copy leaked into the repo
git ls-files ".planning" | xargs -r grep -lF "FAKE NUMBERS" 2>/dev/null | grep -v "260819-u8d-guard-controls-transcript.txt\|260819-u8d-PLAN.md\|260819-u8d-SUMMARY.md" && { echo "FAIL: a fake-number copy is tracked in .planning"; fail=1; }
[ "$fail" -eq 0 ] && echo "TASK 3 VERIFY: PASS"
exit $fail'
    </automated>
  </verify>
  <done>
`260819-u8d-placeholder-guard.sh` exists (≥90 lines) with all five sections and a vacuity floor
ahead of every content check. Against the real uninstantiated draft: `draft` and `quote` exit 0;
`paste-ready` and `arith` exit 1. Against an empty file every section exits 1. All eight red
controls (R1-R8) and three green controls (G1-G3) are transcribed verbatim with commands, output
and exit codes in `260819-u8d-guard-controls-transcript.txt`. No file containing the fake control
values is tracked under `.planning/` except the transcript itself. The amendment's drafting notes
name the guard and its `all` invocation. `260819-u8d-SUMMARY.md` records the red-then-green
sequence explicitly. One commit, explicit paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo draft → public OSF record | A body pasted from this repo becomes a permanent, append-only public pre-registration artifact. Anything wrong crossing here cannot be retracted, only superseded. |
| chat-relayed measurement → amendment body | PENDING PASTE #3 numbers arrive as prose from a VM terminal via a human relay; no byte anchors. |
| this task's drafts → the fire path | The shipped clause-(d) constant governs a $385-1,084 irreversible spend. A draft that reads as authoritative could be mistaken for an adopted change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-u8d-01 | Tampering | amendment body pasted to OSF with `{{SLOT}}` sentinels or an `XX` filename still present | mitigate | `paste-ready` section: zero `{{`/`}}` anywhere, all 13 ledger lines matching anchored filled-value patterns, basename free of `XX`; observed RED against the current draft (control R1/R8) |
| T-u8d-02 | Tampering | a slot "instantiated" by deleting the sentinel rather than filling it | mitigate | the SLOT_LEDGER makes every slot positively checkable; a blank value matches no anchored pattern (control R5) |
| T-u8d-03 | Information disclosure | the §8 provenance quietly softened or trimmed in transit — the confession is the amendment's ethical core | mitigate | `quote` section: per-line containment against verdict lines 102-107 plus a count identity, not a threshold (control R7) |
| T-u8d-04 | Tampering | derived ceiling numbers hand-computed wrong (the project has shipped a wrong count three times) | mitigate | `arith` section verifies six must-be-identity relations rather than matching a count; tolerance derived from the 4-dp print precision and stated (control R6) |
| T-u8d-05 | Spoofing | the fake-number control copy mistaken for the real instantiated draft | mitigate | control copy lives only in the scratchpad, never in `.planning/`, never committed, first line is a shouting FAKE banner; verify greps the tracked tree for leaks |
| T-u8d-06 | Repudiation | HANDOFF gate history overwritten, erasing the dated state of knowledge | mitigate | append-only string extension; verify asserts prefix-preservation on both gate fields and deep-equality of every other key against a pre-edit copy |
| T-u8d-07 | Elevation of privilege | the draft read as authorization to change `_OCCLUSION_ANOMALY_FRACTION` in code, or to contact OSF | mitigate | DRAFT-ONLY banner at the top of the amendment; the HANDOFF producer-gate clause states the constant is not editable until the amendment is POSTED; no `src/`, `tests/`, `config/`, `Snakefile` path is in `files_modified` and the Task 2 verify fails if one appears in the diff |
| T-u8d-08 | Denial of service | GPFS loose-object loss aborts a commit mid-task, leaving records split across states | accept | known ~3-hits-in-3-weeks failure mode with a proven recovery recipe (`git hash-object -w` loop, retry once), stated in every task action; each task is a single atomic commit so a failure leaves a clean prior state |
| T-u8d-09 | Tampering | a guard check silently made vacuous by a renamed heading or an emptied paste block | mitigate | vacuity floors run FIRST in every section (file bytes, marker count and order, paste-block bytes); controls R3/R4 prove they fire |
</threat_model>

<verification>
Run after all three tasks:

```bash
cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis
Q=.planning/quick/260819-u8d-record-occlusion-recalibration-adoption-
A=.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-XX.md

# 1. the guard's own verdict on the real draft: draft+quote green, paste-ready+arith red
bash $Q/260819-u8d-placeholder-guard.sh draft       $A; echo "draft       exit=$?  (expect 0)"
bash $Q/260819-u8d-placeholder-guard.sh quote       $A; echo "quote       exit=$?  (expect 0)"
bash $Q/260819-u8d-placeholder-guard.sh paste-ready $A; echo "paste-ready exit=$?  (expect 1)"
bash $Q/260819-u8d-placeholder-guard.sh arith       $A; echo "arith       exit=$?  (expect 1)"

# 2. HANDOFF still parses and still round-trips byte-exactly
python3 -c "import json,pathlib;r=pathlib.Path('.planning/HANDOFF.json').read_text();d=json.loads(r);print('roundtrip', json.dumps(d,indent=2,ensure_ascii=False)==r, 'no_trailing_nl', not r.endswith(chr(10)))"

# 3. nothing forbidden moved across the three commits
git diff --name-only HEAD~3 HEAD

# 4. the deviation log is untouched (both copies)
git diff --quiet HEAD~3 HEAD -- .planning/osf_deviations.md .planning/amendments/osf_deviations.md && echo "osf_deviations: UNTOUCHED (expected)"

# 5. the posted July amendment is untouched
git diff --quiet HEAD~3 HEAD -- .planning/amendments/osf-amendment-afr-occlusion-exclude-UPDATE-2026-07-10.md && echo "July amendment: UNTOUCHED (expected)"
```

Expected: `git diff --name-only HEAD~3 HEAD` lists exactly the seven files in `files_modified`
and nothing else.
</verification>

<success_criteria>
- `DEC-2026-08-19-occlusion-recalibration-adopted` is in `.planning/DECISIONS.md` with Carter's
  verbatim `"adopt"`, all five adopted elements, the measured literals, the §8 provenance summary,
  the `:45` false-sentence record, the enumerated unchanged-list, and cross-refs.
- `.planning/HANDOFF.json` round-trips byte-exactly, has no trailing newline, and differs from its
  pre-edit state in exactly two gate strings, each a strict prefix-extension.
- The amendment package exists, is paste-ready in structure and uninstantiated in content, and
  carries Seth's §8 verbatim by count identity.
- Every PENDING-PASTE-#3 quantity is a named slot; the 13-slot roster and the SLOT_LEDGER agree.
- The §6 collinearity caveat is its own note file.
- The prepared `osf_deviations.md` entry text exists marked NOT-YET-APPENDED; neither
  `osf_deviations.md` file is modified.
- The guard fails on the current draft and passes on a test-instantiated copy — **both observed**,
  with all eight red and three green controls transcribed verbatim.
- No OSF contact. No edits to `tests/`, `src/`, `config/`, `Snakefile`, the fire verifier, the ox1
  runbooks, the posted July amendment, or `STATE.md`.
- Three atomic commits, `docs(quick-260819-u8d): …`, explicit paths only.
</success_criteria>

<output>
After completion, create
`.planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-SUMMARY.md`
per the GSD summary template. It must record, explicitly:
- the red-then-green guard sequence (which sections were red against the real draft, which were
  green against the instantiated control, and that both were observed rather than assumed);
- the 13-slot roster, so the next session instantiates from the summary without re-deriving it;
- that PENDING PASTE #3 is the single remaining input before Seth's brief-blind review, and that
  `paste-ready` + `arith` must both be GREEN before any OSF paste;
- that `.planning/osf_deviations.md` is deliberately NOT appended, and what event discharges that.
</output>
