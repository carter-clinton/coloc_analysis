---
phase: quick-260629-2im
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/python/plink_ld_to_npz.py
  - tests/m3/test_plink_ld_to_npz.py
autonomous: true
requirements: [m3-02e-T4-OOM]
tags: [ld, oom, plink, npz, m3-02e]

must_haves:
  truths:
    - "read_square_bin verifies square-LD symmetry without materializing a full n_var**2 temporary (no OOM at n_var~102,421 on a 64-128 GB VM)"
    - "The symmetry invariant is still ENFORCED — an asymmetric .ld.bin still raises ValueError (the check is bounded, not skipped)"
    - "The blocked check returns the SAME accept/reject verdict as np.allclose(m, m.T) for symmetric and asymmetric inputs across block-boundary edge cases"
    - "The new tests FAIL on the pre-fix code path (failing-first) and PASS after the fix"
    - "The full tests/m3 suite stays green (287+ baseline)"
  artifacts:
    - path: "src/python/plink_ld_to_npz.py"
      provides: "_is_symmetric_blocked module-level helper + read_square_bin using it"
      contains: "def _is_symmetric_blocked"
    - path: "tests/m3/test_plink_ld_to_npz.py"
      provides: "Failing-first regression for the blocked symmetry check + preserved-invariant test"
      contains: "_is_symmetric_blocked"
  key_links:
    - from: "src/python/plink_ld_to_npz.py:read_square_bin"
      to: "_is_symmetric_blocked"
      via: "if not _is_symmetric_blocked(m, atol=1e-4): raise ValueError(...)"
      pattern: "_is_symmetric_blocked\\(m, atol="
---

<objective>
Fix the OOM kill in the native-plink LD .npz convert step. In `read_square_bin()`,
`np.allclose(m, m.T, atol=1e-4)` materializes several full n_var**2 float32 temporaries
(~39 GiB each at n_var=102,421) ON TOP OF the 39 GiB matrix `m`, blowing past 64 GiB ->
kernel OOM-kill. region 1 banked 0/276.

Replace the full-matrix symmetry check with a memory-bounded, block-wise check that bounds
the transient to ~block*n_var*4 bytes (~420 MB at block=1024) while enforcing the IDENTICAL
invariant. The fix is a reusable helper + a failing-first regression
(`[[feedback_extract_reusable_utilities]]` — this is the third format/memory bug in this loop
after the triangle flag and chr-prefix).

Purpose: Unblock the 276-region AFR native-plink LD panel re-fire (m3-02e-T4).
Output: One behavior file changed (plink_ld_to_npz.py) + a regression in test_plink_ld_to_npz.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# The approved design (source of truth — exact helper body, exact replacement line, exact tests):
@/home/ckclinto/.claude/plans/here-is-the-latest-zazzy-whistle.md

# The file to fix (read_square_bin at lines 136-161; line 156 is the OOM):
@src/python/plink_ld_to_npz.py

# The test file (follow the _symmetric_corr / .astype("<f4").tofile(...) pattern):
@tests/m3/test_plink_ld_to_npz.py

<interfaces>
<!-- Contracts the executor uses directly — no exploration needed. -->

The OOM line to replace (plink_ld_to_npz.py:156, inside read_square_bin):
```python
    if not np.allclose(m, m.T, atol=1e-4):
        raise ValueError(
            f"square LD is not symmetric for {ld_bin_path} "
            f"(plink --r square is symmetric; the PILOT measured sym_check=0.0)."
        )
```
KEEP the ValueError message verbatim. Only the boolean predicate changes.

Existing test helper to reuse (test_plink_ld_to_npz.py:38-44):
```python
def _symmetric_corr(n: int, seed: int = 0) -> np.ndarray:
    """A symmetric float32 correlation-like matrix with unit diagonal."""
    rng = np.random.default_rng(seed)
    a = rng.standard_normal((n, n)).astype("float32")
    m = (a + a.T) / 2.0
    np.fill_diagonal(m, 1.0)
    return m.astype("float32")
```
The square round-trip pattern (test_plink_square_bin_to_npz): write the matrix with
`m.astype("<f4").tofile(ld_bin)`, a 6-col .bim with `_write_bim(bim, _default_bim_rows(n))`,
then `pln.plink_ld_to_npz(mode="square", ld_path=..., bim_path=..., n_var=n, ...)`.
The module is imported as `import plink_ld_to_npz as pln`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing-first blocked-symmetry regression, then the memory-bounded fix</name>
  <files>tests/m3/test_plink_ld_to_npz.py, src/python/plink_ld_to_npz.py</files>
  <behavior>
    RED (write these tests first; they fail against the current code because
    `_is_symmetric_blocked` does not yet exist):

    - test_is_symmetric_blocked_accepts_and_rejects:
        * `pln._is_symmetric_blocked(_symmetric_corr(n), atol=1e-4)` is True for n in a few sizes.
        * Take a `_symmetric_corr(n)` matrix, perturb ONE off-diagonal beyond atol
          (e.g. `m[0, n-1] += 1.0`, leave `m[n-1, 0]` unchanged) -> returns False.

    - test_blocked_check_matches_allclose:
        * For several n INCLUDING n not a multiple of the block (e.g. n=5, 17, 100) and a
          case where `block > n` (call `pln._is_symmetric_blocked(m, atol=1e-4, block=4096)`
          on a small n), assert `_is_symmetric_blocked(m, atol=1e-4)` ==
          `np.allclose(m, m.T, atol=1e-4)` for BOTH a symmetric `_symmetric_corr(n)` and an
          asymmetric variant (one perturbed off-diagonal). Verdicts must agree in every case.

    - test_read_square_bin_rejects_asymmetric:
        * Build an asymmetric matrix with a unit diagonal (so the diagonal check passes and we
          reach the symmetry check): start from `_symmetric_corr(n)`, perturb one off-diagonal
          beyond atol on one side only, re-set `np.fill_diagonal(m, 1.0)`.
        * `m.astype("<f4").tofile(ld_bin)`; assert `pytest.raises(ValueError)` from
          `pln.read_square_bin(ld_bin, n)` — proves the invariant is PRESERVED (bounded, not skipped).

    GREEN: implement `_is_symmetric_blocked` and wire it into read_square_bin so all the new
    tests pass and the existing square round-trip tests stay green.
  </behavior>
  <action>
    1. In `tests/m3/test_plink_ld_to_npz.py`, add the three tests above in the "Square mode"
       section (after `test_plink_square_bin_to_npz`). Reuse the existing module-level
       `_symmetric_corr`, `_write_bim`, `_default_bim_rows` helpers and the
       `m.astype("<f4").tofile(...)` write idiom. Reference `pln._is_symmetric_blocked` and
       `pln.read_square_bin` directly. For the asymmetric-via-one-side perturbation, perturb
       a single off-diagonal cell on ONE side only (do NOT mirror it) so `m != m.T`.

    2. In `src/python/plink_ld_to_npz.py`, add a module-level helper in the "LD readers"
       section (just above `read_square_bin`, after the section banner near line 134), EXACTLY
       as specified in the approved design — copy the body verbatim from
       `/home/ckclinto/.claude/plans/here-is-the-latest-zazzy-whistle.md` lines 44-57:

       ```python
       def _is_symmetric_blocked(m: np.ndarray, atol: float, block: int = 1024) -> bool:
           """Memory-lean symmetry check. ``np.allclose(m, m.T)`` builds several full
           n_var**2 float32 temporaries (~39 GiB each at n_var≈1e5) on top of the matrix
           and OOM-kills a 64 GB VM (m3-02e-T4, region 1). Compare ``block`` rows at a
           time against the matching transposed column block so the transient is bounded
           by ``block * n_var * 4`` bytes (≈420 MB at block=1024, n_var≈1e5)."""
           n = m.shape[0]
           for i in range(0, n, block):
               a = m[i:i + block, :]          # row block (view, b×n)
               b = m[:, i:i + block].T        # transposed col block (b×n)
               if not np.allclose(a, b, atol=atol):
                   return False
           return True
       ```

    3. In `read_square_bin()`, replace the line-156 predicate
       `if not np.allclose(m, m.T, atol=1e-4):` with
       `if not _is_symmetric_blocked(m, atol=1e-4):`. Keep the ValueError message body
       verbatim. Leave the diagonal check (line 151) and the
       `arr.reshape(n_var, n_var).astype("float32", copy=False)` no-copy view (line 150)
       UNCHANGED.

    Stage explicit paths only (never `git add -A` / `-A` on the GPFS tree —
    `[[feedback_multi_terminal_staging]]`): `src/python/plink_ld_to_npz.py` and
    `tests/m3/test_plink_ld_to_npz.py`. Commit atomically.
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && python -m pytest tests/m3/test_plink_ld_to_npz.py -v</automated>
  </verify>
  <done>
    - `_is_symmetric_blocked` exists at module level in plink_ld_to_npz.py with the
      OOM-motivation docstring; `read_square_bin` calls it in place of `np.allclose(m, m.T, ...)`;
      the ValueError message is unchanged.
    - The three new tests PASS; they FAIL on a stash/revert of the source fix (failing-first proven).
    - `python -m pytest tests/m3/test_plink_ld_to_npz.py -v` is fully green, including the
      existing `test_plink_square_bin_to_npz`, `test_lower_triangular_flag_correct_per_mode`,
      `test_npz_keys_match_save_npz_contract`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm full m3 suite stays green (287+ baseline)</name>
  <files>(no edits — full-suite regression gate)</files>
  <action>
    Run the full m3 suite to confirm no collateral regression. Use the m3-r-ld conda env so
    the R/loader tests RUN rather than skip (`[[reference_m3_r_ld_conda_env]]`) — the plink
    tests are pure-Python and pass anywhere, but the full-suite baseline (287+) includes the
    R/loader tests that the env marker enables. The user has /gsd-* commands; do not print
    conda-activate instructions — just invoke the env's pytest.

    If any failure surfaces that is attributable to this change, return to Task 1 and fix;
    pre-existing skips/failures unrelated to read_square_bin are out of scope (note them in
    the SUMMARY, do not fix here).
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && python -m pytest tests/m3 -q</automated>
  </verify>
  <done>
    - `python -m pytest tests/m3 -q` reports no NEW failures vs the 287+ baseline; the
      plink_ld_to_npz tests (incl. the 3 new) are green. Any pre-existing/env-gated skips are
      noted, not introduced by this change.
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/m3/test_plink_ld_to_npz.py -v` — all green, incl. 3 new tests.
- The 3 new tests fail when the source fix is reverted (failing-first), pass after.
- `python -m pytest tests/m3 -q` — full suite green, 287+ baseline preserved.
- Manual read-back: `read_square_bin` no longer references `np.allclose(m, m.T...)`; it calls
  `_is_symmetric_blocked(m, atol=1e-4)`; the ValueError message is byte-identical to the original.
</verification>

<success_criteria>
- read_square_bin enforces square-LD symmetry block-wise (peak transient ~420 MB, not ~3x39 GiB).
- The invariant is preserved (asymmetric .ld.bin still raises ValueError), not skipped.
- New failing-first regression added following the established test pattern.
- Full tests/m3 suite green (287+ baseline).
- Both files staged by explicit path and committed atomically.
</success_criteria>

<output>
After completion, create `.planning/quick/260629-2im-fix-native-plink-ld-npz-convert-oom-via-/260629-2im-SUMMARY.md`.

The SUMMARY MUST record (the orchestrator owns STATE.md / HANDOFF.json — the executor only
notes these facts for the handoff):
- ROOT CAUSE: the OOM was the full-matrix symmetry check (`np.allclose(m, m.T)` building
  multiple n_var**2 float32 temporaries on top of `m`), NOT `np.savez_compressed` (numpy
  writes the array to the zip in chunks); the np.diag diagonal check was always tiny/fine.
- THE FIX: `_is_symmetric_blocked` block-wise check (bounded ~block*n_var*4 transient);
  invariant preserved (asymmetric still raises); commit SHA.
- RE-FIRE PROCEDURE (for the AoU agent): agent `git pull` the fix commit -> AoU UI respec the
  VM to n2-highmem-16 (128 GB) via stop->edit machine type->start (reattachable PD + 379 GB
  bfile survive untouched) -> pre-flight re-confirm region-1 chr1 window ~102,421 -> re-fire
  the loop from region 1 (0 banked -> clean restart; byte-floor resume guard skips nothing) ->
  PROOF POINT: region 1 completes -> .npz count 0->1 in the afr_native_panel bucket, `df` flat
  (.ld.bin reclaimed), panel row status==ok, n_var~102,421, peak RSS well under 128 GB ->
  liveness = bucket .npz count climbing toward 276; STOP-on-complete (stop != delete) holds.
- PUSH NOTE: push to origin and verify origin tip == local HEAD before re-fire
  (`[[feedback_push_ncsu_before_aou_clone_fire]]`) — the AoU agent pulls the fix.
</output>
