---
phase: quick-260821-x91
plan: 01
type: execute
wave: 1
depends_on: []
subsystem: m3-afr-ld-panel
tags: [occlusion, two-condition-gate, osf, mk7ze, tdd, fire-verifier, runbook, re-baseline, m3-07]

files_modified:
  - src/python/occlusion_gate_constants.py
  - src/python/run_native_ld_panel.py
  - src/python/fire_verifier.py
  - tests/m3/test_occlusion_gate_constants.py
  - tests/m3/test_run_native_ld_panel.py
  - tests/m3/test_occlusion_span_filter.py
  - tests/m3/test_fire_verifier.py
  - .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-AGENT-PROMPT.md
  - .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-BROWSER-PASTE.md
  - .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md
  - .planning/HANDOFF.json
  - .planning/STATE.md
  - .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md
  - .planning/quick/260821-x91-remediation-batch-two-condition-occlusio/260821-x91-PLAN.md
  - .planning/quick/260821-x91-remediation-batch-two-condition-occlusio/260821-x91-SUMMARY.md

autonomous: true

requirements:
  - DEC-2026-08-22-occlusion-recalibration-posted
  - OCC-GATE-CONSTANTS-PINNED
  - OCC-GATE-TWO-CONDITION-PRODUCER
  - OCC-GATE-SIDECAR-EVIDENCE
  - OCC-ORACLE-RE-DERIVATION
  - OCC-FIRE-VERIFIER-TWO-CONDITION
  - OCC-RUNBOOK-231-TRUTH
  - OCC-SUITE-REBASELINE

user_setup: []

must_haves:
  truths:
    - "The two posted numbers exist in exactly ONE place in the code and are pinned to the PUBLIC record: `src/python/occlusion_gate_constants.py` defines `OCCLUSION_SITE_FRACTION_CEILING = 0.005056` and `OCCLUSION_INFLATION_CEILING = 3.42`, and `tests/m3/test_occlusion_gate_constants.py` passes by parsing the amendment's SLOT_LEDGER lines and comparing RENDERED STRINGS (`f\"{OCCLUSION_SITE_FRACTION_CEILING*100:.4f}%\" == \"0.5056%\"`, `f\"{OCCLUSION_INFLATION_CEILING:.2f}x\" == \"3.42x\"`) — a must-be-identity transform, never a float tolerance."
    - "That enforcer is evidence and not decoration because it was seen to FAIL: a perturbed value (0.005057) renders to a string that is NOT `0.5056%`, and a one-byte flip inside a tmp_path copy of the amendment's paste block moves the extracted md5 off `13a49f543cabcc27ce9f1e589783c060` — both observed, both pasted in the SUMMARY, neither performed in-tree."
    - "The false premise is GONE from the code, not merely superseded: `grep -rn '_OCCLUSION_ANOMALY_FRACTION' src/ tests/` returns 0 hits, and the comment-stripped source of `run_native_ld_panel.py` contains no `0.0005` while the comment-stripped source of `fire_verifier.py` contains none of `0.0005` / `0.005056` / `0.5056` / `3.42` — all four enforced by a test. AND the false-premise PROSE is gone with it: `grep -c '5 occluded' src/python/fire_verifier.py` == 0 and `grep -c 'known answer (5' src/python/fire_verifier.py` == 0 — the `check_manifest_rows` docstring at `:490` (\"Region 1 is the ONLY region with a known answer (5 occluded)\") and the `check_region1_status` detail at `:595` (\"5 occluded, ~51 ceiling, 10x headroom\") both assert the false premise TODAY, and the banned-literals test bans FLOATS only, so it would never have caught either."
    - "No commit in this batch leaves `tests/m3` red. The `fire_verifier.py` accessor + `check_occlusion_gate` rewrite is MERGED INTO T2 rather than deferred to T3, because `_default_occlusion_fraction()` reads `rnlp._OCCLUSION_ANOMALY_FRACTION` at CALL time: deleting that global in T2 while leaving the verifier to T3 would leave `test_occlusion_fraction_is_the_shipped_module_global` (ERROR) plus `test_occlusion_ceiling_green_region1` / `test_occlusion_ceiling_boundary_is_strictly_greater` / `test_summarize_all_pass_exit_zero` (FAIL) RED between the two commits — a transient red is exactly the state in which a later 'green' carries no information. T3 keeps the oracle re-derivation and the `check_manifest_rows` / `_stage_a` expected_records derivation, both of which are independently green-at-every-commit."
    - "The shipped gate is the POSTED gate, both routes: `run_native_ld_panel.process_region` defers on site fraction STRICTLY above 0.005056 OR occluded-site row/site inflation STRICTLY above 3.42, both routes emit the SAME `deferred_occlusion_anomaly:` status prefix (so `fire_verifier._DEFERRAL_PREFIXES` and the ast status-vocabulary enforcer stay green with no fourth branch), and tests prove: site-fraction-only fire, inflation-only fire (site fraction under), both-under proceeds to exclude-in-lockstep, and equality on EACH condition proceeds (strict `>`)."
    - "Accounting did not move to sites: `_PANEL_COLUMNS` is byte-unchanged (9 columns), `n_dropped_occluded` is still a ROW count, and the deferred row still carries `n_dropped_occluded is None` / `n_var is None` with the detail in the status string."
    - "Every square-path region banks machine-readable gate evidence: `{out_prefix}.occlusion_gate.json` is written on ALL three square outcomes (zero-occlusion, deferral, ok) with keys `region_id, n_rows, n_sites, occ_rows, occ_sites, site_fraction, inflation, site_fraction_ceiling, inflation_ceiling, fired, verdict`; a test recomputes every one of those numbers INDEPENDENTLY from the fixture `.bim` and asserts equality; and `_reclaim_region_scratch` keeps it."
    - "The deferral route ships its own evidence, and that is a deliberate contract CHANGE seen red-then-green: on a deferred region in `gs://` mode the ONLY region object crossing to the bucket is `{region_id}.occlusion_gate.json` (asserted as an exact allow-list, with explicit negatives for `.npz` / `.afreq` / `.occluded.excludelist` / `.occlusion_manifest.tsv`); the `verify_failed` path still crosses NOTHING but the panel row."
    - "The region-1 oracle no longer claims more than was measured: `tests/m3/test_occlusion_span_filter.py` asserts index CONTAINMENT (`_REGION1_REAL_WINDOW_OCCLUDED_ROW_INDICES <= got_row_indices`) and span-multiset CONTAINMENT (`Counter([60,29,7,31,31,17,29]) <= Counter(window_spans)`) instead of the two equalities, `_REGION1_REAL_SAME_POSITION_COUNT` is re-scoped to `_REGION1_NAN_PAIRS_SAME_POSITION_COUNT = 0` (or deleted) with the 'among the 6 NaN pairs' scope stated, and the `:154-183` block separates DERIVED from MEASURED."
    - "The measured substrate totals are pinned LOUDLY and separately: a distinct gated test whose NAME and docstring both carry `MEASURED_NOT_DERIVED` pins `n_rows=102421, n_deletion_rows=7951, n_occluded_rows=231, max_span=170, n_sites=96708, occ_sites=196` with provenance (probe 2 2026-08-19; PENDING PASTE #3 2026-08-20; `afr_cohort.bim`; window 10000-13506933; banked at `.planning/debug/260820-site-basis-sweep-results-as-received.md`) and the rule 'on a break: RE-MEASURE AND RECORD, never edit-to-green'; it skips outside the perimeter and the skip delta 32 → 33 is stated and measured."
    - "`fire_verifier` reproduces the SHIPPED comparison rather than a copy of it: `check_occlusion_gate(occ_rows, occ_sites, n_sites, *, site_ceiling=None, inflation_ceiling=None)` exists, `_default_site_fraction_ceiling()` / `_default_inflation_ceiling()` read `rnlp._OCCLUSION_SITE_FRACTION_CEILING` / `rnlp._OCCLUSION_INFLATION_CEILING` at EVALUATION time (proved by a monkeypatch identity test), it fails closed on any `None`, and `check_manifest_rows` no longer has a default `expected_records`."
    - "Stage A stops taking a human's number: `_stage_a` derives `expected_records` from the region's `.occluded.excludelist` LINE COUNT, cross-checks it against the sidecar's `occ_rows` (mismatch → FAIL; sidecar missing → FAIL; excludelist missing → FAIL), reads the sidecar for `check_occlusion_gate`, and `--expected-records` is an optional override that is LOGGED as an override in the check detail."
    - "The runbooks tell the truth region 1 will actually produce: all three `260812-ox1` files carry `status == \"ok\"`, `n_dropped_occluded == 231`, manifest `232 lines (header + 231 records)`, the sidecar EXPECT block (`occ_rows 231, occ_sites 196, n_sites 96708, site_fraction ≈ 0.2027%, inflation ≈ 1.18x, verdict ok`), the two-condition clause-(d) sentence with region 1's 0.2027% / 1.18x against 0.5056% / 3.42x, and the arbiter rule (the sidecar decides; a mismatch STOPS the run for re-measurement, never edit-to-green); `grep -rn 'near 5\\|exactly 5\\|10x under\\|10× under\\|51\\.2\\|0\\.0005' .planning/quick/260812-ox1*/*.md` returns no live expectation."
    - "The frozen §6b adjudication card was not disturbed: `bash .planning/quick/260817-vbu-release-trsx5-gate-bank-resolved-adjudic/260817-vbu-verify.sh all` produces BYTE-IDENTICAL output before and after the runbook edits (captured to two files and `diff`ed, exit 0 both times), and the forbidden phrases `nothing scientific is lost` / `nothing is lost` remain absent from the whole `260812-ox1` directory."
    - "The suite is re-baselined honestly: `tests/m3` reports 0 failed, and the SUMMARY reconciles the new counts COMPONENT-EXACT against the 992 passed / 32 skipped / 0 failed baseline measured at `ef59ca1` — every added test named, every removed/renamed test named, the skip delta explained — with the reconciled numbers written into `.planning/HANDOFF.json` `suite_baselines` (which is STALE today at 914/31 and must be corrected, not appended to); the full `tests/` suite is run once and its counts reported as collateral."
    - "Nothing public moved and nothing fired: at EVERY commit the amendment's marker-exclusive paste block is 22,945 B / md5 `13a49f543cabcc27ce9f1e589783c060`, `git diff --stat ef59ca1 HEAD -- .planning/amendments/` is empty, `git diff --stat ef59ca1 HEAD -- src/python/occlusion_span_filter.py` is empty, and zero OSF / AoU / VM / Dataproc contact occurred."
    - "The branch is published: after the final task `git status -sb` shows no `ahead`, and every commit staged EXPLICIT paths (no `git add .` / `-A` on the shared GPFS tree)."
  artifacts:
    - path: "src/python/occlusion_gate_constants.py"
      provides: "THE ONE PINNED PLACE — the two posted ceilings plus a provenance docstring (mk7ze / https://osf.io/mk7ze / 2026-08-22T02:58:55Z, the project copy, the SLOT_LEDGER names, the 3x-median derivations, and 'a change here requires a NEW posted OSF amendment FIRST')"
      contains: "OCCLUSION_SITE_FRACTION_CEILING"
      min_lines: 30
    - path: "tests/m3/test_occlusion_gate_constants.py"
      provides: "The enforcer: rendered-string identity vs the SLOT_LEDGER, the paste-block size+md5 pin, the banned-literal scans over the producer and the verifier, and the in-test negative controls"
      contains: "13a49f543cabcc27ce9f1e589783c060"
      min_lines: 60
    - path: "src/python/run_native_ld_panel.py"
      provides: "The shipped two-condition gate + the per-region occlusion_gate.json sidecar; `_OCCLUSION_ANOMALY_FRACTION` gone"
      contains: "_OCCLUSION_SITE_FRACTION_CEILING"
    - path: "src/python/fire_verifier.py"
      provides: "check_occlusion_gate + the two evaluation-time ceiling accessors + a defaultless check_manifest_rows + a Stage A that DERIVES expected_records and cross-checks the sidecar"
      contains: "check_occlusion_gate"
    - path: "tests/m3/test_occlusion_span_filter.py"
      provides: "Layer 1 (DERIVED, containment) separated from Layer 2 (MEASURED_NOT_DERIVED substrate totals)"
      contains: "MEASURED_NOT_DERIVED"
    - path: ".planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-AGENT-PROMPT.md"
      provides: "The fire runbook an agent actually reads — 231 / 232 / sidecar EXPECT / two-condition clause (d) / the sidecar-is-arbiter STOP rule / the new stage-a flags"
      contains: "231"
    - path: ".planning/HANDOFF.json"
      provides: "resume_on_reconnect[0] = Stage A from STEP 7 (Carter fires; an agent never fires) + the corrected suite_baselines"
      contains: "occlusion_gate.json"
  key_links:
    - from: ".planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md (SLOT_LEDGER CEILING_3X_MEDIAN_PCT / INFLATION_CEILING_3X_X)"
      to: "src/python/occlusion_gate_constants.py"
      via: "tests/m3/test_occlusion_gate_constants.py parses the ledger lines and compares RENDERED STRINGS — a must-be-identity transform"
      pattern: "CEILING_3X_MEDIAN_PCT = 0.5056%"
    - from: "src/python/occlusion_gate_constants.py"
      to: "src/python/run_native_ld_panel.py (_OCCLUSION_SITE_FRACTION_CEILING / _OCCLUSION_INFLATION_CEILING)"
      via: "module-level import binding, read as MODULE GLOBALS at evaluation time (never default args) so tests can monkeypatch"
      pattern: "from occlusion_gate_constants import"
    - from: "src/python/run_native_ld_panel.py (the shipped comparison)"
      to: "src/python/fire_verifier.py check_occlusion_gate"
      via: "_default_site_fraction_ceiling() / _default_inflation_ceiling() read the producer's module globals at evaluation time; no hand-typed constant survives the banned-literals test"
      pattern: "rnlp._OCCLUSION_SITE_FRACTION_CEILING"
    - from: "{out_prefix}.occlusion_gate.json (producer sidecar)"
      to: "fire_verifier._stage_a"
      via: "--gate-json feeds check_occlusion_gate AND cross-checks occ_rows against the .occluded.excludelist line count that derives expected_records"
      pattern: "occlusion_gate.json"
    - from: "the posted clause (d) two-condition rule"
      to: ".planning/quick/260812-ox1-*/260812-ox1-{AGENT-PROMPT,BROWSER-PASTE,READY-TO-FIRE}.md"
      via: "the clause-(d) figures paragraph rewritten to 0.2027% / 1.18x vs 0.5056% / 3.42x, with 231 rows / 196 sites / 96,708 sites as the region-1 EXPECT"
      pattern: "0.5056"
---

<objective>
Bring every consumer of the false `0.0005` / "5 occlusions" premise into line with the
POSTED recalibration amendment (OSF file `mk7ze` on `az52u`, 2026-08-22T02:58:55Z), as the
TWO-condition gate and nothing less.

Purpose: `DEC-2026-08-22-occlusion-recalibration-posted` authorises the code change ONLY as
the two-condition gate — pre-registration precedes execution, and the executed rule must be
the pre-registered rule, byte for byte in meaning. Shipping the site-fraction ceiling alone
is NOT authorised.

Output: one pinned constants module + its enforcer test; a producer that defers on either
condition and banks a per-region gate sidecar; a re-derived region-1 test oracle that
separates DERIVED from MEASURED; a fire_verifier that reproduces the shipped comparison and
derives its own expected record count; three corrected runbooks; a reconciled suite
baseline; and a pushed branch.

NO FIRE. No VM, no Dataproc, no OSF contact, no `gsutil`. An agent never fires.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/debug/fire-morning-occlusion-oracle-vs-geometry.md
@.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
@.planning/debug/260820-site-basis-sweep-results-as-received.md
@src/python/run_native_ld_panel.py
@src/python/occlusion_span_filter.py
@src/python/fire_verifier.py

<measured_facts>
<!-- Read-only measurements taken at HEAD ef59ca1 during planning. Do not re-derive; DO re-verify. -->

PYTHON (never miniconda base):
  PY=/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python

BASELINE (tests/m3 at ef59ca1): 1024 collected, 992 passed / 32 skipped / 0 failed.
  `.planning/HANDOFF.json` `suite_baselines["tests/m3"]` is STALE — it still reads
  "914 passed / 31 skipped". T5 CORRECTS it (does not append to it).

PASTE BLOCK (the guard, re-run at the START and END of every task):
  A=.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
  awk '/--- PASTE INTO OSF FROM HERE ---/{f=1;next}/--- PASTE ENDS HERE ---/{f=0}f' "$A" \
    | tee >(wc -c) | md5sum
  MUST be 22945 bytes / 13a49f543cabcc27ce9f1e589783c060  (markers at lines 167 and 501).
  SIZE FIRST, THEN HASH — house style (260817-vbu V1).

SLOT_LEDGER lines to parse (amendment, inside the fenced SLOT_LEDGER block, two leading
spaces each):
  "  CEILING_3X_MEDIAN_PCT = 0.5056%"
  "  INFLATION_CEILING_3X_X = 3.42x"
Derivations stated in the amendment: 0.5056% = 3x the 21-region site-basis MEDIAN 0.1685%;
3.42x = 3x the inflation MEDIAN 1.14x (NOT the mean 1.18x — the amendment says so
explicitly).

POSTED CLAUSE (d), verbatim shape (amendment lines 320-324):
      (i)   n_occluded_sites  >  0.5056% x n_sites
      (ii)  n_occluded_rows / n_occluded_sites  >  3.42x
  Accounting stays on ROWS. Three numbers reported per region: occluded-SITE fraction,
  occluded-ROW count, and that region's OWN row/site inflation at occluded sites.

REGION 1 MEASURED (2026-08-19/20, banked verbatim at
.planning/debug/260820-site-basis-sweep-results-as-received.md:35):
  102421 rows / 96708 sites / 231 occluded rows / 196 occluded sites
  row fraction 0.2255% · SITE fraction 0.2027% · inflation 1.18x
  -> under BOTH ceilings -> region 1 must land `ok`.

SHIPPED SURFACE (line numbers at ef59ca1 — re-locate by symbol, they will move):
  src/python/occlusion_span_filter.py  (FROZEN — must end this task with an EMPTY diff):
    _COL_CHR = 0 · _COL_ID = 1 · _COL_BP = 3 · _COL_ALT = 4 · _COL_REF = 5
    load_bim_rows(bim_path) -> list[list[str]] · detect_occluded_variants(rows) -> (ids, edges)
  src/python/run_native_ld_panel.py:
    :100-119 _PANEL_COLUMNS (9 cols, `n_dropped_occluded` at index 7) — DO NOT TOUCH
    :122-133 the clause-(d) comment block + `_OCCLUSION_ANOMALY_FRACTION = 0.0005`
    :713     _reclaim_region_scratch `_keep_names` = {npz, occluded.excludelist}
    :781     out_prefix = str(compute_dir / region_id)
    :814-830 GATE 1 feasibility ceiling (runs FIRST — unchanged)
    :840     occluded_ids, occlusion_edges = detect_occluded_variants(raw_rows)
    :841-859 GATE 2 (the clause-(d) gate) + the FALSE "10x headroom / 5/102,421 / 51" prose
    :860-899 excludelist write + the two Stage-A manifests
    :1004-1022 the gs:// upload block (inside `if ok:`) — the pattern the sidecar follows
    :1035-1036 `_reclaim_region_scratch` runs ONLY when status == "ok"
  src/python/fire_verifier.py:
    :114-121 _default_occlusion_fraction()
    :278-280 _OK_STATUSES / _DEFERRAL_PREFIXES / _FAILURE_STATUSES
    :485     check_manifest_rows(manifest_path, expected_records: int = 5, ...)
    :546     check_occlusion_ceiling(n_occluded, n_var, frac=None)   -> Check name "occlusion_anomaly_ceiling"
    :578     check_region1_status  (+ the false "5 occluded, ~51 ceiling, 10x headroom" detail)
    :887-912 _stage_a  (3 construction sites use the name "occlusion_anomaly_ceiling")
    :963     a.add_argument("--expected-records", type=int, default=5)

BLAST RADIUS OF THE RENAME (every site — do not go hunting):
  tests/m3/test_run_native_ld_panel.py monkeypatch sites (all -> BOTH new globals):
    1666, 1689, 1711, 1739, 1781, 1929, 1973, 2031, 2098, 2365 (`raising=False` at 2365)
  tests/m3/test_run_native_ld_panel.py prose/comment: 2126-2132 (section header),
    2142-2143 ("the REAL region 1 is 5/102,421 = 4.9e-5" — FALSE), ~2149-2153 (the `_one_occlusion_cohort` docstring) (docstring "0.0005")
  tests/m3/test_run_native_ld_panel.py gate tests: 2166 (defers), 2240 (sub-ceiling),
    2271 (boundary), 2335 (feasibility-before-detect BUILT-IN control)
  tests/m3/test_fire_verifier.py: 71 (REAL_OCCLUSION_ANOMALY), 101 (docstring "5 occluded of
    102,421"), 513/520/529/534/541 (expected_records=5), 551-580 (the ceiling tests +
    identity test), 583-589 (banned literals), 795/801-806/813 (summarize tests reference
    the check by name)
  ast STATUS-VOCABULARY enforcer: tests/m3/test_fire_verifier.py:321 walks the producer and
    requires every emitted status prefix to classify. The new status MUST keep the literal
    prefix `deferred_occlusion_anomaly:` as the f-string's constant head, or that goes RED.
  RUNBOOKS: AGENT-PROMPT 183, 196, 221, 299, 311-315 · BROWSER-PASTE 281, 458 ·
    READY-TO-FIRE 236-264, 321, 329-332

260817-vbu-verify.sh: extracts its §6b card block by REGEX (`^STEP 6b`..`^STEP 7`,
  `^## 6b`..`^## 7`, `^## 6b`..`^## 7[.]`) — NOT by fixed line ranges. Every runbook edit in
  this task is OUTSIDE those blocks (STEP 8 / STEP 10 / the clause-(d) figures paragraph),
  so the output must be byte-identical. V1 prints BLOCK-RELATIVE line numbers, so an edit
  INSIDE a card block would move them — do not make one.
</measured_facts>

<interfaces>
<!-- The contracts the executor writes against. Do not go exploring for these. -->

```python
# src/python/occlusion_gate_constants.py  (NEW — T1)
OCCLUSION_SITE_FRACTION_CEILING: float = 0.005056   # a FRACTION; the amendment writes 0.5056%
OCCLUSION_INFLATION_CEILING: float = 3.42           # occ_rows / occ_sites

# src/python/run_native_ld_panel.py  (T2)
_OCCLUSION_SITE_FRACTION_CEILING = occlusion_gate_constants.OCCLUSION_SITE_FRACTION_CEILING
_OCCLUSION_INFLATION_CEILING     = occlusion_gate_constants.OCCLUSION_INFLATION_CEILING
# {out_prefix}.occlusion_gate.json
{
  "region_id": str, "n_rows": int, "n_sites": int, "occ_rows": int, "occ_sites": int,
  "site_fraction": float, "inflation": float | None,
  "site_fraction_ceiling": float, "inflation_ceiling": float,
  "fired": list[str],            # subset of ["site_fraction", "inflation"], order fixed
  "verdict": str,                # "ok" | "deferred"
}

# src/python/fire_verifier.py  (T2 — MERGED here so no commit ships a red suite;
#   `_default_occlusion_fraction()` reads the producer global T2 deletes, at CALL time)
def _default_site_fraction_ceiling() -> float: ...   # rnlp._OCCLUSION_SITE_FRACTION_CEILING
def _default_inflation_ceiling() -> float: ...       # rnlp._OCCLUSION_INFLATION_CEILING
def check_occlusion_gate(occ_rows: Optional[int], occ_sites: Optional[int],
                         n_sites: Optional[int], *,
                         site_ceiling: Optional[float] = None,
                         inflation_ceiling: Optional[float] = None) -> Check: ...
#   CLI (T2): --gate-json REQUIRED   ->   feeds check_occlusion_gate, FAIL CLOSED if absent

# src/python/fire_verifier.py  (T3)
def check_manifest_rows(manifest_path, expected_records: int, *,
                        region_id: Optional[str] = None,
                        expect_header: bool = True) -> Check: ...   # no default
#   CLI (T3): --excludelist REQUIRED (derives expected_records from its line count)
#             --expected-records default=None (an OVERRIDE, logged as one)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The one pinned place — constants module + its enforcer test (RED → GREEN)</name>
  <files>src/python/occlusion_gate_constants.py, tests/m3/test_occlusion_gate_constants.py</files>
  <behavior>
    - RED FIRST: write `tests/m3/test_occlusion_gate_constants.py` before the module exists;
      the collection/import error IS the red. Paste it verbatim in the SUMMARY.
    - Test A (rendered-string identity, per D1a): parse the amendment for the two SLOT_LEDGER
      lines `  CEILING_3X_MEDIAN_PCT = 0.5056%` and `  INFLATION_CEILING_3X_X = 3.42x`
      (regex on the ledger's own line shape; assert BOTH were found, so a deleted ledger line
      cannot make the test vacuous), then assert
      `f"{OCCLUSION_SITE_FRACTION_CEILING*100:.4f}%" == "0.5056%"` and
      `f"{OCCLUSION_INFLATION_CEILING:.2f}x" == "3.42x"` against the PARSED values — never a
      float tolerance, never a hand-typed second copy of the number.
    - Test B (public-record pin): extract the marker-exclusive paste block in Python (lines
      strictly between `--- PASTE INTO OSF FROM HERE ---` and `--- PASTE ENDS HERE ---`,
      rejoined with "\n" plus a trailing "\n"), assert SIZE 22945 FIRST and md5
      `13a49f543cabcc27ce9f1e589783c060` SECOND (size-first is the 260817-vbu house order).
    - NEGATIVE CONTROL 1 (in-test, parametrised): the perturbed value 0.005057 renders to
      "0.5057%" != "0.5056%", and 3.43 renders to "3.43x" != "3.42x" — i.e. the identity
      transform DISCRIMINATES. Run the same rendering helper the green test uses.
    - NEGATIVE CONTROL 2 (in-test, tmp_path only — NEVER in-tree): copy the amendment text to
      `tmp_path`, flip exactly one byte INSIDE the paste block, re-extract, assert the md5 is
      NOT the anchor and the byte length is unchanged (a one-byte flip, not an edit).
    - The banned-literal scans (D1c) are NOT in this task — they belong to the RED of T2/T3,
      so that every commit this task makes leaves the suite at 0 failed.
  </behavior>
  <action>
Guard first: run the paste-block size+md5 check from `<measured_facts>`; abort if it is not
22945 / 13a49f54.

1. Write `tests/m3/test_occlusion_gate_constants.py` (tests A, B and the two controls above).
   Follow the existing tests/m3 conventions: `PROJECT_ROOT = Path(__file__).resolve().parents[2]`,
   `sys.path.insert(0, str(PROJECT_ROOT / "src" / "python"))`, module-level import of the
   module under test. Run it — it MUST fail on import. Paste the red.

2. Write `src/python/occlusion_gate_constants.py` with exactly the two module constants and a
   provenance docstring carrying, per D1:
     * OSF file `mk7ze`, https://osf.io/mk7ze, posted 2026-08-22T02:58:55Z, on parent record
       `az52u`; the project copy at
       `.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md`
       (paste block 22,945 B / md5 13a49f543cabcc27ce9f1e589783c060)
     * the SLOT_LEDGER slot names `CEILING_3X_MEDIAN_PCT` and `INFLATION_CEILING_3X_X`
     * the derivations: 3x the 21-region site-basis MEDIAN 0.1685%; 3x the inflation MEDIAN
       1.14x (state explicitly that the median, not the 1.18x mean, is the anchor — the
       amendment says so)
     * the units trap, stated once: the code holds a FRACTION (0.005056); the amendment writes
       a PERCENTAGE (0.5056%)
     * the standing rule: "a change here requires a NEW posted OSF amendment FIRST —
       pre-registration precedes execution", and the enforcer's name
       (`tests/m3/test_occlusion_gate_constants.py`) so the invariant has a NAMED enforcer.
   NO other symbols. No helper functions. This module must stay importable with zero imports
   of its own so the producer's import is free.

3. Re-run the test file — GREEN. Then run the two negative controls and paste their output.

4. Re-run the guard. Commit EXPLICIT paths only:
   `git add src/python/occlusion_gate_constants.py tests/m3/test_occlusion_gate_constants.py`
   Message: `feat(quick-260821-x91): T1 — pin the posted two-condition ceilings in one module + enforcer test`
  </action>
  <verify>
    <automated>PY=/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python; $PY -m pytest tests/m3/test_occlusion_gate_constants.py -q && A=.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md && awk '/--- PASTE INTO OSF FROM HERE ---/{f=1;next}/--- PASTE ENDS HERE ---/{f=0}f' "$A" | md5sum | grep -q 13a49f543cabcc27ce9f1e589783c060 && git diff --stat ef59ca1 HEAD -- .planning/amendments/ src/python/occlusion_span_filter.py | wc -l | grep -qx 0 && echo T1-VERIFY-OK</automated>
  </verify>
  <done>`src/python/occlusion_gate_constants.py` exists with exactly two constants and a
provenance docstring; `tests/m3/test_occlusion_gate_constants.py` passes; both negative
controls were OBSERVED RED and their output is in the SUMMARY; the amendment and
`occlusion_span_filter.py` are byte-unchanged; one explicit-path commit exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Producer two-condition gate + gate sidecar + the fire_verifier accessors/gate that read it — MERGED so every commit is 0-failed (RED → GREEN)</name>
  <files>src/python/run_native_ld_panel.py, tests/m3/test_run_native_ld_panel.py, tests/m3/test_occlusion_gate_constants.py, src/python/fire_verifier.py, tests/m3/test_fire_verifier.py</files>
  <behavior>
    SCOPE NOTE (checker BLOCKER 1, resolved by MERGE): the `fire_verifier.py` accessor +
    `check_occlusion_gate` rewrite lives HERE, not in T3. `_default_occlusion_fraction()` reads
    `rnlp._OCCLUSION_ANOMALY_FRACTION` at CALL time, so step 2b's deletion of that global breaks
    `tests/m3/test_fire_verifier.py` the instant it lands. Merging keeps "0 failed at EVERY
    commit" literally true; splitting would require shipping a documented transient red, and a
    green that follows a planned red carries less information than a green that never broke.
    T3 keeps the oracle re-derivation (Layer 1 / Layer 2) and the `check_manifest_rows` /
    `_stage_a` expected_records derivation — both independently green-at-every-commit.

    RED FIRST — write/adjust every assertion below and observe it fail BEFORE touching
    `run_native_ld_panel.py`. Paste each red in the SUMMARY.

    - `test_occlusion_gate_site_fraction_fires` (the RENAME of the existing
      `test_occlusion_anomaly_gate_defers_never_excludes`): the region-1 synthetic fixture (5
      occluded of 11 rows / 11 sites) -> site fraction 45.45% > 0.5056% fires, inflation 1.0
      does NOT -> status starts `deferred_occlusion_anomaly:`, `fired == ["site_fraction"]` in
      the sidecar, no excludelist, no manifest, plink never ran for that region, loop continues.
      THREE PRE-EXISTING ASSERTIONS INSIDE THIS TEST CONTRADICT THE NEW CONTRACT and must be
      replaced in the same edit (all three are RED-before / GREEN-after, and the SUMMARY records
      the first two as a DELIBERATE contract change, never as an edit-to-green):
        * `:2221  assert not any("m2_region_00001" in d for d in cp_dsts), cp_dsts` — the
          blanket negative. REPLACE with the exact allow-list:
          `r1 = [d for d in cp_dsts if "m2_region_00001" in d]` ;
          `assert r1 == [f"{gs_out}/m2_region_00001.occlusion_gate.json"], r1` ; plus explicit
          negatives that no `.npz` / `.afreq` / `.occluded.excludelist` /
          `.occlusion_manifest.tsv` for region 1 crossed.
        * `:2223  assert all(d == panel_uri or "m2_region_00002" in d for d in cp_dsts)` — same
          blanket, second form. Widen to allow exactly the one sidecar URI as well.
        * `:2207  assert "5 occluded of 11" in res[0]["status"]` — the OLD status wording. The
          new f-string reads `5 occluded rows at 5 sites of 11`; assert against the new shape
          (and keep a non-vacuous substring, not just the prefix).
    - `test_occlusion_gate_inflation_fires_with_site_fraction_under`: NEW fixture — 1000 clean
      SNPs at distinct positions + one deletion + a same-position STACK of 8 rows (8 distinct
      ALTs at one bp) inside the deletion footprint -> n_sites=1002, occ_sites=1, occ_rows=8;
      site fraction 0.0998% (UNDER 0.5056%) but inflation 8.0 > 3.42 -> DEFERRED with
      `fired == ["inflation"]`. This is the test that would have been impossible under the old
      single-condition gate — it is the reason the companion condition exists.
    - `test_occlusion_gate_both_under_proceeds`: the existing sub-ceiling fixture (1 occluded of
      2001 rows / 2001 sites; site fraction 0.04998%, inflation 1.0) -> unchanged
      exclude-in-lockstep behaviour: excludelist written, `--exclude` in argv,
      `n_dropped_occluded == 1`, verified `.npz`, status `ok`.
    - `test_occlusion_gate_site_fraction_boundary_is_strictly_greater`: monkeypatch
      `_OCCLUSION_SITE_FRACTION_CEILING` to exactly `1/2001` on the 1-of-2001 fixture ->
      `occ_sites/n_sites == ceiling` EXACTLY -> does NOT defer (strict `>`), status `ok`.
    - `test_occlusion_gate_inflation_boundary_is_strictly_greater`: on the stack fixture,
      monkeypatch `_OCCLUSION_INFLATION_CEILING` to exactly the fixture's inflation (8.0) ->
      does NOT defer, status `ok`. (Both boundary tests monkeypatch the CEILING, not the data:
      the boundary property under test is the OPERATOR; the constants' VALUES are pinned by
      T1's identity test. Say so in each docstring.)
    - `test_occlusion_gate_sidecar_zero_occlusion`: an all-plain cohort -> sidecar exists with
      `occ_rows == 0`, `occ_sites == 0`, `inflation is None`, `fired == []`, `verdict == "ok"`,
      and NO excludelist is written.
    - `test_occlusion_gate_sidecar_numbers_are_independently_recomputed`: the test re-reads the
      fixture `.bim` and recomputes `n_rows`, `n_sites`, `occ_rows`, `occ_sites`,
      `site_fraction`, `inflation` with its OWN set arithmetic, then asserts equality with the
      sidecar JSON field by field. Not a re-read of the producer's own numbers — a second
      computation.
    - `test_occlusion_gate_deferred_region_ships_only_its_sidecar` (CONTRACT CHANGE, red then
      green): in `gs://` mode the set of region-1 cp destinations is EXACTLY
      `{f"{gs_out}/m2_region_00001.occlusion_gate.json"}`, plus explicit negatives that no
      `.npz` / `.afreq` / `.occluded.excludelist` / `.occlusion_manifest.tsv` for that region
      crossed. This REPLACES the old blanket `assert not any("m2_region_00001" in d ...)`.
      It is red before the change (the set is empty) and green after — a genuine TDD red, and
      the SUMMARY must record it as a deliberate contract change ("a deferred region now ships
      its gate evidence"), NOT as an edit-to-green.
    - Banned-literals extension (D1c) added to `tests/m3/test_occlusion_gate_constants.py`:
      the comment-stripped source of `run_native_ld_panel.py` contains neither `0.0005` nor
      `_OCCLUSION_ANOMALY_FRACTION`. Strip comments the way
      `test_no_hardcoded_shipped_constants_in_the_module` already does
      (`"\n".join(ln.split("#", 1)[0] for ln in src.splitlines())`). RED before the producer
      edit.
    - `grep -rn "_OCCLUSION_ANOMALY_FRACTION" src/ tests/` == 0 hits at the end of the task.

    MERGED FROM T3 — the fire_verifier surface that reads the producer's globals (everything
    below must be RED FIRST too, and the reds here are real `AttributeError`/`TypeError`s):
    - `_default_occlusion_fraction()` -> replaced by `_default_site_fraction_ceiling()` and
      `_default_inflation_ceiling()` reading `rnlp._OCCLUSION_SITE_FRACTION_CEILING` /
      `rnlp._OCCLUSION_INFLATION_CEILING` at EVALUATION time; the identity test monkeypatches
      the producer globals and asserts the accessors follow.
    - `check_occlusion_ceiling` -> `check_occlusion_gate(occ_rows, occ_sites, n_sites, *,
      site_ceiling=None, inflation_ceiling=None)`; emitted Check name becomes `occlusion_gate`;
      reproduces the shipped comparison EXACTLY (strict `>` on both, `inflation = occ_rows /
      occ_sites` with `None` when `occ_sites == 0`); FAILS CLOSED on any `None` input;
      `measured` carries `occ_rows, occ_sites, n_sites, site_fraction, inflation,
      site_fraction_ceiling, inflation_ceiling, fired`; severity HARD_STOP.
    - `_stage_a` feeds it from the producer's OWN sidecar (the panel row has `n_dropped_occluded`
      / `n_var` and NO site counts), via a new REQUIRED `--gate-json`; absent / unreadable /
      missing any of `occ_rows` / `occ_sites` / `n_sites` => FAIL CLOSED under the name
      `occlusion_gate`. The `--excludelist` derivation of `expected_records` and its cross-check
      against the sidecar's `occ_rows` are T3's — do NOT pull them forward.
    - `check_manifest_rows` and `check_region1_status` PROSE corrected (checker WARNING 2): the
      `:490` docstring line "Region 1 is the ONLY region with a known answer (5 occluded)" and
      the `:595` detail "(5 occluded, ~51 ceiling, 10x headroom)" are the last two live
      statements of the false premise in shipped code, and the banned-literals test bans FLOATS
      only — it cannot see either. `check_manifest_rows`'s SIGNATURE is untouched here (T3).
    - BANNED-LITERALS TRAP, read before writing the replacement prose: that test scans the
      COMMENT-STRIPPED source, and a docstring / f-string detail is CODE, not a comment. So the
      replacement text must NOT contain `0.005056`, `0.5056` or `3.42` — render the ceilings
      from the accessors instead
      (`f"{_default_site_fraction_ceiling()*100:.4f}% / {_default_inflation_ceiling():.2f}x"`),
      which is the stronger form anyway: identity, not a copy. The MEASURED region-1 figures
      (231 / 196 / 96,708 / 0.2027% / 1.18x) are not shipped constants and may appear as text.
    - `tests/m3/test_fire_verifier.py`: `REAL_OCCLUSION_ANOMALY` (`:71`) becomes a string in the
      NEW producer format (copy the shape from the shipped f-string, not from memory); the
      `_region1_panel` docstring + `n_dropped_occluded` (`:101-105`) become 231; the ceiling
      tests (`551-580`) become gate tests; the identity test targets the two new globals; the
      banned tuple (`583-589`) gains `0.005056`, `0.5056`, `3.42`; the `summarize` /
      `check_status` tests (`795-813`) follow the rename (including
      `"occlusion_gate" in s["hard_stops"]`); every `_stage_a` CLI fixture gains a minimal
      `.occlusion_gate.json` (region-1 measured shape: `occ_rows 231, occ_sites 196,
      n_sites 96708`) so the new required flag is satisfiable. `check_manifest_rows` call sites
      keep passing `expected_records=5` here — T3 removes the default and re-points them.
  </behavior>
  <action>
Guard first (paste-block size+md5).

1. RED: write the seven tests above into `tests/m3/test_run_native_ld_panel.py` (section 18,
   next to the existing gate tests) and the banned-literals test into
   `tests/m3/test_occlusion_gate_constants.py`. In the RENAMED
   `test_occlusion_anomaly_gate_defers_never_excludes` -> `test_occlusion_gate_site_fraction_fires`,
   replace lines `:2221`, `:2223` and `:2207` per behaviour bullet 1 (blanket negative -> exact
   one-object allow-list; the second blanket widened; the old `"5 occluded of 11"` status
   substring -> the new wording). Run and paste every red.

2. GREEN — `src/python/run_native_ld_panel.py`:
   a. Import: `from occlusion_gate_constants import (OCCLUSION_SITE_FRACTION_CEILING,
      OCCLUSION_INFLATION_CEILING)` next to the other `src/python` sibling imports, and bind
      `_OCCLUSION_SITE_FRACTION_CEILING = OCCLUSION_SITE_FRACTION_CEILING` /
      `_OCCLUSION_INFLATION_CEILING = OCCLUSION_INFLATION_CEILING` as MODULE GLOBALS. Both are
      read at EVALUATION TIME inside `process_region` (never bound as default arguments) so
      the topology-dense fixtures can monkeypatch them — keep that sentence in the comment.
   b. Delete `_OCCLUSION_ANOMALY_FRACTION` and rewrite the :122-132 comment block: cite
      `mk7ze` / https://osf.io/mk7ze / 2026-08-22T02:58:55Z, state the TWO conditions with
      their ledger provenance, state that accounting stays ROW-keyed and that both routes emit
      the same status prefix, and state that the values live in `occlusion_gate_constants`
      because a change there requires a new posted amendment first.
   c. Delete the false prose at :847-852 ("10x headroom", "5/102,421", "ceiling 51"). Replace
      with the measured region-1 figures (231 rows / 196 sites of 96,708 -> 0.2027% / 1.18x,
      MEASURED 2026-08-19/20) and a pointer to the sweep file.
   d. After `occluded_ids, occlusion_edges = detect_occluded_variants(raw_rows)` compute, using
      `occlusion_span_filter._COL_CHR` / `._COL_BP` / `._COL_ID` (import the names; do NOT
      re-declare column indices — a second copy is a silent divergence):

          occ = set(occluded_ids)
          def _site(r): return (r[osf._COL_CHR], r[osf._COL_BP])
          n_sites   = len({_site(r) for r in raw_rows})
          occ_sites = len({_site(r) for r in raw_rows if r[osf._COL_ID] in occ})
          occ_rows  = len(occluded_ids)
          site_fraction = (occ_sites / n_sites) if n_sites else 0.0      # guard anyway
          inflation = (occ_rows / occ_sites) if occ_sites else None
          # READ the module globals ONCE, HERE (evaluation time) — these two names are
          # the ONLY ceiling references from here down, in both `fired` and the status
          # f-string. Never a default argument; never a second read.
          site_ceiling      = _OCCLUSION_SITE_FRACTION_CEILING
          inflation_ceiling = _OCCLUSION_INFLATION_CEILING
          fired = [c for c, hit in (
                      ("site_fraction", site_fraction > site_ceiling),
                      ("inflation", inflation is not None
                                    and inflation > inflation_ceiling),
                   ) if hit]

      STRICT `>` on both.
   e. Write the sidecar `{out_prefix}.occlusion_gate.json` IMMEDIATELY (before any branch), with
      the key set from `<interfaces>`; `n_rows = pre_window_n_var`; `verdict = "deferred" if
      fired else "ok"`; `json.dump(..., indent=2, sort_keys=False)` + trailing newline;
      `parent.mkdir(parents=True, exist_ok=True)` as the excludelist write already does. Wrap in
      the same best-effort `try/except` shape as the manifest write ONLY IF it cannot mask a
      deferral — it must NOT: an unwritable sidecar on a DEFERRING region is an evidence loss,
      so let that exception propagate to the outer `except` (status `error:`) rather than
      swallowing it. State that choice in a comment.
   f. If `fired`: set

          result["status"] = (
            f"deferred_occlusion_anomaly: {occ_rows} occluded rows at {occ_sites} sites "
            f"of {n_sites} (site_fraction {site_fraction:.4%} {'>' if 'site_fraction' in fired else '<='} "
            f"ceiling {site_ceiling:.4%}; inflation "
            f"{'n/a' if inflation is None else format(inflation, '.2f') + 'x'} "
            f"{'>' if 'inflation' in fired else '<='} ceiling {inflation_ceiling:.2f}x; "
            f"fired={'+'.join(fired)})")

      The literal `deferred_occlusion_anomaly:` MUST remain the f-string's constant head (the
      ast status-vocabulary enforcer reads it). Print to stderr in the existing shape, upload
      the sidecar when `gs_mode` (the ONLY object a deferred region ships), append the panel
      row, `return result` — still BEFORE any excludelist / manifest / plink. `n_var` and
      `n_dropped_occluded` stay `None`.
   g. `if ok:` upload block (:1004-1022): add an existence-gated `_gsutil_upload` of
      `{out_prefix}.occlusion_gate.json` to `{gs_out_dir}/{region_id}.occlusion_gate.json`,
      following the per-region-manifest pattern verbatim. Do NOT touch the `verify_failed`
      path — it must still cross nothing but the panel row.
   h. `_reclaim_region_scratch`: add `f"{region_id}.occlusion_gate.json"` to `_keep_names` and
      extend the docstring's durable-provenance sentence.
   i. `_PANEL_COLUMNS` UNTOUCHED. `n_dropped_occluded` stays a ROW count.

3. Update the ten monkeypatch sites (1666, 1689, 1711, 1739, 1781, 1929, 1973, 2031, 2098,
   2365) to pin BOTH globals open (`_OCCLUSION_SITE_FRACTION_CEILING` -> 1.0,
   `_OCCLUSION_INFLATION_CEILING` -> a large float). The 5/11 fixture would otherwise fire the
   site-fraction condition. Keep `raising=False` ONLY where it already is.

4. Correct the prose: 2142-2143 -> "the REAL region 1 is 231/102,421 rows = 0.2255%, 196/96,708
   sites = 0.2027%, inflation 1.18x — MEASURED 2026-08-19/20
   (.planning/debug/260820-site-basis-sweep-results-as-received.md)"; the 2126-2132 section
   header and the ~2149-2153 (the `_one_occlusion_cohort` docstring) docstring -> the two-condition rule and its two constants.

5. GREEN — `src/python/fire_verifier.py` + `tests/m3/test_fire_verifier.py`. THIS IS IN T2 BY
   DESIGN (see the SCOPE NOTE): step 2b deletes `_OCCLUSION_ANOMALY_FRACTION`, which
   `_default_occlusion_fraction()` reads at CALL time, so without this step the T2 commit ships
   `test_occlusion_fraction_is_the_shipped_module_global` ERRORed and
   `test_occlusion_ceiling_green_region1` / `test_occlusion_ceiling_boundary_is_strictly_greater`
   / `test_summarize_all_pass_exit_zero` FAILing. Do the TEST edits (5f) BEFORE the module edits
   and paste those reds too.
   a. Replace `_default_occlusion_fraction()` (`:114-121`) with `_default_site_fraction_ceiling()`
      and `_default_inflation_ceiling()` returning `rnlp._OCCLUSION_SITE_FRACTION_CEILING` /
      `rnlp._OCCLUSION_INFLATION_CEILING`. Keep the existing docstring's "read as a MODULE GLOBAL
      off the shipped producer at evaluation time" sentence; add the pointer to
      `occlusion_gate_constants` as the one pinned place.
   b. Replace `check_occlusion_ceiling` (`:547`) with `check_occlusion_gate(occ_rows, occ_sites,
      n_sites, *, site_ceiling=None, inflation_ceiling=None)` per the behaviour list. Check name
      `occlusion_gate`, severity HARD_STOP, strict `>` on both, `inflation = None` when
      `occ_sites == 0`, FAIL CLOSED on any `None` in the three positional inputs.
   c. `_stage_a`: rename ALL THREE `"occlusion_anomaly_ceiling"` Check-construction sites
      (the panel-unreadable branch, the region-missing branch, and the
      `check_occlusion_ceiling(row["n_dropped_occluded"], row["n_var"])` call) to the new name
      and the new call shape. The panel row has NO site counts, so add the REQUIRED
      `a.add_argument("--gate-json", required=True, help="the region's .occlusion_gate.json "
      "sidecar — the shipped gate's own measurement; absent => FAIL CLOSED")` plus a small
      loader that returns a FAIL `occlusion_gate` Check (HARD_STOP) when the sidecar is absent,
      unreadable, or missing any of `occ_rows` / `occ_sites` / `n_sites`. The `--excludelist`
      derivation of `expected_records` and the sidecar cross-check are T3's — do NOT pull them
      forward, and leave `--expected-records` at `default=5` until T3.
   d. Correct the two false-premise PROSE sites (checker WARNING 2 — the banned-literals test
      bans floats only and cannot see either):
        * `:490` `check_manifest_rows` docstring — "Region 1 is the ONLY region with a known
          answer (5 occluded), so it is the one chance to validate the manifest writer against
          ground truth." becomes the measured/derived semantics: region 1's record count is 231
          (MEASURED 2026-08-19/20: 231 occluded ROWS at 196 sites of 96,708 sites / 102,421 rows,
          `.planning/debug/260820-site-basis-sweep-results-as-received.md`), and the caller
          DERIVES `expected_records` from the region's `.occluded.excludelist` line count rather
          than a human typing it (T3) — the count is never the whole check, which is why the
          A-10 content assertions below exist. KEEP the A-10 paragraph verbatim.
        * `:595` `check_region1_status` detail — "(5 occluded, ~51 ceiling, 10x headroom)"
          becomes the measured figures against the posted ceilings RENDERED FROM THE ACCESSORS,
          never typed:
          `f"(MEASURED 2026-08-19/20: 231 occluded rows at 196 of 96,708 sites -> 0.2027% and "
          f"1.18x, under the posted {_default_site_fraction_ceiling()*100:.4f}% / "
          f"{_default_inflation_ceiling():.2f}x)"`.
      TRAP: a docstring and an f-string detail are CODE, not comments — the banned tuple you
      extend in 5f (`0.005056`, `0.5056`, `3.42`) WILL fire on them if you type the ceilings.
      That is why 5d renders them from the accessors.
   e. Module docstring: the "NO HAND-TRANSCRIBED SHIPPED CONSTANTS" paragraph names the two new
      accessors; the stage-a example invocation gains `--gate-json` (it gains `--excludelist`
      in T3, where that flag lands).
   f. `tests/m3/test_fire_verifier.py` per the behaviour list (`REAL_OCCLUSION_ANOMALY` at `:71`
      -> the new producer format; `_region1_panel` at `:101-105` -> 231; the ceiling tests at
      `551-580` -> gate tests; the identity test -> the two new globals; the banned tuple at
      `583-589` gains `0.005056` / `0.5056` / `3.42`; the summarize/check_status tests at
      `795-813` -> `occlusion_gate`; every `_stage_a` CLI fixture gains a minimal
      `.occlusion_gate.json`).

6. Re-run ALL THREE files GREEN — this task's commit must leave `tests/m3` at 0 failed:
   `$PY -m pytest tests/m3/test_run_native_ld_panel.py tests/m3/test_occlusion_gate_constants.py tests/m3/test_fire_verifier.py -q`
   (the ast status-vocabulary enforcer lives in the third file and must stay green; if it goes
   red the status prefix was broken — fix the producer, never the enforcer).

7. Guard again. Commit explicit paths:
   `git add src/python/run_native_ld_panel.py src/python/fire_verifier.py tests/m3/test_run_native_ld_panel.py tests/m3/test_occlusion_gate_constants.py tests/m3/test_fire_verifier.py`
   Message: `feat(quick-260821-x91): T2 — ship the posted TWO-condition occlusion gate + per-region gate sidecar + the fire_verifier gate that reads it`
  </action>
  <verify>
    <automated>PY=/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python; $PY -m pytest tests/m3/test_run_native_ld_panel.py tests/m3/test_occlusion_gate_constants.py tests/m3/test_fire_verifier.py -q && test "$(grep -rn '_OCCLUSION_ANOMALY_FRACTION' src/ tests/ | wc -l)" -eq 0 && test "$(grep -c '5 occluded' src/python/fire_verifier.py)" -eq 0 && test "$(grep -c 'known answer (5' src/python/fire_verifier.py)" -eq 0 && $PY -c "import sys; sys.path.insert(0,'src/python'); import fire_verifier as fv; assert hasattr(fv,'check_occlusion_gate') and hasattr(fv,'_default_site_fraction_ceiling') and hasattr(fv,'_default_inflation_ceiling'); assert not hasattr(fv,'check_occlusion_ceiling') and not hasattr(fv,'_default_occlusion_fraction'); print('FV-API-OK')" && git diff --stat ef59ca1 HEAD -- src/python/occlusion_span_filter.py .planning/amendments/ | wc -l | grep -qx 0 && echo T2-VERIFY-OK</automated>
  </verify>
  <done>The producer defers on EITHER condition with the unchanged status prefix; the sidecar
is written on all three square outcomes and uploaded on the ok and deferral paths only;
`_OCCLUSION_ANOMALY_FRACTION` and the literal `0.0005` are gone from `src/` and `tests/`;
`_PANEL_COLUMNS` is byte-unchanged; `fire_verifier` exposes `_default_site_fraction_ceiling` /
`_default_inflation_ceiling` / `check_occlusion_gate` (and no longer exposes
`_default_occlusion_fraction` / `check_occlusion_ceiling`), `_stage_a` reads the sidecar via
`--gate-json`, and BOTH false-premise prose sites are gone
(`grep -c '5 occluded' src/python/fire_verifier.py` == 0); the three superseded assertions at
`:2207` / `:2221` / `:2223` are replaced by the exact one-object allow-list; every new
assertion was seen RED first and the reds are pasted; the deferral-ships-its-sidecar contract
change is recorded as a DELIBERATE change; `tests/m3` is 0 FAILED AT THIS COMMIT (the whole
point of the merge); one explicit-path commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Oracle re-derivation (Layer 1 DERIVED / Layer 2 MEASURED) + Stage A DERIVES its own expected_records (RED → GREEN)</name>
  <files>tests/m3/test_occlusion_span_filter.py, src/python/fire_verifier.py, tests/m3/test_fire_verifier.py</files>
  <behavior>
    LAYER 1 — DERIVED (tests/m3/test_occlusion_span_filter.py; `occlusion_span_filter.py` itself
    stays FROZEN and must end with an empty diff):
    - `:521` equality -> CONTAINMENT: `_REGION1_REAL_WINDOW_OCCLUDED_ROW_INDICES <=
      got_row_indices` (index containment is what probe 2 actually measured, 2026-08-19).
    - `:523` equality -> multiset CONTAINMENT:
      `Counter([60,29,7,31,31,17,29]) <= Counter(window_spans)`. PORTABILITY TRAP, measured
      during planning: `Counter.__le__` as multiset containment exists only on Python 3.10+
      (3.9 raises `TypeError`), and this test RUNS (does not skip) on the AoU VM where the
      real `.bim` is present. Write it as the explicit, version-independent form
      `all(Counter(window_spans)[span] >= n for span, n in Counter(_SETTLED_SPANS).items())`
      and state the equivalence in a comment — a TypeError on the fire path would look like a
      detector failure.
    - `:188 _REGION1_REAL_SAME_POSITION_COUNT` -> re-scoped to
      `_REGION1_NAN_PAIRS_SAME_POSITION_COUNT = 0` with a docstring/comment stating the scope
      "among the 6 NaN pairs, per the 2026-08-19 verdict" (or delete it outright); the skip
      message at `:510` updated to match — a window-wide 0 is FALSE and must not survive.
    - The `:154-183` comment block rewritten into two labelled halves: DERIVED (the settled-5
      containment at exact row indices; the 7 settled spans present in the window) and
      MEASURED (everything about totals). The `:201` comment "synthetic mirrors the real
      window" corrected — the synthetic mirrors the RULE, not the window inventory.
    - NOT in scope (the dossier is explicit): the index->bp mapping and the five attribution
      edges have NOT been directly observed and must NOT be asserted. Say so in the block.

    LAYER 2 — MEASURED (a SEPARATE gated test, own function, name AND docstring both carrying
    `MEASURED_NOT_DERIVED`):
    - pins `n_rows=102421, n_deletion_rows=7951, n_occluded_rows=231, max_span=170,
      n_sites=96708, occ_sites=196`
    - docstring provenance: probe 2 (2026-08-19) and PENDING PASTE #3 (2026-08-20),
      `afr_cohort.bim`, window 10000-13506933, banked at
      `.planning/debug/260820-site-basis-sweep-results-as-received.md`
    - docstring rule, verbatim in spirit: "on a break: RE-MEASURE AND RECORD, never
      edit-to-green" — a CDR refresh WILL break this and that is the point.
    - skips outside the perimeter on the same `data/aou/region1_window.bim` gate as Layer 1 ->
      the tests/m3 skip count moves 32 -> 33. State the expected delta; MEASURE it in T5 and
      report honestly if it differs.

    FIRE_VERIFIER — the REMAINDER ONLY. T2 already landed (and must stay green; do NOT redo
    them): the two evaluation-time accessors, `check_occlusion_gate`, the `occlusion_gate`
    rename at all three `_stage_a` sites, the required `--gate-json` sidecar read, the corrected
    `check_manifest_rows` / `check_region1_status` PROSE, the extended banned tuple, and the
    `REAL_OCCLUSION_ANOMALY` / `_region1_panel` fixture updates. What is left:
    - `check_manifest_rows(manifest_path, expected_records, ...)` loses its default — and every
      call site (`513/520/529/534/541` plus `_stage_a`) passes it explicitly.
    - `_stage_a`: DERIVE `expected_records` from `--excludelist` line count (non-empty lines);
      cross-check against the sidecar's `occ_rows` -> mismatch = FAIL (fail-closed), sidecar
      missing = FAIL, excludelist missing = FAIL (the sidecar read itself is T2's — reuse its
      loader, do not add a second one); `--expected-records` becomes `default=None`, an OVERRIDE
      that is stated in the check detail when used ("expected_records OVERRIDDEN by
      --expected-records=N; derived value was M"). Emit the derivation as its own Check named
      `expected_records_derivation`.
    - `check_region1_status` semantics and `_REGION1_SEVERITY` UNCHANGED (its detail string was
      already corrected in T2).
    - Module docstring: the stage-a example invocation gains `--excludelist` (T2 added
      `--gate-json`).
    - `tests/m3/test_fire_verifier.py`: T2's `_stage_a` sidecar fixture gains an
      `.occluded.excludelist`, and the MISMATCH negative control is added — a sidecar saying 230
      against an excludelist of 231 must FAIL. That control is the entire point of the
      cross-check, so it is not optional and its red must be pasted.
    - RED FIRST for every one of these; the reds here are real `TypeError`s (the missing
      required argument) and assertion failures, not collection errors.
  </behavior>
  <action>
Guard first (paste-block size+md5).

1. RED: make all the test edits above in `tests/m3/test_occlusion_span_filter.py` and
   `tests/m3/test_fire_verifier.py` FIRST. Run both files; paste the reds (the span-filter
   Layer-1 edits will mostly be green-by-construction since they weaken equalities to
   containment on a SKIPPED gated test — say so honestly and instead demonstrate
   discrimination with an in-test control: a containment assertion against a deliberately
   wrong index set must fail. The Layer-2 test's red is its absence; the fire_verifier reds are
   real `TypeError: check_manifest_rows() missing 1 required positional argument` plus the
   230-vs-231 cross-check control, which must FAIL before `_stage_a` learns to cross-check).

2. GREEN: edit `src/python/fire_verifier.py` per the (narrowed) behaviour list — drop
   `check_manifest_rows`'s `expected_records` default, add the derivation + cross-check to
   `_stage_a` reusing T2's sidecar loader, and change the CLI:
     a.add_argument("--excludelist", required=True, help="the region's .occluded.excludelist — "
                    "expected_records is DERIVED from its line count, never typed by a human")
     a.add_argument("--expected-records", type=int, default=None, help="OPTIONAL override ...")
   (`--gate-json` already exists from T2 — do not add it twice; do not re-rename the three
   `_stage_a` Check sites, T2 did that.)

3. Re-run `$PY -m pytest tests/m3/test_fire_verifier.py tests/m3/test_occlusion_span_filter.py -q`
   -> green, with the skip count from `-rs` recorded.

4. Guard again. Commit explicit paths:
   `git add src/python/fire_verifier.py tests/m3/test_fire_verifier.py tests/m3/test_occlusion_span_filter.py`
   Message: `fix(quick-260821-x91): T3 — re-derive the region-1 oracle (DERIVED vs MEASURED) + two-condition fire_verifier`
  </action>
  <verify>
    <automated>PY=/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python; $PY -m pytest tests/m3/test_fire_verifier.py tests/m3/test_occlusion_span_filter.py -q -rs && $PY -c "import sys; sys.path.insert(0,'src/python'); import fire_verifier as fv; assert hasattr(fv,'check_occlusion_gate'); import inspect; assert inspect.signature(fv.check_manifest_rows).parameters['expected_records'].default is inspect.Parameter.empty; print('API-OK')" && grep -q MEASURED_NOT_DERIVED tests/m3/test_occlusion_span_filter.py && git diff --stat ef59ca1 HEAD -- src/python/occlusion_span_filter.py .planning/amendments/ | wc -l | grep -qx 0 && echo T3-VERIFY-OK</automated>
  </verify>
  <done>Layer 1 asserts only containment and is labelled DERIVED; a separate
`MEASURED_NOT_DERIVED` gated test pins the six substrate totals with provenance and the
re-measure rule; `_REGION1_REAL_SAME_POSITION_COUNT` is re-scoped or gone;
`check_manifest_rows` has no default and every call site passes `expected_records` explicitly;
`_stage_a` DERIVES `expected_records` from the excludelist and cross-checks it against the
sidecar's `occ_rows`, with the 230-vs-231 mismatch control OBSERVED failing;
`--expected-records` is an override that is logged as one; T2's `check_occlusion_gate` /
accessors / banned tuple are still green; `occlusion_span_filter.py` diff is EMPTY;
`tests/m3` is 0 failed at this commit too; one explicit-path commit.</done>
</task>

<task type="auto">
  <name>Task 4: Runbooks tell the truth region 1 will produce — with the §6b card provably untouched</name>
  <files>.planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-AGENT-PROMPT.md, .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-BROWSER-PASTE.md, .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md</files>
  <action>
1. BEFORE any edit, capture the frozen-card baseline:
   `bash .planning/quick/260817-vbu-release-trsx5-gate-bank-resolved-adjudic/260817-vbu-verify.sh all > $SCRATCH/vbu-before.txt 2>&1; echo "exit=$?" >> $SCRATCH/vbu-before.txt`
   ($SCRATCH = the session scratchpad, never `/tmp`, never in-tree.) Also re-run the
   paste-block guard.

2. Edit the three runbooks. Every edit is OUTSIDE the §6b card blocks (`^STEP 6b`..`^STEP 7`,
   `^## 6b`..`^## 7`, `^## 6b`..`^## 7[.]`) — do not touch a line inside them.

   RETIRE (all live expectations built on the false premise):
     * AGENT-PROMPT:183 "n_dropped_occluded near 5"
     * AGENT-PROMPT:196 "EXPECT: 6 lines (header + exactly 5 records)"
     * AGENT-PROMPT:311-315 and READY-TO-FIRE:329-332 and BROWSER-PASTE:458 — the
       "0.0005 x n_var / 60.0 / 51.2 / ~5 occlusions / about 10x under the ceiling" paragraph
     * READY-TO-FIRE:257 "n_dropped_occluded ≈ 5 logged"
     * READY-TO-FIRE:321 "occluded count > 0.0005 × n_var"

   REPLACE WITH (identical substance in all three, worded for each file's register):
     * Region 1 EXPECT: `status == "ok"`, `n_dropped_occluded == 231`
       (MEASURED 2026-08-19/20 — 231 occluded ROWS at 196 sites of 96,708 sites / 102,421 rows;
       source `.planning/debug/260820-site-basis-sweep-results-as-received.md`).
     * THE ARBITER RULE: if the observed count differs, the sidecar
       `m2_region_00001.occlusion_gate.json` is the arbiter and the run STOPS for
       re-measurement — never edit-to-green, never "close enough".
     * Manifest EXPECT: `232 lines (header + 231 records)`, `region_id m2_region_00001` on
       every record row.
     * NEW sidecar EXPECT block (the gsutil cat + what to read):
       `occ_rows 231, occ_sites 196, n_sites 96708, site_fraction ≈ 0.2027%,
       inflation ≈ 1.18x, fired [], verdict ok`.
     * Clause (d) rewritten as the TWO-condition rule, citing the posted amendment
       (`mk7ze`, https://osf.io/mk7ze, 2026-08-22T02:58:55Z): DEFER when EITHER the occluded-
       SITE fraction exceeds 0.5056% (3x the site-basis median 0.1685%) OR the occluded-site
       row/site inflation exceeds 3.42x (3x the inflation median 1.14x); STRICT `>` on both;
       accounting stays ROW-keyed; both routes emit `deferred_occlusion_anomaly:`. Region 1
       sits at 0.2027% and 1.18x — under BOTH — so a deferral there is itself the finding.
     * STEP 7's gated pytest command: add the Layer-1 and Layer-2 test ids from T3 alongside
       the existing `test_region1_real_window_known_answer_gated` selector, and update the
       READY-TO-FIRE:242-251 "cannot fail / off-by-one" adjudication table to the containment
       semantics (an off-by-one now breaks CONTAINMENT, and the response is still: report, fix
       the TEST constant, never touch the frozen `occlusion_span_filter.py`).
     * The `fire_verifier.py stage-a` invocation in all three files: add
       `--excludelist /home/jupyter/native_ld_scratch/m2_region_00001.occluded.excludelist`
       and `--gate-json /home/jupyter/native_ld_scratch/m2_region_00001.occlusion_gate.json`,
       add the two matching `gsutil cp` lines, drop any `--expected-records`, and update the
       EXPECT check list — `occlusion_anomaly_ceiling` becomes `occlusion_gate` and
       `expected_records_derivation` is a new check (AGENT-PROMPT:221, BROWSER-PASTE:281,
       READY-TO-FIRE:264).

   DO NOT introduce `nothing scientific is lost` / `nothing is lost`.

3. AFTER the edits:
   `bash .planning/quick/260817-vbu-release-trsx5-gate-bank-resolved-adjudic/260817-vbu-verify.sh all > $SCRATCH/vbu-after.txt 2>&1; echo "exit=$?" >> $SCRATCH/vbu-after.txt`
   then `diff $SCRATCH/vbu-before.txt $SCRATCH/vbu-after.txt` — MUST be empty, both exits 0.
   If it is not empty: an edit landed inside a card block. REVERT that edit; do not adjust the
   verifier. Paste the diff result in the SUMMARY either way.

4. Guard again. Commit explicit paths (all three runbook files).
   Message: `docs(quick-260821-x91): T4 — runbooks carry the measured region-1 truth (231/196/96,708) + the posted two-condition gate`
  </action>
  <verify>
    <automated>bash .planning/quick/260817-vbu-release-trsx5-gate-bank-resolved-adjudic/260817-vbu-verify.sh all > /dev/null 2>&1 && D=.planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r && test "$(grep -rn 'near 5\|exactly 5\|10x under\|10× under\|51\.2\|0\.0005' $D/*.md | wc -l)" -eq 0 && test "$(grep -rni 'nothing scientific is lost\|nothing is lost' $D/ | wc -l)" -eq 0 && grep -q '231' $D/260812-ox1-AGENT-PROMPT.md && grep -q 'occlusion_gate.json' $D/260812-ox1-AGENT-PROMPT.md && grep -q '0.5056' $D/260812-ox1-READY-TO-FIRE.md && echo T4-VERIFY-OK</automated>
  </verify>
  <done>All three runbooks carry 231 / 232 / the sidecar EXPECT block / the two-condition
clause (d) / the sidecar-is-arbiter STOP rule / the new stage-a flags and check names; no live
"5 occluded" or `0.0005` expectation survives; the forbidden phrases are absent; the
`260817-vbu-verify.sh all` output is byte-identical before and after with exit 0 both times
and the `diff` result is in the SUMMARY; one explicit-path commit.</done>
</task>

<task type="auto">
  <name>Task 5: Suite re-baseline reconciled component-exact + HANDOFF / STATE / continue-here + push</name>
  <files>.planning/HANDOFF.json, .planning/STATE.md, .planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md, .planning/quick/260821-x91-remediation-batch-two-condition-occlusio/260821-x91-PLAN.md, .planning/quick/260821-x91-remediation-batch-two-condition-occlusio/260821-x91-SUMMARY.md</files>
  <action>
Guard first (paste-block size+md5).

1. SUITE:
   `$PY -m pytest tests/m3 -q -rs > $SCRATCH/m3-after.txt 2>&1` (~14 min). 0 failed is a HARD
   GATE — if anything is red, STOP and report; do not proceed to the docs.
   Then RECONCILE component-exact against the ef59ca1 baseline 1024 collected / 992 passed /
   32 skipped / 0 failed:
     * list every ADDED test by name (T1's enforcer + controls; T2's seven gate/sidecar tests;
       T3's `MEASURED_NOT_DERIVED` test and any new fire_verifier tests)
     * list every REMOVED or RENAMED test by name (e.g. the old
       `test_occlusion_anomaly_gate_*` names, `test_occlusion_ceiling_*`,
       `test_occlusion_fraction_is_the_shipped_module_global`)
     * explain the SKIP delta (expected 32 -> 33 from the Layer-2 gated test). If the measured
       delta is anything else, SAY SO AND EXPLAIN IT — do not force the expected number. A new
       test landing as an unexpected SKIP is NOT evidence (HANDOFF `suite_baselines.rule`).
     * the arithmetic must close: `baseline_passed + added - removed == measured_passed`. Show
       the sum. If it does not close, the reconciliation is wrong — find the missing component
       before writing anything down.
   Then run the FULL suite once as collateral: `$PY -m pytest tests -q > $SCRATCH/full-after.txt 2>&1`
   and report its counts (collateral only; note any pre-existing non-m3 state honestly).

2. `.planning/HANDOFF.json` (valid JSON — re-parse after editing):
   * PREPEND a new `resume_on_reconnect[0]` in the existing `> #0 (...)` register, superseding
     the current #0: the remediation batch LANDED (name the five commits), the shipped gate is
     now the posted TWO-condition rule, the sidecar contract, the corrected runbooks, and
     NEXT = Stage A from STEP 7 of `260812-ox1-AGENT-PROMPT.md` — CARTER starts the VM and
     fires; AN AGENT NEVER FIRES.
   * REPLACE `suite_baselines["tests/m3"]` with the reconciled numbers (it is stale at
     914/31 — correct it, and say in the string that it was stale and what the intermediate
     truth at ef59ca1 was: 992/32/0). Update `suite_baselines.rule` with the new skip number.
   * PREFIX `status` with the batch headline.
3. `.planning/STATE.md`: new top `★ RESUME HERE ★` block in house style + refresh `Last
   activity`. DO NOT add the Quick Tasks row — the orchestrator adds it.
4. `.planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md`: refresh the resume block
   (two-condition gate shipped; sidecar; runbooks; NEXT = Stage A, Carter fires).
5. Write `260821-x91-SUMMARY.md`: every RED pasted verbatim (T1 import red, T1's two negative
   controls, T2's seven reds + the banned-literals red, T3's fire_verifier reds and the
   containment discrimination control), the deferral-ships-its-sidecar contract change stated
   as a deliberate change, the vbu before/after diff result, the component-exact suite
   reconciliation table, and an honest note on anything that differed from expectation.
6. Final guard: paste-block md5; `git diff --stat ef59ca1 HEAD -- .planning/amendments/
   src/python/occlusion_span_filter.py` empty; `grep -rn "_OCCLUSION_ANOMALY_FRACTION" src/ tests/`
   = 0.
7. Commit explicit paths (the five files above), then `git push`. Confirm `git status -sb`
   shows no `ahead`.
   Message: `docs(quick-260821-x91): T5 — suite re-baselined + reconciled; HANDOFF/STATE/continue-here refreshed; NEXT = Stage A (Carter fires)`
  </action>
  <verify>
    <automated>PY=/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python; $PY -m pytest tests/m3 -q > $SCRATCH/m3-final.txt 2>&1; tail -2 $SCRATCH/m3-final.txt | grep -Eq '[0-9]+ failed' && { echo T5-SUITE-RED; exit 1; }; $PY -c "import json;d=json.load(open('.planning/HANDOFF.json'));assert 'occlusion_gate' in json.dumps(d);assert '992' in d['suite_baselines']['tests/m3'] or '1024' in json.dumps(d['suite_baselines']);print('HANDOFF-OK')" && test "$(grep -rn '_OCCLUSION_ANOMALY_FRACTION' src/ tests/ | wc -l)" -eq 0 && A=.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md && awk '/--- PASTE INTO OSF FROM HERE ---/{f=1;next}/--- PASTE ENDS HERE ---/{f=0}f' "$A" | md5sum | grep -q 13a49f543cabcc27ce9f1e589783c060 && git status -sb | head -1 | grep -qv ahead && echo T5-VERIFY-OK</automated>
  </verify>
  <done>`tests/m3` reports 0 failed; the new counts are reconciled component-exact against
992/32/0 with the arithmetic shown and the skip delta explained; the full `tests/` run is
reported as collateral; `HANDOFF.json` is valid JSON with a corrected `suite_baselines` and a
new `resume_on_reconnect[0]` naming Stage A (Carter fires); STATE.md and `.continue-here.md`
refreshed (no Quick Tasks row added); the SUMMARY carries every red verbatim; final guards
pass; branch pushed with no `ahead`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| posted OSF record → code constants | A public pre-registration constrains what the code may execute. Any drift is a scientific-integrity defect, not a bug. |
| repo → AoU perimeter (fire path) | The runbooks are executed by a human on a VM against controlled-tier data at $385-1,084. A wrong EXPECT there costs money and can bank defective science. |
| test oracle → substrate | Measured substrate numbers wearing derived-science clothes is the exact defect this batch repairs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-x91-01 | Tampering | `occlusion_gate_constants.py` | mitigate | T1's rendered-string identity test against the amendment's SLOT_LEDGER + the paste-block md5 pin: changing a constant without a new posted amendment fails the suite. Both controls seen red. |
| T-x91-02 | Repudiation | the amendment file | mitigate | Paste-block size+md5 (22,945 B / 13a49f54) asserted at the START and END of every task and in a permanent test; `git diff --stat ef59ca1 HEAD -- .planning/amendments/` must be empty. |
| T-x91-03 | Information disclosure | the new `.occlusion_gate.json` sidecar crossing to GCS | accept | The sidecar carries counts, fractions and policy labels only — no genotypes, no per-person data, no LD values. Same egress class as the excludelist and manifest already uploaded (coordinate/count-only). |
| T-x91-04 | Denial of service | producer sidecar write on a deferring region | mitigate | The sidecar write is NOT swallowed by a best-effort `except` on the deferral path: an unwritable sidecar there is evidence loss and must surface as `error:`, so the loop records it rather than deferring silently without evidence. |
| T-x91-05 | Spoofing | `--expected-records` as a human-typed number | mitigate | T3 makes it an OPTIONAL override; the value is DERIVED from the excludelist line count and cross-checked against the sidecar's `occ_rows`; a mismatch FAILS closed and an override is logged as an override in the check detail. |
| T-x91-06 | Elevation of privilege | scope creep beyond the posted authorisation | mitigate | Only the TWO-condition gate is implemented; accounting stays ROW-keyed; `_PANEL_COLUMNS` untouched; no fourth status branch; `occlusion_span_filter.py` frozen with an empty-diff assertion at every task. |
| T-x91-07 | Tampering | a future reader "fixing" the MEASURED_NOT_DERIVED pin | mitigate | Name + docstring carry the label, the provenance and the explicit rule "RE-MEASURE AND RECORD, never edit-to-green"; physical separation from Layer 1 so substrate drift cannot take the derived science down with it. |
</threat_model>

<verification>
Run after the final task, from the repo root:

```bash
PY=/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python
A=.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md

# 1 — public record frozen (size first, then hash)
awk '/--- PASTE INTO OSF FROM HERE ---/{f=1;next}/--- PASTE ENDS HERE ---/{f=0}f' "$A" | wc -c   # 22945
awk '/--- PASTE INTO OSF FROM HERE ---/{f=1;next}/--- PASTE ENDS HERE ---/{f=0}f' "$A" | md5sum  # 13a49f54…

# 2 — frozen surfaces untouched
git diff --stat ef59ca1 HEAD -- src/python/occlusion_span_filter.py .planning/amendments/       # empty

# 3 — the false premise is gone
grep -rn "_OCCLUSION_ANOMALY_FRACTION" src/ tests/                                              # 0 hits

# 4 — the enforcers
$PY -m pytest tests/m3/test_occlusion_gate_constants.py -q
$PY -m pytest tests/m3 -q -rs                                                                   # 0 failed

# 5 — the frozen §6b card
bash .planning/quick/260817-vbu-release-trsx5-gate-bank-resolved-adjudic/260817-vbu-verify.sh all

# 6 — published
git status -sb | head -1                                                                        # no "ahead"
```
</verification>

<success_criteria>
- Every locked element of the POSTED clause (d) is implemented at full fidelity: BOTH
  conditions, STRICT `>` on both, ROW-keyed accounting, all three numbers reported, one status
  prefix, no fourth branch. Ceiling-alone would be a scope reduction and is NOT acceptable.
- Every new or changed test was seen RED before GREEN and the red output is pasted in the
  SUMMARY.
- `tests/m3` = 0 failed, reconciled component-exact against 992 passed / 32 skipped / 0 failed
  at `ef59ca1`, with the arithmetic shown and any surprise explained rather than forced.
- The amendment, `occlusion_span_filter.py`, and the §6b adjudication card are provably
  unchanged.
- Nothing fired: zero OSF, AoU, VM, Dataproc or `gsutil` contact.
- Branch pushed; `git status -sb` shows no `ahead`.
</success_criteria>

<output>
After completion, create
`.planning/quick/260821-x91-remediation-batch-two-condition-occlusio/260821-x91-SUMMARY.md`
(written in T5, committed with T5's explicit-path commit).
</output>
</content>
</invoke>
