---
phase: quick/260806-pd3
plan: 01
type: execute
wave: 1
depends_on: [quick/260805-o7o, quick/260805-w7u, quick/260806-b77]
subsystem: m3-ld-read-path
tags: [ld-panel, finemap, freeze, blast-radius, provenance, receipt, m3]
autonomous: true
requirements: [BR-K1]
baseline_rev: 63453db
files_modified:
  - src/legacy/region_analysis/scripts/run_susie_rss.R
  - tests/m3/test_ld_read_path.py
  - tests/m3/test_variant_catalog_fallback_legacy_semantics.py
  - src/snakemake/rules/finemap.smk
  - tests/m3/test_finemap_receipt_early_exit.py
  - tests/m3/test_qtl_coloc_allele_join.py
  - src/snakemake/scripts/ld_allele_join.R
  - .planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md
user_setup: []

must_haves:
  truths:
    - "The Path-2 (ld_overlap == 0) branch no longer sets variant_catalog_fallback; the key is assigned TRUE at exactly ONE site in the whole file -- the Path-1 AFR empty-subset revert."
    - "Path-2's SCIENCE behaviour is byte-unchanged: the code-only lines of the branch at HEAD equal dc4bbd2's code-only lines MINUS exactly the one deleted line, and nothing else."
    - "The five MUST-NOT-MOVE sites (init FALSE, the Path-1 mutation, the two early-exit emits, the success emit) are byte-identical to dc4bbd2, asserted per-symbol rather than by a whole-file pin."
    - "Re-inserting the deleted line -- in an in-memory copy of today's source, or in the dc4bbd2 revision -- turns the new assertion RED, proven by two PERMANENT in-suite controls."
    - "The receipt's variant_catalog_fallback_cause decoder still tells the truth after the change: a post-K-1 Path-2 revert renders its OWN token, distinct from the m3-04c-window token and from 'none'."
    - "run_susie_rss.R is re-frozen at commit 1's SHA and every FREEZE pin names that SHA; every DIFFERENTIAL-SUBSTRATE reference to dc4bbd2 is deliberately left as dc4bbd2."
    - "The ld_allele_join.R extraction is DEFERRED in writing, with the helper-usage question ANSWERED and freeze economy recorded as insufficient justification for fire-path risk."
    - "Both suites are green -- tests/m3 0 failed / skipped == 31 / passed strictly greater than 807, and tests/phase2 136/1/0 -- MEASURED AFTER commit 2, never from the intermediate state."
  artifacts:
    - path: "src/legacy/region_analysis/scripts/run_susie_rss.R"
      provides: "the Path-2 branch with the legacy-key overload removed; ld_overlap_zero_fallback remains the Path-2 discriminator"
      contains: "ld_overlap_zero_fallback <- TRUE"
    - path: "tests/m3/test_variant_catalog_fallback_legacy_semantics.py"
      provides: "the K-1 semantics pin -- one-assignment-site, whole-block containment, five untouched sites, two permanent negative controls"
      contains: "PRE_K1_REF"
    - path: "tests/m3/test_ld_read_path.py"
      provides: "AUTH-K1-TEST -- the parity assertion replaced by its strengthened inverse"
      contains: "not in branch"
    - path: "src/snakemake/rules/finemap.smk"
      provides: "a five-outcome variant_catalog_fallback_cause decoder that stays true after K-1"
      contains: "path2_ld_overlap_zero_RETRY"
    - path: ".planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md"
      provides: "K-1 marked CLOSED with the applied commit and the original surface preserved; the ld_allele_join.R extraction registered as DEFERRED with reasons"
      contains: "K-1"
  key_links:
    - from: "src/legacy/region_analysis/scripts/run_susie_rss.R Path-2 branch"
      to: "tests/m3/test_variant_catalog_fallback_legacy_semantics.py"
      via: "brace-walk of the live source AND of git show dc4bbd2, compared line-for-line"
      pattern: "if \\(ld_overlap == 0 && used_variant_catalog && attempt == 1\\)"
    - from: "tests/m3/test_finemap_receipt_early_exit.py FROZEN_R_REV"
      to: "commit 1's SHA"
      via: "the re-pin, applied in commit 2 because the SHA does not exist until commit 1 lands"
      pattern: "FROZEN_R_REV = "
    - from: "src/snakemake/rules/finemap.smk receipt line"
      to: "the five variant_catalog_fallback_cause tokens"
      via: "extraction of the live PYTHON_BIN -c program, executed on JSON fixtures"
      pattern: "variant_catalog_fallback_cause"
---

<objective>
Close blast-radius finding **K-1** by restoring `variant_catalog_fallback`'s **legacy
semantics**: delete the single line that made the Path-2 (`ld_overlap == 0`) retry overload a
**pre-existing** key, so the key means again exactly what every legacy region JSON means by it
-- "the Path-1 AFR variant-catalog empty-subset revert fired".

**Purpose.** A pre-existing key that silently changes meaning forces every future automated
before/after JSON comparison to special-case it, and produces a `false -> true` flip that a
reader *will* chase. `260806-b77` mitigated this with a decoder ring; it did not close it.
Carter authorized **option B** on 2026-08-06.

**Output.** One line deleted from a frozen R file, one comment reworded, one pre-existing
assertion **strengthened**, one NEW test module, a decoder-ring coherence fix, and a complete
re-pin of the freeze at the new SHA. `$0`, NC State only, zero perimeter contact, **no AoU
fire**.

## THE HONEST ACCOUNTING -- read before reporting anything

| Gate row (`m3-04c-BLAST-RADIUS.md:133-144`) | Before | After this plan |
|---|---|---|
| **Publishing the panel provenance** (I, J, K) | PARTIAL -- I and J closed, **K DEFERRED** | **CLEARED** -- K-1 closed here |

**This is the only gate row this plan touches.** It does not re-open A-J, L, M, E-2 or G-2.
**No number moves**: no PIP, credible set, `ld_overlap`, `ld_status` or
`d3b_ld_z_consistency_s` depends on this flag, and that is **asserted mechanically** (T1 STEP
3d/3f), not assumed. Track A is untouched.

## WHAT THIS CHANGE IS AND IS NOT

It is a **reporting-flag** change. The Path-2 branch still reverts to `subset_base`, still
retries exactly once, and still records itself via `ld_overlap_zero_fallback` -- which is still
emitted in the success JSON and still read by the `finemap.smk` receipt. **Nothing becomes
invisible.** Only the *legacy half* of the pair stops being overloaded.

It is also **INERT on today's artifacts**. Measured during planning: **44** region JSONs on this
node carry `variant_catalog_fallback`, **all 44 are `false`**, and **0** carry
`ld_overlap_zero_fallback` at all. So no artifact has ever been produced from a tree in the
m3-04c window, and **no before/after JSON diff can be run here**. Say that plainly in the
SUMMARY -- do not imply an artifact-level verification that was not performed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md
@.planning/quick/260806-b77-close-the-remaining-blast-radius-finding/260806-b77-PLAN.md
@src/legacy/region_analysis/scripts/run_susie_rss.R
@src/snakemake/rules/finemap.smk
@tests/m3/test_ld_read_path.py
@tests/m3/test_finemap_receipt_early_exit.py
@tests/m3/test_qtl_coloc_allele_join.py
@src/snakemake/scripts/ld_allele_join.R
</context>

<authorizations>
Granted by Carter 2026-08-06 ("Let's proceed with your recommendation --auto --chain"; the
recorded recommendation for K-1 was **option B**). **Strictly scoped. Anything outside these
bounds is a STOP-and-surface, not a judgement call.**

**AUTH-K1-UNFREEZE** -- an unfreeze of `src/legacy/region_analysis/scripts/run_susie_rss.R` off
`dc4bbd2`, scoped to **deleting the single line `variant_catalog_fallback <- TRUE` inside the
Path-2 brace block** plus **rewording the comment immediately above it**. **SINGLE-USE**, with a
re-pin obligation at the new SHA. `:787`, `:916`, `:936`, `:968`, `:1208` MUST NOT MOVE.
`ld_overlap_zero_fallback <- TRUE`, `subset <- copy(subset_base)` and `attempt <- attempt + 1`
MUST STAY.

**AUTH-K1-TEST** -- edit
`tests/m3/test_ld_read_path.py::test_path2_ld_overlap_zero_fallback_is_observable_and_read` at
`:451-454` only, in the STRENGTHENING direction given verbatim in `deferred-items.md` section 4,
plus the narrative that frames the withdrawn parity as intended design.
**SPAN CORRECTION, re-derived at HEAD.** The grant text says `:451-453`, but the assertion is a
FOUR-line statement: `:451` `assert "variant_catalog_fallback <- TRUE" in branch, (`, `:452-453`
the message, **`:454` the closing `)`**. Replacing only `:451-453` leaves a dangling paren -- it
fails loud (`SyntaxError` at collection), but it is a plan-fact error either way. **The
authorized subject is the whole statement, `:451-454`.** Report the correction.

**AUTH-K1-REPIN (derived, narrow).** The re-pin obligation inside AUTH-K1-UNFREEZE necessarily
touches pre-existing test files that hold the freeze constant. This authorization is scoped to
**changing the SHA string in FREEZE pins and the prose that names them** -- and, in
`test_finemap_receipt_early_exit.py` only, the decoder-ring block that
`<consistency_question>` requires. **No other assertion in those files may change; the ADDITIONS
enumerated in T2 STEP 5's allow-list (`PRE_K1_SMK_REF`, `CAUSE_TOKENS`, the token-distinctness
test, NC-K5, NC-K6) are INSIDE this grant.** Proven mechanically in T2 STEP 5.

**NOT AUTHORIZED, and each is a STOP:** any other edit to `run_susie_rss.R`; any other
pre-existing test edit; the `ld_allele_join.R` extraction (see `<the_deferral>`); touching
`plink_ld_to_npz.py`, `ld_npz_to_rds.R`, `condition_ld_matrix.py` or the m3-07 occlusion
modules; m3-06 (stays HELD -- no NaN-to-0, no `condition_ld_matrix.py`).
</authorizations>

<verified_anchors>
**Every anchor below was RE-DERIVED at HEAD `63453db` during planning. All of the brief's line
numbers are LIVE -- none is stale.** Re-derive them again before editing anyway; if any has
moved, that is a deviation to report, not to absorb.

| Anchor | Live at `63453db` | Fact |
|---|---|---|
| `run_susie_rss.R:787` | `variant_catalog_fallback <- FALSE` | init. MUST NOT MOVE |
| `run_susie_rss.R:788` | `ld_overlap_zero_fallback <- FALSE  # m3-04c HIGH-2: ...` | init. MUST NOT MOVE |
| `run_susie_rss.R:904-917` | a multi-line `if (` on `nrow(subset) == 0 && variant_catalog_attempted && ancestry_upper == "AFR" && ...` | **Path 1**; sets the key at `:916`. MUST NOT MOVE |
| `run_susie_rss.R:936`, `:968` | `variant_catalog_fallback = variant_catalog_fallback` | the two early-exit emits. MUST NOT MOVE |
| `run_susie_rss.R:1003-1017` | `if (ld_overlap == 0 && used_variant_catalog && attempt == 1) {` | **Path 2**; `:1007-1012` comment, **`:1013` THE LINE TO DELETE**, `:1014` `ld_overlap_zero_fallback <- TRUE`, `:1015` `attempt <- attempt + 1` |
| `run_susie_rss.R:1208-1209` | `variant_catalog_fallback = ...,` / `ld_overlap_zero_fallback = ...,` | the success emit. MUST NOT MOVE |
| `tests/m3/test_ld_read_path.py:449` | `# the Path-2 branch sets BOTH` | a COMMENT that becomes FALSE. **The brief said `:449-450` "stay"; `:449` is this comment, not a docstring** -- reword it. Report as a plan-fact correction. |
| `tests/m3/test_ld_read_path.py:450` | `branch = _brace_block(src, "if (ld_overlap == 0 && used_variant_catalog && attempt == 1)")` | the brace-walk anchor. **Byte-unchanged.** |
| `tests/m3/test_ld_read_path.py:451-454` | the parity assertion: `:451` the `assert ... in branch, (`, `:452-453` the message, **`:454` the closing `)`** | **the AUTH-K1-TEST subject.** The brief said `:451-453`; the live span is FOUR lines, so a 451-453 replacement leaves a dangling paren. **Corrected here -- report it.** |
| `tests/m3/test_ld_read_path.py:117-146` | `_brace_block` / `_paren_block` | reusable; anchor then first `{` then balanced walk |

**Uniqueness, measured (all == 1, so every brace-walk lands on exactly one block):**
`ancestry_upper == "AFR" &&`, `if (nrow(subset) == 0)`,
`if (ld_overlap == 0 && used_variant_catalog && attempt == 1)`,
`variant_catalog_fallback <- FALSE`, `ld_overlap_zero_fallback <- TRUE`,
`Variant catalog removed all SNPs for AFR region`.
**`variant_catalog_fallback <- TRUE` occurs 2 times today and MUST occur exactly 1 after.**
Note `if (nrow(subset) == 0)` occurs once because Path 1 spells it across newlines -- the K-1
edit must not change that count (`test_finemap_receipt_early_exit.py:315` asserts it).

**Every `dc4bbd2` occurrence outside `.planning/`, measured (8 hits, 4 files):**

| Site | Kind | Action in commit 2 |
|---|---|---|
| `tests/m3/test_finemap_receipt_early_exit.py:55` `FROZEN_R_REV` | **FREEZE pin** (consumed at `:334`, `:344`) | **RE-PIN** to new SHA |
| `tests/m3/test_finemap_receipt_early_exit.py:14` | live prose naming the pin | **RE-PIN** |
| `tests/m3/test_qtl_coloc_allele_join.py:106` `FREEZE_REF` | **FREEZE pin** (consumed at `:728`, `:752`, `:1408`) | **RE-PIN** |
| `tests/m3/test_qtl_coloc_allele_join.py:57` | live prose ("stays 0-diff vs ... throughout") | **RE-PIN** |
| `tests/m3/test_qtl_coloc_allele_join.py:1296` | **HISTORICAL** narrative inside AUTH-b77-01's preserved record | **ANNOTATE, DO NOT REWRITE** -- T2 STEP 3 |
| `src/snakemake/scripts/ld_allele_join.R:33`, `:58` | live prose: the current pin + the extraction's containment baseline | **RE-PIN** |
| `src/snakemake/rules/finemap.smk:498` | live prose naming the pin, inside a historical J narrative | **RE-PIN the pin, preserve the J claim** |

**FOUR orchestrator-owned planning docs also assert `dc4bbd2` as the live freeze** (measured
during planning): `.planning/STATE.md:55`; `.planning/HANDOFF.json:75`, `:111` and `:117` (the
`freeze_state` field); and
`.planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md:25`. **DO NOT WRITE ANY OF THEM**
(hard rule 10) -- none is in `files_modified`, so editing them would break T3 STEP 4's scope-diff
gate. They are handed to the orchestrator as the stale-freeze block in T3 STEP 5 item 9. The K-1
spec's "replace it everywhere it is asserted" is satisfied **code-side by commit 2 and docs-side
by the orchestrator's commit** -- say so, rather than implying commit 2 covered both.

**Environment, confirmed present:**
`/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python`,
`/rs1/researchers/c/ckclinto/conda_envs/m3-r-ld/bin/Rscript`.
`Rscript -e 'parse("run_susie_rss.R")'` prints `PARSE_OK` at baseline.

**Baseline, measured:** `tests/m3` collects **838** = 807 passed + 31 skipped.
`tests/phase2` = 136 passed / 1 skipped / 0 failed. `run_susie_rss.R` is currently **0-diff vs
`dc4bbd2`**. Branch `m3-W2-aou-deltas`, HEAD `63453db`.

**Blast-radius survey, measured.** The ONLY consumers of `variant_catalog_fallback` anywhere in
`src/`, `tests/`, `config/`, `Snakefile` are: the six R sites, the `finemap.smk` receipt (plus
its comments), `tests/m3/test_ld_read_path.py:437-476`, and
`tests/m3/test_finemap_receipt_early_exit.py` (fixtures + the `d.get()` pin at `:528`).
**No Python or R aggregator reads it.** The four other modules that `git show <ref>` the R file
(`test_ld_allele_aware_join.py:197`, `test_ld_declared_authoritative.py:215` / `:632`,
`test_ld_allele_aware_wiring.py:359`) use their OWN pre-change substrates and assert on the
allele-join / declared-authoritative wiring -- **none touches the Path-2 branch.**
`_loader_functions_only()` cuts the source at `option_list <-`, far above `:1013`, so **no
behavioural R test's substrate changes.**
</verified_anchors>

<the_commit_cascade>
**The re-pin CANNOT share a commit with the edit: the new SHA does not exist until the edit is
committed.** The sequence is mandatory.

* **Commit 1 (T1)** = the R one-line deletion + comment reword + the AUTH-K1-TEST strengthening
  + the NEW test module.
* **Commit 2 (T2)** = re-pin every FREEZE assertion at commit 1's SHA + the decoder-ring
  coherence fix + the registry updates.

**BETWEEN THE TWO COMMITS THE FREEZE GATES ARE RED, BY DESIGN.** Expect exactly these to fail
after commit 1 and to go green again at commit 2:

* `test_finemap_receipt_early_exit.py::test_run_susie_rss_r_is_still_frozen_at_its_pin`
* `test_finemap_receipt_early_exit.py::test_the_frozen_early_exits_still_emit_no_ld_field` (2 params)
* `test_qtl_coloc_allele_join.py::test_run_susie_rss_is_zero_diff_vs_the_freeze`
* the two mid-control freeze assertions in `test_qtl_coloc_allele_join.py` (`:728`, `:752`)

**T1's verify commands deliberately exclude those modules.** **NO SUITE NUMBER MAY BE REPORTED
FROM THE INTERMEDIATE STATE.** The only numbers that count are T3's, measured after commit 2. If
T1 must be abandoned mid-flight, `git revert` commit 1 rather than leaving the tree between the
two.

**The decoder-ring work is placed in commit 2 on purpose**: it lives in
`test_finemap_receipt_early_exit.py`, which commit 2 must touch anyway, so this avoids editing
one pre-existing test file across two commits. It is verified SAFE to defer: the decoder's four
parametrised cases are driven by JSON **fixtures**, not by the R source, so they stay GREEN
after commit 1. The incoherence commit 2 fixes is semantic, not a red test.
</the_commit_cascade>

<consistency_question>
**THE HIGHEST-RISK PART OF THIS TASK. DO NOT SKIP IT.**

`260806-b77` landed a runtime decoder token, `variant_catalog_fallback_cause`, in the
`finemap.smk` receipt (`:540`):

```python
vcp=('variant_catalog_fallback' in d); vcv=d.get('variant_catalog_fallback'); ozv=d.get('ld_overlap_zero_fallback')
cause=('key_absent' if not vcp else ('none' if not vcv else ('path2_ld_overlap_zero_NO_NUMERIC_CAUSE' if ozv else 'path1_variant_catalog_empty_subset')))
```

**After K-1 a real Path-2 revert emits `variant_catalog_fallback: false` +
`ld_overlap_zero_fallback: true`, so the decoder renders `none`. That is FALSE: Path 2 DID
fire.** The decoder must gain a fifth outcome.

| `variant_catalog_fallback` | `ld_overlap_zero_fallback` | token | meaning after K-1 |
|---|---|---|---|
| key absent | -- | `key_absent` | a JSON older than the key itself |
| `false` | `false` / absent | `none` | neither revert fired. **This is what the 44 on-disk legacy JSONs render** |
| `false` | `true` | **`path2_ld_overlap_zero_RETRY`** (NEW) | the post-K-1 canonical Path-2 signature |
| `true` | `true` | `path2_ld_overlap_zero_NO_NUMERIC_CAUSE` | **now UNREACHABLE from any tree at or after commit 1.** Retained as a **forensic marker**: it dates an artifact to the m3-04c window. 0 such artifacts exist on this node |
| `true` | `false` | `path1_variant_catalog_empty_subset` | the key's restored, and now ONLY, meaning |

**A factual error to fix while here (a comment, never an assertion).**
`test_finemap_receipt_early_exit.py:488` annotates the `key_absent` case with
`# all 1,957 legacy JSONs pre-date it`. That is **wrong**: the legacy JSONs **do** carry
`variant_catalog_fallback: false` (measured here: 44/44), so they render **`none`**, not
`key_absent`. Correct the comment and cite the measurement. Fixing a wrong comment is not an
authorization question; changing an assertion would be.

**Prefix-collision trap.** `assert "variant_catalog_fallback_cause path2_ld_overlap_zero" in out`
would match BOTH `..._RETRY` and `..._NO_NUMERIC_CAUSE`. Design around it TWICE: (1) the new
token is deliberately NOT a prefix of the old one and vice versa; and (2) the assertion matches
a delimiter, e.g. `re.search(rf"variant_catalog_fallback_cause {re.escape(expected)}(\s|$)", out)`.
Add a permanent test asserting the five tokens are pairwise distinct **and** that none is a
prefix of another.
</consistency_question>

<the_deferral>
## The `ld_allele_join.R` extraction: **DEFER**. Evaluated on merits, not accepted on assertion.

Replacing the nested closure `match_indices_allele_aware` (`run_susie_rss.R:220-323`) with
`source("src/snakemake/scripts/ld_allele_join.R")` was proposed for this same unfreeze window on
**freeze-economy** grounds. Verified during planning:

1. **VERIFIED, and decisive.** `run_susie_rss.R` contains **ZERO `source()` calls today**
   (`grep -c "source(" -> 0`). The extraction would introduce a **first-of-its-kind runtime file
   dependency** on the exact code path the ~11-day / $385-1,084 AoU fire exercises. A failed
   `source()` at fire time is a catastrophic, expensive failure mode **that does not exist
   today**.
2. **VERIFIED.** The duplication is ALREADY drift-guarded on **every suite run** by
   `tests/m3/test_qtl_coloc_allele_join.py`'s differential agreement test plus NC-2f/NC-2g
   (`:715-755`), which body-walk the SHIPPED closure out of the real source. **The benefit is
   style, not safety.**
3. **VERIFIED, and the brief's premise was WRONG -- report this as a plan-fact correction.**
   The brief states `run_qtl_coloc.R:164` solves path resolution "with a `--ld-allele-join` CLI
   arg threaded from `qtl_coloc.smk`". It does not. `--ld-allele-join` (`run_qtl_coloc.R:62`) is
   a **boolean flag**; the PATH is resolved **script-relatively** at `:153`
   (`file.path(.script_dir(), "ld_allele_join.R")`). `run_susie_rss.R` lives in
   `src/legacy/region_analysis/scripts/`, so `.script_dir()` would **not** find the shared file
   -- a genuinely NEW path mechanism (a new CLI argument threaded from `finemap.smk`, or a
   repo-root walk) would be required. **That is wider than the brief assumed, not narrower.**
4. **ANSWERED.** `.up`, `.usable`, `.allele_counts0` (`:210-218`) are referenced ONLY at `:223`
   and `:237-249`, all inside the closure's own body, with the closure's sole call site at
   `:332`. **Nothing else in `load_ld_matrix` uses them**, so removal is technically feasible.

**Verdict: DEFER.** Point 4 removes an objection but supplies no justification; points 1 and 3
stand. **Freeze economy is NOT sufficient justification to accept fire-path risk.** Write that
sentence, verbatim in substance, into `deferred-items.md` (T2 STEP 4), together with the three
conditions any future attempt must satisfy: a **fail-CLOSED-and-LOUD** design (a missing or
unsourceable shared file must STOP with a named error and must **never** degrade to a
position-only match -- that degradation **IS finding H**); an
`identical()`-on-the-whole-`load_ld_matrix`-result proof at `allele_aware` **TRUE and FALSE**
against the new pin; and a re-freeze re-pin.
</the_deferral>

<hard_rules>
1. **A GREEN ASSERTION IS EVIDENCE ONLY IF YOU HAVE SEEN IT FAIL.** Every new or changed
   assertion ships with a negative control **OBSERVED RED** and quoted raw in the SUMMARY. This
   project has been bitten **seven** times by assertions structurally incapable of their job.
2. **THE `.pyc` TRAP.** `importlib` validates cached bytecode on `(mtime_seconds, size)`, so a
   byte-length-identical edit reverted within the same second runs **stale bytecode**. **Every
   negative control in this plan perturbs an IN-MEMORY string or reads a past revision via
   `git show` -- none reverts a file on disk and re-imports.** This is not optional shaping; it
   is the design.
3. **NO FIXED-SHA WHOLE-FILE PINS.** `git diff <FIXED-SHA> HEAD -- file == ""` is green once and
   red forever. Every containment assertion in the new module is **SYMBOL-scoped** (a named
   brace block, a named `result <- list(...)` payload, or diff lines mentioning a named symbol),
   so a future authorized unfreeze produces a message naming WHICH block moved. The plain
   `git diff --exit-code <new-sha>` gate stays ONLY where it belongs -- the two freeze-gate
   tests -- which is its intended behaviour.
4. **FREEZE pins move; DIFFERENTIAL SUBSTRATES DO NOT.** `PRE_K1_REF = "dc4bbd2"` in the NEW
   module is a historical substrate, exactly like b77's `PRE_CHANGE_REF`. **It stays `dc4bbd2`
   forever.** Only `FROZEN_R_REV` and `FREEZE_REF` are re-pinned. Getting this backwards
   destroys both negative controls.
5. **SKIPS ARE FIXED AT 31 AND 1.** A new test landing as a SKIP is **not evidence**. The new
   module must be pure source-text + `git` -- **no R execution, no Snakemake, no toolchain
   fixture** -- so it structurally cannot skip. Net test count may only go UP.
6. **Environment.** Never `conda activate`. Python
   `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python`; Snakemake
   `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/snakemake`; Rscript
   `/rs1/researchers/c/ckclinto/conda_envs/m3-r-ld/bin/Rscript`.
7. **Git:** explicit paths only. **NEVER `git add .` or `git add -A`** (multi-terminal GPFS
   tree). No worktree isolation.
8. **NEVER trigger the AoU fire.** No `gsutil` / `gcloud` / `bq` / `wb` / `dataproc` / `hailctl`
   anywhere in the session or the diff. Grep the diff before each commit.
9. `tests/m3/sparse_parent_benchmark.tsv` is rewritten by the suite on every run (timing
   jitter). Restore with `git checkout --`. **Never commit it.**
10. **Do NOT write** `.planning/STATE.md`, `.planning/HANDOFF.json`,
    `.planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md`, `ROADMAP.md`, or any
    PLAN doc -- **all are orchestrator-owned** and ride in the docs commit. (The SUMMARY **is**
    WRITTEN per `<output>` but is **NOT committed** -- the orchestrator commits it.)
    `deferred-items.md` **is** in scope and rides in commit 2.
    ⚠ Three of these carry a STALE `dc4bbd2` freeze claim (`HANDOFF.json:75`, `:111`, `:117`
    `freeze_state`; `.continue-here.md:25`; `STATE.md:55`). **Editing them would ALSO break T3
    STEP 4's scope-diff gate**, since they are not in `files_modified`. **Hand them to the
    orchestrator via T3 STEP 5 item 9 -- do not touch them.**
11. **ANY unexpected red pre-existing test is a STOP-and-surface**, never an edit and never a
    weakened assertion. The three authorizations above are the complete list.
</hard_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (COMMIT 1): delete the Path-2 overload, strengthen the assertion that mandated it, pin the new semantics both ways</name>
  <files>src/legacy/region_analysis/scripts/run_susie_rss.R, tests/m3/test_ld_read_path.py, tests/m3/test_variant_catalog_fallback_legacy_semantics.py</files>

  <behavior>
  - `variant_catalog_fallback <- TRUE` occurs **exactly once** in `run_susie_rss.R`, and that
    occurrence is inside the **Path-1** block, not the Path-2 block. At `dc4bbd2` it occurs
    **twice** and IS inside the Path-2 block -- asserted as a permanent differential.
  - The Path-2 block's **code-only** lines at HEAD equal `dc4bbd2`'s code-only lines **minus
    exactly** `variant_catalog_fallback <- TRUE`, and nothing else -- the mechanical form of
    "no number moves".
  - The five MUST-NOT-MOVE regions are **byte-identical** to `dc4bbd2`, each named individually.
  - Splicing the deleted line back into an **in-memory** copy of today's source turns the K-1
    predicate RED, and the on-disk file is proven byte-unchanged after the control ran.
  - The strengthened `test_ld_read_path.py` assertion still requires
    `ld_overlap_zero_fallback <- TRUE`, `subset <- copy(subset_base)` and
    `attempt <- attempt + 1` in the Path-2 block, and the brace-walk anchor is byte-unchanged.
  - `Rscript -e 'parse(...)'` on the edited file prints `PARSE_OK`.
  </behavior>

  <action>
**STEP 0 -- RE-DERIVE BEFORE TOUCHING ANYTHING.** Confirm at HEAD: `git rev-parse --short HEAD`
is `63453db`; `git diff --exit-code dc4bbd2 -- src/legacy/region_analysis/scripts/run_susie_rss.R`
is clean; the six `variant_catalog_fallback` sites are at `:787 / :916 / :936 / :968 / :1013 /
:1208`; `src.count("variant_catalog_fallback <- TRUE") == 2`; `test_ld_read_path.py:449-454` is
as quoted in `<verified_anchors>`. **Any mismatch is a deviation to REPORT, then re-anchor to the
live text -- never edit by line number against a stale map**
(`[[feedback_check_plan_against_red_before_executing]]`).

**STEP 1 -- THE R EDIT. One line deleted, one comment reworded. Nothing else.**
Inside the Path-2 brace block (`if (ld_overlap == 0 && used_variant_catalog && attempt == 1)`):

* **DELETE** the single line `    variant_catalog_fallback <- TRUE` (currently `:1013`).
* **REWORD** the comment above it (currently `:1007-1012`), which narrates the parity now being
  withdrawn. The replacement must say, in substance:
  * this revert is Path 2 (`ld_overlap == 0`), distinguished by `ld_overlap_zero_fallback`
    **alone**;
  * it deliberately does **NOT** set `variant_catalog_fallback` -- that key is **pre-existing**
    and its meaning is the **Path-1** AFR variant-catalog empty-subset revert, which every
    legacy region JSON records as `false` (**44/44 measured on this node**);
  * **science behaviour is UNCHANGED**: still one retry against `subset_base`;
  * cite **K-1** in `.planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md` and
    **AUTH-K1-UNFREEZE**, and state the unfreeze is now **SPENT**.

**KEEP, untouched:** `subset <- copy(subset_base)`, `used_variant_catalog <- FALSE`,
`ld_overlap_zero_fallback <- TRUE`, `attempt <- attempt + 1`, `next`.
**DO NOT TOUCH** `:787`, `:788`, `:916`, `:936`, `:968`, `:1208`, `:1209`, or any other line.
Then run
`/rs1/researchers/c/ckclinto/conda_envs/m3-r-ld/bin/Rscript -e 'invisible(parse("src/legacy/region_analysis/scripts/run_susie_rss.R")); cat("PARSE_OK\n")'`.

**STEP 2 -- AUTH-K1-TEST: strengthen `tests/m3/test_ld_read_path.py`. Minimal, surgical.**
Exactly three changes inside
`test_path2_ld_overlap_zero_fallback_is_observable_and_read`, and **nothing else in the file**:

  a. Replace the WHOLE four-line statement at `:451-454` -- **including its closing `)` at
     `:454`** -- with the form given verbatim in `deferred-items.md` section 4. Replacing only
     `:451-453` leaves a dangling paren:

```python
    assert "variant_catalog_fallback <- TRUE" not in branch, (
        "Path 2 must NOT set the legacy variant_catalog_fallback key (K-1): it is a "
        "pre-existing key whose meaning is the Path-1 AFR empty-subset revert"
    )
```

  b. Reword the `:449` comment `# the Path-2 branch sets BOTH` -- it becomes false. Use e.g.
     `# the Path-2 branch records itself with ld_overlap_zero_fallback ONLY (K-1)`.
     **The `:450` `_brace_block(...)` call, including its anchor string, stays BYTE-IDENTICAL.**
  c. Update the test's docstring so it no longer presents the parity as the intended design: add
     a short **K-1** paragraph stating that Path-2 parity was **WITHDRAWN** on 2026-08-06 under
     AUTH-K1-TEST, that `ld_overlap_zero_fallback` is now the sole Path-2 discriminator, and that
     the assertion is pinned in **both** directions. **KEEP** the `HONEST LIMITATION` paragraph,
     the "science behaviour is deliberately NOT asserted" paragraph, and the Path-1 narrative.

  **KEEP untouched** in that test: the two init assertions, the `init_gap` parity assertion
  (init parity at `:787-788` is **NOT** withdrawn -- both flags are still initialized `FALSE`
  together), the `ld_overlap_zero_fallback <- TRUE` assertion, the two science-behaviour
  assertions, the success-JSON assertions, and the `smk_shell` assertions.

  **Containment proof (run it, quote it):** `git diff HEAD -- tests/m3/test_ld_read_path.py`
  must touch **only** lines inside that one function, and
  `grep -c "^def test_" tests/m3/test_ld_read_path.py` must be unchanged from HEAD's value.

**STEP 3 -- NEW module `tests/m3/test_variant_catalog_fallback_legacy_semantics.py`.**
Pure source-text + `git`. **No R, no Snakemake, no toolchain fixture** -- it must be structurally
incapable of skipping (hard rule 5). Reuse `_brace_block` / `_paren_block` /
`_success_result_block` by importing from the sibling `test_ld_read_path` module (the established
`tests/m3` pattern: `sys.path.insert` on `Path(__file__).resolve().parent`), so the walker under
test is the **same** walker the authorized assertion uses.

```python
#: The revision that carried the m3-04c Path-2 overload. A DIFFERENTIAL SUBSTRATE,
#: not a freeze pin: it MUST stay dc4bbd2 forever, even after the re-pin.
PRE_K1_REF = "dc4bbd2"
```

  a. **`test_the_legacy_key_is_assigned_at_exactly_one_site`.**
     `src.count("variant_catalog_fallback <- TRUE") == 1`; that occurrence is inside
     `_brace_block(src, 'ancestry_upper == "AFR" &&')` (Path 1) and is **NOT** inside the Path-2
     block. **Non-vacuity:** both anchors occur exactly once, both blocks are non-empty, and each
     contains a second expected marker (`subset <- copy(subset_base)`).
  b. **NC-K1 -- PERMANENT, DIFFERENTIAL, no revert.** Run the identical walk over
     `git show dc4bbd2:src/legacy/region_analysis/scripts/run_susie_rss.R`: assert the count is
     **2** and that the Path-2 block **DOES** contain the line there. This proves the walker can
     see the string when it is present, and that the change is real. The failure message must
     name both possibilities: "the extractor is reading the wrong block, or nothing changed."
  c. **NC-K2 -- PERMANENT, IN-MEMORY.** Factor the K-1 predicate into a module-level helper
     `_assert_path2_does_not_set_the_legacy_key(text)`. Take the live `src`, splice
     `"    variant_catalog_fallback <- TRUE\n"` back in immediately above
     `"    ld_overlap_zero_fallback <- TRUE"` **in the string**, and assert the helper raises
     under `pytest.raises(AssertionError)`. **Then assert `SUSIE_R.read_text() == src`** -- the
     control never wrote the working tree. **This is the `.pyc`-safe shape: no file written, no
     module re-imported.**
  d. **`test_the_path2_block_lost_exactly_one_code_line_and_gained_none`** -- the load-bearing
     containment proof and the mechanical form of "no number moves". Compare the **code-only**
     lines (drop blanks; drop lines whose `lstrip()` starts with `#`) of the Path-2 block at HEAD
     versus at `PRE_K1_REF`:
     * `old_lines - new_lines == {"variant_catalog_fallback <- TRUE"}` (compare stripped);
     * `new_lines - old_lines == set()`;
     * and compare as an **ordered list** too, so a reordering also fails.
     **Non-vacuity:** `len(new_lines) >= 5` and the four survivors are individually present.
     **This is the TIGHTEST pin in the plan, so it carries the same maintenance contract as (e):
     if a future authorized unfreeze legitimately changes this block, UPDATE THE EXPECTED DELTA
     -- do not delete the test.** Put that sentence in the docstring.
  e. **`test_the_five_must_not_move_sites_are_byte_identical`** -- AUTH-K1-UNFREEZE's scope,
     enforced mechanically. `pytest.mark.parametrize` over five NAMED regions, each extracted by
     its own anchor from HEAD and from `PRE_K1_REF` and compared for **byte equality**: the init
     pair (the `variant_catalog_fallback <- FALSE` line plus the following
     `ld_overlap_zero_fallback <- FALSE` line), the Path-1 brace block, the `no_variants`
     `result <- list(...)` payload, the `too_many_variants` payload, and the success payload (via
     `_success_result_block`). Docstring must say: **if a future authorized unfreeze legitimately
     moves one of these, UPDATE THE NAMED REGION -- do not delete the test.**
  f. **`test_the_symbol_scoped_diff_is_exactly_one_removed_line`** -- the whole-file claim in a
     form that is **not** a timebomb. Take `git diff PRE_K1_REF HEAD -- <run_susie_rss.R>`, keep
     only `+`/`-` lines (excluding the `+++` / `---` headers) that mention
     `variant_catalog_fallback` **or** `ld_overlap_zero_fallback` **and** are not comments.
     **⚠ STRIP THE LEADING `+` / `-` BEFORE APPLYING THE COMMENT TEST.** `line.lstrip().startswith("#")`
     on a raw diff line is `False` for `-    # ... variant_catalog_fallback ...`, and STEP 1's
     reworded comment is GUARANTEED to mention the symbol -- so a naive filter leaks comment lines
     into the count and the "exactly one removed line" assertion fails for the wrong reason.
     Strip the marker first, then test:
     assert exactly **one** removed line (`variant_catalog_fallback <- TRUE`) and **zero** added
     lines. Because it is restricted to the two named symbols it survives a future unrelated
     unfreeze, unlike a whole-file pin. **Non-vacuity:** assert the unrestricted diff is
     non-empty, otherwise the filter proves nothing.
  g. **`test_ld_overlap_zero_fallback_is_still_the_path2_discriminator`** -- it is still
     initialized `FALSE`, still set `TRUE` in the Path-2 block, still emitted in the success
     `result <- list(...)`, and still read by the comment-stripped `finemap.smk` shell. **This is
     the "nothing becomes invisible" claim, asserted rather than argued.**

**STEP 4 -- MEASURE, DO NOT ASSUME, THE "NO NUMBER MOVES" CLAIM.** Before committing, run and
quote raw:
`grep -rn "variant_catalog_fallback" --include=*.py --include=*.R --include=*.smk --include=*.yaml src tests config Snakefile`
and confirm the consumer set is exactly the one in `<verified_anchors>`. Then re-run the on-disk
artifact census (`grep -rl "variant_catalog_fallback" --include=*.json .`, excluding `.git`) and
report the measured counts. **The K-1 entry's "1,957" figure is NOT reproducible on this node
(44 measured, all `false`, 0 with `ld_overlap_zero_fallback`). Report that as a DEVIATION, do not
repeat 1,957 as if verified, and do not claim any before/after JSON diff was performed.**

**NEGATIVE CONTROLS.** NC-K1 (b) and NC-K2 (c) are **PERMANENT and in-suite** -- they need no
revert and cannot decay into a claim. In addition, **observe RED once and quote raw**:
* **NC-K3 (the authorized assertion can fail).** Against the **in-memory** spliced source from
  (c), run the strengthened `test_ld_read_path.py` predicate and confirm it fails with the K-1
  rationale message. Drive the same `_brace_block` walk in a scratch snippet under the scratchpad
  dir -- **do NOT write the working tree** and do NOT rely on re-import.
* **NC-K4 (the count assertion can fail).** Confirm (a) goes RED against
  `git show PRE_K1_REF:...` (count 2, not 1). This falls out of (b); quote the message.

**COMMIT 1** -- explicit paths only:
`git add src/legacy/region_analysis/scripts/run_susie_rss.R tests/m3/test_ld_read_path.py tests/m3/test_variant_catalog_fallback_legacy_semantics.py`
Subject: `fix(260806-pd3-T1): stop Path 2 overloading the legacy variant_catalog_fallback key (FINDING K-1)`
Body records: AUTH-K1-UNFREEZE **SPENT**; AUTH-K1-TEST applied; the re-pin obligation
**OUTSTANDING until commit 2**; the freeze gates **expected RED until then**.
Then capture `NEW_SHA=$(git rev-parse --short HEAD)` -- T2 needs it.
  </action>

  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/m3-r-ld/bin/Rscript -e 'invisible(parse("src/legacy/region_analysis/scripts/run_susie_rss.R")); cat("PARSE_OK\n")'</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_variant_catalog_fallback_legacy_semantics.py tests/m3/test_ld_read_path.py tests/m3/test_ld_allele_aware_join.py tests/m3/test_ld_allele_aware_wiring.py tests/m3/test_ld_declared_authoritative.py -q -rs</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -c "import sys; from pathlib import Path; s=Path('src/legacy/region_analysis/scripts/run_susie_rss.R').read_text(); ok=(s.count('variant_catalog_fallback <- TRUE')==1 and s.count('variant_catalog_fallback <- FALSE')==1 and s.count('ld_overlap_zero_fallback <- TRUE')==1 and s.count('ld_overlap_zero_fallback <- FALSE')==1 and s.count('if (nrow(subset) == 0)')==1 and s.count('attempt <- attempt + 1')==1); print('SITE_COUNTS_OK', ok); sys.exit(0 if ok else 1)"</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && git diff 63453db HEAD --name-only -- src config tests | sort; echo "--- files under results/ or src/python/ (must be 0) ---"; git diff 63453db HEAD --name-only | grep -cE "^results/|^src/python/" || true</automated>
  </verify>

  <done>
  `run_susie_rss.R` parses (`PARSE_OK`); `variant_catalog_fallback <- TRUE` occurs exactly once
  and is inside the Path-1 block; the Path-2 block's code-only lines equal `dc4bbd2`'s minus
  exactly that line, ordered comparison included; the five MUST-NOT-MOVE regions are
  byte-identical; the AUTH-K1-TEST edit touched only the one function and the file's `def test_`
  count is unchanged; the NEW module passes with **zero skips**; NC-K1 and NC-K2 are permanent
  in-suite and NC-K3 / NC-K4 were **OBSERVED RED and quoted raw**; the artifact census was
  measured and the 1,957 discrepancy reported as a deviation; the scope diff lists exactly the
  three files and **zero** files under `results/` or `src/python/`. Commit 1 landed, SHA captured.
  **The freeze gates are RED at this point BY DESIGN -- report no suite numbers yet.**
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (COMMIT 2): re-pin the freeze at the new SHA, keep the fallback-cause decoder truthful, close the registries</name>
  <files>tests/m3/test_finemap_receipt_early_exit.py, tests/m3/test_qtl_coloc_allele_join.py, src/snakemake/scripts/ld_allele_join.R, src/snakemake/rules/finemap.smk, .planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md</files>

  <behavior>
  - Every **FREEZE** pin names commit 1's SHA; every remaining `dc4bbd2` hit is an annotated
    HISTORICAL narrative or a DIFFERENTIAL SUBSTRATE, classified in a table.
  - `git diff --exit-code <NEW_SHA> -- run_susie_rss.R` is clean and the freeze-gate tests are
    green again.
  - The receipt renders **five** distinct `variant_catalog_fallback_cause` tokens; the
    `(false, true)` post-K-1 Path-2 case renders `path2_ld_overlap_zero_RETRY`, **not** `none`.
  - No cause token is a prefix of another, and all five are distinct -- asserted permanently.
  - The `d.get('<key>')` literals, the single `{PYTHON_BIN}` line, and
    `--ld-allele-aware {params.ld_allele_aware}` all survive the receipt edit.
  </behavior>

  <action>
**STEP 0 -- RE-DERIVE THE RE-PIN SET YOURSELF.** `grep -rn "dc4bbd2" src tests config Snakefile
scripts` (and separately `.planning/`). **Do not trust the table in `<verified_anchors>` to be
complete** -- it was measured at `63453db`; measure again. Classify **each hit** as one of:
**FREEZE pin** (re-pin), **live prose naming the pin** (re-pin), **HISTORICAL narrative**
(annotate, do not rewrite), or **DIFFERENTIAL SUBSTRATE** (leave alone). Put the classification
table in the SUMMARY. **Misclassifying a substrate as a pin silently destroys a negative
control** (hard rule 4).

**⚠ SWEEP EVERY SHA LITERAL, NOT ONLY `dc4bbd2`.** A `dc4bbd2`-only grep is blind to the other
pins and substrates already living in these files. ⚠ **MEASURE, do not trust this list.** A sweep
of the four extant touched files at HEAD `63453db` returns **19 hits / 4 distinct SHAs**
(`dc4bbd2`x8, `7b1025d`x4, `6b427bc`x4, `3f431ab`x3) plus the `PRE_K1_SMK_REF` this plan adds.
`5ec33bd` and `0378ec8` are **NOT** in these files (they live in
`test_convert_aggregate_target.py` / `test_finemap_summary_panel_visible.py` /
`test_ld_allele_aware_wiring.py`) -- do not go hunting for them here. A match that is **not** a
git SHA is a false positive: ignore it, do not annotate. Run
`grep -rnE "\b[0-9a-f]{7}\b" tests/m3/test_finemap_receipt_early_exit.py tests/m3/test_qtl_coloc_allele_join.py tests/m3/test_variant_catalog_fallback_legacy_semantics.py src/snakemake/scripts/ld_allele_join.R src/snakemake/rules/finemap.smk`
and classify **every** literal into the same four buckets. **Only FREEZE pins move.** A SHA with
no `#:` annotation stating which bucket it is in is itself a finding -- annotate it.

**STEP 1 -- re-pin the two FREEZE constants.**
* `tests/m3/test_finemap_receipt_early_exit.py:55` becomes `FROZEN_R_REV = "<NEW_SHA>"`; update
  its `#:` comment to say the pin was re-set by `quick-260806-pd3` after K-1 and that the `pd3`
  unfreeze is **SPENT**. Update the module docstring at `:13-15` likewise.
* `tests/m3/test_qtl_coloc_allele_join.py:106` becomes `FREEZE_REF = "<NEW_SHA>"` with the same
  comment treatment; update the live prose at `:57`.
* **DO NOT** touch `PRE_CHANGE_REF` in either module (`6b427bc` / `7b1025d`) -- differential
  substrates.

**STEP 2 -- the decoder-ring coherence fix in `src/snakemake/rules/finemap.smk`.** Per
`<consistency_question>`. Edit **only** the `shell:` receipt line and the comments above it.
* In the `{PYTHON_BIN} -c` program, extend `cause` to five outcomes; keep `vcp` / `vcv` / `ozv`
  as they are and keep every `d.get('<key>')` literal:

```python
cause=('key_absent' if not vcp else ('path1_variant_catalog_empty_subset' if (vcv and not ozv) else ('path2_ld_overlap_zero_NO_NUMERIC_CAUSE' if (vcv and ozv) else ('path2_ld_overlap_zero_RETRY' if ozv else 'none'))))
```

* **Hard constraints, all from pre-existing tests, all re-verified before committing:** exactly
  **one** `{PYTHON_BIN}` line in the comment-stripped shell
  (`test_finemap_receipt_early_exit.py:534`); every `d.get('<key>')` literal preserved
  (`:520-531`, `test_ld_allele_aware_wiring.py:281-287`, `test_ld_read_path.py:472-476`);
  `--ld-allele-aware {params.ld_allele_aware}` exactly once; **no new `{PYTHON_BIN}` line before
  the receipt** (`test_ld_read_path_ancestry_gate.py::_rscript_argv`).
* **Rewrite the two now-false comment claims:**
  * `:464-466` -- "`variant_catalog_fallback` is set by **BOTH** reverts" is **FALSE after K-1**.
    It is now set by **Path 1 only**; `ld_overlap_zero_fallback` is the Path-2 discriminator and
    the pair is no longer overloaded.
  * `:513-539` -- the decoder-ring block: replace the "MITIGATION OF K, NOT ITS CLOSURE" framing
    with the **five-row** table from `<consistency_question>`, state that K-1 is **CLOSED** by
    `quick-260806-pd3`, and mark `path2_ld_overlap_zero_NO_NUMERIC_CAUSE` as a **forensic marker
    for the m3-04c window** that no tree at or after commit 1 can emit.
  * `:498` -- re-pin the SHA while **preserving** the historical claim that finding J's fix did
    not touch the R file.

**STEP 3 -- the test side of the decoder, in `tests/m3/test_finemap_receipt_early_exit.py`.**
* Add the **fifth** parametrised case `(False, True, "path2_ld_overlap_zero_RETRY")` with a
  comment naming it the post-K-1 canonical Path-2 signature. Re-narrate the `(True, True)` case
  as the m3-04c-window forensic marker.
* **Fix the factually wrong comment at `:488`** (`# all 1,957 legacy JSONs pre-date it`): the
  legacy JSONs **carry** the key as `false`, so they render **`none`**; cite the measured 44/44
  and 0. **Comment only -- the `(None, None, "key_absent")` assertion itself does not change.**
* **Kill the prefix-collision trap.** Change the assertion to a delimiter-aware match
  (`re.search(rf"variant_catalog_fallback_cause {re.escape(expected)}(\s|$)", out)`) and add
  `test_the_cause_tokens_are_distinct_and_none_is_a_prefix_of_another`.
  **⚠ THE TOKEN SET MUST BE DERIVED FROM LIVE SOURCE, NEVER HAND-TRANSCRIBED.** A module-level
  `CAUSE_TOKENS` literal is a test that agrees with itself: the only edit that turns it RED is
  editing the literal, so renaming `path2_ld_overlap_zero_RETRY` in `finemap.smk:540` would leave
  it GREEN. That is exactly what this module's own docstring forbids at `:31-33` ("EXTRACTED from
  the live `finemap.smk` ... never hand-copied -- the `260805-w7u` body-walk rule. A hand-copy
  would keep passing while the shipped line rotted") and the class named at
  `test_qtl_coloc_allele_join.py:54-55`. Required shape:
  * slice the `cause=(...)` expression out of `_receipt_program(FINEMAP_SMK.read_text())`;
  * recover the tokens with `re.findall(r"'([A-Za-z0-9_]+)'", cause_expr)`;
    ⚠ **The character class MUST admit `A-Z`.** Two of the five tokens carry uppercase runs
    (`path2_ld_overlap_zero_NO_NUMERIC_CAUSE`, `path2_ld_overlap_zero_RETRY`), and a
    lowercase-only class silently recovers **3**, not 5 -- MEASURED, not predicted. The failure
    is doubly nasty: the exactly-5 guard goes RED, and the cheapest local repair (relaxing 5
    to 3) leaves `recovered` holding precisely the two tokens the no-prefix property does NOT
    need and dropping the two it EXISTS for -- re-introducing the vacuity NC-K6 prevents.
    **Do NOT relax the guard; fix the class.**
  * **non-vacuity guard:** assert the slice was found and is non-empty, and that the recovered
    set has **exactly 5** members;
  * assert `set(CAUSE_TOKENS) == recovered` -- the literal survives only as documentation, and
    this equality is what makes a rename in the `.smk` turn the test RED;
  * run the distinct / no-prefix property over **`recovered`**, never over the literal.
* `tests/m3/test_qtl_coloc_allele_join.py:1296` is a **HISTORICAL** narrative inside AUTH-b77-01's
  preserved record. **Re-pinning it would falsify history.** Keep `dc4bbd2` and **annotate**:
  "(the pin in force at that time; re-pinned to `<NEW_SHA>` by `quick-260806-pd3`)". Same
  treatment anywhere else a hit is historical. **Report this as a correction to the brief's
  re-pin list.**

**STEP 4 -- registries.**
* `src/snakemake/scripts/ld_allele_join.R:33` and `:58` -- re-pin the SHA in the live prose, and
  in section **(d) THE NAMED FOLLOW-UP** record that the extraction was **EVALUATED AND DEFERRED**
  on 2026-08-06 by `quick-260806-pd3` **even though a freeze window was open**, with the four
  findings from `<the_deferral>` (including that `run_susie_rss.R` has **zero `source()` calls
  today**, and the correction that `run_qtl_coloc.R` resolves the path via `.script_dir()` at
  `:153` rather than via a CLI argument). Include, in substance: **"freeze economy is not
  sufficient justification to accept fire-path risk."**
* `.planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md` -- mark the **K-1** section
  **CLOSED**, mirroring exactly how AUTH-b77-01 was recorded at `:444`+: **preserve the original
  surface in full** under a `### THE ORIGINAL DEFERRAL, PRESERVED` heading, and prepend a closure
  header carrying the applied commit SHA, the date, both authorizations as GRANTED and SPENT, the
  new freeze pin, and a one-line statement that the gate row **"Publishing the panel provenance"
  is now CLEARED**. Append the `ld_allele_join.R` extraction as a **new, still-open** deferred
  entry with the four findings and the three conditions from `<the_deferral>`.

**STEP 5 -- PROVE THE RE-PIN WAS CONTAINED (AUTH-K1-REPIN's scope, mechanically).** For
`tests/m3/test_qtl_coloc_allele_join.py`, `src/snakemake/scripts/ld_allele_join.R` and
`src/snakemake/rules/finemap.smk:498`, run `git diff <NEW_SHA> -- <file>` and confirm every
changed line's only semantic delta is the SHA string or the annotation prose. For
`tests/m3/test_finemap_receipt_early_exit.py`, confirm the changed hunks are **exactly**: the
SHA, the `PRE_K1_SMK_REF` binding, the `CAUSE_TOKENS` binding, the module docstring, the decoder
parametrize block plus comments, the delimiter-aware assertion, the new token test, **and the two
new permanent controls NC-K5 and NC-K6**. **Those last three are ADDITIONS mandated by this plan's own STEP 3 and
NEGATIVE CONTROLS sections -- they are inside the allow-list, not a scope breach.** Dropping them
to satisfy a narrower reading would delete the plan's ONLY permanent proof that the decoder fix
is load-bearing. **Quote the per-file `git diff --stat` in the SUMMARY.** Any assertion changing
OUTSIDE this allow-list is a STOP.

**NEGATIVE CONTROLS -- observe RED, quote raw.**
* **NC-K5 (the decoder fix is real and load-bearing).** Bind the substrate as a NAMED constant
  beside `FROZEN_R_REV`, with the same never-re-pin annotation `PRE_K1_REF` carries -- **an
  unannotated inline SHA is invisible to the next re-pin sweep, and hard rule 4 exists precisely
  to stop a sweep from bumping a substrate and silently killing the control**:

```python
#: finemap.smk BEFORE the K-1 decoder fix. A DIFFERENTIAL SUBSTRATE, not a freeze
#: pin: it MUST stay 63453db forever. Never re-pin it.
PRE_K1_SMK_REF = "63453db"
```

  Extract the receipt program from `git show {PRE_K1_SMK_REF}:src/snakemake/rules/finemap.smk`
  using the module's existing `_receipt_program` + `_git_show` helpers and drive it over the
  **`(false, true)`** fixture: it
  must print `variant_catalog_fallback_cause none`. The new program on the same fixture must
  print `path2_ld_overlap_zero_RETRY`. **Land this as a PERMANENT in-suite test** in the NC-J1
  style -- it reproduces the incoherence rather than arguing it, needs no revert, and is
  `.pyc`-safe.
* **NC-K6 (the prefix trap is really closed).** Assert that a naive
  `"variant_catalog_fallback_cause path2_ld_overlap_zero" in out` matches **both** the
  `(true, true)` and `(false, true)` outputs while the delimiter-aware matcher separates them.
  **Permanent in-suite** -- this makes the trap's closure a property, not a claim.
* **NC-K7 (the re-pin actually re-pinned).** Before the edit, confirm
  `pytest tests/m3/test_finemap_receipt_early_exit.py::test_run_susie_rss_r_is_still_frozen_at_its_pin -q`
  is **RED** against `dc4bbd2`; after the edit, **GREEN** against `<NEW_SHA>`. Quote both. This is
  the one place a stale-SHA typo would otherwise pass silently.

**COMMIT 2** -- explicit paths only:
`git add tests/m3/test_finemap_receipt_early_exit.py tests/m3/test_qtl_coloc_allele_join.py src/snakemake/scripts/ld_allele_join.R src/snakemake/rules/finemap.smk .planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md`
Subject: `chore(260806-pd3-T2): re-pin run_susie_rss.R at <NEW_SHA> and keep the fallback-cause decoder truthful (K-1)`
  </action>

  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_finemap_receipt_early_exit.py tests/m3/test_qtl_coloc_allele_join.py tests/m3/test_ld_read_path.py tests/m3/test_ld_allele_aware_wiring.py tests/m3/test_ld_read_path_ancestry_gate.py tests/m3/test_ld_declared_authoritative.py tests/m3/test_occlusion_lockstep_wiring.py -q -rs</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && echo "--- remaining dc4bbd2 hits: each MUST be annotated HISTORICAL or DIFFERENTIAL-SUBSTRATE ---"; grep -rn "dc4bbd2" src tests config Snakefile scripts 2>/dev/null || echo NONE; echo "--- EVERY 7-hex SHA literal in the touched modules: each MUST carry a bucket annotation ---"; grep -rnE "\b[0-9a-f]{7}\b" tests/m3/test_finemap_receipt_early_exit.py tests/m3/test_qtl_coloc_allele_join.py tests/m3/test_variant_catalog_fallback_legacy_semantics.py src/snakemake/scripts/ld_allele_join.R src/snakemake/rules/finemap.smk 2>/dev/null | head -40</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && grep -c -- "--ld-allele-aware {params.ld_allele_aware}" src/snakemake/rules/finemap.smk; grep -c "{PYTHON_BIN} -c" src/snakemake/rules/finemap.smk; grep -c "path2_ld_overlap_zero_RETRY" src/snakemake/rules/finemap.smk</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/snakemake --snakefile Snakefile --configfile config/pipeline.yaml --list > /dev/null && echo LIST_OK</automated>
  </verify>

  <done>
  Every FREEZE pin names `<NEW_SHA>`; every remaining `dc4bbd2` hit is classified and annotated in
  the SUMMARY's table; `PRE_K1_REF` / `PRE_CHANGE_REF` were **not** touched; the freeze-gate tests
  are green again; the decoder renders five distinct tokens with the `(false, true)` case as
  `path2_ld_overlap_zero_RETRY`; NC-K5 and NC-K6 are permanent in-suite and NC-K7 was observed RED
  then GREEN with both outputs quoted; the `:488` comment error is corrected with its measurement;
  `--list` is rc 0; the per-file diff containment for AUTH-K1-REPIN is quoted; K-1 is marked
  CLOSED with its original surface preserved and the `ld_allele_join.R` extraction is registered
  as a still-open deferral with reasons. Commit 2 landed.
  </done>
</task>

<task type="auto">
  <name>Task 3: the two-suite gate, the scope + freeze proof, and the honest accounting</name>
  <files>(no source edits; verification only -- any fix required here is a STOP-and-surface, not a silent patch)</files>

  <action>
**Run each suite ONCE, cleanly, AFTER commit 2. Quote every number raw. Numbers measured before
commit 2 are void** (`<the_commit_cascade>`).

**STEP 1 -- `tests/m3`.** Require `failed == 0`, `skipped == 31`, `passed > 807`. Run with `-rs`
and prove **zero** skips originate from `test_variant_catalog_fallback_legacy_semantics.py`;
attribute every remaining skip to a pre-existing gate (`hail` / `chain file not present` /
`M2 union BED` / SKELETON / AoU-perimeter). **Reconcile the `passed` delta arithmetically per
module** -- expected **circa +15**: the new module yields ~11 node IDs (STEP 3(e) is a
`pytest.mark.parametrize` over **five** named MUST-NOT-MOVE regions, so it alone contributes 5),
plus the decoder's 5th parametrised case, plus token-distinctness, plus NC-K5 and NC-K6. The
binding gate is `passed > 807` with skips still exactly 31, so an over-count cannot fail the run
-- but a delta you cannot account for **per module** is a finding, not a rounding error. Do not
open an investigation merely because the total exceeds a prior estimate; reconcile it.

**STEP 2 -- `tests/phase2`.** Exactly `136 passed / 1 skipped / 0 failed`; the skip is
`bedtools not available`.

**STEP 3 -- `snakemake --list` rc 0** on `config/pipeline.yaml` **and** on
`config/pipeline_lsweep_L{15,20,30}_overlay.yaml`.

**STEP 4 -- freeze + scope + containment.**
* `git diff --exit-code <NEW_SHA> -- src/legacy/region_analysis/scripts/run_susie_rss.R`.
* `Rscript -e 'invisible(parse(...)); cat("PARSE_OK\n")'` on the edited R file.
* `git diff --name-only 63453db HEAD -- src config tests .planning | sort` equals **exactly**
  this plan's `files_modified` and **nothing else**.
* **0-diff** on `src/python/plink_ld_to_npz.py`, `src/snakemake/scripts/ld_npz_to_rds.R`,
  `src/python/condition_ld_matrix.py` and the four m3-07 occlusion modules.
* `git diff 63453db HEAD | grep -inE 'condition_ld_matrix|nan_to_num'` empty (m3-06 HELD).
* `git diff 63453db HEAD | grep -inE "gsutil|gcloud|\bbq\b|dataproc|hailctl| wb "` empty.
* `git diff --name-only 63453db HEAD | grep -E "^results/|^src/python/"` empty -- **Track A did
  not move.**
* `git checkout -- tests/m3/sparse_parent_benchmark.tsv`; confirm it is not staged.

**STEP 5 -- THE HONEST ACCOUNTING, written into the SUMMARY.**
1. The gate-binding table against `m3-04c-BLAST-RADIUS.md:133-144`: **"Publishing the panel
   provenance" moves PARTIAL -> CLEARED**, and it is the **only** row this plan touches.
2. **Every** negative control (NC-K1 through NC-K7), quoted **RAW** as observed, with the
   permanent-in-suite ones distinguished from the observed-once ones.
3. **What is INERT today**: 0 region JSONs on this node were produced from an m3-04c-window
   tree, so nothing on disk changes; the fix is proven on **source text and receipt fixtures**.
   **No before/after artifact diff was performed. Say so.**
4. The **1,957 vs 44** discrepancy, reported as a **deviation**, with the measured census.
5. The **plan-fact corrections**: `test_ld_read_path.py:449` is a comment (not a docstring) that
   had to be reworded; `test_qtl_coloc_allele_join.py:1296` is historical and was **annotated,
   not re-pinned**; `run_qtl_coloc.R` resolves `ld_allele_join.R` via `.script_dir()` at `:153`,
   **not** via a CLI argument as the brief stated.
6. The freeze paragraph: `run_susie_rss.R` is now pinned at `<NEW_SHA>`; **AUTH-K1-UNFREEZE and
   AUTH-K1-TEST are both SPENT**; the forward gate is
   `git diff --exit-code <NEW_SHA> -- src/legacy/region_analysis/scripts/run_susie_rss.R`.
7. The **`ld_allele_join.R` extraction: DEFERRED**, with the four findings and the sentence that
   freeze economy is not sufficient justification to accept fire-path risk.
8. Cost: **`$0`, NC State only, zero perimeter contact, AoU fire NOT triggered.**
9. A **STALE-FREEZE HANDOFF BLOCK** for the orchestrator. The K-1 spec
   (`deferred-items.md:389-391`) requires the gate be replaced **"everywhere it is asserted"**.
   Code-side that is done in commit 2; these **orchestrator-owned planning docs** still assert
   `dc4bbd2` and must be updated to `<NEW_SHA>` (and K-1 marked CLOSED) **in the docs commit**:
   * `.planning/STATE.md:55`
   * `.planning/HANDOFF.json:75` and `:111` (both narrate the unfreeze as needed / SPENT)
   * `.planning/HANDOFF.json:117` -- the `freeze_state` field
   * `.planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md:25`

   **The executor wrote NONE of these** (hard rule 10), deliberately: they are not in
   `files_modified`, so touching them would break T3 STEP 4's scope-diff gate. List them
   explicitly so the closure is not left half-applied.
10. `Deviations from the plan` and `Lessons` sections. **A plan fact found wrong is a deviation
    to report, never a thing to silently work around.**
  </action>

  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3 -q -rs 2>&1 | tail -45</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/phase2 -q -rs 2>&1 | tail -15</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && for c in config/pipeline.yaml config/pipeline_lsweep_L15_overlay.yaml config/pipeline_lsweep_L20_overlay.yaml config/pipeline_lsweep_L30_overlay.yaml; do /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/snakemake --snakefile Snakefile --configfile $c --list > /dev/null && echo "OK $c" || echo "FAIL $c"; done</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && git diff --name-only 63453db HEAD -- src config tests .planning | sort; echo "--- forbidden-token scan over the whole diff (must print 0) ---"; git diff 63453db HEAD | grep -icE "condition_ld_matrix|nan_to_num|gsutil|gcloud|dataproc|hailctl" || true; echo "--- Track A files touched (must print 0) ---"; git diff --name-only 63453db HEAD | grep -cE "^results/|^src/python/" || true</automated>
  </verify>

  <done>
  `tests/m3` is **0 failed / 31 skipped / more than 807 passed** with the delta reconciled per
  module and **zero** skips from the new file; `tests/phase2` is **136 / 1 / 0**; `--list` is rc 0
  on all four configs; `run_susie_rss.R` is 0-diff at `<NEW_SHA>` and parses; the scope diff
  equals `files_modified` exactly with **no** `results/` and **no** `src/python/` file touched;
  the m3-06 and AoU token scans are empty; `sparse_parent_benchmark.tsv` is restored and unstaged;
  all ten honest-accounting items are in the SUMMARY, including the STATE.md handoff line and the
  three plan-fact corrections. **No suite number in the SUMMARY was measured before commit 2.**
  </done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| a FROZEN R file -> a single authorized one-line edit | the unfreeze is the only moment the file can change; scope creep here is undetectable later |
| a PRE-EXISTING test that mandates the old behaviour -> its authorized replacement | a "strengthening" that is actually a weakening is the classic failure of this move |
| the old freeze SHA -> the new one | a re-pin that misclassifies a differential substrate as a pin silently kills a negative control |
| the region JSON -> the receipt's decoder | a token that no longer matches the emitting code is worse than no token: it is a confident wrong answer |

## STRIDE threat register

| ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T-pd3-01 | Tampering | `run_susie_rss.R` under an open unfreeze | mitigate | T1 STEP 3d/3e/3f: symbol-scoped block equality against `dc4bbd2` for the Path-2 block and all five MUST-NOT-MOVE regions, plus a symbol-filtered diff shape assertion |
| T-pd3-02 | Repudiation | the strengthened `test_ld_read_path.py` assertion | mitigate | NC-K1 (differential, permanent) and NC-K2 (in-memory splice, permanent) prove a `not in` assertion that CAN fail; NC-K3 observed RED |
| T-pd3-03 | Spoofing | the `not in branch` assertion going vacuous | mitigate | non-vacuity guards: anchor uniqueness, non-empty block, and four positive survivor assertions inside the same block |
| T-pd3-04 | Denial of service | the `.pyc` bytecode cache defeating a control | mitigate | hard rule 2: no on-disk revert anywhere; every control is an in-memory string or a `git show` read |
| T-pd3-05 | Tampering | the re-pin cascade | mitigate | T2 STEP 0 classification table; NC-K7 observed RED then GREEN; per-file diff containment quoted (STEP 5) |
| T-pd3-06 | Repudiation | `variant_catalog_fallback_cause` after K-1 | mitigate | the five-outcome decoder, the `(false, true)` case, NC-K5 reproducing the pre-change `none` permanently |
| T-pd3-07 | Spoofing | prefix collision between two cause tokens | mitigate | non-prefix token naming AND a delimiter-aware matcher AND NC-K6 as a permanent property test |
| T-pd3-08 | Elevation of privilege | scope creep past the three authorizations | mitigate | T3 STEP 4 scope diff must equal `files_modified` exactly; any red pre-existing test is a STOP (hard rule 11) |
| T-pd3-09 | Information disclosure | receipts / registry docs | accept | file paths and counts only; no PII, no credentials; artifacts stay on the NC State node |
| T-pd3-10 | Elevation of privilege | AoU perimeter / billing | mitigate | `$0`, NC State only; forbidden-token grep over the whole diff must be empty; the fire remains Carter's terminal gate |
| T-pd3-11 | Denial of service | the `ld_allele_join.R` extraction on the fire path | transfer | DEFERRED with written reasons; a runtime `source()` on the fire path is not accepted for freeze economy |
| T-pd3-12 | Tampering | reporting numbers from the intermediate commit state | mitigate | `<the_commit_cascade>` names the expected-RED tests; T1's verify excludes them; only T3's post-commit-2 numbers count |
</threat_model>

<verification>
**Every number below MUST be MEASURED and quoted raw, one run each, AFTER commit 2.**

1. `tests/m3`: `failed == 0`, `skipped == 31`, `passed > 807`, with a per-module delta
   reconciliation and **zero** skips from the new module.
2. `tests/phase2`: exactly `136 passed / 1 skipped / 0 failed`.
3. `snakemake --list` rc 0 on `config/pipeline.yaml` and the three `lsweep` overlays.
4. `git diff --exit-code <NEW_SHA> -- src/legacy/region_analysis/scripts/run_susie_rss.R`.
5. `Rscript -e 'parse(run_susie_rss.R)'` prints `PARSE_OK`.
6. `src.count("variant_catalog_fallback <- TRUE") == 1`, inside the Path-1 block.
7. `git diff --name-only 63453db HEAD -- src config tests .planning | sort` equals
   `files_modified` exactly.
8. **0-diff** on `plink_ld_to_npz.py`, `ld_npz_to_rds.R`, `condition_ld_matrix.py`, the four
   m3-07 occlusion modules.
9. `grep -inE 'condition_ld_matrix|nan_to_num'` over the diff is empty (m3-06 HELD).
10. `grep -inE "gsutil|gcloud|\bbq\b|dataproc|hailctl| wb "` over the diff is empty.
11. No file under `results/` or `src/python/` was touched (Track A).
12. `tests/m3/sparse_parent_benchmark.tsv` restored via `git checkout --` and not committed.
13. NC-K1 through NC-K7 each recorded: permanent-in-suite ones named as such; observed-once ones
    quoted RAW as RED with the restored-green run beside them.

**DELIBERATE NON-CRITERIA -- stated as limits, not sold as coverage.**
* **No before/after region-JSON diff.** 0 JSONs on this node were produced from an m3-04c-window
  tree (44 carry the key, all `false`; 0 carry `ld_overlap_zero_fallback`). The closure is proven
  on **source text and receipt fixtures**.
* `snakemake --dry-run` is **NOT** a criterion. `data/processed/ld_reference/` is absent, so
  `resolve_ld_path` raises for every AFR region independently of this plan (`D-04b-03`). `--list`
  is the achievable substitute and does not evaluate input functions.
* **No R behavioural test** exercises Path 2: it needs the whole script (sumstats + regions csv +
  policy + variant list + a real region). That limitation is pre-existing and documented in
  `test_ld_read_path.py`'s own docstring; **this plan does not close it and must not imply it
  did**.
* **0/276 `.npz` banked.** No criterion asserts anything about a real panel on disk.
</verification>

<success_criteria>
1. **K-1 CLOSED.** The Path-2 branch no longer sets `variant_catalog_fallback`; the key is
   assigned at exactly one site, the Path-1 revert; the legacy semantics are restored.
2. **Containment PROVEN, not asserted.** The Path-2 block's code-only lines equal `dc4bbd2`'s
   minus exactly the one deleted line; all five MUST-NOT-MOVE regions are byte-identical; the
   symbol-scoped diff is one removed line and zero added.
3. **The authorized test edit STRENGTHENED the pin.** Both directions are now asserted, with two
   PERMANENT negative controls and two observed-RED ones.
4. **The decoder ring still tells the truth.** Five distinct, non-prefix-colliding tokens; the
   post-K-1 Path-2 case has its own; the pre-change `none` is reproduced permanently by NC-K5;
   the `:488` comment error is corrected with its measurement.
5. **The re-pin is COMPLETE and CORRECTLY CLASSIFIED.** Every FREEZE pin names `<NEW_SHA>`; every
   remaining `dc4bbd2` is an annotated historical narrative or a differential substrate; NC-K7
   proves the re-pin actually took.
6. **The extraction is DEFERRED with reasons**, and freeze economy is recorded in writing as
   insufficient justification for fire-path risk.
7. **Both suites green** at the required numbers with **no new skip**, measured **after commit
   2**; no unauthorized file touched; **Track A did not move**; m3-06 HELD; `$0`, zero perimeter
   contact, **the AoU fire NOT triggered**.
8. **The gate row "Publishing the panel provenance" is reported CLEARED -- and nothing else is.**
   A partial closure reported as a full one is the exact failure class this arc keeps catching;
   so is a full closure inflated into a claim about the AFR results, which this is **not**.
</success_criteria>

<explicitly_out_of_scope>
* **The `ld_allele_join.R` extraction** -- evaluated and DEFERRED; see `<the_deferral>`.
* **E-2** (QTL-beta / panel-ALT orientation) and **G-2** (is a TRANS fit on a 1kG EUR panel
  reportable) -- both Carter's scientific calls.
* **E-3**, **E-4** (`_ancestry_for_region` hardcoded `"EUR"`), **BLOCKER-D**'s large-region
  classes.
* **A / B / C / E / F / G / H / I / J / L / M** -- all closed. **Do not re-open any of them.**
* **m3-06** stays HELD. No NaN-to-0, no `condition_ld_matrix.py`.
* `.planning/STATE.md`, `.planning/HANDOFF.json` and `ROADMAP.md` -- **do not write them.**
</explicitly_out_of_scope>

<output>
On completion write
`.planning/quick/260806-pd3-close-blast-radius-finding-k-1-restore-v/260806-pd3-SUMMARY.md`
using the GSD summary template, carrying every item in T3 STEP 5. **Do not commit it** -- the
orchestrator owns the docs commit.
</output>
