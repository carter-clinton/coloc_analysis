---
phase: quick-260428-pj4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REQ-DOC-PIVOT-PROJECT
  - REQ-DOC-PIVOT-REQUIREMENTS
must_haves:
  truths:
    - "PROJECT.md scope section reflects the 9-trait × 2-ancestry genome-wide reframe per Amendment §12 (no candidate-locus-primary framing remaining)."
    - "PROJECT.md retains the Track A / Track B structural distinction and preserves the Constraints block verbatim from the pre-pivot file."
    - "REQUIREMENTS.md enumerates the new Track B requirements that Amendment §12 mandates (genome-wide region generation, MTAG --overlap, CPASSOC orthogonal corroboration, two-stage coloc gate, HyPrColoc 3+ traits, PolyFun priors, AoU AFR LD mandate, L2G + Borzoi, OSF amendment timing constraint, and the four operational novel-variant definitions from §7 Classes 1-4 plus Class 5 as supplementary)."
    - "REQUIREMENTS.md preserves load-bearing pre-pivot REQs that Track A still depends on (per the Amendment §12 row instructing rewrite-not-deletion)."
    - "Both files cite the authoritative amendment path .planning/amendments/PROJECT-AMENDMENT-2026-04-22-genome-wide-reframe.md so future readers can trace claims to source."
    - "No file outside .planning/PROJECT.md and .planning/REQUIREMENTS.md is touched (Terminal A owns m3-* / ROADMAP / DECISIONS / STATE / amendments / code in parallel)."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Post-pivot project charter (Who / What / Where / Why / Constraints / Goals / Status) reflecting Amendment §12 scope rewrite"
      contains: "Track A, Track B, MTAG, CPASSOC, HyPrColoc, AoU, 9 traits, EUR, AFR"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Post-pivot testable acceptance criteria covering Amendment §3 M0-M6, §7 novelty classes 1-5, AoU LD egress, and preserved pre-pivot Track A REQs"
      contains: "REQ-MTAG-OVERLAP, REQ-CPASSOC-ORTHOGONAL, REQ-TWO-STAGE-COLOC, REQ-HYPRCOLOC-MULTI, REQ-POLYFUN-RESCUE, REQ-AOU-LD-EGRESS, REQ-NOVELTY-CLASS, REQ-CATALOG-VERSION-LOCK, REQ-OSF-PREREG"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/amendments/PROJECT-AMENDMENT-2026-04-22-genome-wide-reframe.md"
      via: "explicit relative-path reference in the Authoritative-source header"
      pattern: "PROJECT-AMENDMENT-2026-04-22-genome-wide-reframe"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/amendments/PROJECT-AMENDMENT-2026-04-22-genome-wide-reframe.md"
      via: "Source: lines on every Track B REQ"
      pattern: "Amendment §"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/amendments/AOU-LD-PIPELINE.md"
      via: "Source: line on REQ-AOU-LD-EGRESS / REQ-AOU-LD-VALIDATION"
      pattern: "AOU-LD-PIPELINE.md"
---

<objective>
Rewrite `.planning/PROJECT.md` and `.planning/REQUIREMENTS.md` to reflect the
2026-04-22 genome-wide reframe per Amendment §12. This is the documentation
half of M0 closeout: PROJECT.md and REQUIREMENTS.md must read as if the
post-pivot scope (9 traits × 2 ancestries, MTAG + CPASSOC + HyPrColoc joint-
signal discovery, AoU AFR LD panel, 5 novelty classes) were the project's
canonical scope, with the candidate-locus prior incarnation surviving only as
Track A's pre-specified methods-validation contribution.

Purpose: M0 cannot close (and the OSF amendment cannot post, blocking all M2
work per Amendment §9.1) until PROJECT.md / REQUIREMENTS.md / ROADMAP.md /
DECISIONS.md align with the amendment. This quick task handles the
PROJECT.md + REQUIREMENTS.md half of that alignment. Terminal A owns the
ROADMAP.md / DECISIONS.md / STATE.md half in parallel and those files are
HARD-LOCKED for this plan.

Output: Two rewritten files committed via `/gsd-quick` flow. No code, no
tests, no other files touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/amendments/PROJECT-AMENDMENT-2026-04-22-genome-wide-reframe.md
@CLAUDE.md

<!-- Amendment §12 is the canonical source for what content goes into the
     two rewrites. The instruction table in §12 reads (verbatim, abridged
     to the two relevant rows):

     | File              | Required update                                                                                  |
     | PROJECT.md        | Replace scope section with 9-trait × 2-ancestry genome-wide framing; add "Track A / Track B"     |
     |                   | structural note; preserve constraints block verbatim                                             |
     | REQUIREMENTS.md   | Add REQ-2 through REQ-N covering: genome-wide region generation (MTAG + CPASSOC union), AoU AFR  |
     |                   | LD mandate, two-stage coloc gate, HyPrColoc 3+ traits, PolyFun priors, L2G+Borzoi gene/variant   |
     |                   | prioritization, OSF amendment timing constraint (post after M1, before M2), and the four pre-   |
     |                   | registered novel-variant operational definitions from Section 7 (Classes 1-4 with thresholds;   |
     |                   | Class 5 as supplementary).                                                                      |

     The CURRENT versions of both files (read into context above) already
     contain post-pivot content — they were rewritten in an earlier pass.
     The executor must verify the current content is consistent with
     Amendment §12 and bring any drift back into alignment, NOT regress the
     files to a pre-pivot state. If the current files already satisfy §12
     fully, the work is to make any small corrections needed and commit.
     Treat this as a "reconcile to spec" rewrite, not a from-scratch
     rewrite. -->

<!-- HARD-LOCKED files (Terminal A owns these in parallel — DO NOT TOUCH):
     .planning/ROADMAP.md
     .planning/DECISIONS.md
     .planning/STATE.md
     .planning/amendments/aou-egress-audit-log.md
     anything under .planning/phases/m3-*
     anything under bin/, src/, envs/, tests/, config/

     ALLOWED files (entire scope of this plan):
     .planning/PROJECT.md
     .planning/REQUIREMENTS.md
     -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite .planning/PROJECT.md to Amendment §12 spec</name>
  <files>.planning/PROJECT.md</files>
  <action>
Open `.planning/PROJECT.md` and verify it already conforms to Amendment §12
row "PROJECT.md" — i.e., scope section is 9-trait × 2-ancestry genome-wide,
Track A / Track B structural note present, Constraints block verbatim from
CLAUDE.md. The current content (read in <context>) already satisfies this
because an earlier pass rewrote it; your job is to **reconcile any drift**
and ensure the file has all five required structural elements, in this
order, top to bottom:

1. **Authoritative-pivot-charter header block** at the top of the file, with
   relative-path links to:
   - `.planning/amendments/PROJECT-AMENDMENT-2026-04-22-genome-wide-reframe.md`
     (post-pivot source of truth)
   - companion docs: `TRACK-A-PIVOT.md`, `SUMSTATS-UPGRADE.tsv`,
     `SUMSTATS-UPGRADE.md`, `AOU-LD-PIPELINE.md`,
     `TRACK-A-FROZEN-NUMBERS.md`
   - `../CLAUDE.md` as the project-instruction anchor

2. **Who / What / Where / Why** sections (post-pivot framing):
   - **Who:** Carter K. Clinton, ASHES Lab, NCSU. Solo author. Rigor via
     multi-method triangulation, OSF pre-registration, hold-out replication.
   - **What:** Two-track program adopted 2026-04-22 per Amendment §3.
     Track A = real-LD audit of 50 curated cardiometabolic regions, target
     venue Genome Medicine → AJHG short report → Bioinformatics. Track B =
     genome-wide joint-signal discovery across 9 traits (BMI, T2D, stroke,
     SBP, asthma, CAD, lipids, eGFR, HbA1c) × 2 ancestries (EUR, AFR), two
     co-equal pre-registered scientific aims (cross-trait pleiotropy
     discovery + novel-variant discovery across 5 classes), milestones
     M0–M6 per Amendment §3, target Nature Genetics.
   - **Where:** GPFS canonical repo path; `/rs1/researchers/c/ckclinto/`
     upstream data root; miniconda3 + gsd-tools env; NCSU HPC LSF; All of
     Us Researcher Workbench (Terra) for Track B M3 AFR LD panel build;
     GPFS solo-mode-with-branch-isolation worktree note.
   - **Why:** One paragraph. Original-research framing (per Carter's
     `feedback_original_research_framing.md` memory — never "revision",
     "cleanup", "fix"). State: at the candidate-locus scale Track A
     quantifies how many published cross-trait pleiotropy claims at 50
     curated cardiometabolic regions survive fully-pre-registered real-LD
     re-analysis (cite Stage 2 numerics: 51/96 non-empty CS vs 48/95
     k2d full-coverage identity-LD = 1.06× yield contrast; 0 Tier A;
     SH2B3 × asthma EUR identity-LD PP.H4=1.0 → real-LD n_cs_a=0 collapse;
     SUPERSEDED 2026-04-25 narrow 12/96 / 4.25× baseline preserved in
     `TRACK-A-FROZEN-NUMBERS.md`). At the genome-wide scale Track B pursues
     hypothesis-agnostic joint-signal discovery via MTAG (Turley 2018) +
     LDSC `--overlap` correction, CPASSOC (Zhu 2015) orthogonal SHom/SHet,
     two-stage scalable coloc (ABF triage → SuSiE-RSS rescue with PolyFun
     baselineLF2 priors per Weissbrod 2020), HyPrColoc (Foley 2021) for
     ≥3-trait shared architecture, and All of Us controlled-tier WGS
     (~60–95k AFR post-QC) as the ancestry-matched LD reference panel
     (~150× sample-size upgrade over 1000G AFR n=661). Five novel-variant
     classes pre-registered per Amendment §7 with locked comparator
     catalogs (GWAS Catalog, Pickrell 2016, Watanabe 2019, Open Targets
     L2G, ClinVar) and SHA-256 version checksums.

3. **Constraints block verbatim from CLAUDE.md** (Amendment §12 explicitly
   says "preserve constraints block verbatim"). Five bullets:
   - 100% public data
   - Solo author (rigor via triangulation + pre-registration + Snakemake +
     hold-out replication)
   - Timeline is not a binding constraint
   - No web/JS stack (R / Python / bash / Snakemake / conda only)
   - Data access lead times are the real critical path (parallel DUAs from
     Day 1 per REQ-1)
   - GPFS filesystem, no worktree isolation, GSD mode `solo` with
     `git.isolation: branch`

4. **Goals for this GSD-managed program** — five numbered items covering:
   (i) execute Track B M0→M6 to Nature Genetics acceptance bar;
   (ii) ship Track A short-form methods paper at Genome Medicine,
   independent of Track B, with bioRxiv preprint in 2026-05/06 ahead of
   Track B M6 submission in 2027-04/05;
   (iii) pre-register all Track B claims on OSF via Amendment §9 flow
   (amendment posts at end of M1, before any M2 MTAG/CPASSOC run; OSF
   submission is the pre-registration gate for M2);
   (iv) deliver reproducible GitHub release with pinned Snakemake pipeline,
   conda envs, Singularity containers, Zenodo DOI deposit of AoU-derived
   AFR LD panels (summary-only per AoU egress policy), hold-out replication
   tables on FinnGen / Pan-UKBB / MVP release n+1 per REQ-1;
   (v) preserve every component of the pre-pivot spine (Phases 0, 1, 2, 5,
   9) as reusable artifacts per Amendment §8 — Track A's primary data and
   Track B's candidate-locus validation subset.

5. **Current status** + **Open human-action items** sections — short, dated.
   Status reflects **M0 ~70% complete**: 6 amendment artifacts committed,
   Track A first-pass draft committed 2026-04-23, M1 sumstats upgrade +
   harmonization COMPLETE 2026-04-25 (verifier PASSED 5/5 ROADMAP success
   criteria, 26 D-16 harmonized triples, 12-trait LDSC bivariate intercept
   matrix, two SHA-256 manifests, OSF paste-ready in
   `OSF-AMENDMENT-TEXT-2026-04-22.md`). Track B M2-M6 GATED on Carter OSF
   web-UI submission per Amendment §9.1. Three open human-action items:
   (a) OSF amendment submission at osf.io/pvb5j (BLOCKS M2);
   (b) BMI EUR primary-source decision (Loh 2022 vs Yengo 2022) — to be
   locked at M1 kickoff before LDSC munge;
   (c) MVP phs001672 DUA submission status for Giri 2019 SBP-AFR.

**Decision-fidelity check** (mandatory before save):

- The two recorded post-2026-04-22 decisions (DEC-2026-04-24-01 GRCh37
  canonical override of Amendment §3 GRCh38 wording; DEC-2026-04-24-02
  AoU AFR-SBP M2 fallback for Giri 2019 D-06) live in DECISIONS.md (which
  is HARD-LOCKED for this plan) — PROJECT.md should NOT re-litigate them
  but MAY mention DEC-2026-04-24-01 in passing in the M1 status paragraph
  since the current PROJECT.md already does so. Do not add or remove
  DEC-* references beyond what is already in the file unless drift from
  Amendment §12 is identified.
- Cite exact numerics from Amendment §2.2 / Stage 2 fire (51/96 real-LD;
  48/95 k2d identity-LD; 1.06× yield contrast; 0 Tier A; SH2B3 collapse).
  Do NOT invent numbers.
- Use original-research framing throughout (no "revision", "cleanup",
  "fix" language) per Carter's standing instruction.
- Per Carter's `feedback_no_conda.md`, do NOT include conda-activation
  instructions in any narrative.

**Forbidden actions:**

- Do NOT touch any file outside `.planning/PROJECT.md`. The Edit tool
  invocation MUST target only this path. If anything else needs to change
  to make PROJECT.md make sense, STOP and surface the issue — do not
  improvise edits to ROADMAP.md, DECISIONS.md, STATE.md, or anything in
  `.planning/amendments/`.
- Do NOT delete the existing file and re-create — use `Edit` (or full-file
  `Write` after `Read`) to preserve git diff readability.
- Do NOT add code blocks containing R / Python / Snakemake / bash —
  PROJECT.md is narrative-only.
- Do NOT add `## Technology Stack` / `## Conventions` / `## Architecture`
  / `## Project Skills` / `## GSD Workflow Enforcement` /
  `## Developer Profile` sections (those are CLAUDE.md's job, generated
  via the GSD profile machinery).
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis &amp;&amp; \
test -f .planning/PROJECT.md &amp;&amp; \
grep -q "PROJECT-AMENDMENT-2026-04-22-genome-wide-reframe" .planning/PROJECT.md &amp;&amp; \
grep -q "Track A" .planning/PROJECT.md &amp;&amp; grep -q "Track B" .planning/PROJECT.md &amp;&amp; \
grep -q "9 complex traits\|9 traits" .planning/PROJECT.md &amp;&amp; \
grep -qi "MTAG" .planning/PROJECT.md &amp;&amp; grep -qi "CPASSOC" .planning/PROJECT.md &amp;&amp; \
grep -qi "HyPrColoc" .planning/PROJECT.md &amp;&amp; grep -qi "All of Us\|AoU" .planning/PROJECT.md &amp;&amp; \
grep -q "EUR" .planning/PROJECT.md &amp;&amp; grep -q "AFR" .planning/PROJECT.md &amp;&amp; \
grep -q "100% public data" .planning/PROJECT.md &amp;&amp; \
grep -q "git.isolation: branch\|GPFS" .planning/PROJECT.md &amp;&amp; \
grep -q "M0\|M6" .planning/PROJECT.md &amp;&amp; \
echo "PROJECT.md content check PASSED"</automated>
  </verify>
  <done>
.planning/PROJECT.md exists with: (a) authoritative-pivot-charter header
linking to the amendment + companion docs; (b) Who / What / Where / Why
sections in post-pivot framing with Track A / Track B distinction;
(c) Constraints block verbatim from CLAUDE.md; (d) Goals (5 items);
(e) Current status + Open human-action items reflecting M0 ~70% complete +
M1 COMPLETE 2026-04-25. No file outside `.planning/PROJECT.md` touched.
Automated verify command above exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite .planning/REQUIREMENTS.md to Amendment §12 spec</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Open `.planning/REQUIREMENTS.md` and verify it conforms to Amendment §12 row
"REQUIREMENTS.md" — i.e., enumerates the new Track B requirements covering
genome-wide region generation, AoU AFR LD mandate, two-stage coloc gate,
HyPrColoc 3+ traits, PolyFun priors, L2G + Borzoi, OSF amendment timing
constraint, and the four pre-registered novel-variant operational
definitions from Amendment §7 (Classes 1-4 with thresholds; Class 5 as
supplementary). The current content (read in <context>) already satisfies
this; your job is to **reconcile any drift** and ensure the file is
internally consistent, not to regenerate from scratch.

Required structural elements, in this order, top to bottom:

1. **Header** + **legend** explaining `[A]` (Track A only), `[B]` (Track B
   only), `[AB]` (shared). Cite the authoritative source path
   (`.planning/amendments/PROJECT-AMENDMENT-2026-04-22-genome-wide-reframe.md`)
   and the AoU LD pipeline spec
   (`.planning/amendments/AOU-LD-PIPELINE.md`).

2. **Preserved pre-pivot REQs (carried forward)** section — Amendment §12
   says "rewrite", not "delete and replace", so load-bearing pre-pivot
   REQs that Track A still depends on (or that apply to both tracks)
   stay. Verify these are present and untouched in substance:
   - **REQ-SNAKEMAKE-CI [AB]** — pinned envs, toy 3-locus CI, CI status
     log
   - **REQ-PUBLIC-DATA-ONLY [AB]** — every data source has `license:` and
     `public: true` flags; AoU controlled-tier counts as public via
     standard academic DUA per CLAUDE.md
   - **REQ-SUSIE-RSS-POLICY [A]** — explicit policy for L=10 cap,
     convergence failures, `min_abs_corr` sensitivity sweep, downweighting
     coincident credible sets
   - **REQ-NEGATIVE-CONTROLS [AB]** — ≥3 negative-control gene/pathway
     sets (HLA, pigmentation OCA2/SLC24A5/MC1R, blood-group ABO/FUT1/FUT2);
     all sets must come up null in coloc + pathway tables (verified in
     Stage 2 224-row null per `TRACK-A-FROZEN-NUMBERS.md`)
   - **REQ-PATH-PARAMETERIZATION [AB]** — no hardcoded absolute paths in
     `src/R`, `src/python`, `src/snakemake`, `config/`; everything via
     `config/pipeline.yaml`
   - **REQ-OSF-PREREG [AB]** — re-scoped per Amendment §9: M2 BLOCKED
     until OSF amendment posts at osf.io/pvb5j; PDF saved with filename
     pattern `osf-amendment-m0-2026-04-XX.pdf` under
     `.planning/amendments/`; M2 Snakemake target `run_mtag_overlap` has
     a guard that refuses to execute until the PDF exists
   - **REQ-PP.H4-THRESHOLD-SWEEP [A]** — Track A reports tier counts
     across PP.H4 ∈ {0.5, 0.7, 0.8, 0.9}; Track B M4 uses region-level
     PP.H4 FDR correction instead (REQ-TWO-STAGE-COLOC handles M4)
   - **REQ-EQUITY-FRAMING [B]** — applies to Track B M6 manuscript;
     `docs/methods/equity_framing.md` referenced from abstract / intro /
     discussion; AFR vs EUR numbers reported with explicit accuracy +
     calibration metrics

3. **New Track B requirements (Amendment §§3, 5, 6, 7)** — per Amendment
   §12 and §7.1, encode the five novelty classes as their own REQs with
   acceptance tests tied to §7.1 thresholds. Verify these are present:
   - **REQ-TRAIT-INVENTORY [B]** — 9 traits × up to 2 ancestries per
     Amendment §4 table; phenotype locks (stroke = all-stroke not
     ischemic-only; LDL-C continuous primary; eGFR creatinine-based
     continuous); ancestry coverage flags including AFR via DIAMANTE-AFR,
     GIGASTROKE-AA, Giri 2019 MVP-AFR (note: Giri 2019 not "Giri 2020" —
     the current REQUIREMENTS.md uses the 2019 citation that matches
     SUMSTATS-UPGRADE.tsv row 13 and DEC-2026-04-24-02; verify and keep
     consistent), GBMI-AFR, GLGC-AFR, CKDGen-AFR / Morris 2019, MAGIC-AFR,
     PAGE / Loh 2022 BMI-AFR, Aragam 2022 CAD-AFR. Acceptance:
     `config/trait_inventory.yaml` enumerates the 9 traits × ancestry
     coverage; harmonized sumstats exist for every (trait, ancestry) cell
     listed before M2 begins.
   - **REQ-MTAG-OVERLAP [B]** — MTAG with `--overlap` using LDSC pairwise
     intercept matrix; `max_FDR` per Turley 2018; mtCOJO sensitivity check
     on top-N MTAG-novel loci where overlap is extreme. Acceptance: LDSC
     pairwise rg intercept matrix exists for all 9-trait ancestry-stratified
     pairs; MTAG output includes `max_FDR` column; ≥1 mtCOJO sensitivity
     table exists.
   - **REQ-CPASSOC-ORTHOGONAL [B]** — CPASSOC SHom/SHet as orthogonal
     joint-signal test; MTAG ∩ CPASSOC reported as the high-confidence
     Class 1 subset.
   - **REQ-TWO-STAGE-COLOC [B]** — fast ABF-coloc genome-wide first as
     triage filter, SuSiE-RSS only on PP.H4 > 0.5 regions; region-level
     PP.H4 FDR correction.
   - **REQ-HYPRCOLOC-MULTI [B]** — HyPrColoc for ≥3-trait simultaneous
     coloc, capped at 3-5 traits per block per Amendment §10 risk row;
     pairwise sensitivity check inside each block.
   - **REQ-POLYFUN-RESCUE [B]** — PolyFun baselineLF2 functional priors;
     rescue table labeled with PolyFun-vs-uniform-prior PIP delta.
   - **REQ-L2G-GENE-PRIORITIZATION [B]** — Open Targets L2G as secondary
     gene-prioritization axis independent of coloc/eQTL.
   - **REQ-BORZOI-VARIANT-EFFECT [B]** — Borzoi variant-effect scoring
     applied to Tier A credible-set variants; Linder 2024 training-
     distribution caveats explicit in methods; Class 5 supplementary not
     primary per Amendment §7.3.
   - **REQ-NOVELTY-CLASS-1 [B]** — joint-signal: (MTAG p < 5e-8 OR
     CPASSOC p < 5e-8) AND max(single-trait p) ≥ 5e-8 AND no single-trait
     GWS hit within ±500 kb in GWAS Catalog v_lock. Output:
     `joint_signal_novel.tsv`.
   - **REQ-NOVELTY-CLASS-2 [B]** — AFR-specific: AFR PP.H4 ≥ 0.8 AND
     |CS| ≤ 25 AND (no overlapping EUR coloc OR AFR lead variant has
     MAF_AFR ≥ 0.01 with MAF_EUR < 0.005). Output:
     `afr_specific_novel.tsv`.
   - **REQ-NOVELTY-CLASS-3 [B]** — secondary-independent: SuSiE-RSS CS
     index ≥ 2 AND CS purity ≥ 0.5 AND PIP_max ≥ 0.5 AND lead variant
     not within ±100 kb of prior GWAS Catalog v_lock entry for the same
     trait. Cross-panel LD-sensitivity parity check per Amendment §10
     risk row. Output: `secondary_signals.tsv`.
   - **REQ-NOVELTY-CLASS-4 [B]** — pleiotropy-class: cross-trait PP.H4 ≥
     0.8 (pairwise) or HyPrColoc regional_prob ≥ 0.8 (≥3 traits) AND not
     present as cross-trait shared in {Pickrell 2016 supplement,
     Watanabe 2019 GWAS Atlas, Open Targets L2G top-3} as locked on M5
     cross-reference date. Output: `pleiotropy_novel.tsv`.
   - **REQ-NOVELTY-CLASS-5 [B]** — functional-mechanism, SUPPLEMENTARY:
     max-tissue Borzoi/Enformer score in top decile across the credible
     set AND no ClinVar pathogenic/likely-pathogenic entry AND no primary-
     literature functional characterization (PubMed search via
     `mcp__claude_ai_PubMed`). Output: `functional_novel.tsv` reported as
     supplementary; methods paragraph explicitly labels Class 5 as
     supplementary.
   - **REQ-CATALOG-VERSION-LOCK [B]** — all comparator catalog versions
     (GWAS Catalog, Pickrell 2016, Watanabe 2019, Open Targets L2G,
     ClinVar) locked at M5 cross-reference date with SHA-256 checksums +
     download URLs in supplement; delta-analysis between lock-date and
     submission-date catalogs if drift occurs during review.
   - **REQ-AOU-LD-EGRESS [B]** — Track B AFR LD reference built inside
     AoU Researcher Workbench from controlled-tier WGS (~60-95k AFR
     post-QC); only summary-level artifacts (LD matrix + AF metadata)
     exported per AoU data-egress policy; zero cells from <20 participants
     (trivially satisfied at n ≈ 60k AFR); AoU P&P draft registered before
     any Dataproc compute; RPS filed per `AOU-LD-PIPELINE.md` §2.1; export
     request categorized as aggregate summary statistics with written AoU
     classification (per AOU-LD-PIPELINE Amendment §10 risk R1).
   - **REQ-AOU-LD-VALIDATION [B]** — four-check protocol on 10-region dev
     subset before scale-up: (1) known-locus LD pattern matches published
     AFR figures; (2) AoU EUR vs 1000G EUR entry-wise r ≥ 0.97 for
     MAF ≥ 0.05; (3) SuSiE-RSS converges on 16q12 BMI AFR with CS size
     ≤ 30 and lead PIP ≥ 0.1; (4) AoU-AFR vs identity-placeholder A/B
     documented for the 10 regions. Output:
     `.planning/phases/m3-aou-afr-ld-panel-build/validation/`.
   - **REQ-REPLICATION-HOLDOUT [B]** — hold-out replication on FinnGen
     R13+ / Pan-UKBB / MVP release n+1 where available for Tier A claimed
     loci and novel-variant classes 1-4 (Class 5 supplementary, no
     replication required). Per-class table with point estimate, 95% CI,
     sign agreement, post-hoc power per replication cohort.

4. **REQ ID cross-reference table** at the bottom — REQ ID, Milestone(s)
   (M0/M1/M2/M3/M4/M5/M6 or `Track-A-finalization`), Track ([A]/[B]/[AB]),
   prior-pivot origin (pre-pivot REQ-N or new Amendment §X). Verify all
   24 REQs in the file appear in the cross-reference.

**Decision-fidelity check** (mandatory before save):

- Source-attribution lines on every Track B REQ must cite Amendment §
  numbers (e.g., "Source: Amendment §3 M2, §6, §10 risk row on MTAG
  overlap"). Source-attribution lines on preserved pre-pivot REQs must
  cite the pre-pivot REQ number and the relevant `GSD_BRIEFING.md` /
  `Revision_Plan.md` section so audit trail is unbroken.
- Acceptance tests must be machine-checkable where possible (file-exists
  patterns, grep returns 0, snakemake target succeeds, threshold met) —
  not vibe-checks.
- Operational definitions for novelty Classes 1-5 must use the EXACT
  thresholds from Amendment §7.1 (no thinking the threshold "feels
  conservative" and tightening it; no relaxing because a current finding
  doesn't pass). The thresholds are pre-registration commitments.
- Use original-research framing throughout (no "revision", "cleanup",
  "fix" language).

**Forbidden actions:**

- Do NOT touch any file outside `.planning/REQUIREMENTS.md`. Especially
  do NOT edit `.planning/amendments/PROJECT-AMENDMENT-2026-04-22-genome-
  wide-reframe.md` or any `.planning/phases/m3-*` file (Terminal A is
  executing m3 in parallel and HARD-LOCKS those).
- Do NOT add or modify REQ-IDs that are not authorized by Amendment §12.
  If an apparent gap exists, surface it for Carter's review rather than
  inventing a new REQ-ID inline.
- Do NOT delete pre-pivot REQs that Track A still relies on (Amendment §8
  preserves Track A's right to use the spine; REQ-SUSIE-RSS-POLICY,
  REQ-PP.H4-THRESHOLD-SWEEP, REQ-NEGATIVE-CONTROLS are load-bearing for
  Track A and must remain).
- Do NOT add code blocks (R / Python / Snakemake / bash) — REQUIREMENTS.md
  is acceptance criteria + rules, not implementation.
  </action>
  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis &amp;&amp; \
test -f .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "PROJECT-AMENDMENT-2026-04-22-genome-wide-reframe" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "AOU-LD-PIPELINE.md" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-SNAKEMAKE-CI" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-PUBLIC-DATA-ONLY" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-SUSIE-RSS-POLICY" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-NEGATIVE-CONTROLS" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-PATH-PARAMETERIZATION" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-OSF-PREREG" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-PP.H4-THRESHOLD-SWEEP" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-EQUITY-FRAMING" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-TRAIT-INVENTORY" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-MTAG-OVERLAP" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-CPASSOC-ORTHOGONAL" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-TWO-STAGE-COLOC" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-HYPRCOLOC-MULTI" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-POLYFUN-RESCUE" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-L2G-GENE-PRIORITIZATION" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-BORZOI-VARIANT-EFFECT" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-NOVELTY-CLASS-1" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-NOVELTY-CLASS-2" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-NOVELTY-CLASS-3" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-NOVELTY-CLASS-4" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-NOVELTY-CLASS-5" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-CATALOG-VERSION-LOCK" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-AOU-LD-EGRESS" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-AOU-LD-VALIDATION" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ-REPLICATION-HOLDOUT" .planning/REQUIREMENTS.md &amp;&amp; \
grep -q "REQ ID cross-reference\|REQ ID | Milestone" .planning/REQUIREMENTS.md &amp;&amp; \
echo "REQUIREMENTS.md content check PASSED"</automated>
  </verify>
  <done>
.planning/REQUIREMENTS.md exists with: (a) header citing the amendment +
AoU LD pipeline spec; (b) preserved pre-pivot REQs that Track A depends on
(8 REQs: SNAKEMAKE-CI, PUBLIC-DATA-ONLY, SUSIE-RSS-POLICY, NEGATIVE-
CONTROLS, PATH-PARAMETERIZATION, OSF-PREREG, PP.H4-THRESHOLD-SWEEP,
EQUITY-FRAMING); (c) new Track B REQs covering the 9 method requirements
(TRAIT-INVENTORY, MTAG-OVERLAP, CPASSOC-ORTHOGONAL, TWO-STAGE-COLOC,
HYPRCOLOC-MULTI, POLYFUN-RESCUE, L2G-GENE-PRIORITIZATION, BORZOI-VARIANT-
EFFECT, REPLICATION-HOLDOUT) + 5 novelty classes (NOVELTY-CLASS-1
through -5) + comparator catalog lock (CATALOG-VERSION-LOCK) + 2 AoU
REQs (AOU-LD-EGRESS, AOU-LD-VALIDATION); (d) REQ ID cross-reference
table covering all 24 REQs. No file outside `.planning/REQUIREMENTS.md`
touched. Automated verify command above exits 0.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run from repo root:

```
git status --short -- .planning/PROJECT.md .planning/REQUIREMENTS.md
git diff --stat -- .planning/PROJECT.md .planning/REQUIREMENTS.md
git status --short -- .planning/ROADMAP.md .planning/DECISIONS.md .planning/STATE.md \
  '.planning/phases/m3-*' .planning/amendments/aou-egress-audit-log.md
```

The first two commands MUST show changes only to PROJECT.md and
REQUIREMENTS.md (or "no changes" if drift was zero). The third command
MUST show ZERO modifications — anything else means a HARD-LOCKED file
was touched and the change must be reverted before commit.

Run the per-task verify scripts (Task 1 + Task 2 grep blocks). Both must
exit 0 and print their PASSED line.

Cross-check vs Amendment §12:
- PROJECT.md: 9-trait genome-wide framing present? Track A / Track B
  structural note present? Constraints block verbatim?
- REQUIREMENTS.md: all 9 method REQs + 5 novelty classes + AoU LD egress
  + AoU LD validation + comparator catalog lock + replication holdout +
  trait inventory present?
</verification>

<success_criteria>
- `.planning/PROJECT.md` rewritten to Amendment §12 spec; all five
  structural elements (header / Who-What-Where-Why / Constraints verbatim /
  Goals / Status+Open-actions) present and post-pivot-consistent.
- `.planning/REQUIREMENTS.md` rewritten to Amendment §12 spec; all 24
  REQs present (8 preserved pre-pivot + 16 new Track B); REQ ID cross-
  reference table populated.
- Both files reference the authoritative amendment path
  (`.planning/amendments/PROJECT-AMENDMENT-2026-04-22-genome-wide-reframe.md`).
- No HARD-LOCKED file touched (ROADMAP.md / DECISIONS.md / STATE.md /
  m3-*/  / aou-egress-audit-log.md / bin/ / src/ / envs/ / tests/ /
  config/).
- Both tasks' automated verify commands exit 0.
- Single `git commit -m "docs(quick-260428-pj4): rewrite PROJECT.md +
  REQUIREMENTS.md to Amendment §12 post-pivot scope"` covers both files.
</success_criteria>

<output>
After completion, create
`.planning/quick/260428-pj4-rewrite-project-md-and-requirements-md-p/260428-pj4-SUMMARY.md`
documenting: which file(s) had drift from Amendment §12 (if any) and what
was reconciled; line-count delta per file; the verify-command outputs;
the commit SHA. Per Carter's STATE.md row policy, append a STATE.md row
ONLY if Carter requests it (Terminal A may be writing STATE.md
concurrently — coordinate via the calling agent rather than racing).
</output>
