---
phase: quick/260806-sr4
plan: 01
type: execute
wave: 1
depends_on: [quick/260806-pd3, quick/260806-b77]
subsystem: m3-source-freeze
tags: [freeze, provenance, test-infrastructure, m3, track-a, k-3]
autonomous: true
requirements: [SR4-SCOPE, SR4-K3, SR4-EXTEND]
baseline_rev: 1b5b8c6
files_modified:
  - tests/m3/source_freeze.py
  - tests/m3/test_source_freeze.py
  - tests/m3/test_source_freeze_pins.py
  - tests/m3/test_finemap_receipt_early_exit.py
  - tests/m3/test_qtl_coloc_allele_join.py
  - src/legacy/region_analysis/scripts/run_susie_rss.R
  - .planning/DECISIONS.md
  - .planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md
user_setup: []

must_haves:
  truths:
    - "ONE reusable utility answers 'has the CODE of this file changed since <ref>, ignoring comments, docstrings, blank lines and trailing whitespace?' for both R and Python, and every freeze gate on run_susie_rss.R is expressed through it."
    - "A comment-only edit to run_susie_rss.R is GREEN under the new mechanism -- demonstrated by landing the K-3 correction (1,944 -> 1,909 and 1,935 -> 1,900) WITHOUT re-pinning: the code pin stays at bf04199 across it."
    - "A code change to any of the five numeric-bearing R symbols goes RED and the failure message NAMES the symbol -- observed, not asserted."
    - "A code change CONCEALED behind a '#' inside a string literal goes RED -- observed -- and the same fixture is shown to be INVISIBLE to a naive regex stripper, so the hazard is demonstrated real rather than argued."
    - "The whole-file code-only floor covers the ~700-line top-level main body of run_susie_rss.R that lives inside no function, including all three toJSON emits -- proven by a control that perturbs code OUTSIDE every named symbol and still goes RED."
    - "The three genuinely-unmoved 'frozen' modules (plink_ld_to_npz.py, condition_ld_matrix.py, occlusion_span_filter.py) gain real enforcing gates pinned at bf16289, replacing per-task hand-checking."
    - "The four modules HANDOFF.json calls frozen-at-bf16289 but which have DEMONSTRABLY MOVED since are reported as a finding and NOT silently gated."
    - "DECISIONS.md carries DEC-2026-08-06-sr4-freeze-scope stating what is pinned, what is deliberately free, and why. The FREEZE convention is what existed nowhere; the entry also registers the NINE pre-existing ad-hoc comment-strippers as superseded-going-forward rather than claiming none existed."
    - "The acceptance test survives a future re-pin: clause (i) reads the live R_CODE_REF while the two historical clauses read K3_PRE_FIX_REF, a differential substrate annotated never-re-pin -- so the one-constant-per-file protocol is true on its FIRST exercise, not just today."
    - "TRACK-A-FROZEN-NUMBERS.md, the aggregator md5 lock and the three SH2B3 .fit.rds md5 pins are BYTE-UNTOUCHED, and DECISIONS.md is append-only (zero deleted lines)."
    - "tests/m3 is 0 failed / skipped == 31 / passed > 822 and tests/phase2 is 136 / 1 / 0."
  artifacts:
    - path: "tests/m3/source_freeze.py"
      provides: "the reusable comment-insensitive, string-literal-safe code-identity utility for R and Python. Its assertions expose a keyword-only actual_text= seam so every control can perturb an in-memory string instead of the working tree; the ACTUAL side is the working-tree read, never git show HEAD:"
      contains: "actual_text"
      min_lines: 120
    - path: "tests/m3/test_source_freeze.py"
      provides: "the utility's own proof: string-literal safety, the concealment hazard both ways, the mask-vs-ast cross-check, the outside-every-symbol control"
      contains: "concealed"
    - path: "tests/m3/test_source_freeze_pins.py"
      provides: "the live pins -- run_susie_rss.R whole-file + 5 symbols at bf04199; three Python modules + their top-level symbols at bf16289"
      contains: "R_CODE_REF"
    - path: "src/legacy/region_analysis/scripts/run_susie_rss.R"
      provides: "the K-3 correction; the ONLY change is two comment digits"
      contains: "1,909 measured"
    - path: ".planning/DECISIONS.md"
      provides: "DEC-2026-08-06-sr4-freeze-scope -- the source-freeze convention, recorded for the first time"
      contains: "DEC-2026-08-06-sr4-freeze-scope"
  key_links:
    - from: "tests/m3/test_finemap_receipt_early_exit.py::_assert_r_freeze_clean"
      to: "tests/m3/source_freeze.py::assert_code_frozen"
      via: "the rewired gate -- no git diff --exit-code on run_susie_rss.R survives anywhere"
      pattern: "assert_code_frozen"
    - from: "tests/m3/test_qtl_coloc_allele_join.py mid-control leak checks"
      to: "tests/m3/source_freeze.py::assert_unchanged_on_disk"
      via: "a SHA-FREE byte comparison against the string the test itself read"
      pattern: "assert_unchanged_on_disk"
    - from: "src/legacy/region_analysis/scripts/run_susie_rss.R:1018-1019"
      to: "the code pin at bf04199"
      via: "the comment-only K-3 edit, which the code pin must NOT notice"
      pattern: "1,909 measured"
---

<objective>
Rescope the `run_susie_rss.R` source freeze from **whole-file byte identity** to a
**comment-insensitive, symbol-scoped code-identity pin**, built as ONE reusable utility, and
land the **K-3** comment correction in the same window as the proof that the new mechanism
permits it.

**Purpose.** Two different things in this repo are called "frozen". Track A's frozen
**NUMBERS** are a recorded decision (`.planning/amendments/TRACK-A-FROZEN-NUMBERS.md`,
`DEC-2026-05-03-vcl-Item2`, the aggregator md5 `558fca45`, three SH2B3 EUR `.fit.rds` md5s) --
**that stays exactly as it is and this plan does not touch it.** The **source-file** freeze
(`git diff --exit-code <SHA> -- run_susie_rss.R`) appears in **no** `DECISIONS.md` entry at
all. It grew up downstream as a proxy for the first, and it is **scoped wrong: it pins BYTES
when what needs protecting is NUMERIC BEHAVIOUR.**

**The justification for *some* guard is real and is preserved.** BLOCKER-1 proved this pipeline
can move Track A numbers **silently**: fixing the LD read path moved EUR `r[1,2]` 0.1 -> 0.9,
credible sets 3 -> 10, nonzero PIPs 200 -> 78, while `ld_status` and `ld_overlap_fraction` --
the two fields anyone would check to argue nothing moved -- stayed **byte-identical**. And
there is no cheap regression oracle: re-checking the AFR side needs the AoU perimeter and the
~11-day billed fire. **The guard is not being weakened. It is being aimed at the right target.**

**The cost of the current over-broad scope is concrete and just landed.** K-3: a known-WRONG
census figure (`1,944`; correct `1,909`) ships inside a **comment** at
`run_susie_rss.R:1018-1019` because correcting a comment would have "cost an unfreeze". **A
rule that makes shipping a known falsehood the cheaper option is mis-scoped.**

**Output.** One utility module, two new test modules, two rewired pre-existing gates, two
corrected digits in a comment, three previously-UNGATED modules turned into real gates, and the
convention itself written down for the first time. `$0`, NC State only, zero perimeter contact,
**no AoU fire**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md
@.planning/quick/260806-pd3-close-blast-radius-finding-k-1-restore-v/260806-pd3-PLAN.md
@src/legacy/region_analysis/scripts/run_susie_rss.R
@tests/m3/test_finemap_receipt_early_exit.py
@tests/m3/test_qtl_coloc_allele_join.py
@src/python/plink_ld_to_npz.py
@src/python/condition_ld_matrix.py
@src/python/occlusion_span_filter.py
@.planning/DECISIONS.md
</context>

<authorizations>
Granted by Carter 2026-08-06 in response to a written proposal -- "yes, proceed".
**Strictly scoped. Anything outside these bounds is a STOP-and-surface, not a judgement call.**

**AUTH-SR4-RESCOPE** -- replace the whole-file byte pins on
`src/legacy/region_analysis/scripts/run_susie_rss.R` with a comment-insensitive, symbol-scoped
numeric pin. **Editing `FROZEN_R_REV` / `FREEZE_REF` and their consumers is authorized**,
including renaming the constants and rewriting the prose that frames them.

**AUTH-SR4-K3** -- an unfreeze of `src/legacy/region_analysis/scripts/run_susie_rss.R`,
**scoped to the K-3 comment correction ONLY**: `1,944` -> `1,909` and `1,935` -> `1,900` at
`:1018-1019`. **NO LOGIC MAY CHANGE.** This is the FIRST edit the new mechanism must permit and
it doubles as the acceptance test.

**AUTH-SR4-EXTEND (derived, narrow).** Adding NEW enforcing gates over files that have **no
enforcing test today** creates no new constraint on any existing assertion and removes no
coverage. Scoped to `src/python/plink_ld_to_npz.py`, `src/python/condition_ld_matrix.py` and
`src/python/occlusion_span_filter.py`, **pinned at `bf16289`, which they are already 0-diff
against (MEASURED -- see `<verified_anchors>`)**. It does **NOT** extend to any file that has
moved since `bf16289`.

**NOT AUTHORIZED, and each is a STOP:** any other edit to `run_susie_rss.R` (logic, whitespace,
any comment other than the two K-3 digits); `.planning/amendments/TRACK-A-FROZEN-NUMBERS.md`,
the aggregator md5 lock, the three SH2B3 `.fit.rds` md5 pins; weakening or deleting any existing
assertion; gating any file that has moved since `bf16289`; m3-06 (stays HELD -- no NaN-to-0, no
`condition_ld_matrix.py` *behaviour* change; a read-only pin over it is inside AUTH-SR4-EXTEND);
`src/snakemake/rules/finemap.smk` and `src/snakemake/scripts/ld_allele_join.R` (see
`<the_narrative_deferral>`).
</authorizations>

<verified_anchors>
**Every fact below was MEASURED at HEAD `1b5b8c6`, branch `m3-W2-aou-deltas`, tracked tree
clean, during planning. Re-derive them before editing anyway; a mismatch is a deviation to
REPORT, not to absorb** (`[[feedback_check_plan_against_red_before_executing]]`).

### The freeze subject

| Fact | Measured |
|---|---|
| `git diff --exit-code bf04199 -- run_susie_rss.R` | **CLEAN** |
| file length | **1,357** lines (`wc -l` / `splitlines()`; `split("\n")` returns 1,358 because of the trailing newline -- do NOT report 1,358 as a deviation) |
| **code-only** lines (comments + blanks removed) | **818** |
| ⇒ **40% of the frozen file is comment** | this is why the byte pin was so brittle |
| K-3 digits | `1,944` at **`:1018`**, `1,935` at **`:1019`** -- LIVE, exactly as the brief states |
| repo-wide `1,944` / `1,935` outside `.planning/` | **only** these two lines (`test_finemap_receipt_early_exit.py:560` mentions 1,944 as an already-CORRECTED historical narrative -- leave it) |

### The five numeric-bearing R symbols -- RE-DERIVED, all live, spans measured

| Symbol | Def line | Span (lines) | Chars |
|---|---|---|---|
| `regularize_ld` | 25 | 25-28 | 103 |
| `run_susie_with_ladder` | 30 | 30-67 | 1,778 |
| `safe_region_id` | 69 | 69-71 | 81 |
| `load_ld_matrix` (contains the nested `match_indices_allele_aware` allele join) | 142 | **142-601** | 20,933 |
| `assert_declared_ld_authoritative` | 642 | 642-657 | 997 |

⚠ **`option_list <- list(` is at `:659`. Lines ~659-1357 are a TOP-LEVEL MAIN BODY inside NO
function**, holding the fitting flow and all three `toJSON` emits (**`:938`, `:970`, `:1357`** --
verified). **The whole-file code-only floor is the ONLY thing covering it. The floor is not
optional and T1 must prove it covers that span.**

### The string-literal hazard -- LATENT in R, LIVE in Python

| File | `#` inside a string literal | `{`/`}` inside a string literal |
|---|---|---|
| `run_susie_rss.R` | **0** | **0** |
| `plink_ld_to_npz.py` **docstrings** | **1** (`:164`) | -- |
| `occlusion_span_filter.py` **docstrings** | **1** (`:1`) | -- |
| `plink_ld_to_npz.py` | 0 | **23** (f-strings) |
| `condition_ld_matrix.py` | 0 | **5** |
| `occlusion_span_filter.py` | 0 | **8** |

⇒ **The `#`-in-string hazard cannot be exercised by any live file's EXECUTABLE code. The
negative control MUST synthesize it.** ⚠ **But it IS live inside two docstrings**
(`plink_ld_to_npz.py:164`, `occlusion_span_filter.py:1`), so `_mask_strip_py` **must handle `#`
inside TRIPLE-QUOTED strings** or STEP 2(g)'s cross-check will disagree for a reason that has
nothing to do with the hazard. The brace-in-string hazard **is live in Python**, so any
brace-walk over raw Python text is already wrong -- which is why T1 uses `ast` for Python, not
brace-walking.

### ⚠ NINE ad-hoc comment-strippers ALREADY EXIST. MEASURED. Two are in the file T2 edits.

**This is the strongest argument for the utility, and it materially changes T3's wording.**

| Site | Name | Lang | Note |
|---|---|---|---|
| `test_qtl_coloc_allele_join.py:125` | `strip_py_comments` | py | in a file T2 edits |
| **`test_qtl_coloc_allele_join.py:1423`** | **`r_code_only`** | **R** | ⭐ **a full R stripper WITH string-literal + backslash handling, in the file T2 edits** |
| `test_qtl_coloc_ld_resolution.py:198` | `code_only` | py | **imported at `test_qtl_coloc_allele_join.py:91`** |
| **`test_variant_catalog_fallback_legacy_semantics.py:110`** | **`_code_lines`** | **R** | ⭐ **an R code-only comparator against a past revision -- a hand-rolled instance of THIS VERY UTILITY** |
| `test_curated_m2_crosswalk_drift.py:341` | `_strip_py_comments` | py | |
| `test_ld_npz_to_rds_bounded.py:153` | `_strip_r_comments` | R | |
| `test_occlusion_lockstep_wiring.py:421` | `_code_only` | py | |
| `test_ld_allele_aware_wiring.py:256` | `_strip_comments` | py | |
| `test_ld_panel_aou_orphan_and_strict.py:492` | `_strip_hash_comments` | py | |

**Three consequences, each of which changes the plan:**

1. **T3 MUST NOT write "a convention that previously existed nowhere" about the stripper.** What
   existed nowhere is the **FREEZE convention** (`DECISIONS.md` has no entry). Nine strippers
   very much existed. Writing otherwise while citing `[[feedback_extract_reusable_utilities]]`
   would be breaking the rule in the very sentence that invokes it.
2. **T2 must not leave `test_qtl_coloc_allele_join.py` holding FOUR similarly-named strippers**
   with different semantics -- that is the K-1 defect shape (one name, several meanings) this
   plan exists to end. T2 STEP 4 **registers** them; it does **not** refactor them.
3. **`r_code_only` is a FREE independent R implementation sitting in the file T2 edits**, so the
   R twin of the Python mask-vs-`ast` cross-check costs almost nothing. T1 STEP 2(j) adds it.

⚠ **DO NOT REFACTOR THE OTHER EIGHT CALL SITES.** Each is load-bearing for a different
assertion with its own deliberate semantics (`strip_py_comments` KEEPS triple-quoted strings
because a Snakemake `shell:` body IS one; `code_only` DELETES them). Rewiring them is a
separate, much larger change with real regression risk and **no authorization**. This plan
**registers** them and supersedes them **going forward**.

### ⚠ PLAN-FACT CORRECTION -- the brief's "other frozen files" set is WRONG. MEASURED.

`.planning/HANDOFF.json:14` claims *"All 7 pinned files 0-line diff vs bf16289"*. **Four of them
are not.** `git diff --numstat bf16289 HEAD`:

| File | Diff vs `bf16289` | Last touched | Verdict |
|---|---|---|---|
| `src/python/plink_ld_to_npz.py` | **0** | 2026-07-03 | **GENUINELY FROZEN -> gate it** |
| `src/python/condition_ld_matrix.py` | **0** | 2026-07-07 | **GENUINELY FROZEN -> gate it** |
| `src/python/occlusion_span_filter.py` | **0** | 2026-07-15 | **GENUINELY FROZEN -> gate it** |
| `src/python/occlusion_manifest.py` | +46 / -8 | 2026-08-04 (`bf963df`) | **HAS MOVED -- do NOT gate** |
| `src/python/occlusion_present_rate_scan.py` | +154 / -21 | 2026-08-04 (`fac9a93`) | **HAS MOVED -- do NOT gate** |
| `src/python/drop_occluded_from_sumstats.py` | +97 / -24 | 2026-08-04 (`bf963df`) | **HAS MOVED -- do NOT gate** |
| `src/scripts/ld_npz_to_rds.R` | +313 / -62 | 2026-08-05 (`57b381f`) | **HAS MOVED -- do NOT gate** |
| `src/snakemake/schemas/pipeline.schema.yaml` | +119 / -0 | 2026-08-06 (`2563451`) | **HAS MOVED -- do NOT gate** |

**`bf16289` appears NOWHERE in `src/`, `tests/`, `config/`, `Snakefile` or `scripts/` -- there is
literally zero enforcement of any of these today.** Only two test files anywhere run
`git diff --exit-code`, and both point at `run_susie_rss.R`. The brief's description of these as
"ritual rather than machinery" is exactly right; it is worse than it looked, because the ritual
has also been reporting a claim that is **false for 5 of 8 files**.

**Consequence for scope.** Gating a file that changed three times in the last three days would
manufacture a nuisance-repin timebomb, and declaring a moving file frozen is a **decision**, not
an inference. **Gate the three that genuinely have not moved. Report the other five as a
finding, register them for Carter, and do NOT gate them.**

### Environment + baselines, MEASURED

* Python `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python` (3.11 -- `ast.unparse`
  available). Rscript `/rs1/researchers/c/ckclinto/conda_envs/m3-r-ld/bin/Rscript`.
* `tests/m3` collects **853** = **822 passed + 31 skipped**; `tests/phase2` collects **137** =
  **136 + 1**. Both confirmed by `--collect-only` at `1b5b8c6`.
* `tests/m3/__init__.py` EXISTS; the established sibling-import pattern is
  `sys.path.insert(0, Path(__file__).resolve().parent)` then a bare
  `from <sibling> import ...` (`test_qtl_coloc_allele_join.py:78-91`). Follow it exactly.
* `git cat-file -t bf16289` -> `commit` (2026-07-16 handoff close). `bf04199` -> the K-1 re-freeze.

### The four `FREEZE_REF` / `FROZEN_R_REV` consumers -- and they do TWO DIFFERENT JOBS

⚠ **This distinction is the highest-value insight in the plan. Do not flatten it.**

| Site | Job | Action |
|---|---|---|
| `test_finemap_receipt_early_exit.py:344-353` `_assert_r_freeze_clean()`, called at `:341` and `:357` | **JOB A -- THE FREEZE GATE** | **RESCOPE** to code-only |
| `test_qtl_coloc_allele_join.py:1415-1420` `test_run_susie_rss_is_zero_diff_vs_the_freeze` | **JOB A** | **RESCOPE** to code-only |
| `test_qtl_coloc_allele_join.py:730-735` (inside NC-2g, after an in-memory alteration) | **JOB B -- WORKING-TREE LEAK DETECTION.** `real = SUSIE_R.read_text()` at `:702`; the assertion asks "did my alteration leak onto disk?" | **DO NOT make this comment-insensitive -- that would WEAKEN it.** Convert to a **SHA-FREE byte** comparison against `real`, **plus the capture guard below** |
| `test_qtl_coloc_allele_join.py:755-759` (same, `real` read at `:741`) | **JOB B** | same |

A code-only check at a JOB B site would let a leaked **comment** edit through unnoticed. A
fixed-SHA byte check there is a timebomb for no reason -- the test already holds the exact bytes
it needs.

⚠ **BUT "strictly stronger" IS NOT FREE -- there is a real gap, and it must be closed rather
than claimed away.** For a leak that occurred **BEFORE** `real` was read, `real` captures the
already-leaked content and the new check passes where the old byte-diff went RED. **Close it at
the capture site:** immediately after each `real = SUSIE_R.read_text()` (`:702`, `:741`) add

```python
assert real == git_show("HEAD", SUSIE_R_REL), (
    "run_susie_rss.R was already modified in the working tree BEFORE this control "
    "captured it -- the leak guard below would be comparing against leaked bytes"
)
```

SHA-free (`HEAD` is symbolic, so no timebomb) and it strictly subsumes the old fixed-SHA check.
**Only with the capture guard is the "strictly stronger than both" claim true as written --
do not make the claim without it.**
</verified_anchors>

<the_central_design_decision>
## The K-3 edit costs NO re-pin. That is the whole point, and it removes pd3's commit cascade.

pd3 needed two commits because its edit was a **code** change, so the new SHA did not exist
until the edit landed. **K-3 is a COMMENT change.** Under a code-only pin, `bf04199` remains a
valid pin **across** it -- **VERIFIED during planning**: the code-only line list of
`run_susie_rss.R` with `1,944 -> 1,909` and `1,935 -> 1,900` applied is **byte-identical** to
the code-only line list at `bf04199`.

**Therefore: DO NOT RE-PIN `run_susie_rss.R`. Keep `bf04199`.**

That is not laziness -- it is a stronger claim and it is the acceptance demonstration:

* `bf04199` keeps meaning **"no CODE has moved since the K-1 closure"**. Re-pinning would
  degrade it to "no code has moved since yesterday's typo fix".
* **Acceptance item 2 is satisfied by construction**: the comment-only edit is provably free,
  because the pin does not move and the gate stays GREEN across it.

⚠ **BUT the constants MUST BE RENAMED.** Keeping the name `FROZEN_R_REV` while silently changing
what it asserts is precisely the defect this project fought in K-1 -- a pre-existing symbol
acquiring a second meaning. Rename:

* `FROZEN_R_REV` -> **`FROZEN_R_CODE_REV`** (`test_finemap_receipt_early_exit.py:60`)
* `FREEZE_REF` -> **`FREEZE_CODE_REF`** (`test_qtl_coloc_allele_join.py:110`)

⇒ **The re-pin ordering hazard is ABSENT from this plan for `run_susie_rss.R`.** The Python
gates likewise pin at `bf16289`, which those files are already 0-diff against. **No task in
this plan leaves the tree in an expected-RED state.** Every commit is independently green.

**T1 lands the MECHANISM and proves it before T2 trusts it with the frozen file.** That ordering
is mandatory: a mechanism that has not been observed RED is not evidence
(`[[feedback_green_assertion_needs_a_negative_control]]`).

### The one honest irony, stated plainly

A code-only pin against a **fixed SHA** still goes RED on every legitimate **code** change.
**That is INTENDED** -- a code change to a frozen file *should* require a decision. What it must
never be is red for a reason nobody cares about. **The re-pin protocol, in one sentence:** when
an authorized unfreeze changes code, update **exactly one constant per FROZEN SUBJECT** --
`R_CODE_REF` for `run_susie_rss.R`, `PY_CODE_REF` for the three Python modules -- to the SHA of
the commit that landed the change, and nothing else. **`FROZEN_R_CODE_REV` and
`FREEZE_CODE_REF` are IMPORT ALIASES of `R_CODE_REF`, not separate constants** (B5), so they
follow automatically. Write that sentence into the DECISIONS entry and into each module's
docstring, next to the constant.
</the_central_design_decision>

<the_narrative_deferral>
## Two prose sites are DEFERRED, deliberately -- and named so the record is not left half-applied.

`src/snakemake/rules/finemap.smk:502` and `src/snakemake/scripts/ld_allele_join.R:33` / `:99`
narrate `bf04199` as a **byte** freeze. After this plan it is a **code** freeze. Those sentences
become imprecise.

**They are NOT edited here.** Both are live fire-path files -- `finemap.smk` is the rule whose
`shell:` body four separate test modules extract and assert against, and `ld_allele_join.R` is
sourced at runtime by `run_qtl_coloc.R`. Adding them to the diff spends fire-path risk to fix
prose, when `DEC-2026-08-06-sr4-freeze-scope` is the canonical record of the scope change and is
cross-referenced from both test modules.

⚠ **`test_finemap_receipt_early_exit.py:568` and `finemap.smk:547`** say
`"unreachable from any tree at or after bf04199"`. That is a **HISTORICAL claim about the K-1
window and it remains TRUE**. Re-pinning or reinterpreting it would falsify history. **Leave
both.** Hand the three deferred prose sites to the orchestrator in T3 STEP 5.
</the_narrative_deferral>

<hard_rules>
1. **A GREEN ASSERTION IS EVIDENCE ONLY IF YOU HAVE SEEN IT FAIL.** Every new assertion ships
   with a negative control **OBSERVED RED** and quoted raw in the SUMMARY. This project has been
   bitten **eight** times by assertions structurally incapable of their job.
2. **THE `.pyc` TRAP.** `importlib` validates cached bytecode on `(mtime_seconds, size)`, so a
   byte-length-identical edit reverted within the same second runs **stale bytecode**
   (`[[feedback_negative_control_defeated_by_bytecode_cache]]`). **Every negative control in
   this plan perturbs an IN-MEMORY string or reads a past revision via `git show`. NOTHING
   reverts a file on disk and re-imports.** This is the design, not shaping.
3. **⚠ THE CORRECTNESS HAZARD IS THE STRIPPER ITSELF.** A stripper that mangles string literals
   could make a **REAL code change invisible** -- turning this guard into exactly the class of
   structurally-incapable assertion it exists to end. `#` inside a string is **NOT** a comment
   (`paste0("#", x)`, `gsub("#.*", "", s)`, `re.compile("#[0-9]+")`). The concealment control
   (T1 NC-SR3) is **mandatory and load-bearing**, and it must ALSO show a naive stripper missing
   the same fixture -- otherwise the control proves nothing about the hazard being real.
4. **⚠ TWO MASK INVARIANTS, BOTH LOAD-BEARING, BOTH RE-MEASURED AFTER CHECKER REVIEW.**
   The R stripper is length-preserving and recovers the code prefix as
   `orig[:len(masked_line.rstrip())]`. Therefore:
   * **(4a) NEWLINES MUST BE WRITTEN THROUGH UNCHANGED, EVEN INSIDE A STRING.** ⚠ **A REAL BUG
     FOUND IN THE PLANNING PROTOTYPE BY THE CHECKER'S W1 FIXTURE.** If a `\n` inside a
     multi-line string is replaced by the filler, the masked text loses a line break, and
     `zip(masked.split("\n"), orig.split("\n"))` **misaligns every subsequent line** -- so the
     comparison drifts wholesale and silently. **MEASURED:** on
     `q <- "line1\nline2 trailing   \nend"` the buggy mask yields `['q <- "line1']` (two lines
     silently vanish); the fixed mask yields
     `['q <- "line1', 'line2 trailing', 'end"']`. It is invisible on `run_susie_rss.R` today
     (818 code lines either way), which is exactly why it needs a **permanent synthetic test**.
   * **(4b) THE FILLER MUST BE NON-WHITESPACE (`_`).** ⚠ **PLAN-FACT CORRECTION -- the original
     justification did NOT reproduce.** `s <- "a  "` is IDENTICAL under both fillers (the
     closing `"` is non-whitespace, so `rstrip()` stops there), and so is the whole real file.
     The rule is correct for **MULTI-LINE** strings: with (4a) fixed, `_` retains
     `line2 trailing` while a space filler drops it entirely (`['q <- "', 'end"']`).
     **MEASURED both ways after the correction.**
   T1 STEP 2(k) pins both invariants permanently; no live file exercises either.
5. **SKIPS ARE FIXED AT 31 AND 1.** A new test landing as a SKIP is **not evidence**. All three
   new modules must be **pure source text + `git` + stdlib** -- **no R execution, no Snakemake,
   no toolchain fixture** -- so they are structurally incapable of skipping. Net test count may
   only go UP.
6. **NO ASSERTION MAY BE WEAKENED OR DELETED.** JOB B sites become *stronger* (SHA-free byte
   equality). If any rewiring would reduce what a site detects, **STOP and surface**.
7. **TRACK A IS UNTOUCHABLE.** `.planning/amendments/TRACK-A-FROZEN-NUMBERS.md`, the aggregator
   md5 `558fca45`, the three SH2B3 EUR `.fit.rds` md5 pins: **byte-unchanged, asserted in T3.**
8. **Environment.** Never `conda activate`. Python
   `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python`; Snakemake
   `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/snakemake`; Rscript
   `/rs1/researchers/c/ckclinto/conda_envs/m3-r-ld/bin/Rscript`.
9. **Git:** explicit paths only. **NEVER `git add .` or `git add -A`** (multi-terminal GPFS
   tree). No worktree isolation.
   ⚠ **EVERY scope-diff gate MUST exclude `':!.planning/quick/260806-sr4-*'`.** The orchestrator
   commits the PLAN doc **before** execution starts (precedent: b77's PLAN landed in `9b2d431`,
   before its T1 `9c0c67b`), so a bare `git diff --name-only 1b5b8c6 HEAD` includes a file the
   executor never wrote and both equality gates fire a **FALSE STOP**.
10. **NEVER trigger the AoU fire.** No `gsutil` / `gcloud` / `bq` / `wb` / `dataproc` /
    `hailctl` anywhere in the session or the diff. Grep the diff before each commit.
11. `tests/m3/sparse_parent_benchmark.tsv` is rewritten by the suite on every run (timing
    jitter). Restore with `git checkout --`. **Never commit it.**
12. **Do NOT write** `.planning/STATE.md`, `.planning/HANDOFF.json`,
    `.planning/phases/m3-aou-afr-ld-panel-build/.continue-here.md`, `ROADMAP.md`, or any PLAN
    doc -- **all orchestrator-owned**. The SUMMARY **is** written per `<output>` but **NOT
    committed**. `DECISIONS.md` and `deferred-items.md` **are** in scope.
13. **ANY unexpected red pre-existing test is a STOP-and-surface**, never an edit and never a
    weakened assertion. The three authorizations above are the complete list.
</hard_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (COMMIT 1): build the code-identity utility, prove it cannot be fooled, and gate the three genuinely-frozen Python modules</name>
  <files>tests/m3/source_freeze.py, tests/m3/test_source_freeze.py, tests/m3/test_source_freeze_pins.py</files>

  <behavior>
  - A **comment-only** edit and a **docstring-only** edit to any supported file are GREEN.
  - A **code** edit is RED, and when it lands inside a named symbol the message NAMES the symbol.
  - A code edit **concealed behind a `#` inside a string literal** is RED -- and the SAME fixture
    is shown **invisible** to a naive `re.sub(r"#.*$", "", line)` stripper, proving the hazard.
  - A code edit **outside every named symbol** (the R top-level main body, ~:659-1357) is RED via
    the whole-file floor -- the floor is proven load-bearing, not assumed.
  - For Python, the hand-written mask stripper and the `ast`-based canonicaliser **agree** on all
    three real modules -- two independent implementations, one answer.
  - Symbol extraction is **non-vacuous**: each of the five R symbols is found exactly once, its
    span is non-empty, and it contains a second expected marker.
  - Both mask invariants are pinned: filling a `\n` inside a multi-line string drops lines, and a
    whitespace filler drops trailing content -- neither is exercised by any live file.
  - The ACTUAL side is the working tree, asserted at module scope via the utility's own stripper;
    no production call site supplies `actual_text`; a non-SHA `ref` is rejected.
  - `plink_ld_to_npz.py`, `condition_ld_matrix.py`, `occlusion_span_filter.py` are code-identical
    to `bf16289`, whole-file and per top-level symbol (13 + 3 + 6 = **22** symbols).
  - Zero skips. Zero R subprocesses. Zero Snakemake.
  </behavior>

  <action>
**STEP 0 -- RE-DERIVE BEFORE WRITING ANYTHING.** Confirm at HEAD: `git rev-parse --short HEAD`
is `1b5b8c6`; the tracked tree is clean; `git diff --exit-code bf04199 -- run_susie_rss.R` is
CLEAN; the `bf16289` numstat table in `<verified_anchors>` reproduces exactly (**all 8 rows** --
if any of the three "0" rows is non-zero, that file leaves AUTH-SR4-EXTEND and you STOP); the
five R symbol def-lines are `25 / 30 / 69 / 142 / 642`; `option_list <- list(` is at `:659`.
**Report every mismatch. Never edit against a stale map.**

**STEP 1 -- `tests/m3/source_freeze.py`. THE UTILITY. Non-`test_`-prefixed so pytest does not
collect it.** Module docstring must state: what this pins (CODE), what it deliberately ignores
(comments, docstrings, blank lines, trailing whitespace), **the one-constant-per-file re-pin
protocol** from `<the_central_design_decision>`, and a cross-reference to
`DEC-2026-08-06-sr4-freeze-scope`.

Public API -- keep it small; this is the reusable utility the project rule
(`[[feedback_extract_reusable_utilities]]`) asks for:

```python
LANG_R = "r"
LANG_PY = "py"

def strip_to_code(text: str, lang: str) -> str: ...
def code_lines(text: str, lang: str) -> list[str]: ...
def symbol_spans(text: str, lang: str) -> dict[str, tuple[int, int]]: ...
def symbol_code(text: str, lang: str, name: str) -> list[str]: ...
def git_show(ref: str, rel_path: str) -> str: ...          # lru_cache'd

# ⚠ THE INJECTION POINT. Without `actual_text` these assertions resolve BOTH
# sides internally and are therefore UNFALSIFIABLE without writing the working
# tree -- which hard rule 2 forbids. Controls pass a perturbed string; production
# callers pass nothing and get the working-tree read.
def assert_code_frozen(rel_path, ref, lang, symbol=None, *,
                       actual_text: str | None = None) -> None: ...
def assert_unchanged_on_disk(path, expected_text: str, *,
                             actual_text: str | None = None) -> None: ...  # JOB B, SHA-free
```

⚠ **`actual_text` is a TEST-CONTROL SEAM, not a production parameter.** It is keyword-only and
defaults to `None`. **No production call site may pass it** -- asserted mechanically and permanently by STEP 2(m),
an AST walk (a `grep` is evaded by `actual_text = forged` and `**{"actual_text": ...}`).
Without it, three of this plan's own negative controls (NC-SR2, NC-SR5, NC-SR8) are
**unrunnable** except by writing the file, monkeypatching `read_text`, or degrading to a bare
`code_lines(a) != code_lines(b)` that never exercises the message path -- leaving
`must_haves.truths` #3 ("the failure message NAMES the symbol -- observed") untested. **That is
precisely the structurally-incapable-assertion class this plan exists to end.**

**(a) R -- length-preserving mask, then brace-walk the MASK, slice the ORIGINAL.**
Walk the text once. Inside a comment (`#` to end of line, only when not inside a string) write a
space. Inside a string (`"` or `'`, honouring `\` escapes) write `_` **for the contents, keeping
the delimiters** -- **hard rule 4: the filler must be non-whitespace** -- **except `\n`, which
is ALWAYS written through unchanged even inside a string (hard rule 4a); filling it deletes a
line and misaligns every subsequent `zip()` pair, and the length invariant does NOT catch it**
(measured: `len(masked) == len(text)` holds under both variants, so test 2(i) is structurally
blind to 4a). Assert `len(masked) == len(text)` -- that invariant is what makes offset-slicing
safe.
`code_lines` = for each (masked, original) line pair, take `original[:len(masked_line.rstrip())]`,
`.rstrip()` it, drop it if blank.
`symbol_spans` = for each `^<name>\s*<-\s*function` match **in the MASKED text** (`re.M`), find
the first `{` after the signature and balance-walk **the masked text** to its partner; return
offsets into the original. **Brace-walking the masked text is what makes multi-line signatures
(`load_ld_matrix` `:142-143`, `assert_declared_ld_authoritative` `:642-643`) and any future
brace-in-string safe.**

**(b) Python -- `ast`, not brace-walking. Braces inside f-strings are LIVE (23 in
`plink_ld_to_npz.py`), so a brace-walk over Python is already wrong.**
`strip_to_code` = `ast.parse` -> drop the leading `Expr(Constant(str))` docstring of every
`Module` / `FunctionDef` / `AsyncFunctionDef` / `ClassDef` (substituting `ast.Pass()` if that
empties a body) -> `ast.fix_missing_locations` -> `ast.unparse`. Comments never enter the AST, so
this is comment-insensitive **by construction**; a `#` inside a string is a `Constant` and
survives. `symbol_spans` = top-level `FunctionDef` / `AsyncFunctionDef` / `ClassDef`
`lineno`/`end_lineno`.
**⚠ Note in the docstring:** `ast.unparse` output can differ across Python minor versions -- this
is harmless **because both sides of every comparison are unparsed by the same interpreter in the
same process**. Say so, so nobody "fixes" it later.
Also implement `_mask_strip_py` (the R-style mask, extended to triple quotes and `\` escapes) --
**not** as the primary path but as the cross-check in STEP 2(g).

**(c) `assert_code_frozen`** compares the **ACTUAL** side against the **REFERENCE** side as an
**ordered list**, so a reordering fails too.

⚠ **THE ACTUAL SIDE IS `Path(rel_path).read_text()` -- THE WORKING TREE. NEVER
`git_show("HEAD", rel_path)`.** Only the *reference* side uses `git_show`. This is not a
stylistic choice:
`_assert_r_freeze_clean()` is called **mid-test** at `test_finemap_receipt_early_exit.py:341`
and `:357` specifically to catch a working-tree write (the source comment at `:340` says *"the
frozen file is READ here, never written"*). **If the actual side became `git show HEAD:`, the
gate would go structurally blind to every uncommitted change and JOB A's leak detection would
vanish with nothing turning red** -- hard rule 6 forbids exactly that. Put this paragraph in the
module docstring, next to the function.

`actual_text=` overrides the working-tree read **for controls only**. With `symbol=` the
comparison uses `symbol_code` and the message leads with the symbol name. Messages must include
the first differing line with its index and a `+`/`-` prefix, must end with the literal re-pin
instruction naming the ONE constant to update, and must name the alternative hypothesis:
*"either the code moved, or the extractor is reading the wrong region."*

**(d) `assert_unchanged_on_disk(path, expected_text)`** -- `Path(path).read_text() ==
expected_text`, **no SHA, no git**. This is JOB B. Message: "an in-memory alteration leaked onto
the working tree". Same `actual_text=` seam, same controls-only rule.

**STEP 2 -- `tests/m3/test_source_freeze.py`. THE UTILITY'S OWN PROOF.** Pure stdlib + `git`.
Every fixture below is an **in-memory string** (hard rule 2).

⚠ **FIXTURE ANCHORS ARE NAMED, NOT LEFT TO JUDGEMENT.** The most obvious unique numeric literal
in `run_susie_rss.R` is `1,944` -- and it is a **trap twice over**: it lives inside a **comment**
(so a code-anchored control using it fails immediately in T1) and **T2 deletes it** (so a
comment-anchored control using it goes RED after commit 2, in a task that never touched the
test). **RULE: no T1 fixture may anchor on `1,944`, `1,935`, or ANY text within
`run_susie_rss.R:1013-1025`.** Use these instead, each verified unique during planning:

| Fixture | Anchor | Kind |
|---|---|---|
| (a) comment-only, R | `# THE TRAP (m3-04c Task 1b)` (`:144`) | comment |
| (a) comment-only, py | the first `#` comment line of each module, located programmatically | comment |
| NC-SR1 code, R | `regularize_ld <- function(R, eps = 1e-4) {` -> `eps = 1e-3` | code |
| NC-SR1 code, py | a module-level constant/identifier asserted `count(...) == 1` first | code |
| NC-SR3 carrier, R | inserted immediately after the `regularize_ld` signature | synthetic |

**Every fixture asserts `src.count(anchor) == 1` before perturbing**, or the control alters
nothing and passes for free.

  a. **`test_a_comment_only_edit_is_invisible`** -- parametrized over `run_susie_rss.R` (R) and
     the three Python modules. Perturb a comment in memory; assert `code_lines` unchanged **and**
     that the perturbed text differs from the original (**non-vacuity**: a no-op replacement
     would make this pass for free).
  b. **`test_a_docstring_only_edit_is_invisible`** -- Python only; same non-vacuity guard.
  c. **NC-SR1 `test_a_code_edit_is_detected`** -- parametrized over all four files. Perturb a
     numeric literal or an identifier; assert `code_lines` differs. Assert the anchor occurs
     **exactly once** first, or the control alters nothing.
  d. **NC-SR2 `test_a_code_edit_inside_a_named_symbol_names_that_symbol`** -- parametrized over
     the five R symbols. Perturb inside the symbol, then
     `pytest.raises(AssertionError, match=<name>)` on
     `assert_code_frozen(SUSIE_R_REL, R_CODE_REF, LANG_R, symbol=<name>,
     actual_text=<perturbed>)`. **This is the ONLY shape that exercises the message path**
     (B1); a bare `code_lines(a) != code_lines(b)` would leave `must_haves.truths` #3 untested.
     Also assert `SUSIE_R.read_text()` is unchanged after each control ran.
  e. **NC-SR3 -- ⚠ THE LOAD-BEARING CONTROL. `test_a_code_edit_concealed_behind_a_hash_in_a_string_is_detected`.**
     Two halves, both asserted, R and Python:
       1. **Build the carrier**, because no live file has a `#` inside a string (measured: 0/0/0/0
          -- so the fixture MUST be synthetic). R: insert
          `.tag <- gsub("#.*", "", "regularize#v1")` immediately after
          `regularize_ld <- function(R, eps = 1e-4) {`. Python: insert a two-line helper whose
          body is `return re.sub("#.*", "", "cond#v1")`.
       2. **Conceal a code change AFTER the in-string `#`, on the SAME LINE** -- R: append
          `; eps <- 1e-3`. Python: append ` or (1e-3)`.
       3. Assert the naive stripper `re.sub(r"#.*$", "", line)` sees **NO** difference between
          carrier and concealed -- **this is what proves the hazard is real rather than
          hypothetical**; and assert `code_lines` **DOES**. **VERIFIED during planning: naive =
          False, masked/ast = True, both languages.** Ship the naive stripper as a
          `_naive_strip_for_contrast` helper inside the TEST module, never in the utility.
  f. **NC-SR4 `test_the_whole_file_floor_covers_code_outside_every_named_symbol`** -- the floor's
     reason to exist. First **prove the gap**: assert the R top-level main body (`option_list <-`
     at `:659` through EOF) is inside **none** of the five symbol spans. Then perturb a line in
     that span (e.g. one of the three `toJSON` emit lines at `:938` / `:970` / `:1357`) and assert
     the **whole-file** comparison goes RED while **all five symbol** comparisons stay GREEN.
     **This is the mechanical proof that symbol pins alone would have missed it.**
  g. **`test_the_python_mask_stripper_agrees_with_the_ast_canonicaliser`** -- parametrized over
     the three Python modules. Two independent implementations, one answer. Compare on
     whitespace-normalised token streams, not raw text (`ast.unparse` reformats). If they cannot
     be made to agree cheaply, **say so and drop this test rather than weakening either side** --
     record it as a deviation; the `ast` path is the primary and NC-SR3 already covers the hazard.
  h. **`test_symbol_extraction_is_non_vacuous`** -- parametrized over the five R symbols: the def
     regex matches exactly once; the span is non-empty; the extracted code contains a second
     expected marker (e.g. `load_ld_matrix` contains `match_indices_allele_aware`;
     `regularize_ld` contains `eps`). **A symbol pin over an empty slice is green forever.**
  i. **`test_the_mask_is_length_preserving`** -- `len(mask(t)) == len(t)` for all four files. The
     invariant the offset-slicing rests on.
  j. **`test_the_r_mask_agrees_with_the_pre_existing_r_code_only`** -- ⭐ **the R twin of (g),
     and it is nearly free**: `r_code_only` (`test_qtl_coloc_allele_join.py:1423`) is an
     independent R stripper with string-literal and backslash handling already sitting in the
     file T2 edits. Import it via the sibling-import preamble and require agreement on
     **all four** R-and-Python-agnostic R inputs -- `run_susie_rss.R` plus the three synthetic
     fixtures from (a)/NC-SR1/NC-SR3 -- comparing whitespace-normalised line lists. **Two
     independent R implementations, one answer.** If they cannot be reconciled cheaply, report
     it as a **finding about `r_code_only`** (it is a pre-existing assertion's substrate) and
     **STOP** rather than weakening either side.
  k. **`test_the_mask_invariants_are_load_bearing`** -- hard rule 4, pinned permanently, since
     **no live file exercises either invariant**. Expose `_mask_r(text, *, filler, keep_newlines)`
     for the test only. On the fixture `q <- "line1\nline2 trailing   \nend"`:
     * **(4a)** `keep_newlines=False` yields `['q <- "line1']` -- **two lines silently vanish**;
       `keep_newlines=True` yields `['q <- "line1', 'line2 trailing', 'end"']`. Assert they
       DIFFER and assert the correct one retains `line2 trailing`.
     * **(4b)** with `keep_newlines=True`, filler `"_"` retains `line2 trailing` while filler
       `" "` yields `['q <- "', 'end"']`. Assert they DIFFER.
     Also assert the production default is `filler="_", keep_newlines=True`, and that on the
     real `run_susie_rss.R` both `keep_newlines` settings give **818** lines -- the measured
     proof that this bug is invisible on today's inputs and needs a synthetic pin.
  l. **NC-SR10 `test_the_actual_side_is_the_working_tree_not_the_index`** -- B3, permanent.
     Drive `assert_code_frozen(..., actual_text=<a perturbed code line>)` and require RED. Then
     assert no `HEAD` read on the actual path -- ⚠ **using the plan's OWN utility, at MODULE
     scope**, because STEP 1(c) mandates a docstring that literally quotes
     `git_show("HEAD", rel_path)` and `git show HEAD:`, so a raw `"HEAD" not in source` check is
     **RED AT BIRTH**:

```python
code = strip_to_code(Path(SOURCE_FREEZE).read_text(), LANG_PY)  # drops docstrings, keeps executable Constants
assert '"HEAD"' not in code and "'HEAD'" not in code
```

     Measured by the checker: **GREEN** on the correct module (the docstring warning survives
     stripping), **RED** when the actual side is swapped to `git_show("HEAD", ...)`. Module scope
     -- not function scope -- so it also survives an `_read_actual()` extraction.
  m. **`test_no_production_call_site_supplies_the_control_seam`** -- the B1 seam guard, permanent
     and AST-based. A `grep` is one-shot and substring-fragile (`actual_text = forged` with
     spaces and `**{"actual_text": ...}` both evade it -- verified). Walk every `.py` in
     `tests/m3/` **except this module**, `ast.parse` it, and assert no `ast.Call` to
     `assert_code_frozen` / `assert_unchanged_on_disk` carries an `actual_text` keyword.
  n. **`test_a_non_sha_ref_is_rejected`** -- the easier weakening, closed.
     `assert_code_frozen(path, "HEAD", LANG_R)` would make every gate permanently green with
     nothing turning red, and `"HEAD"` is not 7-hex so the SHA sweep misses it. One line in
     `assert_code_frozen`: reject any `ref` not matching `^[0-9a-f]{7,40}$`. **Leave `git_show`
     unrestricted -- the JOB B capture guard needs `HEAD`.** This test observes the rejection.

**STEP 3 -- `tests/m3/test_source_freeze_pins.py`. THE PINS.** In THIS task it carries **only
the Python pins**; T2 adds the R pins to the same module.

```python
#: The three m3 modules that have genuinely not moved since the 2026-07-16 freeze
#: declaration. A CODE PIN: comment/docstring edits do not move it. On an
#: AUTHORIZED code change, update THIS ONE CONSTANT to the landing commit's SHA
#: and nothing else. See DEC-2026-08-06-sr4-freeze-scope.
PY_CODE_REF = "bf16289"
```

  a. `test_python_module_code_is_frozen` -- parametrized over the 3 modules (whole-file floor).
  b. `test_python_symbol_code_is_frozen` -- parametrized over **(module, symbol)**, all **22**
     (13 + 3 + 6), symbol lists **DERIVED FROM THE SOURCE at `PY_CODE_REF`, never
     hand-transcribed** -- a hand-typed list is a test that agrees with itself
     (`test_qtl_coloc_allele_join.py:54-55`; the pd3 `CAUSE_TOKENS` precedent).
  c. `test_the_pinned_set_is_exactly_the_files_that_have_not_moved` -- assert `git cat-file -t
     PY_CODE_REF == "commit"`, and that each pinned path exists at both `PY_CODE_REF` and HEAD.
  d. **`test_the_handoff_frozen_claim_is_recorded_as_partly_false`** -- assert the five moving
     files are **NOT** in the pinned set, with the measured numstat in the docstring. **This
     stops a future sweep from "helpfully" adding them back without a decision.**

**NEGATIVE CONTROLS -- observe RED, quote RAW.** NC-SR1 through NC-SR4 are **PERMANENT and
in-suite**; they need no revert and cannot decay into a claim. In addition, observe once and
quote:
* **NC-SR5** -- perturb a `plink_ld_to_npz.py` **code** line in memory and confirm
  `assert_code_frozen(..., actual_text=<perturbed>)` **raises**, with the module named and the
  re-pin instruction present in the message. **The `actual_text=` seam (B1) is what makes this
  runnable at all** -- without it the only escapes are writing the file (hard rule 2) or
  monkeypatching `read_text`.
* **NC-SR6** -- perturb a `plink_ld_to_npz.py` **comment** in memory and confirm the same call
  does **not** raise. The paired shape (same file, same helper, same seam, one RED and one
  GREEN) is what makes comment-insensitivity a **property** rather than an assertion.

**COMMIT 1** -- explicit paths only:
`git add tests/m3/source_freeze.py tests/m3/test_source_freeze.py tests/m3/test_source_freeze_pins.py`
Subject: `feat(260806-sr4-T1): comment-insensitive, string-safe code-identity freeze utility + gates for the three genuinely-frozen modules`
Body: names AUTH-SR4-EXTEND; records that 5 of the 8 files HANDOFF calls frozen-at-`bf16289`
have **moved** and were deliberately NOT gated; records that no existing assertion changed.
  </action>

  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_source_freeze.py tests/m3/test_source_freeze_pins.py -q -rs</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_source_freeze.py tests/m3/test_source_freeze_pins.py -q 2>&1 | grep -ci skip; echo "^ MUST be 0 skips reported"</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && for f in src/python/plink_ld_to_npz.py src/python/condition_ld_matrix.py src/python/occlusion_span_filter.py; do printf "%-45s %s\n" "$f" "$(git diff --numstat bf16289 HEAD -- $f | wc -l)"; done; echo "^ every count MUST be 0"</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && git diff --name-only 1b5b8c6 HEAD -- . ':!.planning/quick/260806-sr4-*' | sort; echo "--- files outside tests/m3 (MUST be 0) ---"; git diff --name-only 1b5b8c6 HEAD -- . ':!.planning/quick/260806-sr4-*' | grep -cvE "^tests/m3/" || true</automated>
  </verify>

  <done>
  The utility exists with the documented API; all three new modules pass with **zero skips** and
  **zero R/Snakemake subprocesses**; NC-SR1 through NC-SR4 are permanent in-suite and NC-SR3
  shows **both** halves (naive stripper blind, utility RED) for **both** languages; NC-SR4 proves
  the whole-file floor catches a change all five symbol pins miss; the 22 Python symbol pins are
  DERIVED not transcribed; the five moved files are asserted **out** of the pinned set with their
  numstat recorded; **STEP 2(k) pins both mask invariants and STEP 2(l)/(m)/(n) pin the actual-side
  read, the seam guard and the ref-format rejection -- all permanent, all written**;
  NC-SR5/NC-SR6 observed and quoted raw; the diff touches **only** `tests/m3/` (**the
  orchestrator-owned `.planning/quick/260806-sr4-*` dir is EXCLUDED from every scope gate -- it
  lands the PLAN doc before execution, and a bare `git diff --name-only` would fire a FALSE
  STOP**). **No pre-existing file was edited in this task.** Commit 1 landed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (COMMIT 2): land K-3, rewire both R freeze gates onto the utility, and prove the comment fix cost no re-pin</name>
  <files>src/legacy/region_analysis/scripts/run_susie_rss.R, tests/m3/test_finemap_receipt_early_exit.py, tests/m3/test_qtl_coloc_allele_join.py, tests/m3/test_source_freeze_pins.py, tests/m3/test_source_freeze.py</files>

  <behavior>
  - `run_susie_rss.R` carries `1,909` and `1,900` at `:1018-1019`; **no other byte moves.**
  - `git diff bf04199 HEAD -- run_susie_rss.R` touches **exactly two lines**, both comment lines,
    both differing **only** in those digits.
  - The code pin **stays `bf04199`** and every R gate is GREEN across the edit -- while
    `git diff --exit-code bf04199` (the OLD gate) is **RED**, proving the rescope is what made
    the edit free.
  - `FROZEN_R_REV` -> `FROZEN_R_CODE_REV` and `FREEZE_REF` -> `FREEZE_CODE_REF`; **no consumer of
    the old names survives anywhere.**
  - The two JOB B leak checks are **SHA-free byte** comparisons and are **stronger**, not weaker.
  - No `git diff --exit-code` on `run_susie_rss.R` survives in any test file.
  </behavior>

  <action>
**STEP 0 -- RE-DERIVE.** `git diff --exit-code bf04199 -- run_susie_rss.R` CLEAN; `:1018` holds
`1,944`, `:1019` holds `1,935`; `grep -c "1,944\|1,935" run_susie_rss.R` == 2; the four consumer
sites in `<verified_anchors>`'s JOB A/JOB B table are live at the stated lines. **Report any
mismatch.**

**STEP 1 -- AUTH-SR4-K3. TWO DIGITS. NOTHING ELSE.**
At `:1018` `1,944` -> `1,909`; at `:1019` `1,935` -> `1,900`. **Do not reflow the comment, do not
fix adjacent prose, do not touch whitespace, do not touch any other line.** The `9` count, the
`687`, the `2,596` and the `0` are all already correct.
Then:
`/rs1/researchers/c/ckclinto/conda_envs/m3-r-ld/bin/Rscript -e 'invisible(parse("src/legacy/region_analysis/scripts/run_susie_rss.R")); cat("PARSE_OK\n")'`
and prove containment immediately:
`git diff -U0 bf04199 -- src/legacy/region_analysis/scripts/run_susie_rss.R` -- **exactly one
hunk, exactly two `-`/`+` pairs, every one of the four lines starting (after the marker) with
whitespace then `#`.** Quote it raw. If the hunk contains a non-comment line, **STOP**.

**STEP 2 -- the R pins, added to `tests/m3/test_source_freeze_pins.py`.**

```python
#: run_susie_rss.R's CODE freeze. ⚠ DELIBERATELY NOT MOVED by quick-260806-sr4:
#: the K-3 edit was COMMENT-ONLY, so this pin remains valid ACROSS it -- which is
#: the acceptance demonstration for the rescope. It still means "no CODE has moved
#: since the K-1 closure". On an AUTHORIZED code change, update THIS ONE CONSTANT.
#: See DEC-2026-08-06-sr4-freeze-scope.
R_CODE_REF = "bf04199"

#: The numeric-bearing symbols. Symbol pins are DIAGNOSTICS (they name WHICH block
#: moved); the whole-file floor is the SAFETY NET and covers the ~700-line
#: top-level main body (:659-1357) that lives inside no function at all.
R_NUMERIC_SYMBOLS = ("regularize_ld", "run_susie_with_ladder", "safe_region_id",
                     "load_ld_matrix", "assert_declared_ld_authoritative")
```

  a. `test_run_susie_rss_whole_file_code_is_frozen` -- the floor.
  b. `test_run_susie_rss_symbol_code_is_frozen` -- parametrized over the five.
  c. **`test_the_k3_comment_fix_did_not_move_the_code_pin`** -- ⚠ **THE ACCEPTANCE TEST,
     PERMANENT AND IN-SUITE -- AND IT NEEDS TWO REFS, NOT ONE.**

     ⚠ **THE TRAP THE CHECKER CAUGHT: evaluating all three clauses against `R_CODE_REF` RE-PLANTS
     THE EXACT TIMEBOMB THIS PLAN REMOVES.** Clauses (ii) and (iii) hold today *only* because of
     K-3's two comment lines. The first time an authorized **code** change bumps `R_CODE_REF`
     per this plan's own protocol, `git show <new-ref>` == HEAD, and (ii) and (iii) both go RED
     -- **making the DECISIONS.md sentence "update exactly one constant per file and nothing
     else" FALSE ON ITS FIRST EXERCISE.** Split the refs:

```python
#: The revision the K-3 digits were corrected FROM. A DIFFERENTIAL SUBSTRATE, not
#: a freeze pin: it MUST stay bf04199 forever, even after R_CODE_REF is re-pinned.
#: Same treatment as PRE_K1_REF. Bumping it in a re-pin sweep silently kills the
#: acceptance proof. See DEC-2026-08-06-sr4-freeze-scope.
K3_PRE_FIX_REF = "bf04199"
```

     * **(i) uses `R_CODE_REF`** -- `code_lines(HEAD) == code_lines(R_CODE_REF)`. This is the
       live pin and it survives a future re-pin correctly.
     * **(ii) and (iii) use `K3_PRE_FIX_REF`** -- the raw texts DIFFER, and the raw diff mentions
       `1,909` / `1,900` and not `1,944` / `1,935`. **Frozen historical facts about the K-3
       edit, true forever, immune to every future re-pin.**

     ⚠ **The split is currently UNOBSERVABLE, so pin it.** Both constants hold `bf04199` today,
     so clauses (ii)/(iii) pass under either ref -- meaning B2 would be fixed in prose, not in
     fact. Factor (ii)/(iii) into a helper taking `ref`, then add a 5-line control: evaluate it
     with `ref="HEAD"` (simulating a post-re-pin `R_CODE_REF`, where `git show` == HEAD) and
     observe **RED**; evaluate it with `K3_PRE_FIX_REF` and observe **GREEN**. That is the only
     thing that makes the split real today.

     Today the two constants hold the same value; that is a coincidence of this window and the
     `#:` annotations say so. Register `K3_PRE_FIX_REF` in T2 STEP 5's SHA table as
     **HISTORICAL / DIFFERENTIAL SUBSTRATE**, and name it in the DECISIONS re-pin protocol as an
     explicit **never-re-pin** exception (`[[feedback_fixing_a_split_unpins_what_it_pinned]]`).

**STEP 3 -- rewire JOB A in `tests/m3/test_finemap_receipt_early_exit.py`. Surgical.**
* `:60` `FROZEN_R_REV = "bf04199"` -> ⚠ **NOT a second literal. IMPORT the single source of
  truth**, following the existing sibling-import precedent at
  `test_qtl_coloc_allele_join.py:91`:

```python
from test_source_freeze_pins import R_CODE_REF as FROZEN_R_CODE_REV  # noqa: E402
```

  The R code pin must be declared **exactly once**, in `test_source_freeze_pins.py`. Three
  independent `bf04199` literals with no linking assertion means a re-pinner who obeys
  "update exactly one constant per file" leaves **two gates RED**. Keep a `#:` note at the import
  saying it is a **CODE** pin, that comment-only edits do not move it, that
  `quick-260806-sr4` rescoped it under AUTH-SR4-RESCOPE, and that `R_CODE_REF` is the only place
  to change it.
* `:344-353` `_assert_r_freeze_clean()` -> delegate to `source_freeze.assert_code_frozen(
  "src/legacy/region_analysis/scripts/run_susie_rss.R", FROZEN_R_CODE_REV, LANG_R)`. **Keep the
  function name and both call sites (`:341`, `:357`) byte-unchanged** -- the smallest possible
  diff, and `test_the_frozen_early_exits_still_emit_no_ld_field` keeps re-asserting the freeze
  mid-test exactly as it does today.
* Module docstring `:12-18` -- rewrite the freeze paragraph: the file is **code-frozen** at
  `bf04199`; `quick-260806-sr4` rescoped byte -> code under AUTH-SR4-RESCOPE and spent
  AUTH-SR4-K3 on a comment-only correction that **did not move the pin**. **KEEP** the K-1
  history and the `dc4bbd2` -> `bf04199` lineage.
* Add the standard sibling-import preamble (`sys.path.insert(0, Path(__file__).resolve().parent)`
  then `from source_freeze import ...`), matching `test_qtl_coloc_allele_join.py:78-91`.
* **NOTHING ELSE IN THIS FILE CHANGES.** `:560` and `:568` are correct historical narrative --
  leave them. `PRE_CHANGE_REF` (`6b427bc`) and `PRE_K1_SMK_REF` (`63453db`) are **DIFFERENTIAL
  SUBSTRATES: DO NOT TOUCH** (`[[feedback_fixing_a_split_unpins_what_it_pinned]]`).

**STEP 4 -- rewire `tests/m3/test_qtl_coloc_allele_join.py`. TWO different jobs -- do not
conflate them.**
* `:110` `FREEZE_REF` -> **IMPORT, do not re-declare** (B5):
  `from test_source_freeze_pins import R_CODE_REF as FREEZE_CODE_REF`, same `#:` treatment.
* **JOB A, `:1415-1420`** `test_run_susie_rss_is_zero_diff_vs_the_freeze` -> rename to
  `test_run_susie_rss_code_is_frozen` and delegate to `assert_code_frozen`.
* **JOB B, `:730-735` and `:755-759`** -> replace the `git diff --exit-code` subprocess with
  `source_freeze.assert_unchanged_on_disk(SUSIE_R, real)`. **`real` is already in scope at
  `:702` / `:741`.** Byte-exact, SHA-free, immune to the timebomb, and it still catches a leaked
  **comment** -- which a code-only check would have missed. Say that in a comment at both sites.
* ⚠ **THE CAPTURE GUARD -- MANDATORY, NOT OPTIONAL. Without it this is a coverage REDUCTION
  (hard rule 6), not a strengthening.** For a leak that occurred **BEFORE** `real` was read,
  `real` captures the already-leaked bytes and the check passes where the old byte-diff went RED.
  Insert **immediately after** each `real = SUSIE_R.read_text()`, at **`:702` and `:741`**:

```python
assert real == git_show("HEAD", SUSIE_R_REL), (
    "run_susie_rss.R was already modified in the working tree BEFORE this control "
    "captured it -- the leak guard below would be comparing against leaked bytes"
)
```

  `HEAD` is symbolic, so no timebomb. **Only with both halves in place is the "strictly stronger
  than both" claim true as written.**
* **Otherwise the surrounding NC-2g machinery is untouched** -- the two capture guards and the
  two leak-check replacements are the ONLY changes inside those tests.
* `:57` prose ("stays 0-diff vs `bf04199` throughout") -> "stays **code**-identical to `bf04199`
  throughout, and byte-unchanged on disk mid-control".
* `:1304` live prose naming the pin -> note the rescope. **`:1300-1301`'s `dc4bbd2` is HISTORICAL
  narrative inside AUTH-b77-01's preserved record -- KEEP IT.** `PRE_CHANGE_REF` (`7b1025d`) is a
  **DIFFERENTIAL SUBSTRATE: DO NOT TOUCH.**
* **REGISTER, DO NOT REFACTOR, the three strippers this file now holds** (`strip_py_comments`
  `:125`, the imported `code_only` `:91`, `r_code_only` `:1423`). Add a short `#:` note above
  each naming its deliberate semantics and pointing at `source_freeze.py` as the forward
  default. ⚠ **Do NOT rewire any of them** -- each backs a different pre-existing assertion
  (`strip_py_comments` KEEPS triple-quoted strings because a Snakemake `shell:` body IS one;
  `code_only` DELETES them), rewiring is unauthorized, and T1 STEP 2(j) already consumes
  `r_code_only` as an independent cross-check rather than replacing it. **A fourth
  similarly-named stripper must NOT be added to this file.**

**STEP 5 -- PROVE THE OLD NAMES ARE GONE AND THE OLD MECHANISM IS GONE.**
`grep -rn "FROZEN_R_REV\|FREEZE_REF" src tests config Snakefile scripts` -> **only** the new
`_CODE_` names (a substring match on the new names is expected; a bare old name is a STOP).
`grep -rn "diff.*--exit-code" tests/` -> **zero hits mentioning `run_susie_rss`.**
Then classify **every** 7-hex SHA literal in the three touched test modules into
{**CODE PIN** (may move on an authorized code change) | **DIFFERENTIAL SUBSTRATE** (never moves)
| **HISTORICAL NARRATIVE** (never moves)} and put the table in the SUMMARY. **A SHA with no `#:`
bucket annotation is itself a finding -- annotate it.**

⚠ **UPGRADE THIS ONE-SHOT CLASSIFICATION INTO A PERMANENT REPO-WIDE GATE (B4).** A one-time
table protects nothing next week, and the substrate population is larger than any hand-list:
**eight** `PRE_CHANGE_REF`s (`test_finemap_receipt_early_exit.py:55`,
`test_qtl_coloc_allele_join.py:105`, `test_finemap_summary_panel_visible.py:42`,
`test_ld_allele_aware_join.py:66`, `test_ld_declared_authoritative.py:70`,
`test_ld_npz_to_rds_bounded.py:98`, `test_curated_m2_crosswalk_drift.py:57`,
`test_qtl_coloc_ld_resolution.py:75`), plus `BASE_COMMIT`
(`test_convert_aggregate_target.py:65`) and `BASELINE_REV`
(`test_ld_read_path_ancestry_gate.py:65`, `test_ld_panel_aou_orphan_and_strict.py:61`). Land
`test_every_pin_constant_declares_its_bucket` in `tests/m3/test_source_freeze_pins.py`: walk
every `.py` under `tests/m3/`, find every module-level assignment whose name matches
`_REF$|_REV$|^BASE_COMMIT$|^BASELINE_REV$` bound to a string literal, and require the preceding
`#:` comment block to contain exactly one of `CODE PIN` / `DIFFERENTIAL SUBSTRATE` /
`HISTORICAL NARRATIVE`. **Annotating the pre-existing constants is comment-only** -- no assertion
changes -- so it is inside AUTH-SR4-RESCOPE's re-pin-prose scope. **If any module resists
annotation for a non-obvious reason, STOP and surface rather than guessing its bucket.**

**NEGATIVE CONTROLS -- observe RED, quote RAW.**
* **NC-SR7 -- ⚠ THE HEADLINE. The rescope is what made the edit free.** After the K-3 edit, run
  the OLD gate `git diff --exit-code bf04199 -- src/legacy/region_analysis/scripts/run_susie_rss.R`
  and show it **RED** (rc 1, two comment lines), then the NEW gate
  `pytest tests/m3/test_source_freeze_pins.py -k k3 -q` **GREEN**. **Quote both, side by side.
  This single pair is acceptance items 1 and 2.**
* **NC-SR8 -- the rewired JOB A gates can still fail.** In memory, perturb a code line of
  `run_susie_rss.R`, then call
  `assert_code_frozen(SUSIE_R_REL, FROZEN_R_CODE_REV, LANG_R, actual_text=<perturbed>)` -- **the
  identical call `_assert_r_freeze_clean` makes, with the seam supplied.** Never write the
  working tree, never re-import (hard rule 2). Confirm RED and quote the message. ⚠ **Do NOT add
  an `actual_text` passthrough to `_assert_r_freeze_clean`** -- it would trip STEP 2(m)'s seam
  guard.
* **NC-SR9 -- the rewired JOB B check can still fail.** Assert
  `assert_unchanged_on_disk(SUSIE_R, real + "\n# leaked")` raises. Land it **permanent in-suite**
  in `test_source_freeze.py` (not in the pre-existing module) so no assertion count moves there.

**COMMIT 2** -- explicit paths only:
`git add src/legacy/region_analysis/scripts/run_susie_rss.R tests/m3/test_finemap_receipt_early_exit.py tests/m3/test_qtl_coloc_allele_join.py tests/m3/test_source_freeze_pins.py tests/m3/test_source_freeze.py`
⚠ **`test_source_freeze.py` MUST be in this commit** -- NC-SR9 is written into it by this task,
and hard rule 9 forbids `git add -A`, so an omission ships an uncommitted control with no
rescue (T3's scope gate stays green because T1 already listed the file).
Subject: `fix(260806-sr4-T2): close K-3 and rescope the run_susie_rss.R freeze from bytes to code (AUTH-SR4-RESCOPE, AUTH-SR4-K3)`
Body: both authorizations **SPENT**; the code pin **deliberately NOT moved** and why; the JOB
A / JOB B split.
  </action>

  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/m3-r-ld/bin/Rscript -e 'invisible(parse("src/legacy/region_analysis/scripts/run_susie_rss.R")); cat("PARSE_OK\n")'</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && echo "=== THE HEADLINE PAIR ==="; git diff -U0 bf04199 -- src/legacy/region_analysis/scripts/run_susie_rss.R; echo "old byte gate rc=$? (RED expected)"; /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_source_freeze_pins.py -q -rs</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_finemap_receipt_early_exit.py tests/m3/test_qtl_coloc_allele_join.py tests/m3/test_ld_read_path.py tests/m3/test_ld_allele_aware_join.py tests/m3/test_ld_allele_aware_wiring.py tests/m3/test_ld_declared_authoritative.py tests/m3/test_variant_catalog_fallback_legacy_semantics.py tests/m3/test_stitch_subregions_to_rds.py -q -rs</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && echo "--- B2 capture guards (MUST be 2) ---"; grep -c 'git_show("HEAD", SUSIE_R_REL)' tests/m3/test_qtl_coloc_allele_join.py; echo "--- B3 NC-SR9 rode in commit 2 (MUST be >= 1) ---"; git show --stat HEAD -- tests/m3/test_source_freeze.py | grep -c leaked || true; echo "--- B5 the R code pin is declared ONCE (MUST be 1) ---"; grep -rn '^R_CODE_REF = ' tests/m3/ | wc -l</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && echo "--- bare old names (MUST be 0) ---"; grep -rn "FROZEN_R_REV\b\|FREEZE_REF\b" src tests config Snakefile scripts 2>/dev/null | grep -v "_CODE_" | wc -l; echo "--- git-diff gates on run_susie_rss in tests (MUST be 0) ---"; grep -rn -- "--exit-code" tests/ 2>/dev/null | grep -c run_susie_rss || true; echo "--- K-3 digits ---"; grep -c "1,909\|1,900" src/legacy/region_analysis/scripts/run_susie_rss.R; grep -c "1,944\|1,935" src/legacy/region_analysis/scripts/run_susie_rss.R</automated>
  </verify>

  <done>
  `run_susie_rss.R` parses and carries `1,909`/`1,900`; the diff vs `bf04199` is **exactly two
  comment lines** differing only in those digits, quoted raw; **NC-SR7 shows the old byte gate RED
  and the new code gate GREEN on the same tree** -- acceptance items 1 and 2 discharged in one
  pair; the code pin was **NOT** moved and a permanent test asserts that; both constants renamed
  with **zero** bare old-name references left and **the R code pin declared exactly once**
  (`R_CODE_REF`, imported by both consumers); both JOB A gates delegate to the utility; both JOB B
  checks are SHA-free byte comparisons **AND both capture guards are in place at `:702`/`:741`**
  (grep count == 2), without which this would be a coverage reduction; **`test_source_freeze.py`
  rode in commit 2** carrying NC-SR9; every `*_REF`/`*_REV`/`BASE_COMMIT`/`BASELINE_REV` constant
  in `tests/m3/` declares its bucket, enforced permanently; NC-SR8 and NC-SR9 observed RED; every SHA literal in the touched modules is bucket-annotated; the eight-module
  regression set is green with skips unchanged. Commit 2 landed.
  </done>
</task>

<task type="auto">
  <name>Task 3 (COMMIT 3): record the freeze convention, register the nine pre-existing strippers, close K-3, and run the two-suite gate with the honest accounting</name>
  <files>.planning/DECISIONS.md, .planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md</files>

  <action>
**STEP 1 -- `.planning/DECISIONS.md`: append `DEC-2026-08-06-sr4-freeze-scope`.** Follow the
file's established shape exactly (`## <date> — DEC-...: <headline>` then **Decision** /
**Context** / **Alternatives considered** / **Why** / **How to apply**). It must state, plainly:

* **What is pinned:** the **CODE** of `run_susie_rss.R` at `bf04199` (whole-file floor + five
  named numeric-bearing symbols) and of `plink_ld_to_npz.py` / `condition_ld_matrix.py` /
  `occlusion_span_filter.py` at `bf16289` (whole-file + 22 top-level symbols).
* **What is deliberately FREE:** comments, docstrings, blank lines, trailing whitespace. **Fixing
  a wrong comment now costs nothing.**
* **Why a guard exists at all:** BLOCKER-1 -- this pipeline moved EUR `r[1,2]` 0.1 -> 0.9,
  credible sets 3 -> 10, nonzero PIPs 200 -> 78 while `ld_status` and `ld_overlap_fraction`
  stayed **byte-identical**; and the AFR regression oracle costs the AoU perimeter and an
  ~11-day billed fire. **Silent numeric drift with no cheap oracle is the threat.**
* **What this is NOT:** it does **not** touch `TRACK-A-FROZEN-NUMBERS.md`, the aggregator md5
  `558fca45`, or the three SH2B3 EUR `.fit.rds` md5 pins. **Those are frozen NUMBERS -- Carter's
  recorded decisions -- and they are a different thing that happens to share a word.**
* **The re-pin protocol, in one sentence:** an authorized **code** change updates **exactly one
  constant per FROZEN SUBJECT** -- `R_CODE_REF` for `run_susie_rss.R`, `PY_CODE_REF` for the
  three Python modules -- to the landing commit's SHA, **and nothing else**.
  `FROZEN_R_CODE_REV` and `FREEZE_CODE_REF` are **import aliases** of `R_CODE_REF`, so they
  follow automatically. Comment/docstring changes update nothing.
* ⚠ **The NEVER-RE-PIN rule, stated as a DERIVED GATE rather than a hand-written list.**
  Differential substrates are not pins, and a sweep that bumps one silently destroys a control
  (`[[feedback_fixing_a_split_unpins_what_it_pinned]]`). **A hand-enumeration is the wrong shape
  here: there are EIGHT `PRE_CHANGE_REF`s plus `BASE_COMMIT` and two `BASELINE_REV`s across
  `tests/m3/`, and any omission LICENSES a sweeper to bump the ones left out.** So the rule is:
  **every `*_REF` / `*_REV` / `BASE_COMMIT` / `BASELINE_REV` constant in `tests/m3/` must carry a
  `#:` bucket annotation from {CODE PIN | DIFFERENTIAL SUBSTRATE | HISTORICAL NARRATIVE}**, and
  only CODE PINs ever move. That is enforced permanently by T2 STEP 5, not by this prose. Record
  `K3_PRE_FIX_REF` as a DIFFERENTIAL SUBSTRATE by name (it is what makes the acceptance proof
  survive the first re-pin); today it and `R_CODE_REF` hold the same value, a coincidence of this
  window, and they diverge **by design** the moment a code change moves `R_CODE_REF`.
* **The stripper inventory.** ⚠ **Do NOT write "no such utility existed" -- NINE ad-hoc
  comment-strippers already do** (table in the plan's `<verified_anchors>`), including
  `r_code_only` and `_code_lines`, which is a hand-rolled instance of this very utility. What
  existed **nowhere** is the **FREEZE convention** -- no `DECISIONS.md` entry, and `bf16289`
  enforced by **zero** tests. State that the nine are **registered and superseded going
  forward**, that **none is refactored here** (each backs a different assertion with deliberate
  semantics and rewiring is unauthorized), and that `r_code_only` is deliberately **kept** and
  consumed as an independent R cross-check. Anything else would break
  `[[feedback_extract_reusable_utilities]]` in the sentence that cites it.
* **Alternatives considered:** (a) keep whole-file byte pins -- **rejected**, it makes shipping a
  known falsehood the cheaper option, which is how K-3 happened, and
  `[[feedback_fixed_sha_whole_file_pin_is_a_timebomb]]` names the failure mode (b77 stalled a
  task one day after the pin was written); (b) symbol pins **only**, no whole-file floor --
  **rejected**, ~700 lines of `run_susie_rss.R` including all three `toJSON` emits live inside no
  function, so an enumerate-the-symbols design has a silent hole (**proven by NC-SR4, not
  argued**); (c) a naive `#`-to-end-of-line stripper -- **rejected**, it makes a code change
  concealed after an in-string `#` **invisible** (**proven by NC-SR3**), which would convert the
  guard into the structurally-incapable-assertion class this project has been bitten by eight
  times; (d) gate all 8 files HANDOFF calls frozen -- **rejected**, 5 have demonstrably moved and
  declaring a moving file frozen is a decision, not an inference.
* **How to apply:** the forward gate is `pytest tests/m3/test_source_freeze_pins.py`; the utility
  is `tests/m3/source_freeze.py`; extending it to a new file is a one-line addition to the pin
  table **plus** a recorded decision that the file is frozen.
* **Cross-refs:** `[[feedback_extract_reusable_utilities]]`,
  `[[feedback_fixed_sha_whole_file_pin_is_a_timebomb]]`,
  `[[feedback_green_assertion_needs_a_negative_control]]`,
  `[[feedback_negative_control_defeated_by_bytecode_cache]]`, K-3 in `deferred-items.md`,
  `DEC-2026-08-05-m3-ld-read-path` (BLOCKER-1, the reason a guard exists).

⚠ **STEP 1b -- ANNOTATE THE FOUR SELF-CONTRADICTING SITES IN `deferred-items.md`.** That file
presents `git diff --exit-code bf04199 -- run_susie_rss.R` as *"the forward gate"* at **`:332`**
(and `:476`, `:588`) and names `FROZEN_R_REV` / `FREEZE_REF` at **`:336-337`**, `:480`, `:633`.
After T2 **none of those exists**. Leaving them makes the file self-contradictory **in the very
commit that closes K-3**, and success criterion 1 would be false as written. **Annotate each in
place** -- `(superseded 2026-08-06 by quick-260806-sr4: the forward gate is now
`pytest tests/m3/test_source_freeze_pins.py`; the constants are `FROZEN_R_CODE_REV` /
`FREEZE_CODE_REF`)` -- **do NOT rewrite the historical sentences around them.** Success criterion
1's "anywhere" is then honest: the mechanism is gone from `tests/`, and every prose reference to
it is marked superseded.

**STEP 2 -- `deferred-items.md`: close K-3, mirroring how AUTH-b77-01 was recorded.** Preserve
the original entry in full under `### THE ORIGINAL DEFERRAL, PRESERVED` and prepend a closure
header carrying: the date, the applied commit SHA, AUTH-SR4-K3 as **GRANTED and SPENT**, the
corrected figures, and the one line that matters -- **the remedy the entry called "requires a
named unfreeze" now requires none at all, because the freeze no longer covers comments.**
Append a **new, still-open** entry recording the **five files HANDOFF calls frozen-at-`bf16289`
that have MOVED** (with the numstat table), framed as a question **for Carter**: are these
frozen-and-drifted, or were they never frozen? **Do not answer it.**

**STEP 3 -- THE TWO-SUITE GATE. Run each ONCE, cleanly, AFTER commit 3. Quote every number raw.**
* `tests/m3`: `failed == 0`, **`skipped == 31`**, `passed > 822`. Run with `-rs` and prove **zero**
  skips originate from the three new modules; attribute every remaining skip to a pre-existing
  gate (`hail` / chain file / M2 union BED / SKELETON / AoU perimeter). **Reconcile the delta
  per module** -- expected **circa +50 to +60** (utility self-tests ~25; the 22 Python symbol
  pins; 3 Python whole-file; 1 R whole-file; 5 R symbol; the K-3 acceptance test; the pin-set
  tests). The binding gate is `passed > 822` with skips exactly 31, so an over-count cannot fail
  the run -- **but a delta you cannot account for per module is a finding, not a rounding
  error.** Do not open an investigation merely because the total exceeds the estimate; reconcile
  it.
* `tests/phase2`: exactly `136 passed / 1 skipped / 0 failed`; the skip is `bedtools not
  available`.
* `snakemake --list` rc 0 on `config/pipeline.yaml` and the three `lsweep` overlays.

**STEP 4 -- SCOPE + TRACK-A PROOF.**
* `git diff --name-only 1b5b8c6 HEAD -- src config tests .planning ':!.planning/quick/260806-sr4-*' | sort` equals this plan's
  `files_modified` **exactly**.
* **`run_susie_rss.R`'s diff vs `bf04199` is exactly two comment lines** (re-quote it).
* **0-diff** on `src/python/plink_ld_to_npz.py`, `src/python/condition_ld_matrix.py`,
  `src/python/occlusion_span_filter.py`, `src/scripts/ld_npz_to_rds.R`,
  `src/python/occlusion_manifest.py`, `src/python/occlusion_present_rate_scan.py`,
  `src/python/drop_occluded_from_sumstats.py`, `src/snakemake/rules/finemap.smk`,
  `src/snakemake/scripts/ld_allele_join.R`, `src/snakemake/schemas/pipeline.schema.yaml`.
* **TRACK A UNTOUCHED, asserted not assumed:** `git diff --name-only 1b5b8c6 HEAD` contains **no**
  `results/`, **no** `src/python/`, and **no** `.planning/amendments/` path; and
  `git diff --exit-code 1b5b8c6 HEAD -- .planning/amendments/TRACK-A-FROZEN-NUMBERS.md` is clean.
* ⚠ **`.planning/DECISIONS.md` IS EDITED BY THIS TASK AND IT HOLDS TRACK A's md5 LOCKS** -- the
  aggregator `558fca45` and the three SH2B3 `.fit.rds` md5s live at `:880-885`. Hard rule 7 said
  "byte-unchanged, asserted in T3" but had **no assertion covering this file**. Add both:
  * `git diff 1b5b8c6 HEAD -- .planning/DECISIONS.md | grep -c '^-[^-]'` is **0** -- the edit is
    strictly **APPEND-ONLY**, so no recorded decision can be silently rewritten;
  * `grep -c "558fca45" .planning/DECISIONS.md` is unchanged from its pre-edit value, and the
    three SH2B3 `.fit.rds` md5 literals are all still present.
* `git diff 1b5b8c6 HEAD | grep -inE 'condition_ld_matrix.*=|nan_to_num'` empty (m3-06 HELD --
  a read-only pin is not a behaviour change).
* `git diff 1b5b8c6 HEAD | grep -inE "gsutil|gcloud|\bbq\b|dataproc|hailctl| wb "` empty.
* `git checkout -- tests/m3/sparse_parent_benchmark.tsv`; confirm not staged.

**STEP 5 -- THE HONEST ACCOUNTING, written into the SUMMARY.**
1. **The acceptance table**, one row per clause, each marked MET/NOT-MET with the raw evidence:
   (1) K-3 landed; (2) the comment edit provably free -- **NC-SR7's RED/GREEN pair**; (3) a code
   change to a pinned symbol RED -- **observed**; (4) a change concealed behind a `#` in a string
   RED -- **observed**, with the naive stripper shown blind to the same fixture; (5) both suites
   at baseline-or-better, skips **31** and **1**; (6) `DECISIONS.md` records the convention.
2. **⚠ THE PLAN-FACT CORRECTION, prominently:** `HANDOFF.json:14`'s *"All 7 pinned files 0-line
   diff vs bf16289"* is **FALSE for 5 of 8** files. Quote the numstat table. State that **3** were
   gated and **5** were deliberately **not**, and why. **Do not bury this.**
3. **What is NOT covered, stated as limits, not sold as coverage:** the pins are over **source
   text** only -- no fit was run, no `.rds`, `.npz` or region JSON was produced or compared, and
   **no numeric behaviour was executed**. The guard detects *that* code moved, never *whether* a
   number moved. YAML support was **deliberately not built** (`pipeline.schema.yaml` has moved
   since `bf16289`, so there was nothing to gate) -- say so rather than implying coverage.
4. Every negative control **NC-SR1 -- NC-SR10** quoted **RAW**, with the **permanent in-suite**
   ones (SR1-SR4, SR9, SR10) distinguished from the **observed-once** ones (SR5-SR8).
5. **The freeze paragraph:** `run_susie_rss.R` is **code**-frozen at `bf04199`, **the pin did NOT
   move**, AUTH-SR4-RESCOPE and AUTH-SR4-K3 are both **SPENT**, and the forward gate is
   `pytest tests/m3/test_source_freeze_pins.py`.
6. **Track A:** `TRACK-A-FROZEN-NUMBERS.md`, the aggregator md5 lock and the three `.fit.rds`
   md5 pins are **byte-untouched**, proven by the STEP 4 commands.
7. Cost: **`$0`, NC State only, zero perimeter contact, AoU fire NOT triggered.**
8. **A HANDOFF BLOCK for the orchestrator** -- the executor wrote **none** of these (hard rule
   12): `.planning/STATE.md` and `.planning/HANDOFF.json:14` carry the **false** 7-files-0-diff
   claim and `HANDOFF.json:118`'s `freeze_state` describes a **byte** gate that no longer exists;
   `HANDOFF.json:76` says K-3 is "REGISTERED BUT NOT A LICENCE" and is now **CLOSED**; and the
   **three deferred prose sites** (`finemap.smk:502`, `ld_allele_join.R:33` and `:99`) still
   narrate a byte freeze -- see `<the_narrative_deferral>`.
9. **The open question for Carter**, verbatim from STEP 2: the five moved-but-called-frozen files.
10. `Deviations from the plan` and `Lessons`. **A plan fact found wrong is a deviation to report,
    never a thing to silently work around.**

**COMMIT 3** -- explicit paths only:
`git add .planning/DECISIONS.md .planning/phases/m3-aou-afr-ld-panel-build/deferred-items.md`
Subject: `docs(260806-sr4-T3): record DEC-2026-08-06-sr4-freeze-scope and close K-3`
  </action>

  <verify>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3 -q -rs 2>&1 | tail -45</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/phase2 -q -rs 2>&1 | tail -15</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && for c in config/pipeline.yaml config/pipeline_lsweep_L15_overlay.yaml config/pipeline_lsweep_L20_overlay.yaml config/pipeline_lsweep_L30_overlay.yaml; do /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/snakemake --snakefile Snakefile --configfile $c --list > /dev/null && echo "OK $c" || echo "FAIL $c"; done</automated>
    <automated>cd /gpfs_common/share01/clintonlab/ckclinto/coloc_analysis && echo "--- scope ---"; git diff --name-only 1b5b8c6 HEAD -- src config tests .planning ':!.planning/quick/260806-sr4-*' | sort; echo "--- Track A paths touched (MUST be 0) ---"; git diff --name-only 1b5b8c6 HEAD | grep -cE "^results/|^src/python/|^\.planning/amendments/" || true; echo "--- TRACK-A-FROZEN-NUMBERS.md ---"; git diff --exit-code 1b5b8c6 HEAD -- .planning/amendments/TRACK-A-FROZEN-NUMBERS.md > /dev/null && echo CLEAN || echo DIRTY; echo "--- DECISIONS append-only (MUST be 0) ---"; git diff 1b5b8c6 HEAD -- .planning/DECISIONS.md | grep -c '^-[^-]' || true; grep -c 558fca45 .planning/DECISIONS.md; echo "--- forbidden tokens (MUST be 0) ---"; git diff 1b5b8c6 HEAD | grep -icE "gsutil|gcloud|dataproc|hailctl|nan_to_num" || true; echo "--- 10 must-be-0-diff files ---"; for f in src/python/plink_ld_to_npz.py src/python/condition_ld_matrix.py src/python/occlusion_span_filter.py src/scripts/ld_npz_to_rds.R src/python/occlusion_manifest.py src/python/occlusion_present_rate_scan.py src/python/drop_occluded_from_sumstats.py src/snakemake/rules/finemap.smk src/snakemake/scripts/ld_allele_join.R src/snakemake/schemas/pipeline.schema.yaml; do printf "%-55s %s\n" "$f" "$(git diff 1b5b8c6 HEAD -- $f | wc -l)"; done</automated>
  </verify>

  <done>
  `DECISIONS.md` carries `DEC-2026-08-06-sr4-freeze-scope` with all four rejected alternatives and
  the one-sentence re-pin protocol; K-3 is CLOSED with its original surface preserved and the five
  moved-but-called-frozen files registered as an OPEN question for Carter; `tests/m3` is **0 failed
  / 31 skipped / >822 passed** with the delta reconciled per module and **zero** skips from the new
  modules; `tests/phase2` is **136 / 1 / 0**; `--list` rc 0 on all four configs; the scope diff
  equals `files_modified` exactly; all ten must-be-0-diff files are 0; **Track A is byte-untouched**;
  the forbidden-token scan is empty; `sparse_parent_benchmark.tsv` restored and unstaged; all ten
  honest-accounting items are in the SUMMARY, with the HANDOFF false-claim correction **prominent,
  not buried**. Commit 3 landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| a comment/docstring edit -> the freeze gate | the gate must let it through; if it does not, the K-3 absurdity returns |
| a **code** edit -> the freeze gate | the gate must stop it; a stripper bug here makes a real numeric change **invisible** |
| a string literal -> the comment stripper | `#` and `{` inside strings are the exact place a hand-rolled stripper goes wrong |
| a **fixed** pin SHA -> every future legitimate change | a pin that is red forever after gets deleted, and then there is no guard at all |
| "frozen NUMBERS" (Track A) -> "frozen SOURCE" (this plan) | two different things sharing one word; conflating them either breaks a recorded decision or manufactures one |
| an UNGATED file called "frozen" -> a per-task hand check | ritual that has already reported a claim false for 5 of 8 files |

## STRIDE threat register

| ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T-sr4-01 | **Tampering** | a code change concealed behind a `#` inside a string literal | **mitigate** | R = length-preserving string mask (contents -> `_`, delimiters kept) then brace-walk the MASK; Python = `ast` (a `#` in a string is a `Constant`). **NC-SR3 is permanent, synthetic (no live file has the shape), and shows the naive stripper blind to the same fixture** |
| T-sr4-02 | **Tampering** | a code change in the ~700-line top-level main body inside no named symbol (incl. all three `toJSON` emits) | **mitigate** | the whole-file **code-only floor** is the safety net; **NC-SR4 proves it catches a perturbation all five symbol pins miss**, and first proves the span is inside no symbol |
| T-sr4-03 | Tampering | brace-in-string breaking the R brace-walk (23 live instances in `plink_ld_to_npz.py`) | mitigate | the walk runs over the **masked** text, never raw; Python never brace-walks at all |
| T-sr4-04 | **Spoofing** | a symbol pin that extracts an empty or wrong slice -> green forever | **mitigate** | `test_symbol_extraction_is_non_vacuous`: exactly-one def match, non-empty span, a second expected marker per symbol; failure messages name both hypotheses |
| T-sr4-05 | Spoofing | a hand-transcribed symbol list agreeing with itself | mitigate | the 22 Python symbols are **DERIVED from the source at `PY_CODE_REF`**, never typed (the pd3 `CAUSE_TOKENS` precedent) |
| T-sr4-06 | **Denial of service** | the replacement becoming its own timebomb | **mitigate** | the pin is comment-insensitive, so the commonest edits are free; **ONE constant per file**; the protocol is in the constant's `#:`, the module docstring **and** the DECISIONS entry; **the K-3 edit demonstrably costs no re-pin** |
| T-sr4-07 | Denial of service | the `.pyc` bytecode cache defeating a control | mitigate | hard rule 2: every control is an in-memory string or a `git show` read; **nothing on disk is reverted and nothing is re-imported** |
| T-sr4-08 | **Elevation of privilege** | the K-3 unfreeze widening past two digits | **mitigate** | T2 STEP 1 asserts the `bf04199` diff is **one hunk, two `-`/`+` pairs, all four lines comments**; a non-comment line is a STOP; T3 STEP 4 re-quotes it |
| T-sr4-09 | **Repudiation** | JOB B leak detection silently weakened by a comment-insensitive check, or by a leak that predates the `real` capture | **mitigate** | JOB A / JOB B split is explicit; JOB B becomes **SHA-free byte** equality **PLUS the mandatory capture guard `assert real == git_show("HEAD", SUSIE_R_REL)` at `:702`/`:741`** -- only both halves together are strictly stronger than the fixed-SHA byte diff they replace, and T2 `<verify>` counts them (== 2); NC-SR9 permanent |
| T-sr4-10 | Tampering | a re-pin sweep bumping a DIFFERENTIAL SUBSTRATE and killing a control | mitigate | T2 STEP 5 classifies **every** 7-hex literal into CODE PIN / SUBSTRATE / HISTORICAL; `PRE_CHANGE_REF`, `PRE_K1_REF`, `PRE_K1_SMK_REF` named DO-NOT-TOUCH (`[[feedback_fixing_a_split_unpins_what_it_pinned]]`) |
| T-sr4-11 | **Elevation of privilege** | scope creep into Track A's frozen NUMBERS | **mitigate** | hard rule 7; T3 STEP 4 asserts **no** `results/`, `src/python/` or `.planning/amendments/` path in the diff **and** `TRACK-A-FROZEN-NUMBERS.md` 0-diff |
| T-sr4-12 | Elevation of privilege | gating a file that is actually under active development | mitigate | AUTH-SR4-EXTEND is bounded to the three MEASURED-0-diff files; `test_the_handoff_frozen_claim_is_recorded_as_partly_false` stops a future sweep re-adding the other five without a decision |
| T-sr4-13 | Repudiation | reporting a suite number from an intermediate state | mitigate | **no task leaves the tree expected-RED** (`<the_central_design_decision>`); T3's numbers are measured after commit 3 |
| T-sr4-14 | Elevation of privilege | AoU perimeter / billing | mitigate | `$0`, NC State only; forbidden-token grep over the whole diff must be empty; the fire remains Carter's terminal gate |
| T-sr4-15 | Information disclosure | new test modules + registry docs | **accept** | source paths, line counts and SHAs only; no PII, no credentials; everything stays on the NC State node |
| T-sr4-16 | **Repudiation** | the acceptance test itself becoming a timebomb on the first re-pin | **mitigate** | **B2**: clause (i) reads the live `R_CODE_REF`; clauses (ii)/(iii) read `K3_PRE_FIX_REF`, a never-re-pinned substrate. Without the split, the DECISIONS re-pin protocol is **false on its first exercise** |
| T-sr4-17 | **Denial of service** | an assertion that cannot be falsified without writing the working tree | **mitigate** | **B1**: the keyword-only `actual_text=` seam. Three named controls (NC-SR2 / SR5 / SR8) are otherwise **unrunnable**, and the alternatives all violate hard rule 2 or skip the message path. Production call sites are grep-asserted to never pass it |
| T-sr4-18 | **Tampering** | the ACTUAL side silently becoming `git show HEAD:` | **mitigate** | **B3**: the working-tree read is mandated in STEP 1(c) and the module docstring; **NC-SR10** is permanent and asserts no `HEAD` read on the actual path. A committed read would blind the mid-test leak detection at `:341` / `:357` with nothing turning red |
| T-sr4-19 | Spoofing | the R mask silently misaligning lines on a multi-line string | mitigate | hard rule 4(a) + T1 STEP 2(k): newlines are written through even inside strings, pinned by a synthetic fixture, since the real file cannot exercise it |
</threat_model>

<verification>
**Every number MEASURED and quoted raw, one run each, AFTER commit 3.**

1. `tests/m3`: `failed == 0`, **`skipped == 31`**, `passed > 822`, with a per-module delta
   reconciliation and **zero** skips from the three new modules.
2. `tests/phase2`: exactly `136 passed / 1 skipped / 0 failed`.
3. `snakemake --list` rc 0 on `config/pipeline.yaml` + the three `lsweep` overlays.
4. `Rscript -e 'parse(run_susie_rss.R)'` prints `PARSE_OK`.
5. `git diff -U0 bf04199 -- run_susie_rss.R` = one hunk, two `-`/`+` pairs, all four lines
   comments, differing **only** in `1,944`->`1,909` and `1,935`->`1,900`.
6. **NC-SR7 quoted as a pair:** the OLD byte gate **RED**, the NEW code gate **GREEN**, same tree.
7. `grep` finds **zero** bare `FROZEN_R_REV` / `FREEZE_REF` and **zero** `--exit-code` gates on
   `run_susie_rss.R` anywhere in `tests/`.
8. `git diff --name-only 1b5b8c6 HEAD -- src config tests .planning ':!.planning/quick/260806-sr4-*' | sort` equals
   `files_modified` exactly.
9. **0-diff** on all ten files listed in T3 STEP 4.
10. **No** `results/`, `src/python/` or `.planning/amendments/` path in the diff; and
    `TRACK-A-FROZEN-NUMBERS.md` 0-diff.
11. Forbidden-token scan (`gsutil|gcloud|bq|dataproc|hailctl|nan_to_num`) over the diff empty.
12. `tests/m3/sparse_parent_benchmark.tsv` restored via `git checkout --`, not committed.
13. NC-SR1 -- NC-SR10 each recorded, permanent-in-suite distinguished from observed-once.
14. `test_no_production_call_site_supplies_the_control_seam` is GREEN -- an **AST** walk of every
    `.py` in `tests/m3/` (a `grep` is evaded by `actual_text = forged` and
    `**{"actual_text": ...}`, both verified).
15. `git diff 1b5b8c6 HEAD -- .planning/DECISIONS.md | grep -c '^-[^-]'` -> **0** (append-only),
    and the aggregator md5 `558fca45` + the three SH2B3 `.fit.rds` md5 literals are still present.
16. Every scope-diff gate excludes `':!.planning/quick/260806-sr4-*'` -- the orchestrator commits
    the PLAN doc before execution and a bare diff fires a FALSE STOP.

**DELIBERATE NON-CRITERIA -- stated as limits, not sold as coverage.**
* **No fit is run and no artifact is produced or compared.** The pins are over **source text**.
  They detect *that code moved*, never *whether a number moved*. The only oracle for the latter is
  the AoU perimeter and the ~11-day billed fire -- which is the whole reason a source guard
  exists.
* **YAML support was NOT built.** `pipeline.schema.yaml` has moved since `bf16289` (+119 lines),
  so there is nothing to gate; building the parser anyway would be coverage theatre. Recorded as
  an explicit deferral, per the brief's "say so rather than half-doing it".
* **5 of the 8 files HANDOFF calls frozen are NOT gated** -- they have demonstrably moved.
  Registered as an open question for Carter, **not silently resolved either way**.
* **Three prose sites still narrate a byte freeze** (`finemap.smk:502`,
  `ld_allele_join.R:33`/`:99`) -- deliberately deferred on fire-path risk; handed to the
  orchestrator.
* `snakemake --dry-run` is **NOT** a criterion (`data/processed/ld_reference/` is absent;
  `D-04b-03`). `--list` is the achievable substitute.
</verification>

<success_criteria>
1. **ONE reusable utility** answers the code-identity question for R and Python, and **every**
   freeze gate on `run_susie_rss.R` runs through it. No `git diff --exit-code` on that file
   survives anywhere.
2. **K-3 is CLOSED.** `1,909` / `1,900` ship, the diff is two comment lines, and **the pin did not
   move** -- the comment fix was **free**, demonstrated by NC-SR7's RED/GREEN pair on one tree.
3. **The stripper cannot be fooled, and it was PROVEN, not argued.** A code change in a named
   symbol goes RED naming the symbol; a change concealed behind a `#` inside a string goes RED
   while a naive stripper is shown **blind to the same fixture**; a change outside every symbol
   goes RED via the floor while all five symbol pins stay green.
4. **The floor is not optional and that is mechanical.** NC-SR4 proves the ~700-line top-level
   main body -- including all three `toJSON` emits -- is covered by nothing else.
5. **The ritual became machinery for the three files where that was honest**, and the **false
   HANDOFF claim about the other five is reported prominently, not buried**, with the numstat.
6. **The convention is written down** as `DEC-2026-08-06-sr4-freeze-scope`: what is pinned, what
   is deliberately free, why a guard exists (BLOCKER-1's silent numeric drift), the four rejected
   alternatives, the **one-constant-per-file** re-pin protocol **and its never-re-pin exceptions**
   -- and it **registers the nine pre-existing strippers** rather than claiming none existed.
7. **The replacement is not itself a timebomb, and that is mechanical.** The acceptance test
   splits `R_CODE_REF` from `K3_PRE_FIX_REF` so it survives the first authorized re-pin; every
   assertion is falsifiable through the `actual_text=` seam without writing the working tree; and
   the ACTUAL side is the working tree, so mid-test leak detection still fires.
8. **Track A did not move.** `TRACK-A-FROZEN-NUMBERS.md`, the aggregator md5 lock and the three
   SH2B3 `.fit.rds` md5 pins are byte-untouched, **asserted**, and `DECISIONS.md` is
   **append-only** (zero deleted lines).
9. **Both suites green** at 0 failed / **31** skipped / >822 passed and 136 / **1** / 0, with
   **no new skip**; `$0`, zero perimeter contact, **the AoU fire NOT triggered**.
10. **The scope of the claim is reported honestly.** This plan re-aimed a *source* guard. It did
   **not** verify any AFR number, close E-2, or touch the fire gate -- and a summary that implies
   otherwise is the exact failure class this arc keeps catching.
</success_criteria>

<explicitly_out_of_scope>
* **`.planning/amendments/TRACK-A-FROZEN-NUMBERS.md`, the aggregator md5 `558fca45`, the three
  SH2B3 EUR `.fit.rds` md5 pins** -- Carter's recorded decisions. **Untouched.**
* **The five moved-but-called-frozen files** (`occlusion_manifest.py`,
  `occlusion_present_rate_scan.py`, `drop_occluded_from_sumstats.py`, `ld_npz_to_rds.R`,
  `pipeline.schema.yaml`) -- registered as an open question for Carter, **not gated**.
* **YAML support in the utility** -- explicitly deferred, with the reason.
* **`src/snakemake/rules/finemap.smk` and `src/snakemake/scripts/ld_allele_join.R`** -- see
  `<the_narrative_deferral>`.
* **E-2** (QTL-beta / panel-ALT orientation) and **G-2** -- Carter's scientific calls.
* **K-2** (the `ld_allele_join.R` extraction) -- DECLINED on fire-path risk; stays declined.
* **m3-06** stays HELD. No NaN-to-0, no `condition_ld_matrix.py` behaviour change.
* **The AoU fire.** An agent must never trigger it.
* `.planning/STATE.md`, `.planning/HANDOFF.json`, `.continue-here.md`, `ROADMAP.md` -- **do not
  write them.**
</explicitly_out_of_scope>

<output>
On completion write
`.planning/quick/260806-sr4-replace-whole-file-freeze-pins-with-comm/260806-sr4-SUMMARY.md`
using the GSD summary template, carrying every item in T3 STEP 5. **Do not commit it** -- the
orchestrator owns the docs commit.
</output>
