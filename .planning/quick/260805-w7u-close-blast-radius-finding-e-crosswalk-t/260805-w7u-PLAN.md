---
phase: quick/260805-w7u
plan: 01
type: execute
wave: 1
depends_on: [quick/260805-23d, quick/260805-o7o]
subsystem: m3-ld-read-path
tags: [R, coloc, susieR, ld-panel, alleles, qtl, aou, snakemake, blast-radius]
autonomous: true
requirements: [E, E-coupled-key, E-coupled-class]

files_modified:
  - src/python/ld_read_path.py
  - src/snakemake/rules/qtl_coloc.smk
  - src/python/build_qtl_coloc_manifest.py
  - src/snakemake/schemas/pipeline.schema.yaml
  - config/pipeline.yaml
  - src/snakemake/scripts/ld_allele_join.R          # NEW (shared, source()-able)
  - src/snakemake/scripts/run_qtl_coloc.R
  - tests/m3/test_qtl_coloc_ld_resolution.py        # NEW
  - tests/m3/test_qtl_coloc_allele_join.py          # NEW

user_setup: []

must_haves:
  truths:
    - "For an allow-listed ancestry the LD .rds that reaches coloc::runsusie is the SAME artifact resolve_ld_path hands run_finemap -- one resolver, one crosswalk object, one allow-list, zero second sources of truth"
    - "The qtl_coloc manifest can no longer supply a competing LD path for an allow-listed ancestry: it emits a value that is provably not a path and cannot be silently opened"
    - "A panel row binds to the GWAS-fit variant whose REF/ALT it actually matches, never an arbitrary ALT at the same position"
    - "When the gate is ON and the panel<->fit bridge cannot be built or falls below the production overlap threshold, the job EXITS NON-ZERO with a named reason and the measured per-key-space overlaps -- it does not write a status JSON and exit 0"
    - "The LD handed to coloc::runsusie is a base matrix, and it is produced WITHOUT ever densifying the full n_var x n_var panel"
    - "Every disposition class and the opened panel path reach the per-pair JSON and a per-pair log receipt"
    - "For EUR (and every ancestry off the allow-list) the emitted JSON is BYTE-IDENTICAL to 7b1025d's, proven with an inverted control that is observed non-identical"
    - "The new coloc-side matcher agrees with the SHIPPED run_susie_rss.R matcher on the same fixtures, asserted differentially against the REAL frozen source extracted by body-walk (never hand-copied), with controls that observe the agreement test RED for both a perturbed new matcher and a perturbed extraction source"
  artifacts:
    - path: "src/python/ld_read_path.py"
      provides: "ld_coloc_applies -- the SINGLE gate for both halves of this plan"
      contains: "def ld_coloc_applies"
    - path: "src/snakemake/rules/qtl_coloc.smk"
      provides: "resolver-routed _qtl_coloc_ld_input, --ld-allele-join / --variant-list threading, per-pair receipt"
      contains: "resolve_ld_path"
    - path: "src/python/build_qtl_coloc_manifest.py"
      provides: "--resolver-ancestries; loud non-path sentinel in ld_matrix_path for gated ancestries"
      contains: "RESOLVED_BY_LD_PANEL_RESOLVER"
    - path: "src/snakemake/scripts/ld_allele_join.R"
      provides: "shared allele-aware 4-key join + dispositions, source()-able; the extraction target"
      exports: ["ld_allele_join_indices"]
    - path: "src/snakemake/scripts/run_qtl_coloc.R"
      provides: "panel<->catalog<->fit bridge, hard stop() on unbridgeable/low overlap, bounded sparse->dense, JSON provenance"
      contains: "ld_allele_join.R"
    - path: "tests/m3/test_qtl_coloc_ld_resolution.py"
      provides: "resolution acceptance + EUR containment + no-second-crosswalk-load control"
    - path: "tests/m3/test_qtl_coloc_allele_join.py"
      provides: "join acceptance at production thresholds, differential agreement vs the shipped matcher, byte-identical EUR JSON + inverted control"
      contains: "extract_nested"
  key_links:
    - from: "src/snakemake/rules/qtl_coloc.smk::_qtl_coloc_ld_input"
      to: "src/python/ld_panel.py::resolve_ld_path"
      via: "ld_matrix_region_id(CURATED_TO_M2 from finemap.smk's module scope)"
      pattern: "resolve_ld_path\\("
    - from: "config/pipeline.yaml ld_read_path.coloc"
      to: "src/python/ld_read_path.py::ld_coloc_applies"
      via: "same allow-list, second independent lever"
      pattern: "ld_coloc_applies"
    - from: "qtl_coloc.smk params.ld_allele_join"
      to: "run_qtl_coloc.R --ld-allele-join"
      via: "rendered shell arg"
      pattern: "--ld-allele-join \\{params.ld_allele_join\\}"
    - from: "src/snakemake/scripts/run_qtl_coloc.R"
      to: "src/snakemake/scripts/ld_allele_join.R"
      via: "source() of the shared matcher"
      pattern: "source\\(.*ld_allele_join\\.R"
---

<objective>
Close blast-radius finding **E** — `m3-04c-BLAST-RADIUS.md:141`, gate row
"**Any GWAS×QTL colocalization**" — together with the coloc-side key defect and the
coloc-side matrix-class defect that closing E alone would activate.

**Purpose.** `qtl_coloc.smk` is the one LD consumer that was never crosswalked. After the
fire, an AFR GWAS fit produced on the AoU panel would be colocalized against a *1kG* LD
matrix inside one `coloc.susie`. Fixing only the path substitutes a **different** silent
failure (empty overlap, exit 0, "no colocalization found" reads as a scientific result).
This plan fixes the path, the key, the matrix class, and the silence, behind **one** gate.

**Output.** One resolver-routed coloc LD path; one shared allele-aware matcher; a loud,
non-zero exit where verification is impossible; per-pair panel provenance; and a
byte-identical EUR/Track-A path proven with an inverted control.

**Cost.** `$0`, NC State, autonomous, zero perimeter contact. **The AoU fire is NOT triggered.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/m3-aou-afr-ld-panel-build/m3-04c-BLAST-RADIUS.md
@.planning/quick/260805-o7o-close-blast-radius-findings-h-and-i-alle/260805-o7o-SUMMARY.md
@.planning/quick/260805-23d-m3-04c-blast-radius-remediation-eur-tran/260805-23d-SUMMARY.md
@src/snakemake/rules/qtl_coloc.smk
@src/snakemake/scripts/run_qtl_coloc.R
@src/python/build_qtl_coloc_manifest.py
@src/python/ld_read_path.py
@src/python/ld_panel.py
@src/snakemake/rules/finemap.smk
@src/legacy/region_analysis/scripts/run_susie_rss.R
@src/scripts/ld_npz_to_rds.R

<measured_facts>
<!-- EVERY line number below re-verified against HEAD 7b1025d before this plan was
     written. Two of the brief's mechanism claims are CORRECTED here; both corrections
     are load-bearing (fixing the brief's stated site would be a NO-OP). -->

**HEAD:** `7b1025d`. `grep -cE "CURATED_TO_M2|resolve_ld_path|ld_read_path|curated_to_m2"
src/snakemake/rules/qtl_coloc.smk` = **0** — the brief's premise re-confirmed at HEAD.

**F1. Verified anchors (all at HEAD `7b1025d`):**
| File | Anchor | Line |
|---|---|---|
| `qtl_coloc.smk` | `def _qtl_coloc_ld_input` | 203 (body 203-218) |
| `qtl_coloc.smk` | `rule build_qtl_coloc_manifest` | 241 |
| `qtl_coloc.smk` | `ld_reference=config["paths"]["ld_reference"]` (builder params) | 264 |
| `qtl_coloc.smk` | `rule run_qtl_coloc` | 282 |
| `qtl_coloc.smk` | `--ld-matrix {input.ld_matrix} \` | 316 |
| `run_qtl_coloc.R` | `if (n_snps_overlap < 50)` (GWASxQTL gate) | 183 |
| `run_qtl_coloc.R` | `ld_obj <- readRDS(opt$ld_matrix)` | 204 |
| `run_qtl_coloc.R` | `build_ld_rownames <- function(obj)` | 206 (body 206-219) |
| `run_qtl_coloc.R` | `use_identity <- isTRUE(ld_obj$use_identity) \|\| is.null(ld_obj$R)` | 222 |
| `run_qtl_coloc.R` | `ld_snp_names <- rownames(ld_full) %\|\|% colnames(ld_full) %\|\|% build_ld_rownames(ld_obj)` | 232 |
| `run_qtl_coloc.R` | `if (n_snps_overlap < 50)` (post-LD-intersection gate) | 267 |
| `run_qtl_coloc.R` | `ld_matrix_subset <- ld_full[overlap_snps, overlap_snps, drop = FALSE]` | 272 |
| `run_qtl_coloc.R` | `qtl_df <- qtl_df[match(overlap_snps, qtl_df[[match_key]]), ]` | 276 |
| `run_qtl_coloc.R` | `LD       = ld_matrix_subset` | 290 |
| `build_qtl_coloc_manifest.py` | `ld_matrix_path = os.path.join(` | 297 |
| `build_qtl_coloc_manifest.py` | `"ld_matrix_path": ld_matrix_path,` | 324 |
| `build_qtl_coloc_manifest.py` | `parser.add_argument("--ld-reference", required=True,` | 154 |
| `finemap.smk` | `ld_matrix=lambda wildcards: str(resolve_ld_path(` (REFERENCE, do NOT edit) | 310-323 |
| `finemap.smk` | `CURATED_TO_M2 = load_curated_to_m2(...)` | 166 |
| `finemap.smk` | `params.region_id` — **DO NOT TOUCH** | 350 |
| `run_susie_rss.R` | `match_indices_allele_aware <- function` (SHIPPED matcher, FROZEN) | 220-323 |
| `run_susie_rss.R` | `match_indices <- function` (branch point) | 325 |
| `run_susie_rss.R` | rsID-override bridge (`ld_ids <- as.character(...)`) | 1042-1060 |
| `ld_npz_to_rds.R` | `dimnames = list(snp_ids_grch37, snp_ids_grch37)` | 440 |
| `plink_ld_to_rds.R` | `dimnames(R) <- NULL` (legacy 1kG) | 88 |
| `pipeline.schema.yaml` | `ld_read_path:` / `allele_aware:` / `additionalProperties: false` | 388 / 406 / **431** |
| `config/pipeline.yaml` | `ld_read_path:` block | 309-312 |

**F2. ⚠ CORRECTION #1 — `build_ld_rownames` IS NOT ON THE AoU PATH. Fixing it is a NO-OP.**
The brief states the SNP_ID branch of `build_ld_rownames` (`:206-219`) wins. It does not run
at all for the AoU panel. `ld_npz_to_rds.R:440` writes `R` as a `dsCMatrix` **WITH dimnames**
= the GRCh37 `chr:pos:ref:alt` ids. `run_qtl_coloc.R:232` is
`rownames(ld_full) %||% colnames(ld_full) %||% build_ld_rownames(ld_obj)` and `%||%`
(`:24`) short-circuits on the first non-NULL — so `ld_snp_names` comes from **`rownames(ld_full)`**
and `build_ld_rownames` is never called. The OUTCOME the brief describes is right
(panel-space keys, zero overlap); the SITE is not. The legacy 1kG `.rds` is the opposite
shape (`plink_ld_to_rds.R:88` sets `dimnames(R) <- NULL`, so `build_ld_rownames` *does* fire
there and yields the rsID `SNP_ID` — which is exactly why EUR coloc works today).
**Fix the resolution of `ld_snp_names` at `:232`, not `build_ld_rownames`.**

**F3. CORRECTION #2 — the empty overlap does NOT reach a silent identity; it is a soft-silent
EXIT 0, and there are FOUR such layers stacked.** Traced at HEAD:
1. `:265-271` `intersect(overlap_snps, ld_snp_names)` = 0 → `write_status_json("too_few_snps", "Only 0 SNPs after LD intersection (need >= 50)")` → **`quit(status = 0)`**.
2. **MEASURED, not argued** — even with keys fixed, `ld_full` is a `dsCMatrix`, `:272` sparse-subsets it, and `:290` hands it to `coloc::runsusie`:
   ```
   dense  -> OK cs= 1
   sparse -> ERROR: LD must be of class matrix
   ```
   (`coloc::check_dataset(req="LD")` **passes** on the sparse matrix — the check does not catch it.)
   `runsusie` is wrapped in `tryCatch` (`:306-316`) → `NULL` → `write_status_json("qtl_susie_failed", ...)` → **exit 0**.
3. `use_identity` (`:222-227`) builds `diag(length(overlap_snps))` over the *fit* keys and NEVER intersects the panel — coloc runs on the identity with only a `cat()`. The emitted JSON records **nothing** about which LD was used.
4. Snakemake sees rc 0 for all of the above; `aggregate_qtl_coloc.py` writes the row with `status` set and empty PP columns.
So: **recorded, but exit 0.** In bulk this is indistinguishable from biology. Per the brief this
is the same defect class as returning the wrong thing — and worse for a reviewer. **Make it exit non-zero.**

**F4. The bridge that exists, and the one that does not.**
* GWAS fit names (`colnames(gwas_fit$alpha)`) are **sumstats-space**: `run_susie_rss.R:1222-1227`
  builds `snp_names` from `subset$SNP_ID`, falling back to `sprintf("%s:%s", CHR, POS)`.
  The rsID-override bridge at `:1042-1060` fires **only when the LD panel's `SNP_ID` is a clean
  rsID** (`is_rsid_ld <- grepl("^rs[0-9]+$", ld_ids)`) — the AoU panel's is
  `chr:pos:ref:alt`, so it never fires. Verified by experiment that
  `coloc:::annotate_susie(fit, snp_names, R)` succeeds even when `R`'s dimnames are
  panel-space, and `colnames(fit$alpha)` comes out as `snp_names`.
* Both sumstats key conventions are live for AFR. `tests/m3/test_ld_allele_aware_join.py`
  records it as MEASURED: `asthma.AFR -> rs151190501`, `stroke.AFR -> 1:662622`.
  **A panel-only bridge therefore cannot reach rsID-named fits.**
* The bridge that DOES exist is the region variant catalog
  `{ld_reference}/variants/{region}.tsv` — columns `CHR, POS, REF, ALT, SNP_ID`
  (`build_ld_rds.py:288`), GRCh37, already `run_finemap.input.variants`, already the artifact
  o7o's "allele-keyed catalog join" used, and already resolvable through
  `occlusion_lockstep_cli.lockstep_variants_path` (`:147`) which `finemap.smk` imports.
  **`(CHR, POS, REF, ALT)` on both sides means the SHIPPED matcher's contract IS satisfiable
  on the coloc path** — panel = `variants_dt`, catalog = `subset_dt`.
* The QTL side is **GRCh38** (`variant_id = chr12_110962202_G_A`) against a **GRCh37** panel.
  It is therefore NOT a candidate for the position join. See `<explicitly_deferred>`.

**F5. TWO places decide the coloc LD path.** `_qtl_coloc_ld_input` (`:203-218`) falls back to a
constructed legacy path only when the manifest lacks `ld_matrix_path` — and
`build_qtl_coloc_manifest.py:297,324` always writes it, so the fallback is dead and the
**manifest is authoritative today**. Other readers of that column:
`src/python/sample_null_loci.py:378` (a SECOND `run_qtl_coloc.R` caller, always
`ancestry="EUR"`, builds its own manifest with `ld_matrix_path: ""`),
`plan_ld_builds.py:238`, `fine_mapping_gap_reports.py:91`.

**F6. Namespace.** `Snakefile:113` includes `finemap.smk`, `:138` includes `qtl_coloc.smk`.
Snakemake includes share one global namespace, and `qtl_coloc.smk` already hard-depends on
`finemap.smk` (`_qtl_coloc_gwas_fit_input:197` calls `finemap_output()` **eagerly** as a
`.get()` default). So `CURATED_TO_M2` (`finemap.smk:166`) and `REGION_SAFE_TO_ID`
(`Snakefile:45-60`) are in scope. **Reuse them — do NOT call `load_curated_to_m2` again.**

**F7. Schema.** `additionalProperties: false` is at `pipeline.schema.yaml:431`, **TOP LEVEL
ONLY**. `ld_read_path` (`:388`) declares none of its own, so a NEW SUB-KEY under it needs no
entry to avoid `validate()` death. This was MEASURED in 260805-o7o (Deviation 1) after a prior
plan asserted the opposite. **Do not specify a negative control that depends on the false
version.** The achievable control is o7o's substitute: WITH the entry a non-boolean is
rejected (`ValidationError`, rc=1); WITHOUT it the same bad value is silently accepted (rc=0).

**F8. Baselines, both measured for this plan:**
* `tests/m3` — **641 passed / 31 skipped / 0 failed (672 collected)** (from `260805-o7o-VERIFICATION.md`, independently re-run there).
* `tests/phase2` — **136 passed / 1 skipped / 0 failed (137 collected)**, measured at HEAD `7b1025d` during planning. The one skip is `test_negative_controls.py:138` (`bedtools not available`). `tests/phase2/test_run_qtl_coloc.py` (24 tests) pins `run_qtl_coloc.R`'s CLI surface and WILL see this change.
</measured_facts>

<precondition_stated_honestly>
**THE PANEL DOES NOT EXIST. THIS IS WIRING THAT GOES LIVE AFTER THE FIRE.**
* `0/276` `.npz` banked; `find data/processed -name AFR_aou` → **nothing**.
* `data/processed/ld_reference/` **does not exist on this node at all**.
* `results/qtl_coloc/qtl_coloc_manifest.tsv` does not exist; `results/qtl_coloc/*.json` → **0 files**; `results/fine_mapping/**/*.json` → **0 files**.
* `resolve_ld_path` returns the first **EXISTING** path (`ld_panel.py:87`) and otherwise raises.

**Consequences that BIND this plan:**
1. Every acceptance test proves RESOLUTION and the JOIN against **fixtures**. Nothing may
   assert against a real panel on disk.
2. **No `--dry-run` acceptance criterion.** A prior plan in this arc shipped an unsatisfiable
   one for exactly this reason. `snakemake --list` (rc 0) is the only workflow-level gate —
   it does not evaluate input functions, so it cannot observe the resolver either way.
   That is stated as a limit, not sold as coverage.
3. With the gate ON and no panel present, `_qtl_coloc_ld_input` will **raise
   `FileNotFoundError`** at DAG-build time — the same property `finemap.smk` already has.
   That is LOUD and correct, and it is asserted as a required behaviour (T1.4), not worked around.
</precondition_stated_honestly>

<the_reuse_decision>
**DECIDED: do NOT edit the frozen file; land the matcher in a NEW shared source
`src/snakemake/scripts/ld_allele_join.R`, `source()`d by `run_qtl_coloc.R` only, and hold it
to the shipped implementation with a DIFFERENTIAL AGREEMENT TEST that drives both on the same
fixtures. Extraction of `run_susie_rss.R` onto the same file is the correct END STATE and is
recorded as a named follow-up, not attempted here.**

**Why extraction is right and still not done now.** F4 establishes that the shipped
`match_indices_allele_aware` contract (`subset_dt`/`variants_dt`, both with `CHR/POS/REF/ALT`)
**is** satisfiable on the coloc path once the catalog is the `subset_dt` side. So this is
genuinely the same join, and `[[feedback_extract_reusable_utilities]]` points squarely at one
implementation. The obstacle is not aesthetic: the matcher lives INSIDE
`src/legacy/region_analysis/scripts/run_susie_rss.R`, **RE-FROZEN at `dc4bbd2`**, and the
unfreeze granted 2026-08-05 is **SPENT**. A pure-move extraction is still a frozen-file edit
and would need its own `identical()`-on-the-whole-object 0-behaviour proof across every
`allele_aware` state — that is a reviewed task of its own, not a rider on this one.

**Why the duplication is acceptable HERE and would not be otherwise.** A second independent
allele-key implementation is precisely how finding H came to exist in two places. The
difference is that agreement is **machine-checked on every suite run**, not documented:
`tests/m3/test_qtl_coloc_allele_join.py` drives the SHIPPED matcher — obtained from the real
frozen source by the body-walk extraction specified in **Task 2 STEP 0**, never hand-copied —
and the NEW matcher on the SAME fixtures, and asserts identical `keep`, `ld`, `orient` and all
six counters. That test carries two negative controls: NC-2f (perturb the new implementation →
agreement RED) and **NC-2g** (prove the extractor returns the SHIPPED function, not a stale or
hand-written stand-in). The new file is deliberately shaped as a drop-in `source()` target so
the follow-up extraction is a delete-and-source, not a rewrite.

⚠ **THE AGREEMENT MECHANISM IS THE WHOLE JUSTIFICATION. If STEP 0 turns out to be infeasible,
that is a STOP-and-surface, not an improvisation.** Landing `ld_allele_join.R` *without*
machine-checked agreement re-creates the two-independent-implementations condition that let
finding H live in two consumers, and is a scope change only Carter may authorize. The
specific catastrophic improvisation to refuse: hand-transcribing the matcher's logic into the
test file, which yields a test that agrees **with itself** — a vacuous assertion wearing a
green check, the class this arc has now caught FIVE times.

**FOLLOW-UP, to be recorded in the SUMMARY, NOT executed here:**
> `run_susie_rss.R:220-323` should be replaced by `source("src/snakemake/scripts/ld_allele_join.R")`
> the next time the freeze is opened for an independent reason. Requires: a named unfreeze
> authorization; an `identical()`-on-the-whole-`load_ld_matrix`-result proof at
> `allele_aware` TRUE **and** FALSE against `dc4bbd2`; and a RE-FREEZE re-pin at the new SHA,
> after which `git diff --exit-code <new-sha> -- run_susie_rss.R` becomes the forward gate.
</the_reuse_decision>

<the_gate_is_a_single_lever>
**ONE gate governs BOTH halves: `ld_coloc_applies(ancestry, config)` =
`ld_read_path_applies(ancestry, config)` **AND** `ld_read_path.coloc is True`.**

This is deliberate and load-bearing. Two independent levers would permit the state
*resolution ON / join OFF* — which is exactly the "silently wrong LD traded for silently no
LD" outcome the brief forbids. With `coloc` absent or false, the entire coloc path is
byte-identical to `7b1025d` for **every** ancestry including AFR (E stays open, nothing
breaks). With it true, both halves land together. `ld_read_path.enabled: false`,
`ancestries: []`, or `coloc: false` are each a one-line kill switch. Fail-safe on every
uncertain answer is **CHANGE NOTHING** (`[[feedback_failsafe_default_is_caller_relative]]`).

⚠ `src/python/sample_null_loci.py:378` is a SECOND caller of `run_qtl_coloc.R` and will
**never** pass the new flags. Its rows are `ancestry="EUR"` with `ld_matrix_path: ""`. The R
script's new options MUST therefore default to the legacy behaviour with the flags absent —
that caller is the live proof that the default direction is the right one, and T2 must assert it.
</the_gate_is_a_single_lever>

<explicitly_deferred>
Named here so nobody discovers them by diffing, and NOT fixed in this plan:

* **E-2 — the QTL-beta ↔ panel-ALT orientation.** `qtl_data$LD` is signed on the panel's ALT
  (plink `--keep-allele-order`); `qtl_data$beta` is signed on the QTL's effect allele. A
  transposed variant mis-signs the QTL SuSiE fit — finding H's family, on the QTL side.
  It is **pre-existing on the legacy 1kG/EUR path** (unaddressed there today), is **not named
  by finding E**, and correcting it would move **Track-A EUR** numbers, which hard rule 3
  forbids. It also needs a GRCh38↔GRCh37 allele reconciliation (F4) that is its own task.
  **This plan makes its magnitude MEASURABLE** by emitting the panel↔catalog orientation
  counters, and closes the *row-binding* half (a multiallelic site binding to an arbitrary
  ALT's LD ROW is a wrong-row error independent of sign).
* **The coloc-side analogue of finding I in the SUMMARY table.**
  `aggregate_qtl_coloc.py::METADATA_COLS` is a closed list; the new provenance fields land in
  the per-pair JSON and a per-pair log receipt (`finemap.smk:441`'s pattern) but NOT in
  `qtl_coloc_summary.tsv`. `aggregate_qtl_coloc.py` is **out of scope and must not be edited**.
* Findings **G, J, K, L, M** and BLOCKER-D's MC4R/FTO/HLA classes: OPEN, untouched.
  **H and I: closed and verified 7/7 — do NOT re-open.** m3-06 stays HELD (no NaN→0, no
  `condition_ld_matrix.py`).
</explicitly_deferred>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One resolver decides the coloc LD path; the manifest is made to fail loudly</name>
  <files>
src/python/ld_read_path.py
src/snakemake/rules/qtl_coloc.smk
src/python/build_qtl_coloc_manifest.py
src/snakemake/schemas/pipeline.schema.yaml
config/pipeline.yaml
tests/m3/test_qtl_coloc_ld_resolution.py   (NEW)
  </files>

  <behavior>
Write these as failing tests FIRST in `tests/m3/test_qtl_coloc_ld_resolution.py`.

- `ld_coloc_applies("AFR", cfg)` is True only when `ld_read_path.enabled` is not false AND
  `"AFR"` is in `ancestries` AND `coloc is True`. Every degraded shape — block absent,
  block not a dict, `enabled: false`, `ancestries: []`, `coloc` sub-key ABSENT,
  `coloc: false`, `coloc: "true"` (string, not bool) — is **False**.
- Against the REAL shipped `config/pipeline.yaml`: AFR → True; **EUR, TRANS, EAS, HIS → False**.
- With the gate OFF, `_qtl_coloc_ld_input` returns a string **character-for-character equal**
  to `7b1025d`'s expression for the same manifest row (both with and without an
  `ld_matrix_path` column present).
- With the gate ON and a fixture panel on disk at the AoU chain-head path,
  `_qtl_coloc_ld_input` returns **that** path, and it is `==` the string
  `finemap.smk`'s `ld_matrix` lambda produces for the same (region, ancestry).
- With the gate ON and **no** panel anywhere, `_qtl_coloc_ld_input` raises
  `FileNotFoundError` whose message names the region — it does NOT return a legacy path.
- With the gate ON, a manifest row whose `ld_matrix_path` points at a **different, existing**
  file is IGNORED: the resolver's answer wins.
- `build_manifest(..., resolver_ancestries={"AFR"})` writes
  `ld_matrix_path == "RESOLVED_BY_LD_PANEL_RESOLVER"` for AFR rows and the unchanged legacy
  path for EUR rows. With `resolver_ancestries=None` (the default) **every** row is
  byte-identical to `7b1025d`'s output.
- `qtl_coloc.smk` contains **no** call to `load_curated_to_m2` (a second crosswalk load is a
  second source of truth) and **no** second `ld_read_path` block read.
  </behavior>

  <action>
**STEP 1 — `src/python/ld_read_path.py`.** Add `ld_coloc_applies(ancestry, config) -> bool`
and export it in `__all__`. Body:
```python
def ld_coloc_applies(ancestry, config) -> bool:
    if not ld_read_path_applies(ancestry, config):
        return False
    return _ld_read_path_block(config).get("coloc") is True
```
`is True` (not truthiness) mirrors `ld_allele_aware` (`:152`) exactly. Docstring must state:
(a) it is the **single** gate for BOTH the resolver route and the allele join, and why two
levers would permit "resolution ON / join OFF"; (b) the fail-safe direction is CHANGE NOTHING;
(c) unlike `ld_allele_aware` this returns a **bool**, because both consumers are Python —
the R script gets a separately-rendered `"true"`/`"false"` string via `params` (below).
Also add `ld_coloc_join(ancestry, config) -> str` returning `"true"`/`"false"` off
`ld_coloc_applies`, for shell interpolation — same string rationale as
`ld_file_authoritative` (`:114-122`).

**STEP 2 — `config/pipeline.yaml`.** Add `coloc: true` inside the existing `ld_read_path`
block (`:309-312`), with a comment naming finding E, the single-lever rationale, and the
kill-switch semantics. **Do not create a new top-level key.**

**STEP 3 — `src/snakemake/schemas/pipeline.schema.yaml`.** Add `coloc: {type: boolean,
description: ...}` under `ld_read_path.properties` (after `allele_aware`, `:406-408`).
⚠ The comment MUST record F7 correctly: this entry **types** the key; it is **not** required
to avoid `validate()` death, because `additionalProperties: false` is at `:431` (top level)
only and `ld_read_path` declares none of its own. Do not repeat the o7o-era error in either
direction.

**STEP 4 — `src/snakemake/rules/qtl_coloc.smk`.** Mirror `finemap.smk:42-79` for the sys.path
+ imports (`from ld_panel import resolve_ld_path`, `from ld_read_path import
ld_coloc_applies, ld_coloc_join, ld_matrix_region_id`). Do **not** import
`load_curated_to_m2`. Rewrite `_qtl_coloc_ld_input` (`:203-218`):
```python
def _qtl_coloc_ld_input(wildcards):
    row = _qtl_coloc_manifest_row(wildcards.qtl_coloc_id)
    if row is None:
        return os.path.join(QTL_COLOC_DIR,
                            f"_MISSING_MANIFEST_{wildcards.qtl_coloc_id}.ld.rds")
    ancestry = row["ancestry"]
    if ld_coloc_applies(ancestry, config):
        return str(resolve_ld_path(
            region_id=ld_matrix_region_id(
                row["region"], ancestry, config, CURATED_TO_M2, REGION_SAFE_TO_ID),
            ancestry=ancestry, config=config, region_safe=row["region"]))
    return row.get(
        "ld_matrix_path",
        os.path.join(config["paths"]["ld_reference"], ancestry, f"{row['region']}.rds"))
```
The OFF branch is `7b1025d`'s expression **character-for-character** — that is the Track-A
containment and it must not be "tidied". Add a comment block, in the register of
`finemap.smk:103-158`, recording: the disclosed analysis change (the first AFR region with an
`AFR_aou/<m2_id>.rds` on disk switches panel and its coloc numerics WILL move); that
`CURATED_TO_M2` / `REGION_SAFE_TO_ID` are **reused from `finemap.smk`'s module scope**
(F6) so there is exactly one crosswalk object; and that the raise-on-missing-panel is
intended (F3 — loud beats the fourth exit-0 layer).

**STEP 5 — `src/python/build_qtl_coloc_manifest.py`.** Add
`--resolver-ancestries` (comma-separated, default `""` = none) and thread it into
`build_manifest(...)` as `resolver_ancestries: set[str] | None`. At `:297-300`:
```python
if resolver_ancestries and ancestry in resolver_ancestries:
    ld_matrix_path = LD_PATH_RESOLVER_SENTINEL   # "RESOLVED_BY_LD_PANEL_RESOLVER"
else:
    ld_matrix_path = os.path.join(ld_reference, ancestry, f"{region_id}.rds")
```
Module-level `LD_PATH_RESOLVER_SENTINEL = "RESOLVED_BY_LD_PANEL_RESOLVER"` with a comment:
this column is **not** the source of truth for gated ancestries; the sentinel is deliberately
**not path-shaped** so any other reader (`plan_ld_builds.py:238`,
`fine_mapping_gap_reports.py:91`, `sample_null_loci.py:378`) fails visibly rather than opening
a plausible-but-stale path — the "a fallback that is never CONSTRUCTED cannot be silently
taken" discipline from `260805-23d`. Do NOT change `fieldnames` (`:354-359`) — no column added,
no column reordered.

**STEP 6 — wire the builder arg.** In `rule build_qtl_coloc_manifest` (`:241`) add
`params.resolver_ancestries=",".join(a for a in
config.get("ld_read_path", {}).get("ancestries", []) if ld_coloc_applies(a, config))`
and pass `--resolver-ancestries {params.resolver_ancestries}` in the shell (`:268-279`).
Both places therefore read the **same** allow-list from the **same** config.
  </action>

  <verify>
    <automated>/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_qtl_coloc_ld_resolution.py -q -rs</automated>
    <automated>/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/snakemake --snakefile Snakefile --list >/dev/null && echo LIST_OK</automated>
    <automated>grep -c "load_curated_to_m2" src/snakemake/rules/qtl_coloc.smk   # MUST be 0</automated>
    <automated>git diff 7b1025d -- src/snakemake/rules/finemap.smk | wc -l   # MUST be 0</automated>
    <negative_control id="NC-1a" required="true">
Revert STEP 4's ON branch to `7b1025d`'s body and re-run the module. The gate-ON tests MUST
go RED (`FileNotFoundError` not raised; resolver path not returned; disagreement with
`finemap.smk`'s lambda). Record the raw `E AssertionError` lines in the SUMMARY. Restore.
⚠ The revert edit MUST change the file's **byte length** OR the test must read the source at
call time — `SourceFileLoader` validates `.pyc` on `(mtime_seconds, size)`, so a
byte-length-identical restore inside the same wall-clock second runs STALE bytecode
(measured in 260805-o7o). The `.smk` is not imported as a module here, but
`build_qtl_coloc_manifest.py` and `ld_read_path.py` ARE — apply the rule to all three.
    </negative_control>
    <negative_control id="NC-1b" required="true">
Put `EUR` on `ld_read_path.ancestries` in a synthetic config; the test asserting
`ld_coloc_applies("EUR", shipped_cfg) is False` must go RED against the synthetic and stay
GREEN against the shipped file. Then stub `ld_coloc_applies` to `return True` and confirm the
OFF-branch character-for-character test goes RED.
    </negative_control>
    <negative_control id="NC-1c" required="true">
Schema, per F7 — the ACHIEVABLE control, not the false one. WITH the `coloc` entry, set
`coloc: "not-a-boolean"` and run `--list`: expect `ValidationError ... is not of type
'boolean'`, rc=1. WITHOUT the entry, the same bad value: expect **rc=0, silently accepted**.
Quote both. Do NOT assert that removing the entry breaks `--list`.
    </negative_control>
    <negative_control id="NC-1d" required="true">
Drop the `resolver_ancestries` branch in STEP 5 (always legacy path). The sentinel test MUST
go RED. Conversely, set `resolver_ancestries=None` and assert the emitted TSV is byte-identical
to `7b1025d`'s builder output on the same inputs (this is the "default changes nothing" proof).
    </negative_control>
  </verify>

  <done>
`ld_coloc_applies` / `ld_coloc_join` exist and are exported; `config/pipeline.yaml` carries
`coloc: true`; the schema types it; `_qtl_coloc_ld_input` routes through
`ld_matrix_region_id` + `resolve_ld_path` on the allow-list and is byte-identical off it;
the manifest emits a non-path sentinel for gated ancestries and is byte-identical by default;
`qtl_coloc.smk` loads no second crosswalk; `--list` rc 0; `finemap.smk` 0-diff;
all four negative controls OBSERVED RED and quoted.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The coloc join is keyed identically, fails loudly, and hands coloc a real matrix</name>
  <files>
src/snakemake/scripts/ld_allele_join.R   (NEW)
src/snakemake/scripts/run_qtl_coloc.R
src/snakemake/rules/qtl_coloc.smk
tests/m3/test_qtl_coloc_allele_join.py   (NEW)
  </files>

  <behavior>
Write these as failing tests FIRST in `tests/m3/test_qtl_coloc_allele_join.py`. Thresholds are
**READ** from `config/susie_policy.yaml` (50 / 0.5 / 10), never hardcoded permissive values —
`test_ld_read_path.py`'s 8 gate-disabled tests are the cautionary precedent. Follow the
NO-SKIP rule: `_require_m3_r_toolchain()` ERRORS rather than skipping when the `m3-r-ld`
marker env is present.

**Row binding**
- A multiallelic site (panel rows `A:C` and `A:G` at the same CHR:POS) binds the catalog's
  `REF=A ALT=C` row to the panel's `A:C` row — **not** the first hit.
- A transposed pair (catalog `A/C`, panel `C/A`) binds, with `orient = -1`.
- Palindromic (`A/T`, `T/A`, `C/G`, `G/C`) → DROP, counted `dropped_palindromic`.
- Position present, no compatible pair → DROP, `dropped_mismatch`.
- A 4-key duplicated in the panel → DROP, `dropped_ambiguous`.
- `REF`/`ALT` missing / `""` / `"N"` / NA → DROP, `dropped_unusable`.
- A position absent from the panel is ordinary non-overlap and is **NOT** counted.

**Differential agreement (the duplication's price)**
- On >= 5 shared fixtures spanning every class above, `ld_allele_join_indices` and the
  SHIPPED `match_indices_allele_aware` — obtained via the STEP 0 extractor, which returns a
  standalone 2-arg callable exposing all four components (`keep`, `ld`, `orient`, `counts`) —
  return **identical** `keep`, `ld`, `orient`, and all six counters.
- The STEP 0 extractor is a NAMED PRIVATE HELPER in the test module, exercised by its own
  test: it yields `class == "function"`, `length(formals(...)) == 2`, and on the canonical
  multiallelic fixture (panel `12:100 A/G`, `12:100 A/C`, `12:200 C/T`; subset `12:100 A/C`,
  `12:200 T/C`) returns `keep = 1 2`, `ld = 2 3`, `orient = 1 -1`, `exact = 1`, `flipped = 1`,
  all four `dropped_* = 0` — i.e. it binds to the SECOND panel row, not the first hit.

**Bridge + loud failure**
- Fit names in `chr:pos` form bridge via the catalog and produce >= 50 overlap on the fixture.
- Fit names in **rsID** form bridge via the catalog's `SNP_ID` column and produce >= 50 overlap.
- Gate ON + a catalog that cannot bridge (overlap < 50) → the script **exits NON-ZERO**, writes
  **no** output JSON, and stderr names: the reason, the region, the ancestry, the realized
  overlap, the threshold, and the measured overlap of EACH candidate panel key space.
- Gate ON + `--variant-list` absent or unreadable → same non-zero structured rejection,
  distinct named reason.
- Gate ON + `use_identity=TRUE` panel → **non-zero** structured rejection. Silently fitting
  `coloc.susie` on `diag(n)` under an armed gate is the `use_identity` bypass class
  (`BLAST-RADIUS:38`) and is not permitted.
- Gate **OFF**, identical inputs → the legacy `write_status_json("too_few_snps", ...)` +
  **rc 0**, byte-identical to `7b1025d`.

**Matrix class (measured defect)**
- `qtl_data$LD` handed to `coloc::runsusie` satisfies `is.matrix(...) == TRUE` even when
  `ld_obj$R` is a `dsCMatrix`, and the end-to-end fixture run reaches `status="success"`.
- **Memory bound:** the coercion is asserted to happen on the SUBSET only. The test asserts
  the source contains no `as.matrix(` applied to the full panel object, and drives a fixture
  whose full panel is much larger than the subset.

**Track-A / EUR containment**
- `run_qtl_coloc.R` at HEAD vs `git show 7b1025d:` source, same EUR fixture, flags absent:
  the emitted JSON is **BYTE-IDENTICAL**.
- Same, with the flags explicitly rendered `false`: **BYTE-IDENTICAL**.
- **INVERTED CONTROL:** the same comparison on an AFR fixture with the gate ON must be
  **NOT identical**. `status` and `n_snps_overlap` alone are DISQUALIFIED as evidence —
  compare the whole file.
- `sample_null_loci.py`'s exact argv (`:369-384`, no new flags) still yields the legacy path.
  </behavior>

  <action>
**STEP 0 — the differential-agreement extractor. DO THIS FIRST; it gates STEP 1.**

⚠ **`_loader_functions_only` ALONE IS INSUFFICIENT HERE, and the reason must be recorded in
the helper's docstring so a future reader does not repeat this.** Its documented contract
(`tests/m3/test_stitch_subregions_to_rds.py:225-238`) is "write a temp R file with ONLY the
function-definition prefix of `run_susie_rss.R` (up to the top-level `option_list <- list(`
marker) so the loader-contract test can source `load_ld_matrix()` WITHOUT triggering the
script's top-level argument parsing." That contract does **not** extend to exposing nested
closures. `match_indices_allele_aware` is defined at `run_susie_rss.R:220`, **inside**
`load_ld_matrix` (`:142`-`:601`; `option_list <- list(` is at `:659`), so it exists only
during a live call. MEASURED against the sourced prefix:

```
exists load_ld_matrix: TRUE
exists match_indices_allele_aware (top level): FALSE
```

Routing through a real `load_ld_matrix()` call does **not** rescue it either: the returned
object carries `subset_idx` (=`keep`), `allele_orient` (=`orient`) and `allele_counts`
(=`counts`), but **never `ld_idx`** — it is consumed internally at `:489-493` to reorder
`variants`/`R` and then discarded. The behavior spec above requires comparing that component.

**THE MECHANISM (verified working during planning; use it, do not improvise).** Source the
`_loader_functions_only` prefix into a fresh environment, then walk `body(load_ld_matrix)` for
the `<-` assignment expression whose LHS is the target name and `eval()` its RHS in a child of
that environment. This yields a genuinely standalone callable copy of the REAL shipped
function, **without invoking `load_ld_matrix()` and without editing `run_susie_rss.R`**.
Extract the three helpers `.up`, `.usable`, `.allele_counts0` the same way into the child
environment FIRST — the matcher closes over them. Shape:

```r
extract_nested <- function(fn, name) {
  found <- NULL
  walk <- function(e) {
    if (!is.null(found) || !is.call(e)) return(invisible(NULL))
    if (length(e) == 3L && as.character(e[[1]]) %in% c("<-", "=") &&
        is.name(e[[2]]) && identical(as.character(e[[2]]), name)) {
      found <<- e[[3]]; return(invisible(NULL))
    }
    for (i in seq_along(e)) if (!is.null(e[[i]])) try(walk(e[[i]]), silent = TRUE)
    invisible(NULL)
  }
  walk(body(fn))
  if (is.null(found)) stop("STOP-and-surface: could not extract ", name)
  found
}
```

Land it as a **named private helper in `tests/m3/test_qtl_coloc_allele_join.py`** (one
reviewable place), not inline in a test. `run_susie_rss.R` stays 0-diff — this reads the
frozen source, it does not modify it.

**If extraction proves genuinely infeasible: STOP and surface.** Do NOT hand-transcribe the
matcher into the test, and do NOT proceed to STEP 1 without the agreement test. See
`<the_reuse_decision>`.

**STEP 1 — `src/snakemake/scripts/ld_allele_join.R` (NEW).** A standalone, `source()`-able
file defining `ld_allele_join_indices(subset_dt, variants_dt)` plus the three helpers
(`.up`, `.usable`, `.allele_counts0`). Port the SHIPPED body from
`run_susie_rss.R:220-323` **semantics-for-semantics**: two `match()` calls on 4-keys (exact
and REF/ALT-transposed); duplicated panel 4-keys nulled out of the match TABLE before
matching; `NO_PANEL_KEY<i>` sentinels so `match(NA, table)` cannot hit; `paste()`-renders-NA
nulling on both position keys; the `order(keep_idx)` lockstep applied to
`keep_idx`, `ld_idx` AND `orient` **together**; the two structured rejections
(`alleles_unavailable_panel`, `alleles_unavailable_sumstats`); the same six counters and the
same `dropped_unusable`-is-the-subset-side labelling note.
Header comment MUST state: (a) this is a DELIBERATE second implementation, (b) the frozen-file
reason it is not an extraction, (c) that agreement is enforced by
`tests/m3/test_qtl_coloc_allele_join.py`'s differential test and NOT by discipline, (d) the
named follow-up from `<the_reuse_decision>`. **`run_susie_rss.R` MUST NOT be modified.**

**STEP 2 — `src/snakemake/scripts/run_qtl_coloc.R`.** Add three options to `option_list`
(`:29-54`), all defaulting to the legacy behaviour so `sample_null_loci.py:369-384` is inert:
`--ld-allele-join` (character, default `"false"`; `stop()` on any value other than
`"true"`/`"false"` — never silently default, mirroring `--ld-authoritative`);
`--variant-list` (character, default `NULL`);
`--ld-panel-key` is NOT added (the key space is measured, not declared).
Then:
1. `source()` `ld_allele_join.R`, resolved relative to the script's own path
   (`sys.frame(1)$ofile` / `commandArgs(trailingOnly = FALSE)` `--file=` idiom), so the two
   `run_qtl_coloc.R` callers both find it.
2. **Replace the resolution of `ld_snp_names` at `:232`** — NOT `build_ld_rownames` (F2,
   which is a no-op site). Under the gate: read the catalog TSV, build the panel-side
   `variants_dt` from `ld_obj$variants` (`CHR/POS/REF/ALT`), call
   `ld_allele_join_indices(catalog, panel_variants)`, and produce `ld_snp_names` as the
   catalog's `SNP_ID` for the bound rows — i.e. the **fit key space**. Keep integer panel row
   indices alongside. Leave the ungated path byte-identical.
3. **Measure, do not assume.** Compute the overlap of `overlap_snps` against EACH candidate
   panel key space (`rownames(ld_full)`, `CHR:POS`, `CHR:POS:REF:ALT`, and the
   catalog-bridged names) and log all of them — the same best-overlap discipline the script
   already uses on the QTL side (`:171-176`). The bridged names are what is USED under the
   gate; the others are diagnostic only.
4. **Loud failure.** Under the gate, replace the `:265-271` soft path with a hard
   `stop(sprintf(...))` naming reason / region / ancestry / realized overlap / threshold /
   every measured candidate overlap. Same for a missing-or-unreadable catalog and for
   `use_identity` (`:222`). Non-zero exit, no output JSON. Ungated: byte-identical
   `write_status_json` + `quit(status = 0)`.
5. **Bounded coercion.** Replace `:272` with: subset the SPARSE matrix by the integer panel
   indices FIRST, then `as.matrix()` the **subset**, then attach the fit-space dimnames.
   ⚠ `as.matrix()` on the full panel is the BLOCKER-D dense-materialisation hazard —
   `n_var` 75,497 → ~45 GB (`[[feedback_dense_matrix_verify_memory_bounded]]`). Never
   densify `ld_full`.
6. **Provenance.** Add to BOTH the success `output` list (`:409-424`) and every
   `write_status_json` result (`:73-95`): `ld_matrix` (the path actually opened),
   `ld_allele_join` (the flag as parsed), `ld_key_space` (which bridge was used),
   `ld_panel_overlap`, and the six counters. Use `NA` (JSON `null`), never `0`, when
   unmeasured — the o7o "not measured vs measured clean" convention. **Attach additive fields
   CONDITIONALLY**: in R `list(k = NULL)` *creates* a NULL element (unlike `x$k <- NULL`),
   which would break the byte-identical-EUR proof. This exact trap cost o7o a rewrite.

**STEP 3 — `src/snakemake/rules/qtl_coloc.smk`.** In `rule run_qtl_coloc` (`:282`):
* `input.variants=lambda wc: ([lockstep_variants_path(row["region"], row["ancestry"], config,
  config["paths"]["ld_reference"])] if (row := _qtl_coloc_manifest_row(wc.qtl_coloc_id)) and
  ld_coloc_applies(row["ancestry"], config) else [])` — returning `[]` off the allow-list
  means **EUR's DAG gains no new edge**. Import `lockstep_variants_path` from
  `occlusion_lockstep_cli` using the same sys.path idiom `finemap.smk:65-68` uses.
* `params.ld_allele_join=lambda wc: ld_coloc_join(_qtl_manifest_field(wc, "ancestry"), config)`.
* Shell (`:310-326`): append `--ld-allele-join {params.ld_allele_join}` and, guarded so the
  token is only emitted when the input list is non-empty, `--variant-list {input.variants}`.
* Append a per-pair **receipt** one-liner after the `Rscript` call, in the exact register of
  `finemap.smk:441`: a `{PYTHON_BIN} -c "import json,sys; ..."` that prints `ld_matrix`,
  `ld_key_space`, `ld_panel_overlap`, `ld_allele_join` and the six counters to a `log:`
  artifact. ⚠ Do NOT re-declare `params.region_id` anywhere and do NOT touch
  `finemap.smk:349-350`.

**STEP 4 — STOP conditions, not edits.** `tests/phase2/test_run_qtl_coloc.py` (24 tests) pins
`run_qtl_coloc.R`'s CLI surface. If ANY pre-existing test in `tests/m3` or `tests/phase2` goes
RED: **STOP and surface it with the raw failure text.** 260805-o7o's `AUTH-o7o-01` is
**NOT inherited** — do not edit any pre-existing test file, and do not add a
"make the tests pass" step.
  </action>

  <verify>
    <automated>/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_qtl_coloc_allele_join.py -q -rs</automated>
    <automated>/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/phase2/test_run_qtl_coloc.py -q -rs   # MUST stay 24 passed; any RED = STOP</automated>
    <automated>/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/snakemake --snakefile Snakefile --list >/dev/null && echo LIST_OK</automated>
    <automated>git diff --exit-code dc4bbd2 -- src/legacy/region_analysis/scripts/run_susie_rss.R && echo FREEZE_OK</automated>
    <automated>grep -c -- '--ld-allele-join {params.ld_allele_join}' src/snakemake/rules/qtl_coloc.smk   # MUST be 1</automated>
    <negative_control id="NC-2a" required="true">
Disable the new branch in `ld_allele_join.R` (`if (isTRUE(...))` → `if (FALSE)`, or return the
position-only match). The multiallelic, transposed, palindromic, mismatch, ambiguous and
unusable tests MUST all go RED, and the differential agreement test MUST go RED. Quote every
raw `E AssertionError`.
    </negative_control>
    <negative_control id="NC-2b" required="true">
Break the `order(keep_idx)` lockstep (`orient[ord]` → `rev(orient)`). The alignment test MUST
go RED. This is the one way this fix could itself introduce a sign error — o7o caught the same
defect this way.
    </negative_control>
    <negative_control id="NC-2c" required="true">
Revert the loud failure to `write_status_json` + `quit(status = 0)`. The exit-code assertions
MUST go RED (rc 0 where non-zero was required; an output JSON present where none was allowed).
    </negative_control>
    <negative_control id="NC-2d" required="true">
Revert the bounded coercion to `ld_matrix_subset <- ld_full[...]` with no `as.matrix`. The
end-to-end fixture MUST go RED with the MEASURED error `LD must be of class matrix` — this is
the empirical fact recorded in F3, and observing it is what makes the fix evidence.
Separately, replace the subset-then-coerce with `as.matrix(ld_full)` and confirm the
memory-bound assertion goes RED.
    </negative_control>
    <negative_control id="NC-2e" required="true">
Attach the additive JSON fields UNCONDITIONALLY (the R `list(k = NULL)` trap). The
byte-identical-EUR test MUST go RED. Then run the **inverted** control: the AFR fixture with
the gate ON must be observed **NOT** byte-identical. A byte-identity proof that has never been
seen to fail is not evidence.
    </negative_control>
    <negative_control id="NC-2f" required="true">
Perturb `ld_allele_join.R` only (e.g. drop the palindromic mask) and confirm the DIFFERENTIAL
agreement test alone goes RED while the shipped matcher is untouched — proving the agreement
test actually compares the two implementations rather than one against itself.
    </negative_control>
    <negative_control id="NC-2g" required="true">
**The extractor itself.** A plain "it is callable" check would pass on a hand-copied
duplicate — the exact failure STEP 0 exists to prevent — so prove the helper returns the
SHIPPED function. Point `extract_nested` at a source string in which the matcher's body has
been DELIBERATELY ALTERED (e.g. the palindromic set changed to `c("AT")` only, or `orient`
forced to `1`) and confirm the DIFFERENTIAL agreement test goes RED against that altered
source while staying GREEN against the real file. Then confirm that deleting the assignment
entirely raises the named `STOP-and-surface: could not extract ...` rather than silently
returning `NULL`. Quote both raw. ⚠ Perform the alteration on an in-memory / temp copy of the
source text — `run_susie_rss.R` must remain 0-diff vs `dc4bbd2` at all times, including
mid-control.
    </negative_control>
  </verify>

  <done>
`ld_allele_join.R` exists and is `source()`d by `run_qtl_coloc.R`; every disposition class is
classified and counted; the catalog bridges BOTH rsID- and `chr:pos`-named fits; unbridgeable /
sub-threshold / missing-catalog / `use_identity` all exit NON-ZERO with named reasons under the
gate and are byte-identical rc-0 off it; `qtl_data$LD` is a base matrix produced without
densifying the full panel; provenance + six counters reach the JSON and the log receipt;
EUR JSON byte-identical with an inverted control OBSERVED non-identical; the differential
agreement test passes and was OBSERVED RED under NC-2f **and NC-2g**, with the STEP 0
extractor proven to return the shipped function rather than a stand-in; `run_susie_rss.R`
0-diff vs `dc4bbd2`;
`tests/phase2/test_run_qtl_coloc.py` still 24 passed; all seven negative controls quoted (NC-2a..g).
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-suite gate, scope proof, freeze re-pin, and the SUMMARY</name>
  <files>
.planning/quick/260805-w7u-close-blast-radius-finding-e-crosswalk-t/260805-w7u-SUMMARY.md
  </files>

  <action>
**STEP 1 — Independent full-suite runs, quoted raw, one run each.**
* `tests/m3` — baseline **641 passed / 31 skipped / 0 failed (672 collected)**.
  Required: `failed == 0`, `passed >= 641`, **`skipped <= 31`**.
* `tests/phase2` — baseline **136 passed / 1 skipped / 0 failed (137 collected)**.
  Required: `failed == 0`, `passed >= 136`, `skipped <= 1`.
Run with `-rs`. **A new test landing as a SKIP is not evidence** — reconcile the delta
per-module and state the skip origin of every skip. Reconcile `passed` arithmetically
(`baseline + per-module new == observed`) as o7o did; a budget is not a target.

**STEP 2 — Workflow gates.** `snakemake --list` rc 0 on `config/pipeline.yaml` **and** on
`config/pipeline_lsweep_L15_overlay.yaml`, `..._L20_...`, `..._L30_...`.
⚠ **Do NOT run `--dry-run`** as an acceptance criterion — see `<precondition_stated_honestly>`.

**STEP 3 — Scope + freeze proof, by diff, not by assertion.**
* `git diff --name-only 7b1025d HEAD -- src config tests | sort` — MUST be exactly this plan's
  `files_modified`. No pre-existing test file. If one is present, that is a violation to be
  surfaced, not absorbed.
* `git diff --exit-code dc4bbd2 -- src/legacy/region_analysis/scripts/run_susie_rss.R`
* `git diff --exit-code 7b1025d -- src/python/plink_ld_to_npz.py src/scripts/ld_npz_to_rds.R src/python/condition_ld_matrix.py`
* `git diff --exit-code 7b1025d -- src/snakemake/rules/finemap.smk src/python/aggregate_qtl_coloc.py src/python/ld_panel.py`
* `git diff 7b1025d HEAD -- src/snakemake/rules/finemap.smk | grep -c "region_id"` → 0.
* m3-07 modules 0-diff; `grep -rnE 'condition_ld_matrix|nan_to_num'` over the edited files → empty.
* `git diff 7b1025d HEAD -- src config tests | grep -inE "gsutil|gcloud|\bbq\b|dataproc|hailctl| wb "` → empty.
* Findings G / J / K / L / M and BLOCKER-D's large-region classes: prove untouched by the same
  `--name-only` diff, not by claim.
* `.planning/STATE.md` and `.planning/HANDOFF.json` are **Carter's** — do NOT write them
  (standing directive, `260805-23d-SUMMARY.md` deviation 5). `ROADMAP.md` untouched.

**STEP 4 — SUMMARY.** Use the o7o SUMMARY's shape. It MUST contain:
1. The gate-binding table against `m3-04c-BLAST-RADIUS.md:133-144`, with
   **"Any GWAS×QTL colocalization" → DISCHARGED** and every other row's status carried forward
   unchanged (**G, J, K, L, M and BLOCKER-D's large-region classes explicitly OPEN and untouched;
   H and I NOT re-opened**).
2. **THE BEHAVIOUR CHANGE, NAMED PLAINLY** — for AFR only, the coloc LD panel switches from
   1kG to AoU the first time an `AFR_aou/<m2_id>.rds` exists; variants that previously entered
   on a first-hit position match may now be DROPPED (palindromic / mismatch / ambiguous /
   unusable); jobs that previously wrote `too_few_snps` at rc 0 now **fail the DAG**. Any AFR
   coloc figure or table regenerated after this lands is NOT comparable to one produced before
   it. This is a disclosable analysis change for the manuscript / OSF record.
3. **The two brief corrections**, with evidence: F2 (`build_ld_rownames` is not on the AoU
   path — `ld_npz_to_rds.R:440` gives `R` dimnames and `%||%` short-circuits at `:232`) and
   F3 (four stacked exit-0 layers, including the MEASURED
   `runsusie -> "LD must be of class matrix"` on a `dsCMatrix`).
4. **The reuse decision, verbatim from `<the_reuse_decision>`**, including the named
   extraction follow-up and what re-freezing will mean.
5. **`<explicitly_deferred>` reproduced in full**, especially **E-2** (QTL-beta ↔ panel-ALT
   orientation), so nobody discovers it by diffing.
6. Every negative control (NC-1a..d, NC-2a..g) with its **raw observed RED**, plus any control
   found unachievable — disclosed as a deviation with the measured behaviour and an achievable
   substitute, exactly as o7o handled the schema control.
7. Frozen-contract re-pin statement and the freeze SHAs.
8. `$0 / NC State / zero perimeter contact / the ~11-day AoU fire was NOT triggered.`
  </action>

  <verify>
    <automated>/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3 -q -rs 2>&1 | tail -5</automated>
    <automated>/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/phase2 -q -rs 2>&1 | tail -5</automated>
    <automated>for c in config/pipeline.yaml config/pipeline_lsweep_L15_overlay.yaml config/pipeline_lsweep_L20_overlay.yaml config/pipeline_lsweep_L30_overlay.yaml; do /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/snakemake --snakefile Snakefile --configfile "$c" --list >/dev/null && echo "OK $c" || echo "FAIL $c"; done
</automated>
    <automated>git diff --name-only 7b1025d HEAD -- src config tests | sort</automated>
    <automated>git diff --exit-code dc4bbd2 -- src/legacy/region_analysis/scripts/run_susie_rss.R && echo FREEZE_RUN_SUSIE_OK</automated>
    <automated>git diff --exit-code 7b1025d -- src/python/plink_ld_to_npz.py src/scripts/ld_npz_to_rds.R src/python/condition_ld_matrix.py && echo FROZEN_CONTRACTS_OK</automated>
    <automated>git diff --exit-code 7b1025d -- src/snakemake/rules/finemap.smk src/python/aggregate_qtl_coloc.py src/python/ld_panel.py && echo OUT_OF_SCOPE_OK</automated>
    <automated>git diff 7b1025d HEAD -- src config tests | grep -inE "gsutil|gcloud|\bbq\b|dataproc|hailctl| wb " ; echo "rc=$? (1 = no perimeter strings, PASS)"</automated>
  </verify>

  <done>
`tests/m3` >= 641 passed / <= 31 skipped / 0 failed and `tests/phase2` >= 136 passed /
<= 1 skipped / 0 failed, both independently run and reconciled per-module with every skip's
origin named; `--list` rc 0 on the base config and all three lsweep overlays;
`git diff --name-only 7b1025d HEAD -- src config tests` equals this plan's `files_modified`
exactly; `run_susie_rss.R` 0-diff vs `dc4bbd2`; frozen contracts, `finemap.smk`,
`ld_panel.py`, `aggregate_qtl_coloc.py` and the m3-07 modules all 0-diff; no perimeter string
in the diff; `.planning/STATE.md` / `HANDOFF.json` / `ROADMAP.md` untouched; SUMMARY written
with all eight required sections and every negative control's raw RED quoted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `config/pipeline.yaml` → resolver | Config decides which LD **bytes** a published fit is colocalized against |
| manifest TSV → `_qtl_coloc_ld_input` | An on-disk TSV, regenerable out-of-band, competing with the resolver as a path oracle |
| LD panel `.rds` → `coloc::runsusie` | Untrusted key space and matrix class crossing into a numeric fit |
| region variant catalog TSV → the join | A file path interpolated into a shell and read as the bridge |
| `wildcards.qtl_coloc_id` → filesystem | Pre-existing path-traversal surface (T-02-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-w7u-01 | Tampering | `qtl_coloc_manifest.tsv` `ld_matrix_path` | **mitigate** | Under the gate the column is never read; the builder emits a non-path sentinel so any other reader fails visibly (T1 STEP 5), and T1's behavior pins that a competing existing file loses to the resolver |
| T-w7u-02 | Spoofing | LD panel `.rds` presenting keys in the wrong space | **mitigate** | Keys are bridged through the catalog with an allele-aware 4-key join; sub-threshold overlap is a non-zero exit naming every measured candidate space (T2 STEP 2/4) |
| T-w7u-03 | Repudiation | Per-pair JSON cannot say which panel it used | **mitigate** | `ld_matrix`, `ld_key_space`, `ld_panel_overlap`, `ld_allele_join` + six counters in the JSON and in a per-pair log receipt (T2 STEP 2.6 / STEP 3) |
| T-w7u-04 | Denial of Service | `as.matrix()` on a full `n_var²` panel (~45 GB at SH2B3) | **mitigate** | Subset the sparse matrix by integer index FIRST, coerce the subset only; asserted by a memory-bound test and NC-2d |
| T-w7u-05 | Elevation of Privilege | Config change silently repointing a published Track-A fit | **mitigate** | Single gate, `is True` sub-key, fail-safe CHANGE NOTHING; byte-identical EUR JSON proven with an inverted control (NC-2e) |
| T-w7u-06 | Information Disclosure | Region/catalog paths interpolated into the rule shell | **accept** | `wildcard_constraints qtl_coloc_id=r"[A-Za-z0-9_.\-]+"` (`qtl_coloc.smk:26-27`, T-02-05) already anchors the only user-influenced component; the catalog path is config-derived, not wildcard-derived |
| T-w7u-07 | Tampering | A second `ld_read_path` / crosswalk read drifting from `finemap.smk`'s | **mitigate** | `CURATED_TO_M2` and `REGION_SAFE_TO_ID` reused from the shared module scope; `grep -c load_curated_to_m2 qtl_coloc.smk == 0` is an automated verify |
| T-w7u-08 | Tampering | Two allele-key implementations drifting apart | **mitigate** | Differential agreement test driving both on shared fixtures, with NC-2f proving the comparison can fail |
| T-w7u-10 | Spoofing | The "shipped" side of the agreement test is actually a hand-copied duplicate, so the test agrees with itself | **mitigate** | STEP 0 body-walk extraction from the real frozen source, landed as one reviewable named helper; NC-2g proves the extractor tracks the source by going RED against a deliberately altered one; infeasibility is a STOP-and-surface, never an improvisation |
| T-w7u-09 | Repudiation | A green assertion that was never observed red | **mitigate** | Eleven mandatory negative controls (NC-1a..d, NC-2a..g), each with its raw RED quoted in the SUMMARY; `.pyc` staleness explicitly guarded (NC-1a) |
</threat_model>

<verification>
**Automated, all `$0`, all on NC State.**

1. `pytest tests/m3/test_qtl_coloc_ld_resolution.py -q -rs` — passes, 0 skips.
2. `pytest tests/m3/test_qtl_coloc_allele_join.py -q -rs` — passes, 0 skips.
3. `pytest tests/m3 -q -rs` — `failed == 0`, `passed >= 641`, `skipped <= 31`.
4. `pytest tests/phase2 -q -rs` — `failed == 0`, `passed >= 136`, `skipped <= 1`.
5. `snakemake --list` rc 0 on `config/pipeline.yaml` + the three lsweep overlays.
6. `git diff --exit-code dc4bbd2 -- run_susie_rss.R` (freeze), and 0-diff on
   `plink_ld_to_npz.py`, `ld_npz_to_rds.R`, `condition_ld_matrix.py`, `finemap.smk`,
   `ld_panel.py`, `aggregate_qtl_coloc.py`, the four m3-07 modules.
7. `git diff --name-only 7b1025d HEAD -- src config tests` == `files_modified`.
8. **Eleven** negative controls (NC-1a..d, NC-2a..g) OBSERVED RED and quoted raw.

**Explicitly NOT a verification (and why):**
* `snakemake --dry-run` — the AoU panel does not exist and `data/processed/ld_reference/`
  is absent, so any dry-run criterion touching the resolved AFR path is unsatisfiable. A
  prior plan in this arc shipped exactly that. `--list` is the achievable substitute and does
  **not** evaluate input functions — stated as a limit, not sold as coverage.
* `ld_status` / `ld_overlap_fraction` — DISQUALIFIED as EUR-invariance evidence (m3-04c
  proved EUR numerics move while both stay byte-identical). Whole-file JSON comparison only.
* `status` / `n_snps_overlap` alone — same disqualification, coloc-side.
</verification>

<success_criteria>
- [ ] `m3-04c-BLAST-RADIUS.md:141` gate row "Any GWAS×QTL colocalization" is **DISCHARGED**:
      for an allow-listed ancestry the LD reaching `coloc::runsusie` is the artifact
      `resolve_ld_path` selects, via the same crosswalk object `run_finemap` uses.
- [ ] The manifest can no longer supply a competing path for a gated ancestry, and a stale
      value cannot be silently opened.
- [ ] Closing E did **not** trade silently-wrong LD for silently-no LD: the four stacked
      exit-0 layers (F3) become one non-zero exit with a named reason under the gate.
- [ ] The coloc join is keyed identically to the finemap join, with the same six dispositions,
      held to the shipped matcher by a differential test that has been observed RED — where
      "the shipped matcher" is the REAL frozen source obtained by the STEP 0 body-walk
      extraction, proven by NC-2g to be neither hand-copied nor stale.
- [ ] `coloc::runsusie` receives a base matrix, produced without ever densifying the panel.
- [ ] Every disposition class and the opened panel path reach the JSON and a log receipt.
- [ ] Track-A EUR is byte-identical, proven on the whole emitted JSON, with an inverted
      control observed non-identical.
- [ ] Baselines held: `tests/m3` 0 failed / <= 31 skipped; `tests/phase2` 0 failed / <= 1
      skipped. No pre-existing test edited (AUTH-o7o-01 is NOT inherited); any RED was a
      STOP-and-surface.
- [ ] `run_susie_rss.R` 0-diff vs `dc4bbd2`; frozen contracts 0-diff; m3-06 stays HELD.
- [ ] G, J, K, L, M and BLOCKER-D's large-region classes untouched, proven by diff. H and I
      not re-opened.
- [ ] E-2 (QTL-beta ↔ panel-ALT orientation) is named as deferred, with its magnitude made
      measurable rather than left invisible.
- [ ] All **eleven** negative controls observed RED and quoted; no assertion accepted green
      that was never seen fail.
- [ ] `$0`, NC State, zero perimeter contact, the ~11-day AoU fire NOT triggered.
- [ ] `.planning/STATE.md` and `.planning/HANDOFF.json` NOT written (Carter's).
</success_criteria>

<output>
On completion write
`.planning/quick/260805-w7u-close-blast-radius-finding-e-crosswalk-t/260805-w7u-SUMMARY.md`
with the eight sections listed in Task 3 STEP 4. It is the sole planning artifact —
`ROADMAP.md`, `STATE.md` and `HANDOFF.json` stay untouched.

**Commits: explicit paths only, no `git add .` / `-A` (GPFS shared tree).**
| Task | Message |
|---|---|
| 1 | `feat(260805-w7u-T1): route the coloc LD path through the resolver; make the manifest fail loudly (FINDING E)` |
| 2 | `feat(260805-w7u-T2): key the coloc join identically, exit non-zero when unbridgeable, hand coloc a real matrix` |
| 3 | `docs(260805-w7u-T3): full-suite gate + SUMMARY -- finding E DISCHARGED, E-2 deferred` |
</output>
