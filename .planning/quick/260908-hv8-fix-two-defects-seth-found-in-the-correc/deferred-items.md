# Deferred items — 260908-hv8

Out-of-scope discoveries. **Not fixed here.** Recorded so they are not lost.

## 1. `tcujq` two-file docstring defect — DEFERRED (unchanged)
Carried forward from `260903-ict` / `260904-dgi`. Lives under `src/`. This task was
**DOCS-ONLY**; `src/` and `tests/` were not touched (`git status --porcelain -- src tests`
empty at start and at finish). No change of status.

## 2. PART D — pairs-per-deletion measurement — QUEUED, NOT EXECUTED
**Carter fires this, never an agent.** Still the open measurement that would let Finding 2's
magnitude be attributed (the within-window clustering vs heterogeneity question from
`260904-dgi` A3). Untouched here.

## 3. Historical task records still contain the retired phrase — INTENTIONALLY LEFT
The phrase `no covering deletion` remains in prior task dirs:
- `.planning/quick/260903-ict-…/CONTENT-SPEC.md:155,176`
- `.planning/quick/260903-ict-…/260903-ict-PLAN.md:553`
- `.planning/STATE.md:2355` (the `260904-dgi` row, quoting what that task did)

These are the **immutable record of what was done at the time**. Editing them would falsify
history rather than correct a live claim. The guard for this task is scoped to the
**disclosure**, which is the artifact that would be read by a third party. Left deliberately.

## 4. Not re-verified: other `mk7ze line N` citations elsewhere in the repo
This task swept and fixed citations to the **two multi-line sentences** D7 names, plus
`mk7ze line 104` (verified correct). Other single-line mk7ze citations (`line 82`,
`lines 321-322`, `line 278`) were **not** re-measured; `line 82` was incidentally confirmed
correct while verifying D9. A full citation audit of the disclosure was not in scope.
