# Deferred items — 260908-n48

Out-of-scope discoveries. **Not fixed here.** Recorded so they are not lost.

## 1. `tcujq` two-file docstring defect — DEFERRED (unchanged)
Carried forward from `260903-ict` / `260904-dgi` / `260908-hv8`. Lives under `src/`. This task
was **DOCS-ONLY**; `git status --porcelain -- src tests` was EMPTY at start and at finish.
No change of status.

## 2. PART D — pairs-per-occluding-deletion DISTRIBUTION — QUEUED, NOT EXECUTED
**Carter fires this, never an agent.** It is a **MEASUREMENT, not an objection**, and it is now
recorded as such in the disclosure. It is the sole remaining pre-posting item.

## 3. Spec-internal conflict between `ALSO RECORD` and the forbidden-phrase list
The brief asked for two phrases to be quoted verbatim that its own verification list forbids.
Resolved in-task by recording the substance without planting the literal strings (see SUMMARY
"DEVIATION"). **Worth fixing in the spec-writing process, not just in this task:** a brief that
quotes a retired phrase in order to repudiate it will collide with any guard that retires the
phrase. Prefer specifying the MEANING to be recorded, not the literal sentence.

## 4. Historical task records still contain the retired phrases — INTENTIONALLY LEFT
`no covering deletion` remains in prior task dirs and at `STATE.md:2355`, and both retired
phrases appear in this task's own SUMMARY where the conflict is explained. These are the
**immutable record of what was done at the time**. The guard is scoped to the **disclosure** —
the artifact a third party reads — exactly as `260908-hv8` scoped it.

## 5. Stale `gitStatus` context at session start
The harness reported HEAD as `d66efe7`; the measured HEAD was `d034370`. Measured rather than
trusted. Noted because a copied HEAD pin would have made the `cmp` reference wrong.

## 6. Not re-verified: other `mk7ze line N` citations
As in hv8, `line 82`, `lines 321-322`, `line 278` were not re-measured. A full citation audit of
the disclosure remains out of scope.
