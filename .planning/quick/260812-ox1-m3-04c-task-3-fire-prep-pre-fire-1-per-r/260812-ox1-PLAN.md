---
phase: quick-260812-ox1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/python/run_native_ld_panel.py
  - tests/m3/test_run_native_ld_panel.py
  - .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-evidence.log
  - .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-evidence.tsv
  - .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md
autonomous: true
requirements: [OX1-D1, OX1-D2, OX1-D3, OX1-D4]   # the four CONTEXT.md deliverables, in order

must_haves:
  truths:
    - "In gs:// mode, a region WITH occluded variants uploads {region_id}.occlusion_manifest.tsv inside the existing `if ok:` gate, alongside the excludelist — proven by a test against _MockGsutil, observed RED before the implementation landed"
    - "A region with ZERO occlusions uploads no manifest and raises no error (absent => skipped); a verify_failed region uploads NO region artifact — no cp destination ending .npz / .afreq / .occluded.excludelist / .occlusion_manifest.tsv; the unconditional panel-TSV status-row cp is the ONLY permitted cp destination (fire-path-faithful — review §4 row 3: prior fires appended status=error rows unconditionally)"
    - "The per-region manifest filename matches the lockstep aggregation glob *occlusion_manifest*.tsv — asserted by a test that reads the literal patterns from the .smk, not merely claimed"
    - "The shared local occlusion_manifest.tsv behavior is byte-unchanged in local (non-gs) mode"
    - "Full tests/m3 + tests/phase2 ran exactly ONCE post-change: 902+N passed / EXACTLY 31 skipped / 0 failed and 136 / EXACTLY 1 / 0; the new baseline (N = count of new tests) is recorded"
    - "Fresh L-01..L-20 evidence (log + TSV, rcw schema) exists in THIS quick dir, anchored at the post-Task-1 HEAD; the rcw evidence files are byte-untouched"
    - "PRE-FIRE 3 is settled to a decision-grade answer: the gated test computes both sides in the same 0-based space and therefore CANNOT false-pass on an origin error; the runbook names it as the instrument and FORBIDS manual line-number comparison"
    - "260812-ox1-READY-TO-FIRE.md exists, contains ONLY Carter's remaining actions in fire order, quotes every perimeter command from the CORRECTED review text only, and states AN AGENT MUST NEVER FIRE IT"
    - "ZERO perimeter contact across the whole task (no gsutil/gcloud/bq/wb — grep of the evidence log proves 0 invocations); $0; nothing fired"
  artifacts:
    - path: "src/python/run_native_ld_panel.py"
      provides: "Per-region Stage-A manifest write (occlusion block, best-effort guarded) + existence-gated upload (inside `if ok:`)"
      contains: "occlusion_manifest.tsv"
    - path: "tests/m3/test_run_native_ld_panel.py"
      provides: "Five new tests, all named with substring per_region_occlusion_manifest"
      contains: "per_region_occlusion_manifest"
    - path: ".planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-evidence.log"
      provides: "Verbatim command+output blocks for L-01..L-20 re-anchor + PRE-FIRE 3 CONTEXT blocks + RED/GREEN transcripts + zero-perimeter proof"
    - path: ".planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-evidence.tsv"
      provides: "Machine-readable L-01..L-20 rows, same 8-column schema as 260811-rcw-evidence.tsv"
    - path: ".planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md"
      provides: "Carter-only runbook: 11 ordered items, PRE-FIRE 1b decision template, PRE-FIRE 3 interpretation table"
      contains: "AN AGENT MUST NEVER FIRE"
  key_links:
    - from: "src/python/run_native_ld_panel.py (occlusion block, inside the existing best-effort try at ~:821)"
      to: "src/python/occlusion_manifest.py append_region_manifest"
      via: "ocm.build_region_records(region_id, raw_rows) + ocm.append_region_manifest(Path(f'{out_prefix}.occlusion_manifest.tsv'), records) — fresh per-region path => header row written"
      pattern: "append_region_manifest"
    - from: "src/python/run_native_ld_panel.py (`if ok:` upload block, ~:922-938)"
      to: "gs://<out-dir>/{region_id}.occlusion_manifest.tsv"
      via: "_gsutil_upload gated on Path(...).is_file()"
      pattern: "occlusion_manifest\\.tsv"
    - from: "tests/m3/test_run_native_ld_panel.py glob-contract test"
      to: "src/snakemake/rules/m3_occlusion_lockstep.smk:174-177 (*occlusion_manifest*.tsv patterns)"
      via: "fnmatch against the literal patterns read from the .smk source"
      pattern: "occlusion_manifest\\*\\.tsv"
    - from: "260812-ox1-READY-TO-FIRE.md"
      to: "260811-rcw-PRE-FIRE-GATE-REVIEW.md §4/§5 + ## Corrections (2026-08-12)"
      via: "every quoted command copied from the CORRECTED review text ONLY (never the m3-04c PLAN or blast radius — review §2.1(8): their pointers drifted)"
      pattern: "gsutil ls gs://rw-migration-aou-rw-476cdac2/ld/AFR_aou"
---

<objective>
m3-04c Task 3 fire-prep: advance the PRE-FIRE checklist as far as an agent can, per
Carter's directive ("do all you can for the checklist and get me to the point where im
ready to fire"). Four deliverables from the LOCKED CONTEXT.md:

1. **PRE-FIRE 1 lands** — per-region occlusion-manifest upload (the review's PREFERRED
   lower-risk option), TDD, reviewed code + tests.
2. **L-check re-anchor** — L-01..L-20 re-measured at the post-change HEAD; fresh
   evidence log + TSV in this quick dir.
3. **PRE-FIRE 3 settled code-side** — the 0-vs-1-based index-origin question documented
   to a decision-grade answer with the safe in-perimeter instrument named.
4. **READY-TO-FIRE runbook** — one document with ONLY Carter's remaining actions.

Purpose: close the last agent-closeable gap (the manifest-egress pre-registration
compliance item, PRE-FIRE 1) and hand Carter a single, corrected-command-only surface
for the ~11-day / $385–1,084 fire decision. **AN AGENT MUST NEVER FIRE IT.**

Output: 1 modified source file, ~5 new tests, 3 new docs artifacts, 3 atomic commits.
</objective>

<hard_bounds>
⛔ **ZERO perimeter contact** — no `gsutil`, `gcloud`, `bq`, `wb`, not even read-only.
The `_MockGsutil` harness substitutes in tests. Nothing started on any VM/cluster. $0.
No OSF posting, no manuscript edits.

⛔ **Do NOT edit:** the three `PY_FROZEN_RELS` files (`src/python/plink_ld_to_npz.py`,
`src/python/condition_ld_matrix.py`, `src/python/occlusion_span_filter.py` — pinned by
`tests/m3/test_source_freeze_pins.py:83-87`), `run_susie_rss.R` (CODE-frozen),
`.planning/amendments/`, `results/`, `docs/manuscript/`, the rcw quick dir
(`.planning/quick/260811-rcw-*` — dated record), any `260811-*` or `260812-09a` quick dir.

⛔ **DECISIONS.md: this task ADDS no decision.** PRE-FIRE 1 landing implements the
review's recorded recommendation; branch (i) selection remains Carter's signature in the
runbook template. If you believe a decision is being made, STOP and report instead.

Git: explicit paths only (never `git add .`/`-A` — GPFS shared tree); no worktrees;
one atomic commit per task. If the GPFS loose-object loss recurs ("invalid object /
Error building trees"), recover via the guarded `git hash-object -w` recipe before
retrying the commit.
</hard_bounds>

<context>
@.planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-CONTEXT.md   (LOCKED decisions — authoritative)
@.planning/quick/260811-rcw-assemble-the-m3-04c-task-3-pre-fire-gate/260811-rcw-PRE-FIRE-GATE-REVIEW.md   (THE fire surface — read `## Corrections (2026-08-12)` FIRST, then §2, §3, §4, §5)
@.planning/quick/260811-rcw-assemble-the-m3-04c-task-3-pre-fire-gate/260811-rcw-evidence.tsv   (L-check schema: check_id, area, what, command, expected, observed, verdict, evidence_date)
@src/python/run_native_ld_panel.py
@src/python/occlusion_manifest.py
@src/snakemake/rules/m3_occlusion_lockstep.smk
@tests/m3/test_run_native_ld_panel.py
@tests/m3/test_occlusion_span_filter.py
@.claude/skills/aou-ld-pipeline/SKILL.md   (runbook step-1 source: fresh-clone checklist + invariants; NO perimeter action from it)

<interfaces>
<!-- Verified at planning time (HEAD 76dd7cd, 2026-08-12). Re-anchor line numbers -->
<!-- against YOUR tree before citing them anywhere durable (review §2.1(8) discipline). -->

From src/python/run_native_ld_panel.py:
```python
# :759  out_prefix = str(compute_dir / region_id)
# :821-831  the existing best-effort Stage-A append (KEEP UNCHANGED, add beside it):
try:
    ocm.append_occlusion_rows(compute_dir, region_id, raw_rows, edges=occlusion_edges)
except Exception as manifest_exc:  # noqa: BLE001 — provenance is best-effort
    print(f"WARN {region_id}: occlusion manifest append failed ...", file=sys.stderr, flush=True)

# :921-938  the `if ok:` upload block (gs mode). Current upload set: .npz (:925-926),
# .afreq (:928-929), .occluded.excludelist (:934-937). The manifest upload goes HERE.
# :934  gating pattern to mirror: `if exclude_path is not None and Path(exclude_path).is_file():`
# :930-933  the m3-07b egress comment discipline to mirror (coordinate/id-only, no genotypes)

# :947  append_panel_row(panel_tsv, result, scratch_dir=compute_dir) — UNCONDITIONAL
#       (runs for ok / verify_failed / error alike); in gs mode the default panel TSV
#       URI is set at ~:734 (`_gs_join(gs_out_dir, _DEFAULT_PANEL_NAME)`) and
#       append_panel_row's gs branch ends with `_gsutil_upload(local_mirror, gs_uri)`
#       — i.e. a cp to the panel-TSV URI fires on EVERY region, including
#       verify_failed. Test (c) must scope its assertion accordingly (see behavior).

# :670-700  _reclaim_region_scratch: keep-set = {f"{region_id}.npz", f"{region_id}.occluded.excludelist"}
# => the per-region manifest IS reclaimed after a successful region (gs: after upload —
# bucket copy durable; local: shared occlusion_manifest.tsv is the durable local record,
# and it does NOT match the f"{region_id}.*" reclaim glob). This is ACCEPTABLE under the
# locked design — do NOT widen the keep-set.
```

From src/python/occlusion_manifest.py:
```python
def append_region_manifest(manifest_path, records) -> Path   # :155
    # fresh-file path :179-181 (header written); append path :195-196; dedup (region_id, variant_id)
def append_occlusion_rows(out_dir, region_id, rows, *, edges=None,
                          manifest_name="occlusion_manifest.tsv") -> Path   # :200
def build_region_records(region_id, rows) -> list[dict]   # records carry STAGE_A_COLUMNS
def aggregate_manifests(manifest_paths, out_path) -> Path  # skips absent/empty inputs (:230-235)
# => a zero-occlusion region legitimately has NO manifest anywhere; nothing may assert
#    coverage == 276 (false-invariant rule, gates.blocker4_partial_rollup)
```

From src/snakemake/rules/m3_occlusion_lockstep.smk (:174-177):
```python
OCCLUSION_MANIFESTS = _occl_sorted_glob(
    os.path.join(OCCLUSION_MANIFEST_DIR, "**", "*occlusion_manifest*.tsv"),
    os.path.join(OCCLUSION_MANIFEST_DIR, "*occlusion_manifest*.tsv"),
)
```

From tests/m3/test_run_native_ld_panel.py:
```python
class _MockGsutil:  # :656+ — records every argv in .calls; banks upload bytes in
                    # .contents[dst] at cp time (assertions survive scratch reclaim)
_setup_region1_cohort(tmp_path)  # ~:1620s — returns (bfile, bim, (chrom, from_bp, to_bp),
                                 # occluded_ids (5), vid_at); pair with
                                 # _MockPlink(bim, nan_snps=occluded_ids)
_MockPlink(bim, corrupt_regions={...})   # :352 usage — forces verify_failed
_setup_cohort(tmp_path)                  # zero-occlusion fixture (gs tests :754+)
# Existing gs-mode assertions use MEMBERSHIP (`in cp_dsts`) + suffix checks — planning-time
# grep confirmed NO test pins the upload set as exactly-three cp destinations. Re-verify:
#   grep -n "cp_dsts\|calls if c\[0\] == .cp." tests/m3/test_run_native_ld_panel.py
# If you find one, widen it AND pay with a stricter direct assertion (AUTH-o7o-01).
```

From tests/m3/test_occlusion_span_filter.py (PRE-FIRE 3 facts — verify, then cite):
```python
# :180-183  note: "0-based is assumed here (the natural enumerate(rows) index).
#            If the real .bim shows 1-based, add 1." — the source doc does not state the base
# :186  _REGION1_REAL_WINDOW_OCCLUDED_ROW_INDICES = {10328, 44784, 46714, 59097, 66730}
# :492  def test_region1_real_window_known_answer_gated():  # skips when
#        data/aou/region1_window.bim is absent
# :520  got_row_indices = {i for i, r in enumerate(rows) if r[1] in occluded_set}
#        => BOTH sides computed in the SAME 0-based space => ORIGIN-SAFE: an origin
#        error makes it FAIL loudly; it cannot false-pass.
#        (CONTEXT.md cites :519 — re-anchor and record the measured line.)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PRE-FIRE 1 — per-region occlusion-manifest upload (TDD: RED observed, then GREEN)</name>
  <files>tests/m3/test_run_native_ld_panel.py, src/python/run_native_ld_panel.py</files>
  <behavior>
    Five new tests in tests/m3/test_run_native_ld_panel.py. EVERY name MUST contain the
    substring `per_region_occlusion_manifest` so one `-k` selects all five. Use the
    existing `_MockGsutil` + `_MockPlink` + `_setup_region1_cohort` / `_setup_cohort`
    harness. NO real gsutil anywhere.

    (a) test_gs_per_region_occlusion_manifest_uploaded_for_occluded_region — region-1
        topology (`_setup_region1_cohort`, `_MockPlink(bim, nan_snps=occluded_ids)`),
        gs:// out-dir + scratch_dir + _MockGsutil. Assert: status == "ok"; cp
        destinations include `{gs_out}/m2_region_00001.occlusion_manifest.tsv`; the
        excludelist upload is STILL present (unchanged property); AND the uploaded
        bytes (`mock_gs.contents[uri]`) parse as a TSV whose header matches the
        STAGE_A_COLUMNS ordering and whose data rows carry exactly the 5 occluded
        variant_ids under region_id m2_region_00001 (the stricter direct assertion
        that pays for any pin widening — AUTH-o7o-01 precedent). RED pre-impl.
    (b) test_gs_per_region_occlusion_manifest_only_for_occluded_regions — one region
        WITH occlusions + one zero-occlusion region against ONE shared _MockGsutil
        (two drv.run_native_ld_panel calls if one mixed bfile is impractical).
        Assert: the occluded region's manifest IS uploaded (RED pre-impl); the clean
        region has NO cp dst ending in its `.occlusion_manifest.tsv`; both regions
        status == "ok"; no exception (absent => skipped, never an error).
    (c) test_gs_per_region_occlusion_manifest_never_uploaded_on_verify_failed —
        region-1 topology + `corrupt_regions={"m2_region_00001"}` forcing content
        verify failure. Assert: status == "verify_failed"; the per-region manifest
        file EXISTS in local scratch (written pre-plink — RED pre-impl); NO REGION
        ARTIFACT was uploaded — assert NO cp destination ends with `.npz`, `.afreq`,
        `.occluded.excludelist`, or `.occlusion_manifest.tsv` (the region-artifact
        upload set stays inside `if ok:` — unchanged); the ONLY permitted cp
        destination is the panel-TSV URI (assert every issued cp destination equals
        it). Do NOT assert zero cp calls — that is unsatisfiable: the test's
        DOCSTRING must state WHY a panel-TSV cp IS expected on verify_failed —
        `append_panel_row` (~:947 — re-anchor) runs UNCONDITIONALLY and in gs mode
        uploads the default gs:// panel TSV (~:734) via cp at the tail of its gs
        branch; this is the fire-path-faithful default panel behavior the review §4
        row 3 documents ("prior fires appended status=error rows unconditionally").
    (d) test_per_region_occlusion_manifest_name_matches_lockstep_glob — read the
        LITERAL glob pattern strings out of src/snakemake/rules/m3_occlusion_lockstep.smk
        source text (regex for the `*occlusion_manifest*.tsv` literals; fail loudly if
        absent = the contract moved), then assert
        `fnmatch.fnmatch("m2_region_00001.occlusion_manifest.tsv", "*occlusion_manifest*.tsv")`
        is True, plus an in-test NEGATIVE control: a name that must NOT match
        (e.g. "m2_region_00001.occl.tsv") asserts False.
    (e) test_per_region_occlusion_manifest_leaves_shared_local_manifest_byte_unchanged —
        LOCAL mode, region-1 topology. Independently construct the expected shared
        manifest bytes (ocm.build_region_records + ocm.append_region_manifest to a
        separate tmp path) and assert `{out_dir}/occlusion_manifest.tsv` is
        byte-identical. Assert the expected construction is non-empty (5 rows) so the
        comparison cannot pass vacuously.
  </behavior>
  <action>
    **Order is mandatory (standing rule: a green assertion needs a negative control —
    every NEW-behavior assertion must be OBSERVED RED before the impl lands):**

    1. Write the five tests. Do NOT touch src yet.
    2. **RED run** — capture verbatim output (goes into the evidence log as CONTEXT-RED
       in Task 2):
       `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_run_native_ld_panel.py -k per_region_occlusion_manifest -q`
       Expected RED: (a), (b), (c) FAIL (no per-region manifest exists yet). (d) and
       (e) pin UNCHANGED/contract behavior and are expected GREEN pre-impl; prove EACH
       can fail via a one-edit deliberate mutation ((d): fnmatch a wrong name; (e):
       perturb the expected bytes), observe the failure, REVERT, and record both runs.
       Beware the .pyc same-second staleness trap
       (feedback_negative_control_defeated_by_bytecode_cache) — if a revert-and-rerun
       lands in the same second, touch the file or clear __pycache__.
    3. Implement in src/python/run_native_ld_panel.py — TWO edits ONLY:
       * **Occlusion block** (inside the EXISTING best-effort `try:` at ~:821, AFTER
         the existing `ocm.append_occlusion_rows(...)` call, which stays byte-
         unchanged): additionally write the per-region Stage-A manifest —
         `ocm.append_region_manifest(Path(f"{out_prefix}.occlusion_manifest.tsv"), ocm.build_region_records(region_id, raw_rows))`.
         Fresh per-region path => header row via append_region_manifest's fresh-file
         branch. Both writes stay inside the ONE try/except: provenance must NEVER
         abort a region. One-line comment citing WHY per-region beats shared-object
         upload (review §5 PRE-FIRE 1: P3 lesson ff8cc47; overwrite race under any
         future sharded fan-out) — one line, not an essay.
       * **Upload block** (inside `if ok:` at ~:922, after the excludelist upload
         ~:934-937, mirroring its existence-gate shape):
         ```python
         region_manifest = Path(f"{out_prefix}.occlusion_manifest.tsv")
         if region_manifest.is_file():
             _gsutil_upload(region_manifest,
                            _gs_join(gs_out_dir, f"{region_id}.occlusion_manifest.tsv"))
         ```
         with a comment mirroring the m3-07b egress discipline at :930-933
         (coordinate/id-only — same egress class as the excludelist; no genotypes, no
         per-person counts). NO object is ever overwritten: per-region names are
         unique and resume skips banked regions before this block.
       Do NOT touch `_reclaim_region_scratch` (per-region manifest is transient in
       scratch after success; gs: bucket copy durable; local: the SHARED
       occlusion_manifest.tsv is the durable record, outside the reclaim glob).
    4. **GREEN run** — same `-k` command as step 2: all five pass. Then containment:
       `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_run_native_ld_panel.py -q`
       (whole file green — no existing gs/local/occlusion test regressed). The FULL
       tests/m3 + tests/phase2 runs happen ONCE, in Task 2 (they serve as both this
       task's suite verification and L-03/L-04 — do not run them twice).
    5. Commit (one atomic commit, code + tests, explicit paths only):
       `git add src/python/run_native_ld_panel.py tests/m3/test_run_native_ld_panel.py`
       `git commit -m "feat(quick-260812-ox1): PRE-FIRE 1 — per-region occlusion-manifest upload (TDD; review §5 preferred option)"`
  </action>
  <verify>
    <automated>/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3/test_run_native_ld_panel.py -q</automated>
  </verify>
  <done>
    Five new tests, all matching `-k per_region_occlusion_manifest`; (a)/(b)/(c)
    OBSERVED RED then GREEN with verbatim transcripts captured; (d)/(e) failability
    demonstrated via deliberate mutation then reverted; whole test file green; the
    shared manifest append and the existing 3-object upload set byte-unchanged; frozen
    files untouched (`git status --porcelain -- src/python/plink_ld_to_npz.py src/python/condition_ld_matrix.py src/python/occlusion_span_filter.py` prints nothing);
    one atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: L-check re-anchor at post-change HEAD + PRE-FIRE 3 index-origin settle (evidence)</name>
  <files>.planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-evidence.log, .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-evidence.tsv</files>
  <action>
    **Deliverable 2 — re-run the L-01..L-20 command set** from 260811-rcw-evidence.tsv
    (column `command`) at the post-Task-1 HEAD. Write `260812-ox1-evidence.log`
    (`##### L-NN` blocks: command + verbatim unedited output) and
    `260812-ox1-evidence.tsv` (same 8-column schema, evidence_date 2026-08-12). The
    rcw evidence files are a dated record — do NOT modify them.

    **Gate anchor (FIRST line of the evidence log):** record
    `task_start_sha=<pre-Task-1 HEAD>` — the parent of Task 1's feat commit (at Task 2
    start, with only Task 1's commit landed since task start, this is
    `git rev-parse HEAD^`; sanity-check it against `git log --oneline -2`). This
    anchors the rcw-untouched gate, which requires BOTH
    `git log --oneline <task_start_sha>..HEAD -- <rcw-dir>` EMPTY (catches COMMITTED
    edits) AND `git status --porcelain <rcw-dir>` EMPTY (catches uncommitted edits +
    untracked additions) — `git diff --quiet HEAD` alone misses both classes.

    Corrected-label discipline (from the review's Corrections layer):
    * L-09: label it a CONFIG-VALUE READ ONLY (enforcement chain not measured here).
    * L-11: label it FILE-WIDE PRESENCE; the scoped proof is L-13's
      test_run_finemap_shell_passes_the_declared_ld_matrix.
    * L-16: run WITHOUT `2>/dev/null` blindness —
      `test -d data/interim/aou_ld_exports/AFR_aou; echo dir_exists=$?` FIRST, then the
      `ls ... | wc -l`; record WHICH case the 0 means (dir-absent vs dir-empty).
    * L-03/L-04 = THE full-suite runs for this whole quick task (each suite EXACTLY once):
      `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/m3 -q`
      `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest tests/phase2 -q`
      Afterwards: `git checkout -- tests/m3/sparse_parent_benchmark.tsv` (known jitter).

    Expected deltas vs rcw — any OTHER delta is a FINDING to report, never to paper over:
    * L-01: the new HEAD (Task 1's commit).
    * L-03: passed = 902 + N where N = EXACTLY the number of tests Task 1 added
      (record the new baseline, e.g. 907 if N=5); skips EXACTLY 31; 0 failed.
    * L-04: 136 passed / EXACTLY 1 skipped / 0 failed.
    * L-11/L-12: unchanged at 1. All other expectations unchanged from the rcw TSV.
    * L-02 runs BEFORE the suites; L-02 and L-20 must both show EMPTY tracked-tree
      drift under src/tests/config/Snakefile (Task 1 is already committed).

    **Deliverable 3 — PRE-FIRE 3 index-origin settle** (append as CONTEXT blocks in the
    same evidence log; the prose write-up lands in the runbook in Task 3):
    * CONTEXT-P3a — verify the facts at YOUR HEAD (re-anchor; do not trust cited lines):
      grep -n the constant `_REGION1_REAL_WINDOW_OCCLUDED_ROW_INDICES` + its 0-based
      note in tests/m3/test_occlusion_span_filter.py; grep -n `enumerate(rows)` inside
      test_region1_real_window_known_answer_gated; run
      `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -m pytest "tests/m3/test_occlusion_span_filter.py::test_region1_real_window_known_answer_gated" -rs -q`
      and record the SKIP message verbatim (proves the gate + the held oracle).
    * CONTEXT-P3b — trace the constant's provenance in
      `.planning/amendments/m3_nan_conditioning_scientific_review.md` (READ ONLY — the
      amendments dir is untouchable): quote the index-adjacency language ("pairs are
      index-adjacent (10327/10328, 46713/46714/46715, …)"; "one variant (46714) chains
      two pairs"). State which base the source doc fixes, OR state explicitly that the
      base is UNRECOVERABLE from the doc (adjacency language is base-invariant) and
      the gated test's loud-fail property is therefore the decision instrument. Do not
      manufacture certainty the doc does not contain.
    * CONTEXT-P3c — the origin-safety argument, stated as measured: the gated test
      computes BOTH sides in the same 0-based `enumerate` space, so a mis-based oracle
      produces a LOUD FAIL (a uniformly ±1-shifted set), never a false pass; the
      §4-row-4 off-by-one risk belongs ONLY to a MANUAL line-number comparison, which
      the runbook must forbid.
    * Fold in Task 1's RED/GREEN transcripts as CONTEXT-RED / CONTEXT-GREEN blocks.
    * Close the log with the zero-perimeter proof, recorded rcw-style:
      `grep -cE '^\$ .*(gsutil|gcloud|bq |wb )' 260812-ox1-evidence.log` must print 0.

    Commit (docs only, explicit paths):
    `git add .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-evidence.log .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-evidence.tsv`
    `git commit -m "docs(quick-260812-ox1): L-01..L-20 re-anchored at post-change HEAD; PRE-FIRE 3 settle evidence; new suite baseline recorded"`
  </action>
  <verify>
    <automated>awk -F'\t' 'NR>1 {t++; if ($7=="PASS") p++; else bad++} END {exit (t==20 && p==20 && bad==0)?0:1}' .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-evidence.tsv && TS=$(grep -m1 -oE 'task_start_sha=[0-9a-f]{7,40}' .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-evidence.log | cut -d= -f2) && test -n "$TS" && test -z "$(git log --oneline "$TS"..HEAD -- .planning/quick/260811-rcw-assemble-the-m3-04c-task-3-pre-fire-gate/)" && test -z "$(git status --porcelain .planning/quick/260811-rcw-assemble-the-m3-04c-task-3-pre-fire-gate/)" && grep -cE '^\$ .*(gsutil|gcloud|bq |wb )' .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-evidence.log | grep -qx 0</automated>
  </verify>
  <done>
    20 L-rows re-measured at the post-change HEAD with 20 data rows / 20 PASS / 0
    non-PASS (or any non-expected delta surfaced loudly as a FINDING with its own
    block — a FINDING row is a STOP signal for the runbook's green claim, report it in
    the summary); new baseline (902+N / 31 / 0 and 136 / 1 / 0) recorded in L-03/L-04
    rows; L-16 run with the test-d preamble and its 0 disambiguated; CONTEXT-P3a/b/c
    blocks present with the gated test's verbatim SKIP message and the
    provenance-trace verdict; `task_start_sha=` recorded as the log's FIRST line and
    the rcw dir untouched under BOTH gates (git-log range EMPTY and porcelain EMPTY);
    zero-perimeter grep == 0; one docs commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 3: READY-TO-FIRE runbook (Carter-only actions; corrected commands only)</name>
  <files>.planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md</files>
  <action>
    Write `260812-ox1-READY-TO-FIRE.md` — ONE document, fire order, ONLY Carter's
    remaining items. **Every perimeter command is COPIED from the CORRECTED review text
    (§4 rows 1-5, the liveness-arbiter block, §5 STEP A/B, and `## Corrections
    (2026-08-12)`) — NEVER from the m3-04c PLAN or the blast radius (review §2.1(8):
    their line numbers and commands drifted).** Cross-check each quoted command
    character-for-character against the review before writing it down.

    **Never-prefix rule — PARAPHRASE ONLY:** the runbook states, in paraphrase, that
    WORKSPACE_BUCKET already carries gs:// — NEVER prefix it; a double prefix empties
    stdout and FALSE-PASSES the 0-count row — WITHOUT reproducing the double-prefix
    literal anywhere in the runbook (the verbatim-copy rule applies to RUNNABLE
    commands only; warnings are paraphrased).

    **Header (verbatim requirements):** produced by an agent; verified at <the L-01
    HEAD from Task 2>; agent-verifiable rows green as of 2026-08-12 per
    260812-ox1-evidence.tsv (note: commits after that HEAD are .planning-only — 0
    files under src/tests/config/Snakefile); the fire decision and EVERY perimeter
    command are Carter's; **AN AGENT MUST NEVER FIRE IT** (HANDOFF do_not[0]).

    **Items, in fire order:**
    1. **Push/pull gate** — origin == local at fire time (`git push` from NCSU; NOTE:
       push does NOT push tags); Workbench clone on `m3-W2-aou-deltas` +
       `git checkout -f` per the skill's fresh-clone checklist (SKILL.md); never run
       from `main`.
    2. **§4 row 1 — bucket `.npz` count**: the corrected literal-bucket command
       `gsutil ls gs://rw-migration-aou-rw-476cdac2/ld/AFR_aou/*.npz | wc -l`,
       expected **0** pre-fire; the quoted env-form alternate; the ⚠⚠ never-prefix
       warning (paraphrase, per the rule above). Anything > 0 = a prior fire banked
       regions — reconcile before re-firing (`force_fresh=False` on resume).
    3. **§4 row 2 — VM state**: read the AoU environment panel in the Workbench UI
       (do not shell out); expected present, STOPPED, disk intact; ⚠ read the
       DISK-TYPE label before any destructive env action (Reattachable PD rule).
    4. **§4 row 3 — stale panel TSV**:
       `gsutil stat gs://rw-migration-aou-rw-476cdac2/ld/AFR_aou/m3-W2-native-plink-panel.tsv`;
       if present `gsutil cat ... | head -1` — expect **9 tab-separated columns** with
       `n_dropped_occluded` at index **7**; otherwise `gsutil rm` it. Note: "0/276
       banked" does NOT evidence the TSV's absence (prior fires appended status=error
       rows unconditionally).
    5. **§4 row 5 — cohort-MT data-layer re-verify**:
       `gsutil du -s gs://rw-migration-aou-rw-476cdac2/ld/mt_AFR_qc.mt/entries/rows/parts/`
       (expect ≫ 1 GB) AND `count_cols()`/`count_rows()` off the MT; canonical path has
       NO `/mt/` subdirectory; `_SUCCESS` is NOT evidence of data.
    6. **GATE 1 cost/credit** — eyeball the live balance in the Workbench billing
       panel before a $385–1,084 commit (the CDR-pin half needs no re-verification;
       `_resolve_aux_base` makes v8→v9 a path no-op).
    7. **PRE-FIRE 1b decision template** — state that PRE-FIRE 1 LANDED (this task,
       with commit SHA), so **branch (i) is the default**. Template records: chosen
       branch, date, signature (LEFT BLANK for Carter), and the branch-(ii) re-entry
       instruction: "re-read this at STEP E; (ii) is diagnosable only post-fire — the
       per-region append is best-effort (run_native_ld_panel continues on any
       manifest exception while still writing the excludelist), so GATE 1
       (`allow_partial_manifest`) can still fire; under (ii) the stamp stays
       `stage_a_manifest`; `excludelist_degraded` + `allow_degraded: true` belongs to
       branch (iii) ONLY (review §2.1(9))." Pre-fill the branch-(i) language; leave
       signature/date to Carter.
    8. **PRE-FIRE 3 — the gated real-`.bim` validation** (from Task 2's settle):
       Carter's in-perimeter action = place the real region-1 window `.bim` at
       `data/aou/region1_window.bim` and run
       `pytest "tests/m3/test_occlusion_span_filter.py::test_region1_real_window_known_answer_gated" -rs -q`
       (in the repo clone, NOT a manual comparison — **manual line-number comparison
       is FORBIDDEN**: the ±1 off-by-one risk lives only there; the gated test
       computes both sides in the same 0-based space and fails loudly on any origin
       error). Interpretation table:
       | outcome | meaning | action |
       | PASS | origin settled (0-based); PRE-FIRE 3 CLOSED | proceed |
       | FAIL, observed set == expected shifted uniformly ±1 | the oracle's base was off by one | report; a one-line constant fix in the TEST file (not freeze-gated) + re-run; do NOT touch occlusion_span_filter.py (frozen) |
       | any other FAIL | detector/window mismatch — a real finding | STOP; do not fire; report |
    9. **STEP A — region-1 re-run gate**: PASS criteria verbatim from the review
       (`.npz` 0→1; panel `status == ok`; `n_var` slightly under 102,421;
       `n_dropped_occluded` ≈ 5; no "not symmetric", no "Killed", no dmesg OOM;
       FAIL → stop and report, do not proceed to 276) — PLUS the SH2B3 `__sub14`
       follow-up: once `m2_region_00040__sub14` is banked, run one AFR `run_finemap`
       at SH2B3_12q24 and read the `estimate_s` log line; if `ld_matrix` reads
       `identity`, the coverage gate rejected the panel — report with the observed
       `ld_overlap`/`ld_overlap_fraction`; the three remedies are scientific calls,
       not executor calls.
    10. **STEP B — the fire**: `nohup` + `timeout 312h`, server-side, on the
        STOPPED-not-deleted VM; liveness = the GCS `.npz` object listing climbing
        toward 276 (NOT the kernel light, NOT `_SUCCESS`, NOT the log); the corrected
        poll command in BOTH forms + the never-prefix warning (paraphrase, per the
        rule above); check-ins every 2-3 days; **276 IS NOT A PASS BAR** (a
        verify_failed region never uploads; a partial bank is a real, reportable
        outcome; a count that stops climbing is the signal to investigate); do NOT
        restart the kernel; teardown is UI-only.
    11. **STEP C/D/E/F/G pointers** — one line each + where the full text lives
        (review §5): C = size/plan egress (plan_ld_egress.py; confirm the REAL AoU
        egress threshold on the first request); D = per-group egress + audit log +
        SHA-256 sub-manifests (now INCLUDING the per-region occlusion manifests —
        PRE-FIRE 1 landed); E = catalog rule under the RECORDED 1b branch (re-read
        the 1b template now); F = OSF amendment-update (agent DRAFTS, Carter POSTS,
        GUID recorded); G = end-to-end read-path proof (`ld_file_declared` ==
        `ld_matrix` == the `AFR_aou/…rds` path, never identity).

    Commit (docs only, explicit paths):
    `git add .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md`
    `git commit -m "docs(quick-260812-ox1): READY-TO-FIRE runbook — Carter-only sequence, corrected commands only"`
  </action>
  <verify>
    <automated>grep -q "AN AGENT MUST NEVER FIRE" .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md && grep -qF 'gsutil ls gs://rw-migration-aou-rw-476cdac2/ld/AFR_aou/*.npz | wc -l' .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md && grep -q "276 IS NOT A PASS BAR\|276 is NOT a pass bar" .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md && ! grep -q 'gs://\$' .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-READY-TO-FIRE.md</automated>
  </verify>
  <done>
    The runbook exists with the header disclaimers (agent-produced, verified-at HEAD,
    AN AGENT MUST NEVER FIRE IT), all 11 items in fire order, every perimeter command
    byte-matched against the CORRECTED review text (spot-check items 2, 4, 5 and 10
    against the review with grep -F before committing), the PRE-FIRE 1b template
    pre-filled for branch (i) with signature/date blank, the PRE-FIRE 3 section with
    its interpretation table and the manual-comparison prohibition, and one-line
    C-G pointers. No `gs://$`-prefixed bucket-variable form (braced or unbraced)
    appears anywhere in the document — the never-prefix rule appears in PARAPHRASE
    only. One docs commit landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NC State ↔ AoU perimeter | NONE crossed this task ($0, zero perimeter contact). The code Task 1 lands will LATER run in-perimeter during the fire |
| Perimeter → bucket (future) | The per-region manifest upload crosses the egress boundary at fire time — its content class matters now |
| Runbook → Carter's shell | The runbook is an instruction surface; a wrong command there becomes a perimeter action with real cost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ox1-01 | I (info disclosure) | per-region manifest upload | mitigate | Content is coordinate/id-only (STAGE_A_COLUMNS: variant ids, positions, spans — no genotypes, no per-person counts); same egress class as the already-uploaded excludelist; test (a) asserts the uploaded bytes are exactly the Stage-A records; comment mirrors the m3-07b egress discipline |
| T-ox1-02 | T (tampering) | shared occlusion_manifest.tsv behavior | mitigate | Test (e) byte-compares the shared local manifest against an independently constructed expectation; the existing append call is left byte-unchanged |
| T-ox1-03 | D (denial: fire abort) | occlusion block | mitigate | Both manifest writes stay inside the ONE best-effort try/except — a provenance failure can never abort a region (test (b) proves absent => skipped, no error) |
| T-ox1-04 | S/R (false instruction) | READY-TO-FIRE runbook | mitigate | Commands copied ONLY from the corrected review; Task 3 verify greps the literal-bucket poll command AND asserts ANY `gs://$`-prefixed form (braced or unbraced — the double-prefix FALSE-PASS class) is ABSENT from the runbook |
| T-ox1-05 | R (repudiation of evidence) | 260812-ox1 evidence files | mitigate | rcw evidence untouched (task-start-SHA git-log range gate + porcelain gate — catches committed edits AND untracked additions); fresh files carry their own HEAD anchor + verbatim outputs; zero-perimeter grep recorded inside the log |
| T-ox1-06 | E (privilege: agent fires) | the fire itself | mitigate | Hard bound + runbook header + must_have truth all state AN AGENT MUST NEVER FIRE IT; no perimeter tool is invoked anywhere in this plan |
</threat_model>

<verification>
Overall checks after all three tasks (run by the executor, results in the summary):

1. `git log --oneline -3` — exactly 3 new commits (feat + 2 docs), explicit-path staged.
2. `git status --porcelain -- src tests config Snakefile` — EMPTY.
3. `git status --porcelain -- tests/m3/test_source_freeze_pins.py src/python/plink_ld_to_npz.py src/python/condition_ld_matrix.py src/python/occlusion_span_filter.py` — EMPTY (frozen surface untouched; the freeze-pin tests also ran green inside L-03).
4. rcw dir untouched under BOTH gates: `git log --oneline <task_start_sha>..HEAD -- .planning/quick/260811-rcw-assemble-the-m3-04c-task-3-pre-fire-gate/` prints NOTHING (task_start_sha = the value recorded as the evidence log's first line) AND `git status --porcelain .planning/quick/260811-rcw-assemble-the-m3-04c-task-3-pre-fire-gate/` prints NOTHING.
5. The new suite baseline is stated explicitly in the summary: tests/m3 = 902+N passed / 31 skipped / 0 failed (N named), tests/phase2 = 136 / 1 / 0.
6. `grep -cE '^\$ .*(gsutil|gcloud|bq |wb )' .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-evidence.log | grep -qx 0` — the evidence log records 0 real perimeter invocations as command lines (mock only in pytest).
7. DECISIONS.md unchanged (`git diff --quiet HEAD -- .planning/DECISIONS.md`) — this task recorded no decision.
</verification>

<success_criteria>
- [ ] OX1-D1: per-region occlusion-manifest upload landed TDD (RED observed with named commands, then GREEN); 5 new tests; shared-manifest + upload-set behavior otherwise byte-unchanged; glob compatibility asserted by test, not claimed
- [ ] OX1-D2: L-01..L-20 re-anchored at the post-change HEAD; 260812-ox1-evidence.log + .tsv in this quick dir (rcw schema); corrected labels honored (L-09 config-read, L-11 presence-only, L-16 disambiguated); new baseline recorded; skips exactly 31 and 1; any unexpected delta reported as a FINDING
- [ ] OX1-D3: PRE-FIRE 3 settled decision-grade — origin-safety of the gated test stated as measured, provenance trace of the constant recorded (or its unrecoverability stated), safe instrument named in the runbook, manual comparison forbidden
- [ ] OX1-D4: 260812-ox1-READY-TO-FIRE.md complete — 11 Carter-only items in fire order, corrected commands only, PRE-FIRE 1b template (branch (i) pre-filled, unsigned), never-fire disclaimer
- [ ] Zero perimeter contact, $0, nothing fired; no decision added; 3 atomic commits with explicit paths
</success_criteria>

<output>
After completion, create
`.planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-SUMMARY.md`
covering: the N new tests + new suite baseline, the RED/GREEN evidence pointers, the
L-check deltas (expected vs findings), the PRE-FIRE 3 verdict sentence, the runbook
path, and the three commit SHAs. State explicitly: what remains is Carter's alone.
</output>
