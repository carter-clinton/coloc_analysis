# Quick 260707-w78 — SUMMARY

**Status:** Done (docs-only). **Commit:** `_this-commit_`. **Branch:** `m3-W2-aou-deltas`.

## What landed

`.planning/amendments/m3_region1_occlusion_hinge_check.md` — the durable, reproducible record of
Seth's exclude-vs-flag "hinge fact" check for the region-1 overlapping-deletion occlusion.

## Finding (decisive)

- **Join key is `(CHR,POS)`-only** (`snp_id_bridge.R:110-121`); panel is GRCh38, sumstats GRCh37
  (`ld_npz_to_rds.R` liftover). Lifted the committed 3-record tangle
  GRCh38 chr1:5922716/5922718/5922724 → GRCh37 chr1:5982776/5982778/5982784.
- **Occluded SNP `rs182965575` (GRCh37 chr1:5982778, G/A, MAF ~0.014) is PRESENT in 7 of 9 AFR
  harmonized sumstats files** (asthma, t2d, bmi, hdl, ldl, tc, tg; absent only in the two stroke
  files) with real β/SE. The occluding deletion anchor (5982776) and second occluded SNP (5982784)
  are absent from all AFR sumstats.
- ⟹ **Seth's "present" case.** `NaN→0` definitively wrong (would fabricate `r=0` vs the common
  neighbors rs11120783/rs10864245); plain panel-only exclude is unsafe (asymmetry).
- **Recommended policy: exclude-in-lockstep across panel AND sumstats + provenance manifest**
  (Flag and Exclude converge on the same fit since the LD row is undefined; Flag adds machinery).
- Informs the other two calls: **scope = upstream span-filter for all 276** (systemic dbSNP variant
  + `.bim` geometry) + lockstep sumstats-side drop at m3-04; **amendment = exclusion + provenance,
  never zeroing**.

## Limitations recorded

Partial (only the committed tangle; the 5 direct pairs are in the still-blocked
`m3_region1_nan_geometry_verdict.md`, anchor `4543dcf4…`); position-only match (allele orientation
in the blocked verdict). One present occluded variant is sufficient to mandate lockstep handling.

## Guardrails honored

m3-06 HELD; `condition_ld_matrix.py` FROZEN; raw-panel NaN-raise intact; OSF amendment / coverage
flag / tag untouched; no loop contact. No source code changed.

## Still open (not this task)

Full occluded list (base64 verdict transfer); Carter's 3 policy calls; AoU loop-state resolution
(in-perimeter). Plan: `260707-w78-PLAN.md`.
