# Quick 260707-w78 — Record region-1 occlusion hinge check

**Type:** docs-only (no code, no perimeter access, no spend). Investigation completed this
session; this task durably records the result beside the m3-06 scientific-review memo.

## Goal

Persist the exclude-vs-flag "hinge fact" check Seth's review named — whether region-1's
occluded variants survive into the harmonized AFR sumstats — as an auditable in-repo diagnostic,
so the panel overlapping-variant policy decision rests on a recorded, reproducible finding.

## Task

1. Author `.planning/amendments/m3_region1_occlusion_hinge_check.md` — method (join key
   `(CHR,POS)`-only; GRCh38→GRCh37 liftover of the committed 3-record tangle; scan of all AFR
   harmonized sumstats), verbatim evidence rows, result (occluded SNP `rs182965575` @ GRCh37
   chr1:5982778 present in 7/9 AFR files → Seth's "present" case), recommendation
   (exclude-in-lockstep + provenance, not panel-only, not flag, never NaN→0), scope/amendment
   implications, limitations (partial — 5 direct pairs in the blocked verdict; position-only),
   cross-references, reproduction commands.
2. Update STATE.md: Quick Tasks Completed row + Last activity.

## Guardrails (do-NOTs)

- Keep m3-06 HELD; do NOT touch `condition_ld_matrix.py` (FROZEN) or the raw-panel NaN-raise.
- Do NOT re-fire / contact the AoU loop; do NOT alter the OSF amendment, coverage flag, or tag.
- Byte-faithful numbers: the recorded positions/rsIDs/trait counts are exactly what was observed.

## must_haves

- `m3_region1_occlusion_hinge_check.md` exists in `.planning/amendments/` with the evidence table,
  the "present case" result, and the exclude-in-lockstep recommendation.
- STATE.md carries a `260707-w78` Quick Tasks Completed row.
- No source code, OSF artifact, or loop state changed.
