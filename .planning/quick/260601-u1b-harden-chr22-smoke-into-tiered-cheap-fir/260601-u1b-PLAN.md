---
phase: quick-260601-u1b
plan: 1
slug: harden-chr22-smoke-into-tiered-cheap-first
type: execute
wave: 1
status: planned
authored: 2026-06-01
mode: quick-full
autonomous: true
depends_on: []
predecessor: 260601-cca (env-derive AUX base + suffix-discovery — DO NOT break)
parent_design: APPROVED brainstorm (this session) — tiered cheap-first chr22 validation
decision_anchor: >
  m3-W1 empty-MT catastrophe ($2,140 / 0x0 MTs) is a Hail checkpoint
  write-finalization failure under spark.executor.cores=1/mem=5g. Partition count
  is FIXED at naive_coalesce(2048)+repartition(2048) (aou_ld_panel.py:944,951)
  regardless of interval_filter, so a sub-chromosomal nano-interval keeps the
  catastrophe-triggering 2048-partition profile FAITHFUL while making per-partition
  data trivial. Tiers form a memory-pressure gradient: cheap FAIL rules catastrophe
  IN; cheap PASS does NOT exonerate (only chr22/full reaches genome-scale pressure).
constraints_honored:
  - "TDD mandatory for CODE items (A,B): explicit RED (failing test, watch it fail) before GREEN"
  - "Do NOT weaken any Track-4 guard — _assert_checkpoint_nonempty count_rows>0/count_cols>0 stays HARD"
  - "pytest via /rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/pytest (Python 3.11), NEVER miniconda3 base"
  - "explicit git paths only on GPFS — never git add -A / git add ."
  - "repo artifacts only — launches NOTHING, spends NOTHING; Carter holds every AoU/$ trigger"
  - "rigor-over-speed: cheap-tier pass labeled 'no cheap failure mode reproduced', NEVER 'validated'"
tech_stack:
  patterns:
    - "TDD RED→GREEN: write failing test in tests/m3/, run smoke_dev pytest, confirm fail, then implement"
    - "Pure-Python helpers in src/python/aou_ld_panel.py with `from aou_ld_panel import X` test imports"
    - "Dependency injection for testability: lister/copier/subprocess as keyword params with prod defaults (mirrors existing _hail_hadoop_lister + lister=lambda d: entries test pattern)"
    - "file:// scheme dispatch in helpers for local-FS tests without a Hail dependency (mirrors _has_checkpoint / _validate_checkpoint_populated)"
    - "Notebook = JSON; edit via Python json round-trip or jupyter, preserving cell structure"
    - "INTERVAL→suffix derivation: 'chr22'→'_chr22'; 'chr22:16000000-18000000'→'_chr22_16000000_18000000'"
files_modified:
  - src/python/aou_ld_panel.py  # Task 1: du-floor parameterization helper (A) + _capture_catastrophe_forensics (B)
  - tests/m3/test_aou_ld_panel_local.py  # Task 1: RED-first tests for (A)+(B)
  - .planning/notebooks/AOU-0.5-mechanism-probe_template.ipynb  # Task 2: NEW Tier 0 synthetic-MT probe (C)
  - .planning/notebooks/AOU-1-chr22-smoke_template.ipynb  # Task 2: parameterize INTERVAL + apply du-floor change (D)
  - .planning/quick/260601-u1b-harden-chr22-smoke-into-tiered-cheap-fir/TIERED-VALIDATION-RUNBOOK.md  # Task 3: tier-gated runbook (E)

requirements: [QUICK-260601-U1B]

must_haves:
  truths:
    - "A ~2 Mb nano-interval MT no longer false-positives the du byte-floor (the count_rows>0/count_cols>0 hard guard remains the real gate)"
    - "An interval-agnostic du-floor helper exists in aou_ld_panel.py with passing tests, parameterized per tier (explicit floor arg and/or interval-span scaling)"
    - "_capture_catastrophe_forensics(uri, *, phase) exists, is best-effort (never raises), and captures _SUCCESS-mtime-vs-part-mtimes + MT listing + hail.log copy + Spark-REST snapshot + a _forensics/<phase>_capture.json"
    - "The existing _assert_checkpoint_nonempty hard-fail/raise semantics are UNCHANGED (no guard weakened)"
    - "A new Tier 0 mechanism-probe notebook exists that exercises the 2048-partition cores=1/5g write path with a synthetic range_matrix_table, ZERO source read, ZERO QC"
    - "AOU-1-chr22-smoke is parameterized by a single INTERVAL variable (default 'chr22'; settable to a nano sub-interval) threaded into all three load_qc_cohort calls, the du-floor cells, and output suffixes — and ALL Track-4 cells are retained"
    - "A tier-gated runbook documents Gate A (Tier 0 probe), Gate B (Tier 1 nano), Gate C (Tier 2 chr22) with cluster specs, $ envelopes, decision rules, and the rigor caveat that cheap-tier pass != validated"
    - "All ~94 prior tests still pass under smoke_dev pytest (no regression)"
  artifacts:
    - path: "src/python/aou_ld_panel.py"
      provides: "interval-scaled/parameterized du-floor helper + _capture_catastrophe_forensics(uri, *, phase)"
      contains: "_capture_catastrophe_forensics"
    - path: "tests/m3/test_aou_ld_panel_local.py"
      provides: "RED-first tests for du-floor helper (nano-interval no-false-positive + explicit floor) and forensics helper (mtime distinguisher, defensive never-raise, json emission)"
      contains: "_capture_catastrophe_forensics"
    - path: ".planning/notebooks/AOU-0.5-mechanism-probe_template.ipynb"
      provides: "Tier 0 synthetic-MT probe: cores=1/5g lever + range_matrix_table(50000,2000).repartition(2048).checkpoint + _assert_checkpoint_nonempty(phase='probe') + few-MB du-floor + forensic-capture-on-fail"
      min_lines: 1
    - path: ".planning/notebooks/AOU-1-chr22-smoke_template.ipynb"
      provides: "single INTERVAL variable threaded into 3 load_qc_cohort calls + du-floor cells + output suffixes; all Track-4 cells retained; serves Tier 1 (nano) AND Tier 2 (chr22)"
      contains: "INTERVAL"
    - path: ".planning/quick/260601-u1b-harden-chr22-smoke-into-tiered-cheap-fir/TIERED-VALIDATION-RUNBOOK.md"
      provides: "Gate A/B/C decision tree, cluster specs, $ envelopes, watchpoints, rigor framing"
      contains: "Gate A"
  key_links:
    - from: "AOU-0.5-mechanism-probe_template.ipynb probe cell"
      to: "aou_ld_panel._assert_checkpoint_nonempty + _capture_catastrophe_forensics"
      via: "imported helper calls on the synthetic-MT checkpoint"
      pattern: "_assert_checkpoint_nonempty|_capture_catastrophe_forensics"
    - from: "AOU-1-chr22-smoke INTERVAL variable"
      to: "load_qc_cohort(interval_filter=INTERVAL) + du-floor cell URIs + output suffixes"
      via: "single notebook variable threaded through all three cohort fires + validation cells"
      pattern: "interval_filter=INTERVAL"
    - from: "du-floor cells (Cell 3.5/4.5/5.5)"
      to: "interval-agnostic count>0 hard guard (already in _assert_checkpoint_nonempty inside load_qc_cohort)"
      via: "du byte-floor demoted to soft-warn / parameterized floor; count>0 stays the HARD gate"
      pattern: "soft|warn|floor"
    - from: "TIERED-VALIDATION-RUNBOOK.md Gate A→B→C"
      to: "AOU-0.5 probe → AOU-1 (nano) → AOU-1 (chr22) → AOU-2-AOU-4-TRACK-4-PATTERN.md full-genome"
      via: "decision gates escalate cheap→expensive; 1000G AFR is the documented FAIL safety net"
      pattern: "Gate [ABC]|1000G"
---

<objective>
Harden the AoU chr22 smoke into a tiered, cheap-first validation sequence
(Tier 0 synthetic probe → Tier 1 nano-interval → Tier 2 chr22) plus forensic
capture, so the m3-W1 empty-MT catastrophe ($2,140 burn, 0x0 schema-only MTs)
fails as cheaply and diagnostically as possible BEFORE Carter greenlights the
expensive full-genome rebuild.

Purpose: The catastrophe is a Hail checkpoint write-finalization failure under
spark.executor.cores=1/mem=5g — the driver writes _SUCCESS on driver-side task
accounting without validating executor row-group payloads. KEY VERIFIED FACT:
partition count is FIXED at naive_coalesce(2048)+repartition(2048)
(aou_ld_panel.py:944,951) regardless of interval_filter, so a sub-chromosomal
nano-interval keeps the exact catastrophe-triggering 2048-partition profile
(FAITHFUL) while making per-partition data trivial (plausibly runs on a far
smaller / cheaper cluster). Track-4 defensive assertions are load-bearing and
STAY — we make platform validation cheaper-to-first-failure and more
diagnostic, NOT weaker.

RIGOR FRAMING (load-bearing; must appear in notebook + runbook prose): the tiers
form a memory-pressure gradient. A cheap tier FAILING definitively rules the
catastrophe IN (cheap win). A cheap tier PASSING does NOT fully exonerate —
only the chr22/full fire reaches genome-scale memory pressure. Never label a
cheap-tier pass "validated"; label it "no cheap failure mode reproduced,
escalating to the real test."

Output: This task produces REPO ARTIFACTS ONLY (code + tests + 2 notebooks +
runbook). It launches NOTHING on AoU and spends NOTHING. Carter holds every
launch / compute / $ trigger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<key_facts>
Verified from source this planning session (do NOT re-derive — execute against these):

1. **Track-4 hard guard (DO NOT WEAKEN):** `_assert_checkpoint_nonempty(mt, uri, *, phase)`
   at aou_ld_panel.py:706 raises RuntimeError if `count_rows()==0 or count_cols()==0`.
   It is called at every checkpoint site inside `load_qc_cohort` (lines 957, 984, 1005).
   This is the interval-agnostic HARD gate. It STAYS hard and raising.

2. **du-floor lives in the NOTEBOOK, not the code.** The hardcoded
   `_MIN_BYTES = 50000000` (50 MB) is in AOU-1 Cells 3.5/4.5/5.5 (notebook cells
   4, 6, 8). Each cell does `subprocess.run(['gsutil','du','-s', _entries_dir])`
   then `assert _size_bytes > _MIN_BYTES`. THIS is what false-positives on a
   ~2 Mb nano-interval. There is NO du-floor in aou_ld_panel.py today.

3. **`_qc_checkpoint_uri` does NOT honor interval_filter** (interval suffix is
   intermediate-only, in `_intermediate_checkpoint_uri`). AOU-1 manually suffixes
   the FINAL MT URI: `_qc_checkpoint_uri(bucket,'afr',False) + "_chr22"`. So
   parameterizing INTERVAL means deriving the suffix from INTERVAL
   ('chr22'→'_chr22'; 'chr22:16000000-18000000'→'_chr22_16000000_18000000').

4. **Module already imports** `json`, `os`, `sys`, `subprocess` (subprocess
   imported locally at line 399), and has `_hail_hadoop_lister(dirpath)` (line 141,
   uses `hl.hadoop_ls`). Test pattern for injection: pass `lister=lambda d: entries`
   (see tests lines 360-440). file:// scheme dispatch pattern: see `_has_checkpoint`
   (580) and `_validate_checkpoint_populated` (628).

5. **MIN_ENTRIES_FILE_BYTES = 1024** (line 625) is the entries-part stub
   discriminator used by `_validate_checkpoint_populated`. Distinct from the
   notebook du-floor; do not conflate.

6. **Test invocation:** `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/pytest`
   (Python 3.11). ~94 tests pass currently. Tests live in tests/m3/, import via
   `from aou_ld_panel import X`. NEVER use miniconda3 base (Python 3.13).

7. **Spark lever (AOU-1 Cell 1a, must be byte-identical in AOU-0.5 Cell 1a):**
   `PYSPARK_SUBMIT_ARGS="--conf spark.executor.cores=1 ... pyspark-shell"` set
   BEFORE `import hail`. Cell 1b: `hl.init(...)` + `assert spark.executor.cores=='1'`.

8. **Reference docs:** .planning/debug/m3-W1-empty-mt-catastrophe.md (catastrophe
   forensics), .planning/notebooks/AOU-2-AOU-4-TRACK-4-PATTERN.md (full-genome
   Wave 2 target), AOU-1-chr22-smoke_template.ipynb (the parameterization base).
</key_facts>

<conftest_note>
tests/m3/ uses `from aou_ld_panel import X` (not `from src.python...`). A conftest
or sys.path shim already makes this work for the ~94 existing tests — follow the
exact import style already in test_aou_ld_panel_local.py; do not introduce a new
import convention.
</conftest_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: (A) parameterize the du-floor + (B) _capture_catastrophe_forensics — TDD, code+tests</name>
  <files>src/python/aou_ld_panel.py, tests/m3/test_aou_ld_panel_local.py</files>

  <behavior>
  TWO helpers, BOTH RED-first (write failing test, run smoke_dev pytest, watch it
  FAIL, then implement to GREEN). Do NOT weaken `_assert_checkpoint_nonempty`.

  --- (A) du-floor parameterization helper ---
  The 50 MB notebook floor false-positives on a ~2 Mb nano-interval. Make the
  count_rows>0/count_cols>0 guard (already the HARD guard in load_qc_cohort) the
  authoritative gate, and demote the du byte-floor to a parameterized soft check.
  Add a pure helper to aou_ld_panel.py — choose ONE clean shape (planner leaves
  the exact signature to your judgment, but it MUST be pure + unit-testable):
    EITHER  `_interval_scaled_du_floor(interval_filter: str | None, *, base_floor_bytes: int) -> int`
            returning a floor that scales with interval span (full-genome / chrN →
            base_floor_bytes; a span-bounded interval like 'chr22:16000000-18000000'
            → a proportionally small floor, e.g. base * span_bp / chr_len_bp, floored
            at a few MB), OR
    `_du_floor_for_tier(tier_floor_bytes: int) -> int` accepting an explicit
            per-tier floor (notebook passes the tier's floor in).
  Tests (RED first):
    - Test 1: a nano-interval ('chr22:16000000-18000000', ~2 Mb) yields a floor
      well BELOW the catastrophe footer-stub size (~70 KiB) is NOT required, but
      the floor MUST be < a realistic ~2 Mb-cohort entries size AND must NOT be
      the unscaled 50 MB (i.e. no false-positive). Assert floor < 50_000_000 and
      floor >= a documented minimum (a few MB) for the nano span.
    - Test 2: interval_filter=None or a whole-chromosome 'chr22' yields the full
      base floor (no down-scaling that would weaken the chr22-tier check).
    - Test 3 (if you choose the explicit-floor shape): the helper returns exactly
      the floor it is handed (pure pass-through / no surprise scaling), so the
      notebook stays the single source of per-tier policy.
  RATIONALE the tests must encode in comments: the du-floor is a DIAGNOSTIC
  soft-signal scaled to the interval; the count>0 assertion inside
  load_qc_cohort is the real catastrophe gate and is untouched.

  --- (B) _capture_catastrophe_forensics(uri, *, phase) ---
  New helper in aou_ld_panel.py, callable at ANY Track-4 halt. MUST be defensive:
  it NEVER raises (best-effort; wrap every sub-step in try/except; on total failure
  return a dict/None sentinel, never propagate). Captures the hypothesis-distinguisher
  data described in the approved design:
    (a) _SUCCESS mtime vs entries/entries/parts/ part mtimes (resolves
        Hail-finalize-on-empty-contents vs kill-interrupted-write — see
        feedback_w1_catastrophe_hypothesis_distinguisher);
    (b) listing of the MT dir + entries part sizes;
    (c) copy /tmp/hail.log → gs://<bucket>/ld/_forensics/<phase>_hail.log;
    (d) Spark REST active-stages snapshot if reachable (best-effort HTTP/JSON);
    (e) write a _forensics/<phase>_capture.json with the gathered facts.
  WIRING DECISION (you decide, pick the LOWER-RISK option and document it in the
  docstring + a test): the safest wiring is a STANDALONE helper invoked by notebook
  cells on AssertionError (NOT injected into _assert_checkpoint_nonempty), so the
  existing hard-fail/raise semantics of the Track-4 guard are byte-for-byte
  unchanged. Default to standalone unless you can prove injecting it preserves the
  raise contract exactly.
  Testability: inject the side-effecting collaborators as keyword params with
  production defaults — e.g. `lister=_hail_hadoop_lister`,
  `copier=<hl.hadoop_copy wrapper>`, `http_getter=<requests/urllib wrapper>`,
  `now=...`, `writer=...` — mirroring the existing `lister=lambda d: entries`
  test idiom. file:// dispatch is fine for the json-write path so tests run with
  no Hail / no network.
  Tests (RED first):
    - Test 1: given a mocked listing where _SUCCESS mtime > all part mtimes (the
      Hail-finalize-on-empty signature) the capture json records that ordering /
      a hypothesis flag.
    - Test 2: given a mocked listing where some part mtimes > _SUCCESS mtime
      (kill-interrupted-write signature) the capture records the opposite flag.
    - Test 3 (defensive): when the injected lister/copier/http_getter RAISES, the
      helper does NOT propagate — it returns its sentinel and still writes whatever
      partial json it could (best-effort). This is the load-bearing "never raise
      from within capture" guarantee.
    - Test 4: the helper writes a parseable _forensics/<phase>_capture.json
      (assert file exists + json.loads round-trips + contains phase + uri).
  </behavior>

  <action>
  RED→GREEN per helper. For EACH of (A) and (B):
  1. RED: add the failing tests above to tests/m3/test_aou_ld_panel_local.py
     (import via `from aou_ld_panel import ...`). Run smoke_dev pytest scoped to
     the new tests and CONFIRM they fail with ImportError/AssertionError. CAPTURE
     the failing pytest output AT THE MOMENT OF THE RED RUN (before writing ANY
     implementation) by appending it to
     `.planning/quick/260601-u1b-harden-chr22-smoke-into-tiered-cheap-fir/red_fail_output.txt`
     — do this for BOTH helper (A) and helper (B), labeling each block (e.g.
     `=== RED (A) du-floor ===` / `=== RED (B) forensics ===`). Example:
     `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/pytest tests/m3/test_aou_ld_panel_local.py -k <new_test_selector> -x -q 2>&1 | tee -a .planning/quick/260601-u1b-harden-chr22-smoke-into-tiered-cheap-fir/red_fail_output.txt`.
     This watch-it-fail evidence MUST be captured during the failing run, NOT
     reconstructed afterward; the final SUMMARY quotes/incorporates this file.
     This is the mandated watch-it-fail step.
  2. GREEN: implement the helper in src/python/aou_ld_panel.py (pure helper for A;
     defensive injected-collaborator helper for B). Re-run scoped pytest → GREEN.
  3. Do NOT touch `_assert_checkpoint_nonempty` body (lines 706-753) — verify by
     diff that those lines are byte-identical post-task.
  Honor CLAUDE.md: stage with explicit paths only (git add src/python/aou_ld_panel.py
  tests/m3/test_aou_ld_panel_local.py) — never `git add -A`/`.`. Do not disturb the
  260601-cca env-derive / suffix-discovery code (`_resolve_aux_base`,
  `_resolve_aux_file`, `_intermediate_checkpoint_uri`).
  </action>

  <verify>
    <automated>/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/pytest tests/m3/test_aou_ld_panel_local.py -x -q</automated>
  </verify>

  <done>
  Both helpers exist in aou_ld_panel.py with docstrings stating the rigor framing
  (du-floor = diagnostic soft-signal; count>0 = real gate) and the forensics
  never-raise guarantee. New RED tests were observed failing pre-implementation;
  red_fail_output.txt exists and contains the pre-implementation failing pytest
  output for both (A) and (B), and the SUMMARY quotes/incorporates it. Tests now
  pass. ALL prior tests still pass (full test_aou_ld_panel_local.py green under
  smoke_dev pytest; ~94+new). A diff confirms `_assert_checkpoint_nonempty` body
  is unchanged. Staged with explicit paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: (C) NEW AOU-0.5 mechanism-probe notebook + (D) parameterize AOU-1 by INTERVAL</name>
  <files>.planning/notebooks/AOU-0.5-mechanism-probe_template.ipynb, .planning/notebooks/AOU-1-chr22-smoke_template.ipynb</files>

  <action>
  Two notebook artifacts. Edit notebooks as JSON via a Python json round-trip (or
  jupyter), preserving valid .ipynb structure (nbformat 4). Notebooks launch
  nothing here — they are templates Carter fires later.

  --- (C) NEW .planning/notebooks/AOU-0.5-mechanism-probe_template.ipynb (Tier 0) ---
  Cell 0 (markdown) — purpose: isolate the 2048-partition executor write/finalize
    path under cores=1/5g with ZERO source read + ZERO QC. State the HONEST CAVEAT
    verbatim in spirit: a PASS rules out ONLY the pure-write-path failure mode, NOT
    memory-pressure truncation during QC; never label this "validated". Cost ~$1-3;
    cluster = 4× n1-highmem-16 = 64 vCPU (shared with Tier 1); NOT part of the
    compute-free AOU-0 precheck; Carter holds the trigger.
  Cell 1a (code) — PYSPARK_SUBMIT_ARGS lever IDENTICAL to AOU-1 Cell 1a
    (spark.executor.cores=1, executor mem=5g, driver.cores=1; set in os.environ
    BEFORE `import hail`). Copy AOU-1 Cell 1a content faithfully (read it from the
    AOU-1 notebook json; do not paraphrase the conf string).
  Cell 1b (code) — `hl.init(...)` with idempotent direct/quiet/GRCh38, log=/tmp/hail.log,
    matching AOU-1 Cell 1b; then `assert spark.conf.get('spark.executor.cores')=='1'`.
  Probe cell (code) —
    ```
    import os
    from aou_ld_panel import _assert_checkpoint_nonempty, _capture_catastrophe_forensics
    mt = hl.utils.range_matrix_table(n_rows=50_000, n_cols=2_000).repartition(2048)
    mt = mt.annotate_entries(x=hl.rand_norm())
    _uri = f"{os.environ['WORKSPACE_BUCKET']}/ld/_probe_synthetic.mt"
    mt = mt.checkpoint(_uri, overwrite=True)
    try:
        _assert_checkpoint_nonempty(mt, _uri, phase='probe')
        # few-MB du-floor (NOT 50 MB) on entries/entries/parts/ as a soft signal
        import subprocess
        _entries = _uri.rstrip('/') + '/entries/entries/parts/'
        _r = subprocess.run(['gsutil','du','-s', _entries], capture_output=True, text=True)
        # soft-floor a few MB; print, do not hard-fail below floor — the count>0 above is the gate
        ...
    except Exception:
        _capture_catastrophe_forensics(_uri, phase='probe')
        raise
    ```
    (Implement that sketch into a real cell — the few-MB floor MUST NOT be 50 MB;
    use a few-MB constant or the Task-1 helper. Forensic-capture-on-fail wraps the
    assertion + du check; re-raise after capture so the cell still halts loudly.)
  Add a closing markdown cell mirroring AOU-1's output-summary cell, stating the
  Gate-A decision rule (PASS→Tier 1 same cluster; FAIL→catastrophe ruled IN→pivot
  to 1000G AFR) and the caveat that PASS is "no cheap failure mode reproduced".

  --- (D) parameterize .planning/notebooks/AOU-1-chr22-smoke_template.ipynb ---
  Introduce ONE notebook variable near the top (new code cell after Cell 1b, or
  extend an existing setup cell): `INTERVAL = "chr22"  # set to "chr22:16000000-18000000" for the Tier 1 nano fire (~2 Mb, gene-dense, strict subset of chr22)`.
  Then thread INTERVAL through:
    - all THREE load_qc_cohort(...) calls: `interval_filter=INTERVAL` (Cells 3, 4, 5
      = notebook cells 3, 5, 7 — currently hardcode `interval_filter="chr22"`).
    - the THREE du-floor cells (Cells 3.5/4.5/5.5 = notebook cells 4, 6, 8): derive
      the output suffix from INTERVAL and use the Task-1 parameterized floor
      instead of hardcoded `_MIN_BYTES = 50000000`. Add a small inline helper or use
      the Task-1 helper to compute the suffix:
      `_suffix = "_" + INTERVAL.replace(":", "_").replace("-", "_")` →
      'chr22'→'_chr22'; 'chr22:16000000-18000000'→'_chr22_16000000_18000000'.
      Replace the manual `+ "_chr22"` with `+ _suffix` so nano outputs DO NOT
      collide with chr22 outputs.
    - the output/summary markdown cell + Cell 7 cohort-summary references: make them
      INTERVAL-aware (f-string the suffix) instead of literal 'chr22'. Specifically,
      Cell 7's `"cohort": ["AFR_pca_chr22", ...]` and `"interval_filter": ["chr22"] * 3`
      MUST be derived from INTERVAL/_suffix (e.g. cohort labels built with `_suffix`,
      interval_filter column set to `[INTERVAL] * 3`) — no bare `"chr22"` literal may
      remain at any call site or summary cell; the ONLY allowed standalone `"chr22"`
      literal in the whole notebook is the `INTERVAL = "chr22"` default assignment.
  Wrap each du-floor cell's assertion in the forensic-capture-on-fail pattern
  (call `_capture_catastrophe_forensics(_ckpt_uri, phase=<afr|afr_sens|eur>)` on
  AssertionError, then re-raise) — additive, does not change the existing HALT
  behavior.
  KEEP ALL Track-4 cells (Cells 3.5/4.5/5.5/6/7 and the count_cols/count_rows
  prints). Do NOT remove any guard. Do NOT delete the du-floor cells — parameterize
  them (the Task-1 du-floor helper MUST be wired into AOU-1; deleting a floor cell
  is a guard-weakening regression, NOT an acceptable way to remove the 50 MB
  literal). Update Cell 0 markdown prose to say the notebook now serves BOTH Tier 1
  (nano) and Tier 2 (chr22) and to carry the rigor caveat (cheap pass != validated).

  Verify both notebooks are valid JSON / parseable nbformat after editing. Stage
  with explicit paths only.
  </action>

  <verify>
    <automated>/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/python -c "import json; nb1=json.load(open('.planning/notebooks/AOU-0.5-mechanism-probe_template.ipynb')); nb2=json.load(open('.planning/notebooks/AOU-1-chr22-smoke_template.ipynb')); s2=''.join(''.join(c['source']) for c in nb2['cells']); s1=''.join(''.join(c['source']) for c in nb1['cells']); assert 'INTERVAL' in s2, 'AOU-1 missing INTERVAL var'; assert s2.count('interval_filter=INTERVAL')==3, 'expected 3 interval_filter=INTERVAL calls, got %d' % s2.count('interval_filter=INTERVAL'); assert '50000000' not in s2 and '50_000_000' not in s2, 'AOU-1 still has hardcoded 50MB floor'; assert ('_interval_scaled_du_floor' in s2) or ('_du_floor_for_tier' in s2), 'Task-1 du-floor helper not wired into AOU-1 (floor cell deleted instead of parameterized?)'; assert s2.count('\"chr22\"') <= 2, 'Cell 7 / call sites still have hardcoded \"chr22\" literals beyond the INTERVAL default + Cell-0 backtick prose'; assert 'range_matrix_table' in s1 and '2048' in s1 and '_capture_catastrophe_forensics' in s1, 'AOU-0.5 probe incomplete'; assert 'executor.cores=1' in s1, 'AOU-0.5 missing cores=1 lever'; print('NOTEBOOKS OK')"</automated>
  </verify>

  <done>
  AOU-0.5-mechanism-probe_template.ipynb exists: valid nbformat, has the cores=1/5g
  lever, a range_matrix_table(50000,2000).repartition(2048).annotate_entries.checkpoint
  probe, _assert_checkpoint_nonempty(phase='probe'), a few-MB (NOT 50 MB) du-floor,
  forensic-capture-on-fail, and the honest caveat + Gate-A rule in markdown. AOU-1
  has a single INTERVAL variable threaded into exactly 3 load_qc_cohort
  interval_filter args + all 3 du-floor cells (Task-1 parameterized floor helper
  wired in — floor cells RETAINED not deleted, INTERVAL-derived suffix, no hardcoded
  50 MB) + an INTERVAL-aware Cell 7 / summary prose with no bare "chr22" literal
  beyond the INTERVAL default; all Track-4 cells retained; valid nbformat. Both
  staged with explicit paths.
  </done>
</task>

<task type="auto">
  <name>Task 3: (E) tier-gated validation runbook</name>
  <files>.planning/quick/260601-u1b-harden-chr22-smoke-into-tiered-cheap-fir/TIERED-VALIDATION-RUNBOOK.md</files>

  <action>
  Author the tier-gated runbook as a new markdown doc in this quick dir. It must
  encode the decision tree, cluster specs, $ envelopes, and watchpoints from the
  approved design, and carry the rigor framing in prose. Sections:

  1. **Purpose + rigor framing** — tiers = memory-pressure gradient; cheap FAIL
     rules catastrophe IN (cheap win); cheap PASS does NOT exonerate (only chr22/full
     reaches genome-scale memory pressure); never label a cheap-tier pass "validated"
     → label "no cheap failure mode reproduced, escalating to the real test".
     State that all guards (Track-4 count>0 + du soft-floor + forensics) are intact.

  2. **Gate A — Tier 0 mechanism probe (AOU-0.5).** Cluster: 4× n1-highmem-16 =
     64 vCPU (shared with Tier 1). Cost ~$1-3. NON-preemptible workers (spot muddies
     the kill-interrupted-write hypothesis). Run AOU-0.5. PASS → proceed to Tier 1
     on the SAME cluster. FAIL → catastrophe ruled IN at the pure-write path → pivot
     Wave 2 to 1000G AFR (≈$1-3 spent). Note: NOT part of compute-free AOU-0.

  3. **Gate B — Tier 1 nano (AOU-1 with INTERVAL='chr22:16000000-18000000').**
     Same 64-vCPU cluster. Faithful 2048-partition / cores=1,5g profile, trivial
     per-partition data. PASS → tear down cluster + Carter sign-off to spin the
     Tier-2 chr22 cluster. FAIL-empty (count>0 raise / sub-floor) → catastrophe IN →
     1000G AFR. FAIL-wedge (stall) → escalate cluster sizing (a data point on the
     real sizing requirement) per feedback_aou_cluster_sizing_for_ld_panel.

  4. **Gate C — Tier 2 chr22 (AOU-1 with INTERVAL='chr22').** Cluster: 24×
     n1-highmem-16 = 384 vCPU. Cost ~$35-80, ~2h. NON-preemptible. PASS →
     full-genome rebuild greenlit (Wave 2, ref AOU-2-AOU-4-TRACK-4-PATTERN.md).
     FAIL → 1000G AFR safety net (documented fallback only — do not propose
     proactively; see feedback_no_1000g_ld_pivot).

  5. **Watchpoints** — websocket-drop orphan-kernel billing
     (feedback_aou_websocket_drop_zombie_pattern); delete each Dataproc cluster the
     MOMENT its tier finishes; preserve /tmp/hail.log on any stall (the
     _capture_catastrophe_forensics helper copies it to gs://<bucket>/ld/_forensics/);
     NON-preemptible workers for ALL validation tiers (reserve spot for the eventual
     production full-fire only); Carter holds every launch/$ trigger.

  6. **Forensics + hypothesis distinguisher** — point to
     _capture_catastrophe_forensics and the _SUCCESS-mtime-vs-part-mtimes test that
     resolves Hail-finalize-on-empty vs kill-interrupted-write
     (feedback_w1_catastrophe_hypothesis_distinguisher); note Track-4 patches defend
     against BOTH so this affects re-fire STRATEGY, not patch strategy.

  Stage with explicit path only.
  </action>

  <verify>
    <automated>test -f .planning/quick/260601-u1b-harden-chr22-smoke-into-tiered-cheap-fir/TIERED-VALIDATION-RUNBOOK.md && grep -q "Gate A" .planning/quick/260601-u1b-harden-chr22-smoke-into-tiered-cheap-fir/TIERED-VALIDATION-RUNBOOK.md && grep -q "Gate B" .planning/quick/260601-u1b-harden-chr22-smoke-into-tiered-cheap-fir/TIERED-VALIDATION-RUNBOOK.md && grep -q "Gate C" .planning/quick/260601-u1b-harden-chr22-smoke-into-tiered-cheap-fir/TIERED-VALIDATION-RUNBOOK.md && grep -qi "1000G" .planning/quick/260601-u1b-harden-chr22-smoke-into-tiered-cheap-fir/TIERED-VALIDATION-RUNBOOK.md && grep -qi "non-preemptible" .planning/quick/260601-u1b-harden-chr22-smoke-into-tiered-cheap-fir/TIERED-VALIDATION-RUNBOOK.md && echo "RUNBOOK OK"</automated>
  </verify>

  <done>
  TIERED-VALIDATION-RUNBOOK.md exists with all three gates (A/B/C), cluster specs
  (64 vCPU / 64 vCPU / 384 vCPU), $ envelopes ($1-3 / $1-3 / $35-80), the rigor
  framing (cheap pass != validated), watchpoints (orphan-kernel billing, delete
  cluster on tier finish, preserve hail.log, NON-preemptible for all tiers), and
  the 1000G AFR fallback. Carter-holds-trigger discipline stated. Staged with
  explicit path.
  </done>
</task>

</tasks>

<verification>
- `/rs1/researchers/c/ckclinto/conda_envs/smoke_dev/bin/pytest tests/m3/test_aou_ld_panel_local.py -q` → all pass (~94 prior + new RED-then-GREEN tests for du-floor helper + _capture_catastrophe_forensics).
- `_assert_checkpoint_nonempty` body byte-identical pre/post (no Track-4 guard weakened) — confirm by git diff of lines 706-753.
- Both notebooks parse as valid nbformat JSON; AOU-1 has a single INTERVAL var threaded into exactly 3 interval_filter args + all 3 du-floor cells with no hardcoded 50 MB; AOU-0.5 has the cores=1/5g lever + 2048-partition synthetic probe + forensic-capture-on-fail.
- Runbook present with Gate A/B/C, cluster specs, $ envelopes, NON-preemptible discipline, 1000G fallback, rigor caveat.
- Repo-only: nothing launched, nothing spent. Staged with explicit git paths (never -A/.).
</verification>

<success_criteria>
- All `must_haves.truths` observable: nano-interval no longer false-positives the floor; interval-agnostic count>0 stays the hard gate; forensics helper exists + never raises + emits capture json; AOU-0.5 probe + parameterized AOU-1 + tier-gated runbook all exist; no Track-4 guard weakened; full test suite green.
- TDD discipline honored on (A)+(B): failing tests observed BEFORE implementation, captured to red_fail_output.txt at the moment of the RED run and quoted in SUMMARY.
- CLAUDE.md honored: smoke_dev pytest (Python 3.11) not miniconda3; explicit git paths; 260601-cca code untouched; rigor-over-speed (cheap pass framed as "no cheap failure reproduced", never "validated").
</success_criteria>

<output>
After completion, create
`.planning/quick/260601-u1b-harden-chr22-smoke-into-tiered-cheap-fir/260601-u1b-SUMMARY.md`
recording: the RED fail-line outputs for Task 1 (quoted from red_fail_output.txt as
proof of watch-it-fail), final test pass count, the chosen du-floor helper signature
+ forensics wiring decision (standalone vs injected) with rationale, and confirmation
that no Track-4 guard was weakened.
</output>
