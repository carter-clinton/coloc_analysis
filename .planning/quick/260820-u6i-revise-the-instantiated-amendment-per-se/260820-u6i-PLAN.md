---
phase: quick-260820-u6i
plan: 01
type: execute
wave: 1
depends_on: []
subsystem: pre-registration
tags: [osf, amendment, occlusion, clause-d, anomaly-gate, multiplicity, companion-gate, count-vs-fraction, guard, slot-instantiation, seth-attack, m3-07]

files_modified:
  - .planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh
  - .planning/quick/260820-s2x-instantiate-the-occlusion-recalibration-/260820-s2x-instantiate.py
  - .planning/debug/260820-SETH-ATTACK-instantiated-amendment-as-received.md
  - .planning/quick/260820-u6i-revise-the-instantiated-amendment-per-se/260820-u6i-guard-transcript.txt
  - .planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
  - .planning/debug/260820-COURIER-TO-SETH-revision-reply.md
  - .planning/quick/260820-u6i-revise-the-instantiated-amendment-per-se/260820-u6i-PLAN.md
  - .planning/quick/260820-u6i-revise-the-instantiated-amendment-per-se/260820-u6i-SUMMARY.md

autonomous: true

requirements:
  - DEC-2026-08-19-occlusion-recalibration-adopted
  - OSF-AMEND-OCCLUSION-REVISE-PER-SETH
  - OSF-AMEND-OCCLUSION-COMPANION-INFLATION-GATE

user_setup: []

must_haves:
  truths:
    - "A reviewer reading only the paste block cannot mis-apply 1.18x as a fraction converter: the text states it is a COUNT ratio, states that it does NOT convert between the two percentages, and gives the measured fraction ratio 1.12x separately."
    - "The no-calibrate-to-pass paragraph states outright that the adopted ceiling is numerically more permissive than the rejected 10x-withdrawn candidate, and why that is the point rather than a defect."
    - "Clause (d) defers a region on EITHER the site-fraction ceiling OR a row/site inflation above 3.42x, both derived by the same location-statistic x3 rule, and every region reports its site fraction, its row count and its inflation."
    - "The gate's companion anchor is the MEDIAN inflation (1.14x) and the text says so explicitly, so no reader confuses it with the reported sample MEAN (1.18x)."
    - "The same-position collinearity note has a specific, named home: its substance is disclosed inside the paste block and its exact repo path is carried in the NOT-YET-APPENDED deviations block, which never reaches OSF."
    - "Seth's status line appears VERBATIM — his own words at the banked attack record's line 104, lowercase 'not posted', NOT the capitalized paraphrase in that record's own provenance header — in the amendment's outside-the-markers notes and in the reply courier."
    - "Every new number in the amendment entered through the extended instantiation script from a banked record; none was hand-typed; the 11 pre-existing Class-M values did not move; and the two argv-sourced Class-P slots were force-substituted, so `PRE_EXECUTE_COMMIT` advanced to the revising task's pre-commit HEAD at every occurrence rather than pinning a stale tree."
    - "The extended guard has been seen RED once per new identity on a perturbed copy, and every pre-existing control reproduces its exit code and its signature strings against the extended guard."
    - "The guard extension is provably additions-only: the guard's own commit carries ZERO deleted lines."
    - "Nothing was posted and nothing was fired: the DRAFT-NOT-POSTED banner is intact, `_OCCLUSION_ANOMALY_FRACTION` is untouched in code, and `.planning/osf_deviations.md` is byte-unchanged."
  artifacts:
    - path: ".planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh"
      provides: "Extended enforcer: 21-slot roster, *_X filled-value pattern arm, three new arithmetic identities + one ordering check at a stated TOL_X of 0.01, header CHANGELOG"
      contains: "ROSTER+=("
    - path: ".planning/quick/260820-s2x-instantiate-the-occlusion-recalibration-/260820-s2x-instantiate.py"
      provides: "Extended substitution engine: learns the 8 new slots from the banked inflation column and the banked row-sweep summary, --dry-run and --second-pass modes, pre-registered rendered-string expectations"
      contains: "--second-pass"
    - path: ".planning/quick/260820-u6i-revise-the-instantiated-amendment-per-se/260820-u6i-guard-transcript.txt"
      provides: "Verbatim green + 16 reds + the 2x2 guard/document matrix"
      min_lines: 200
    - path: ".planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md"
      provides: "The revised, re-instantiated amendment answering Seth's §2/§3/§4/§6 with 21 filled ledger slots"
      contains: "SLOT_LEDGER"
    - path: ".planning/debug/260820-SETH-ATTACK-instantiated-amendment-as-received.md"
      provides: "The banked attack — the sixth supporting record the pre-paste checklist requires"
      contains: "measurement banked; amendment drafted"
    - path: ".planning/debug/260820-COURIER-TO-SETH-revision-reply.md"
      provides: "Short reply courier with the new numbers, the guard-extension note and post-final-commit byte anchors"
      min_lines: 20
  key_links:
    - from: ".planning/debug/260820-site-basis-sweep-results-as-received.md"
      to: ".planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md"
      via: "260820-s2x-instantiate.py --second-pass, parsing the banked inflation column"
      pattern: "INFLATION_(MIN|MEDIAN|MAX|CEILING_3X|MARGIN)_X"
    - from: ".planning/debug/260819-occ-measure-sweep-results-as-received.md"
      to: ".planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md"
      via: "ROW_MEDIAN_PCT parsed from the row-sweep SUMMARY line and reconciled against its own frac column"
      pattern: "ROW_MEDIAN_PCT = 0\\.1888%"
    - from: ".planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh"
      to: ".planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md"
      via: "guard all — exit 0 is the paste gate"
      pattern: "GUARD all: GREEN"
    - from: ".planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md"
      to: ".planning/amendments/note-same-position-collinearity-2026-08-19.md"
      via: "path named in the NOT-YET-APPENDED deviations block, below the paste closer"
      pattern: "note-same-position-collinearity-2026-08-19\\.md"
    - from: ".planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md"
      to: ".planning/debug/260820-COURIER-TO-SETH-revision-reply.md"
      via: "wc -c + md5sum anchors computed after the amendment's final commit"
      pattern: "md5"
---

<objective>
Revise the instantiated occlusion-recalibration amendment against Seth's attack, closing all four
work items he raised, and extend the two enforcers so the new claims are machine-checked rather
than asserted.

Purpose: Seth found one BLOCKING defect (the 1.18x count-ratio-vs-fraction-ratio trap — the same
defect class the amendment exists to correct, reappearing inside the correction), one
self-undercutting comparison, and a hole in his own C2 advice (a site-basis gate is
multiplicity-invariant BY DESIGN and therefore multiplicity-BLIND). We adopt the companion gate
rather than disclosing the blind spot, because an undisclosed blind spot in a pre-registered
detector is the reviewer question with no good answer later — and a disclosed one still ships a
detector that cannot see the anomaly class clause (d) exists to catch.

Output: a revised amendment carrying 21 script-filled ledger slots; an additions-only guard
extension with three new identities and one new ordering check, each seen red; an extended
instantiation engine that learns the companion-gate numbers from the banked inflation column; the
banked attack record; and a short reply courier with refreshed byte anchors.

NOT IN SCOPE — and no task may drift into these: OSF contact of any kind, posting, changing
`_OCCLUSION_ANOMALY_FRACTION` or any code/test/config/Snakefile/runbook, editing the posted July
amendment, editing either `osf_deviations.md`, editing `.planning/STATE.md` or
`.planning/ROADMAP.md`, or editing the §6 collinearity note file itself. AN AGENT NEVER POSTS AND
NEVER FIRES.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/debug/260820-SETH-ATTACK-instantiated-amendment-as-received.md
@.planning/debug/260820-site-basis-sweep-results-as-received.md
@.planning/debug/260819-occ-measure-sweep-results-as-received.md
@.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
@.planning/amendments/note-same-position-collinearity-2026-08-19.md
@.planning/quick/260820-s2x-instantiate-the-occlusion-recalibration-/260820-s2x-instantiate.py
@.planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh
@.planning/quick/260820-s2x-instantiate-the-occlusion-recalibration-/260820-s2x-PLAN.md

<interfaces>
<!-- Contracts the executor needs. Do not go looking for these in the codebase; they are here. -->

MEASURED STATE AT PLANNING TIME (re-measure, do not copy blindly — but a different answer means
something moved and you must STOP and report):
  HEAD                           f47d9a5f72116917cde16f29343e67d363eb68b1
  amendment                      31,685 B / md5 b8f9a978c9bdbc7892f97b5d90cf9d27
  guard all on the amendment     EXIT=0 (GREEN)
  ledger-pattern lines in file   13   (grep -c -E '^  [A-Z0-9_]+ = ')
  `0.1888` occurrences in file   3    (lines 199, 271, 424)
  Seth attack record             UNTRACKED  -> Task 1 banks it (becomes the SIXTH supporting record)

THE 8 NEW SLOTS — values PRE-REGISTERED here so a script that computes anything else is loudly
wrong. All are recomputed BY SCRIPT from banked columns; none is an argument, none is hand-typed.

  Source = the `inflation` column (8th) of the 21-row per-region table in
  .planning/debug/260820-site-basis-sweep-results-as-received.md:
    1.79 1.12 1.37 1.06 1.07 1.08 1.16 1.04 1.07 1.06 1.20
    1.20 1.09 1.30 1.22 1.19 1.14 1.11 1.20 1.14 1.18      (n=21)

  SLOT                      RENDERED    HOW
  INFLATION_MIN_X           1.04x       min(column)
  INFLATION_MEDIAN_X        1.14x       median(column)
  INFLATION_MAX_X           1.79x       max(column)
  INFLATION_ROBUST_SIGMA_X  0.0890x     1.4826 * MAD(column)   (MAD = 0.06)  <- 4 dp, see below
  INFLATION_CEILING_3X_X    3.42x       3 * INFLATION_MEDIAN_X
  INFLATION_MARGIN_X        1.91x       INFLATION_CEILING_3X_X / INFLATION_MAX_X  (=1.910614...)
  ROW_MEDIAN_PCT            0.1888%     parsed from the ROW-basis sweep SUMMARY line in
                                        .planning/debug/260819-occ-measure-sweep-results-as-received.md,
                                        reconciled against that file's own 21 `frac=` values
  FRACTION_RATIO_X          1.12x       ROW_MEDIAN_PCT / SITE_MEDIAN_PCT  (=1.120475...)

  The existing MEAN_ROW_SITE_INFLATION = 1.18x is the MEAN of the same column and STAYS. It is
  the reported summary figure. It is NOT the companion gate's anchor — the median is.

  RENDER PRECISION. The existing engine renders x-ratios at 2 dp (`%.2fx`). That is right for
  every new slot EXCEPT INFLATION_ROBUST_SIGMA_X, where 2 dp collapses 0.089 to `0.09x` and
  destroys the quantity. Render that ONE slot at 4 dp (`0.0890x`). The guard's `*_X` pattern
  `^  NAME = [0-9]+\.[0-9]+x$` accepts both widths, so this needs no guard special case.

  NAMING NOTE. The brief called this slot `INFLATION_ROBUST_SIGMA`. It is named
  `INFLATION_ROBUST_SIGMA_X` here so a SINGLE new `*_X)` arm in the guard's filled-value
  dispatch covers all six inflation/ratio slots. The guard's fail-closed `*)` arm ("no
  filled-value pattern defined for $s") STAYS, so a future slot with an unrecognised suffix
  still fails rather than passing silently. Record this rename in the SUMMARY.

THE THREE NEW ARITHMETIC IDENTITIES (tolerance TOL_X = 0.01, stated because an unstated
tolerance is an unfalsifiable check; TOL_RATIO = 0.02 for the pre-existing CEILING_MARGIN_X
identity is UNTOUCHED — this is a new constant, never a widening of an old one):
    INFLATION_CEILING_3X_X == 3 * INFLATION_MEDIAN_X            3.42  vs 3.42      |d|=0.0000
    INFLATION_MARGIN_X     == INFLATION_CEILING_3X_X / INFLATION_MAX_X
                                                                1.91  vs 1.910614 |d|=0.0006
    FRACTION_RATIO_X       == ROW_MEDIAN_PCT / SITE_MEDIAN_PCT   1.12  vs 1.120475 |d|=0.0005
PLUS one new ordering check:
    INFLATION_MIN_X <= INFLATION_MEDIAN_X <= INFLATION_MAX_X    1.04 <= 1.14 <= 1.79

THE MECHANISM SENTENCE FOR §2 (Seth's arithmetic, independently reproduced):
    fraction ratio = count ratio x (n_sites / n_rows)
    region 1: n_rows/n_sites = 102,421 / 96,708 = 1.059
    1.18 / 1.059 = 1.114  ~  the measured 1.12 ratio of the two medians
  So: 1.18x is a COUNT ratio (occluded rows / occluded sites). Applying it to a FRACTION
  overshoots, because that fraction's denominator inflates too. DO NOT put Seth's wrong-answer
  demonstration (`0.1685 x 1.18 = 0.1988%`) into the amendment: `0.1988%` is a 4-decimal
  percentage and would be an unclassified literal in the census, and the amendment states the
  correction, not the mistake.

GUARD CONTRACT (what you are extending — read before touching it):
  ROSTER=(...)                      bash array, 13 names. Drives BOTH `draft` and `paste-ready`.
  sec_draft                         requires every roster NAME present in the file, and
                                    `grep -c -E '^  [A-Z0-9_]+ = '` == ${#ROSTER[@]}.
                                    => growing the roster to 21 AUTOMATICALLY requires 21
                                    ledger lines. No new code needed for that check.
  sec_paste_ready                   per-slot `case "$s" in` filled-value pattern dispatch:
                                      MEAN_ROW_SITE_INFLATION|CEILING_MARGIN_X) -> x
                                      POSTING_DATE) -> date   PRE_EXECUTE_COMMIT) -> hex
                                      *_PCT) -> %             *) -> FAIL (fail-closed)
                                    ROW_MEDIAN_PCT is already covered by *_PCT. The six
                                    inflation/ratio slots need one new *_X) arm, INSERTED
                                    BEFORE *_PCT). Insertion deletes nothing.
  sec_arith                         python heredoc. `NEEDED = [...]`, `num(key,suffix)`,
                                    `ident(name,lhs,rhs,tol,unit)`; failure message is
                                    "<name> BROKEN (...)". Ends `sys.exit(rc)`.
  ZERO-DELETION TECHNIQUE           bash: append `ROSTER+=(...)` AFTER the existing literal.
                                    python: append `NEEDED += [...]` AFTER the existing literal.
                                    New identities: insert immediately BEFORE `sys.exit(rc)`.
                                    Header CHANGELOG: append a new numbered comment section.
                                    Done this way the guard's diff carries ZERO deleted lines,
                                    which is the enforceable form of "strictly additive".

SCRIPT CONTRACT (what you are extending):
  SOURCE                            module constant, hard-coded to the site-basis record. That
                                    hard-coding IS a safety property — a real run cannot be
                                    pointed at a different file. Preserve it (see --control-source).
  parse_summary / parse_table       regex parsers over the banked record.
  reconcile()                       recomputes every printed aggregate from the 21-row table and
                                    ABORTS before writing on any disagreement. Must still run
                                    and still print its RECONCILIATION block.
  GUARD_PATTERNS dict               the guard's own patterns restated; `ROSTER = list(...)`.
                                    Extend with `GUARD_PATTERNS.update({...})` (additive).
  "refusing to run twice"           `if n_open == 0: die(...)`. The amendment is ALREADY
                                    instantiated, so a naive re-run aborts here. Do NOT delete
                                    this check — gate it behind an explicit --second-pass mode.

CENSUS (carried forward from 260820-s2x-PLAN.md Task 2, Step 2 — reuse, do not re-derive):
  SITE set (must all be present)  0.1345% 0.1685% 0.2698% 0.0274% 0.2507% 0.2781% 0.3370%
                                  0.5396% 0.5056%   plus 1.87x 1.18x
  ROW set (must carry a row label, or sit in the §8 verbatim block)
                                  0.1323% 0.1888% 0.3527% 0.0393% 0.5664% 0.2255% 0.0059% 0.4%
  4-DECIMAL EXEMPTION SET — EXACTLY TWO LITERALS, UNCHANGED BY THIS TASK:
                                  0.0068%  (inventory share, no occlusion basis)
                                  0.1234%  (the rendering EXAMPLE in the instantiation record)
  => Any OTHER 4-decimal percentage anywhere in the file is a DRIFT FINDING. The seven new
     x-ratio literals end in `x`, not `%`, so the census regex `\d+\.\d{4}%` does not see them
     and the exemption set does NOT grow. Any prose you add must therefore avoid 4-decimal
     percentages: write the §4 worked example at 2 dp ("approximately 0.17% (site basis)",
     "approximately 3.4% of rows (row basis)"), never at 4.
  => NEW required-presence set (the 7 new x-literals):
                                  1.04x 1.14x 1.79x 0.0890x 3.42x 1.91x 1.12x
  => NEW forbidden strings:       `0.1988%` (Seth's wrong-answer demo) and `1.42x` (Seth's
                                  superseded run-collapse estimate, which lives in the banked
                                  record's parenthetical and must not migrate into the amendment).

TRANSCRIPT FORMAT — mandatory, because the verify blocks parse it. One file,
`260820-u6i-guard-transcript.txt`, sections opened by a line beginning `### `, each carrying the
command, the FULL output verbatim, and an `EXIT=<code>` line per invocation. A whole-file grep
cannot tell the controls apart, so each verify extracts its OWN section by token.

SCRATCH DISCIPLINE. Every control copy lives in the session scratchpad, NEVER in the repo tree.
Copies carrying perturbed numbers get first line `FAKE NUMBERS - GUARD CONTROL ONLY - NEVER POST`.
The 21-slot pre-revision fixture carries real numbers in a provisional document state, so it gets
the distinct first line `GUARD FIXTURE - SCRATCH ONLY - NEVER POST`. The tracked tree is grepped
for BOTH banners afterwards (first-line scan, not a substring grep — those strings legitimately
appear as prose in this PLAN and in the SUMMARY).
</interfaces>
</context>

<constraints>
GPFS / staging:
- No worktrees. Explicit-path staging ONLY — never `git add .` or `git add -A`.
- Never stage `tests/m3/sparse_parent_benchmark.tsv`.
- GPFS object-store contingency: if a commit fails with "invalid object" / "Error building
  trees", run the guarded `git hash-object -w` recovery loop over the staged paths and retry the
  commit ONCE. If it fails twice, STOP and report.

Scope:
- NO OSF contact. NO posting. AN AGENT MUST NEVER FIRE.
- Do NOT touch: `tests/`, `src/`, `config/`, `Snakefile`, the ox1 runbooks, the posted July
  amendment, EITHER `osf_deviations.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, or
  `.planning/amendments/note-same-position-collinearity-2026-08-19.md`.
- Do NOT touch the §8 verbatim provenance block or the quoted posted-record parenthetical.
- The DRAFT-NOT-POSTED banner at the top of the amendment STAYS.

Mechanism:
- Hand-typing a NUMBER into the amendment is FORBIDDEN. Every slot value enters through
  `260820-s2x-instantiate.py`. Extend that engine in place — do NOT fork it.
- The guard is extended STRICTLY ADDITIVELY, in its OWN commit, whose diff must carry ZERO
  deleted lines. Never satisfy a check by deleting a slot, loosening a pattern, widening an
  existing tolerance, or trimming the verbatim quote. If the guard and the draft disagree on
  something that is not one of this plan's declared extensions, STOP and report.
- Every new check must be SEEN RED on a perturbed copy before its green is trusted.

Commits: `docs(quick-260820-u6i): ...`, atomic; the guard edit is its own commit.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Extend the guard additions-only and teach the engine the companion-gate slots, with every new check seen red</name>

  <files>
.planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh   (extend, own commit)
.planning/quick/260820-s2x-instantiate-the-occlusion-recalibration-/260820-s2x-instantiate.py         (extend in place)
.planning/debug/260820-SETH-ATTACK-instantiated-amendment-as-received.md                              (bank — currently untracked)
.planning/quick/260820-u6i-revise-the-instantiated-amendment-per-se/260820-u6i-guard-transcript.txt   (create)
  </files>

  <action>
Set `SCRATCH` to the session scratchpad directory and `PRE=$(git rev-parse HEAD)` BEFORE any edit.
Record both in the SUMMARY. Everything below assumes the repo root as cwd.

**Step 1 — extend the guard. FOUR insertions, ZERO deletions.**

  G1. **Header CHANGELOG.** Append a new numbered comment section `# 5. CHANGELOG.` after the
      existing section 4 and before the `# Usage:` line. It must state: the date and quick ID;
      that the extension adds 8 roster slots, one `*_X)` filled-value arm, three arithmetic
      identities and one ordering check at a stated `TOL_X` of 0.01; that NO existing check was
      removed, renamed, loosened or given a wider tolerance; that the diff on this file carries
      ZERO deleted lines; and the path of `260820-u6i-guard-transcript.txt` where the new reds
      and the re-run pre-existing controls are transcribed verbatim.

  G2. **Roster append.** Immediately AFTER the closing `)` of the existing `ROSTER=(...)` literal,
      add a short comment and:
      ```
      ROSTER+=(INFLATION_MIN_X INFLATION_MEDIAN_X INFLATION_MAX_X INFLATION_ROBUST_SIGMA_X
               INFLATION_CEILING_3X_X INFLATION_MARGIN_X FRACTION_RATIO_X ROW_MEDIAN_PCT)
      ```
      `+=` is used precisely so the existing literal's last line is not rewritten. `sec_draft`'s
      ledger-line count check now demands 21 automatically — that is intended and needs no edit.

  G3. **`sec_paste_ready` pattern arm.** INSERT one new arm immediately BEFORE the `*_PCT)` arm:
      ```
      *_X)                pat="^  ${s} = [0-9]+\.[0-9]+x$" ;;
      ```
      Do not touch the existing arms. `CEILING_MARGIN_X` is still caught by the earlier explicit
      arm (first match wins), so its behaviour is bit-identical.

  G4. **`sec_arith` additions**, inside the python heredoc:
      (i) immediately AFTER the `NEEDED = [...]` literal, append
      ```
      NEEDED += ["INFLATION_MIN_X","INFLATION_MEDIAN_X","INFLATION_MAX_X",
                 "INFLATION_ROBUST_SIGMA_X","INFLATION_CEILING_3X_X","INFLATION_MARGIN_X",
                 "FRACTION_RATIO_X","ROW_MEDIAN_PCT"]
      TOL_X = 0.01
      ```
      with a comment giving the reason for 0.01 (inputs render at 2 dp; worst-case propagated
      rounding is far inside it) AND stating explicitly that `TOL_RATIO` is untouched.
      (ii) immediately BEFORE `sys.exit(rc)`, insert the parses and checks:
      ```
      imn   = num("INFLATION_MIN_X", "x")
      imed  = num("INFLATION_MEDIAN_X", "x")
      imx   = num("INFLATION_MAX_X", "x")
      isig  = num("INFLATION_ROBUST_SIGMA_X", "x")
      ic3   = num("INFLATION_CEILING_3X_X", "x")
      imarg = num("INFLATION_MARGIN_X", "x")
      fr    = num("FRACTION_RATIO_X", "x")
      rmed  = num("ROW_MEDIAN_PCT", "%")
      ok("INFLATION_ROBUST_SIGMA_X parsed as %.4fx (reported dispersion; no identity)" % isig)
      ident("INFLATION_CEILING_3X_X == 3*INFLATION_MEDIAN_X", ic3, 3*imed, TOL_X, "x")
      if imx == 0:
          bad("INFLATION_MARGIN_X == INFLATION_CEILING_3X_X / INFLATION_MAX_X undefined (INFLATION_MAX_X is 0)")
      else:
          ident("INFLATION_MARGIN_X == INFLATION_CEILING_3X_X / INFLATION_MAX_X", imarg, ic3/imx, TOL_X, "x")
      if med == 0:
          bad("FRACTION_RATIO_X == ROW_MEDIAN_PCT / SITE_MEDIAN_PCT undefined (SITE_MEDIAN_PCT is 0)")
      else:
          ident("FRACTION_RATIO_X == ROW_MEDIAN_PCT / SITE_MEDIAN_PCT", fr, rmed/med, TOL_X, "x")
      if imn <= imed <= imx:
          ok("inflation ordering holds (min %.2fx <= median %.2fx <= max %.2fx)" % (imn, imed, imx))
      else:
          bad("inflation ordering BROKEN (min %.2fx, median %.2fx, max %.2fx)" % (imn, imed, imx))
      ```
      `ident()`'s failure text is `"<name> BROKEN (...)"`, so the four new signature strings are
      exactly the four names above with ` BROKEN` appended (and `inflation ordering BROKEN`).

  Run `bash -n` on the guard. Then commit the guard ALONE:
  ```
  git add .planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh
  git commit -m "docs(quick-260820-u6i): extend the placeholder guard additions-only for the multiplicity companion gate and the fraction-ratio slot (8 slots, one *_X pattern arm, three identities + an ordering check at TOL_X 0.01; zero deleted lines)"
  ```
  Then MEASURE the additivity claim rather than asserting it:
  ```
  git diff --numstat HEAD^ HEAD -- <guard>     # second field MUST be 0
  ```
  If it is not 0, the extension is not additive — undo and redo it with `+=` / insertion. Record
  the numstat line verbatim in the SUMMARY.

**Step 2 — extend the instantiation engine (in place, at its s2x path).**

  S1. **Docstring CHANGELOG.** Add a `CHANGELOG` block to the module docstring recording the
      quick ID, the 8 new slots, the second banked source, the two new modes, and the
      INFLATION_ROBUST_SIGMA_X 4-dp render exception with its reason.

  S2. **Second banked source.** Add `SOURCE_ROW = ".planning/debug/260819-occ-measure-sweep-results-as-received.md"`
      alongside the existing hard-coded `SOURCE`. Parse its
      `SUMMARY n=21: frac min=...% median=...% max=...%` line for `ROW_MEDIAN_PCT`, and — same
      discipline as the existing `reconcile()`, because an aggregate can agree while its
      components are wrong — recompute min/median/max from that file's own 21 `frac=` values and
      require agreement inside `TOL_RECON_PCT` BEFORE anything is written. Abort on mismatch.

  S3. **Companion-gate statistics from the inflation column.** `parse_table()` already captures
      `inflation`. Compute min / median / (1.4826*MAD) / max of that column, then
      `INFLATION_CEILING_3X_X = 3 * median` and `INFLATION_MARGIN_X = ceiling / max`, and
      `FRACTION_RATIO_X = ROW_MEDIAN_PCT / SITE_MEDIAN_PCT`. These four column statistics have NO
      printed upstream aggregate to reconcile against (unlike the site-fraction summary), so they
      get a different, stronger check instead — S4.

  S4. **Pre-registered rendered-string expectations.** Add
      ```
      EXPECTED_RENDER = {
        "INFLATION_MIN_X": "1.04x", "INFLATION_MEDIAN_X": "1.14x", "INFLATION_MAX_X": "1.79x",
        "INFLATION_ROBUST_SIGMA_X": "0.0890x", "INFLATION_CEILING_3X_X": "3.42x",
        "INFLATION_MARGIN_X": "1.91x", "FRACTION_RATIO_X": "1.12x", "ROW_MEDIAN_PCT": "0.1888%",
      }
      ```
      with a comment pointing at this PLAN as the pre-registration. After rendering, require
      byte-exact equality for each; on mismatch `die("PRE-REGISTERED EXPECTATION FAILED for %s: computed %s, expected %s")`.
      A must-be-identity string comparison is used deliberately in preference to a
      must-be-close numeric one.

  S5. **Render precision.** Keep `%.2fx` for every x-slot EXCEPT `INFLATION_ROBUST_SIGMA_X`,
      which renders `%.4fx`. Keep `%.4f%%` for every `_PCT` slot.

  S6. **`--dry-run`.** Computes, reconciles, renders, prints the full 21-row SUBSTITUTION LEDGER
      and the EXPECTED_RENDER verdicts, and WRITES NOTHING. Under `--dry-run` the positional
      `amendment` argument is optional and `--pre-execute-commit` / `--posting-date` default to
      placeholder strings that are printed as such.

  S7. **`--second-pass`. Class-M and Class-P behave DIFFERENTLY here — this distinction is the
      whole design, and collapsing it deadlocks the mode.** The existing
      `if n_open == 0: die("refusing to run twice")` is a real safety property and is NOT deleted —
      it is gated. Under `--second-pass`:

      **Class-M (the 19 MEASURED slots) — sentinel-or-verify.** A Class-M slot with sentinels is
      substituted as before. A Class-M slot with ZERO `{{SLOT}}` occurrences is not an error, but
      the script MUST then find its existing SLOT_LEDGER line and assert the value is byte-identical
      to the value it just computed from the banked records. A measured value that drifted, or was
      hand-edited, ABORTS the run. This is the drift trap and it stays sharp.

      **Class-P (`POSTING_DATE`, `PRE_EXECUTE_COMMIT`) — ALWAYS force-substituted.** These are
      argv-sourced, not measured, and they are DEFINED to move: the amendment's own pre-paste table
      commits to re-confirming both at posting. They are therefore replaced at EVERY occurrence —
      the SLOT_LEDGER line and every other occurrence in the document — regardless of whether a
      sentinel is present, by locating the slot's CURRENT ledger value and replacing that literal
      string document-wide. **The Class-M zero-occurrence drift-abort MUST NOT be applied to
      Class-P.** Applying it would deadlock the mode on its first real use: `PRE_EXECUTE_COMMIT`
      already carries a filled value from the first instantiation and any later HEAD is necessarily
      a different hash, so a Class-M-style verify would abort before re-instantiation could begin.

      Force-substitution safety, because a blind document-wide string replace is a sharp tool:
        - refuse if the current ledger value for a Class-P slot is empty or still a sentinel;
        - count occurrences of the OLD literal BEFORE replacing, replace all of them, then assert
          the new value occurs exactly that many times and — unless old == new — that the old
          literal occurs ZERO times afterwards;
        - assert the old literal is not a substring of anything it must not touch: in particular
          `POSTING_DATE` = `2026-08-21` must never disturb the `2026-08-20` INSTANTIATION date in
          the basename or in prose, and the two strings are distinct, so a plain literal replace is
          safe — but ASSERT the post-replace count of `2026-08-20` is unchanged rather than
          assuming it.
      PRE-REGISTERED OCCURRENCE COUNTS, measured on the live file at planning time (a different
      count means the document moved and the executor must STOP and report, not adapt):
      `PRE_EXECUTE_COMMIT` full hash **2** (SLOT_LEDGER line + pre-paste table row);
      `POSTING_DATE` `2026-08-21` **3** (SLOT_LEDGER line, pre-paste table row, paste block's
      **Date:** line); the untouched instantiation date `2026-08-20` **3**.
      Report Class-P slots as `FORCE-SUBSTITUTED (argv)` in the ledger's SOURCE column.

      The total assertion becomes `sum(replacements over sentinel-carrying slots) ==
      pre-count('{{') == pre-count('}}')` — Class-P force-substitutions are counted and reported
      SEPARATELY, since they consume no sentinel. The post-write readback still requires zero
      surviving delimiters. Print each slot's mode as `SUBSTITUTED`, `VERIFIED-IN-PLACE` or
      `FORCE-SUBSTITUTED (argv)` in the ledger's SOURCE column.

  S8. **`--control-source PATH` / `--control-row-source PATH`.** For negative controls only.
      Supplying either FORCES `--dry-run` (the write path is disabled), prints a loud
      `*** NEGATIVE CONTROL MODE — WRITE PATH DISABLED ***` banner, and is refused in combination
      with a real write. This preserves "a real run cannot be pointed at a different file" while
      making the S4 expectation check falsifiable.

**Step 3 — build the 21-slot fixture (scratch only).** `mkdir -p "$SCRATCH/u6i"`. Copy the
tracked amendment to `$SCRATCH/u6i/fixture.md`, prepend the first line
`GUARD FIXTURE - SCRATCH ONLY - NEVER POST`, and append the 8 new ledger lines to its SLOT_LEDGER
block using the values printed by `--dry-run` (copy them from that output, do not retype).
This fixture is the positive control and the base for the perturbation controls; it never enters
the repo.

**Step 4 — run the control matrix and transcribe it VERBATIM** into
`.planning/quick/260820-u6i-revise-the-instantiated-amendment-per-se/260820-u6i-guard-transcript.txt`.
Section headers must carry these tokens EXACTLY:

```
### DRY-RUN  — extended engine --dry-run: 21 renders vs pre-registered expectations   EXIT=0
### G-NEW    — extended guard `all` on the 21-slot fixture                            EXIT=0
### MATRIX-A — PRE-EXTENSION guard (git show $PRE:<guard>) on the PRE-REVISION file    EXIT=0
### NC-0     — extended guard `all` on the tracked 13-slot amendment                   EXIT=1
### NC-A     — fixture, INFLATION_MEDIAN_X -> 1.50x, section arith                     EXIT=1
### NC-B     — fixture, INFLATION_MAX_X    -> 2.50x, section arith                     EXIT=1
### NC-C     — fixture, ROW_MEDIAN_PCT     -> 0.2500%, section arith                   EXIT=1
### NC-D     — fixture, INFLATION_MIN_X    -> 1.50x, section arith                     EXIT=1
### NC-E     — fixture, INFLATION_CEILING_3X_X value blanked, section paste-ready      EXIT=1
### NC-F     — fixture, FRACTION_RATIO_X ledger LINE deleted, sections draft+paste-ready  EXIT=1 x2
### NC-G     — engine --control-source with one inflation cell perturbed               EXIT=1
### REG-1    — pre-instantiation XX version, sections paste-ready + arith              EXIT=1 x2
### REG-2    — fixture, SITE_MAX_PCT -> 0.2500%, section arith                         EXIT=1
### REG-3    — fixture, SITE_MIN_PCT value blanked, section paste-ready                EXIT=1
### REG-4    — empty file, section draft                                               EXIT=1
### REG-5    — fixture, PASTE opener deleted, section draft                            EXIT=1
### REG-6    — fixture, one verbatim §8 quote line trimmed, section quote              EXIT=1
```

  REQUIRED SIGNATURE STRINGS (each must appear in its OWN section):
  - NC-A  `INFLATION_CEILING_3X_X == 3*INFLATION_MEDIAN_X BROKEN`
          and the ordering must still PASS (1.04 <= 1.50 <= 1.79) — the control is isolated.
  - NC-B  `INFLATION_MARGIN_X == INFLATION_CEILING_3X_X / INFLATION_MAX_X BROKEN`
  - NC-C  `FRACTION_RATIO_X == ROW_MEDIAN_PCT / SITE_MEDIAN_PCT BROKEN`
  - NC-D  `inflation ordering BROKEN`   (1.50 <= 1.14 is false; no identity uses the min, so
          this is the sole failure)
  - NC-E  `ledger line INFLATION_CEILING_3X_X does not match its filled-value pattern` — and
          this section must show that failure for that slot and NO other (this is what proves
          the new `*_X)` arm is load-bearing rather than decorative).
  - NC-F  `SLOT_LEDGER carries 20 ledger lines, want 21` (draft) — proves the roster growth is
          load-bearing — plus the paste-ready failure naming FRACTION_RATIO_X.
  - NC-G  `PRE-REGISTERED EXPECTATION FAILED`
  - REG-2 BOTH `TWO_X_MAX_PCT == 2*SITE_MAX_PCT BROKEN` and
          `CEILING_MARGIN_X == CEILING_3X_MEDIAN_PCT / SITE_MAX_PCT BROKEN` — the s2x NC-2
          signatures, reproduced against the EXTENDED guard.
  - REG-3 `ledger line SITE_MIN_PCT does not match its filled-value pattern`, for that slot and
          no other — the s2x NC-3 signature, reproduced.
  - REG-1 `basename still carries the XX date placeholder` (paste-ready). Reconstruct with
          `git show <a commit before the rename>:...-2026-08-XX.md`, preserving the XX basename.

  ⚠ **TWO EXPECTED, DOCUMENTED BEHAVIOUR CHANGES — do not "fix" either, do not hide either.**
  1. **REG-2 and REG-3 are REBASED onto the 21-slot fixture, not onto the tracked 13-slot file.**
     Against the extended guard a 13-slot file fails `arith` at "SLOT_LEDGER is missing lines:
     INFLATION_*" and exits BEFORE reaching the identity checks, so the old signatures could not
     appear. Rebasing is what makes "identical behaviour" a meaningful claim rather than a
     coincidence. State this in the SUMMARY.
  2. **REG-1's `arith` message changes** from "cannot verify — draft not instantiated" to
     "SLOT_LEDGER is missing lines: ..." — a 13-slot file is missing 8 roster lines before it can
     be judged uninstantiated. Same exit code, different message. Record it as a KNOWN CHANGE.

  The 2x2 matrix (2 guards x 2 document versions) is the honest form of "green is evidence".
  Task 1 fills the two PRE-REVISION cells (MATRIX-A green, NC-0 red); Task 2 fills the two
  REVISED cells. Note explicitly in the SUMMARY that the brief's phrase "the pre-revision version
  still GREEN too" is true ONLY under the PRE-EXTENSION guard — under the extended guard it is
  correctly RED, and that red is NC-0.

**Step 5 — bank the attack and commit the rest.**
```
git add .planning/debug/260820-SETH-ATTACK-instantiated-amendment-as-received.md \
        .planning/quick/260820-s2x-instantiate-the-occlusion-recalibration-/260820-s2x-instantiate.py \
        .planning/quick/260820-u6i-revise-the-instantiated-amendment-per-se/260820-u6i-guard-transcript.txt
git commit -m "docs(quick-260820-u6i): bank Seth's attack as the sixth supporting record; teach the instantiation engine the 8 companion-gate slots (inflation column + row-sweep median, dry-run + second-pass + control-source modes) with 17 transcribed controls"
```
Then grep the TRACKED tree, first-line only, for both scratch banners and require zero hits.
  </action>

  <verify>
    <automated>
set -uo pipefail
G=.planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh
S=.planning/quick/260820-s2x-instantiate-the-occlusion-recalibration-/260820-s2x-instantiate.py
T=.planning/quick/260820-u6i-revise-the-instantiated-amendment-per-se/260820-u6i-guard-transcript.txt
A=.planning/debug/260820-SETH-ATTACK-instantiated-amendment-as-received.md
sec(){ awk -v s="### $1" 'index($0,s)==1{f=1;next} /^### /{f=0} f' "$T"; }

# --- 1. the guard extension is provably additions-only -------------------------------------
GC=$(git log --format=%H -1 -- "$G")
DEL=$(git diff --numstat "$GC^" "$GC" -- "$G" | awk '{print $2}')
test "$DEL" = "0" || { echo "FAIL guard commit deleted $DEL line(s) — extension is not additive"; exit 1; }
git show --stat --format='' "$GC" | grep -q "$(basename "$G")" || { echo "FAIL guard commit does not contain the guard"; exit 1; }
test "$(git show --format='' --name-only "$GC" | grep -c .)" -eq 1 || { echo "FAIL guard commit is not guard-only"; exit 1; }
bash -n "$G" || { echo "FAIL guard syntax"; exit 1; }
grep -q 'ROSTER+=(' "$G" || { echo "FAIL roster not extended by append"; exit 1; }
grep -q 'TOL_X' "$G" || { echo "FAIL x-ratio tolerance not introduced"; exit 1; }
grep -q '0\.01' "$G" || { echo "FAIL x-ratio tolerance value not stated"; exit 1; }
grep -q 'TOL_RATIO = 0.02' "$G" || { echo "FAIL pre-existing TOL_RATIO was altered or removed"; exit 1; }
grep -q 'CHANGELOG' "$G" || { echo "FAIL guard CHANGELOG absent"; exit 1; }
grep -q 'no filled-value pattern defined for' "$G" || { echo "FAIL fail-closed pattern arm lost"; exit 1; }
for s in INFLATION_MIN_X INFLATION_MEDIAN_X INFLATION_MAX_X INFLATION_ROBUST_SIGMA_X \
         INFLATION_CEILING_3X_X INFLATION_MARGIN_X FRACTION_RATIO_X ROW_MEDIAN_PCT; do
  grep -q "$s" "$G" || { echo "FAIL guard does not know slot $s"; exit 1; }
done

# --- 2. the engine computes the pre-registered renders, from the banked records -------------
python3 -c "import ast,sys; ast.parse(open('$S').read())" || { echo "FAIL engine syntax"; exit 1; }
grep -q 'SOURCE_ROW' "$S" || { echo "FAIL engine does not read the banked row sweep"; exit 1; }
grep -q 'EXPECTED_RENDER' "$S" || { echo "FAIL engine has no pre-registered expectations"; exit 1; }
grep -q 'second-pass' "$S" || { echo "FAIL engine has no second-pass mode"; exit 1; }
grep -q 'refusing to run twice' "$S" || { echo "FAIL the run-twice safety check was deleted rather than gated"; exit 1; }
DR=$(python3 "$S" --dry-run 2>&1) || { echo "FAIL --dry-run exited nonzero"; echo "$DR"; exit 1; }
echo "$DR" | grep -q 'RECONCILIATION: OK' || { echo "FAIL reconciliation block missing from dry-run"; exit 1; }
for pair in "INFLATION_MIN_X|1\.04x" "INFLATION_MEDIAN_X|1\.14x" "INFLATION_MAX_X|1\.79x" \
            "INFLATION_ROBUST_SIGMA_X|0\.0890x" "INFLATION_CEILING_3X_X|3\.42x" \
            "INFLATION_MARGIN_X|1\.91x" "FRACTION_RATIO_X|1\.12x" "ROW_MEDIAN_PCT|0\.1888%" \
            "MEAN_ROW_SITE_INFLATION|1\.18x" "SITE_MEDIAN_PCT|0\.1685%"; do
  k=${pair%%|*}; v=${pair#*|}
  echo "$DR" | grep -qE "^  ${k} +\| ${v}" || { echo "FAIL dry-run did not render $k as ${v}"; exit 1; }
done

# --- 3. the transcript: every control present, with its own signature -----------------------
test -f "$T" || { echo "FAIL transcript missing"; exit 1; }
for tok in DRY-RUN G-NEW MATRIX-A NC-0 NC-A NC-B NC-C NC-D NC-E NC-F NC-G REG-1 REG-2 REG-3 REG-4 REG-5 REG-6; do
  grep -q "^### ${tok}" "$T" || { echo "FAIL transcript section missing: $tok"; exit 1; }
done
test "$(grep -c '^EXIT=0' "$T")" -ge 3  || { echo "FAIL fewer than 3 greens transcribed"; exit 1; }
test "$(grep -c '^EXIT=1' "$T")" -ge 16 || { echo "FAIL fewer than 16 reds transcribed"; exit 1; }
sec G-NEW    | grep -q 'GUARD all: GREEN'                                                  || { echo "FAIL G-NEW not green"; exit 1; }
sec G-NEW    | grep -q '^EXIT=0'                                                           || { echo "FAIL G-NEW exit"; exit 1; }
sec MATRIX-A | grep -q '^EXIT=0'                                                           || { echo "FAIL MATRIX-A must be green (old guard x old file)"; exit 1; }
sec NC-0     | grep -q '^EXIT=1'                                                           || { echo "FAIL NC-0 must be red (new guard x old file)"; exit 1; }
sec NC-A | grep -q 'INFLATION_CEILING_3X_X == 3\*INFLATION_MEDIAN_X BROKEN'                || { echo "FAIL NC-A signature"; exit 1; }
sec NC-A | grep -q 'inflation ordering holds'                                              || { echo "FAIL NC-A not isolated (ordering should still pass)"; exit 1; }
sec NC-B | grep -q 'INFLATION_MARGIN_X == INFLATION_CEILING_3X_X / INFLATION_MAX_X BROKEN' || { echo "FAIL NC-B signature"; exit 1; }
sec NC-C | grep -q 'FRACTION_RATIO_X == ROW_MEDIAN_PCT / SITE_MEDIAN_PCT BROKEN'           || { echo "FAIL NC-C signature"; exit 1; }
sec NC-D | grep -q 'inflation ordering BROKEN'                                             || { echo "FAIL NC-D signature"; exit 1; }
sec NC-E | grep -q 'ledger line INFLATION_CEILING_3X_X does not match its filled-value pattern' || { echo "FAIL NC-E signature"; exit 1; }
test "$(sec NC-E | grep -c 'does not match its filled-value pattern')" -eq 1                || { echo "FAIL NC-E not isolated to one slot"; exit 1; }
sec NC-F | grep -q 'ledger lines, want 21'                                                 || { echo "FAIL NC-F did not trip the roster-count check"; exit 1; }
sec NC-G | grep -q 'PRE-REGISTERED EXPECTATION FAILED'                                     || { echo "FAIL NC-G signature"; exit 1; }
sec REG-1 | grep -q 'basename still carries the XX date placeholder'                       || { echo "FAIL REG-1 signature"; exit 1; }
test "$(sec REG-1 | grep -c '^EXIT=1')" -ge 2                                              || { echo "FAIL REG-1 needs both paste-ready and arith red"; exit 1; }
sec REG-2 | grep -q 'TWO_X_MAX_PCT == 2\*SITE_MAX_PCT BROKEN'                              || { echo "FAIL REG-2 lost the s2x NC-2 signature"; exit 1; }
sec REG-2 | grep -q 'CEILING_MARGIN_X == CEILING_3X_MEDIAN_PCT / SITE_MAX_PCT BROKEN'      || { echo "FAIL REG-2 lost the second s2x NC-2 signature"; exit 1; }
sec REG-3 | grep -q 'ledger line SITE_MIN_PCT does not match its filled-value pattern'     || { echo "FAIL REG-3 lost the s2x NC-3 signature"; exit 1; }
test "$(sec REG-3 | grep -c 'does not match its filled-value pattern')" -eq 1              || { echo "FAIL REG-3 not isolated to one slot"; exit 1; }
sec REG-4 | grep -q 'under byte floor'                                                     || { echo "FAIL REG-4 vacuity floor not exercised"; exit 1; }
sec REG-5 | grep -q 'PASTE opener occurs'                                                  || { echo "FAIL REG-5 opener check not exercised"; exit 1; }
sec REG-6 | grep -q 'source line NOT carried verbatim'                                     || { echo "FAIL REG-6 quote check not exercised"; exit 1; }

# --- 4. the attack is banked; no scratch banner reached the tracked tree --------------------
git ls-files --error-unmatch "$A" >/dev/null 2>&1 || { echo "FAIL attack record not banked"; exit 1; }
grep -q 'measurement banked; amendment drafted' "$A" || { echo "FAIL attack record lacks the status line"; exit 1; }
LEAK=0
for f in $(git ls-files '.planning/*'); do
  h=$(head -1 "$f" 2>/dev/null)
  case "$h" in
    "FAKE NUMBERS - GUARD CONTROL ONLY - NEVER POST"*|"GUARD FIXTURE - SCRATCH ONLY - NEVER POST"*)
      echo "FAIL scratch control copy leaked into the tracked tree: $f"; LEAK=1 ;;
  esac
done
test "$LEAK" -eq 0 || exit 1

# --- 5. nothing forbidden moved -------------------------------------------------------------
git status --porcelain .planning/osf_deviations.md .planning/STATE.md .planning/ROADMAP.md \
  .planning/amendments/note-same-position-collinearity-2026-08-19.md | grep . \
  && { echo "FAIL a forbidden file was modified"; exit 1; }
git diff --name-only "$GC^^" HEAD | grep -qvE '^\.planning/(quick|debug)/' \
  && { echo "FAIL task 1 commits touched something outside .planning/quick and .planning/debug"; exit 1; }
echo "TASK1 VERIFY OK"
    </automated>
  </verify>

  <done>
The guard carries 21 roster slots, one new `*_X)` filled-value arm, three new identities and one
new ordering check at an explicitly stated `TOL_X` of 0.01, plus a header CHANGELOG — in its own
commit whose `git diff --numstat` deleted-lines field is literally `0`, with `TOL_RATIO` and the
fail-closed `*)` arm untouched. The engine computes all 8 new values from the two banked records
(never from arguments), asserts each against its pre-registered rendered string, and carries
`--dry-run`, `--second-pass` and negative-control source overrides without deleting the
run-twice safety check. Seventeen controls are transcribed verbatim with exit codes: three greens
(including the pre-extension-guard x pre-revision-file matrix cell) and sixteen reds, each new
identity seen red in isolation and each pre-existing s2x/u8d control reproducing its signature
strings against the extended guard. The two expected behaviour changes (REG-2/REG-3 rebased onto
the 21-slot fixture; REG-1's arith message) are recorded as known, not hidden. Seth's attack is
banked as the sixth supporting record. No scratch banner appears on the first line of any tracked
file. `.planning/osf_deviations.md`, `STATE.md`, `ROADMAP.md` and the collinearity note are
byte-unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Revise the amendment against Seth's four asks, re-instantiate by script, and re-green the guard</name>

  <files>
.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
.planning/quick/260820-u6i-revise-the-instantiated-amendment-per-se/260820-u6i-guard-transcript.txt   (append)
  </files>

  <action>
Capture `PRE_REV=$(git rev-parse HEAD)` BEFORE any edit and record it in the SUMMARY — every
"did it move?" check below compares against that blob.

**REVISION REGISTER — every item must be dispositioned, none may be skipped. For EVERY item
record in the SUMMARY: the line number, the verdict (CHANGED / STANDS / STOP), and for every
CHANGED item the BEFORE and AFTER text VERBATIM.** Numbers enter as `{{SLOT}}` sentinels ONLY;
hand-typing a number is forbidden.

  R1. **Preamble (outside the paste markers).** Add a REVISION note: the body was revised on
      2026-08-20 against the attack banked at
      `.planning/debug/260820-SETH-ATTACK-instantiated-amendment-as-received.md`, re-instantiated
      by the extended engine in `--second-pass` mode, and re-verified by the extended guard whose
      green and reds are transcribed in `260820-u6i-guard-transcript.txt` (keep the existing
      pointer to the s2x transcript — it is the history of the first instantiation, not a stale
      claim). Then add Seth's status line VERBATIM, as its own sentence. **Take it from the
      banked attack record's LINE 104 — his own words, lowercase `not posted`. Do NOT copy the
      capitalized `NOT posted` form from that record's provenance header at lines 10-11: that is
      our adopted restatement, not his sentence, and quoting a paraphrase as verbatim is the same
      defect class this amendment exists to correct.** The sentence:
      `measurement banked; amendment drafted, not posted; code constant unchanged; fire HELD; an agent never posts and never fires`
      **Do NOT touch the DRAFT-NOT-POSTED banner at lines 1-3.**

  R2. **Pre-Paste table, `What changes` row.** Currently names two changes. Add the third: the
      clause-(d) gate gains a companion condition on the occluded-site row/site inflation ratio.

  R3. **Pre-paste checklist item 3.** Says "the five supporting records". Seth's attack is now
      banked, so it is six — update the count and name the attack record.

  R4. **Post-Paste checklist item 6.** Says code may change "only to the site-basis metric and
      ceiling pre-registered above". Under-specified now. Extend to name BOTH conditions, so the
      post-posting instruction cannot be read as authorising the single-metric gate.

  R5. **SLOT_LEDGER.** Insert the 8 new sentinel lines AFTER `CEILING_MARGIN_X` and BEFORE
      `POSTING_DATE` (Class-P stays last), in this order:
      `ROW_MEDIAN_PCT`, `FRACTION_RATIO_X`, `INFLATION_MIN_X`, `INFLATION_MEDIAN_X`,
      `INFLATION_MAX_X`, `INFLATION_ROBUST_SIGMA_X`, `INFLATION_CEILING_3X_X`,
      `INFLATION_MARGIN_X`. Each as `  NAME = {{NAME}}`. Ledger lines total 21.

  R6. **Instantiation record block.** "The eleven Class-M values" -> nineteen. Extend the
      formula block with the new derivations (`INFLATION_CEILING_3X_X = 3 * INFLATION_MEDIAN_X`,
      `INFLATION_MARGIN_X = INFLATION_CEILING_3X_X / INFLATION_MAX_X`,
      `FRACTION_RATIO_X = ROW_MEDIAN_PCT / SITE_MEDIAN_PCT`) and state that the inflation
      min/median/robust-sigma/max are computed from the banked table's own 8th column, that
      `ROW_MEDIAN_PCT` comes from the SECOND banked record (the row-basis sweep) and is
      reconciled against that file's own 21 per-region fractions, and that these four column
      statistics have no printed upstream aggregate and are therefore checked against
      pre-registered rendered strings instead. Update the render sentence to cover the 4-dp
      exception for `INFLATION_ROBUST_SIGMA_X`. Keep the `0.1234%` rendering example EXACTLY as
      it is — it is one of the two census exemptions and the exemption set must not drift.

  R7. ★ **§2 — THE BLOCKING FIX. Basis-conventions paragraph.** The clause
      "…so row-basis counts exceed site-basis counts by a representation-dependent factor —
      measured here as 1.18x on average across the sample." is the trap. Replace with text that
      states, explicitly and in this order:
        (i) `{{MEAN_ROW_SITE_INFLATION}}` is a COUNT ratio — occluded ROWS divided by occluded
            SITES — and nothing else;
        (ii) it **does NOT convert between the two percentages**, because both fractions carry
             denominators that also differ (`n_rows` exceeds `n_sites`), so applying a count
             ratio to a fraction OVERSHOOTS;
        (iii) the measured ratio of the two medians is `{{FRACTION_RATIO_X}}` — row-basis median
              `{{ROW_MEDIAN_PCT}}` over site-basis median 0.1685% (site basis);
        (iv) the mechanism in one line:
             `fraction ratio = count ratio x (n_sites / n_rows)`, and in region 1
             `n_rows / n_sites = 102,421 / 96,708 = 1.059`;
        (v) the standing practice that removes the need to convert at all — every figure in this
            document carries its basis label and both figures are restated wherever both matter.
      Name the defect class in one sentence: this is a quantity measured on one object asserted
      about another — the same class as the error this amendment corrects — and it is fixed here
      rather than left for a reader to trip over. **Do NOT reproduce the wrong-answer worked
      example (`0.1685 x 1.18 = 0.1988%`)**: `0.1988%` is a 4-decimal percentage and would be an
      unclassified census literal, and the amendment states the correction, not the mistake.

  R8. **Clause (d), *Accounting.* paragraph.** It already promises both numbers per region. Extend
      to: the per-region report carries the occluded-site fraction (site basis), the occluded-row
      count (row basis) AND that region's own row/site inflation at occluded sites. Then add the
      mean-vs-median disambiguation, because this is the most quotable confusion in the revision:
      the SAMPLE MEAN inflation `{{MEAN_ROW_SITE_INFLATION}}` is the reported summary figure; the
      companion gate's anchor is the MEDIAN `{{INFLATION_MEDIAN_X}}`, chosen because the site-
      fraction ceiling is anchored on a median too and the two ceilings must be derived by the
      same rule. Say that outright.

  R9. ★ **§4a — clause (d), *Ceiling.* paragraph: the two-condition gate.** Rewrite so the gate
      is stated as a disjunction — a region is DEFERRED if EITHER
        (i) `n_occluded_sites > {{CEILING_3X_MEDIAN_PCT}} x n_sites`, or
        (ii) `n_occluded_rows / n_occluded_sites > {{INFLATION_CEILING_3X_X}}`.
      If you render this as a pseudo-code block, indent it FOUR spaces or more: any line matching
      `^  [A-Z0-9_]+ = ` is counted by the guard as a SLOT_LEDGER line and would break the
      21-line count. Keep the existing site-ceiling sentences (3x the measured site-basis median
      of 0.1685% (site basis), 0.005056 as a bare fraction, `{{CEILING_MARGIN_X}}` margin over
      the observed site-basis maximum, the min and robust sigma) intact.

  R10. ★ **§4b — new sub-paragraph, *Companion condition on multiplicity (why a site gate needs
       one).*** Place it immediately after *Ceiling.* and before *Derivation…*. It must state:
        - the honest framing: site basis was chosen BECAUSE it is multiplicity-invariant, and
          invariance cuts both ways — invariant means BLIND. A region can sit at a perfectly
          normal site-basis rate while excluding a pathological number of rows.
        - the worked illustration, at TWO decimal places and with basis labels on both figures
          (four-decimal percentages are forbidden here — see the census): a region at
          approximately 0.17% (site basis), near the measured median, whose occluded sites sit at
          the observed maximum multiplicity of 21 would exclude approximately 3.4% of its rows
          (row basis) and would sail through a site-only gate. A region whose occluded sites are
          systematically high-multiplicity IS a representation anomaly — precisely the class
          clause (d) exists to catch.
        - the derivation, by the SAME rule as the main ceiling: the measured inflation
          distribution across the 21 sampled regions is min `{{INFLATION_MIN_X}}`, median
          `{{INFLATION_MEDIAN_X}}`, max `{{INFLATION_MAX_X}}`, robust sigma (1.4826 x MAD)
          `{{INFLATION_ROBUST_SIGMA_X}}`; the companion ceiling is three times the MEDIAN,
          `{{INFLATION_CEILING_3X_X}}`, anchored on a location statistic rather than a sample
          edge, giving `{{INFLATION_MARGIN_X}}` margin over the observed maximum.
        - the no-calibrate-to-pass commitment, carried to the companion explicitly: the
          multiplier was fixed by the rule, not chosen for what it clears; that the companion
          also defers 0 of 21 is a CONSEQUENCE and is stated as one; and re-widening it in
          response to a firing region is prohibited without a further amendment, exactly as for
          the site ceiling.
        - the provenance, stated plainly rather than buried: the blind spot was raised by the
          project's external methodological reviewer as a correction to his OWN earlier
          recommendation, and it is closed here rather than disclosed as a limitation, because a
          disclosed blind spot still ships a detector that cannot see the class it exists to catch.

  R11. ★ **§3 — no-calibrate-to-pass paragraph.** Append Seth's pre-emption, using his wording as
       the base: note that the adopted ceiling is numerically slightly MORE permissive than the
       rejected 10x-withdrawn candidate; that the objection to 8x/10x is not that they are too
       loose but that they would have been SELECTED for clearing the sample; and that the adopted
       value being more permissive while still rejecting them is exactly the point — the
       derivation is independent of what passes. **Basis discipline:** the comparison is
       row-against-row — the adopted ceiling's row-basis restatement 0.5664% (row basis) against
       10x the withdrawn constant, 0.5% of rows (row basis). Write `0.5%`, never `0.5000%` — a
       4-decimal percentage there would be an unclassified census literal.

  R12. ★ **§6a — clause (d), *Limitation* paragraph (inside the paste block).** Two additions.
       (i) Extend the n=21 caveat to BOTH ceilings: the inflation distribution is measured on the
           same 21 regions and its upper tail is likewise unmeasured; both fold in at closeout
           and are reported there against both conditions. Do NOT write that the gate is
           multiplicity-blind — it no longer is; that disclosure obligation is discharged by
           adopting the companion, and saying otherwise would be false.
       (ii) The same-position collinearity caveat, by SUBSTANCE: fine-mapping at multiallelic
            sites carries near-collinear predictors, because a `.bim` row is biallelic by
            construction so one k-allelic site renders as k same-position rows whose dosages are
            structurally anti-correlated; this is a known property of split-multiallelic
            representation, not a defect of this pipeline; a credible set whose members share a
            `(CHR, POS)` should be read as ONE site with unresolved allele identity; the caveat is
            recorded as a separate dated methods note in the project record and is carried into
            the manuscript's limitations.
       **DESIGN DECISION — record it in the SUMMARY with this rationale.** The repo PATH of the
       note goes in the deviations block (R13), NOT in the paste block. A posted OSF record must
       be self-contained: a public reader cannot resolve `.planning/...`, and the note itself
       declares "INTERNAL RECORD. NOT part of any OSF amendment and NOT posted". So the public
       text carries the substance and the internal block carries the address. The note gets a
       specific, checkable home either way — which was Seth's actual ask ("'somewhere' needs to
       become a specific place").

  R13. ★ **§6b — the prepared NOT-YET-APPENDED deviations block (below the paste closer).** Three
       edits. (i) Add the collinearity line naming the exact path:
       `same-position collinearity caveat recorded at .planning/amendments/note-same-position-collinearity-2026-08-19.md; fine-mapping at multiallelic sites carries near-collinear predictors; known split-representation property, not a defect`.
       (ii) Extend the `What it RECALIBRATES` bullet with the companion condition and its measured
       basis (min/median/max/robust-sigma of the inflation distribution, ceiling
       `{{INFLATION_CEILING_3X_X}}`, margin `{{INFLATION_MARGIN_X}}`). (iii) Convert that bullet's
       row-basis median literal to `{{ROW_MEDIAN_PCT}}`. Leave every `<TO BE FILLED AT POSTING>`
       marker untouched. **`.planning/osf_deviations.md` itself stays byte-unchanged and unstaged.**

  R14. **Row-median literals -> sentinel.** All THREE occurrences of `0.1888%` (measured at lines
       199, 271 and 424) become `{{ROW_MEDIAN_PCT}}`, so the row median is script-sourced from the
       banked row sweep and no longer hand-typed anywhere. **DECIDED, do not re-open:** the
       adjacent `0.5664% (row basis)` at line 271 does NOT become a slot. Rationale for the
       SUMMARY: it is pre-existing, was audited at s2x A15, is arithmetically transparent
       (3 x 0.1888 = 0.5664 exactly), sits in the census ROW set with its label, and every added
       slot enlarges the roster the guard must carry. Record the decision; do not add the slot.

  R15. **(g) What is UNCHANGED.** The three outcome tokens stay. Add one explicit line: the
       companion condition introduces NO new branch — a region deferred by either condition routes
       to the SAME `BRANCH_AFR_OCC_DEFERRED` token, and the defer-not-exclude protocol is
       untouched. This is the "what is unchanged" side of the companion condition and it is what
       stops a reviewer wondering whether a fourth branch appeared.

  R16. **Purpose-of-amendment paragraph.** Its "what moves is the numerical gate that decides
       when a region's occlusion count is anomalous" sentence now under-describes the change.
       Extend it to name the companion condition, keeping the UNCHANGED enumeration intact.

  R17. **Pre-Paste table, `Pre-execute commit gate` row prose.** It currently reads "the HEAD of
       `m3-W2-aou-deltas` at INSTANTIATION time, captured before the first commit of the
       instantiating task." Under the adopted reading (b) that value ADVANCES at this revision, so
       the prose must follow the number: state that it is the HEAD captured before the first commit
       of the REVISING task, that it superseded the first instantiation's value when the branch
       advanced, and keep the existing RE-CONFIRMED-AT-POSTING sentence verbatim — that sentence is
       the standing authority for the advance and must not be weakened. **Do not hand-type the
       hash: the value itself arrives through the engine's Class-P force-substitution.**

**Step 2 — re-instantiate BY SCRIPT.**
```
python3 .planning/quick/260820-s2x-instantiate-the-occlusion-recalibration-/260820-s2x-instantiate.py \
        --second-pass \
        .planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md \
        --pre-execute-commit "$PRE_REV" --posting-date 2026-08-21 | tee "$SCRATCH/u6i/ledger2.txt"
```
Requirements: exit 0; the **11 pre-existing Class-M** slots report `VERIFIED-IN-PLACE`; the **8
new** slots report `SUBSTITUTED`; the **2 Class-P** slots report `FORCE-SUBSTITUTED (argv)`;
zero `{{`/`}}` survive. Paste the full 21-row ledger into the SUMMARY.

⚠ **`PRE_EXECUTE_COMMIT` ADVANCES, AND THAT IS PRE-DECIDED — do not re-open the fork.** The slot
already carries `8638ed37…` from the first instantiation and carries ZERO sentinels, so it reaches
`--second-pass` through the Class-P force-substitution path in S7, never through the Class-M
verify path. Had it gone through the Class-M path, the mode would abort here every time: the
stored hash and any later HEAD are necessarily different, which is drift for a measured slot and
merely the expected re-confirmation for an argv slot.

**Reading adopted: (b) — the value ADVANCES to `$PRE_REV`,** the HEAD immediately before Task 2's
commit. Rationale: the row's operative commitment is "RE-CONFIRMED AT POSTING: re-read HEAD,
confirm no `_OCCLUSION_ANOMALY_FRACTION` or gate-constant change has landed since, and update this
value if the branch has advanced." The branch HAS advanced (Task 1 landed two commits), so the row
instructs exactly this update. Pinning the stale hash would leave the gate commit pointing at a
tree that predates the guard and engine this revision depends on.

Both occurrences move together by construction — force-substitution is document-wide — so the
ledger line and the pre-paste table row cannot drift apart. The verify block asserts the ledger
value equals the hash Task 2 passed, and Task 3 asserts the courier names the same advanced hash.
The SUMMARY records the old hash, the new hash, and this rationale.

**Step 3 — the census + drift checks** (Step 2 of the s2x register, extended). Run the verify
block's python census. The exemption set stays byte-identical at exactly two literals. Any other
4-decimal percentage is a DRIFT FINDING — report it, do not silently keep it.

**Step 4 — the 2x2 matrix and the green.** Append three sections to the transcript:
```
### SECOND-PASS — engine --second-pass on the revised amendment          EXIT=0
### MATRIX-B    — PRE-EXTENSION guard (git show $PRE:<guard>) on the REVISED file   EXIT=1
### MATRIX-C    — extended guard `all` on the REVISED file                EXIT=0
```
MATRIX-B is expected RED (the stale 13-slot guard counts 21 ledger lines and fails `draft`), and
that red is the point: it proves the stale guard cannot bless the new document, so a future
session cannot validate this file with the wrong enforcer. Record the completed 2x2 in the SUMMARY:

    old guard x pre-revision file = GREEN (MATRIX-A)   new guard x pre-revision file = RED (NC-0)
    old guard x revised file      = RED   (MATRIX-B)   new guard x revised file      = GREEN (MATRIX-C)

**Step 5 — commit.**
```
git add .planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md \
        .planning/quick/260820-u6i-revise-the-instantiated-amendment-per-se/260820-u6i-guard-transcript.txt
git commit -m "docs(quick-260820-u6i): answer Seth's attack — 1.18x named a COUNT ratio with the measured 1.12x fraction ratio, permissiveness comparison pre-empted, clause (d) gains the 3.42x multiplicity companion condition (1.91x margin), collinearity note given a home"
```
  </action>

  <verify>
    <automated>
set -uo pipefail
N=.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
G=.planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh
T=.planning/quick/260820-u6i-revise-the-instantiated-amendment-per-se/260820-u6i-guard-transcript.txt
sec(){ awk -v s="### $1" 'index($0,s)==1{f=1;next} /^### /{f=0} f' "$T"; }
PREV=$(git log --format=%H -2 -- "$N" | tail -1)

# --- 1. the guard is green, and the 2x2 matrix is complete ----------------------------------
bash "$G" all "$N" | tail -1 | grep -q 'GUARD all: GREEN' || { echo "FAIL guard not green on the revised file"; exit 1; }
for tok in SECOND-PASS MATRIX-B MATRIX-C; do grep -q "^### ${tok}" "$T" || { echo "FAIL transcript section missing: $tok"; exit 1; }; done
sec MATRIX-B | grep -q '^EXIT=1' || { echo "FAIL MATRIX-B must be RED (stale guard cannot bless the revised file)"; exit 1; }
sec MATRIX-C | grep -q '^EXIT=0' || { echo "FAIL MATRIX-C must be GREEN"; exit 1; }
sec SECOND-PASS | grep -q 'VERIFIED-IN-PLACE' || { echo "FAIL second-pass did not verify the pre-existing Class-M slots in place"; exit 1; }
sec SECOND-PASS | grep -q 'FORCE-SUBSTITUTED' || { echo "FAIL second-pass did not force-substitute the Class-P slots"; exit 1; }

# --- 2. structure: 21 ledger lines, zero sentinels, banner intact ----------------------------
test "$(grep -c -E '^  [A-Z0-9_]+ = ' "$N")" -eq 21 || { echo "FAIL ledger-pattern lines != 21 (a pseudo-code block may be masquerading as a ledger line)"; exit 1; }
grep -q '{{' "$N" && { echo "FAIL sentinels survive"; exit 1; }
grep -q '}}' "$N" && { echo "FAIL sentinels survive"; exit 1; }
grep -q 'DRAFT — NOT POSTED' "$N" || { echo "FAIL draft-not-posted banner lost"; exit 1; }

# --- 3. Class-M did NOT move; Class-P advanced to exactly the hash Task 2 passed --------------
EXPECT_COMMIT=$(git rev-parse HEAD^)          # == $PRE_REV: HEAD immediately before Task 2's commit
python3 - "$N" "$PREV" "$EXPECT_COMMIT" <<'PY'
import re,subprocess,sys
txt=open(sys.argv[1]).read()
new=dict(re.findall(r"(?m)^  ([A-Z0-9_]+) = (.*)$", txt))
old=dict(re.findall(r"(?m)^  ([A-Z0-9_]+) = (.*)$",
    subprocess.check_output(["git","show","%s:%s"%(sys.argv[2],sys.argv[1])]).decode()))
expect=sys.argv[3]
assert len(old)==13, "expected 13 pre-revision ledger lines, saw %d"%len(old)
rc=0
CLASS_P={"PRE_EXECUTE_COMMIT","POSTING_DATE"}
moved=[k for k,v in old.items() if k not in CLASS_P and new.get(k)!=v]
if moved:
    print("FAIL pre-existing Class-M values moved: %s"%moved); rc=1
else:
    print("PRE-EXISTING 11 CLASS-M VALUES UNMOVED")
# Class-P is argv-sourced and force-substituted: assert it ADVANCED to the hash Task 2 passed.
got=new.get("PRE_EXECUTE_COMMIT","").strip()
was=old.get("PRE_EXECUTE_COMMIT","").strip()
if got!=expect:
    print("FAIL PRE_EXECUTE_COMMIT is %r, expected the hash Task 2 passed: %r"%(got,expect)); rc=1
elif got==was:
    print("FAIL PRE_EXECUTE_COMMIT did not advance - the Class-P force-substitution never ran"); rc=1
else:
    print("PRE_EXECUTE_COMMIT ADVANCED %s -> %s"%(was[:7],got[:7]))
# force-substitution is document-wide, so the ledger line and the pre-paste row cannot disagree
if txt.count(got)!=2:
    print("FAIL PRE_EXECUTE_COMMIT occurs %d time(s), want exactly 2 (ledger line + pre-paste row)"%txt.count(got)); rc=1
if was and was in txt:
    print("FAIL the superseded pre-execute hash still appears in the file"); rc=1
if new.get("POSTING_DATE","").strip()!="2026-08-21":
    print("FAIL POSTING_DATE moved off its provisional value"); rc=1
if txt.count("2026-08-21")!=3:
    print("FAIL POSTING_DATE occurs %d time(s), want exactly 3 - ledger and paste block have drifted"%txt.count("2026-08-21")); rc=1
sys.exit(rc)
PY
test $? -eq 0 || exit 1

# --- 4. Seth's four asks are answered in the text --------------------------------------------
CL=$(grep -n -F -- '--- PASTE ENDS HERE ---' "$N" | head -1 | cut -d: -f1)
head -n "$CL" "$N" > /dev/null
IN=$(head -n "$CL" "$N"); OUT=$(tail -n +"$CL" "$N")
# §2 blocking fix
printf '%s' "$IN" | grep -qi 'count ratio'  || { echo "FAIL S2: 1.18x not named a COUNT ratio"; exit 1; }
printf '%s' "$IN" | grep -q  '1.12x'        || { echo "FAIL S2: measured fraction ratio absent"; exit 1; }
printf '%s' "$IN" | grep -q  '102,421 / 96,708\|102,421/96,708' || { echo "FAIL S2: mechanism denominators absent"; exit 1; }
# §3 pre-emption
printf '%s' "$IN" | grep -qi 'more permissive' || { echo "FAIL S3: permissiveness comparison not pre-empted"; exit 1; }
# §4 companion gate
for v in 3.42x 1.91x 1.14x 1.79x 1.04x 0.0890x; do
  printf '%s' "$IN" | grep -q -- "$v" || { echo "FAIL S4: companion figure $v absent from the paste block"; exit 1; }
done
printf '%s' "$IN" | grep -qi 'multiplicity' || { echo "FAIL S4: companion rationale absent"; exit 1; }
printf '%s' "$IN" | grep -q 'BRANCH_AFR_OCC_DEFERRED' || { echo "FAIL S4: branch token not reaffirmed"; exit 1; }
# §6 collinearity: substance inside, path outside, path NEVER inside
printf '%s' "$IN"  | grep -qi 'near-collinear' || { echo "FAIL S6: collinearity substance absent from the paste block"; exit 1; }
printf '%s' "$IN"  | grep -q  'note-same-position-collinearity' && { echo "FAIL S6: internal repo path leaked into the paste block"; exit 1; }
printf '%s' "$OUT" | grep -q  'note-same-position-collinearity-2026-08-19.md' || { echo "FAIL S6: note path absent from the deviations block"; exit 1; }
# §0 status line, verbatim
grep -q 'measurement banked; amendment drafted, not posted; code constant unchanged; fire HELD; an agent never posts and never fires' "$N" \
  || { echo "FAIL status line not carried verbatim"; exit 1; }

# --- 5. census + drift ------------------------------------------------------------------------
python3 - "$N" <<'PY'
import re,sys
t=open(sys.argv[1]).read()
site={"0.1345%","0.1685%","0.2698%","0.0274%","0.2507%","0.2781%","0.3370%","0.5396%","0.5056%"}
row={"0.1323%","0.1888%","0.3527%","0.0393%","0.5664%","0.2255%","0.0059%"}
# 4-decimal EXEMPTION SET — byte-identical to the s2x register. NOT grown by quick-260820-u6i:
# the seven new literals are x-ratios, not percentages, so this regex never sees them.
nonbasis={"0.0068%","0.1234%"}
newx={"1.04x","1.14x","1.79x","0.0890x","3.42x","1.91x","1.12x"}
found={m.group(0) for m in re.finditer(r'\d+\.\d{4}%',t)}
extra=found-site-row-nonbasis
print("SITE seen:",sorted(found&site)); print("ROW seen:",sorted(found&row))
print("EXEMPT seen:",sorted(found&nonbasis))
rc=0
if extra: print("DRIFT — unclassified 4dp percentages:",sorted(extra)); rc=1
miss_site=site-found
if miss_site: print("FAIL — site-basis value absent:",sorted(miss_site)); rc=1
miss_x=[v for v in sorted(newx) if v not in t]
if miss_x: print("FAIL — new companion literal absent:",miss_x); rc=1
for forbidden in ("0.1988%","1.42x"):
    if forbidden in t: print("FAIL — forbidden literal present:",forbidden); rc=1
if rc==0: print("CENSUS OK — exemption set unchanged at 2 literals; 7 new x-literals present")
sys.exit(rc)
PY
test $? -eq 0 || exit 1

# --- 6. nothing forbidden moved ---------------------------------------------------------------
git status --porcelain .planning/osf_deviations.md .planning/STATE.md .planning/ROADMAP.md \
  .planning/amendments/note-same-position-collinearity-2026-08-19.md | grep . \
  && { echo "FAIL a forbidden file was modified"; exit 1; }
git show --format='' --name-only HEAD | grep -qvE '^\.planning/(amendments|quick)/' \
  && { echo "FAIL commit touched something outside .planning/amendments and .planning/quick"; exit 1; }
git diff --name-only HEAD -- src tests config Snakefile | grep . \
  && { echo "FAIL code tree touched"; exit 1; }
echo "TASK2 VERIFY OK"
    </automated>
  </verify>

  <done>
All 17 register items are dispositioned in the SUMMARY with line numbers and verbatim
before/after for every CHANGED item. §2 states that 1.18x is a COUNT ratio, that it does not
convert between the two percentages, and gives the measured 1.12x fraction ratio with the
`fraction ratio = count ratio x (n_sites/n_rows)` mechanism and the 102,421/96,708 denominators —
without reproducing the wrong-answer demonstration. §3 states outright that the adopted ceiling is
more permissive than the rejected 10x candidate and why that is the point, comparing row against
row. Clause (d) is a disjunction: the site-fraction ceiling OR a row/site inflation above 3.42x
(3 x the measured median 1.14x, 1.91x margin over the observed 1.79x), with the illustration at
two decimals, the no-calibrate-to-pass commitment carried to the companion, the mean-vs-median
anchor disambiguated, per-region reporting of all three quantities, and (g) reaffirming that no
fourth branch appeared. The collinearity caveat's substance is in the paste block and its repo
path is in the deviations block only. Seth's status line is verbatim. The ledger carries 21 filled
slots, all 8 new values arrived through the engine from banked records, the 11 pre-existing
Class-M measurement values are byte-unmoved while the two Class-P slots were force-substituted
(`PRE_EXECUTE_COMMIT` advanced to the HEAD before this task's commit, at both of its occurrences,
with the superseded hash gone from the file), the census exemption set is still exactly two literals with
no unclassified 4-decimal percentage and no forbidden literal, and the 2x2 guard/document matrix
is complete with the extended guard GREEN and the stale guard correctly RED on the revised file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the short reply courier with post-final-commit anchors</name>

  <files>
.planning/debug/260820-COURIER-TO-SETH-revision-reply.md
  </files>

  <action>
Write `.planning/debug/260820-COURIER-TO-SETH-revision-reply.md`, matching the provenance-header
style of `.planning/debug/260820-COURIER-TO-SETH-instantiated-amendment.md`. **Hard cap 50 lines.**
Write terse and tabular from the first draft. Seth wrote the attack — do not restate it back to
him, do not include reassurance ("we believe this is correct", "the audit came out clean"), and do
not pad. Facts, then the ask.

Required content, in this order:

  1. **Provenance line + the status line VERBATIM** as the opening statement of record:
     `measurement banked; amendment drafted, not posted; code constant unchanged; fire HELD; an agent never posts and never fires`
     Adopted from his §0. No OSF contact has occurred; `_OCCLUSION_ANOMALY_FRACTION` is still
     0.0005 in code.

  2. **§2 — fixed (his blocking item).** 1.18x is now stated in the document as a COUNT ratio that
     does NOT convert between the two percentages; the measured ratio of the two medians is
     **1.12x** (0.1888% row / 0.1685% site), with `fraction ratio = count ratio x (n_sites/n_rows)`
     and region 1's 102,421/96,708 = 1.059 shown. Both the row median and the fraction ratio are
     now ledger slots with an arithmetic identity, so the stated ratio cannot drift from its
     operands.

  3. **§3 — pre-empted.** One sentence, in the document, saying the adopted ceiling is more
     permissive than the rejected 10x-withdrawn candidate and that the objection to 8x/10x is
     selection-for-clearing-the-sample rather than magnitude.

  4. **§4 — ADOPTED, not disclosed.** Give him the number he deliberately declined to pick, with
     its derivation: measured inflation across the 21 regions min **1.04x** / median **1.14x** /
     max **1.79x** / robust sigma **0.0890x**; companion ceiling = 3 x median = **3.42x**, by the
     same location-statistic rule as the main ceiling, margin **1.91x** over the observed max.
     Clause (d) now defers on EITHER the site fraction above 0.5056% (site basis) OR the
     occluded-site row/site inflation above 3.42x; accounting stays on rows; site fraction, row
     count and inflation are all reported per region; no new branch — both routes go to
     `BRANCH_AFR_OCC_DEFERRED`. State that it is a ledger slot, not a hardcoded literal, as he
     asked. Note the one thing he should check: the reported summary figure is the sample MEAN
     1.18x while the gate is anchored on the MEDIAN 1.14x, and the document says so explicitly.

  5. **§6 — homed.** Substance in the amendment's limitation paragraph; the note's exact repo path
     in the NOT-YET-APPENDED deviations entry. Say why the path is not in the public text: an OSF
     record must be self-contained and the note is an internal record.

  6. **The guard extension, one short block.** Strictly additive, own commit, ZERO deleted lines in
     the diff; three new identities plus an inflation ordering check at a stated x-ratio tolerance
     of 0.01; the pre-existing 0.02 tolerance untouched; each new identity seen RED on a perturbed
     copy; his previous controls re-run against the extended guard and reproducing their signature
     strings; and the 2x2 matrix (old guard x old file GREEN, new guard x old file RED, old guard x
     new file RED, new guard x new file GREEN).

  7. **New anchors.** `wc -c` and `md5sum` of the amendment, **computed AFTER Task 2's commit,
     never guessed**, alongside the previous 31,685 B / b8f9a978c9bdbc7892f97b5d90cf9d27 so he can
     see the file moved. Name the travelling path. **Also name the pre-execute gate commit and the
     fact that it ADVANCED**: it now reads the HEAD captured before the revising task's commit and
     supersedes `8638ed3` from the first instantiation, because the row's own standing instruction
     is to re-read HEAD and update the value if the branch has advanced — which it has. Give the
     short form of both hashes so he can see the move, and state that both of its occurrences
     (SLOT_LEDGER line and pre-paste table row) moved together by construction, since the same
     drift-apart risk he flagged for `POSTING_DATE` applies to this row too.

  8. **The ask — narrow.** A final pass on ONLY the changed passages: the basis-conventions
     paragraph, the no-calibrate-to-pass paragraph, the *Ceiling* + *Companion condition*
     sub-paragraphs, the limitation paragraph, and the deviations-block collinearity line. Not a
     re-read of the whole document.

Compute the anchors and write them in, then commit:
```
git add .planning/debug/260820-COURIER-TO-SETH-revision-reply.md
git commit -m "docs(quick-260820-u6i): reply courier to Seth — count-vs-fraction fix, permissiveness pre-emption, 3.42x companion gate adopted (1.91x margin), collinearity note homed, guard extension + refreshed anchors"
```
Finally re-run `guard all` (this task touches nothing in the amendment, so it is a no-change
confirmation) and record the exit code in the SUMMARY.
  </action>

  <verify>
    <automated>
set -uo pipefail
C=.planning/debug/260820-COURIER-TO-SETH-revision-reply.md
N=.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
G=.planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh
test -f "$C" || { echo "FAIL courier missing"; exit 1; }
test "$(wc -l < "$C")" -le 50 || { echo "FAIL courier over the 50-line cap"; exit 1; }
grep -q 'measurement banked; amendment drafted, not posted; code constant unchanged; fire HELD; an agent never posts and never fires' "$C" \
  || { echo "FAIL status line not verbatim"; exit 1; }
for v in 1.12x 3.42x 1.91x 1.14x 1.79x 1.04x 0.0890x; do
  grep -q -- "$v" "$C" || { echo "FAIL courier missing $v"; exit 1; }
done
grep -qi 'count ratio' "$C" || { echo "FAIL courier does not state the count-ratio fix"; exit 1; }
grep -qi 'more permissive' "$C" || { echo "FAIL courier does not report the permissiveness pre-emption"; exit 1; }
grep -q 'note-same-position-collinearity-2026-08-19.md' "$C" || { echo "FAIL courier does not name the note path"; exit 1; }
grep -q 'BRANCH_AFR_OCC_DEFERRED' "$C" || { echo "FAIL courier does not state the no-new-branch fact"; exit 1; }
grep -qi 'zero deleted lines\|0 deleted lines' "$C" || { echo "FAIL courier does not report the additions-only guard diff"; exit 1; }
grep -q '0.01' "$C" || { echo "FAIL courier does not state the x-ratio tolerance"; exit 1; }
grep -q 'osf-amendment-occlusion-gate-recalibration-2026-08-20.md' "$C" || { echo "FAIL travelling file not named"; exit 1; }
grep -q 'b8f9a978c9bdbc7892f97b5d90cf9d27' "$C" || { echo "FAIL prior anchor absent — he cannot see the file moved"; exit 1; }
ANCHOR=$(md5sum "$N" | cut -c1-32); grep -q "$ANCHOR" "$C" || { echo "FAIL md5 anchor absent or stale"; exit 1; }
BYTES=$(wc -c < "$N" | tr -d ' ');  grep -q "$BYTES"  "$C" || { echo "FAIL byte anchor absent or stale"; exit 1; }
PEC=$(grep -oE '^  PRE_EXECUTE_COMMIT = [0-9a-f]{40}$' "$N" | awk '{print $3}')
test -n "$PEC" || { echo "FAIL cannot read PRE_EXECUTE_COMMIT from the amendment ledger"; exit 1; }
test "$PEC" != "8638ed37c1431ea73566fd03ad1541ba95416fe4" || { echo "FAIL pre-execute gate commit never advanced"; exit 1; }
grep -q "$(echo "$PEC" | cut -c1-7)" "$C" || { echo "FAIL courier does not name the advanced pre-execute gate commit"; exit 1; }
grep -q '8638ed3' "$C" || { echo "FAIL courier does not name the superseded gate commit — the advance is invisible"; exit 1; }
git ls-files --error-unmatch "$C" >/dev/null 2>&1 || { echo "FAIL courier untracked"; exit 1; }
git status --porcelain "$N" | grep . && { echo "FAIL amendment modified after its anchors were taken"; exit 1; }
bash "$G" all "$N" >/dev/null; test $? -eq 0 || { echo "FAIL guard no longer green"; exit 1; }
git status --porcelain .planning/osf_deviations.md .planning/STATE.md .planning/ROADMAP.md | grep . \
  && { echo "FAIL a forbidden file was modified"; exit 1; }
echo "TASK3 VERIFY OK"
    </automated>
  </verify>

  <done>
The courier exists, is at most 50 lines, opens with Seth's status line verbatim, reports each of
his four asks with the new numbers (1.12x fraction ratio; 3.42x companion ceiling from the 1.14x
median with 1.91x margin over the 1.79x observed max; the collinearity note's exact path and why
it is not in the public text; the no-new-branch fact), reports the guard extension as
additions-only with a zero-deletion diff and a stated 0.01 x-ratio tolerance whose identities were
seen red, names the travelling file with fresh `wc -c` + `md5sum` anchors that match the committed
amendment byte-for-byte alongside the superseded 31,685 B / b8f9a978 anchors, names the advanced
pre-execute gate commit beside the superseded `8638ed3`, and asks for a final pass on ONLY the
changed passages. `guard all` exit 0; nothing forbidden was modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| chat -> repo | Seth's attack arrives as relayed text with no byte anchor, and its asks become edits to a public-record artifact. |
| draft -> OSF | The instantiated text is one Carter action away from a permanent public record; nothing downstream re-checks it. |
| count basis -> fraction basis | The exact defect being fixed: a ratio measured on counts asserted about fractions whose denominators also differ. |
| site metric -> row reality | A multiplicity-invariant metric is multiplicity-blind; the substrate's real exclusions are rows. |
| guard -> guard' | An extension to the enforcer is a change to the thing that decides whether everything else is trustworthy. |
| scratch -> tracked tree | Perturbed-number control copies exist on disk during every negative control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-u6i-01 | Tampering | `260819-u8d-placeholder-guard.sh` — an extension silently loosens an existing check, so a broken document re-greens | mitigate | Additions-only by construction (`ROSTER+=`, `NEEDED +=`, insertion before `sys.exit`), MEASURED by `git diff --numstat` deleted-lines == 0 on a guard-only commit; `TOL_RATIO = 0.02` and the fail-closed `*)` arm asserted still present; REG-1..REG-6 rebased onto the 21-slot fixture reproduce the s2x/u8d exit codes and signature strings. |
| T-u6i-02 | Spoofing | A hand-typed number in the amendment claiming script provenance | mitigate | `--second-pass` asserts every non-sentinel **Class-M** slot's existing ledger value is byte-identical to the value the engine just computed from the banked records; the 11 pre-existing Class-M values are diffed against the pre-revision blob (Class-P is argv-sourced and force-substituted, and is asserted to EQUAL the hash the task passed rather than to be unchanged); the census requires all 7 new x-literals present. |
| T-u6i-03 | Repudiation | The fix prose is written but describes a relation the numbers do not hold — the very defect class Seth found, recurring inside its own correction | mitigate | `FRACTION_RATIO_X` is a ledger slot with the identity `== ROW_MEDIAN_PCT / SITE_MEDIAN_PCT` at tol 0.01, and `ROW_MEDIAN_PCT` is itself script-sourced, so the claimed ratio cannot drift from its operands; likewise `INFLATION_CEILING_3X_X == 3*INFLATION_MEDIAN_X` and `INFLATION_MARGIN_X == ceiling/max`. |
| T-u6i-04 | Information disclosure | A scratch control copy carrying perturbed numbers reaches the tracked tree and is later read as real | mitigate | Two distinct mandatory banners on scratch copies; a FIRST-LINE scan (not a substring grep, which the PLAN and SUMMARY prose would false-positive) over every tracked `.planning/*` file; all controls confined to the session scratchpad. |
| T-u6i-05 | Denial of service (of the detector) | The companion ceiling is set where nothing can ever fire — calibrate-to-pass, reintroduced through the back door | mitigate | Derived by the pre-registered `3 x location statistic` rule that produced the main ceiling, from a distribution measured before the multiplier was chosen; the 1.91x margin and the 0/21 outcome are stated as CONSEQUENCES; the prohibition on re-widening without a further amendment is carried explicitly to the companion. |
| T-u6i-06 | Elevation of privilege | An agent posts to OSF or fires the panel run | mitigate | The DRAFT-NOT-POSTED banner is untouched and asserted present; Seth's status line ("an agent never posts and never fires") is carried verbatim in both the amendment and the courier; scope forbids OSF contact; verify asserts `src/ tests/ config/ Snakefile` untouched and `osf_deviations.md` / `STATE.md` / `ROADMAP.md` unmodified. |
| T-u6i-07 | Tampering | A pseudo-code block in the new clause-(d) text matches `^  [A-Z0-9_]+ = ` and silently inflates the guard's ledger-line count | mitigate | R9 mandates four-space indentation for any pseudo-code; the verify asserts the ledger-pattern line count is exactly 21. |
| T-u6i-08 | Repudiation | A future session validates this 21-slot document with the stale 13-slot guard and reads a spurious result | mitigate | MATRIX-B pins the stale guard as RED on the revised file, transcribed verbatim; the guard CHANGELOG records the extension and points at the transcript. |
</threat_model>

<verification>
Run from the repo root after all three tasks:

```bash
N=.planning/amendments/osf-amendment-occlusion-gate-recalibration-2026-08-20.md
G=.planning/quick/260819-u8d-record-occlusion-recalibration-adoption-/260819-u8d-placeholder-guard.sh
T=.planning/quick/260820-u6i-revise-the-instantiated-amendment-per-se/260820-u6i-guard-transcript.txt

# the gate
bash "$G" all "$N"; echo "EXIT=$?"                       # must be 0
grep -c -E '^  [A-Z0-9_]+ = ' "$N"                       # must be 21
grep -c '{{' "$N"                                        # must be 0

# the evidence
grep -c '^### ' "$T"                                     # >= 20 sections
grep -c '^EXIT=1' "$T"                                   # >= 17 reds
grep -c '^EXIT=0' "$T"                                   # >= 5 greens

# additions-only, measured not asserted
GC=$(git log --format=%H -1 -- "$G"); git diff --numstat "$GC^" "$GC" -- "$G"   # deletions field == 0

# the six supporting records the pre-paste checklist now requires
for f in 260819-SETH-VERDICT-adjudication-confirmed-as-received.md \
         260819-SETH-C1C2C3-convergence-as-received.md \
         260819-occ-measure-sweep-results-as-received.md \
         260819-supplement-results-as-received.md \
         260820-site-basis-sweep-results-as-received.md \
         260820-SETH-ATTACK-instantiated-amendment-as-received.md; do
  git ls-files --error-unmatch ".planning/debug/$f" >/dev/null || echo "MISSING $f"
done

# nothing fired, nothing posted, nothing forbidden moved
grep -rn '_OCCLUSION_ANOMALY_FRACTION' src config 2>/dev/null | grep -v '0.0005' # must be empty
git status --porcelain .planning/osf_deviations.md .planning/STATE.md .planning/ROADMAP.md \
  .planning/amendments/note-same-position-collinearity-2026-08-19.md              # must be empty
git status --porcelain tests/m3/sparse_parent_benchmark.tsv                       # must be unstaged
```
</verification>

<success_criteria>
- [ ] `guard all` exits 0 on the revised amendment; 21 filled ledger lines; zero surviving sentinels.
- [ ] The guard's own commit shows `0` in the `git diff --numstat` deletions field; `TOL_RATIO = 0.02` and the fail-closed `*)` arm are still present.
- [ ] Each of the three new identities and the new ordering check has been seen RED in isolation on a perturbed copy (NC-A..NC-D), and the new pattern arm and roster growth have been seen load-bearing (NC-E, NC-F).
- [ ] The engine's pre-registered rendered-string expectations have been seen RED (NC-G) against a perturbed banked column.
- [ ] REG-2 and REG-3 reproduce the s2x NC-2/NC-3 signature strings against the extended guard, rebased onto the 21-slot fixture; REG-1's exit codes reproduce with its message change recorded as known; REG-4/5/6 confirm the vacuity and quote sections are unweakened.
- [ ] The 2x2 guard/document matrix is complete and transcribed: GREEN / RED / RED / GREEN.
- [ ] All 8 new values entered through the engine from the two banked records; the 11 pre-existing Class-M values are byte-unmoved.
- [ ] Class-P force-substitution ran: `PRE_EXECUTE_COMMIT` advanced to the HEAD before Task 2's commit at BOTH occurrences, the superseded hash is gone from the file, `POSTING_DATE` still occurs exactly 3 times, and the courier names the advanced hash.
- [ ] §2: 1.18x named a COUNT ratio, explicitly non-converting, with the measured 1.12x fraction ratio and its mechanism — and no `0.1988%` anywhere.
- [ ] §3: the permissiveness comparison is pre-empted in the document, row against row, with no `0.5000%`.
- [ ] §4: clause (d) is a disjunction with a 3.42x companion ceiling derived by the same rule, 1.91x margin, mean-vs-median anchor disambiguated, all three quantities reported per region, and no fourth branch.
- [ ] §6: collinearity substance inside the paste block, exact repo path outside it, and the path NEVER inside it.
- [ ] Seth's status line verbatim in both the amendment and the courier.
- [ ] Census: exemption set still exactly `{0.0068%, 0.1234%}`; no unclassified 4-decimal percentage; all 7 new x-literals present; no forbidden literal.
- [ ] Courier <= 50 lines with anchors matching the committed amendment byte-for-byte, alongside the superseded anchors.
- [ ] Three commits (guard-only; engine + attack + transcript; amendment + transcript; courier — four total, the guard's being separate), all `docs(quick-260820-u6i): ...`, all explicit-path staged.
- [ ] No OSF contact, no code change, no fire.
</success_criteria>

<output>
After completion, create
`.planning/quick/260820-u6i-revise-the-instantiated-amendment-per-se/260820-u6i-SUMMARY.md`.

It MUST carry:
1. **The revision register** — all 17 items with line numbers, verdicts, and verbatim BEFORE/AFTER for every CHANGED item.
2. **The full `--second-pass` SUBSTITUTION LEDGER stdout**, verbatim (21 rows, each marked SUBSTITUTED or VERIFIED-IN-PLACE).
3. **The `git diff --numstat` line for the guard commit**, verbatim, as the additions-only evidence.
4. **The control table** — every section token with its exit code and the signature string observed, plus the two documented behaviour changes (REG-2/REG-3 rebasing; REG-1's arith message).
5. **The completed 2x2 guard/document matrix.**
6. **The census output**, verbatim.
7. **Decisions recorded with rationale:** the `INFLATION_ROBUST_SIGMA_X` rename and its 4-dp render; the collinearity note's split home (substance public, path internal); the refusal to add a `ROW_CEILING_3X_PCT` slot for `0.5664%`; and the Class-P force-substitution outcome — the old and new `PRE_EXECUTE_COMMIT` hashes, why
   reading (b) is not a choice but the row's own standing instruction, and confirmation that both
   occurrences moved together.
8. **Old and new byte anchors** for the amendment.
9. **A closing status line**, verbatim: `measurement banked; amendment drafted, not posted; code constant unchanged; fire HELD; an agent never posts and never fires`.
</output>
</content>
</invoke>
