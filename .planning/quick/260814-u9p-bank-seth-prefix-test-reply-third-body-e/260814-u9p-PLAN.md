---
phase: quick-260814-u9p
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [U9P-1, U9P-2, U9P-3]
files_modified:
  - .planning/quick/260814-guk-seth-update-2-reply-remediation-fix-inva/260814-guk-verify.sh
  - .planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-verify.sh
  - .planning/osf_deviations.md
  - .planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-SETH-REPLY-VERBATIM.md
  - .planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-REPLY-TO-SETH.md

must_haves:
  truths:
    - "A 64-char sha256 no longer trips the hex-run invariant, and a 63-char one still does — both observed, not assumed."
    - "The 260814-guk fire gate still returns 10/10 ALL CHECKS PASSED after the widening."
    - "The ledger records that the posted trsx5 body is an UNEXPLAINED THIRD BODY, with the STOP verdict explicitly unchanged."
    - "The tgf 'most plausibly Seth's lineage' reading is on the record as FALSIFIED, dated, with the original wording preserved."
    - "Seth's reply is banked byte-exact as a courier-in record, and a reply exists that asks for the never-arrived 9,907-B courier re-send."
    - "No network contact, no perimeter contact, nothing fired, $0."
  artifacts:
    - path: ".planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-verify.sh"
      provides: "Re-runnable gate: controls | ledger | reply | all"
      min_lines: 120
    - path: ".planning/osf_deviations.md"
      provides: "RECHARACTERIZED 2026-08-14 sub-entry, pure append"
      contains: "UNEXPLAINED THIRD BODY"
    - path: ".planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-SETH-REPLY-VERBATIM.md"
      provides: "Courier-in record, byte-exact as written by the orchestrator"
      contains: "--- VERBATIM BODY BEGINS ---"
    - path: ".planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-REPLY-TO-SETH.md"
      provides: "Reply covering (a)-(e)"
      min_lines: 60
  key_links:
    - from: "260814-u9p-verify.sh controls"
      to: "260814-guk-verify.sh _hexlen"
      via: "sub-mode invocation on synthetic control files"
      pattern: "_hexlen"
    - from: "RECHARACTERIZED sub-entry"
      to: "ADJUDICATED sub-entry (50dc51d)"
      via: "adjacency + explicit FALSIFIED back-reference"
      pattern: "ADJUDICATED 2026-08-14"
---

<objective>
Bank Seth's prefix-test reply and act on its two consequences.

Seth ran the ask-#1 test against his own 9,907-byte body and the answer is **NO**. Combined
with our own negative prefix test, **both lineages are falsified as the posted body's source
by tail-truncation**. The posted 9,695-byte trsx5 body is an **UNEXPLAINED THIRD BODY**, not a
truncation of anything either of us holds. The STOP verdict is untouched by this — it
adjudicated on size alone — but our tgf *reading* ("most plausibly Seth's lineage") is
falsified, the ledger says "truncated" where it should say "unexplained third body", and
Seth's second deliverable (his complete 9,907-B body, base64, sha256-anchored) **never arrived
in our records**.

Purpose: keep the public-facing record honest at the moment the evidence moved against our own
reading, and unblock legitimate sha256 anchors in the invariant that will receive them.

Output: one widened + control-paid invariant, one dated ledger recharacterization (pure
append), one courier-in record, one reply.

**HELD, unchanged by this task:** the fire and the obligation-(2) posting. Nothing here fires
anything. $0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-SETH-REPLY-VERBATIM.md
@.planning/quick/260814-guk-seth-update-2-reply-remediation-fix-inva/260814-guk-verify.sh
@.planning/quick/260814-tgf-bank-trsx5-byte-check-adjudication-stop-/260814-tgf-COURIER-ADDENDUM-TO-SETH.md

Ledger tail (the ADJUDICATED sub-entry this task appends next to): `.planning/osf_deviations.md`
lines 190-252, landed at `50dc51d`. Read it before Task 2 — do not re-read the whole 330-line file.

**⛔ DO NOT TOUCH (this task):** `.planning/STATE.md`, `.planning/ROADMAP.md` (the orchestrator
layers those), and the §6b card text in
`.planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/260812-ox1-{READY-TO-FIRE,AGENT-PROMPT,BROWSER-PASTE}.md`.
</context>

<facts>
## ⚠ HASH TABLE — TRANSCRIBE FROM HERE. NEVER RE-DERIVE, NEVER RE-TYPE FROM MEMORY.

Every value below was length-checked firsthand by the planner on 2026-08-14 (~21:50 EDT).
An md5 is **32** characters. Copy-paste; do not retype. If any value you are about to write is
not 32 characters, **STOP** — that is the exact defect class this task exists to remove.

| # | Value | len | What it is |
|---|-------|-----|------------|
| H1 | `c19be8b2ad7cd6a45fee1d668d8a9cf9` | 32 | **posted** trsx5 body (9,695 B), Carter's authenticated download |
| H2 | `425d925a88ab474ec2396cbea25e665c` | 32 | Seth's complete lineage (9,907 B) — we do **not** hold the body |
| H3 | `a81c22d95e7b83488c015357445f3482` | 32 | `head -c 9695` of **Seth's** 9,907-B body → **≠ H1** (ask-#1 = NO) |
| H4 | `6b75e660e52413e4cbec116f315590b6` | 32 | `head -c 9695` of **our** 9,758-B canonical → **≠ H1** (tgf, negative) |
| H5 | `28ecdb3160833da80cfa25952f76415b` | 32 | repo-canonical paste block (9,758 B) @ `ac4c990` |
| H6 | `c19e8b2ad7cd6a45fee1d668d8a9cf9` | **31** | the historical DEFECT literal. Quote only where labelled 31. |
| X1 | `40831cdebcc71de21cd536fa…` | 24 + ellipsis | ⚠ **DISPLAY-TRUNCATED sha256. NOT AN ANCHOR. NEVER USABLE FOR VERIFICATION.** Write it **only** with its `…` and an adjacent do-not-use warning. Never pad it, never complete it, never present it bare. |

Numbers (transcribe, do not recompute): 9,695 / 9,758 / 9,907 / 9,912 B; deltas **212**
(9,907 − 9,695) and **149** (9,907 − 9,758); sweep ranges **9,600–9,919** bytes, **51** line
boundaries, **1–25**-line contiguous blocks; draft gate/date combinations **5fd58a5** and
**0f3c68b** × **2026-07-10** and **2026-07-04**. Commits: `50dc51d` (ledger ADJUDICATED append),
`de49638` (tgf courier addendum), `ad5f8ea` (tgf closeout = HEAD at plan time), `ac4c990`
(canonical anchor commit).

## The findings this task records (from Seth's reply, verbatim in intent)

- **F-1 — ask #1 is NO.** Seth re-verified his own body (H2) first, then `head -c 9695` of it →
  **H3**, which is **not** H1.
- **F-2 — both prefix tests are negative.** Ours (H4 ≠ H1) and his (H3 ≠ H1). Neither lineage
  yields the posted body by tail-truncation.
- **F-3 — exhaustive sweep, all negative** (Seth, against his own lineage): byte-prefix at every
  length 9,600–9,919; line-prefix at all 51 line boundaries; whitespace normalizations at full
  length (CRLF, trailing-whitespace strip, blank-run collapse, trailing-newline add/remove);
  single-line deletion; contiguous 1–25-line block deletion at every offset (**not one such
  candidate even produces a 9,695-byte body**); his earlier draft's 9,912-B paste region and its
  placeholder-fill variants (5fd58a5/0f3c68b × 2026-07-10/2026-07-04). **Finding: the posted body
  is an UNEXPLAINED THIRD BODY.** The 212-byte delta is **unexplained, not mislocated**. Our tgf
  reading "most plausibly belongs to Seth's lineage" is **FALSIFIED — by Seth, against his own
  side**, which is the honest direction.
- **F-4 — the STOP verdict is UNCHANGED.** It adjudicated on **size alone** (9,695 ∉ {9,758,
  9,907}) and is untouched by the recharacterization. **Fire and obligation-(2) remain HELD.**
- **F-5 — the ask-#2 courier NEVER ARRIVED.** `seth_courier_9907_body_for_hpc.md` is absent from
  our records: the orchestrator grepped `.planning/` for both the **filename** and the sha256
  prefix `40831cdebcc71de21cd536fa…` (X1) — **zero hits for either**; H2 appears only ever as a
  *reference value*, never as a body. Re-send required, with the **full 64-character** sha256
  travelling **with** the body.
- **F-6 — the decisive artifact** is Carter's downloaded 9,695-B posted body itself. Seth holds
  both lineages and can diff it against both; we replicate independently once the 9,695-B body
  **and** the re-sent 9,907-B courier are both in-repo (**size-first on arrival**: 9,695 / H1).
- **F-7 — sequencing, a STANDING RECOMMENDATION (Carter has NOT formally decided):** read the
  posted body first → both sides characterize independently → adjudicate the true complete body →
  re-post as a **NEW OSF VERSION** (never a silent swap) → record URL / timestamp / bytes / md5 →
  only then does the fire unhold.
</facts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the hex-run invariant to {32, 64} and pay for it with negative controls</name>
  <files>
    .planning/quick/260814-guk-seth-update-2-reply-remediation-fix-inva/260814-guk-verify.sh
    .planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-verify.sh (new)
  </files>

  <behavior>
    Observed-before-and-after, using the shipped `_hexlen` sub-mode (never a re-implementation):
    - BEFORE the edit: a file containing a 64-char hex run is **RED**. (Capture this transcript —
      it is the evidence that the widening is real and not a no-op.)
    - AFTER the edit: 31-char run **RED**, 63-char run **RED**, 32-char run **GREEN**,
      64-char run **GREEN**.
    - AFTER the edit: `bash 260814-guk-verify.sh fire` is **10/10 ALL CHECKS PASSED**.
    - `260814-u9p-verify.sh ledger` and `... reply` are expected **RED** at this commit (their
      artifacts land in Tasks 2 and 3). Record that RED — it is the non-vacuity evidence for the
      greens that follow.
  </behavior>

  <action>
**1a — capture the pre-edit RED.** Make a temp dir off-repo (`CTL="$(mktemp -d)"`, which honours
`$TMPDIR`; never inside the repo, never committed). Build the control strings with a **self-checking
premise** — do not hand-count:

```bash
s64="$(printf 'deadbeef%.0s' $(seq 1 8))"   # synthetic, obviously not a real anchor
s63="${s64%?}"
s32='28ecdb3160833da80cfa25952f76415b'      # H5, transcribed
s31='c19e8b2ad7cd6a45fee1d668d8a9cf9'       # H6, the historical 31-char defect
for pair in "64:$s64" "63:$s63" "32:$s32" "31:$s31"; do
  want="${pair%%:*}"; s="${pair#*:}"
  [ "${#s}" = "$want" ] || { echo "CONTROL PREMISE WRONG: expected $want got ${#s}"; exit 1; }
done
```

Write each into `$CTL/len{31,32,63,64}.txt` (one run per file, plus a one-line `# synthetic
control — not an anchor` comment; the comment must contain no hex run ≥20 chars). Run the
**unmodified** guk script against `len64.txt`:
`bash .planning/quick/260814-guk-seth-update-2-reply-remediation-fix-inva/260814-guk-verify.sh _hexlen "$CTL/len64.txt"`
→ must exit 1. Paste that exact failure text into the SUMMARY. If it exits 0, **STOP and report**
(the invariant was already widened by someone else and this task's premise is wrong).

**1b — the widening (the only production edit in this plan).** In `260814-guk-verify.sh`:
- `hexlen_bad()` (~line 66): flag a hex run of ≥20 chars only when its length is **neither 32 nor
  64** — i.e. `if (length($0) != 32 && length($0) != 64)`. Nothing else in the function changes.
- Fix the two now-false message strings so the script does not ship a statement it has made
  untrue: the `_hexlen` PASS line (~line 86) and F1's PASS line (~line 123) currently say
  "is exactly 32"; make them read "is 32 (md5) or 64 (sha256)". Fix the corresponding FAIL text
  ("are not 32 chars" → "are neither 32 (md5) nor 64 (sha256)").
- Add a dated changelog block to the file header (below the existing WHY-F1 rationale, above
  `set -uo pipefail`), stating: **widened 2026-08-14 (`quick-260814-u9p`) per Seth's correction —
  a 64-char sha256 anchor is legitimate and the pre-widening invariant would have rejected one.
  Truncating a sha256 to 32 characters to satisfy the old rule is FORBIDDEN: it manufactures the
  exact silent-mismatch class this invariant exists to catch.** Also note there that `P6`
  (section `reply`) carries its own inline allow-list (32 or 40, plus the labelled 31-char
  exemption) scoped to the **guk-era** reply document and is **deliberately untouched** by this
  change.
- **Do not** touch F2/F3/F4/F5, the `MD5_*`/`BAD31` constants, or any section body.

**1c — write `260814-u9p-verify.sh`** in the u9p task dir, same shape as the guk script
(`HERE`/`ROOT` derivation, `pass`/`fail`/`RC`, `usage: {controls|ledger|reply|all}`, exit 0/1).
Header comment must state: `$0`, read-only, **no network, no OSF, no perimeter**. Modes:

- **`controls`** — rebuilds the four control files (with the same premise assertion; failure of an
  assertion is a FAIL, never a skip), runs the shipped `_hexlen` sub-mode against each, and
  requires **31 RED, 63 RED, 32 GREEN, 64 GREEN**; then runs
  `bash <guk>/260814-guk-verify.sh fire` and requires exit 0 **and** the literal
  `ALL CHECKS PASSED`; then greps `hexlen_bad()`'s body in the guk script and requires both `32`
  and `64` appear in its length condition (so a silent revert goes red structurally too).
  Cleans up `$CTL` via `trap ... EXIT`.
- **`ledger`** and **`reply`** — the checks specified in Tasks 2 and 3. Write them **now**, in this
  commit, so they are observed RED before they are observed GREEN.

**1d — gate and commit.** Run `bash 260814-u9p-verify.sh controls` (must be all-PASS) and
`bash .../260814-guk-verify.sh fire` (must be **10/10 ALL CHECKS PASSED**). Then run
`260814-u9p-verify.sh ledger` and `... reply` and record their RED output.
**If the `fire` section regresses for any reason: STOP and report. Do not improvise a fix.**
The `record` and `reply` sections of the *guk* script are out of scope here (the orchestrator is
editing STATE.md concurrently, which they read); do not gate on them, but report anything alarming.

Stage by **explicit paths only** (the two files above — never `git add .`/`-A`) and commit:
`fix(260814-u9p): widen hex-run invariant to {32, 64} — sha256 anchors are legitimate; controls 31/63 RED, 32/64 GREEN; fire 10/10`
GPFS "invalid object / Error building trees" → **STOP and report**. Do **not** push.
  </action>

  <verify>
    <automated>bash .planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-verify.sh controls && bash .planning/quick/260814-guk-seth-update-2-reply-remediation-fix-inva/260814-guk-verify.sh fire</automated>
  </verify>

  <done>
    `controls` exits 0 with 31 RED / 63 RED / 32 GREEN / 64 GREEN and the structural
    `hexlen_bad` check; the guk `fire` section is 10/10 ALL CHECKS PASSED; the pre-edit 64-char
    RED transcript is captured in the SUMMARY; `ledger`/`reply` observed RED; one commit, no push,
    no temp files committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append the dated RECHARACTERIZED sub-entry to the OSF deviations ledger</name>
  <files>
    .planning/osf_deviations.md
    .planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-verify.sh (ledger checks, if any refinement is needed)
  </files>

  <action>
**PURE APPEND.** Insert a new sub-entry immediately **after** the `ADJUDICATED 2026-08-14` block
(which ends at the `- **Provenance.**` bullet, before the `---` separator preceding
`### REQ-AOU-LD-VALIDATION Check 2 redefined...`), so the two sit adjacent in the trsx5 section.
**Zero pre-existing lines may be deleted or reworded** — including the now-falsified reading, which
stays on the record verbatim (project norm: preserve, date, supersede; never rewrite history).

Heading: `### RECHARACTERIZED 2026-08-14 — the posted trsx5 body is an UNEXPLAINED THIRD BODY (both lineages falsified)`

Required content, in this order, with every hash transcribed from the HASH TABLE:

1. **STATUS line.** The **STOP verdict of the ADJUDICATED sub-entry above stands UNCHANGED** — it
   adjudicated on **size alone** (9,695 ∉ {9,758, 9,907}) and nothing here disturbs it. What
   changes is the **characterization**, not the verdict. **Fire HELD. Obligation-(2) posting HELD.**
2. **F-1 verbatim** — Seth's ask-#1 answer is **NO**: he re-verified his own 9,907-B body (**H2**)
   first, then `head -c 9695` of it → **H3**, which is **not** the posted **H1**. Show it as a
   fenced block in the same shape the ADJUDICATED entry uses for its own prefix test.
3. **F-2** — both prefix tests are negative: ours **H4 ≠ H1** (tgf, our 9,758-B canonical **H5**)
   and his **H3 ≠ H1**. **Neither lineage yields the posted body by tail-truncation.**
4. **F-3** — Seth's exhaustive sweep, listed item by item with its numbers (9,600–9,919; 51 line
   boundaries; the four whitespace normalizations; single-line deletion; contiguous 1–25-line block
   deletion at every offset, **none of which even produces a 9,695-byte body**; the 9,912-B draft
   paste region and its placeholder-fill variants 5fd58a5/0f3c68b × 2026-07-10/2026-07-04). State
   the conclusion **as a finding**: the posted body is an **unexplained third body**; the **212-byte
   delta is unexplained, not mislocated**.
5. **FALSIFICATION, explicit and dated.** The tgf sub-entry's *reading* — "the posted body most
   plausibly belongs to Seth's 9,907-byte lineage" — is **FALSIFIED as of 2026-08-14**, by Seth,
   against his own side. Quote the falsified phrase so a reader can find it above, and state
   plainly that the original wording is **preserved unaltered** in the ADJUDICATED sub-entry
   (`50dc51d`). Note that it was labelled a *reading, not a finding* at the time, and that the
   label held.
6. **What is now open.** The 149-B lineage delta remains unreconciled **and** is no longer the only
   open question: a body neither party holds was posted, and no mechanism explains it. Say the
   honest thing — *unexplained* — rather than offering an unsupported mechanism.
7. **Decisive artifact (F-6).** Carter's downloaded 9,695-B posted body. Seth holds both lineages
   and can diff against both; we replicate independently once the 9,695-B body and the re-sent
   9,907-B courier are in-repo, **size-first on arrival** (9,695 / **H1**).
8. **Next step (F-7) — ⚠ labelled `RECOMMENDATION ONLY, NOT A DECISION. Carter has not decided.`**
   read the posted body first → both sides characterize independently → adjudicate the true
   complete body → re-post as a **NEW OSF VERSION**, never a silent swap → record URL / timestamp /
   bytes / md5 here → only then does the fire unhold.
9. **Vocabulary correction.** "The truncated post" is retired as a description; **"unexplained
   third body"** is the phrasing from here — mirroring Seth's own correction of his framing.
10. **Provenance.** Banked by `quick-260814-u9p` on 2026-08-14 from Seth's couriered reply
    (verbatim record at
    `.planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-SETH-REPLY-VERBATIM.md`).
    Seth's measurements are **his**, unreproduced by us (we do not hold his body). **No agent
    contacted OSF. Zero perimeter contact. $0. Nothing fired.** Appended, never rewritten.

⚠ **X1 must not appear in this ledger entry at all** — it is a display-truncated sha256 with no
verification value, and the ledger is a public-facing record. It belongs only in the reply
(Task 3), where it is quoted with its ellipsis and its do-not-use warning.

Then implement/confirm the `ledger` mode of `260814-u9p-verify.sh`:
- **L1 (append-only, the load-bearing one):** `git diff --numstat 50dc51d -- .planning/osf_deviations.md`
  → **deletions must be 0**. If `git cat-file -e 50dc51d^{commit}` fails, **FAIL loudly** ("the pin
  cannot be evaluated"), never silently pass.
- **L2:** the new heading exists, contains `RECHARACTERIZED` and `2026-08-14`, and its line number
  is **greater** than the `### ADJUDICATED 2026-08-14` heading's (adjacency/ordering).
- **L3 (non-vacuity):** the extracted block (new heading → next `^### ` or `^---`) is ≥ 25 lines,
  else FAIL — an empty block would pass every content check trivially.
- **L4:** the block carries **all five** md5 literals H1-H5, and every hex run ≥20 chars inside it
  is 32 or 64 (reuse the widened logic via `260814-guk-verify.sh _hexlen` on the extracted block
  written to a temp file, so this check exercises the shipped code path).
- **L5:** the block contains `UNEXPLAINED THIRD BODY` (case-insensitive ok), `FALSIFIED`,
  `RECOMMENDATION`, `NEW OSF VERSION` (or `new OSF version`), and the sweep tokens `9,600`, `9,919`,
  `9,912`, `5fd58a5`, `0f3c68b`, `212`.
- **L6:** the block asserts the STOP verdict is unchanged — matches `grep -iE 'stands? UNCHANGED|verdict.*unchanged'`.
- **L7 (preservation):** the ADJUDICATED block above **still** contains
  `most plausibly belongs to Seth's` — proving the falsified reading was preserved, not deleted.
- **L8:** X1 (`40831cdebcc71de21cd536fa`) appears **0 times** in `.planning/osf_deviations.md`.

Gate on `bash 260814-u9p-verify.sh ledger` (all-PASS) and re-run `... controls` (must stay green).
Stage `.planning/osf_deviations.md` (+ the verify script if refined) by explicit path and commit:
`docs(260814-u9p): RECHARACTERIZED — the posted trsx5 body is an UNEXPLAINED THIRD BODY (both lineages falsified); STOP verdict unchanged`
Do **not** push. GPFS object error → STOP and report.
  </action>

  <verify>
    <automated>bash .planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-verify.sh ledger && git diff --numstat 50dc51d -- .planning/osf_deviations.md</automated>
  </verify>

  <done>
    `ledger` mode all-PASS (L1-L8), `git diff --numstat 50dc51d -- .planning/osf_deviations.md`
    shows **0 deletions**, the ADJUDICATED block is byte-unchanged, `controls` still green, one
    commit, no push.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bank the courier-in verbatim and write the reply to Seth</name>
  <files>
    .planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-SETH-REPLY-VERBATIM.md
    .planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-REPLY-TO-SETH.md (new)
    .planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-verify.sh (reply checks, if refinement needed)
  </files>

  <action>
**3a — commit the courier-in AS-IS.** `260814-u9p-SETH-REPLY-VERBATIM.md` was written by the
orchestrator and is **not to be edited by one character** — not the prose, not the two X1
occurrences, not the ⚠ note. Planner-measured at 2026-08-14 ~21:50 EDT, before any staging:
**5,763 bytes / 62 lines / md5 `47a017bf8753b147f498dea97cc64338`**. Procedure: `md5sum` it
**before** `git add`; if it does not equal that value, **STOP and report** (someone edited it — do
not "fix" it either way). After committing, re-derive from the object store
(`git show HEAD:<path> | md5sum`) and require the **same** value — that is the byte-exact proof.
`git show --numstat HEAD` for this path must show **0 deletions** and the path added (`A`).

⚠ The verbatim file contains two **24-char** hex runs (X1). That is **correct and must stay** — it
is Seth's own display truncation, faithfully banked. **No hex-length invariant may be run over this
file, and nobody may "repair" those runs.** State this in the file's commit message and in the
verify script's comment so a future reader does not treat it as a defect.

**3b — write `260814-u9p-REPLY-TO-SETH.md`** (≥ 60 lines), addressed to Seth, in the register of the
tgf addendum. Open by conceding the substantive point: **he falsified our reading, against his own
side, and that is the correct honest direction**; we adopt his framing shift — **"unexplained third
body"** from here, and we retire "the truncated post" as a description. Then, sectioned (a)-(e):

**(a) Ask #2 — your courier NEVER ARRIVED.** State F-5 plainly and firsthand: we grepped
`.planning/` for **both** the filename `seth_courier_9907_body_for_hpc.md` **and** the sha256 prefix
`40831cdebcc71de21cd536fa…` — **zero hits for either**; **H2** appears in our records only ever as a
*reference value*, never as a body. So we still **cannot** compute the 149-byte diff, and we will
not promise one we cannot produce. **Please re-send**, base64, with the **full 64-character sha256
travelling with the body**. ⚠ Include the explicit warning: the anchor as it reached us is
**display-truncated** — `40831cdebcc71de21cd536fa…` — and is therefore **not usable for
verification**; we will not pad it, complete it, or treat it as an anchor. Give the size-first
arrival recipe: **9,907 first, hashes only to confirm which known body it is.**

**(b) The hex-run invariant is WIDENED — and paid for.** Accepts **{32, 64}** now. List the
negative controls **as run**: 31-char RED, 63-char RED, 32-char GREEN, 64-char GREEN, exercised
through the shipped `_hexlen` sub-mode (not a re-implementation), with the pre-edit 64-char RED
captured first so the green is evidence. The fire gate re-ran **10/10**. Adopt his rule **verbatim
in substance**: truncating a sha256 to 32 characters to satisfy the invariant is **forbidden** — it
manufactures the exact silent-mismatch class the invariant exists to catch.

**(c) Sequencing — read the posted body first, ADOPTED as the standing recommendation.** Per F-7,
labelled a **recommendation, not a decision** (Carter has not formally decided). Carter ships the
**9,695-B download in BOTH directions**: base64 to you, and into the repo so we replicate
independently (**size-first on arrival: 9,695 / H1**). **Both sides characterize independently
before any adjudication or re-post** — no shared intermediate, so the two characterizations are a
real cross-check rather than one opinion echoed twice. Then adjudicate the true complete body →
re-post as a **NEW OSF VERSION**.

**(d) What is held.** Fire **HELD**; obligation-(2) posting **HELD** by the same gate; the STOP
verdict is unchanged (it adjudicated on size alone and this reply does not touch it);
**new OSF version, never a silent swap** — reaffirmed without qualification; `$0`, zero perimeter
contact, nothing fired, no agent contacted OSF. Note that the ledger now carries the dated
**RECHARACTERIZED** sub-entry (pure append; the falsified reading preserved above it, unaltered).

**(e) Closing.** Our tgf reading is on the record as **FALSIFIED**, dated, not deleted. Say the
212-byte delta is **unexplained, not mislocated** — and that we are not going to offer a mechanism
we cannot support either.

Transcription rules for this document: every md5 written must be exactly 32 characters and copied
from the HASH TABLE; **no 64-char hex run may appear at all** (we hold no legitimate sha256 to
transcribe here); X1 may appear **only** with its `…` and an adjacent truncation warning.

**3c — implement/confirm the `reply` mode** of `260814-u9p-verify.sh`:
- **V1 (byte-exact courier-in):** `git show HEAD:<verbatim path> | md5sum` equals
  `47a017bf8753b147f498dea97cc64338`, and the worktree copy equals it too
  (`git diff --stat -- <path>` empty).
- **V2:** the verbatim file still contains `--- VERBATIM BODY BEGINS ---` and
  `--- VERBATIM BODY ENDS ---`. **No hexlen check is run over this file** (comment must say why).
- **V3:** `260814-u9p-REPLY-TO-SETH.md` exists and is ≥ 60 lines.
- **V4 (coverage a-e):** contains `NEVER ARRIVED`, `seth_courier_9907_body_for_hpc.md`, `re-send`
  (case-insensitive), `64-char` (or `64-character`), `WIDENED` (case-insensitive), `{32, 64}`,
  `unexplained third body` (case-insensitive), `HELD`, `new OSF version` (case-insensitive),
  `size-first` (case-insensitive), `9,695`, `9,907`, `149`, `212`, `FALSIFIED` (case-insensitive),
  `RECOMMENDATION` (case-insensitive), and the four control lengths `31`, `63`, `32`, `64`.
- **V5 (hex invariant with a narrow, labelled exemption):** every hex run ≥20 chars in the reply is
  32 or 64, **except** the literal `40831cdebcc71de21cd536fa`, exempt **only** on lines that also
  carry the ellipsis `…` **and** a truncation warning token (`TRUNCATED`/`truncated`); count check —
  the number of lines carrying that literal must equal the number that also carry both markers.
- **V6 (scoped strictness):** the count of **64-char** hex runs in the reply is **0** (nothing
  legitimate to transcribe), and the count of 32-char runs is > 0.
- **V7:** all five literals H1-H5 appear in full in the reply.
- **V8:** the 31-char defect literal H6 appears **0 times** in the reply (unlike the guk reply, this
  document has no reason to quote it).

Gate on `bash 260814-u9p-verify.sh all` (controls + ledger + reply, all-PASS) and one final
`bash .../260814-guk-verify.sh fire` → **10/10**. Stage by explicit paths and commit:
`docs(260814-u9p): courier-in Seth's prefix-test reply (verbatim) + reply — 9,907 courier never arrived, re-send; invariant widened to {32,64}; read-the-posted-body-first`
Do **not** push (the orchestrator pushes). GPFS object error → STOP and report.
  </action>

  <verify>
    <automated>bash .planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-verify.sh all && bash .planning/quick/260814-guk-seth-update-2-reply-remediation-fix-inva/260814-guk-verify.sh fire</automated>
  </verify>

  <done>
    The verbatim courier-in is committed byte-exact (md5 `47a017bf8753b147f498dea97cc64338` before
    staging and from the object store after, 0 deletions, added as a new path); the reply exists,
    covers (a)-(e), carries no bare or padded sha256 and no 64-char run at all; `all` is all-PASS;
    the guk `fire` gate is 10/10; one commit, no push.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seth (couriered chat) → this repo | Untrusted-by-construction text with no repo/perimeter access on his side; his measurements are unreproduced by us and must be recorded **as his**, never restated as ours |
| This repo → the public record (OSF ledger) | `.planning/osf_deviations.md` is reviewer-facing; a wrong or deleted line here is a disclosure defect |
| Plan text → committed hashes | Every hash is transcribed from the HASH TABLE; re-derivation or retyping is the known failure mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-u9p-01 | Tampering | `.planning/osf_deviations.md` | mitigate | L1 asserts **0 deleted lines** vs `50dc51d`; L7 asserts the falsified reading survives verbatim; append-only enforced by a check, not by intent |
| T-u9p-02 | Information disclosure | X1 truncated sha256 | mitigate | V5 permits the literal only with `…` + a truncation warning; V6 forbids **any** 64-char run in the reply, so a padded/invented anchor cannot slip in; L8 keeps X1 out of the public ledger entirely |
| T-u9p-03 | Spoofing | courier-in provenance | mitigate | V1 pins the verbatim file's md5 before staging **and** from the object store after commit; Seth's measurements are attributed to him and marked unreproduced |
| T-u9p-04 | Denial of service (the fire gate) | `260814-guk-verify.sh` | mitigate | Widening is the minimum edit; `fire` 10/10 required after; any regression = STOP, no improvisation |
| T-u9p-05 | Elevation of privilege | irreversible spend / OSF write | mitigate | No network, no OSF, no perimeter, no push; fire + obligation-(2) remain HELD; an agent never fires |
| T-u9p-06 | Repudiation | "green" invariant with no failure evidence | mitigate | Pre-edit 64-char RED captured; 31/63 RED post-edit; `ledger`/`reply` observed RED before GREEN |
</threat_model>

<verification>
1. `bash .planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-verify.sh all` → exit 0, every check PASS.
2. `bash .planning/quick/260814-guk-seth-update-2-reply-remediation-fix-inva/260814-guk-verify.sh fire` → **10/10 ALL CHECKS PASSED**.
3. `git diff --numstat 50dc51d -- .planning/osf_deviations.md` → deletions column is **0**.
4. `git log --oneline -3` → exactly three new commits, one per task; `git status --porcelain` clean
   for the five listed paths; **no temp/control files committed**; **origin not pushed**.
5. `git status --porcelain .planning/STATE.md .planning/ROADMAP.md .planning/quick/260812-ox1-m3-04c-task-3-fire-prep-pre-fire-1-per-r/`
   → **no modifications from this task** (STATE/ROADMAP are the orchestrator's; the §6b cards are untouchable).
</verification>

<success_criteria>
- The hex-run invariant accepts {32, 64} and has been **seen to reject** 31 and 63 and to reject 64
  before the change; the fire gate is unchanged at 10/10.
- The ledger says **unexplained third body**, states the STOP verdict is unchanged, records the
  falsification of our own reading with the original wording preserved, and deleted nothing.
- Seth's reply is banked byte-exact; our reply asks for the re-send with a full 64-char anchor,
  adopts his no-truncation rule, and adopts read-the-posted-body-first as a labelled recommendation.
- Fire and obligation-(2) still HELD. `$0`. Zero perimeter contact. Nothing pushed.
</success_criteria>

<output>
After completion, create
`.planning/quick/260814-u9p-bank-seth-prefix-test-reply-third-body-e/260814-u9p-SUMMARY.md`
including the **pre-edit 64-char RED transcript**, the four post-edit control results, the
`fire` 10/10 transcript, the `git diff --numstat 50dc51d` deletion count, and the three commit SHAs.
</output>
