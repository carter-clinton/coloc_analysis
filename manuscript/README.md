# manuscript/

The actual paper.

| Planned subdir / file | Purpose |
|---|---|
| `draft/` | Main text (`main.tex` / `main.qmd`), bibliography, class files |
| `supplementary/` | Supplementary text, figures, tables — regenerated from `src/snakemake/` outputs + `figures/` |
| `tables/` | Tables 1, 2, 3 + supplementary tables. Table 2 specifically replaces the broken cross-ancestry concordance (see Revision_Plan Phase 4). |
| `response_to_reviewers/` | Pre-submission response-to-reviewers template forces rigor even without prior reviews |
| `cover_letter/` | Cover letter versioned per target journal (Nat Genet, AJHG, Nat Metab, Cell Genomics, Genome Medicine) |

## Current draft

A prior v10 draft (`ajhg_manu_v10.pdf`) is referenced in the Revision_Plan but
is **not** committed in this repo — it remains on /rs1 or elsewhere in Carter's
files. The revision work rewrites the manuscript from the new analytical
outputs, not from v10.
